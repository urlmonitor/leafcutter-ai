id: BO-1800c-1-i
readiness: draft
priority: medium
title: "An inherently sequential task is not force-parallelized"
component: build-orchestration
level: L3
status: active
req_status: draft
work_status: todo
roadmap_phase: phase_1
created_by: "docs/acceptance-criteria/build-orchestration/BO-1800-isolated-parallel-delivery/BO-1800c.yaml"
criteria: |
  Given a task whose steps are inherently sequential (each step depends on the
    previous one),
  When work on that task is scheduled,
  Then it is not split across multiple agents to force artificial parallelism,
  And it runs with a single collaborator (or the minimum the dependencies allow)
    rather than being padded up toward the per-feature cap just because the cap
    permits it.
depends_on: [BO-1800c-1]
doc_links:
  - path: docs/architecture/adrs/ADR-018-agent-isolation-topology.md
    relationship: describes
    status: exists
assigned_agent: python-coder
estimated_complexity: S
it_requirements: |
  ADR-018 Decision §3 / Alternatives: the per-feature cap is an UPPER bound, not a
  target; sequential work must not be force-parallelized (past a small agent count
  returns diminish and error amplifies). IT-PO: this is a scheduling/dispatch
  judgment; confirm whether it is a supervisor heuristic or an explicit dependency
  check.
delivers_to: null
expects_from:
  ac_id: BO-1800c-1
  contract: "The per-feature agent cap whose 'upper bound not target' semantics this case exercises."
origin_agent: business-analyst
created: 2026-07-06
amended_by: []
superseded_by: null
covered_by: []
implemented_by: []
