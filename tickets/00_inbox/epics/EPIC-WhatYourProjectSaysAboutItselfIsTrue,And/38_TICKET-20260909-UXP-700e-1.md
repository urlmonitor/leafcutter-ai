---
ac_traceability:
  id: UXP-700e-1
  path: docs/acceptance-criteria/ux-prototyping/UXP-700-truthful-project-record/UXP-700e-1.yaml
advances_current_outcome: true
agents:
  ac-fulfillment-gate: needed
  ac-validator: needed
  architect-review: needed
  commit: needed
  documentation-expert: needed
  documentation-verifier: needed
  frontend-coder: not_needed
  pr-reviewer: not_needed
  pull-request: not_needed
  python-coder: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
  user-surface-smoker: not_needed
change_target:
- code
- schema
complexity: medium
components:
- ux_prototyping
created: '2026-09-09'
depends_on: []
documentation_required: true
files_touched:
- docs/product-truth/README.md
- docs/product-truth/schemas/flow.schema.json
priority: medium
requires_adr: false
requires_diagram: false
risk_surface: internal
roadmap_phase: phase_1
source_ac: UXP-700e-1
status: todo
title: The record declares a reviewable size for each thing it holds, and reports
  what exceeds it
---

# The record declares a reviewable size for each thing it holds, and reports what exceeds it

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `UXP-700e-1` — The record declares a reviewable size for each thing it holds, and reports what exceeds it — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `UXP-700e-1`. Component: `ux-prototyping`. Assigned agent: `python-coder`. Estimated complexity: `M`. Complexity: `medium`.

## Acceptance Criteria

```gherkin
Given the shapes the record allows for a journey, an example dataset and a screen,
When a bound is declared for one of their fields - a longest permitted description, a largest
  permitted number of steps,
Then an artifact exceeding a bound is reported naming the artifact, the field, the measured
  size and the bound,
And an artifact within every bound is not reported,
And the report states, for each bound, how many artifacts were measured against it, so a
  bound that measured nothing is distinguishable from a bound nothing exceeded.
```

- [ ] AC-1: an artifact exceeding a bound is reported naming the artifact, the field, the measured
- [ ] AC-2: an artifact within every bound is not reported
- [ ] AC-3: the report states, for each bound, how many artifacts were measured against it, so a

## Test Requirements

```yaml
tests:
- name: test_artifact_exceeding_a_bound_is_reported_with_field_size_and_bound
  file: unit_tests/product_truth/test_uxp_700e_1.py
  covers:
  - UXP-700e-1
  asserts: A journey whose description exceeds the declared bound is reported naming
    the journey, the field, its measured length and the limit.
  framework: unittest
  type: integration
  angle: criterion
- name: test_artifact_within_every_bound_is_not_reported
  file: unit_tests/product_truth/test_uxp_700e_1.py
  covers:
  - UXP-700e-1
  asserts: A journey inside all bounds produces no size finding.
  framework: unittest
  type: integration
  angle: boundary
- name: test_each_bound_states_how_many_artifacts_it_measured
  file: unit_tests/product_truth/test_uxp_700e_1.py
  covers:
  - UXP-700e-1
  asserts: The report carries a measured count per bound, and it rises by one when
    an artifact is added.
  framework: unittest
  type: integration
  angle: seam
- name: test_uxp_700e_1_reachable_from_entry_point
  file: unit_tests/product_truth/test_uxp_700e_1.py
  covers:
  - UXP-700e-1
  angle: reachability
  asserts: 'REQUIRED — invoke the production entry point (CLI, hook, slash command,
    workflow dispatch, or main()) as a subprocess/dispatch and assert the new behaviour
    actually occurs. Do NOT satisfy this by importing the function directly. The AC
    authored no test_spec, so the entry point is not declared: resolve it before writing
    this test, and do not delete this entry.'
```

## Agent Contracts

### documentation-expert

Existing docs to update / cross-link:

- docs/product-truth/schemas/flow.schema.json (relationship: specifies | status: exists | relevance: The shape that gains the bounds. Today summary has no maxLength and steps has no maxItems.)
- docs/product-truth/README.md (relationship: specifies | status: exists)

- [ ] AC-1: reference-doc | docs/product-truth/schemas/flow.schema.json | an artifact exceeding a bound is reported naming the artifact, the field, the measured

### Delivers To

- **Agent:** python-coder
- **Contract:** A per-bound measurement report the transition rules in UXP-700e-1-i and UXP-700e-1-ii gate on.

### Expects From

- **AC:** UXP-700b-2
- **Contract:** The per-check measured-population convention these bound counts follow.

## Sign-offs

- [ ] architect-review
- [ ] test-writer
- [ ] python-coder
- [ ] test-runner
- [ ] documentation-expert
- [ ] ac-validator
- [ ] ac-fulfillment-gate
- [ ] documentation-verifier
- [ ] commit

## Comments
