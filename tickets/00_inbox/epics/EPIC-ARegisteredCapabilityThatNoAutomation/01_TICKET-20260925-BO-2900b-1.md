---
ac_traceability:
  id: BO-2900b-1
  path: docs/acceptance-criteria/build-orchestration/BO-2900-runtime-reachability-guard/BO-2900b-1.yaml
advances_current_outcome: true
agents:
  ac-fulfillment-gate: needed
  ac-validator: needed
  architect-review: needed
  commit: needed
  documentation-expert: needed
  documentation-verifier: needed
  frontend-coder: not_needed
  pr-reviewer: needed
  pull-request: not_needed
  python-coder: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
  user-surface-smoker: not_needed
change_target: pipeline
complexity: high
components:
- build_orchestration
- build_pipeline
created: '2026-09-25'
depends_on: []
documentation_required: true
files_touched:
- scripts/commit_guardian/commit_guardian.json
priority: medium
requires_adr: false
requires_diagram: false
risk_surface: contract_boundary
roadmap_phase: phase_1
source_ac: BO-2900b-1
status: todo
title: A registered capability that no automation runs is reported and refuses the
  change
---

# A registered capability that no automation runs is reported and refuses the change

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BO-2900b-1` — A registered capability that no automation runs is reported and refuses the change — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BO-2900b-1`. Component: `build-orchestration`. Assigned agent: `python-coder`. Estimated complexity: `L`. Complexity: `high`.

## Acceptance Criteria

```gherkin
Given the product's operator-facing command surface registers four actions —
  `claim`, `release`, `mark-done` and `report`,
  And the automation that drives builds invokes `claim`, `release` and `mark-done`,
  And no automation invokes `report`,
When the connection from registered capabilities to the automation that runs them
  is checked,
Then `report` is reported as a registered capability that nothing runs, naming the
  capability and the command surface it is registered on,
  And `claim`, `release` and `mark-done` are not reported,
  And the change is refused rather than passing with an advisory note,
  And the set of capabilities checked is the set the command surface actually
    registers when it is built — obtained by asking the built surface what it
    accepts — so a capability that appears in the source but is never registered is
    not checked, and a capability registered only under a condition that is false
    when the surface is built is not checked.
```

- [ ] AC-1: the automation that drives builds invokes `claim`, `release` and `mark-done`
- [ ] AC-2: no automation invokes `report`
- [ ] AC-3: `report` is reported as a registered capability that nothing runs, naming the
- [ ] AC-4: `claim`, `release` and `mark-done` are not reported
- [ ] AC-5: the change is refused rather than passing with an advisory note
- [ ] AC-6: the set of capabilities checked is the set the command surface actually

## Test Requirements

```yaml
tests:
- name: test_uncalled_registered_capability_is_reported_and_the_check_exits_nonzero
  file: unit_tests/commit_guardian/test_bo_2900b_1.py
  covers:
  - BO-2900b-1
  asserts: Invokes check_reachability.main(['--mode','precommit',...]) as the real
    entry point over a fixture surface registering claim/release/mark-done/report
    with automation invoking only the first three; asserts a non-zero return code
    and that the emitted output names `report` and the surface it is registered on.
  framework: unittest
  type: behavioral
- name: test_called_capabilities_are_not_reported_in_the_same_run
  file: unit_tests/commit_guardian/test_bo_2900b_1.py
  covers:
  - BO-2900b-1
  asserts: 'Same run as above: asserts claim, release and mark-done appear nowhere
    in the findings, so a report-everything implementation fails alongside the positive
    assertion.'
  framework: unittest
  type: behavioral
- name: test_inventory_comes_from_the_built_parser_not_the_source_text
  file: unit_tests/commit_guardian/test_bo_2900b_1.py
  covers:
  - BO-2900b-1
  asserts: 'A fixture surface whose source mentions an `add_parser(''ghost'')` call
    inside a branch that is false at build time, and which registers a capability
    through a loop over a table, is inventoried by running the check: `ghost` is absent
    from the findings and the loop-registered capability is present.'
  framework: unittest
  type: behavioral
- name: test_failure_output_does_not_name_a_skip_or_bypass_flag
  file: unit_tests/commit_guardian/test_bo_2900b_1.py
  covers:
  - BO-2900b-1
  asserts: Asserts the text emitted on the refusing run contains neither 'SKIP=' nor
    '--no-verify', so the blocking gate never documents its own off switch.
  framework: unittest
  type: behavioral
- name: test_bo_2900b_1_reachable_from_entry_point
  file: unit_tests/commit_guardian/test_bo_2900b_1.py
  covers:
  - BO-2900b-1
  angle: reachability
  asserts: 'REQUIRED — invoke the production entry point (CLI, hook, slash command,
    workflow dispatch, or main()) as a subprocess/dispatch and assert the new behaviour
    actually occurs. Do NOT satisfy this by importing the function directly. The AC
    authored no test_spec, so the entry point is not declared: resolve it before writing
    this test, and do not delete this entry.'
```

## Implementation Notes

```yaml
config_schema_fragment:
  uncalled_capability_finding:
    description: '{ kind: ''uncalled_capability'', capability: str, surface: ''<module
      path>'', ways_forward: [''add the automation invocation in this change'', ''record
      an exemption for the capability with a stated reason''] }. Emitted by the forward
      direction of the reachability check; grouped separately from stale-caller findings
      per BO-2900c-2.'
    type: object
constraints:
- 'ENFORCEMENT SURFACE: a new commit-guardian hook `check-reachability`, implemented
  as scripts/commit_guardian/check_reachability.py with two modes (`--mode precommit`,
  `--mode ci`), registered in the hooks_manifest of scripts/commit_guardian/commit_guardian.json
  AND its template twin templates/scripts/commit_guardian/commit_guardian.json (n_location_rule
  ''all'' — the check-hook-parity hook enforces this), plus a CI job in .github/workflows/ci.yml
  running `--mode ci`. This mirrors the proven check_done_proof.py two-layer precedent;
  do not invent a third gate convention.'
- Create the shared seam scripts/commit_guardian/_reachability_inventory.py exposing
  registered_capabilities(surface_module) -> set[str] and collected_invocations(script_paths)
  -> list[Invocation]. BO-2900c consumes the SAME module for the reverse direction
  — one seam, two verdicts. Do not fork it.
- 'registered_capabilities MUST be obtained from the BUILT surface: import the surface
  module, call its own parser builder (fast_lane._build_cli_parser(), scripts/build_orchestration/fast_lane.py:775)
  and read the argparse _SubParsersAction choices keys. Never regex `add_parser(`
  out of the source, and never top up the built inventory with source-scanned extras
  — that reintroduces exactly the false alarms the conditional-registration clause
  forbids.'
- 'REFUSES, NOT WARNS: a finding must produce a non-zero exit from the hook and the
  CI job. Do not add a global advisory/downgrade flag. The only sanctioned relief
  is a recorded exemption per BO-2900d-1. The failure output must not print SKIP=check-reachability
  or --no-verify (BO-2900e-1 final clause, BO-2900b-1-i).'
- Every new module (check_reachability.py, _reachability_inventory.py) and config/reachability_exemptions.yaml
  MUST be added to the build deploy manifest in scripts/build_phases.py in the same
  change. The hook runs from the DEPLOYED layout; an undeployed dependency crashes
  with ModuleNotFoundError once the gate is required — see CLAUDE.md 'New Hook / Gate
  Dependencies Must Be in the Build Deploy-Manifest'.
- The set of surfaces to inventory is derived, not hardcoded (see BO-2900c-4); at
  minimum it must include scripts/build_orchestration/fast_lane.py. Importing a surface
  module must not execute its main() — build the parser only.
n_location_rule: all
post_write_commands:
- ./build-self.sh
reference_file_path: scripts/commit_guardian/commit_guardian.json
required_skills:
- python-coder
```

---
Reference material (read-only, do not copy blindly): a prototype static caller finder lives at the absolute path C:/Users/Hendrik/Code/leafcutter/leafcutter-ai/debugging/test_judge/callers.py (untracked, NOT on origin/main — read it from that absolute path; a worktree will not contain it). It counts only AST Call nodes as callers (docstring/log/text mentions do not count — the BO-2900b-3 rule), excludes tests/docs/tickets, and resolves call sites with `git grep` then AST confirmation. On 38 labelled leafcutter incidents it flagged 4/17 vacuous tests with 0/21 false alarms, including the stranded BO-2400f lifecycle functions (claim_build_set, release_claim). Its first version counted a docstring and log strings as callers — exactly the failure BO-2900b-3 warns about. Evidence: C:/Users/Hendrik/Code/leafcutter/leafcutter-ai/docs/analysis/2026-09-25-jev-test-triage-leafcutter-replication.md, section "Development pass". Limits: one hop, Python direct calls only; JS/YAML/CI invocations need their own handling.
---

## Agent Contracts

### documentation-expert

Existing docs to update / cross-link:

- scripts/build_orchestration/fast_lane.py (relationship: describes | status: exists | relevance: The concrete command surface this check is calibrated against — its registered actions are the forward inventory.)
- templates/workflows-js/fast-lane-ship.js (relationship: describes | status: exists | relevance: The concrete automation expected to invoke those actions — the caller side of the connection.)
- docs/reference/workflow-authoring-contract.md (relationship: related | status: exists | relevance: The existing contract for what an automation script may do; the caller side of this check must be consistent with it.)
- docs/architecture/components/build-orchestration.md (relationship: describes | status: exists | relevance: The component page that owns the command surface and the automation that drives it; the new gate belongs in its picture.)
- docs/architecture/adrs/ADR-001-self-hosting-boundary.md (relationship: describes | status: exists | relevance: Governs the scripts/ <-> templates/scripts/ parity this hook registration must respect on both sides.)

- [ ] AC-1: how-to | scripts/build_orchestration/fast_lane.py | `report` is reported as a registered capability that nothing runs, naming the
- [ ] AC-2: component-diagram | scripts/build_orchestration/fast_lane.py | `report` is reported as a registered capability that nothing runs, naming the

### Delivers To

- **Agent:** python-coder
- **Contract:** registered_capabilities(surface) -> set[str] taken from the built argparse subparser choices, plus list[uncalled_capability_finding] as specified in config_schema_fragment. The capability inventory is consumed by BO-2900c-1 and BO-2900c-2 for the reverse comparison; the findings are consumed by BO-2900e-1 (message) and BO-2900e-2 (single-run completeness).

### Expects From

- **AC:** BO-2900b-3
- **Contract:** collected_invocations(script_paths) -> list[{ script: str, line: int, surface: str, capability: str }], collected from executed invocations only. A registered capability is 'run by automation' iff its name appears as the capability of at least one collected Invocation.

## Sign-offs

- [ ] architect-review
- [ ] test-writer
- [ ] python-coder
- [ ] test-runner
- [ ] documentation-expert
- [ ] pr-reviewer
- [ ] ac-validator
- [ ] ac-fulfillment-gate
- [ ] documentation-verifier
- [ ] commit

## Comments
