---
ac_traceability:
  id: UXP-700c-2-i
  path: docs/acceptance-criteria/ux-prototyping/UXP-700-truthful-project-record/UXP-700c-2-i.yaml
advances_current_outcome: true
agents:
  ac-fulfillment-gate: needed
  ac-validator: needed
  architect-review: signed_off
  commit: needed
  documentation-expert: needed
  documentation-verifier: needed
  frontend-coder: not_needed
  pr-reviewer: needed
  pull-request: not_needed
  python-coder: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: failed
  user-surface-smoker: not_needed
change_target: code
complexity: low
components:
- ux_prototyping
created: '2026-09-09'
depends_on: []
documentation_required: true
files_touched: []
priority: medium
requires_adr: false
requires_diagram: false
risk_surface: contract_boundary
roadmap_phase: phase_1
source_ac: UXP-700c-2-i
status: todo
title: A journey never confirmed is reported as never confirmed, not as current
---

# A journey never confirmed is reported as never confirmed, not as current

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `UXP-700c-2-i` — A journey never confirmed is reported as never confirmed, not as current — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `UXP-700c-2-i`. Component: `ux-prototyping`. Assigned agent: `python-coder`. Estimated complexity: `S`. Complexity: `low`.

## Acceptance Criteria

```gherkin
Given a newly authored journey carrying no record of ever having been confirmed against what
  it describes,
When the record is checked,
Then it is reported as never confirmed, naming the journey,
And it is not reported as up to date,
And it is not reported as behind, because there is no earlier confirmation for it to be
  behind,
And the count of journeys compared does not include it.
```

- [ ] AC-1: it is reported as never confirmed, naming the journey
- [ ] AC-2: it is not reported as up to date
- [ ] AC-3: it is not reported as behind, because there is no earlier confirmation for it to be
- [ ] AC-4: the count of journeys compared does not include it.

## Test Requirements

```yaml
tests:
- name: test_unconfirmed_journey_is_reported_as_never_confirmed
  file: unit_tests/product_truth/test_uxp_700c_2_i.py
  covers:
  - UXP-700c-2-i
  asserts: A journey with no confirmation record is reported as never confirmed, by
    name.
  framework: unittest
  type: integration
  angle: boundary
- name: test_unconfirmed_journey_is_neither_current_nor_behind
  file: unit_tests/product_truth/test_uxp_700c_2_i.py
  covers:
  - UXP-700c-2-i
  asserts: The never-confirmed journey appears in neither the current set nor the
    behind set.
  framework: unittest
  type: integration
  angle: boundary
- name: test_uxp_700c_2_i_reachable_from_entry_point
  file: unit_tests/product_truth/test_uxp_700c_2_i.py
  covers:
  - UXP-700c-2-i
  angle: reachability
  asserts: 'REQUIRED — invoke the production entry point (CLI, hook, slash command,
    workflow dispatch, or main()) as a subprocess/dispatch and assert the new behaviour
    actually occurs. Do NOT satisfy this by importing the function directly. The AC
    authored no test_spec, so the entry point is not declared: resolve it before writing
    this test, and do not delete this entry.'
```

## Agent Contracts

### documentation-expert

Existing docs to update / cross-link:

- docs/acceptance-criteria/guardrail-engine/GE-120-green-means-checked/GE-120.yaml (relationship: context | status: exists)

- [ ] AC-1: (unspecified genre) | docs/acceptance-criteria/guardrail-engine/GE-120-green-means-checked/GE-120.yaml | it is reported as never confirmed, naming the journey

### Expects From

- **AC:** UXP-700c-2
- **Contract:** The freshness verdict this record adds the never-confirmed value to.

## Sign-offs

- [x] architect-review — 2026-09-09 17:17
- [ ] test-writer — failed 2026-09-09 17:23
- [ ] python-coder
- [ ] test-runner
- [ ] documentation-expert
- [ ] pr-reviewer
- [ ] ac-validator
- [ ] ac-fulfillment-gate
- [ ] documentation-verifier
- [ ] commit

## Comments

### 2026-09-09 17:17 — architect-review (status: ok)
feedback-id: fb_2026-09-09_3ff12053
completion_manifest:
  blast_radius_reviewed: true
  rubric_applied: true
  requires_adr_confirmed: true
Classified SMALL. This ticket adds a `never_confirmed` third value to the freshness
verdict that UXP-700c-2 (ticket 21, still `todo`, no `_check_freshness`-style helper
exists yet in docs/product-truth/scripts/validate_product_truth.py) is expected to
introduce; components: 1 (ux_prototyping), estimated file count ≤5 (extend the same
`_check_*` helper pattern `_check_pointers` already established for UXP-700c-1, plus
`unit_tests/product_truth/test_uxp_700c_2_i.py`), no cross-module boundary crossed, and
no always-large trigger fires (no alembic migration, no hypertable change, no FastAPI
surface change, no ADR-contract change). `requires_adr: false` confirmed as already set
correctly in frontmatter. Flagged as an acceptance adjustment: ticket frontmatter
`depends_on: []` does not name ticket 21 (UXP-700c-2), even though this AC's
`expects_from` explicitly names UXP-700c-2's freshness verdict as its precondition and
that verdict function does not exist in the codebase yet — python-coder/test-writer
should sequence after ticket 21 lands, and ticket-supervisor should treat this as an
execution-order dependency even though the frontmatter field is empty. No architectural
concerns beyond naming: recommend the third verdict value use the exact literal
`never_confirmed` (snake_case, matching existing `impl_status` vocabulary conventions
in the same store) rather than a spaced phrase, for machine-parseability by UXP-700c-3's
gate.

### 2026-09-09 17:23 — test-writer (status: blocker)
feedback-id: fb_2026-09-09_1cdde7d0
completion_manifest:
  precondition_verified:
    result: false
    reason: "Ticket 21 (UXP-700c-2, tickets/00_inbox/epics/EPIC-TruthfulProjectRecord/21_TICKET-20260909-UXP-700c-2.md) is status: todo with every Sign-offs checkbox unchecked, including architect-review. I verified against the actual code in the worktree named by worktree_path (EPIC-TruthfulProjectRecord, branch EPIC-TruthfulProjectRecord): docs/product-truth/scripts/validate_product_truth.py defines _check_flow, _check_impl_status, _check_truth_evidence, _check_expands, _check_product_truth, _check_derived_indexes, _check_screens, _check_declared_invariants, _check_mock_invariants, _check_pointers, _check_index, _check_eval — no _check_freshness (or any freshness/behind/current/never_confirmed verdict helper) exists anywhere in that file or elsewhere under docs/product-truth/scripts/. This AC's own expects_from names UXP-700c-2's freshness verdict as its precondition (docs/acceptance-criteria/.../UXP-700c-2-i.yaml: depends_on: [UXP-700c-2]), and architect-review's own comment above already flagged this exact gap and recommended sequencing test-writer/python-coder after ticket 21 lands."
    remediation: "Dispatch ticket 21 (UXP-700c-2) through architect-review, test-writer, and python-coder first, so the base current/behind freshness-verdict function (and its entry point) actually exists. Re-dispatch this ticket's test-writer only after that lands, so the never_confirmed test target can be resolved against the real function shape rather than guessed."
  tests_written: false
  cross_layer_seam_answer:
    result: not_applicable
    reason: "No tests were written — the phase halted before any test authoring because the function under test does not exist in any form (not even the two-value current/behind base this AC extends), so there is no producer or consumer to pin a seam test to yet."
  reachability_entry_point_answer:
    result: not_found
    reason: "Test Requirements' own reachability entry names no entry point (correctly flagged as undeclared by the requirement itself). Step 1 of the resolution procedure could not be completed: the unit this AC extends (a freshness-verdict check analogous to _check_pointers) has zero implementation anywhere in docs/product-truth/scripts/validate_product_truth.py or its CLI/hook wrapper, so there is no real invocation surface to check against — inventing one would mean designing ticket 21's contract from inside ticket 22, which is architect-review's job, not test-writer's."
    remediation: "Resolve after ticket 21 (UXP-700c-2) lands; its python-coder sign-off will fix the real function name, module, and (if any) CLI/hook entry point that this ticket's reachability test must invoke."
Blocked before writing any test: ticket 21 (UXP-700c-2), the sole precondition this AC's
`expects_from` names, is still `todo` with zero sign-offs, and the freshness-verdict
function (current/behind) it is meant to introduce does not exist anywhere in
`docs/product-truth/scripts/validate_product_truth.py` in the EPIC-TruthfulProjectRecord
worktree — confirmed by direct grep against the real file, not inferred from ticket
status alone. Writing tests now would require inventing the entire base verdict
contract (module, function name, return shape, entry point) that ticket 21 is
responsible for designing, which violates the Architectural Context Enforcement rule
against guessing undesigned architecture. Per architect-review's own comment above,
recommend ticket-supervisor dispatch ticket 21 through test-writer/python-coder before
re-dispatching test-writer on this ticket. `agents.test-writer` set to `failed` (not
`needed`) so this halt is visible without requiring a re-read of this comment; no
production or test files were touched.
