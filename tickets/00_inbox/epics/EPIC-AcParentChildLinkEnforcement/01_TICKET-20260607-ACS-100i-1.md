---
advances_current_outcome: true
agents:
  commit: signed_off
  documentation-expert: not_needed
  pr-reviewer: signed_off
  pull-request: needed
  python-coder: signed_off
  sql-coder: not_needed
  test-runner: signed_off
  test-writer: signed_off
components:
- ac-store
created: '2026-06-07'
depends_on:
- ACS-100i
files_touched:
- docs/reference/ac-schema.md
- docs/architecture/adrs/ADR-007-ac-store-schema-id-format-enforcement.md
priority: medium
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: ACS-100i-1
status: in_progress
title: Parent ID is derived from child ID by stripping the last segment
---

# Parent ID is derived from child ID by stripping the last segment

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `ACS-100i-1` — Parent ID is derived from child ID by stripping the last segment — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `ACS-100i-1`. Component: `ac-store`. Assigned agent: `python-coder`. Estimated complexity: `S`.

## Acceptance Criteria

```gherkin
Given an AC YAML file with id "ACS-300h-1",
When the validator parses the ID to determine its parent,
Then it derives the parent ID as "ACS-300h"
  by stripping the last hyphen-delimited segment ("-1").

And given an AC YAML file with id "ACS-300h-2-i",
When the validator parses the ID to determine its parent,
Then it derives the parent ID as "ACS-300h-2"
  by stripping the last hyphen-delimited segment ("-i").

And given an AC YAML file with id "ACS-100",
When the validator parses the ID to determine its parent,
Then it determines the AC has no parent
  because the ID matches the root pattern (PREFIX-NNN with no additional segments).

And given an AC YAML file with id "ACS-100a",
When the validator parses the ID to determine its parent,
Then it derives the parent ID as "ACS-100"
  by stripping the last hyphen-delimited segment ("-a" after PREFIX-NNN).
```

## Sign-offs

- [x] python-coder — 2026-06-08 08:30
- [x] test-writer — 2026-06-08 00:00
- [x] test-runner — 2026-06-08 08:31
- [x] pr-reviewer — 2026-06-08 08:32
- [x] commit — 2026-06-08 08:33
- [ ] pull-request

## Comments

### 2026-06-08 00:00 — ticket-supervisor (status: ok)
test_requirements empty — test-writer phase skipped (docs-only or config-only ticket)

### 2026-06-08 08:30 — python-coder (status: ok)
feedback-id: fb_2026-06-08_1be5f51a
completion_manifest:
  code_implemented: true
  tests_passing: true
  doc_enforcer_clean: true
  complexity_check_clean: true
Added derive_parent_id() to scripts/ac_store/scan_ac_store.py with two compiled regexes and rsplit fallback; created unit_tests/ac_store/test_parent_id_derivation.py with 14 tests covering all 4 Gherkin scenarios; all 14 tests green, no regressions in existing test_tree_traversal.py suite.

### 2026-06-08 08:31 — test-runner (status: ok)
feedback-id: fb_2026-06-08_36711344
completion_manifest:
  tests_passing: true
  no_regressions: true
14/14 tests in test_parent_id_derivation.py passed; existing test_tree_traversal.py 8/8 green; no regressions detected.

### 2026-06-08 08:32 — pr-reviewer (status: ok)
feedback-id: fb_2026-06-08_0c2ae5a2
completion_manifest:
  code_review_passed: true
  tests_adequate: true
  conventions_followed: true
derive_parent_id() correctly handles all 4 Gherkin ACs; pure function with no I/O (correct per error-handling policy §Rule 4); comprehensive tests with covers tags; decision history updated; approved.

### 2026-06-08 08:33 — commit (status: ok)
feedback-id: fb_2026-06-08_679f8820
completion_manifest:
  pre_commit_hooks_pass: true
  commit_message_valid: true
  ticket_staged: true
Committed 65f003ca: 7 files changed, 582 insertions(+), 18 deletions(-). Pre-commit ran without errors (no .pre-commit-config.yaml required PRE_COMMIT_ALLOW_NO_CONFIG=1).
