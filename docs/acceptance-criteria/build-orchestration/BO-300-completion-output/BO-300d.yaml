id: BO-300d
title: "Work a run left unbuilt is counted and named, never left for you to notice"
component: build-orchestration
components:
  - build_orchestration
level: L1
status: active
req_status: draft
work_status: todo
readiness: draft
priority: medium
roadmap_phase: phase_1
criteria: |
  A run that did part of the job says how much is left and what to do about it.

  When a run ends with work still unbuilt, its closing report states how many
  pieces were not built, names them, and says for each one why it was left — a
  cause only where the run can establish that cause from what it holds, and an
  explicit "cause not determined" where it cannot — together with the action that
  finishes it. A run that built part of the work can never read as one that
  finished it, and you never have to compare what ran against what exists to find
  out the difference.
depends_on:
  - BO-300
doc_links:
  - path: docs/known-issues/build-orchestration.md
    relationship: describes
    status: exists
    relevance: "KI-BO-025 (the run built 17 of 37 and said nothing about the other 20) and KI-BO-026 (the work that WAS surfaced was attributed to a cause the run cannot actually establish)."
  - path: docs/acceptance-criteria/build-orchestration/BO-300-completion-output/BO-300a-5.yaml
    relationship: related
    status: exists
    relevance: "The completion guard, already done: it re-reads the work at the end and withholds the finished claim. This criterion does not restate that behaviour — it adds the count, the honest attribution, and the next action on top of it."
assigned_agent: null
estimated_complexity: L
it_requirements:
  config_schema_fragment: null
  reference_file_path: templates/workflows-js/build-feature.js
  n_location_rule: "1 for the behaviour -- the epic completion path in build-feature.js, where compareEpicTicketSets() (deployed ~1838) feeds epicRecheckReport() (deployed ~1952) and the Step 6 message is assembled. Four of the five children change that one location and should land as one coordinated pass; the fifth (BO-300d-5) is a new page at docs/reference/unbuilt-work-reporting.md. templates/workflows-js/build-ticket.js is the TWIN and correctly carries no counterpart -- inspect it and record that, per BO-300a-5."
  required_skills:
    - python-coder
    - reference-author
  post_write_commands:
    - "python -m unittest discover -s unit_tests/prompt_assembly -p 'test_unbuilt_work_*.py'"
    - "python -m unittest discover -s unit_tests/prompt_assembly -p 'test_epic_*.py'"
  constraints:
    - "COMPOSITE ONLY -- no implementation is assigned at this level. The behaviour is delivered by BO-300d-1 through BO-300d-4 (python-coder) and documented by BO-300d-5 (reference-author). This mapping states the constraints that bind ALL of them and must not be satisfied anywhere on its own."
    - "DO NOT RE-SPECIFY OR WEAKEN THE COMPLETION GUARD. BO-300a-5 is done and correct: the body of work is re-read at reporting time, compared by identity and not by count, unbuilt pieces are named, and the finished claim is withheld. That guard is why KI-BO-025 was caught rather than shipping as a false-complete. Every child adds on top of it; none may reimplement or relax it."
    - "THE ESTABLISHABILITY TABLE IS BINDING ON EVERY CHILD AND IS CLOSED. Plan member with no completed record -> planned and not finished. Plan member with a recorded outcome saying the work stopped short -> stopped part-way, and this wins on precedence. Not a plan member -> cause not determined. Nothing in the data flow records what the body of work contained when the plan was fixed, so no child may require the never-selected / arrived-mid-run distinction, and no child may introduce a plan-time snapshot in order to make it."
    - "THE FAMILY'S SHARPEST FALSE GREEN, stated once here because it threatens every child: the code as it stands already emits a well-formed report with the right pieces and the right withheld verdict. A test asserting that unbuilt work was reported passes against the broken implementation. Every discriminating assertion in this family is on a VALUE -- the stated count, the cause for a known-never-selected piece, the action that cause selected -- never on the presence of a report."
    - "UNDETERMINED IS AN OBLIGATION, NOT A LICENCE. An implementation that labels everything undetermined is a failure of this L1, not a conservative reading of it. The two distinctions the run CAN make are pinned by their own assertions in BO-300d-2 so that implementation goes red."
    - "THE CLEAN-RUN CONTROL IS MANDATORY AND IS NOT OPTIONAL POLISH. A run that built everything reports clean, with no unbuilt section, no count, no names and no action. Warning on every run is the cheapest way to pass the rest of this family and returns the operator to ignoring the sentence, which is the silence being fixed."
    - "VERIFY BEHAVIOURALLY. Per CLAUDE.md 'Gate / Workflow ACs -- Verify Behaviorally, Not by Grep'. This repo has already shipped a runner that passed grep-only structural tests while never executing its gates. Drive the real workflow through unit_tests/prompt_assembly/harness_build_ticket_guard.mjs and assert on the emitted text; where the unit layer cannot run the workflow, assert the report's values are CONSUMED in control flow, not merely produced."
    - "SCOPE FENCE. BO-300a-5 is done and settled that mid-run-arriving work is reported and left outstanding; it is not reopened here. This L1 buys exactly three deltas: the count, an honest cause vocabulary, and a per-cause next action -- plus the reference entry that documents them."
delivers_to: null
expects_from:
  - ac_id: BO-300a-5
    contract: "The completion-time re-read of the body of work, the named unbuilt set compared by identity, the withheld finished claim, and the affirmative-confirmation polarity rule from BO-300a-5-i. This L1 consumes all of it and adds the count, the honest cause, and the action."
test_required: false
test_rationale: |
  Composite. This record has no executable behaviour of its own and no surface a test
  could address that is not addressed by one of its five children: the count by
  BO-300d-1, the cause vocabulary and the false-attribution discrimination by
  BO-300d-2 and BO-300d-2-i, the per-cause action by BO-300d-3, the empty/partial
  self-account and the mandatory clean-run control by BO-300d-4, and the
  documentation parity by BO-300d-5. A test authored at this level would either
  duplicate one of those or assert the aggregate shape of the report, which is
  precisely the assertion this family has identified as passing against the broken
  implementation. Coverage is the union of the children; the composite is done when
  they are.
origin_agent: product-owner
created: 2026-08-26
change_target:
  - pipeline
  - docs
risk_surface: contract_boundary
amended_by:
  - reason: "Criteria body corrected 2026-08-26 during L2 decomposition. It enumerated three causes for unbuilt work -- never taken on, arrived after the run started, stopped part-way -- while this record's own notes required an undetermined bucket wherever the distinction cannot be made. Those two halves disagreed, and the criteria half demanded an attribution the run cannot make: nothing in the data flow records what the body of work contained at the moment the plan was fixed, so 'never selected' and 'arrived mid-run' are indistinguishable from what the run holds. That is precisely the specification defect KI-BO-026 records, restated inside the record that exists to fix it. The notes were taken as authoritative and the criteria body now reads 'a cause only where the run can establish that cause from what it holds, and an explicit cause not determined where it cannot'. No obligation was removed: the count, the naming, the per-piece action and the withheld completion claim are all unchanged, and the undetermined bucket is an obligation to be explicit rather than a licence to say nothing. Aligned with the concurrent BO-300a-5 amendment, whose clause reads 'a cause is stated for a piece of unbuilt work only where the drive can establish that cause from what it holds'."
    by: business-analyst
  - reason: technical-enrichment
    by: it-po
    at: '2026-08-31'
    note: "Composite enrichment only -- assigned_agent deliberately left null; implementation is assigned on the five children (python-coder on BO-300d-1/-2/-2-i/-3/-4, reference-author on BO-300d-5). Added estimated_complexity L, a structured it_requirements mapping carrying the constraints binding on every child (the establishability table, the do-not-re-specify fence around BO-300a-5, the family's sharpest false green, the undetermined-is-an-obligation rule, the mandatory clean-run control, and the verify-behaviourally rule), an expects_from contract against BO-300a-5, test_required false with a rationale naming where each child's coverage lives, and change_target/risk_surface. criteria untouched -- the 2026-08-26 amendment above stands as written; readiness and priority left for the approval gate."
superseded_by: null
covered_by:
  - BO-300d-1
  - BO-300d-2
  - BO-300d-3
  - BO-300d-4
  - BO-300d-5
implemented_by: []
documentation_triggers:
  - reference-doc
notes: |
  ORIGIN. KI-BO-025 (severity high) and its companion KI-BO-026 (medium), both
  2026-08-25. A run built 17 of 37 pieces of work. It did not claim to have
  finished — the completion guard behind BO-300a-5 caught the shortfall and
  withheld that claim, which is the part that already works and must be kept. What
  the run did not do was say how many pieces it had not built, and the pieces it
  did surface were filed under a cause it has no way to establish: they were
  described as having been added after the run started, when in fact they had been
  there all along and were simply never taken on. The operator who follows that
  sentence goes looking for a change that never happened.

  DO NOT RE-SPECIFY THE COMPLETION GUARD. BO-300a-5 is done and correct: the work
  is re-read before the run reports, the unbuilt pieces are named, the finished
  claim is withheld. Three things are missing and are what this L1 buys:
    1. THE COUNT — how many pieces were left, stated by the run rather than
       arrived at by the operator comparing two lists by hand.
    2. THE HONEST REASON — the run must not assert a cause it cannot establish. If
       a distinction cannot be made from what the run knows, the honest output says
       the cause is undetermined and gives the action that is safe in either case.
       An unfounded explanation that sounds complete is worse than an acknowledged
       gap, because it stops the reader looking.
    3. THE NEXT ACTION — stated per reason, in the operator's terms.

  WHAT IS AND IS NOT ESTABLISHABLE, settled 2026-08-26 alongside the BO-300a-5
  amendment and binding on the L2s:
    - A piece the plan WAS built from that has no completed record — establishable
      as planned and not finished, because plan membership and the completed record
      are both in hand.
    - A piece the plan was built from for which the run holds a recorded outcome
      saying the work stopped short — establishable as stopped part-way, because
      that outcome is in hand.
    - A piece the plan was NOT built from — CAUSE NOT DETERMINED. Splitting "never
      selected" from "arrived after the run started" needs a snapshot of the body
      of work taken at plan time, and no such value exists anywhere in the data
      flow. No L2 may require that distinction.

  BINDING CONSTRAINT — ABSENCE OF EVIDENCE IS NOT SUCCESS. A run that leaves work
  behind and says nothing must be a failure of this criterion, not a pass. Silence
  is the defect being fixed; coverage that only checks the wording of a report
  that was emitted cannot see the case where none was. The BA must include the
  case where the run's own view of what it did is EMPTY or PARTIAL and assert that
  something is still said.

  DECOMPOSITION WARNING for the BA. A control case is mandatory: a run that
  genuinely built everything must still report cleanly, with no residual-work
  section — otherwise the cheapest way to satisfy every other scenario is to warn
  on every run, which trains the operator to ignore the warning and puts us back
  where we started. Per CLAUDE.md "Gate / Workflow ACs — Verify Behaviorally, Not
  by Grep", assert on the text the run actually emits, not on an internal
  structure that knows the right answer while the output still reads finished.

  DECOMPOSED 2026-08-26 (business-analyst). Five L2 children, this L1's full child
  cap: BO-300d-1 (the count), BO-300d-2 (a cause only where establishable, with an
  explicit undetermined bucket; + BO-300d-2-i, exactly one reason per piece and the
  reasons summing to the count), BO-300d-3 (the next action per reason),
  BO-300d-4 (the empty/partial self-view case and the mandatory clean-run control),
  BO-300d-5 (the reference entry). The phrase "discovered after planning" is stale
  terminology surviving in BO-300a-5-i and BO-300a-5-iii and is deliberately not
  used in any of them.
