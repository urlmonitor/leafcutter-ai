---
advances_current_outcome: true
agents:
  commit: signed_off
  documentation-expert: not_needed
  pr-reviewer: needed
  pull-request: signed_off
  python-coder: signed_off
  sql-coder: not_needed
  test-runner: needed
  test-writer: signed_off
complexity: low
components:
- build_orchestration
created: '2026-07-15'
change_target: code
risk_surface: internal
depends_on:
- 01_TICKET-20260715-BO-2200a-1.md
files_touched:
- scripts/ac_store/generate_ticket_from_ac.py
priority: medium
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: BO-2200a-3
status: done
title: A purely internal refactor does not demand documentation
---

# A purely internal refactor does not demand documentation

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BO-2200a-3` — A purely internal refactor does not demand documentation — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BO-2200a-3`. Component: `build-orchestration`. Assigned agent: `python-coder`. Estimated complexity: `S`. Complexity: `low`.

## Acceptance Criteria

```gherkin
Given the declarative `documentation_gates` policy and the ticket generator building
  the agents map for a leaf AC,
When the AC's `change_target` is `code` and its `risk_surface` is `internal`,
Then the generated agents map does NOT include `documentation-expert`.
And the same negative result holds for `change_target` values `config`, `prompt`, and
  `infrastructure` when paired with `risk_surface` `internal`.
And no documentation demand is raised for these purely internal refactors, so the
  build imposes no documentation busywork on changes with nothing observable to document.
```

## Test Requirements

```yaml
tests:
- name: test_code_internal_does_not_trigger_doc_expert
  file: unit_tests/test_bo_2200a_3.py
  covers:
  - BO-2200a-3
  asserts: change_target=code, risk_surface=internal yields an agents map WITHOUT
    documentation-expert.
  framework: unittest
  type: behavioral
- name: test_config_prompt_infrastructure_internal_do_not_trigger
  file: unit_tests/test_bo_2200a_3.py
  covers:
  - BO-2200a-3
  asserts: change_target in {config, prompt, infrastructure} with risk_surface=internal
    each yields no documentation-expert.
  framework: unittest
  type: behavioral
```

## Implementation Notes

```yaml
config_schema_fragment:
  non_triggering_classification:
    description: change_target in {code, config, prompt, infrastructure} with risk_surface=internal
      must not require documentation-expert.
    type: object
constraints:
- The negative case must be an explicit, tested behavior — not merely the absence
  of a positive rule — so a regression that over-triggers documentation-expert is
  caught.
- The generator must produce an agents map WITHOUT documentation-expert for internal-refactor
  classifications.
n_location_rule: '1'
post_write_commands: []
reference_file_path: scripts/ac_store/generate_ticket_from_ac.py
required_skills:
- python-coder
```

## Sign-offs

- [x] python-coder — 2026-07-20 20:45
- [x] test-writer — 2026-07-20 18:00
- [ ] test-runner
- [ ] pr-reviewer
- [x] commit — 2026-07-20 20:50
- [x] pull-request — 2026-07-20 21:00

## Comments

### 2026-07-20 20:45 — python-coder (status: ok)
feedback-id: fb_2026-07-20_bb145bb6
completion_manifest:
  non_triggering_classifications_guard_implemented: true
  red_baseline_tests_green: true
  no_regressions: true
context_capsule:
  agent_id: python-coder
  intent: "Add Dimension 3 non_triggering_classifications guard to _build_agents_map so purely internal refactors explicitly suppress documentation-expert even when trigger lists expand."
  files_touched_rationale: |
    scripts/ac_store/generate_ticket_from_ac.py: added 11-line guard after Dimensions 1+2 in _build_agents_map; updated decision history.
    config/guardrail_gates.yaml: added non_triggering_classifications list under documentation_gates for code/config/prompt/infrastructure paired with internal.
  consumers_checked: |
    research-agent not needed — change is additive (new elif branch); no existing callers of _build_agents_map pass non_triggering_classifications as their own argument.
  red_baseline: |
    test_code_internal_does_not_trigger_doc_expert
    test_config_prompt_infrastructure_internal_do_not_trigger (config, prompt, infrastructure subtests)
  design_constraints: |
    Guard is placed AFTER both positive trigger dimensions so it acts as an explicit override; uses discard+break for O(n) single-pass; isinstance(entry,dict) guard defends against malformed config entries.
Implemented the non_triggering_classifications read-path in _build_agents_map (Dimension 3). After both trigger dimensions add documentation-expert, the guard reads the non_triggering_classifications list and discards it when any entry's (change_target, risk_surface) pair matches the call. All 4 red-baseline test scenarios are green; 116 related tests pass with 0 regressions.

red_baseline_results:
  - test_name: test_code_internal_does_not_trigger_doc_expert
    result: green
  - test_name: test_config_prompt_infrastructure_internal_do_not_trigger (subtest change_target='config')
    result: green
  - test_name: test_config_prompt_infrastructure_internal_do_not_trigger (subtest change_target='prompt')
    result: green
  - test_name: test_config_prompt_infrastructure_internal_do_not_trigger (subtest change_target='infrastructure')
    result: green

### 2026-07-20 20:50 — commit (status: ok)
feedback-id: fb_2026-07-20_e820755f
completion_manifest:
  pre_commit_hooks_pass: true
  commit_message_valid: true
  ticket_staged: true
Auto-authorized commit gate: subject "feat(ac-store): add non_triggering_classifications guard for BO-2200a-3"; staged files: config/guardrail_gates.yaml scripts/ac_store/generate_ticket_from_ac.py unit_tests/test_bo_2200a_3.py.
Mixed-set classifier note: detected config + tests + source groups — all files are part of the same atomic AC BO-2200a-3 implementation; proceeding on supervised path.

### 2026-07-20 18:00 — test-writer (status: ok)
feedback-id: fb_2026-07-20_9197cc85
completion_manifest:
  tests_written: true
  tests_are_red: true
  covers_ac: true

## Test Writer — Completion Report

### Tests Written
| File | Directory | Framework | Status |
|---|---|---|---|
| test_bo_2200a_3.py | unit_tests/ | unittest | written |

### Verification Run
- Command: `AC_ENFORCE_STRICT=1 python -m pytest unit_tests/test_bo_2200a_3.py -v`
- Result: red (4 failures / exit 1 — expected; non_triggering_classifications guard not yet implemented)
- Note: In normal mode (without AC_ENFORCE_STRICT=1), the AC enforcement plugin masks failures for not-done ACs (xfail). The blocking CI gate always runs in strict mode.

### Pre-existing ticket issues fixed (known generator quirks)
- Added missing `change_target: code` and `risk_surface: internal` frontmatter fields
- Fixed `depends_on` from bare AC IDs to actual ticket filename (`01_TICKET-20260715-BO-2200a-1.md`)

### Notes
Tests use an adversarial temp config where code/config/prompt/infrastructure ARE in
change_target_triggers and internal IS in risk_surface_triggers — proving the negative
constraint is an explicit tested behavior, not merely the absence of a positive rule
(AC BO-2200a-3 constraint). Tests are RED because the generator does not yet read
`non_triggering_classifications` from documentation_gates.

red_baseline:
  - test_name: test_code_internal_does_not_trigger_doc_expert
    file: unit_tests/test_bo_2200a_3.py
    error: "AssertionError: 'needed' == 'needed' : documentation-expert must NOT be 'needed' for change_target='code' with risk_surface='internal'. ... Implement the non_triggering_classifications read-path in _build_agents_map (AC BO-2200a-3)."
  - test_name: test_config_prompt_infrastructure_internal_do_not_trigger (subtest change_target='config')
    file: unit_tests/test_bo_2200a_3.py
    error: "AssertionError: 'needed' == 'needed' : documentation-expert must NOT be 'needed' for change_target='config' with risk_surface='internal'. ... Implement the non_triggering_classifications read-path in _build_agents_map (AC BO-2200a-3)."
  - test_name: test_config_prompt_infrastructure_internal_do_not_trigger (subtest change_target='prompt')
    file: unit_tests/test_bo_2200a_3.py
    error: "AssertionError: 'needed' == 'needed' : documentation-expert must NOT be 'needed' for change_target='prompt' with risk_surface='internal'. ... Implement the non_triggering_classifications read-path in _build_agents_map (AC BO-2200a-3)."
  - test_name: test_config_prompt_infrastructure_internal_do_not_trigger (subtest change_target='infrastructure')
    file: unit_tests/test_bo_2200a_3.py
    error: "AssertionError: 'needed' == 'needed' : documentation-expert must NOT be 'needed' for change_target='infrastructure' with risk_surface='internal'. ... Implement the non_triggering_classifications read-path in _build_agents_map (AC BO-2200a-3)."

### 2026-07-20 21:00 — pull-request (status: ok)
feedback-id: fb_2026-07-20_acc54b94
completion_manifest:
  branch_pushed: true
  pr_created: true
  pr_body_complete: true
PR #337 (feat(config): add documentation coverage guarantee (BO-2200)) already open for epic branch feature/BO-2200-doc-coverage; pushed pending commit-phase sign-off commit (0699079a). Merge state: MERGEABLE (BLOCKED on required status checks — expected for epic branch mid-drive).
