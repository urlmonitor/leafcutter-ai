---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  pr-reviewer: needed
  pull-request: needed
  python-coder: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: failed
components:
- build_pipeline
created: '2026-06-11'
depends_on:
- 07_TICKET-20260611-BP-900b-2.md
files_touched:
- scripts/build.py
- docs/architecture/components/template-compiler.md
priority: critical
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: BP-900b-3
status: todo
title: Build exits non-zero when broken references are found
---

# Build exits non-zero when broken references are found

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BP-900b-3` — Build exits non-zero when broken references are found — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BP-900b-3`. Component: `build_pipeline`. Assigned agent: `python-coder`. Estimated complexity: `S`.

## Acceptance Criteria

```gherkin
Given the guard has identified one or more broken script references
(scripts referenced in templates but not present in the deployable set),
When the guard completes its scan,
Then build.py exits with a non-zero exit code
And the build does NOT produce a successful output directory
And no partial deployment is written to the target
```

## Sign-offs

- [ ] python-coder
- [ ] test-writer — failed 2026-08-18 08:35
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit
- [ ] pull-request

## Comments

### 2026-08-18 08:35 — test-writer (status: blocker)
feedback-id: (submit-failed)
Attempted to establish a red baseline for AC BP-900b-3 ("build.py exits non-zero
when broken references are found, with no partial deployment written"). Found
that `scripts/build.py` already implements this exact behavior:
`_check_script_reference_guard()` (lines ~719-774) is already wired into
`main()` (lines ~1515-1525, `if _check_script_reference_guard(package_root):
return 1`) — the guard explicitly cites `(BP-900b-3)` in its own inline
comment — and runs before `_run_phases()` writes any output. The existing
`unit_tests/test_build_guard_real_package.py` already has
`test_guard_exits_0_on_clean_package` and `test_guard_exits_1_on_broken_ref`
covering the guard function's return value, both green. I additionally wrote
two NEW tests closing a real coverage gap this AC specifically requires (a
main()-level wiring/behavioral test, per CLAUDE.md "Gate / Workflow ACs —
Verify Behaviorally, Not by Grep", since the existing tests only exercised
the guard function directly, not main()'s consumption of its return value or
the "no partial deployment" half of the AC):
`test_main_exits_nonzero_and_writes_nothing_on_broken_script_ref` and
`test_main_does_not_call_script_reference_guard_under_validate_only` in
`unit_tests/test_build_guard_real_package.py`. Both ran GREEN immediately
(non-zero-exit red baseline could not be established) because the production
code this AC describes was already present before this ticket's coder phase
ran — the AC store entry `BP-900b-3.yaml` itself already shows
`work_status: done` and `covered_by: unit_tests/test_build_guard_real_package.py`.
This matches the "Competing-solution merge" / TDD-order-violation pattern
documented in this repo's memory and CLAUDE.md ("TDD Order — test-writer Must
Precede python-coder": a suite found green before the coder phase is a
TDD-order violation to document, not silently sign off as red). I cannot
honestly report `red_baseline_verified: true`, so per the test-writer
contract ("the tests you write MUST be red when you sign off") I am not
signing off green. Suggested remediation: a human/supervisor should confirm
whether this ticket's implementation was already delivered on `origin/main`
independently (likely via a prior epic that also touched
`_check_script_reference_guard`), and either (a) mark this ticket/AC
done-already with the two new tests recorded as its formal coverage, or (b)
dispatch `python-coder` to run a verify-only pass (no code changes expected)
and sign off explicitly noting the pre-existing implementation. Separately,
note this ticket's frontmatter has two pre-existing schema violations
unrelated to this finding (flagged by `ticket_frontmatter_guard.py` on every
edit): `depends_on: [BP-900b, BP-900b-2]` references files not present in
this epic folder, and `change_target` / `risk_surface` are missing — these
are out of test-writer's scope to fix but should be resolved before this
ticket can pass the frontmatter guard cleanly.
