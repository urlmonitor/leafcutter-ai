---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  pr-reviewer: needed
  pull-request: needed
  python-coder: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
components:
- build-orchestration
created: '2026-07-07'
depends_on:
- BO-1800d
files_touched:
- docs/architecture/adrs/ADR-018-agent-isolation-topology.md
- docs/architecture/components/worktree-manager.md
priority: medium
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: BO-1800d-2
status: todo
title: Garbage collection runs only single-writer, between drives
---

# Garbage collection runs only single-writer, between drives

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BO-1800d-2` — Garbage collection runs only single-writer, between drives — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BO-1800d-2`. Component: `build-orchestration`. Assigned agent: `python-coder`. Estimated complexity: `M`.

## Acceptance Criteria

```gherkin
Given garbage collection / maintenance needs to run on a repository,
When it is invoked,
Then it runs only when no drive is active against that repository (between drives),
  as a single-writer operation,
And gc/maintenance is never started against a repository while a drive is writing
  to it.
```

## Sign-offs

- [ ] python-coder
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit
- [ ] pull-request

## Comments
