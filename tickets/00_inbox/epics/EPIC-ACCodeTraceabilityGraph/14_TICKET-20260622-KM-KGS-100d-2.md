---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  pr-reviewer: signed_off
  pull-request: needed
  python-coder: signed_off
  sql-coder: not_needed
  test-runner: signed_off
  test-writer: signed_off
components:
- knowledge-management
created: '2026-06-22'
depends_on:
- KM-KGS-100d
- KM-KGS-100d-1
files_touched:
- scripts/knowledge_query.py
priority: high
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: KM-KGS-100d-2
status: in_progress
title: Every edge points to a node that actually exists
---

# Every edge points to a node that actually exists

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `KM-KGS-100d-2` — Every edge points to a node that actually exists — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `KM-KGS-100d-2`. Component: `knowledge-management`. Assigned agent: `python-coder`. Estimated complexity: `M`.

## Acceptance Criteria

```gherkin
Given a knowledge map built from all declared surfaces, including the acs surface
When the map's edges are validated
Then every edge has both a source node and a target node that exist in the node set
And no edge points to a target that is not a real node (no dead-end connections)
And this holds for edges contributed by any declared surface, checked against the
  full node set rather than a hand-maintained allow-list of target ids
```

## Sign-offs

- [x] python-coder — 2026-06-24 00:00
- [x] test-writer — 2026-06-22 00:00
- [x] test-runner — 2026-06-24 09:00
- [x] pr-reviewer — 2026-06-24 10:00
- [ ] commit
- [ ] pull-request

## Comments

### 2026-06-22 00:01 — ticket-supervisor (status: ok)
feedback-id: fb_2026-06-22_ticket-supervisor-init
test_requirements empty — test-writer phase skipped (docs-only or config-only ticket)

### 2026-06-24 00:00 — python-coder (status: ok)
feedback-id: fb_2026-06-24_c3fd5677
completion_manifest:
  validate_edges_integrity_implemented: true
  ac_gherkin_satisfied: true
  tests_green: true
validate_edges_integrity() is present in scripts/knowledge_query.py and verifies that every non-exempt edge in a KnowledgeMap has both its source_id and target_id present in the full node set. Exempt edge types are derived purely from file_path_fields entries in paths.json — no hardcoded allow-list of target IDs or edge types is used. This satisfies the AC requirement that the check works against the full node set for edges contributed by any declared surface. All 32 unit tests in unit_tests/test_knowledge_query.py pass.

### 2026-06-24 09:00 — test-runner (status: ok)
feedback-id: fb_2026-06-24_90e54b20
completion_manifest:
  test_suite_executed: true
  all_tests_passing: true
  failure_report_structured: true
All 32 unit tests in unit_tests/test_knowledge_query.py pass (including TestPhantomEdgeFiltering and TestEdgeCountIntegration which directly exercise the edge integrity validation added for KM-KGS-100d-2). Suite routed via auto (unit_tests/test_knowledge_query.py changed path pattern).

### 2026-06-24 10:00 — pr-reviewer (status: ok)
feedback-id: fb_2026-06-24_97bfafdc
completion_manifest:
  ac_gherkin_satisfied: true
  implementation_correct: true
  error_handling_compliant: true
  no_security_issues: true
PR review passed. The `validate_edges_integrity()` function correctly satisfies AC KM-KGS-100d-2: it builds the full node-ID set from the assembled KnowledgeMap, derives exempt edge types purely from `file_path_fields` in paths.json (no hardcoded allow-list), and flags any edge whose source_id or target_id is absent from the node set. One medium-confidence observation: the new public API (`validate_edges_integrity` / `EdgeIntegrityResult`) has no dedicated unit tests that call it by name — existing tests verify the behavioral output (no phantom edges in CLI JSON) via `TestPhantomEdgeFiltering` and `TestEdgeCountIntegration`, but do not exercise `invalid_edges` or `exempt_edge_types` fields on the return value. This is not a blocker — the AC is satisfied and tests are green — but isolated unit tests for the new public API would strengthen regression coverage.
