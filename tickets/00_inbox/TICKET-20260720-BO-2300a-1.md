---
ac_traceability:
  id: BO-2300a-1
  path: docs/acceptance-criteria/build-orchestration/BO-2300-interactive-pause-resume/BO-2300a-1.yaml
advances_current_outcome: true
agents:
  architect-review: needed
  commit: needed
  pr-reviewer: needed
  pull-request: needed
  python-coder: needed
  test-runner: needed
  test-writer: needed
change_target: pipeline
complexity: high
components:
- build_orchestration
created: '2026-07-20'
depends_on: []
files_touched:
- templates/workflows-js/plan-feature.js
- templates/workflows-js/build-feature.js
- templates/workflows-js/finalize-feature.js
- templates/workflows-js/build-epic.js
- templates/workflows-js/build-ticket.js
- unit_tests/workflows/test_bo_2300_pause_resume.py
priority: high
requires_adr: false
requires_diagram: false
risk_surface: internal
roadmap_phase: phase_1
source_ac: BO-2300a-1
status: todo
title: A gate pauses and persists resumable state instead of cancelling
---

# A gate pauses and persists resumable state instead of cancelling

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BO-2300a-1` — A gate pauses and persists resumable state instead of cancelling — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BO-2300a-1`. Component: `build-orchestration`. Assigned agent: `python-coder`. Estimated complexity: `L`. Complexity: `high`.

## Acceptance Criteria

```gherkin
Given a background run is executing with no human attached to answer questions,
And the run reaches an interactive decision point (a gate that would ask
  "approve, edit, or cancel?"),
When the run cannot obtain an immediate answer,
Then the run does NOT cancel and does NOT discard the work completed so far,
And the run enters a paused state that records a pending question tied to the
  run so the run can be resumed from that exact point later.
```

- [ ] AC-1: the run reaches an interactive decision point (a gate that would ask
- [ ] AC-2: the run does NOT cancel and does NOT discard the work completed so far
- [ ] AC-3: the run enters a paused state that records a pending question tied to the

## Test Requirements

```yaml
tests:
- name: test_gate_pauses_instead_of_cancelling
  file: unit_tests/workflows/test_bo_2300_pause_resume.py
  covers:
  - BO-2300a-1
  asserts: A headless run reaching an interactive gate enters the paused state (does
    not cancel), preserves committed work, and writes a run-keyed pending-question record.
  framework: pytest
  type: behavioral
- name: test_pause_is_idempotent_on_same_gate
  file: unit_tests/workflows/test_bo_2300_pause_resume.py
  covers:
  - BO-2300a-1
  asserts: Re-reaching the same gate for an already-paused run does not create a duplicate
    pending-question record.
  framework: pytest
  type: behavioral
- name: test_paused_state_distinct_from_cancelled
  file: unit_tests/workflows/test_bo_2300_pause_resume.py
  covers:
  - BO-2300a-2
  asserts: A paused run reports 'paused_awaiting_input' and a cancelled run reports
    'cancelled'; only the paused run is resumable.
  framework: pytest
  type: behavioral
- name: test_gateless_run_never_pauses
  file: unit_tests/workflows/test_bo_2300_pause_resume.py
  covers:
  - BO-2300a-1-i
  asserts: A run that hits no interactive gate completes normally, records no pending
    question, behaves as before the mechanism existed.
  framework: pytest
  type: behavioral
- name: test_pending_question_declares_type_and_shape
  file: unit_tests/workflows/test_bo_2300_pause_resume.py
  covers:
  - BO-2300b-1
  asserts: The emitted question declares exactly one type (single_choice / priority_choice
    / free_text) and its valid answer shape; the gate declares which type it needs.
  framework: pytest
  type: behavioral
- name: test_wrong_shape_answer_rejected_and_reprompted
  file: unit_tests/workflows/test_bo_2300_pause_resume.py
  covers:
  - BO-2300b-2
  asserts: An answer not matching the declared shape is rejected, not applied; the run
    stays paused and re-presents the same question with its valid shape.
  framework: pytest
  type: behavioral
- name: test_unparseable_answer_reprompts_never_crashes
  file: unit_tests/workflows/test_bo_2300_pause_resume.py
  covers:
  - BO-2300b-2-i
  asserts: An empty/malformed answer does not crash the run; not applied; run stays
    paused and re-prompts.
  framework: pytest
  type: behavioral
- name: test_context_snapshot_captured_at_pause_and_surfaced
  file: unit_tests/workflows/test_bo_2300_pause_resume.py
  covers:
  - BO-2300c-1
  asserts: The pause captures a context snapshot at pause time (work done / proposal /
    decision) and surfaces it with the question.
  framework: pytest
  type: behavioral
- name: test_valid_answer_applied_by_type_and_resumes_from_pause
  file: unit_tests/workflows/test_bo_2300_pause_resume.py
  covers:
  - BO-2300d-1
  asserts: 'approve→proceed, edit→re-dispatch-with-feedback, cancel→graceful-stop,
    priority→set; run resumes from the pause point (not restart); prior stages not repeated.'
  framework: pytest
  type: behavioral
- name: test_resume_preserves_committed_earlier_stages
  file: unit_tests/workflows/test_bo_2300_pause_resume.py
  covers:
  - BO-2300d-1-i
  asserts: Pausing during stage 3 of a multi-stage run and resuming does not re-run
    committed stages 1-2; their results remain unchanged.
  framework: pytest
  type: behavioral
- name: test_cancel_answer_graceful_keeps_stages_no_pr
  file: unit_tests/workflows/test_bo_2300_pause_resume.py
  covers:
  - BO-2300d-1-ii
  asserts: A 'cancel' answer stops gracefully, keeps earlier committed stages, opens
    no PR.
  framework: pytest
  type: behavioral
- name: test_paused_state_durable_across_process_exit
  file: unit_tests/workflows/test_bo_2300_pause_resume.py
  covers:
  - BO-2300e-1
  asserts: After the workflow process exits, the pending question + context + place
    persist; a new process resumes from the pause point; committed stages carried forward.
  framework: pytest
  type: behavioral
- name: test_reanswer_is_idempotent
  file: unit_tests/workflows/test_bo_2300_pause_resume.py
  covers:
  - BO-2300e-1-i
  asserts: Submitting the same answer twice (or resuming twice) does not double-apply
    or re-run later stages; state matches applying once.
  framework: pytest
  type: behavioral
- name: test_resume_with_no_pending_pause_is_noop
  file: unit_tests/workflows/test_bo_2300_pause_resume.py
  covers:
  - BO-2300e-1-ii
  asserts: Answering/resuming a run with no pending question leaves state unchanged
    and reports 'nothing to resume' (not an error).
  framework: pytest
  type: behavioral
- name: test_stale_pause_fails_gracefully
  file: unit_tests/workflows/test_bo_2300_pause_resume.py
  covers:
  - BO-2300e-1-iii
  asserts: A stale/expired pause is not silently resumed into a corrupt state; answerer
    is told it can't resume and why; committed stages remain.
  framework: pytest
  type: behavioral
```

## Implementation Notes

```yaml
config_schema_fragment:
  description: Durable pending-question record persisted on pause, keyed by run id.
  properties:
    answer:
      type:
      - object
      - 'null'
    context:
      description: snapshot, shape defined by BO-2300c-1
      type: object
    pause_point:
      description: resume marker referencing the existing resumeFromRunId run-resume
        cache
      type: object
    question:
      description: shape defined by BO-2300b-1
      type: object
    run_id:
      type: string
    status:
      enum:
      - paused_awaiting_input
      - cancelled
      - running
      - completed
      type: string
  required:
  - run_id
  - status
  - question
  - pause_point
  type: object
constraints:
- 'This is the defect being fixed: at an interactive gate with no reachable answerer
  the E2 engine today resolves the gate as blocked and CANCELS headless. The run must
  instead PAUSE and persist, discarding no committed work.'
- The pending-question record must be keyed to the run id and written to a run-scoped
  durable store that survives the workflow process exiting; align with how the existing
  crash-resume run state is persisted (the workflow body itself stays stateless per
  docs/reference/workflow-constraints.md).
- 'Every interactive gate that today cancels headless must be migrated to pause: apply
  the same mechanism across templates/workflows-js/plan-feature.js, build-feature.js,
  and finalize-feature.js (hence n_location_rule=all) so no gate silently retains
  the cancel-on-headless bug.'
- All state-store reads/writes are external I/O and must follow the project error-handling
  policy (typed except, WARNING+ log, no bare except, no silent swallow).
- Authored in E2 top-level-body form per ADR-017; do not use Date.now()/new Date()/Math.random()
  in the workflow body (pass any timestamp/seed via args); the build-time E1 shim
  is regenerated by build.py, never hand-authored.
- Engine JS under templates/workflows-js/ and any Python glue under scripts/ must
  keep deployed/template parity via the build.py round-trip (ADR-001).
- 'Pausing must be idempotent: re-reaching the same gate for an already-paused run
  must not create a duplicate pending-question record.'
- Pause/persist transitions must be logged at an appropriate severity for observability.
- 'ARCHITECT-REVIEW SCOPING (resolve first): a true pause that survives the workflow
  PROCESS EXITING and resumes in a NEW process (BO-2300e) may require Workflow-engine /
  runtime (Claude Code harness) support for a pending-question input channel, not just
  edits to the workflow scripts. Determine up front what is achievable purely in
  templates/workflows-js/*.js + the existing resumeFromRunId run-resume cache vs what
  needs an engine capability; if the latter, flag the boundary and split the
  engine-dependent portion rather than faking a durable channel in the script body.'
- 'This ticket covers the FULL python-coder AC set: BO-2300a-1/-2, BO-2300b-1/-2,
  BO-2300c-1, BO-2300d-1, BO-2300e-1 and the L3 edges (a-1-i, b-2-i, d-1-i, d-1-ii,
  e-1-i, e-1-ii, e-1-iii). The two diagram ACs (BO-2300a-3 state, BO-2300d-2 sequence)
  are a separate architecture-diagram-author ticket.'
n_location_rule: all
post_write_commands:
- python scripts/build.py --target-dir .
reference_file_path: templates/workflows-js/plan-feature.js
required_skills:
- python-coder
```

## Agent Contracts

### Delivers To

- **Agent:** python-coder
- **Contract:** Durable pending-question record keyed by run id: { run_id: string, status: 'paused_awaiting_input', question: <object, shape from BO-2300b-1>, context: <snapshot, shape from BO-2300c-1>, pause_point: <resume marker referencing the existing resumeFromRunId cache>, answer: null }, written to a run-scoped durable state store that survives process exit. Consumed by the distinct-state (BO-2300a-2), apply/resume (BO-2300d-1), and durability (BO-2300e-1) behaviors.

## Sign-offs

- [ ] architect-review
- [ ] test-writer
- [ ] python-coder
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit
- [ ] pull-request

## Comments
