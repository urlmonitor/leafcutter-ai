---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  pr-reviewer: needed
  pull-request: not_needed
  python-coder: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
components:
- build_orchestration
created: '2026-07-07'
depends_on:
- BO-1800c
- BO-1800a-1
files_touched:
- docs/architecture/adrs/ADR-018-agent-isolation-topology.md
- docs/architecture/agent_delivery_workflows.md
priority: medium
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: BO-1800c-2
status: todo
title: The number of independent features in flight is not capped by a fixed limit
---

# The number of independent features in flight is not capped by a fixed limit

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BO-1800c-2` — The number of independent features in flight is not capped by a fixed limit — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BO-1800c-2`. Component: `build-orchestration`. Assigned agent: `python-coder`. Estimated complexity: `M`.

## Acceptance Criteria

```gherkin
Given multiple independent features are ready to be built in parallel,
When they are dispatched as concurrent drives (each in its own isolated clone),
Then the system does not reject or queue a drive because of a fixed maximum count
  of concurrent features/drives,
And the count of independent features that may run at once is limited only by host
  resources (per BO-1800c-3), not by an arbitrary configured cap.
```

## Sign-offs

- [ ] python-coder
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit

## Comments
