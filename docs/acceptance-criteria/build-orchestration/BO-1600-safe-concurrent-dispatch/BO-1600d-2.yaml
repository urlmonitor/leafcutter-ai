id: BO-1600d-2
components:
  - build_orchestration
readiness: approved
priority: high
title: "Recovery is non-destructive by default: it prints exactly what it will do and only acts on explicit confirmation"
component: build-orchestration
level: L2
status: active
req_status: draft
work_status: done
created_by: "docs/acceptance-criteria/build-orchestration/BO-1600-safe-concurrent-dispatch/BO-1600d.yaml"
criteria: |
  Given the human has started the recovery entry point against a damaged shared
    repository
  When recovery runs without an explicit execute confirmation (the default, dry-run-first mode)
  Then it prints the exact ordered list of actions it would take — naming each
    specific target (for example "remove 3 zero-byte loose objects: <listed paths>",
    "re-fetch objects from origin", "reset branch <name> to reflog entry <ref>",
    "rebuild the index cache-tree") — and makes no change to the repository
  And it exits after printing the plan, having performed zero git writes, so a reader
    can review the plan before anything happens
  And only when the human supplies the explicit execute confirmation (an execute flag
    or an interactive yes) does recovery carry out the printed actions
  And the set of actions actually executed matches the plan that was printed for that
    same repository state — recovery does not execute an action it did not first show
depends_on:
  - BO-1600d
  - BO-1600d-1
doc_links:
  - path: "docs/acceptance-criteria/build-orchestration/BO-1600-safe-concurrent-dispatch/BO-1600d.yaml"
    relationship: parent
    status: exists
  - path: "docs/acceptance-criteria/build-orchestration/PROJECT_CONTEXT.md"
    relationship: describes
    status: exists
assigned_agent: python-coder
estimated_complexity: M
it_requirements:
  - "Default mode is dry-run-first: with no execute confirmation the recovery prints the ordered action plan (each action names its concrete targets) and performs ZERO git writes, then exits. Execution requires an explicit execute flag (e.g. --execute) or an interactive yes; --dry-run is the effective default when no execute confirmation is given."
  - "The printed plan and the executed actions must be derived from the SAME plan-computation pass over the current repository state, so the executed set cannot diverge from what was shown (no 'compute plan for display, recompute different plan for execute')."
  - "Non-destructive-by-default means the failure mode of any ambiguity is to print-and-stop, not to act; when the plan cannot be computed the entry point reports why and makes no change (ties to the unrecoverable/refuse L3s BO-1600d-3-* )."
  - "PORTABLE SURFACE: this plan/confirm layer lives inside the same standalone recovery script pinned in BO-1600d-1 (templates/scripts/git_recovery.py); it is not a separate deployable artifact."
  - "Must handle errors gracefully per the project error handling policy (CLAUDE.md Rules 1-3): wrap the git/filesystem operations in try/except with a specific exception type, log at WARNING or higher, and treat an errored step as a stop, never as a silent skip that leaves a partial plan applied."
  - "PORTABLE per ADR-001: the dry-run-first + confirm behavior ships to consumers via build.py (BO-1600d-4); it must NOT rely on the leafcutter repo's own root CLAUDE.md."
delivers_to:
  agent: python-coder
  contract: "The plan/confirm layer: a single plan-computation pass over the current repository state that both prints the ordered, target-named action list (dry-run-first default, zero writes) and drives execution only after explicit confirmation. The recovery-step engine (BO-1600d-3) supplies the individual actions; this layer sequences, displays, and gates them."
expects_from:
  ac_id: BO-1600d-1
  contract: "The human-invoked, pre-confirmation-inert recovery entry point that this plan/confirm layer runs inside."
origin_agent: business-analyst-v3
created: 2026-07-01
amended_by:
  - reason: it-po-technical-enrichment
    entry: "Pinned portable surface (plan/confirm layer lives inside templates/scripts/git_recovery.py, not a separate artifact) and named the --dry-run default / --execute flag mechanics. readiness draft->reviewed."
    confirmed_by: it-po-v3
    confirmed_at: 2026-07-01
superseded_by: null
covered_by:
- tests/test_git_recovery.py::TestDryRunFirstBehavior
implemented_by:
- tickets/00_inbox/epics/EPIC-GuidedGitRecovery/02_TICKET-20260706-BO-1600d-2.md
notes: |
  Non-destructive-by-default and dry-run-first are the two safety properties the
  user called out explicitly. Splitting them from the step engine (BO-1600d-3)
  keeps "how we gate and show actions" separate from "what each recovery action
  is", so the safety contract is independently testable.
