id: ACD-1600f-1-i
components:
  - ac_driven_dev
readiness: reviewed
priority: medium
title: "A fixture using `request` where the AC specifies `input` is flagged"
component: ac-driven-dev
level: L3
status: active
req_status: draft
work_status: todo
roadmap_phase: phase_acbuild_1_foundation
criteria: |
  Given an AC specifies the input field name as `input`,
  And its referenced eval fixture uses the field name `request` for the same field,
  When the consistency check runs,
  Then the pair is flagged as a contradiction and blocks dispatch,
  And the message names `input` (AC-specified) versus `request` (fixture-actual).
depends_on: [ACD-1600f-1]
doc_links:
  - path: docs/reference/ac-schema.md
    relationship: describes
    status: exists
  - path: docs/architecture/adrs/ADR-026-ac-driven-build-v2-phased-migration.md
    relationship: describes
    status: exists
assigned_agent: python-coder
estimated_complexity: S
it_requirements:
  - "Canonical source to change: scripts/ac_store/validate_ac.py (AC authoring-time validation; source-only Python)."
  - "The check compares the field names the AC specifies against the referenced artifact's actual schema and flags a mismatch as a contradiction that blocks dispatch."
  - "The message must name both sides of the mismatch: the AC-specified name (`input`) and the artifact-actual name (`request`)."
delivers_to: null
expects_from:
  ac_id: ACD-1600f-1
  contract: "The AC-vs-artifact consistency check: given an AC's specified field names and a referenced artifact, return the set of mismatching (ac-specified, artifact-actual) field pairs."
origin_agent: BrainCandy
created: 2026-08-11
amended_by: []
superseded_by: null
covered_by: []
implemented_by: []
change_target: pipeline
risk_surface: internal
test_spec:
  - name: test_request_vs_input_mismatch_flagged_blocks_dispatch
    target_dir: unit_tests/ac_store/
    framework: unittest
    type: unit
    description: "An AC specifying `input` whose fixture uses `request` is flagged as a contradiction that blocks dispatch."
  - name: test_mismatch_message_names_both_sides
    target_dir: unit_tests/ac_store/
    framework: unittest
    type: unit
    description: "The flag message names `input` (AC-specified) versus `request` (fixture-actual)."
notes: |
  This is the empirically observed contradiction that motivated ACD-1600f.
