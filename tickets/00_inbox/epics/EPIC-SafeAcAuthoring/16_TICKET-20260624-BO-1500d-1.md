---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  pr-reviewer: needed
  pull-request: needed
  python-coder: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
components:
- build-orchestration
created: '2026-06-24'
depends_on:
- BO-1500d
- BO-1500c-1
files_touched:
- templates/skills/create-ac/SKILL.md
- scripts/workflows/plan-feature.js
priority: high
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: BO-1500d-1
status: todo
title: The PR number and URL are reported back to the user the moment the PR is opened
---

# The PR number and URL are reported back to the user the moment the PR is opened

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BO-1500d-1` — The PR number and URL are reported back to the user the moment the PR is opened — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BO-1500d-1`. Component: `build-orchestration`. Assigned agent: `python-coder`. Estimated complexity: `S`.

## Acceptance Criteria

```gherkin
Given the authoring workflow has just opened the delivery pull request to main,
When the workflow returns control to the user,
Then it prints the pull request's number and its clickable URL in the final
  output the user sees,
And the URL points to the pull request that carries the approved AC files,
And the user does not have to run a separate command or search the repository
  to find the pull request.
```

## Sign-offs

- [ ] python-coder
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit
- [ ] pull-request

## Comments
