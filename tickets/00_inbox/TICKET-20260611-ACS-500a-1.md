---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  pr-reviewer: needed
  pull-request: needed
  python-coder: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
components:
- ac-store
created: '2026-06-11'
depends_on:
- ACS-500a
files_touched:
- docs/reference/ac-schema.md
- docs/acceptance-criteria/index.yaml
- docs/architecture/adrs/ADR-007-ac-store-schema-id-format-enforcement.md
- config/ac_store_schema.json
priority: high
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: ACS-500a-1
status: todo
title: A pattern AC defines shared behavior with parameterized slots
---

# A pattern AC defines shared behavior with parameterized slots

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `ACS-500a-1` — A pattern AC defines shared behavior with parameterized slots — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `ACS-500a-1`. Component: `ac-store`. Assigned agent: `python-coder`. Estimated complexity: `M`.

## Acceptance Criteria

```gherkin
Given an AC YAML file under docs/acceptance-criteria/ with level: L2,
When its criteria field describes behavior that multiple pages or endpoints share
  (e.g. "sortable table with columns {columns}, sorted by {default_sort}"),
And its id is referenced by other ACs via the implements_pattern field,
Then that AC is the single authoritative definition of the shared behavior,
And its criteria uses named placeholders in curly braces to mark the slots
  that consuming ACs fill via pattern_bindings,
And no other AC in the store contains an equivalent criteria body
  for the same behavior (single source of truth).
```

## Sign-offs

- [ ] python-coder
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit
- [ ] pull-request

## Comments
