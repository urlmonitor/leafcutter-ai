---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  pr-reviewer: needed
  pull-request: needed
  python-coder: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
components:
- commit_guardian
created: '2026-06-16'
depends_on:
- GE-100e
files_touched:
- docs/acceptance-criteria/guardrail-engine/GE-100-code-quality-hooks/GE-100e.yaml
- docs/pre-commit-hooks.md
priority: high
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: GE-100e-1
status: todo
title: diff-cover hook blocks commit when strict mode is enabled and coverage is below
  threshold
---

# diff-cover hook blocks commit when strict mode is enabled and coverage is below threshold

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `GE-100e-1` — diff-cover hook blocks commit when strict mode is enabled and coverage is below threshold — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `GE-100e-1`. Component: `guardrail-engine`. Assigned agent: `python-coder`. Estimated complexity: `S`.

## Acceptance Criteria

```gherkin
Given the commit guardian configuration has the diff_coverage section with:
  enabled: true
  strict: true
  min_coverage_percent: 80
  coverage_xml_path: "coverage.xml"
  compare_branch: "origin/main"
  max_age_seconds: 3600
And the diff-cover binary is installed
And coverage.xml exists and was generated 600 seconds ago (within freshness window)
And the developer has staged Python files whose diff coverage against
  origin/main measures at 45% (below the 80% threshold)
When the diff-cover hook runs at pre-commit time
Then the hook exits with code 1 (blocking)
And it emits an error listing each under-covered file with its percentage
And the error states the overall diff coverage (45%) vs the threshold (80%)
And the commit is prevented
```

## Sign-offs

- [ ] python-coder
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit
- [ ] pull-request

## Comments
