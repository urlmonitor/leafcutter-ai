---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  pr-reviewer: needed
  pull-request: needed
  python-coder: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
components:
- ac_store
created: '2026-06-11'
depends_on:
- ACS-500e
- ACS-500e-1
files_touched:
- docs/reference/ac-schema.md
- docs/architecture/adrs/ADR-008-ac-store-schema-id-format-enforcement.md
- docs/architecture/adrs/ADR-010-ac-store-as-authoritative-backlog.md
priority: high
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: ACS-500e-2
status: todo
title: Composition depth is visible through the AC parent-child hierarchy
---

# Composition depth is visible through the AC parent-child hierarchy

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `ACS-500e-2` — Composition depth is visible through the AC parent-child hierarchy — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `ACS-500e-2`. Component: `ac-store`. Assigned agent: `python-coder`. Estimated complexity: `M`.

## Acceptance Criteria

```gherkin
Given a page AC references composite pattern "crud-list-page" (PTN-020),
And PTN-020 depends_on atomic patterns [PTN-010, PTN-011, PTN-012],
When a reader traverses the dependency graph starting from the page AC,
Then they can resolve the full behavior stack in order:
  1. Page-specific criteria (from the page AC itself),
  2. Composite wiring behavior (from PTN-020),
  3. Atomic behaviors (from PTN-010, PTN-011, PTN-012),
And each layer is a distinct AC file with its own id and criteria,
And the layering from atomic to composite to page-specific is
  expressible using only the standard depends_on and implements_pattern
  fields (no additional hierarchy mechanism is required).
```

## Sign-offs

- [ ] python-coder
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit
- [ ] pull-request

## Comments
