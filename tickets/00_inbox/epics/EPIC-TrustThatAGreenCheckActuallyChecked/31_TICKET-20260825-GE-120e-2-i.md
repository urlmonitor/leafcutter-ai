---
ac_traceability:
  id: GE-120e-2-i
  path: docs/acceptance-criteria/guardrail-engine/GE-120-green-means-checked/GE-120e-2-i.yaml
advances_current_outcome: true
agents:
  commit: signed_off
  pull-request: not_needed
  test-runner: needed
  test-writer: signed_off
change_target: code
complexity: medium
components:
- commit_guardian
- testing_quality
created: '2026-08-25'
depends_on: []
files_touched:
- unit_tests/portability
priority: high
requires_adr: false
requires_diagram: false
risk_surface: internal
roadmap_phase: phase_1
source_ac: GE-120e-2-i
status: todo
title: A check whose recorded change-set source disagrees with what it actually inspects
  is named by running it, not by believing it
---

# A check whose recorded change-set source disagrees with what it actually inspects is named by running it, not by believing it

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `GE-120e-2-i` — A check whose recorded change-set source disagrees with what it actually inspects is named by running it, not by believing it — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `GE-120e-2-i`. Component: `guardrail-engine`. Assigned agent: `test-writer`. Estimated complexity: `M`. Complexity: `medium`.

## Acceptance Criteria

```gherkin
Given a check whose manifest entry records that the commit path hands it the files to
  inspect, so it is outside the set GE-120e-2 governs,
And a commit whose staged tree holds carried-in work that would trip that check, and whose
  author wrote nothing that would,
When that check is executed as a process against that commit,
Then it raises no objection,
And a check that does raise one is named, together with the content it objected to and the
  state that content came in from, and the determination reports a failure,
And the same is asserted for every manifest entry recorded as being handed its files, so a
  recorded source that is not what the check does is caught by running the check rather
  than by reading the record.

Coverage note: the record is a claim, and this AC is the only thing that tests the claim.
Cover it by planting a check whose entry claims it is handed its files while it derives
its own change set, and asserting the sweep names that check and fails. A sweep that
cannot be made to fail this way is not evidence.
```

- [ ] AC-1: a commit whose staged tree holds carried-in work that would trip that check, and whose
- [ ] AC-2: it raises no objection
- [ ] AC-3: a check that does raise one is named, together with the content it objected to and the
- [ ] AC-4: the same is asserted for every manifest entry recorded as being handed its files, so a

## Implementation Notes

```yaml
- THE SWEEP EXECUTES CHECKS AS PROCESSES. Reuse GE-120c-1's out-of-process harness
  (unit_tests/portability/) so each check runs from the deployed layout with the source
  tree off its import path. The record is a claim about runtime behaviour; importing
  the check module to inspect it is exactly the reading-the-record move this AC exists
  to replace.
- SELF-DEMONSTRATION IS A DELIVERABLE. Plant a check whose manifest entry claims it
  is handed its files while it derives its own change set, and show the sweep FAILS
  on it. A sweep that has never been observed to fail is not evidence — the same standard
  GE-120c-1 sets for its harness. The planted check must be a fixture the sweep discovers
  through the manifest, not a special case coded into the assertion.
- SUBJECT SET IS THE COMPLEMENT, DERIVED NOT LISTED. Iterate every manifest entry
  recorded as handed-its-files, read from GE-120e-2's manifest reader at run time.
  No hand-written check list and no hard-coded entry count — a new hook joins the
  sweep by being registered, not by anyone remembering.
- 'THE FAILURE REPORT MUST NAME THREE THINGS: the check, the content it objected to,
  and the state that content arrived from. ''Check X failed'' is not actionable and
  does not distinguish this defect from an unrelated check failure.'
- RUNTIME. Roughly fifty processes per sweep today. Build the fixture repository ONCE
  per sweep, never per check, and mark the sweep slow rather than letting it quietly
  narrow its subject list to stay fast.
- DO NOT DUPLICATE GE-120e-3. That AC's differential pair covers the GOVERNED set;
  this sweep covers the complement. If the two ever iterate the same entries, one
  of them is reading the record wrongly — surface that as a failure rather than tolerating
  the overlap.
- IF A SHARED REGISTRY-CONFORMANCE MECHANISM EMERGES from GE-120a-2-i or GE-120c-3,
  use it rather than adding a fourth private manifest sweep. Record which was chosen.
- 'Error-handling policy applies to the sweep''s own subprocess and filesystem calls:
  specific exceptions, logged at WARNING, never swallowed, and a setup failure is
  a reported failure rather than a skipped case (GE-120c-1-i).'
```

## Agent Contracts

### Expects From

- **AC:** GE-120e-2
- **Contract:** The recorded change-set source on each manifest entry, and the manifest-derived candidate set.

## Sign-offs

- [x] test-writer — 2026-08-25 20:52
- [ ] test-runner
- [x] commit — 2026-08-25 22:10

## Comments

### 2026-08-25 20:52 — test-writer (status: ok)
feedback-id: fb_2026-08-25_9057159a
completion_manifest:
  tests_written: true
  red_baseline_captured: true
  dependency_gap_flagged: true
Wrote unit_tests/portability/test_ge_120e_2_i.py with the five tests named in
GE-120e-2-i's AC-store test_spec (authoritative over the ticket body, which
carries no `## Test Requirements` block — that absence is expected here since
`assigned_agent: test-writer` on this AC means test-writer is the sole
implementer of this verification apparatus, per the same rationale GE-120c-1's
notes give for the identical shape). Neither of this AC's two real
dependencies exist yet in the repo: GE-120c-1's out-of-process harness
(unit_tests/portability/harness.py — confirmed absent) and GE-120e-2's
manifest determination callable (no `change_set_source` key anywhere in
templates/scripts/commit_guardian/commit_guardian.json, no
scripts/commit_guardian/change_set_source.py — confirmed absent). All 5 tests
therefore fail via an explicit `self.fail()` naming the missing dependency —
verified as genuinely RED by running `AC_ENFORCE_STRICT=1 python3 -m pytest
unit_tests/portability/test_ge_120e_2_i.py -v` (plain `pytest` masks these as
`xfail`/exit 0 via the repo's `pytest_ac_enforcement` plugin since this AC's
`work_status` is not yet `done` — a known masking behaviour, not a real
green; STRICT mode is required to see the true failures). The test file's
docstring records the exact module paths and function signatures this
ticket's tests expect GE-120c-1's and GE-120e-2's implementers to honour
(`scripts.commit_guardian.change_set_source.determine_change_set_sources`,
`unit_tests.portability.harness`), plus the `change_set_source` field
vocabulary (`handed_by_commit_path` / `self_derived`) — these are proposed
contracts, not confirmed ones, since no coder is dispatched on this ticket
to consult. Also flagging a ticket-authoring gap: this ticket's own
`depends_on:` frontmatter is empty, even though the AC store correctly
records `depends_on: [GE-120e-2]` for GE-120e-2-i — not fixed here as it is
out of this phase's scope.

red_baseline:
  - test_name: test_ge120e2i_handed_files_check_raises_no_objection_to_carried_in_work
    file: unit_tests/portability/test_ge_120e_2_i.py
    error: "AssertionError: scripts.commit_guardian.change_set_source.determine_change_set_sources does not exist yet — GE-120e-2 has not landed. This test cannot identify the handed-its-files complement until it does."
  - test_name: test_ge120e2i_misrecorded_check_is_named_and_the_sweep_fails
    file: unit_tests/portability/test_ge_120e_2_i.py
    error: "AssertionError: Cannot demonstrate the sweep failing on a planted misrecorded check until both scripts.commit_guardian.change_set_source.determine_change_set_sources (GE-120e-2) and unit_tests.portability.harness (GE-120c-1) exist."
  - test_name: test_ge120e2i_sweep_covers_every_handed_files_manifest_entry
    file: unit_tests/portability/test_ge_120e_2_i.py
    error: "AssertionError: scripts.commit_guardian.change_set_source.determine_change_set_sources does not exist yet — GE-120e-2 has not landed. The derived-complement property cannot be exercised until it does."
  - test_name: test_ge120e2i_failure_report_names_objected_content_and_originating_state
    file: unit_tests/portability/test_ge_120e_2_i.py
    error: "AssertionError: Cannot verify the three-part failure report until both scripts.commit_guardian.change_set_source.determine_change_set_sources (GE-120e-2) and unit_tests.portability.harness (GE-120c-1) exist."
  - test_name: test_ge120e2i_setup_failure_is_reported_as_a_failure_not_a_skip
    file: unit_tests/portability/test_ge_120e_2_i.py
    error: "AssertionError: unit_tests.portability.harness does not exist yet — GE-120c-1's out-of-process harness has not landed. The setup-failure reporting contract cannot be exercised until it does."
    note: "Verified red via `AC_ENFORCE_STRICT=1 pytest`; plain pytest run reports these as xfail (exit 0) due to pytest_ac_enforcement masking non-done ACs — do not treat plain-run exit 0 as green."

### 2026-08-25 21:30 — commit (status: blocker)
feedback-id: fb_2026-08-25_9062f7ce
completion_manifest:
  pre_commit_hooks_pass:
    result: failed
    reason: "check-build-drift blocks every commit attempt in this shared worktree right now (RESULT gaps=155 then 156 across two attempts), independent of this ticket's diff."
    remediation: "Not this phase's to fix — see blocker narrative below. Needs either serialized commits in this worktree or a build.py run to register the concurrently-staged templates."
  commit_message_valid: true
  ticket_staged: false
Re-ran `AC_ENFORCE_STRICT=1 python3 -m pytest unit_tests/portability/test_ge_120e_2_i.py -q`
immediately before attempting the commit and confirmed the red baseline
test-writer reported still holds unchanged (all 5 tests fail via explicit
`self.fail()` naming the same two missing dependencies:
`scripts.commit_guardian.change_set_source` and
`unit_tests.portability.harness`). No coder phase ran or was expected on
this ticket: its two real dependencies (GE-120c-1's harness, GE-120e-2's
manifest-determination callable) do not exist yet, so this ticket's own
scope ends at a red test file per its `assigned_agent: test-writer` framing.

BLOCKER — could not commit. Staged `unit_tests/portability/test_ge_120e_2_i.py`
plus this ticket file via `git add <name>` (not `-A`) and attempted
`git commit -m ... -- <those two paths>`, twice. Both attempts were rejected
by the `check-build-drift` pre-commit hook (exit 2), NOT because of anything
in this ticket's diff: `git commit -- <pathspec>` restricts which changes
land in the resulting commit, but pre-commit hooks still evaluate
`git diff --cached` against the FULL staged index, not the pathspec subset —
so the hook saw every other ticket's concurrently-staged work in this shared
worktree too. Between the two attempts the staged index visibly grew (14 to
20 files, e.g. `templates/scripts/commit_guardian/check_outcome.py`,
`check_ac_parent_covered_by.py`, `frontmatter_validators.py`,
ticket 36/GE-120e-4-i, `test_ge_120e_4_i.py` all appeared mid-flight from
other concurrently-running agents), and the drift count grew from
`gaps=155` to `gaps=156` between the two commit attempts — real,
legitimate drift (other tickets edited `templates/scripts/commit_guardian/*.py`
without a `build.py` run to register them in `.build_manifest.json`), just
not drift this ticket introduced or can fix without touching other agents'
in-flight work. Also note the hook itself runs from the `.leafcutter` symlink
target, which resolves to the sibling main clone
(`/home/henzeh/projects/leafcutter/leafcutter-ai`), not this worktree — so
"gaps" are reported against that main clone's manifest state, adding a second
layer of indirection to an already-noisy signal.

Did NOT use `--no-verify` or `SKIP=check-build-drift` — no live user
authorized a hook bypass in this session, and per this agent's protocol that
authorization must be personal and cannot be inferred from ticket dispatch.
Reverted the premature sign-off (checkbox + `agents.commit`) I had written
before confirming the commit landed, since `git log -1` still shows HEAD
unmoved (`498b1641`) — no commit was created.

Recommended remediation for whoever picks this back up: either (a) serialize
commits in this worktree — one ticket's commit phase completes and its
consumer clears the shared index before the next starts — or (b) have the
agent currently editing `templates/scripts/commit_guardian/*.py` run
`build.py` to refresh `.build_manifest.json` before any further commit
attempts in this worktree, or (c) get explicit user authorization for
`SKIP=check-build-drift` if the drift is confirmed to be entirely other
tickets' legitimate in-flight work rather than a real regression.

### 2026-08-25 22:10 — commit (status: ok)
feedback-id: fb_2026-08-25_e8e18ea3
completion_manifest:
  pre_commit_hooks_pass: true
  commit_message_valid: true
  ticket_staged: true
Retried the Step 0a pre-commit hook probe before doing anything else this
pass, since the prior blocker's own narrative flagged that the
`verify_precommit_active.py` orchestrator resolves paths from
`Path.cwd()` (not argv), and the initial `git_hook` check in this session
read `false` while `binary`/`config`/`canary` all read `true` — an
inconsistent pattern for a genuinely-uninstalled hook, since an uninstalled
hook normally fails `config` too. Root cause: the probe was invoked with
cwd = `/home/henzeh/projects/leafcutter` (the build-output workspace root,
not a git repo, per this session's own environment banner), so
`_resolve_git_commondir` had no `.git` to find and `check_c_git_hook`
failed closed on a directory that was never the subject under test.
Re-ran the identical probe with cwd explicitly set to this worktree's root
(`env --chdir=<worktree-root> python3 .../verify_precommit_active.py`) and
got `{"binary": true, "config": true, "git_hook": true, "canary": true,
"failing_checks": []}` — clean. This is a false-negative in how *this
agent invoked* the probe, not a defect in the probe or in this worktree's
hook installation; noting it here in case another agent hits the same
cwd trap.

Re-ran `AC_ENFORCE_STRICT=1 python3 -m pytest
unit_tests/portability/test_ge_120e_2_i.py -q` immediately before
committing and confirmed the red baseline is unchanged from both prior
sign-offs: all 5 tests still fail via the same explicit `self.fail()`
calls naming `scripts.commit_guardian.change_set_source` and
`unit_tests.portability.harness` as the missing dependencies — no drift,
no accidental green.

Staged only this ticket's two files by name (`git add
unit_tests/portability/test_ge_120e_2_i.py` and this ticket file — not
`git add -A`) and committed with `git commit -- <those two paths>`,
following this shared worktree's established pattern for keeping other
tickets' concurrently-staged work out of this commit's content while
still letting pre-commit hooks see the full staged index (a pre-existing,
documented worktree behaviour, not something this phase can or should
change). The commit succeeded — `check-build-drift` and all other hooks
passed this time, unlike the prior attempt's `gaps=155`/`156` blocker;
the drift that blocked that attempt was evidently resolved by another
agent's `build.py` run in the interim. Verified `git log -1` moved to a
new SHA containing exactly these two files before reporting done.
