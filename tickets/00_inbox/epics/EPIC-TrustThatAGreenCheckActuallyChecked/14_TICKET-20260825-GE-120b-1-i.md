---
ac_traceability:
  id: GE-120b-1-i
  path: docs/acceptance-criteria/guardrail-engine/GE-120-green-means-checked/GE-120b-1-i.yaml
advances_current_outcome: true
agents:
  ac-fulfillment-gate: needed
  ac-validator: needed
  architect-review: needed
  commit: needed
  documentation-expert: needed
  documentation-verifier: needed
  pr-reviewer: needed
  pull-request: not_needed
  python-coder: needed
  test-runner: needed
  test-writer: needed
change_target: code
complexity: low
components:
- commit_guardian
- precommit_hooks
created: '2026-08-25'
depends_on:
- 04_TICKET-20260825-GE-120a-2.md
documentation_required: true
files_touched:
- scripts/build.py
- scripts/commit_guardian
- templates/scripts/commit_guardian
- templates/scripts/commit_guardian/check_ac_parent_covered_by.py
priority: high
requires_adr: false
requires_diagram: false
risk_surface: contract_boundary
roadmap_phase: phase_1
source_ac: GE-120b-1-i
status: todo
title: A working copy with no deployed layout at all still does not pass silently
---

# A working copy with no deployed layout at all still does not pass silently

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `GE-120b-1-i` — A working copy with no deployed layout at all still does not pass silently — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `GE-120b-1-i`. Component: `guardrail-engine`. Assigned agent: `python-coder`. Estimated complexity: `S`. Complexity: `low`.

## Acceptance Criteria

```gherkin
Given a working copy that exposes neither the deployed layout nor any built layout it
  could be resolved from, because the package has never been built in that workspace,
When the AC-parent-covered-by check is executed there against staged files that violate the
  parent covered_by rule in 6 places,
Then the check either reaches the same blocked verdict as the main checkout, or reports the
  could-not-check outcome defined by GE-120a-1,
And in neither case does it report an ordinary clean pass,
And its output names the missing prerequisite in terms the reader can act on — that the
  workspace has not been built — rather than naming an internal module that failed to
  import,
And which of the two outcomes occurs is a stated, reviewable consequence of the check's
  recorded cannot-run disposition (GE-120a-2), not an accident of where resolution
  happened to fail.

Coverage note: exercise this against a working copy in which the deployed layout genuinely
does not exist. Simulating it by deleting one file from an otherwise built layout tests a
different condition.
```

- [ ] AC-1: the check either reaches the same blocked verdict as the main checkout, or reports the
- [ ] AC-2: in neither case does it report an ordinary clean pass
- [ ] AC-3: its output names the missing prerequisite in terms the reader can act on — that the
- [ ] AC-4: which of the two outcomes occurs is a stated, reviewable consequence of the check's

## Test Requirements

```yaml
tests:
- name: test_ge120b1i_never_built_copy_does_not_report_clean_pass
  file: unit_tests/portability/test_ge_120b_1_i.py
  covers:
  - GE-120b-1-i
  asserts: 'Working copy with no deployed and no built layout, 6 violating staged
    files: the result is blocked or could-not-check, never an ordinary clean pass.'
  framework: unittest
  type: behavioral
- name: test_ge120b1i_outcome_follows_the_recorded_disposition
  file: unit_tests/portability/test_ge_120b_1_i.py
  covers:
  - GE-120b-1-i
  asserts: Which of the two acceptable outcomes occurs changes when the recorded cannot-run
    disposition changes, and not otherwise.
  framework: unittest
  type: behavioral
- name: test_ge120b1i_message_names_unbuilt_workspace_not_missing_module
  file: unit_tests/portability/test_ge_120b_1_i.py
  covers:
  - GE-120b-1-i
  asserts: The output names the unbuilt workspace and the resolving command rather
    than an internal module import failure.
  framework: unittest
  type: behavioral
- name: test_ge_120b_1_i_reachable_from_entry_point
  file: unit_tests/portability/test_ge_120b_1_i.py
  covers:
  - GE-120b-1-i
  angle: reachability
  asserts: 'REQUIRED — invoke the production entry point (CLI, hook, slash command,
    workflow dispatch, or main()) as a subprocess/dispatch and assert the new behaviour
    actually occurs. Do NOT satisfy this by importing the function directly. The AC
    authored no test_spec, so the entry point is not declared: resolve it before writing
    this test, and do not delete this entry.'
```

## Implementation Notes

```yaml
- 'Define the bare case precisely: a workspace where ''python scripts/build.py --target-dir
  .'' has never run — no .leafcutter/ directory and no scripts/commit_guardian/ deployed
  copy, only templates/scripts/commit_guardian/. This is DISTINCT from a built-but-incomplete
  workspace where the layout exists and one artifact is absent; the classifier must
  discriminate on the presence of the LAYOUT, not on the absence of whichever single
  file was looked up first.'
- Message wording is a criterion, not polish. It names the actionable cause — the
  workspace has not been built, and the single command that fixes it ('python scripts/build.py
  --target-dir <workspace>', or ./build-self.sh in the package's own workspace) —
  instead of the internal symptom 'ac_parent_id.py not found via package import, script-relative,
  or project-root walk'.
- Which of the two acceptable outcomes occurs must be READ FROM the recorded cannot-run
  disposition for check-ac-parent-covered-by (GE-120a-2), not decided by where resolution
  happened to fail. Two different resolution failures under the same disposition must
  produce the same outcome; if they do not, the disposition is decoration.
- 'An ordinary clean pass is FORBIDDEN in this state under either disposition. This
  is the seam between the two L1s: without this clause an implementer can satisfy
  GE-120b-1 by making resolution succeed in the linked and buildable cases while leaving
  the bare case exiting 0 exactly as before, with both parent ACs'' letter met.'
- Share the cause-detection helper with GE-120d-2-i, which draws the same never-built
  vs built-but-incomplete line on the set-up side. Two independent classifiers for
  one distinction will drift.
- 'ADR-001 deployed/template parity: edit templates/scripts/commit_guardian/ then
  run ''python scripts/build.py --target-dir .''; scripts/commit_guardian/ and .leafcutter/scripts/commit_guardian/
  must match afterwards.'
```

## Agent Contracts

### documentation-expert

Existing docs to update / cross-link:

- templates/scripts/commit_guardian/check_ac_parent_covered_by.py (relationship: modifies | status: exists)
- docs/how-to/managing-pre-commit-hooks.md (relationship: related | status: exists)
- docs/architecture/adrs/ADR-001-self-hosting-boundary.md (relationship: context | status: exists | relevance: Defines the built workspace this AC's never-built case is the absence of.)

- [ ] AC-1: (unspecified genre) | templates/scripts/commit_guardian/check_ac_parent_covered_by.py | the check either reaches the same blocked verdict as the main checkout, or reports the

### Expects From

- **AC:** GE-120a-2
- **Contract:** The recorded cannot-run disposition for this check, which selects between the two acceptable outcomes.

## Sign-offs

- [ ] architect-review
- [ ] test-writer
- [ ] python-coder
- [ ] test-runner
- [ ] documentation-expert
- [ ] pr-reviewer
- [ ] ac-validator
- [ ] ac-fulfillment-gate
- [ ] documentation-verifier
- [ ] commit

## Comments
