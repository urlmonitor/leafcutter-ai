---
ac_traceability:
  id: BP-1400c-1-i
  path: docs/acceptance-criteria/build_pipeline/BP-1400-web-app-ci-gate/BP-1400c-1-i.yaml
advances_current_outcome: true
agents:
  ac-fulfillment-gate: needed
  ac-validator: needed
  architect-review: needed
  commit: needed
  pr-reviewer: needed
  pull-request: needed
  python-coder: needed
  test-runner: needed
  test-writer: needed
change_target: pipeline
complexity: low
components:
- build_pipeline
created: '2026-07-21'
depends_on:
- 08_TICKET-20260721-BP-1400c-1.md
files_touched:
- .github/workflows/ci.yml
priority: high
requires_adr: false
requires_diagram: false
risk_surface: safety
roadmap_phase: phase_1
source_ac: BP-1400c-1-i
status: todo
title: The /about route is loaded headlessly and a non-200 or render error on it blocks
  the pull request
---

# The /about route is loaded headlessly and a non-200 or render error on it blocks the pull request

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BP-1400c-1-i` — The /about route is loaded headlessly and a non-200 or render error on it blocks the pull request — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BP-1400c-1-i`. Component: `build-pipeline`. Assigned agent: `python-coder`. Estimated complexity: `S`. Complexity: `low`.

## Acceptance Criteria

```gherkin
Given a pull request that changes the web app (under leafcutter-web/), and the
  web app exposes the /about route,
When the web-app route-render check runs and headlessly loads /about,
Then a /about response with a non-200 HTTP status, or any console error or
  render error emitted while /about loads, makes the check report a failing
  result and the pull request is reported as not mergeable,
And when /about loads with a 200 status and no console or render error, the
  check reports /about as passing.
```

- [ ] AC-1: a /about response with a non-200 HTTP status, or any console error or
- [ ] AC-2: when /about loads with a 200 status and no console or render error, the

## Test Requirements

```yaml
tests:
- name: test_about_route_smoke
  file: leafcutter-web/tests/test_bp_1400c_1_i.py
  covers:
  - BP-1400c-1-i
  asserts: 'The route-smoke harness loads /about headlessly: a non-200 or any console/render
    error fails the run (check reports failing); a 200 /about with no console/render
    error passes.'
  framework: playwright
  type: e2e
```

## Implementation Notes

```yaml
- 'Exercises the per-route pass/fail behavior of the route-smoke harness (BP-1400c-1)
  on the concrete /about route: the harness must headlessly load /about, and a non-200
  status OR any console/render error emitted while /about loads must drive a failing
  result (non-zero exit), while a clean 200 /about with no console/render error passes.'
- /about must be an actual route the harness enumerates — verify it is covered by
  the route-enumeration source (leafcutter-web/app/about) so it cannot be silently
  dropped from the swept set.
- Any deliberately-broken /about used to verify the failing direction must be transient/scoped
  to the verification (throwaway PR or temporary edit removed before merge) so it
  does not become a standing red; this AC verifies the GATE, it does not ship a broken
  /about.
- Not-mergeable enforcement assumes the required-status-check wiring in BP-1400a-4.
```

## Agent Contracts

### Expects From

- **AC:** BP-1400c-1
- **Contract:** The blocking web-app route-render check whose per-route pass/fail behavior this case exercises on /about

## Sign-offs

- [ ] architect-review
- [ ] test-writer
- [ ] python-coder
- [ ] test-runner
- [ ] pr-reviewer
- [ ] ac-validator
- [ ] ac-fulfillment-gate
- [ ] commit
- [ ] pull-request

## Comments
