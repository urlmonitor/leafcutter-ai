amended_by: []
assigned_agent: python-coder
change_target: code
component: ticket-creation
components:
- ticket_creation_pipeline
covered_by: []
created: 2026-08-11
criteria: "Given generate_ticket_from_ac generating a ticket from an AC,\nWhen it\
  \ builds the ticket frontmatter,\nThen files_touched contains only real edit-surface\
  \ paths (the it_requirements\n  reference_file_path and edit-surface doc_links)\
  \ and NOT illustrative file paths\n  that merely appear inside prose it_requirements\
  \ bullets (e.g. example paths like\n  src/foo.py / deploy/foo.py used to describe\
  \ a scenario),\nAnd depends_on written to the ticket is guard-valid: an AC dependency\
  \ that is the\n  AC's structural parent (or any AC id with no corresponding ticket\
  \ in scope) is\n  dropped or translated to a co-located ticket filename, so the\
  \ generated ticket\n  passes ticket_frontmatter_guard without manual fixup,\nAnd\
  \ a standalone leaf AC whose only depends_on is its structural parent yields a\n\
  \  ticket with depends_on: [] (not a dangling AC-id reference).\n"
delivers_to: null
depends_on:
- TKT-600a
doc_links:
- path: scripts/ac_store/generate_ticket_from_ac.py
  relationship: modifies
  status: exists
- path: templates/skills/build-ac/SKILL.md
  relationship: related
  status: exists
estimated_complexity: M
expects_from: null
id: TKT-600a-1
implemented_by: []
it_requirements:
  config_schema_fragment: null
  n_location_rule: '2'
  notes: 'Two independent defects in generate_ticket_from_ac.py, both observed live
    during

    the BO-2600 build:

    (1) files_touched over-extraction: _build_files_touched -> _extract_paths_from_prose

    pulls any ''/''-containing token with a known extension out of list-form

    it_requirements bullets, so illustrative example paths (e.g. src/foo.py,

    deploy/foo.py, src/config/foo.py used only to describe an exploit scenario) land
    in

    files_touched. Restrict prose extraction (e.g. only accept the structured

    reference_file_path + edit-surface doc_links; do not harvest arbitrary paths from

    narrative bullets), or gate on file existence.

    (2) depends_on passthrough: _build_frontmatter writes `ac.get("depends_on") or
    []`

    verbatim (AC ids), which ticket_frontmatter_guard rejects because ticket depends_on

    must reference co-located ticket files. Drop the structural-parent AC id

    (derive_parent_id) and translate remaining AC-id deps to their generated ticket

    filenames when in an epic context, else emit [].

    Preserve existing behavior for ACs that already produce correct output.

    '
  post_write_commands: []
  reference_file_path: scripts/ac_store/generate_ticket_from_ac.py
  required_skills:
  - python-coder
level: L2
origin_agent: BrainCandy
priority: medium
readiness: draft
req_status: draft
risk_surface: contract_boundary
roadmap_phase: phase_1
status: active
superseded_by: null
test_spec:
- description: An AC whose prose it_requirements mention example paths yields files_touched
    WITHOUT those illustrative paths
  framework: pytest
  name: test_files_touched_excludes_prose_illustration_paths
  target_dir: tests/ac_store/
  type: unit
- description: The structured reference_file_path and edit-surface doc_links still
    appear in files_touched
  framework: pytest
  name: test_files_touched_keeps_real_reference_file_path
  target_dir: tests/ac_store/
  type: unit
- description: 'A leaf AC whose only depends_on is its structural parent yields ticket
    depends_on: []'
  framework: pytest
  name: test_depends_on_drops_structural_parent
  target_dir: tests/ac_store/
  type: unit
- description: Generated ticket depends_on never contains a raw AC id that has no
    co-located ticket (passes ticket_frontmatter_guard)
  framework: pytest
  name: test_depends_on_is_guard_valid
  target_dir: tests/ac_store/
  type: unit
title: Generated files_touched excludes prose-illustration paths; depends_on is guard-valid
work_status: done
