---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  pr-reviewer: needed
  pull-request: needed
  python-coder: signed_off
  sql-coder: not_needed
  test-runner: signed_off
  test-writer: signed_off
change_target: pipeline
components:
- build_pipeline
created: '2026-06-11'
depends_on: []
files_touched:
- scripts/build_propagation_audit.py
- docs/architecture/components/template-compiler.md
priority: critical
requires_adr: false
requires_diagram: false
risk_surface: internal
roadmap_phase: phase_1
source_ac: BP-900c-1
status: todo
title: Each broken-reference entry names the missing script, the referencing template,
  and a suggested action
---

# Each broken-reference entry names the missing script, the referencing template, and a suggested action

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BP-900c-1` — Each broken-reference entry names the missing script, the referencing template, and a suggested action — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BP-900c-1`. Component: `build_pipeline`. Assigned agent: `python-coder`. Estimated complexity: `S`.

## Acceptance Criteria

```gherkin
Given the build-time guard has detected that "scripts/ac_store/ac_prioritizer.py"
is referenced in the compiled template "agents/build-ac.md"
but is not in the deployable script set,
When the guard emits the error report,
Then the report entry for this reference contains all three fields:
  (a) the missing path: "scripts/ac_store/ac_prioritizer.py"
  (b) the referencing template: "agents/build-ac.md"
  (c) a suggested action: one of "add a deploy phase in build_phases.py"
      or "add to the external-dependency allowlist"
And no field is empty or omitted
```

## Sign-offs

- [x] python-coder — 2026-08-18 08:18
- [x] test-writer — 2026-08-18 09:15
- [x] test-runner — 2026-08-18 09:45
- [ ] pr-reviewer
- [ ] commit
- [ ] pull-request

## Comments

### 2026-08-18 09:15 — test-writer (status: ok)
feedback-id: (submit-failed)
completion_manifest:
  tests_written: true
  red_baseline_confirmed: true
No `## Test Requirements` section was present (v2.0.0 gap per known project
history), so per the Skip rule I derived the test contract from
`source_ac: BP-900c-1` directly (`docs/acceptance-criteria/build_pipeline/
BP-900-deployment-completeness/BP-900c-1.yaml`), using its `criteria` Gherkin
and `it_requirements.constraints` (no `test_spec` field was present).

Wrote `unit_tests/build_guards/test_bp900c1_broken_ref_entry_fields.py` with
two tests tagged `# covers: BP-900c-1`:
1. `test_ac_bp900c1_entry_has_all_three_nonempty_fields` — schema-level test
   using the AC's own literal example (`scripts/ac_store/ac_prioritizer.py`
   referenced by `agents/build-ac.md`) against the real, unmocked
   `build_broken_ref_report()` in `scripts/build_propagation_audit.py`.
   **Source-of-Truth classification (ADR-003 Rule 1): (classification:
   consumer_drift).** The AC's Gherkin criteria illustrates `suggested_action`
   with exactly two example values, but `build_broken_ref_report()` already
   ships a THIRD value (`ACTION_COMMIT_UNDER_TEMPLATES`) added by a later AC,
   BP-900c-3 (PR #186) — which this AC's own YAML `notes` field explicitly
   authorizes ("those ACs change which value is chosen, not the schema of the
   entry") and which `it_requirements.constraints` corroborates ("one of a
   finite set of known remediation options", not "exactly one of two"). Per
   Rule 5 (prefer expanding the test over shrinking production), the test
   asserts membership in the module's exported action constants (currently
   three) rather than hardcoding the stale two-value literal — this correctly
   passes today (`CODE_NO_TEST` backfill: production for this AC's schema
   shipped under BP-900c-3, mirroring the existing precedent in
   `unit_tests/build_guards/test_bp_stragglers_backfill.py` for BP-900c-1-1).
2. `test_ac_bp900c1_architecture_doc_documents_broken_ref_entry_schema` —
   asserts `docs/architecture/components/template-compiler.md` (one of this
   ticket's `files_touched` targets) names `BrokenRefEntry`, `missing_path`,
   `referencing_template`, and `suggested_action`. **This test is genuinely
   RED**: the doc currently has no section describing the broken-reference
   report entry schema at all (confirmed by direct grep before authoring the
   test). This is the real remaining work item for `python-coder` on this
   ticket.

### Verification Run
- Command: `python3 -m pytest unit_tests/build_guards/test_bp900c1_broken_ref_entry_fields.py -q`
- Result: 1 failed, 1 passed — exit code 1 (red, as required).

### Anomaly (pre-existing, out of scope for this phase)
`ticket_frontmatter_guard.py` fired a PostToolUse warning on this ticket
(pre-existing, not introduced by this edit): `depends_on: [BP-900c]` has no
matching ticket file in this epic folder (only child-AC tickets 900c-1,
900c-1-1, 900c-2 exist — no 900c parent ticket), and `change_target` /
`risk_surface` are absent from frontmatter even though the source AC YAML has
`change_target: pipeline` / `risk_surface: internal`. This matches the known
`generate_ticket_from_ac.py` generator-quirk pattern (project memory:
`project_build_ac_generator_quirks.md`). Left unresolved as out of scope for
test-writer; flagging for `python-coder` / `commit` to address before the
ticket can pass `check_ticket_signoff_parity` / frontmatter validation at
commit time.

### 2026-08-18 08:18 — python-coder (status: ok)
feedback-id: (submit-failed)
completion_manifest:
  ac_bp900c1_implemented: true
  red_baseline_test_turned_green: true
  no_python_source_change_needed: true
  frontmatter_anomaly_fixed: true
red_baseline_results:
  - test_name: test_ac_bp900c1_architecture_doc_documents_broken_ref_entry_schema
    result: green
  - test_name: test_ac_bp900c1_entry_has_all_three_nonempty_fields
    result: green (was already green pre-existing production; confirmed still green)

Investigated `scripts/build_propagation_audit.py`: the `BrokenRefEntry`
dataclass and `build_broken_ref_report()` factory already implement AC
BP-900c-1's three-field schema in full (they were authored under BP-900c-3,
per test-writer's `consumer_drift` classification) — no Python source change
was required or made. The only remaining gap was documentation: added a new
"Broken-Reference Report Entry Schema (AC BP-900c-1)" section to
`docs/architecture/components/template-compiler.md` naming `BrokenRefEntry`,
`missing_path`, `referencing_templates`/`referencing_template`, and
`suggested_action` (including all three `ACTION_*` constants), which turns
the previously-red doc-coverage test green. Verified with
`python3 -m pytest unit_tests/build_guards/test_bp900c1_broken_ref_entry_fields.py -q`
→ 2 passed, and the full `unit_tests/build_guards/` suite → 75 passed, 4
subtests passed (no regressions). `submit_feedback.py` is absent from this
worktree's `scripts/feedback/` (only present under `templates/scripts/feedback/`,
consistent with the shim-deployment pattern noted in project memory), so
feedback submission failed as expected; using the `(submit-failed)` fallback.
Also resolved the frontmatter anomaly test-writer flagged: removed the
dangling `depends_on: [BP-900c]` (no such ticket exists in this epic folder;
replaced with `depends_on: []`) and added `change_target: pipeline` /
`risk_surface: internal` from the source AC YAML
(`docs/acceptance-criteria/build_pipeline/BP-900-deployment-completeness/BP-900c-1.yaml`),
so `check_ticket_signoff_parity` / `ticket_frontmatter_guard` no longer block
this ticket at commit time. No `## Agent Contracts` section is present, so
this is a v1 ticket — §2c AC Coverage Sign-Off was skipped per the v1/v2
detection rule.
### 2026-08-18 09:45 — test-runner (status: ok)
feedback-id: (submit-failed)
completion_manifest:
  test_suite_executed: true
  all_tests_passing: true
  failure_report_structured: true

Ran `unit_tests/build_guards/test_bp900c1_broken_ref_entry_fields.py` directly
(no CI wrapper available in this worktree layout) — both tests pass: the
schema test (`test_ac_bp900c1_entry_has_all_three_nonempty_fields`) and the
previously-red doc-coverage test
(`test_ac_bp900c1_architecture_doc_documents_broken_ref_entry_schema`), which
python-coder's doc addition to `docs/architecture/components/template-compiler.md`
turned green. Also reran the full `unit_tests/build_guards/` suite for
regressions: 75 passed, 4 subtests passed, 0 failures. No source change was
needed this phase; signing off clean.
