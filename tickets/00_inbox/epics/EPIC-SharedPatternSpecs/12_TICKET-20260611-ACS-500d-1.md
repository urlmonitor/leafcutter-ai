---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  pr-reviewer: needed
  pull-request: needed
  python-coder: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
components:
- ac_store
created: '2026-06-11'
depends_on:
- ACS-500d
- ACS-500b-1
files_touched:
- docs/reference/ac-schema.md
- docs/architecture/adrs/ADR-007-ac-store-schema-id-format-enforcement.md
- docs/architecture/adrs/ADR-010-ac-store-as-authoritative-backlog.md
priority: high
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: ACS-500d-1
status: todo
title: Updating a pattern AC's criteria changes effective behavior for all consumers
---

# Updating a pattern AC's criteria changes effective behavior for all consumers

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `ACS-500d-1` — Updating a pattern AC's criteria changes effective behavior for all consumers — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `ACS-500d-1`. Component: `ac-store`. Assigned agent: `python-coder`. Estimated complexity: `M`.

## Acceptance Criteria

```gherkin
Given three page-level ACs each have implements_pattern: "PTN-001"
  referencing a pattern that defines "paginated-collection" with
  criteria including "displays total count above the table",
When the pattern AC (PTN-001) is amended to change the criteria to
  "displays total count below the table",
Then each consuming AC inherits the updated behavior without any
  modification to its own YAML file,
And an agent reading any of the three consuming ACs resolves the
  effective behavior by following the implements_pattern reference
  to retrieve the current pattern criteria,
And no migration ticket or manual update to the three consuming
  ACs is required.
```

## Sign-offs

- [ ] python-coder
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit
- [ ] pull-request

## Comments
