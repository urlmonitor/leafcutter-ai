---
title: "Twenty-three known-issue entries are recovered from an unmerged branch and renumbered into main's register"
date: "2026-08-26"
time: "11:30"
type: manual
components: 
  - infrastructure
summary: "PR #495 built a parallel known-issues register that lost every reconciliation conflict and was discarded; a pairwise comparison found the two registers disjoint, so 23 entries of six review rounds' analysis were about to be lost with the branch."
description: "Recovers the branch-only content of PR #495's parallel known-issues register (its own id scheme, KI-CG-9 / KI-BO-4 / KI-TQ-6) into main's KI-<COMP>-NNN scheme, allocating upward from each register's highest existing number and never reusing one: commit-guardian KI-CG-021..031, build-orchestration KI-BO-030 and KI-BO-031, feedback-collector KI-FC-003, supervisor-system KI-SS-002..004, testing-quality KI-TQ-004..009. The evidence is carried across intact rather than summarised — reproduction commands, counts, file:line references, and the why-nothing-caught-it reasoning are what make these entries worth anything. Every claim was re-verified against main at 37655862 before filing, because the entries were written days ago against a branch that main has since moved ~90 commits past, and each Status line now states plainly whether the code it describes is live on main or exists only on the unmerged branch: an entry naming a file the reader cannot find is indistinguishable from a stale one. Two entries were dropped as no longer true rather than filed. The agent-card frontmatter defect is fixed — card is now a valid config/doc_types.json type and running check_doc_frontmatter.py against a real card exits 0. The origin/main-staleness test defect is fixed — that assertion was amended on 2026-08-18 to a one-directional id-set difference that tolerates main growing ahead. One entry needed correcting rather than copying: KI-CG-12 claimed check_adr_collision.py was 'registered, required, live on main today' and fails closed without origin/main, and both halves are false for main. The hook appears in none of the 55 entries in commit_guardian.json and has never been registered since the initial commit, and main's copy fails OPEN (its docstring says 'Empty on any error'; the body is 'if rc != 0: return set()'). The registration and the fail-closed conversion were both made on the branch, whose author then observed the consequence and recorded it as pre-existing. It is filed as KI-CG-022 at medium rather than high, framed as a latent blocker that becomes live when PR #495 lands, with the correction recorded in the entry. KI-SUP-1 needed the same treatment: the mechanism it named is closed on main (a blocker or failed phase result is intercepted before commit can be reached) but the same shape survives one branch over, where a verification-failed gate pushes to unverifiedPhases and the loop continues to commit at priority 12 with nothing consulting it — so KI-SS-002 files the live mechanism and keeps the superseded one visible. The two partially-covered entries were handled differently from each other on their merits: the agent-card drift is fully covered by build-pipeline.md's KI-BP-002, including the AC-store drift source, so it is folded in as an earlier fourth occurrence per that file's own rule rather than duplicated, and it contributes the earliest recorded instance plus a per-run cost of eight dirtied cards; the six-rounds-never-asked-what-invokes-the-code pattern overlaps KI-BO-011 and KI-BO-028 on the class but is a review-method defect rather than a test-pointed-at-dead-code defect, and its remedy (a registration test on every hook AC) is implied by neither, so it is filed as KI-TQ-007 with an explicit cross-reference. Also fixes a dangling reference that was live on main: docs/reference/architecture-docs-layout.md and BP-900h-6.yaml both cited KI-BO-4 and anchored into an entry that existed only on the branch, so the anchor resolved to nothing; those citations and three further ones in GE-122d-3-ii.yaml and two epic tickets are repointed at KI-BO-030. The branch's docs/known-issues/README.md is deliberately not introduced: main has no README for this directory, and the branch's indexed only 5 of main's 13 registers while citing ids that no longer exist."
breaking: false
---

## Entry
