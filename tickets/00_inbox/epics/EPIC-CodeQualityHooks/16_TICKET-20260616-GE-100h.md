---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  pr-reviewer: signed_off
  pull-request: needed
  python-coder: signed_off
  sql-coder: not_needed
  test-runner: signed_off
  test-writer: signed_off
components:
- guardrail-engine
created: '2026-06-16'
depends_on:
- GE-100
files_touched:
- docs/acceptance-criteria/guardrail-engine/GE-100-code-quality-hooks/GE-100.yaml
- docs/pre-commit-hooks.md
- docs/how-to/managing-pre-commit-hooks.md
priority: high
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: GE-100h
status: in_progress
title: Both hooks ship disabled in commit_guardian.json with correct hooks_manifest
  entries
---

# Both hooks ship disabled in commit_guardian.json with correct hooks_manifest entries

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `GE-100h` — Both hooks ship disabled in commit_guardian.json with correct hooks_manifest entries — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `GE-100h`. Component: `guardrail-engine`. Assigned agent: `python-coder`. Estimated complexity: `S`.

## Acceptance Criteria

```gherkin
Given the canonical commit_guardian.json is read after the hooks are added
When a consumer inspects the hooks_manifest.hooks array
Then it contains an entry for the duplicate code hook with:
  - id: "check-duplicate-code"
  - enabled: false
  - files pattern matching: "\.(py|ts|js|tsx|jsx|sql)$"
  - pass_filenames: false
  - stages: ["pre-commit"]
And it contains an entry for the diff coverage hook with:
  - id: "check-diff-coverage"
  - enabled: false
  - files pattern matching: "\.(py|sql)$"
  - pass_filenames: false
  - stages: ["pre-commit"]
And the top-level JSON contains a "duplicate_code" configuration section with:
  - enabled: false
  - strict: false
  - min_lines: 5
  - min_tokens: 50
  - threshold_percent: 5
  - checked_extensions: [".py", ".ts", ".js", ".tsx", ".jsx", ".sql"]
  - excluded_dirs: ["node_modules", ".venv", "__pycache__"]
And the top-level JSON contains a "diff_coverage" configuration section with:
  - enabled: false
  - strict: false
  - min_coverage_percent: 80
  - coverage_xml_path: "coverage.xml"
  - compare_branch: "origin/main"
  - max_age_seconds: 3600
```

## Sign-offs

- [x] python-coder — 2026-06-18 10:30
- [x] test-writer — 2026-06-18 09:00
- [x] test-runner — 2026-06-18 15:30
- [x] pr-reviewer — 2026-06-18 16:00
- [ ] commit
- [ ] pull-request

## Comments

### 2026-06-18 09:00 — ticket-supervisor (status: ok)
feedback-id: (submit-failed)
test_requirements empty — test-writer phase skipped (docs-only or config-only ticket)

### 2026-06-18 10:30 — python-coder (status: ok)
feedback-id: fb_2026-06-18_dc5e8955

AC GE-100h is satisfied. All three commit_guardian.json copies were updated:

1. `scripts/commit_guardian/commit_guardian.json` (deployed) — added `enabled: false` and `files: "\\.(py|ts|js|tsx|jsx|sql)$"` to the `check-duplicate-code` manifest entry; added `excluded_dirs` to the `duplicate_code` config section. The `check-diff-coverage` entry and `diff_coverage` config were already correct.

2. `templates/scripts/commit_guardian/commit_guardian.json` (canonical template) — same changes as above.

3. `templates/commit-guardian/commit_guardian.json` (deprecated, sync'd for completeness) — added both `check-duplicate-code` and `check-diff-coverage` manifest entries with correct fields; added both `duplicate_code` and `diff_coverage` top-level config sections.

AC store: `GE-100h.yaml` work_status updated to `done`; `GE-100.yaml` `implemented_by` updated to include `GE-100h`. Docs (`docs/pre-commit-hooks.md` and `docs/how-to/managing-pre-commit-hooks.md`) already document both hooks correctly — no changes needed.

### 2026-06-18 15:30 — test-runner (status: ok)
feedback-id: fb_2026-06-18_dd9bb1d1
completion_manifest:
  test_suite_executed: true
  all_tests_passing: true
  failure_report_structured: true

Suite: commit_guardian (direct). test_check_diff_coverage.py: 51 tests, 51 passed, 0 failures. All GE-100h AC values verified to match ticket ACs exactly. 11 pre-existing failures in test_transform_hooks_and_autofix_emission are unrelated to GE-100h.

### 2026-06-18 16:00 — pr-reviewer (status: ok)
feedback-id: fb_2026-06-18_5d0caee0
completion_manifest:
  hooks_manifest_check_duplicate_code: true
  hooks_manifest_check_diff_coverage: true
  duplicate_code_config_section: true
  diff_coverage_config_section: true
  json_validity_all_three_copies: true
  no_unintended_changes: true

All GE-100h ACs satisfied across all three commit_guardian.json copies. Both hook manifest entries carry correct `id`, `enabled: false`, `files` pattern, `pass_filenames: false`, and `stages: ["pre-commit"]`. Both top-level config sections (`duplicate_code`, `diff_coverage`) contain every required field with exact AC-specified values. All three JSON files parse clean. No high- or medium-confidence findings. Pre-existing `{{config.output_root}}` vs bare-path inconsistency in templates/scripts/commit_guardian/commit_guardian.json predates this diff and is out of scope for this ticket.

### 2026-06-18 17:00 — commit (status: ok)
feedback-id: fb_2026-06-18_0ceb3a7b
Auto-authorized commit gate: subject "feat(guardrail-engine): ship check-duplicate-code and check-diff-coverage hooks disabled (GE-100h)"; staged files: docs/acceptance-criteria/guardrail-engine/GE-100-code-quality-hooks/GE-100.yaml docs/acceptance-criteria/guardrail-engine/GE-100-code-quality-hooks/GE-100h.yaml scripts/commit_guardian/commit_guardian.json templates/commit-guardian/commit_guardian.json templates/scripts/commit_guardian/commit_guardian.json tickets/00_inbox/epics/EPIC-CodeQualityHooks/16_TICKET-20260616-GE-100h.md.
