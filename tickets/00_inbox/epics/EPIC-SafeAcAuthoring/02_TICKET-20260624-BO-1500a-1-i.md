---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  pr-reviewer: signed_off
  pull-request: needed
  python-coder: signed_off
  sql-coder: not_needed
  test-runner: signed_off
  test-writer: signed_off
components:
- build-orchestration
created: '2026-06-24'
depends_on:
- BO-1500a-1
files_touched:
- templates/skills/build-single-ticket/SKILL.md
- scripts/setup_ticket_worktree.py
priority: high
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: BO-1500a-1-i
status: in_progress
title: An existing authoring worktree/branch from a prior run is reused, not blindly
  recreated
---

# An existing authoring worktree/branch from a prior run is reused, not blindly recreated

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BO-1500a-1-i` — An existing authoring worktree/branch from a prior run is reused, not blindly recreated — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BO-1500a-1-i`. Component: `build-orchestration`. Assigned agent: `python-coder`. Estimated complexity: `M`.

## Acceptance Criteria

```gherkin
Given a previous authoring run for the same feature crashed and left behind
  an authoring worktree and its branch on disk,
When a new AC-authoring run for that same feature begins,
Then the workflow detects the pre-existing authoring worktree and branch
  before attempting to create a new one,
And it reuses the existing authoring worktree and branch instead of failing
  with a "worktree already exists" error,
And it does not delete or overwrite any AC YAML files already committed on
  that branch,
And if no usable prior worktree exists, it creates a fresh one as in the
  normal path.
```

## Sign-offs

- [x] python-coder — 2026-06-24 16:00
- [x] test-writer — 2026-06-24 00:00
- [x] test-runner — 2026-06-24 17:00
- [x] pr-reviewer — 2026-06-24 18:00
- [ ] commit
- [ ] pull-request

## Comments

### 2026-06-24 10:00 — ticket-supervisor (status: ok)
feedback-id: fb_2026-06-24_ticket-supervisor-skip
test_requirements empty — test-writer phase skipped (## Test Requirements block absent from ticket)

### 2026-06-24 16:00 — python-coder (status: ok)
feedback-id: fb_2026-06-24_2655f40e
completion_manifest:
  worktree_reuse_implemented: true
  branch_reuse_implemented: true
  no_yaml_overwrite: true
  fresh_path_preserved: true
  skill_updated: true
Implemented AC BO-1500a-1-i. Added `_branch_exists()` helper (git branch --list probe). Extended `_worktree_exists()` with `ac-authoring/` prefix support. Refactored `cmd_create_ac_worktree` to use `_worktree_exists()`, eliminating the inline duplicate worktree-list parser. Modified `_create_ac_worktree()` to detect branch-without-worktree scenario and select checkout-only `git worktree add <path> <branch>` form, preserving existing AC YAML commits. Updated build-single-ticket/SKILL.md with idempotent/reuse guarantee note. All three reuse scenarios covered: (1) worktree registered, (2) branch exists but worktree pruned, (3) fresh path.

### 2026-06-24 17:00 — test-runner (status: ok)
feedback-id: fb_2026-06-24_e4b8c235
completion_manifest:
  syntax_valid: true
  tests_pass: true
  no_regressions: true
Syntax check passed. 5 behavioral mock-tests of AC scenarios all pass (worktree-registered reuse, branch-without-worktree reuse, fresh creation). Existing suite tests/test_setup_ticket_worktree.py: 7 pass, 2 fail (pre-existing baseline failures in templates/scripts/ confirmed by stash-and-retest — not introduced by this ticket).

### 2026-06-24 18:00 — pr-reviewer (status: ok)
feedback-id: fb_2026-06-24_5b488be2
completion_manifest:
  ac_criteria_met: true
  error_handling_compliant: true
  all_scenarios_covered: true
  no_high_confidence_blockers: true
Reviewed diff for AC BO-1500a-1-i. All three scenarios verified: (1) worktree registered — `_worktree_exists` now checks `ac-authoring/` prefix and returns path without calling worktree-add; (2) branch exists but worktree pruned — `_branch_exists` probe triggers checkout-only `git worktree add <path> <branch>`, preserving AC YAML commits; (3) fresh path — `-b origin/main` form unchanged. Error handling: no bare except, no silent swallow, all subprocess I/O wrapped with specific types and re-raise. Two medium-confidence findings noted (theoretical stderr-closed edge case; `_worktree_exists` CWD dependency is pre-existing design, not a regression) — both below the Opus escalation threshold (count = 2, threshold > 3). No high-confidence blockers. Suppressed: 0 low nits, 0 Opus-dropped mediums.
