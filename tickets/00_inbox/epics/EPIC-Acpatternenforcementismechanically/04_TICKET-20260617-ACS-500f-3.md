---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  pr-reviewer: needed
  pull-request: needed
  python-coder: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
components:
- ac-store
created: '2026-06-17'
depends_on:
- ACS-500f
files_touched:
- config/ac_store_schema.json
- docs/reference/ac-schema.md
priority: medium
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: ACS-500f-3
status: todo
title: AC store schema accepts the real hierarchical id format and the pattern_slots
  field
---

# AC store schema accepts the real hierarchical id format and the pattern_slots field

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `ACS-500f-3` — AC store schema accepts the real hierarchical id format and the pattern_slots field — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `ACS-500f-3`. Component: `ac-store`. Assigned agent: `python-coder`. Estimated complexity: `M`.

## Acceptance Criteria

```gherkin
Given the AC store schema is validated against an AC record whose id uses a
  hierarchical form with a lowercase L1 segment and a numeric leaf suffix
  (e.g. "ACS-500a-1"),
When that record is validated against the schema,
Then the record is accepted (the id format does not cause a validation failure).

Given an AC record whose id uses an L3 extension form (e.g. "ACS-500a-1-i"),
When it is validated against the schema,
Then the record is accepted.

Given a pattern AC record that includes a pattern_slots field holding a list
  of {word} placeholder strings,
When it is validated against the schema,
Then the record is accepted, and the pattern_slots field does not cause a
  validation failure.

Given the set of AC YAML records that already exist in the store under
  docs/acceptance-criteria/,
When every one of those records is validated against the schema,
Then no currently-valid stored record is rejected by the schema.

Given a root-level AC id of the documented base form (e.g. "ACS-100"),
When it is validated against the schema,
Then it is still accepted (no regression for the base id form).
```

## Sign-offs

- [ ] python-coder
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit
- [ ] pull-request

## Comments
