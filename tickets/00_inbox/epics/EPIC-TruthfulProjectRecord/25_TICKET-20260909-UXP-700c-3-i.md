---
ac_traceability:
  id: UXP-700c-3-i
  path: docs/acceptance-criteria/ux-prototyping/UXP-700-truthful-project-record/UXP-700c-3-i.yaml
advances_current_outcome: true
agents:
  ac-fulfillment-gate: needed
  ac-validator: needed
  architect-review: signed_off
  commit: needed
  documentation-expert: needed
  documentation-verifier: needed
  frontend-coder: not_needed
  pr-reviewer: needed
  pull-request: not_needed
  python-coder: needed
  sql-coder: not_needed
  test-runner: needed
change_target: code
complexity: medium
components:
- ux_prototyping
- precommit_hooks
created: '2026-09-09'
depends_on: []
documentation_required: true
files_touched: []
priority: medium
requires_adr: false
requires_diagram: false
risk_surface: contract_boundary
roadmap_phase: phase_1
source_ac: UXP-700c-3-i
status: todo
title: The verdict does not depend on which operating system the check ran on
---

# The verdict does not depend on which operating system the check ran on

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `UXP-700c-3-i` — The verdict does not depend on which operating system the check ran on — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `UXP-700c-3-i`. Component: `ux-prototyping`. Assigned agent: `python-coder`. Estimated complexity: `M`. Complexity: `medium`.

## Acceptance Criteria

```gherkin
Given the same project record and the same automatic check,
When the check runs on Windows and again on a POSIX system,
Then both runs report the same verdict and name the same artifacts,
And a difference that consists only of how the two systems spell a path is never reported as
  the record having fallen behind,
And the keys the record's index uses to name its own files are spelled identically on both
  systems, so a rebuild produced on one is byte-identical to a rebuild produced on the other.
```

- [ ] AC-1: both runs report the same verdict and name the same artifacts
- [ ] AC-2: a difference that consists only of how the two systems spell a path is never reported as
- [ ] AC-3: the keys the record's index uses to name its own files are spelled identically on both

## Test Requirements

```yaml
tests:
- name: test_index_path_keys_use_the_canonical_separator_on_every_platform
  file: unit_tests/portability/test_uxp_700c_3_i.py
  covers:
  - UXP-700c-3-i
  asserts: Index keys built from OS-native path objects are emitted with forward slashes
    regardless of the host separator.
  framework: unittest
  type: unit
  angle: criterion
- name: test_separator_only_difference_is_not_reported_as_the_record_being_behind
  file: unit_tests/portability/test_uxp_700c_3_i.py
  covers:
  - UXP-700c-3-i
  asserts: An index whose keys differ from a fresh rebuild only by path separator
    produces no behind or drift finding.
  framework: unittest
  type: unit
  angle: boundary
- name: test_rebuild_is_byte_identical_across_simulated_host_separators
  file: unit_tests/portability/test_uxp_700c_3_i.py
  covers:
  - UXP-700c-3-i
  asserts: Rebuilding the index with the host separator forced to each value yields
    identical bytes.
  framework: unittest
  type: unit
  angle: seam
- name: test_uxp_700c_3_i_reachable_from_entry_point
  file: unit_tests/portability/test_uxp_700c_3_i.py
  covers:
  - UXP-700c-3-i
  angle: reachability
  asserts: 'REQUIRED — invoke the production entry point (CLI, hook, slash command,
    workflow dispatch, or main()) as a subprocess/dispatch and assert the new behaviour
    actually occurs. Do NOT satisfy this by importing the function directly. The AC
    authored no test_spec, so the entry point is not declared: resolve it before writing
    this test, and do not delete this entry.'
```

## Agent Contracts

### documentation-expert

Existing docs to update / cross-link:

- docs/acceptance-criteria/guardrail-engine/GE-120-green-means-checked/GE-120.yaml (relationship: context | status: exists | relevance: GE-120 requires a check to reach the same verdict wherever you are working. This record is that requirement for the record's own checker.)

- [ ] AC-1: (unspecified genre) | docs/acceptance-criteria/guardrail-engine/GE-120-green-means-checked/GE-120.yaml | both runs report the same verdict and name the same artifacts

### Expects From

- **AC:** UXP-700c-3
- **Contract:** The gate whose verdict this record makes platform-independent.

## Sign-offs

- [x] architect-review — 2026-09-09 17:18
- [ ] test-runner
- [ ] documentation-expert
- [ ] pr-reviewer
- [ ] ac-validator
- [ ] ac-fulfillment-gate
- [ ] documentation-verifier
- [ ] commit

## Comments

### 2026-09-09 17:18 — architect-review (status: ok)
feedback-id: fb_2026-09-09_e7e73ec1
completion_manifest:
  blast_radius_reviewed: true
  rubric_applied: true
  classification_decision: true
Blast-radius review (direct, research-agent unavailable in this session):
affected files are `docs/product-truth/scripts/generate_product_truth.py` and
`docs/product-truth/scripts/validate_product_truth.py` (single component:
`ux_prototyping`/product-truth), triggered via the `check-product-truth-validate`
/ `check-product-truth-generate` pre-commit hooks (`.pre-commit-config.yaml:374-386`,
entry point `python .leafcutter/scripts/commit_guardian/run_hook.py
docs/product-truth/scripts/validate_product_truth.py --quiet`) — `precommit_hooks`
is the trigger surface, not a file that this ticket's code changes touch. No
always-large trigger fires: no Alembic migration, no hypertable change, no
FastAPI/public-API surface, no ADR file or ADR-named contract is being changed
(ADR-021/ADR-023/ADR-042 describe product-truth policy but are not amended
here). File count ≤5, single practical component, no cross-package boundary
crossed → **small**. `requires_adr: false` confirmed (no HIGH-impact trigger,
only 1 component substantively modified). No diagram change warranted
(`suggested_diagrams: []`) — this is a portability bugfix/test-hardening
ticket, not a new pipeline, container, or user flow.

Important finding for downstream agents: the underlying defect this AC
describes is already fixed and recorded as
`KI-BP-20260907-0812` (`docs/known-issues/build-pipeline.md:4189-4196`,
fixed by commit `26f35212`) — `generate_product_truth.py:422` already emits
`path.relative_to(STORE).as_posix()`, and the regression test at
`unit_tests/portability/test_uxp_700c_3_i.py` already exists and passes
(`2 passed`). That file's test names
(`test_load_flows_emits_no_backslash_on_any_platform`,
`test_committed_index_holds_no_windows_written_path`) do not match the four
tests this ticket's frontmatter `Test Requirements` names
(`test_index_path_keys_use_the_canonical_separator_on_every_platform`,
`test_separator_only_difference_is_not_reported_as_the_record_being_behind`,
`test_rebuild_is_byte_identical_across_simulated_host_separators`,
`test_uxp_700c_3_i_reachable_from_entry_point`) — `test-writer` should extend
this existing file with the still-missing angles (byte-identical rebuild
across simulated separators, and the reachability test) rather than assume
no coverage exists; `python-coder` should expect little or no production
code change is needed unless one of the new test angles surfaces a real gap
(e.g. in `_check_derived_indexes` in `validate_product_truth.py:474`).
Entry-point resolution for the reachability angle: the hook invocation above
is the real production entry point — `run_hook.py` dispatching
`validate_product_truth.py --quiet` (or `generate_product_truth.py --check
--quiet`) as a subprocess, per `.pre-commit-config.yaml:374-386`.

Acceptance-criteria adjustments: none required to the AC text itself: (1)
naming clarification only — the Test Requirements' four test names should
be treated as *additions* to the existing
`unit_tests/portability/test_uxp_700c_3_i.py`, not a fresh file; (2) the
reachability entry point named above should be copied through verbatim by
test-writer rather than re-resolved.

feedback-id: fb_2026-09-09_945d8553
completion_manifest:
  tests_written: true
  red_baseline_confirmed: true
  cross_layer_seam_answer:
    result: covered
    producing_side: "docs/product-truth/scripts/generate_product_truth.py load_flows()/build_by_flow() (real producer, invoked for real against a synthetic tempdir store)"
    consuming_side: "docs/product-truth/scripts/validate_product_truth.py _check_derived_indexes() (real consumer/drift-comparator) and, separately, the check-product-truth-validate pre-commit hook as invoked by scripts/commit_guardian/run_hook.py"
  reachability_entry_point_answer:
    result: resolved
    entry_point: "python .leafcutter/scripts/commit_guardian/run_hook.py docs/product-truth/scripts/validate_product_truth.py --quiet (CLI hook runner via subprocess) — named verbatim by architect-review above; copied through unchanged, not re-resolved."
  worktree_dispatch_matched_ticket_path:
    result: false
    reason: "The dispatched ticket_path resolved to no file in either the assigned worktree_path (EPIC-TruthfulProjectRecord) or its literal concatenated form. The .md ticket file physically existed only under C:\\Users\\Hendrik\\Code\\leafcutter\\leafcutter-ai\\tickets\\00_inbox\\epics\\EPIC-TruthfulProjectRecord\\25_TICKET-20260909-UXP-700c-3-i.md — a DIFFERENT git worktree, on branch epic/uxp-700-product-truth, not EPIC-TruthfulProjectRecord. Cross-checked physical evidence (unit_tests/portability/test_uxp_700c_3_i.py is byte-identical in both worktrees; .pre-commit-config.yaml's check-product-truth-validate hook at the exact line numbers 374-386 architect-review cited exists ONLY in EPIC-TruthfulProjectRecord — leafcutter-ai has no product-truth hook entries at all and only 186 total lines) confirms EPIC-TruthfulProjectRecord is the correct worktree for this ticket's real artifacts, and the ticket .md file's placement in leafcutter-ai/tickets/... is itself the defect."
    remediation: "Copied the ticket .md file verbatim into tickets/00_inbox/epics/EPIC-TruthfulProjectRecord/ inside the assigned EPIC-TruthfulProjectRecord worktree (no content changes) so sign-off could proceed inside the correct worktree. Did NOT touch the original file in the leafcutter-ai worktree. Escalate to a human/supervisor to fix the ticket-generation tooling's worktree targeting — this is a dispatch/placement defect, not a ticket-content defect, and downstream agents (python-coder etc.) dispatched with the same literal ticket_path string will hit the identical resolution failure unless the harness is corrected or points them at this copy."
red_baseline:
  - test_name: test_separator_only_difference_is_not_reported_as_the_record_being_behind
    file: unit_tests/portability/test_uxp_700c_3_i.py
    error: "AssertionError: Lists differ: ['[index] by_flow does not match a fresh rebuild — run generate_product_truth.py'] != []"
Extended the existing unit_tests/portability/test_uxp_700c_3_i.py (per architect-review's explicit instruction to extend, not replace) with the three still-missing Test Requirements angles: `test_rebuild_is_byte_identical_across_simulated_host_separators` (seam, AC-1/AC-3 — simulates both host flavours via PurePosixPath/PureWindowsPath against the real producer's actual output; passes immediately, confirming the existing fix), `test_uxp_700c_3_i_reachable_from_entry_point` (reachability, AC-1 — real subprocess invocation of the named entry point, parses and asserts on the JSON verdict line; passes immediately), and `test_separator_only_difference_is_not_reported_as_the_record_being_behind` (boundary, AC-2 — feeds `_check_derived_indexes` a `by_flow` entry that differs from a fresh rebuild ONLY by path separator; this is genuinely RED today: exact dict comparison has no separator tolerance, so a foreign-spelled artifact IS currently misreported as "does not match a fresh rebuild", contradicting AC-2 literally). Verified via `python -m unittest unit_tests.portability.test_uxp_700c_3_i` — non-zero exit, 1 failure (the boundary test), 4 passed. The two passing new tests are not under-specified; they correctly re-confirm the already-landed fix (commit 26f35212) as architect-review predicted, and are excluded from `red_baseline` per the "do not include passing tests" schema rule. `python-coder`'s real remaining work is narrowly scoped: give `_check_derived_indexes` (or its `by_flow` comparison specifically) separator-tolerant comparison so a foreign-spelled `path` value is not conflated with genuine content drift. ANOMALY: this ticket's dispatched `ticket_path` did not resolve inside the assigned `worktree_path` — see `worktree_dispatch_matched_ticket_path` above; I created a verbatim copy of the ticket at this path inside EPIC-TruthfulProjectRecord to proceed. A human should verify downstream agents are dispatched against this copy, not the original leafcutter-ai-worktree file.

## Escalation

Branch: none
Reason: No always-large trigger (no migration/hypertable/public-API/ADR-contract
change); blast radius is 2 files in one practical component
(`docs/product-truth/scripts/`, component `ux_prototyping`), triggered only via
a pre-commit hook (`precommit_hooks` is the trigger surface, not an edited
component) — well under the 5-file / 3-component large threshold.
