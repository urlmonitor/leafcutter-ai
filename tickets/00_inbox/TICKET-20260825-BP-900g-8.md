---
ac_traceability:
  id: BP-900g-8
  path: docs/acceptance-criteria/build_pipeline/BP-900-deployment-completeness/BP-900g-8.yaml
advances_current_outcome: true
agents:
  ac-fulfillment-gate: needed
  ac-validator: needed
  architect-review: signed_off
  commit: needed
  documentation-expert: needed
  documentation-verifier: needed
  pr-reviewer: needed
  pull-request: needed
  python-coder: signed_off
  test-runner: signed_off
  test-writer: signed_off
change_target: pipeline
complexity: high
components:
- build_pipeline
created: '2026-08-25'
depends_on: []
documentation_required: true
files_touched:
- scripts/build.py
- scripts/build_phases.py
- scripts/build_referential_integrity.py
priority: high
requires_adr: false
requires_diagram: false
risk_surface: contract_boundary
roadmap_phase: phase_1
source_ac: BP-900g-8
status: todo
title: A deployed script's own intra-package imports are deployed with it, proven
  in the deployed tree
---

# A deployed script's own intra-package imports are deployed with it, proven in the deployed tree

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BP-900g-8` — A deployed script's own intra-package imports are deployed with it, proven in the deployed tree — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BP-900g-8`. Component: `build-pipeline`. Assigned agent: `python-coder`. Estimated complexity: `L`. Complexity: `high`.

## Acceptance Criteria

```gherkin
Given any script the build deploys into a consumer install,
And that script resolving, importing, or executing another module belonging to the
  same package — whether by import statement, by path construction relative to its
  own location, or by dynamic loader,
When the build runs its deploy-completeness preflight,
Then every such intra-package dependency is itself deployed, or the build exits
  non-zero naming the deployed script, the dependency it needs, and the deploy
  phase that would have to carry it,
And the dependency set is DERIVED from what the deployed script actually resolves,
  so a module added to a deployed script tomorrow is covered without anyone editing
  a list — a hand-maintained manifest of dependencies does not satisfy this
  criterion even when it happens to be complete on the day it is written,
And the closure is transitive: a dependency of a dependency is subject to the same
  rule, so a chain that is correct at its first hop and broken at its second fails
  the build rather than passing it,
And the demonstration runs the build into a temporary target directory and then
  exercises the DEPLOYED copy in a fresh subprocess, with a negative control that
  withholds one dependency from the deploy and must be shown to fail the build —
  a check performed against the source tree does not satisfy this criterion,
  because the source tree contains every module by construction and is therefore
  structurally blind to the defect,
Because at HEAD 339b0981c scripts/ac_store/generate_ticket_from_ac.py is deployed
  by the build_ac_store phase and resolves its sibling
  scripts/ac_store/_component_migration_map.py, which that phase does not deploy —
  so in every consumer install the sibling is absent. That deploy map already
  carries THREE separate comments warning about this exact ModuleNotFoundError
  failure mode, for done_proof.py, test_enforcement.py and ac_parent_id.py, and
  still contains a live fourth instance. A hand-maintained list annotated with
  warnings about the way it fails is not a mechanism; it is a record of the
  mechanism's absence.
```

- [ ] AC-1: that script resolving, importing, or executing another module belonging to the
- [ ] AC-2: every such intra-package dependency is itself deployed, or the build exits
- [ ] AC-3: the dependency set is DERIVED from what the deployed script actually resolves
- [ ] AC-4: the closure is transitive: a dependency of a dependency is subject to the same
- [ ] AC-5: the demonstration runs the build into a temporary target directory and then

## Test Requirements

```yaml
tests:
- name: test_bp_900g_8_derived_closure_includes_the_sibling_the_deploy_map_omits
  file: unit_tests/test_bp_900g_8.py
  covers:
  - BP-900g-8
  asserts: Compute the closure for scripts/ac_store/generate_ticket_from_ac.py from
    the code and assert _component_migration_map.py is in it, and that it is reported
    as NOT covered by the build_ac_store declaration. Asserts the closure's contents
    and the containment verdict — never that a name appears in a list literal in build_phases.py.
  framework: unittest
  type: unit
  angle: criterion
- name: test_bp_900g_8_build_subprocess_blocks_when_a_resolved_dependency_is_withheld_from_the_deploy
  file: unit_tests/test_bp_900g_8.py
  covers:
  - BP-900g-8
  asserts: 'PRODUCTION ENTRY POINT with the must_block modifier, plus its paired control.
    Copy the package tree to a temp dir, withhold one intra-package dependency of
    a deployed script from the deploy declaration, run `python scripts/build.py --target-dir
    <tmp_target>` as a SUBPROCESS: assert non-zero exit and that the output names
    the deployed script, the missing dependency, and the phase that would have to
    carry it. Then run the SAME command against the unmodified tree: exit zero. Both
    halves through the same command — a positive-path build alone cannot tell a working
    guard from an absent one.'
  framework: unittest
  type: integration
  angle: reachability
- name: test_bp_900g_8_deployed_tree_contains_the_full_closure_of_every_deployed_script
  file: unit_tests/test_bp_900g_8.py
  covers:
  - BP-900g-8
  asserts: Run build.py into a temp target, then walk the DEPLOYED tree (not the source
    tree) and assert that for every deployed Python script, every module in its derived
    closure is present in the deployed tree. The source tree contains every module
    by construction, so the same assertion made there is structurally incapable of
    failing — this is the BP-811 distinction between the copy tier and the reachability
    tier.
  framework: unittest
  type: integration
  angle: deployed
- name: test_bp_900g_8_deployed_generate_ticket_cold_imports_in_a_fresh_subprocess
  file: unit_tests/test_bp_900g_8.py
  covers:
  - BP-900g-8
  asserts: 'From the temp target produced by the harness, execute the DEPLOYED copy
    of generate_ticket_from_ac.py in a genuinely fresh `python` subprocess with the
    package source tree absent from sys.path, and assert it reaches its entry point
    without a ModuleNotFoundError. importlib.reload does NOT satisfy this: it re-executes
    inside an already-populated namespace and masks cold-import failures — that is
    exactly how the GenReviewFixes H-2 forward-reference NameError survived its tests.'
  framework: unittest
  type: integration
  angle: real_artifact
```

## Implementation Notes

```yaml
config_schema_fragment:
  description: The three sets this AC and BP-900g-5 together govern, named explicitly
    so an implementer cannot satisfy one by collapsing it into another. The whole
    defect class is these three being assumed equal while never being compared.
  properties:
    deploy_declaration:
      description: 'SET B — the deploy_map: what a phase declares it ships. Remains
        an explicit, human-readable declaration. It is CHECKED against set A, not
        generated from it.'
      items:
        type: string
      type: array
    guard_manifest:
      description: 'SET C — _manifest_ac_store_scripts: the guard''s model of what
        has been deployed. Per BP-900g-5''s fifth it_requirement this must derive
        FROM set B. It must never be the source set B is derived from.'
      items:
        type: string
      type: array
    invariant:
      description: B must contain closure(A), enforced by a build failure; C is derived
        from B. Deriving B from A automatically, or deriving C from anything other
        than B, does not satisfy this AC.
      type: string
    resolved_closure:
      description: SET A — the transitive set of intra-package modules a deployed
        script actually resolves, DERIVED from the code by static analysis of imports,
        relative path construction, and dynamic loader calls. Never hand-maintained.
        This is the new artifact this AC introduces.
      items:
        type: string
      type: array
  required:
  - resolved_closure
  - deploy_declaration
  - guard_manifest
  type: object
constraints:
- 'THE BINDING DIRECTION RULE (resolves the conflict the BA escalated). Three sets,
  two edges, and the direction of each edge is fixed. Edge 1: B must CONTAIN closure(A)
  — checked at build time, failing the build when it does not. Edge 2: C DERIVES FROM
  B — this is BP-900g-5''s fifth it_requirement and it stays. What is FORBIDDEN: (a)
  generating B from A, which silently deploys whatever a typo''d import names and
  leaves BP-900g-9 with no declaration to check; (b) deriving the checker''s view
  from B, which makes the guard and the thing it polices agree by construction and
  detect nothing — this is the failure the AC''s own criteria name; (c) deriving C
  from A, which restores the divergence BP-900g-5 was closing. Full rationale in this
  AC''s notes under IT-PO RULING.'
- 'DERIVED MEANS DERIVED FROM THE CODE. The closure must be computed from what the
  deployed script resolves — import statements, paths built relative to __file__,
  and dynamic loader calls — not read from any list, map, allowlist, or annotation.
  The test that proves this is the newly-added-module case in BP-900g-8-i: add a module
  inside the test, have an already-deployed script resolve it, edit NO list, and require
  the build to either deploy it or fail naming it. An implementation that passes every
  other test and fails that one is an enumeration wearing the word ''derived''.'
- TRANSITIVE, AND SAY SO IN THE FAILURE. The closure follows dependencies of dependencies.
  When it fails at the second hop the message must name the module that is actually
  missing, not only the top-level deployed script, or the author fixes the wrong hop.
- 'EVERY DEPLOY PHASE, NOT ONLY build_ac_store. n_location_rule is ''all'': the live
  instance is in build_ac_store, but any phase in scripts/build_phases.py that ships
  intra-package Python modules is subject to the same closure. A fix scoped to the
  one phase where the defect was found reproduces the history the criteria cite —
  three prior instances, each fixed individually, each annotated with a warning comment,
  and a fourth still live.'
- DO NOT SATISFY THIS BY ADDING _component_migration_map.py TO THE DEPLOY MAP. That
  makes the live symptom disappear and leaves the mechanism exactly as it was. Adding
  it is necessary and is not sufficient; the closure check must independently fail
  on a tree where it is withheld.
- OUT-OF-PACKAGE MODULES ARE NOT OURS TO SHIP. The standard library, installed third-party
  distributions, and host-project paths the consumer owns must produce no finding.
  A closure that demands the build ship the standard library gets an allowlist, and
  an allowlist is a hand-maintained list wearing a different name. Respect BP-900g-5's
  narrowed output-root prefix bound and keep BP-900g-6's negative-control tests green
  — that bound exists because BP-900g-4 mis-normalised a host-project path (debugging/scripts/...)
  into a deploy key.
- STATIC ANALYSIS HAS A KNOWN BLIND SPOT AND IT MUST BE DECLARED, NOT PAPERED OVER.
  A dynamic loader that builds a module path from a runtime value cannot always be
  resolved statically. Where the closure cannot decide, it must report the unresolvable
  reference for a human rather than assume it is external — an unknown silently classified
  as not-ours is the same fail-open in a new place. BP-900g-10 is the runtime backstop
  for whatever the closure still misses; it is not a licence for the closure to guess.
- IDEMPOTENCY AND BUDGET. The closure computation must be deterministic and byte-stable
  across runs on an unchanged tree, and must run inside the existing build preflight's
  budget — static analysis of the package's own Python files, no network, no per-module
  subprocess.
n_location_rule: all
post_write_commands:
- python scripts/build.py --target-dir .
- python -m pytest unit_tests/ -q
reference_file_path: scripts/build_phases.py
required_skills:
- python-coder
```

## Agent Contracts

### documentation-expert

Existing docs to update / cross-link:

- docs/acceptance-criteria/build_pipeline/BP-900-deployment-completeness/BP-900g.yaml (relationship: parent | status: exists)
- scripts/build_phases.py (relationship: modifies | status: exists | relevance: Holds the build_ac_store deploy_map (~lines 851-873) carrying the live fourth instance and the three prior warning comments. The manifest that must become derived rather than hand-maintained.)
- scripts/ac_store/generate_ticket_from_ac.py (relationship: describes | status: exists | relevance: The deployed script with the undeployed dependency (~line 328 resolves and execs its sibling). The concrete instance, not the criterion.)
- scripts/ac_store/_component_migration_map.py (relationship: describes | status: exists | relevance: Present in source, absent from the deploy map — the dependency whose absence is invisible to every source-tree check.)
- scripts/build_referential_integrity.py (relationship: modifies | status: exists | relevance: The existing reference guard; BP-900g-6 established its output-root prefix bound, which this AC must not widen.)
- docs/testing/test-angles.md (relationship: describes | status: exists | relevance: Angle vocabulary for the acceptance demonstration (deployed + reachability + must_block).)
- docs/acceptance-criteria/build_pipeline/BP-900-deployment-completeness/BP-900g-5.yaml (relationship: describes | status: exists | relevance: IT-PO ADDITION — READ THIS BEFORE TOUCHING build_phases.py. Its fifth it_requirement asks that _manifest_ac_store_scripts derive FROM the deploy_map. That is a different edge of the same triangle and it is NOT in conflict with this AC, but it can be satisfied in a way that IS. The ruling on which direction wins is in this AC's notes and is binding on the implementer.)
- scripts/build.py (relationship: modifies | status: exists | relevance: IT-PO ADDITION. The process the preflight must run inside and whose exit code the criterion asserts on, and the command the deployed-tree harness invokes (`python scripts/build.py --target-dir <tmp>`). The harness this AC builds is reused by BP-900g-8-i and BP-900g-10.)
- CLAUDE.md (relationship: describes | status: exists | relevance: IT-PO ADDITION. 'New Hook / Gate Dependencies Must Be in the Build Deploy-Manifest' is the prose statement of this defect after the done_proof.py incident. It records the rule as a convention for humans to remember, which is precisely what this AC replaces with a mechanism — the convention was written and the fourth instance still shipped.)

- [ ] AC-1: [(unspecified genre)] docs/acceptance-criteria/build_pipeline/BP-900-deployment-completeness/BP-900g.yaml — every such intra-package dependency is itself deployed, or the build exits

### Delivers To

- **Agent:** python-coder
- **Contract:** Two artifacts. (1) The derived transitive intra-package dependency closure — a function over the package tree returning, per deployed script, the set of intra-package modules it resolves, computed from the code and never from a list, together with the build-time check that set B (the deploy declaration) contains it. (2) The deployed-tree test harness: run `python scripts/build.py --target-dir <tmp>` into a temporary target and exercise the DEPLOYED copy in a genuinely fresh subprocess with the source tree off sys.path. Consumed by BP-900g-8-i (which drives three surface shapes through the same harness) and by BP-900g-10 (which removes a dependency from that same deployed tree and asserts the runtime behaviour). The harness is shared, not rebuilt per AC.

## Sign-offs

- [x] architect-review — 2026-08-25 17:02
- [x] test-writer — 2026-08-25 17:45
- [x] python-coder — 2026-08-25 18:40
- [x] test-runner — 2026-08-25 23:05
- [ ] documentation-expert
- [ ] pr-reviewer
- [ ] ac-validator
- [ ] ac-fulfillment-gate
- [ ] documentation-verifier
- [ ] commit
- [ ] pull-request

## Comments

### 2026-08-25 17:02 — architect-review (status: ok)
feedback-id: fb_2026-08-25_d4e745db
completion_manifest:
  blast_radius_reviewed: true
  rubric_applied: true
  escalation_decision_made: true
Impact classification: SMALL. Files touched (scripts/build.py, scripts/build_phases.py, scripts/build_referential_integrity.py) = 3 (≤5), all within the single `build_pipeline` component, no cross-module boundary crossing. None of the always-large triggers fired: no Alembic migration, no hypertable change, no FastAPI public-API surface touched, and no `docs/architecture/adrs/ADR-*` file modified — the binding-direction rule (Set A resolved_closure / Set B deploy_declaration / Set C guard_manifest, and B-contains-closure(A) / C-derives-from-B) was already adjudicated by IT-PO under BP-900g-5, so this ticket implements a previously-settled contract rather than opening a new cross-component one; `requires_adr: false` and `requires_diagram: false` in frontmatter are confirmed correct.

Design note: the change adds an AST-based static-analysis closure computation plus a build-time containment check inside the existing `build.py` preflight, applied to every deploy phase in `build_phases.py` (`n_location_rule: all`), not just `build_ac_store`. Two layering risks to watch during implementation: (1) the preflight ordering in `build.py` must run early enough to fail the whole `--target-dir` invocation before any phase partially deploys, and must run for every code path that reaches deployment, not only the default CLI flags; (2) per the ticket's own "STATIC ANALYSIS HAS A KNOWN BLIND SPOT" constraint, any unresolvable dynamic-loader reference must be surfaced as an explicit human-facing finding rather than silently classified as external — this is the fail-open seam most likely to regress under time pressure. Respect BP-900g-6's narrowed output-root prefix bound in `build_referential_integrity.py`; do not widen it while adding the new closure check.

Acceptance-criteria adjustments: none — the Gherkin scenario and the four constraints (binding-direction, derived-not-enumerated, transitive-with-named-hop, every-deploy-phase) are already precise enough to implement and test against directly.

### 2026-08-25 17:45 — test-writer (status: ok)
feedback-id: fb_2026-08-25_e44a5101
completion_manifest:
  tests_written: true
  red_baseline_confirmed: true
  ac1_ac5_mapped: true
Wrote `unit_tests/test_bp_900g_8.py` with the four tests specified in `## Test Requirements`, covering AC-1 through AC-5 (derived closure computation, build-time containment check with a synthetic withheld declaration, a real `python scripts/build.py` subprocess must_block + positive-control pair, a deployed-tree transitive-closure walk, and a fresh-subprocess cold-import check of the deployed `generate_ticket_from_ac.py`). No `### test-writer` subsection exists under `## Agent Contracts` and no `## AC Coverage` table exists in this ticket body, so per signoff SKILL.md §2c.2 the AC-checkbox-flip and coverage-table steps are skipped (out of scope for this agent on this ticket). All four tests were run via `AC_ENFORCE_STRICT=1 python -m pytest unit_tests/test_bp_900g_8.py -v` and confirmed red for the right reasons: two fail with `AttributeError` against the not-yet-implemented `compute_intra_package_closure`/`find_uncovered_closure_dependencies` functions, one fails because the real `build.py` currently exits 0 with the dependency withheld (no guard exists yet), and one fails because the deployed `generate_ticket_from_ac.py` currently logs `Cannot load MIGRATION_MAP from ...` in a fresh subprocess (silent degradation, confirmed empirically via a probe build before authoring the assertion, to avoid an under-specified exit-0-only check). `git status --short scripts/build_phases.py` confirmed the test's on-disk withhold/restore cycle leaves the source tree clean.
red_baseline:
  - test_name: test_bp_900g_8_derived_closure_includes_the_sibling_the_deploy_map_omits
    file: unit_tests/test_bp_900g_8.py
    error: "AttributeError: module 'build_referential_integrity' has no attribute 'compute_intra_package_closure'"
  - test_name: test_bp_900g_8_build_subprocess_blocks_when_a_resolved_dependency_is_withheld_from_the_deploy
    file: unit_tests/test_bp_900g_8.py
    error: "AssertionError: build.py --target-dir exited 0 while a resolved intra-package dependency (_component_migration_map.py, resolved by generate_ticket_from_ac.py) was withheld from the build_ac_store deploy declaration."
  - test_name: test_bp_900g_8_deployed_tree_contains_the_full_closure_of_every_deployed_script
    file: unit_tests/test_bp_900g_8.py
    error: "AttributeError: module 'build_referential_integrity' has no attribute 'compute_intra_package_closure'"
  - test_name: test_bp_900g_8_deployed_generate_ticket_cold_imports_in_a_fresh_subprocess
    file: unit_tests/test_bp_900g_8.py
    error: "AssertionError: The deployed generate_ticket_from_ac.py logged 'Cannot load MIGRATION_MAP from ...' -- proof that its sibling _component_migration_map.py did not load from the DEPLOYED tree in a fresh subprocess."

### 2026-08-25 18:40 — python-coder (status: ok)
feedback-id: fb_2026-08-25_80c1516b
completion_manifest:
  closure_computation_implemented: true
  build_time_guard_wired: true
  binding_direction_rule_respected: true
  n_location_rule_all_applied: true
  red_baseline_all_green: true
  full_unit_suite_green: true
red_baseline_results:
  - test_name: test_bp_900g_8_derived_closure_includes_the_sibling_the_deploy_map_omits
    result: green
  - test_name: test_bp_900g_8_build_subprocess_blocks_when_a_resolved_dependency_is_withheld_from_the_deploy
    result: green
  - test_name: test_bp_900g_8_deployed_tree_contains_the_full_closure_of_every_deployed_script
    result: green
  - test_name: test_bp_900g_8_deployed_generate_ticket_cold_imports_in_a_fresh_subprocess
    result: green
No `### python-coder` subsection exists under `## Agent Contracts` (only `### documentation-expert` and `### Delivers To` prose) and there is no `## Implementation Tasks` section in this ticket, so per signoff SKILL.md §1.5 and §2c.2 the task-checkbox and AC-checkbox-flip steps are skipped, matching test-writer's prior sign-off on this same ticket.

Implemented both deliverables named in `### Delivers To`. (1) `build_referential_integrity.compute_intra_package_closure(script, root)` — AST-based static analysis that walks a script's `import`/`from...import` statements (including relative imports), plus `importlib.util.spec_from_file_location(name, path)` dynamic loads where the `path` expression reduces to a concrete file via a function-scoped local-variable-aware evaluator (`_eval_static_path`) recognising `Path(__file__).resolve().parent / "..."`-style chains — transitively, with existence-under-*root* as the sole external/internal discriminator (no allowlist). An unresolvable dynamic-loader expression (e.g. `goal_to_epic.py`'s runtime source/deployed-layout ternary) is logged as a WARNING naming the file/line rather than silently treated as external, per the ticket's blind-spot constraint. `find_uncovered_closure_dependencies(script_rel_path, root, declared)` is the Set-B-contains-closure(A) containment check. (2) `build.py._check_intra_package_closure_guard`, wired into `main()` alongside the existing script-reference and tracked-source preflights (before `_run_phases()` writes anything), applied uniformly across every deploy category `_get_source_deployable_scripts` covers (`n_location_rule: all`) — not scoped to `build_ac_store` alone.

Per the binding-direction rule: Set B (`build_phases.AC_STORE_DEPLOY_MAP`, extracted from the previously-inline `deploy_map` local so it can be shared) stays an explicit, human-authored declaration — never generated from Set A. Set C (`build._manifest_ac_store_scripts`) now derives FROM `AC_STORE_DEPLOY_MAP` instead of scanning every file physically present in `scripts/ac_store/` (BP-900g-5's fifth it_requirement, previously unimplemented despite that AC's `work_status: done`) — this was necessary for the guard to be meaningful at all, since checking Set A against a Set C that was really "whatever exists in source" made containment trivially true by construction. `_get_source_paths_for_guard`'s ac_store section was updated the same way to keep `test_guard_source_paths_match_deployable_set` (cardinality parity) green.

Added `_component_migration_map.py` to `AC_STORE_DEPLOY_MAP` (the AC's cited instance — necessary, not sufficient per the ticket's explicit constraint). Running the new guard against the real deploy map (before that entry existed) also surfaced a SECOND, previously-unknown instance of the same defect class: `validate_ac_schema.py` does `from _ac_components import components_field_errors, load_registry_ids`, and `_ac_components.py` was likewise present in source but absent from the map — concrete evidence the mechanism is derived, not an enumeration of the one cited case. Both entries are now in `AC_STORE_DEPLOY_MAP`.

Verification: all 4 tests in `unit_tests/test_bp_900g_8.py` pass (`AC_ENFORCE_STRICT=1`). The real `python scripts/build.py --target-dir .` run against this worktree's own deployment target exits 0. Full `python -m pytest unit_tests/ -q` (the ticket's `post_write_commands`) — 3984 passed, 7 skipped, 2 xfailed, 0 failed (an initial run surfaced one pre-existing-pattern regression, `test_guard_source_paths_match_deployable_set`, from the Set-C fix described above; fixed by updating `_get_source_paths_for_guard` in the same commit, then reconfirmed green). `ruff check scripts/build.py scripts/build_phases.py scripts/build_referential_integrity.py` passes with no findings.

### 2026-08-25 23:05 — test-runner (status: ok)
feedback-id: (recorded-by-proxy)
completion_manifest:
  suite_run: true
  failure_triaged: true
  recorded_by: main-loop proxy, not the driving session
Full suite re-run independently from a settled tree:
`python3 -m pytest unit_tests -q --no-header -p no:cacheprovider`
→ **3983 passed, 1 failed, 7 skipped, 2 xfailed, 467 subtests passed in 479.89s.**
All four `unit_tests/test_bp_900g_8.py` tests pass.

The single failure is
`unit_tests/commit_guardian/test_ge_122e_1.py::TestGE122e1::test_goal_record_claims_a_new_id_and_its_folder_matches_origin_main`:
`AssertionError: Lists differ: ['GE-120c-6'] != []`.

**Triaged as branch staleness, not a regression, and verified rather than assumed.**
That test compares this working tree's `GE-120-green-means-checked/` folder against the
same folder at `origin/main`. This worktree sits at `12716f07` (an older main);
`GE-120c-6.yaml` was added to `origin/main` afterwards. Confirmed both directions:
`git ls-tree -r origin/main` lists `GE-120c-6.yaml`, and `ls` in this worktree does not.
So the test correctly reports an id present on main and absent locally — a consequence of
the branch being behind, in a `guardrail-engine` AC folder, with no relationship to
`build.py`, `build_phases.py` or `build_referential_integrity.py`. It will clear when this
branch takes `origin/main`, which must happen before merge in any case.

**Why this sign-off is written by a proxy.** The driving session recorded in
`debugging/logs/feedback.jsonl` at 18:20Z that it was "unable to write ticket sign-off
because Edit/Write tools are disabled for this session" — a tooling blocker, not a test
failure. Three concurrent full-suite runs were observed against this worktree, which is
the retry loop that produced: the suite passes, the sign-off cannot be recorded, the phase
is retried. That is also a live hazard, because this AC's own tests withhold and restore
`scripts/build_phases.py` on disk; concurrent runs can interleave and leave it withheld.
The runs have since exited and the tree was verified intact (`AC_STORE_DEPLOY_MAP` present,
file parses, two independent pre/post snapshots byte-identical) before this was written.

I did not re-derive the earlier phases' work and have signed off only this phase.

## Escalation

Branch: none
Reason: 3 files in one component (build_pipeline/); no always-large trigger fired (no Alembic migration, no hypertable change, no FastAPI public-API change, no ADR file touched).
