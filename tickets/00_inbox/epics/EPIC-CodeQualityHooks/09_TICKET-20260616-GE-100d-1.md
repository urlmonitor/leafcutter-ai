---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  pr-reviewer: needed
  pull-request: needed
  python-coder: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
components:
- guardrail-engine
created: '2026-06-16'
depends_on:
- GE-100d
files_touched:
- docs/acceptance-criteria/guardrail-engine/GE-100-code-quality-hooks/GE-100d.yaml
- docs/pre-commit-hooks.md
priority: high
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: GE-100d-1
status: todo
title: diff-cover hook falls back through compare branch chain when origin/main is
  unavailable
---

# diff-cover hook falls back through compare branch chain when origin/main is unavailable

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `GE-100d-1` — diff-cover hook falls back through compare branch chain when origin/main is unavailable — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `GE-100d-1`. Component: `guardrail-engine`. Assigned agent: `python-coder`. Estimated complexity: `S`.

## Acceptance Criteria

```gherkin
Given the commit guardian configuration has the diff_coverage section with:
  enabled: true
  compare_branch: "origin/main"
And the diff-cover binary is installed
And coverage.xml exists and is fresh
And the git remote "origin" does not have a "main" branch
  (e.g. the remote uses "master" or the remote is unreachable)
But the local branch "main" exists
When the diff-cover hook runs at pre-commit time
Then the hook falls back to comparing against the local "main" branch
And the coverage check proceeds using "main" as the comparison base

---

Given the same configuration
And neither "origin/main" nor a local "main" branch exists
When the diff-cover hook runs at pre-commit time
Then the hook falls back to comparing against "HEAD~1"
And the coverage check proceeds using the previous commit as the base
```

## Sign-offs

- [ ] python-coder
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit
- [ ] pull-request

## Comments
