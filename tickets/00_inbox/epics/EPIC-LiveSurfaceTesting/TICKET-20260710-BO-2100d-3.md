---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  pr-reviewer: needed
  pull-request: needed
  python-coder: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
complexity: medium
components:
- build_orchestration
created: '2026-07-10'
depends_on:
- BO-2100d
files_touched:
- scripts/live_surface_startup.py
- docs/architecture/adrs/ADR-020-live-surface-tester.md
priority: high
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: BO-2100d-3
status: todo
title: Startup and finalize resolve the deployed scripts from the real deployed dir
  in a worktree
---

# Startup and finalize resolve the deployed scripts from the real deployed dir in a worktree

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BO-2100d-3` — Startup and finalize resolve the deployed scripts from the real deployed dir in a worktree — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BO-2100d-3`. Component: `build-orchestration`. Assigned agent: `python-coder`. Estimated complexity: `M`. Complexity: `medium`.

## Acceptance Criteria

```gherkin
Given a git worktree where the package is deployed under `.leafcutter/` (not
  `.claude/`, which the ADR and agent text referenced)
When startup and finalize teardown resolve and invoke the live-surface scripts
  (live_surface_startup.py and port_registry.py)
Then they locate the scripts at their actual deployed location in the worktree and
  run successfully
  And they do NOT fail with a not-found error caused by a hardcoded `.claude` path
  And live testing works from a fresh worktree without manual path fixups.
```

## Test Requirements

```yaml
tests:
- name: test_bo_2100d_3_they_locate_the_scripts_at_their_actual_deployed
  file: unit_tests/test_bo_2100d_3.py
  covers:
  - BO-2100d-3
  asserts: they locate the scripts at their actual deployed location in the worktree
    and
```

## Implementation Notes

```yaml
- Script resolution must locate live_surface_startup.py and port_registry.py at their
  real deployed location (`.leafcutter/`) in a worktree; no hardcoded `.claude` path
  may remain in the resolution logic.
- Live testing must work from a fresh worktree with no manual path fixups.
- The ADR/agent text that referenced `.claude` must be corrected so documentation
  matches the deployed reality.
- Path resolution and file-not-found handling must follow the project error-handling
  policy (clear, named error rather than a silent failure).
```

## Sign-offs

- [ ] python-coder
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit
- [ ] pull-request

## Comments
