id: BP-600e-3
title: "Quick-fix workflow preserves progress when escalating to full build pipeline"
component: build-pipeline
level: L2
status: active
readiness: reviewed
priority: medium
work_status: todo
criteria: |
  Given the workflow has paused with a scope-exceeded warning
    (from BP-600e-1 or BP-600e-2),
  When the user chooses to escalate to the full build pipeline,
  Then the AC YAML file already created is preserved in the AC store,
  And the test file already written is preserved in the test directory,
  And the workflow outputs a summary of what was completed and what
    remains (AC ID, test file path, diagnosed file, root cause),
  And it provides the AC ID so the user can reference it in
    /create-ac or /build-feature.
depends_on:
  - BP-600e
  - BP-600e-1
  - BP-600e-2
doc_links:
  - path: docs/architecture/agent_delivery_workflows.md
    relationship: describes
    status: exists
  - path: docs/architecture/adrs/ADR-006-flatten-supervisor-chain.md
    relationship: describes
    status: exists
  - path: templates/skills/build-single-ticket/SKILL.md
    relationship: contrast
    status: exists
assigned_agent: workflow-architect
estimated_complexity: M
it_requirements:
  - "Must operate at depth 0 per ADR-006 — phase agents dispatched at depth 1 via the Agent tool"
  - "Requires Claude Code >= 2.1.154 for JS workflow script support"
  - "Must handle errors gracefully per the project error handling policy"
  - "Must not delete, revert, or modify any artifacts already created (AC YAML, test file, fix changes)"
  - "Escalation summary must include all file paths and the AC ID in a copy-pasteable format"
delivers_to: null
expects_from:
  ac_id: BP-600e-1
  contract: "Paused workflow state containing: { ac_id: string, ac_path: string, test_file: string, target_file: string, diagnosis: object, artifacts_created: string[] }"
origin_agent: business-analyst-v3
created: 2026-06-05
amended_by: []
superseded_by: null
covered_by: [BP-600e-3-i]
implemented_by:
- tickets/00_inbox/TICKET-20260608-BP-600e-3.md
