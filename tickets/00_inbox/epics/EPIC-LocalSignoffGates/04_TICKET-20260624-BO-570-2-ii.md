---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  llm-expert: needed
  pr-reviewer: needed
  pull-request: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
components:
- build_orchestration
created: '2026-06-24'
depends_on:
- BO-570-2
files_touched:
- templates/agents/frontend-coder.md
- templates/skills/signoff/SKILL.md
- templates/skills/webapp-testing/SKILL.md
priority: medium
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: BO-570-2-ii
status: todo
title: Render tooling unavailable in the worktree surfaces clearly and never silently
  passes
---

# Render tooling unavailable in the worktree surfaces clearly and never silently passes

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BO-570-2-ii` — Render tooling unavailable in the worktree surfaces clearly and never silently passes — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BO-570-2-ii`. Component: `build-orchestration`. Assigned agent: `llm-expert`. Estimated complexity: `S`.

## Acceptance Criteria

```gherkin
Given a ticket whose changed surface includes frontend files,
  and the tooling needed to perform the dev/route render is not available
  in the worktree (for example the render runner cannot start or its
  dependencies are not installed),
When the sign-off gate attempts the dev-render smoke check,
Then the gate does NOT record the smoke check as passed,
And it records the check as inconclusive, naming the reason the render could
  not be performed (tooling unavailable),
And the sign-off cannot reach an "ok" verdict on a frontend surface whose
  render was never actually exercised,
And the inconclusive outcome is surfaced to the supervisor for resolution
  rather than being swallowed silently.
```

## Sign-offs

- [ ] llm-expert
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit
- [ ] pull-request

## Comments
