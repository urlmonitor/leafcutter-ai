---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  pr-reviewer: needed
  pull-request: needed
  python-coder: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
complexity: low
components:
- build_orchestration
created: '2026-07-15'
depends_on:
- BO-2200b-5
files_touched:
- scripts/ac_store/generate_ticket_from_ac.py
priority: medium
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: BO-2200b-5-i
status: todo
title: A hand-edited not_needed on the verifier is restored to needed at generation
  time
---

# A hand-edited not_needed on the verifier is restored to needed at generation time

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BO-2200b-5-i` — A hand-edited not_needed on the verifier is restored to needed at generation time — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BO-2200b-5-i`. Component: `build-orchestration`. Assigned agent: `python-coder`. Estimated complexity: `S`. Complexity: `low`.

## Acceptance Criteria

```gherkin
Given a doc-triggering ticket whose agents map was generated with `documentation-expert`
  and `documentation-verifier` both `needed`,
When someone hand-edits the ticket to set `documentation-verifier` (or
  `documentation-expert`) to `not_needed` and the ticket is re-generated or re-validated,
Then the status is restored to `needed` for both agents.
And the restore is silent-proof: the change is either overwritten or surfaced as a
  validation error, never silently accepted.
```

## Test Requirements

```yaml
tests:
- name: test_hand_edited_not_needed_restored_on_regeneration
  file: unit_tests/test_bo_2200b_5_i.py
  covers:
  - BO-2200b-5-i
  asserts: 'A ticket hand-edited to documentation-verifier: not_needed is restored
    to needed when re-generated/re-validated.'
  framework: unittest
  type: behavioral
- name: test_downgrade_not_silently_accepted
  file: unit_tests/test_bo_2200b_5_i.py
  covers:
  - BO-2200b-5-i
  asserts: The manual downgrade is either overwritten or surfaced as a validation
    error — never silently accepted.
  framework: unittest
  type: behavioral
```

## Implementation Notes

```yaml
config_schema_fragment:
  restore_on_regenerate:
    description: Re-generation/re-validation must re-assert documentation-expert and
      documentation-verifier to needed rather than trust an on-disk not_needed.
    type: object
constraints:
- The non-suppressibility guard must survive a manual downgrade in the ticket file,
  exactly as the TDD-mandatory guard restores test-writer/test-runner.
- Re-generation or re-validation must re-assert needed; a hand-edited not_needed must
  be overwritten or surfaced as a validation error, never silently accepted.
n_location_rule: '1'
post_write_commands: []
reference_file_path: scripts/ac_store/generate_ticket_from_ac.py
required_skills:
- python-coder
```

## Sign-offs

- [ ] python-coder
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit
- [ ] pull-request

## Comments
