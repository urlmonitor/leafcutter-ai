id: PER-100d-2b
title: "Persona-detail script reads and formats full persona YAML content"
component: persona-management
level: L2
status: active
req_status: active
work_status: todo
readiness: reviewed
priority: medium
criteria: |
  Given a persona directory exists at docs/personas/field-service-engineer/persona.yaml
  When the persona detail script is invoked with argument "field-service-engineer"
  Then it reads the full persona.yaml file
  And returns the content formatted as readable Markdown sections including:
    - identity (name, role, archetype)
    - goals (full list)
    - frustrations (full list)
    - context (environment, tools, constraints)
    - All phase-specific sections (discovery, design, development, marketing)
  And the output is written to stdout for the calling skill to capture
depends_on:
  - PER-100g
  - PER-100a-1
  - PER-100a-2
doc_links:
  - path: docs/architecture/agent_knowledge_plane.md
    relationship: describes
    status: exists
assigned_agent: python-coder
estimated_complexity: S
it_requirements:
  - "Must handle errors gracefully per the project error handling policy"
  - "Must complete in <200ms for a single persona file"
  - "Must output valid Markdown to stdout with no side effects"
  - "Must return a non-zero exit code if the persona slug does not resolve to a directory"
delivers_to:
  agent: llm-expert
  contract: "Python script at scripts/persona_detail.py that accepts --persona <slug> and returns formatted Markdown to stdout"
expects_from:
  ac_id: PER-100a-2
  contract: "Persona YAML files with phase-specific sections populated"
origin_agent: it-po-v3
created: 2026-06-08
amended_by:
  - reason: "split: PER-100d -> PER-100d + PER-100g, 2026-06-08"
superseded_by: null
covered_by: []
implemented_by: []
change_target: code
risk_surface: contract_boundary
