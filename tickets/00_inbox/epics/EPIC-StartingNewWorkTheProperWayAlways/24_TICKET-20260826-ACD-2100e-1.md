---
ac_coverage: 7/7
ac_traceability:
  id: ACD-2100e-1
  path: docs/acceptance-criteria/ac-driven-dev/ACD-2100-entry-point-unblocked/ACD-2100e-1.yaml
advances_current_outcome: true
agents:
  ac-fulfillment-gate: signed_off
  ac-validator: signed_off
  architecture-diagram-author: signed_off
  commit: needed
  documentation-expert: signed_off
  documentation-verifier: signed_off
  pull-request: not_needed
change_target: docs
complexity: medium
components:
- ac_driven_dev
- documentation_system
created: '2026-08-26'
depends_on:
- tickets/00_inbox/epics/EPIC-StartingNewWorkTheProperWayAlways/13_TICKET-20260826-ACD-2100c-1.md
- tickets/00_inbox/epics/EPIC-StartingNewWorkTheProperWayAlways/14_TICKET-20260826-ACD-2100c-2.md
- tickets/00_inbox/epics/EPIC-StartingNewWorkTheProperWayAlways/17_TICKET-20260826-ACD-2100c-4.md
- tickets/00_inbox/epics/EPIC-StartingNewWorkTheProperWayAlways/18_TICKET-20260826-ACD-2100c-5.md
documentation_required: true
files_touched:
- docs/INDEX.md
- docs/architecture/diagrams/c3-008-plan-feature-decision-gate-sequence.md
- docs/architecture/components/ac-driven-dev.md
- docs/architecture/components/interactive-pause-resume-substrate.md
- docs/architecture/diagrams/c3-005-artifact-knowledge-graph.md
- docs/components.json
- templates/workflows-js/plan-feature.js
- docs/acceptance-criteria/ac-driven-dev/ACD-2100-entry-point-unblocked/ACD-2100c.yaml
- docs/acceptance-criteria/ac-driven-dev/ACD-2100-entry-point-unblocked/ACD-2100c-1.yaml
- docs/acceptance-criteria/ac-driven-dev/ACD-2100-entry-point-unblocked/ACD-2100c-2.yaml
- docs/acceptance-criteria/ac-driven-dev/ACD-2100-entry-point-unblocked/ACD-2100c-4.yaml
- docs/acceptance-criteria/ac-driven-dev/ACD-2100-entry-point-unblocked/ACD-2100c-5.yaml
- docs/acceptance-criteria/ac-driven-dev/ACD-2100-entry-point-unblocked/ACD-2100e.yaml
- docs/acceptance-criteria/ac-driven-dev/ACD-2100-entry-point-unblocked/ACD-2100e-1.yaml
- docs/acceptance-criteria/ac-driven-dev/ACD-2100-entry-point-unblocked/ACD-2100e-2.yaml
- tickets/00_inbox/epics/EPIC-StartingNewWorkTheProperWayAlways/25_TICKET-20260826-ACD-2100e-2.md
priority: medium
requires_adr: false
requires_diagram: false
risk_surface: internal
roadmap_phase: phase_1
source_ac: ACD-2100e-1
status: todo
title: A sequence diagram shows every point where the route stops to ask and what
  follows from each answer
---

# A sequence diagram shows every point where the route stops to ask and what follows from each answer

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `ACD-2100e-1` — A sequence diagram shows every point where the route stops to ask and what follows from each answer — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `ACD-2100e-1`. Component: `ac-driven-dev`. Assigned agent: `architecture-diagram-author`. Estimated complexity: `M`. Complexity: `medium`.

## Acceptance Criteria

```gherkin
Given a reader who wants to know where the route stops to ask for a decision and
  what follows from each answer,
When they open the project's architecture diagrams,
Then a sequence diagram depicts each point at which the route asks for a
  decision -- approving a drafted product-truth artifact; approving the
  mid-pipeline authoring stage; giving final approval; choosing how to proceed
  when the request looks already covered; and deciding what to do with stranded
  drafts from an earlier run -- and depicts no decision point the route does not
  present,
And each of them is shown as a question put to the person running the route with
  no exchange in which an agent supplies the answer,
And each of them is shown with all three of its exits: an answer from the person
  and the run continuing; no person reachable and the run waiting on a record
  that outlives the process; and an answer that cannot be attributed to the
  person refused with the run still waiting,
And exactly one path in the diagram ends in the drafted work being discarded and
  it is the path the person chooses,
And each path depicted names the criterion whose delivered behaviour it depicts,
And the diagram is reachable from the documentation index and declares the same
  component as this criterion.
```

- [x] AC-1: a sequence diagram depicts each point at which the route asks for a
- [x] AC-2: each of them is shown as a question put to the person running the route with
- [x] AC-3: each of them is shown with all three of its exits: an answer from the person
- [x] AC-4: the run continuing; no person reachable and the run waiting on a record
- [x] AC-5: exactly one path in the diagram ends in the drafted work being discarded and
- [x] AC-6: each path depicted names the criterion whose delivered behaviour it depicts
- [x] AC-7: the diagram is reachable from the documentation index and declares the same

## Implementation Notes

```yaml
- TARGET FILE -- a NEW sequence diagram at exactly docs/architecture/diagrams/c3-008-plan-feature-decision-gate-sequence.md.
  ACD-2100c-1, c-2, c-4 and c-5 all link that exact path as planned, and the name
  satisfies check_diagram_naming's ^c[1-4]-NNN-[a-z0-9-]+.md filename pattern.
  RENUMBERED 006 -> 008 ON 2026-09-14 BY SUPERVISOR ADJUDICATION. The original
  rationale ("c3-005 is the highest c3 number in the directory so c3-006 is free")
  was true at authoring time (2026-08-26) and is now false: c3-006-whole-collection-uniqueness-pass.md
  and c3-007-promise-versus-claim-sequence.md have since merged to main, and
  scripts/next_diagram_seq.py 3 returns 8. See the Comments section for the
  adjudication record. Do not "restore" c3-006 from the older sibling records.
- 'NOTHING IN THIS REPOSITORY WILL CATCH A WRONG PATH -- check-doc-links IS NOT THE
  NET THE STORE NOTES ASSUME. check_doc_links validates DOC_LINKS declared in Python
  and SQL files only, and it is advisory: its own docstring states every failure is
  a warning and it always exits 0. No hook validates a markdown-to-markdown link at
  all. So the four inbound planned links and every outbound link this diagram carries
  are unguarded in both directions. Publishing at the exact path above is the entire
  defence.'
- 'MERMAID COMPLEXITY IS THE BINDING CONSTRAINT AND IT WILL BITE -- DECIDE THE COMPOSITION
  BEFORE DRAWING. check_mermaid_complexity caps a sequence diagram at 8 participants,
  25 interactions and 4 boundaries. Five decision points each shown with three exits
  is at least fifteen arrows before a single setup or return message. The cap must
  NOT be met by dropping an exit or a decision point: the closed-set clause and the
  three-exits clause are the whole criterion. If the depiction cannot be expressed
  within the cap, raise it as a finding -- a second diagram would need its own record,
  and four inbound links point at this one.'
- 'THE PARENT LINK IS A BLOCKING BIDIRECTIONAL COMMIT-TIME REQUIREMENT AND NO ELIGIBLE
  PARENT EXISTS TODAY. check_mermaid_parent_link (exit 1 on violation) requires every
  doc under docs/architecture/ to declare parent: or root: true, the parent file to
  exist, the parent''s flight_level to be STRICTLY higher than the child''s, the parent''s
  children: list to contain this file, and a markdown link to the parent below the
  first mermaid fence. docs/architecture/components/ac-driven-dev.md is itself flight_level
  L3-Component, so it is NOT eligible as the parent of an L3 diagram. Resolve this
  before authoring rather than at commit time: either declare a strictly-higher parent
  and add this file to that parent''s children: list in the SAME commit (docs/architecture/components/interactive-pause-resume-substrate.md
  is the precedent shape at L2-Container but is tagged build_orchestration), or declare
  root: true (docs/architecture/diagrams/c3-005-artifact-knowledge-graph.md is the
  precedent inside this component).'
- 'FRONTMATTER components: MUST BE THE UNDERSCORE GRAPH ID ac_driven_dev -- NOT the
  kebab ac-driven-dev. check_doc_frontmatter validates every components entry against
  the keys of docs/components.json, which are the underscore ids. That declaration
  is the mechanically checkable half of the last Then clause. ACD-2100d-4''s equivalent
  requirement states the kebab form; that wording is wrong and must not be copied.'
- docs/INDEX.md IS GENERATED. Register the diagram by running the build and committing
  the regenerated index -- do not hand-edit it. A hand-edited index is reverted by
  the next build.
- LENGTH IS A REAL CONSTRAINT -- check_doc_length caps a non-ADR document at 300 lines
  and 25 sections. Link out to ADR-024 and to the adjacent diagrams rather than absorbing
  either.
- 'TWO ADJACENT DIAGRAMS EXIST AND BOTH MUST BE LINKED RATHER THAN RESTATED. c3-002-interactive-pause-resume-sequence.md
  is the ADR-024 substrate at message level -- resolveGate, the persist/read exchange,
  the pending-question store, resumeFromRunId -- and c3-001-interactive-pause-resume-run-lifecycle.md
  is the same substrate as a state diagram. Both are tagged build_orchestration and
  both hang off interactive-pause-resume-substrate.md. Neither depicts which decisions
  the route stops at or what each exit costs, which is this diagram''s subject: they
  document how a pause is mechanically effected. If on reading them the author concludes
  c3-006 would duplicate them, that is a finding to raise -- not a licence to merge,
  because four inbound links point here.'
- 'DO NOT DRAW ANY PART OF THIS FROM KI-ACD-005''s FIX DIRECTION. That known issue''s
  stated root cause and stated fix are both out of date -- resolveGate already pauses
  on an unparseable answer and the observed loss came from a well-formed one -- so
  a path drawn from it is wrong on the day it is published. templates/workflows-js/plan-feature.js
  is likewise not the source: it is being replaced underneath this page by the very
  records in depends_on. The source for every depicted path is the delivered behaviour
  of ACD-2100c-1, c-2, c-4 and c-5.'
```

## Agent Contracts

### documentation-expert

Existing docs to update / cross-link:

- docs/architecture/diagrams/c3-008-plan-feature-decision-gate-sequence.md (relationship: creates | status: planned | relevance: THE TARGET FILE, AND THIS RECORD'S SOLE DELIVERABLE. ACD-2100c-1, c-2, c-4 and c-5 already link this exact path as planned; publishing under any other name leaves all four dangling and check-doc-links cannot catch a target that never appears.)
- docs/architecture/adrs/ADR-024-interactive-pause-resume.md (relationship: describes | status: exists | relevance: The pause and resume contract that gives a decision point somewhere to go when no person is reachable -- the second of the three exits.)
- docs/architecture/diagrams/c3-002-interactive-pause-resume-sequence.md (relationship: related | status: exists | relevance: ADJACENT DIAGRAM WITH A DIFFERENT SUBJECT -- the substrate's message-level pause/persist/resume exchange, tagged build_orchestration. This one depicts the route's decision points and their exits. Link to it rather than restating it.)
- docs/architecture/diagrams/c3-001-interactive-pause-resume-run-lifecycle.md (relationship: related | status: exists | relevance: SECOND ADJACENT DIAGRAM -- FOUND BY THE IT PO, NOT IN THE BA's NOTE. The same ADR-024 substrate as a state diagram (running -> paused_awaiting_input -> resumed / nothing_to_resume / unresumable_stale), tagged build_orchestration with interactive-pause-resume-substrate.md as parent. Same instruction as c3-002: link rather than restate.)
- docs/architecture/components/ac-driven-dev.md (relationship: describes | status: exists | relevance: Component doc for the route whose decision points the diagram depicts and the component the diagram must declare. NOTE it is itself flight_level L3-Component so it cannot serve as this diagram's parent -- see the requirements.)
- templates/workflows-js/plan-feature.js (relationship: related | status: exists | relevance: NOT THE SOURCE FOR THIS DIAGRAM. ACD-2100c-1's contract is that the diagram depicts the delivered set of decision points rather than re-derives them from this file; the file is changing underneath the page.)
- docs/INDEX.md (relationship: describes | status: exists | relevance: The index the last clause requires the diagram to be reachable from. It is generated.)

- [x] AC-1: sequence-diagram | docs/architecture/diagrams/c3-008-plan-feature-decision-gate-sequence.md | a sequence diagram depicts each point at which the route asks for a <!-- signed: documentation-expert -->
- [x] AC-2: how-to | docs/architecture/diagrams/c3-008-plan-feature-decision-gate-sequence.md | a sequence diagram depicts each point at which the route asks for a <!-- signed: documentation-expert -->

### Delivers To

- **Agent:** documentation-expert
- **Contract:** ORDERING GAP -- SEE NOTES. The published diagram at docs/architecture/diagrams/c3-008-plan-feature-decision-gate-sequence.md. ACD-2100e-2's how-to links that exact path as planned and sends an operator there to learn how their run came to be waiting, so this record is that link's target. The link is plain markdown and no hook in this repository validates a markdown-to-markdown link, so if the how-to is published first the link is dead and nothing reports it.

### Expects From

- **AC:** ACD-2100c-1
- **Contract:** The final set of decision points and the channel each is put on, which the sequence diagram depicts rather than re-derives from the source.
- **AC:** ACD-2100c-2
- **Contract:** The unreachable-channel exit: no step after the decision point runs, a record naming the run and that decision point survives the process, and the run reports that it is waiting. This is the second of the three exits the diagram shows on every decision point.
- **AC:** ACD-2100c-4
- **Contract:** The refused-answer exit: an answer that cannot be attributed to the person is not carried out and the run stays at the decision point still waiting. This is the third exit.
- **AC:** ACD-2100c-5
- **Contract:** The closed set of ways a run ends holding drafted work -- which of them discard it and which do not -- so the diagram can show discarding on exactly one path and attribute that path to the person.

## Sign-offs

- [x] documentation-expert — 2026-09-14 11:35
- [x] ac-validator — 2026-09-14 12:10
- [x] ac-fulfillment-gate — 2026-09-14 12:21
- [x] documentation-verifier — 2026-09-14 12:27
- [x] architecture-diagram-author — 2026-09-14 11:20
- [ ] commit

## Comments

### 2026-09-14 10:32 — architecture-diagram-author (status: question)

feedback-id: fb_2026-09-14_25a1a905

The mandated target path is unpublishable and I will not pick its replacement
unilaterally, because the fix requires edits outside this ticket's `files_touched`.

**The blocker.** Implementation Notes assert "c3-005 is the highest c3 number in the
directory so c3-006 is free". That was true when ACD-2100e-1 was authored (2026-08-26);
it is false now. Two L3 diagrams have since landed on main:
`c3-006-whole-collection-uniqueness-pass.md` (PR #495, GE-122) and
`c3-007-promise-versus-claim-sequence.md` (PR #702, BP-1100g). `scripts/next_diagram_seq.py 3`
returns **8**.

**The notes name the wrong guard, and the real one is blocking.** The notes argue that
nothing catches a wrong path, so "publishing at the exact path above is the entire
defence". The opposite risk is the live one: `check-identifier-uniqueness`
(`.pre-commit-config.yaml` L112, `always_run: true`, `pass_filenames: false`) runs
`scan_diagrams()` over the whole directory on **every** commit and fails on level+sequence
collisions. Measured against the real tree:

```
BASELINE (real tree):
   NamespaceVerdict(passed=True, inspected_count=26, findings=[])
WITH ticket-mandated c3-006-plan-feature-decision-gate-sequence.md:
   NamespaceVerdict(passed=False, inspected_count=27, findings=[Finding(number='c3-006',
     paths=['.../c3-006-plan-feature-decision-gate-sequence.md',
            '.../c3-006-whole-collection-uniqueness-pass.md'], declared_states={})])
```

Because the hook is `always_run`, committing at c3-006 would block every commit in the
repo, not just this ticket's.

**Why I am not just publishing at c3-008.** Every inbound reference to the planned path
is a string in the AC store or a ticket body — ACD-2100c, c-1, c-2, c-4, c-5, e, e-1, e-2
and six ticket files. None is in a published doc, so nothing is dangling *today*. But none
of those files is in this ticket's `files_touched` either, so renaming silently strands all
of them and makes `files_touched` itself wrong — the phantom-done surface. That is a scope
decision for the adjudicator, not for me.

**Options.**

- **A (recommended).** Publish at `docs/architecture/diagrams/c3-008-plan-feature-decision-gate-sequence.md`
  (the allocator's answer) and add the eight AC records + six ticket files to `files_touched`
  so the planned-path string is updated in the same commit. No published doc links the path
  yet, so this costs nothing but the string edits.
- **B.** Keep c3-006 in the name and renumber `c3-006-whole-collection-uniqueness-pass.md`.
  Rejected unless instructed: that file is merged on main and is itself referenced elsewhere.

**Everything else on this record is feasible — the number is the sole blocker.**
I resolved the two other risks the notes flag, so option A unblocks immediately:

- *Parent link.* The notes are right that no strictly-higher parent exists
  (`ac-driven-dev.md` is itself L3). `root: true` is the in-component precedent —
  `c3-005-artifact-knowledge-graph.md` uses exactly that — and it satisfies
  `check_mermaid_parent_link` without touching
  `interactive-pause-resume-substrate.md`, which is tagged `build_orchestration`.
- *Mermaid complexity.* Confirmed caps are 8 participants / 25 interactions / 4 boundaries.
  Five decision points x three exits fits: 4 participants (operator, route, pending-question
  store, answer-attribution check) and ~20 interactions, using one `alt` per decision point.
  No exit or decision point needs dropping, so the closed-set and three-exits clauses hold.
- *Existing-diagram check (skill 3b).* No existing L3 diagram declares `ac_driven_dev` in
  frontmatter `components:` — c3-005 declares `knowledge_management`, c2-005 is L2 — so a
  new file is correct and this is not a duplicate of c3-001/c3-002.
- *Frontmatter.* Will declare `components: [ac_driven_dev]` (underscore graph id), per the
  notes and against ACD-2100d-4's kebab wording.

**Adjudication required:** confirm option A (or name another), and authorise the
`files_touched` expansion to cover the AC-store and ticket path strings. I will then
scaffold and publish in one pass. No file has been written.

### 2026-09-14 10:35 — documentation-expert (status: blocker)

feedback-id: fb_2026-09-14_6311a698
completion_manifest:
  doc_written:
    result: false
    reason: "The sole deliverable this ticket's Agent Contracts assign to documentation-expert is the sequence diagram at docs/architecture/diagrams/c3-006-plan-feature-decision-gate-sequence.md, plus cross-links from ADR-024, c3-002, c3-001, and ac-driven-dev.md to it. architecture-diagram-author (comment above, status: question) confirmed that path is unpublishable: c3-006 and c3-007 are already occupied on main (PR #495, PR #702), and check-identifier-uniqueness is always_run so committing at c3-006 blocks every commit in the repo, not just this ticket's. No file exists at any candidate path yet."
    remediation: "Do not resolve this by unilateral choice of a replacement number. The adjudicator (ticket/epic supervisor or the user) must confirm architecture-diagram-author's Option A (publish at c3-008 and expand files_touched to cover the AC-store and ticket path strings that reference the planned c3-006 name) or name another resolution. Once the diagram exists at its adjudicated path, respawn documentation-expert to perform the cross-links."
  cross_links_added: false
  diataxis_genre_correct: true
Blocked on the same unresolved architectural question architecture-diagram-author raised: the ticket-mandated target path docs/architecture/diagrams/c3-006-plan-feature-decision-gate-sequence.md collides with a file already merged to main and cannot be published there without breaking the always-run check-identifier-uniqueness hook for every commit in the repo. My own deliverable (cross-linking ADR-024, c3-002, c3-001, and ac-driven-dev.md to the new diagram, and ensuring docs/INDEX.md reachability) depends entirely on that diagram existing at a settled path, so I cannot proceed without guessing at a path resolution that is explicitly flagged as requiring adjudication and touches files outside this ticket's declared files_touched. No doc file was written or edited by this phase. Recommend the supervisor route this ticket to the user/adjudicator for the Option A vs. Option B decision (or another resolution), then respawn both architecture-diagram-author and documentation-expert.

### 2026-09-14 11:20 — architecture-diagram-author (status: ok)

feedback-id: fb_2026-09-14_064b1740
completion_manifest:
  diagram_created: true
  flight_level_correct: true
  cross_links_added:
    result: false
    reason: "The ADR-024 / c3-002 / c3-001 / ac-driven-dev.md cross-links and the docs/INDEX.md reachability clause (AC-7) are owned by documentation-expert, which runs after this phase. This dispatch was explicitly scoped to exclude them. The diagram's own outbound links to all four of those documents ARE present in its Cross-References section; what is absent is the inbound half from those four pages back to c3-008."
    remediation: "Respawn documentation-expert to add the inbound cross-links and to regenerate docs/INDEX.md via the build (the index is generated — do not hand-edit it). The target path is now settled, which was the sole thing blocking that phase."
  path_collision_resolved: true

**ADJUDICATION RECORD — the c3-006 collision and its resolution.**

The collision I raised at 10:32 was confirmed by the supervisor against the real
directory listing: `docs/architecture/diagrams/` contains BOTH
`c3-006-whole-collection-uniqueness-pass.md` and `c3-007-promise-versus-claim-sequence.md`,
so `c3-006` is occupied and `scripts/next_diagram_seq.py 3` returns `8`.

**Decision: Option A.** The diagram is published at
`docs/architecture/diagrams/c3-008-plan-feature-decision-gate-sequence.md`.
**Option B (renumbering the already-merged `c3-006-whole-collection-uniqueness-pass.md`)
was rejected** — that file is on main and renumbering it would break inbound links to it.

Why this is recorded here rather than left in the history: four sibling AC records and
this ticket's own Implementation Notes asserted `c3-006` as a fixed, non-negotiable path,
on the rationale that "c3-005 is the highest c3 number in the directory". That rationale
was true on 2026-08-26 and false by 2026-09-14. I have corrected that sentence in both
`ACD-2100e-1.yaml` and this ticket's notes so the stale premise cannot be re-derived by a
later reader. **Do not "restore" c3-006 from the older sibling records.**

**Re-pointed the planned path in the AC store (the live source of truth)** — all eight
records under `docs/acceptance-criteria/ac-driven-dev/ACD-2100-entry-point-unblocked/`:
`ACD-2100c`, `c-1`, `c-2`, `c-4`, `c-5`, `ACD-2100e`, `e-1`, `e-2`. Verified zero
`c3-006-plan-feature` references remain anywhere under `docs/`. Also re-pointed ticket 25.
Tickets 13, 14, 15, 17 and 18 were deliberately **not** edited: they are done and
committed, and their bodies are a historical record of what was planned at authoring time.
They therefore still name `c3-006` — that is intentional, not drift.

The historical text inside this ticket's own `## Comments` (the measured collision evidence
at 10:32 and documentation-expert's 10:35 blocker) still says `c3-006` for the same reason.

**The diagram.** Five decision points, each with all three exits, within the
8-participant / 25-interaction / 4-boundary cap: 6 participants, 15 interactions, 0
boundaries. The three exits are drawn **once** around the whole set rather than copied five
times, because `ACD-2100c-1`'s delivered behaviour is explicitly class-wide (its own test
requires a *sixth* point to inherit the rule with no per-site registration) — copying them
would depict five per-site patches where the mechanism is one rule. No exit and no decision
point was dropped to meet the cap, so the closed-set and three-exits clauses both hold.
Exactly one path discards the drafted work and it is the person's own choice to stop
(`ACD-2100c-5`). Frontmatter declares `components: [ac_driven_dev]` — the underscore graph
id, per this ticket's notes and against ACD-2100d-4's kebab wording.

Verified green: `check_diagram_naming` (0), `check_doc_frontmatter` (PASSED 1 doc),
`check_mermaid_complexity` (0), `check_doc_length` (0), `validate_ac_schema` (all 31 valid).
`check_identifier_uniqueness` verbatim: `acceptance-criteria: OK (3814 inspected)` /
`decisions: OK (39 inspected)` / `diagrams: OK (27 inspected)` / `work-items: OK (299
inspected)`, exit 0 — 27 diagrams against a 26 baseline, no collision.

Parent link resolved with `root: true`, matching the in-component precedent
`c3-005-artifact-knowledge-graph.md`. `check_mermaid_parent_link` returns early on
`root: true` (line 242), so all three sub-checks — level order, prose link, and the parent's
`children:` back-reference — are skipped. No edit to
`interactive-pause-resume-substrate.md` was needed.

### 2026-09-14 11:35 — documentation-expert (status: ok)

feedback-id: fb_2026-09-14_e0160b04
completion_manifest:
  doc_written: true
  cross_links_added: true
  diataxis_genre_correct: true
Added the inbound half of the cross-link pairs architecture-diagram-author's 11:20
sign-off flagged as absent: `docs/architecture/adrs/ADR-024-interactive-pause-resume.md`
(new `## References` entry), `docs/architecture/diagrams/c3-001-interactive-pause-resume-run-lifecycle.md`
and `c3-002-interactive-pause-resume-sequence.md` (new `## Cross-References` entries), and
`docs/architecture/components/ac-driven-dev.md` (new `related_docs:` frontmatter entry plus
a new `## Architecture Diagrams` body section) — all four now link forward to
`docs/architecture/diagrams/c3-008-plan-feature-decision-gate-sequence.md`, closing AC-7's
reachability requirement via the chain `docs/INDEX.md` → `ac-driven-dev.md` (already listed
under Components) → `c3-008...md`, both declaring the same underscore component
`ac_driven_dev`. Ran `scripts/generate_doc_index.py`: zero-byte diff, confirmed expected —
the "Architecture Diagrams" category in `_CATEGORIES` scans `docs/architecture/*.md`
non-recursively and has never reached the `docs/architecture/diagrams/` subdirectory, so no
c3-NNN diagram (including the pre-existing c3-001/c3-002/c3-005) has ever appeared there;
this is a pre-existing gap in the generator, out of this ticket's scope, and the
component-page cross-link is the mechanism that actually satisfies reachability. Re-ran
`check_doc_frontmatter`, `check_doc_length`, and `check_mermaid_parent_link` on every edited
file (all pass) and `check_identifier_uniqueness` from the worktree root via
`env --chdir=<worktree> python3 templates/scripts/commit_guardian/check_identifier_uniqueness.py`
(diagrams: OK, 27 inspected — unchanged from architecture-diagram-author's baseline, since
no new diagram file was added in this phase). Flipped both `documentation-expert` AC
checkboxes under `## Agent Contracts`. Note: `templates/workflows-js/plan-feature.js` was
read for context per its Agent Contracts entry but intentionally not edited or linked from,
per the ticket notes' explicit instruction that it is not this diagram's source and is being
replaced underneath this page.

### 2026-09-14 12:21 — ac-fulfillment-gate (status: ok)

feedback-id: (submit-failed)
completion_manifest:
  work_status_verified: true
  implemented_by_verified: true
  covered_by_verified: true
Ran `.leafcutter/scripts/ac_store/ac_coverage_resolver.py` against this ticket, which
resolved exactly one AC to check: `ACD-2100e-1` (L2, block form `{id, path}`). Two fields
were stale and auto-fixed, both append-only: `work_status: todo` -> `done` (branch evidence:
`git diff main...HEAD` plus the untracked new diagram file both intersect this ticket's
`files_touched`), and `implemented_by` gained
`docs/architecture/diagrams/c3-008-plan-feature-decision-gate-sequence.md` and
`docs/architecture/components/ac-driven-dev.md` — the two artifacts that actually deliver
this criterion's Then-clauses (the diagram itself, and the component page providing the
AC-7 reachability chain), rather than every `files_touched ∩ diff` entry mechanically
(several other touched files in this ticket's `files_touched` are sibling AC-store yaml
records and a renumbering-fix ticket, which are not themselves implementation artifacts of
this diagram AC; adding them would have been noise, not evidence). Ran
`scripts/commit_guardian/check_ac_schema.py` on the edited file after the edit: exit 0.
`covered_by` remains `[]` and I am treating that as a PASS rather than a blocker: this AC
declares `test_required: false` with an explicit `test_rationale` (no executable surface —
a mermaid diagram), and `templates/scripts/commit_guardian/check_done_proof.py` — the actual
authoritative CI gate this record's mechanical enforcement ultimately answers to — silently
exempts `test_required: false` ACs from the covers-tag/covered_by mandate by design (BO-2500b).
I confirmed no `# covers: ACD-2100e-1` tag exists anywhere under `tests/` or `unit_tests/`
before applying the exemption, so this is not a missed auto-fix. **Anomaly**: the shared
`ac_coverage_resolver.py` does not implement this `test_required:false` exemption — it still
reports `covered_by` as a failed field for this AC even after the other fixes, so its `ok:
false` / non-zero exit should not be read as this gate's final verdict; my own Step 2f/3c
judgment, informed by `check_done_proof.py`'s documented semantics, supersedes it here. Also
note: `submit_feedback.py` rejects `ac-fulfillment-gate` as a writer for every category tried
(`complete`, `quality-concern`) per the deployed `config/feedback_categories.yaml` allowed-writers
list — this agent is not registered there at all; used `feedback-id: (submit-failed)` per the
signoff skill's fallback rule rather than aborting sign-off.

### 2026-09-14 12:27 — documentation-verifier (status: ok)

feedback-id: fb_2026-09-14_950a9106
completion_manifest:
  required_docs_list_parsed: true
  all_required_docs_present_in_diff: true
  no_placeholder_content_in_changed_docs: true
Documentation coverage verified. Required doc (1, named twice — AC-1 and AC-2 both target
the same path): `docs/architecture/diagrams/c3-008-plan-feature-decision-gate-sequence.md`.
Integration target resolved to `origin/main` (Candidate 1, `@{upstream}` =
`origin/EPIC-StartingNewWorkTheProperWayAlways`, was rejected as self-tracking per the
template's mandatory check before falling through to Candidate 2, `refs/remotes/origin/HEAD`).
All four placeholder sub-checks passed on the file: 6a helper script scan (`build_placeholder_detection.scan_for_placeholders`)
returned zero hits; 6b `\bTBD\b` grep, zero; 6c unfilled `{token}`-style grep, zero; 6d read
the full file and confirmed substantial real content (frontmatter, a 5-decision/3-exit
mermaid sequence diagram, prose, two tables, a callout block quote, and a Cross-References
section) — not a stub. **Anomaly (template gap, not a ticket defect):** the file is a
brand-new file that is still fully **untracked** (`git status --porcelain` shows `??`, since
the `commit` phase has not yet run) — it therefore appears in neither of the two literal Step
4b commands (`git diff --name-only origin/main...HEAD` and `git diff HEAD --name-only`),
because plain `git diff` never surfaces untracked paths regardless of comparison base. I
resolved this by using `git status --porcelain` (read-only) to confirm the path is present in
the working tree with real content, consistent with Step 4b's own stated intent ("OR it is
sitting uncommitted in the working tree right now") which the two prescribed commands do not
fully implement for a wholly new, never-`git add`-ed file. Recommend a future edit to this
template's Step 4b add a third, explicitly-untracked-files check (e.g. `git ls-files --others
--exclude-standard` or `git status --porcelain --untracked-files=all`) so this doesn't require
per-invocation judgment calls on every ticket that authors a brand-new doc before its commit
phase runs — this is likely to recur on every doc-creating ticket verified prior to commit.
