---
ac_traceability:
  id: UXP-700e-1-i
  path: docs/acceptance-criteria/ux-prototyping/UXP-700-truthful-project-record/UXP-700e-1-i.yaml
advances_current_outcome: true
agents:
  ac-fulfillment-gate: needed
  ac-validator: needed
  architect-review: signed_off
  commit: needed
  documentation-expert: needed
  documentation-verifier: needed
  frontend-coder: not_needed
  pr-reviewer: needed
  pull-request: not_needed
  python-coder: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: signed_off
  user-surface-smoker: not_needed
change_target:
- code
- schema
complexity: medium
components:
- ux_prototyping
created: '2026-09-09'
depends_on: []
documentation_required: true
files_touched:
- docs/product-truth/schemas/flow.schema.json
priority: medium
requires_adr: false
requires_diagram: false
risk_surface: contract_boundary
roadmap_phase: phase_1
source_ac: UXP-700e-1-i
status: todo
title: A new bound arrives as a warning behind a declared shape version, and blocks
  nothing that predates it
---

# A new bound arrives as a warning behind a declared shape version, and blocks nothing that predates it

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `UXP-700e-1-i` — A new bound arrives as a warning behind a declared shape version, and blocks nothing that predates it — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `UXP-700e-1-i`. Component: `ux-prototyping`. Assigned agent: `python-coder`. Estimated complexity: `M`. Complexity: `medium`.

## Acceptance Criteria

```gherkin
Given a proposed longest-description bound of 120 characters, which every one of the 14
  journeys in the record today exceeds,
When that bound is introduced,
Then each journey states which version of the record's shape it was authored against,
And a journey authored against a version older than the one in which the bound took effect is
  reported as a warning and does not stop the work that triggered the check,
And only a journey declaring the version in which the bound took effect is held to it,
And a journey declaring no version at all is treated as predating the bound and is reported
  as needing a version, not as violating the bound.
```

- [ ] AC-1: each journey states which version of the record's shape it was authored against
- [ ] AC-2: a journey authored against a version older than the one in which the bound took effect is
- [ ] AC-3: only a journey declaring the version in which the bound took effect is held to it
- [ ] AC-4: a journey declaring no version at all is treated as predating the bound and is reported

## Test Requirements

```yaml
tests:
- name: test_journey_predating_the_bound_is_warned_and_not_blocked
  file: unit_tests/product_truth/test_uxp_700e_1_i.py
  covers:
  - UXP-700e-1-i
  asserts: A journey declaring an older shape version exceeding the bound produces
    a warning and does not stop the caller.
  framework: unittest
  type: integration
  angle: boundary
- name: test_journey_declaring_the_new_version_is_held_to_the_bound
  file: unit_tests/product_truth/test_uxp_700e_1_i.py
  covers:
  - UXP-700e-1-i
  asserts: A journey declaring the version in which the bound took effect and exceeding
    it is reported as a violation.
  framework: unittest
  type: integration
  angle: criterion
- name: test_journey_with_no_declared_version_is_treated_as_predating_the_bound
  file: unit_tests/product_truth/test_uxp_700e_1_i.py
  covers:
  - UXP-700e-1-i
  asserts: An unversioned journey is reported as needing a version, not as violating
    the bound.
  framework: unittest
  type: integration
  angle: boundary
- name: test_uxp_700e_1_i_reachable_from_entry_point
  file: unit_tests/product_truth/test_uxp_700e_1_i.py
  covers:
  - UXP-700e-1-i
  angle: reachability
  asserts: 'REQUIRED — invoke the production entry point (CLI, hook, slash command,
    workflow dispatch, or main()) as a subprocess/dispatch and assert the new behaviour
    actually occurs. Do NOT satisfy this by importing the function directly. The AC
    authored no test_spec, so the entry point is not declared: resolve it before writing
    this test, and do not delete this entry.'
```

## Agent Contracts

### documentation-expert

Existing docs to update / cross-link:

- docs/product-truth/schemas/flow.schema.json (relationship: specifies | status: exists)

- [ ] AC-1: (unspecified genre) | docs/product-truth/schemas/flow.schema.json | each journey states which version of the record's shape it was authored against

### Expects From

- **AC:** UXP-700e-1
- **Contract:** The bound measurement this record decides the consequence of.

## Sign-offs

- [x] architect-review — 2026-09-09 17:17
- [x] test-writer — 2026-09-09 18:05
- [ ] python-coder
- [ ] test-runner
- [ ] documentation-expert
- [ ] pr-reviewer
- [ ] ac-validator
- [ ] ac-fulfillment-gate
- [ ] documentation-verifier
- [ ] commit

## Comments

### 2026-09-09 17:17 — architect-review (status: ok)
feedback-id: fb_2026-09-09_8ac77888
completion_manifest:
  blast_radius_reviewed: true
  rubric_classification_recorded: true
  cross_layer_seam_answer:
    result: not_applicable
    reason: "architect-review performs classification/routing only; it writes no production code and creates no layer boundary of its own."
    remediation: "n/a — not_applicable is a first-class outcome, not a defect."
Classified small: 1 file touched (docs/product-truth/schemas/flow.schema.json), 1 component (ux_prototyping), no cross-module boundary, and none of the always-large triggers (alembic, hypertable, public API, ADR-contract) fired. Confirmed requires_adr: false and requires_diagram: false already set in frontmatter align with judgment — this extends the existing bound-declaration pattern from UXP-700e-1 rather than introducing a new cross-cutting policy. Flagged one design concern for python-coder: the schema's existing `version` field (integer, no description) risks ambiguity with the new "shape version a journey was authored against" concept this AC introduces — use a distinctly named field (e.g. `shape_version`) rather than overloading `version`. No acceptance-criteria adjustments proposed. No sub-agents were available in this invocation's toolset (no Agent/Task tool), so blast-radius analysis was performed directly via Bash/Read against the repo instead of dispatching research-agent; noting this as a process anomaly, not a scope gap.

### 2026-09-09 18:05 — test-writer (status: ok)
feedback-id: fb_2026-09-09_4eae9a97
completion_manifest:
  tests_written: true
  red_baseline_captured: true
  cross_layer_seam_answer:
    result: covered
    producing_side: "generate_product_truth.load_flows() reading a real on-disk *.flow.json file"
    consuming_side: "validate_product_truth._check_shape_version_bounds() (new function this ticket adds)"
  reachability_entry_point_answer:
    result: resolved
    entry_point: "python docs/product-truth/scripts/validate_product_truth.py (CLI via subprocess, main() guarded by if __name__ == '__main__':) — the ticket's own Test Requirements entry for this test carried the unresolved sentinel, so this entry point was resolved fresh via Step 1 category 1 (existing CLI script with a main() guard), matching the same, already-shipped entry point UXP-700c-1's sibling test pinned."
red_baseline:
  - test_name: test_journey_predating_the_bound_is_warned_and_not_blocked
    file: unit_tests/product_truth/test_uxp_700e_1_i.py
    error: "AttributeError: module 'validate_product_truth' has no attribute '_check_shape_version_bounds'"
  - test_name: test_journey_declaring_the_new_version_is_held_to_the_bound
    file: unit_tests/product_truth/test_uxp_700e_1_i.py
    error: "AttributeError: module 'validate_product_truth' has no attribute '_check_shape_version_bounds'"
  - test_name: test_journey_with_no_declared_version_is_treated_as_predating_the_bound
    file: unit_tests/product_truth/test_uxp_700e_1_i.py
    error: "AttributeError: module 'validate_product_truth' has no attribute '_check_shape_version_bounds'"
  - test_name: test_journey_within_the_bound_is_never_reported
    file: unit_tests/product_truth/test_uxp_700e_1_i.py
    error: "AttributeError: module 'validate_product_truth' has no attribute '_check_shape_version_bounds'"
  - test_name: test_on_disk_journey_predating_the_bound_is_warned_via_the_real_loader
    file: unit_tests/product_truth/test_uxp_700e_1_i.py
    error: "AttributeError: module 'validate_product_truth' has no attribute '_check_shape_version_bounds'"
  - test_name: test_uxp_700e_1_i_reachable_from_entry_point
    file: unit_tests/product_truth/test_uxp_700e_1_i.py
    error: "AssertionError: 1 != 0 : a journey that only PREDATES the newly introduced bound must not stop the run via the real CLI; stdout='{\"outcome\": \"failed\", \"empty_types\": [\"mock-data\", \"mockups\"]}\\n' stderr=\"resolved 0 pointer(s)\\nFAIL: [schema] flow fixture-product/cli-old-shape-journey: Additional properties are not allowed ('shape_version' was unexpected)\\n1 error(s), 0 warning(s)\\n\""
    note: "Confirmed under AC_ENFORCE_STRICT=1 (this repo's conftest downgrades AC-tagged failures to xfail when the AC's work_status != done, so the default run shows this as 1 xfailed rather than a raw failure — the underlying assertion is genuinely red either way, exit code 1 on the whole file)."
Wrote unit_tests/product_truth/test_uxp_700e_1_i.py: 6 failing tests (3 direct-call, 1 seam, 1 boundary-extra, 1 reachability) pinning UXP-700e-1-i's version-gated bound-transition contract against a NEW `_check_shape_version_bounds()` this ticket adds to the existing, already-shipped docs/product-truth/scripts/validate_product_truth.py, following the same `_check_*`-extension pattern architect-review directed for the sibling ticket UXP-700c-1. Also added a `shape_version` OPTIONAL flow field to the fixture builder (matching architect-review's guidance in this ticket's own Comments to keep it distinct from the schema's existing `version` field) — python-coder must add this property to docs/product-truth/schemas/flow.schema.json (this ticket's declared files_touched) and implement + wire in `_check_shape_version_bounds`. Verified red: `python -m pytest unit_tests/product_truth/test_uxp_700e_1_i.py -q` exits 1 (5 raw AttributeError failures + 1 AC-enforcement-masked xfail whose underlying AssertionError was independently confirmed red under `AC_ENFORCE_STRICT=1`); no syntax/import errors. Note: UXP-700e-1 (this AC's own `depends_on`, ticket #38 in this same worktree) has not yet been implemented either — `_check_shape_version_bounds` and the bound-measurement machinery it will sit alongside do not exist in `validate_product_truth.py` today, so python-coder for this ticket may need to land (or coordinate landing) both #38 and #39's production code together, per UXP-700e-1's own notes ("the transition rules ... must land in the same piece of work").
