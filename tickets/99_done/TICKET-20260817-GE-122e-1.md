---
ac_traceability:
  id: GE-122e-1
  path: docs/acceptance-criteria/guardrail-engine/GE-122-numbers-mean-one-thing/GE-122e-1.yaml
advances_current_outcome: true
agents:
  ac-fulfillment-gate: needed
  ac-validator: needed
  commit: needed
  documentation-expert: needed
  documentation-verifier: needed
  pull-request: needed
  python-coder: signed_off
  test-runner: signed_off
  test-writer: signed_off
change_target: docs
complexity: medium
components:
- commit_guardian
- ac_store
created: '2026-08-17'
depends_on: []
documentation_required: true
files_touched:
- docs/acceptance-criteria/guardrail-engine/GE-111-traceability-stays-honest/GE-111f.yaml
- docs/acceptance-criteria/guardrail-engine/GE-111-traceability-stays-honest/GE-111.yaml
- templates/scripts/commit_guardian/check_contract_shrinking.py
- unit_tests/commit_guardian/test_contract_shrinking_ignores_mentions.py
- unit_tests/commit_guardian/test_ge_119_contract_shrinking_rename_aware.py
- unit_tests/commit_guardian/test_ge_122e_1.py
- changelogs/2026-08-14-0037-fix-guardian-tell-an-edited-test-from-a-deleted-one-ge-119.md
priority: high
requires_adr: false
requires_diagram: false
risk_surface: internal
roadmap_phase: phase_1
source_ac: GE-122e-1
status: done
title: The one contested requirement number is resolved without breaking what cites
  it
---

# The one contested requirement number is resolved without breaking what cites it

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `GE-122e-1` (originally authored and implemented as `GE-120e-1`; renumbered 2026-08-18 after this tree's own identifier collision with origin/main — see the AC file's amended_by history) — The one contested requirement number is resolved without breaking what cites it — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `GE-120e-1`, since renumbered to `GE-122e-1`. Component: `guardrail-engine`. Assigned agent: `python-coder`. Estimated complexity: `M`. Complexity: `medium`.

### `files_touched` was corrected by hand — read this before trusting it

The generator derives `files_touched` from the AC's `doc_links`, which conflates "files this work modifies" with "documents the implementer should read". Two corrections were applied:

- **Removed `scripts/adr_refs.py`.** It is a `relationship: context` link — a worked precedent for three-way citation rewriting. Do NOT modify it.
- **Added four paths the generator could not infer**: `templates/scripts/commit_guardian/check_contract_shrinking.py` (line 54 cites the moved record and must be repointed), the changelog that means the moved record (gets a clarifying note, NOT a rewritten citation), the new test module, and the parent AC whose `covered_by` must gain the moved record.

The expected destination is now **`GE-111f`**, under `GE-111-traceability-stays-honest/`, not the `GE-121` the AC notes originally suggested. Reason (found while writing the tests): `check_ac_parent_covered_by.py` and `scan_ac_orphans.py` both derive a record's parent from its **id shape** and never read the documented `parent:` field. A root-shaped `GE-121` therefore has no derivable parent, so the back-link this AC requires would sit in the file unenforced by the gate meant to police it — a paper-only parent, which is the failure class this whole tree exists to end. `GE-111f` is verified free by ADR-029's both-tests rule (zero files, zero citations repo-wide) and `GE-111` has 5 children against a cap of 7.

This is still the expected resolution, not a directive. If you justify a different parent, take **that parent's next free suffix** rather than a new root number, and update `files_touched` in the same commit.

### Two facts in the AC were wrong and have been corrected — do not re-introduce them

1. **The tag count is fifteen across two modules, not nine in one.** 9 in `test_ge_119_contract_shrinking_rename_aware.py`, 6 in `test_contract_shrinking_ignores_mentions.py`. Both the BA and the IT PO reported nine-in-one-file because they enumerated citations by looking in the file whose *name* contained the identifier. Enumerate by grepping content across the whole test tree. Repairing only nine would leave six tags silently resolving to the goal record — the exact false-coverage failure this AC exists to prevent.
2. **`GE-119` cannot be the parent**, despite being the best semantic fit. Adding a child would modify `GE-119.yaml`, which the criterion requires to be byte-identical afterwards.

### Citations that must NOT be rewritten

A blanket search-and-replace fails this ticket. These cite the **goal** record or its children and stay exactly as they are: the 32 files under `GE-119-green-means-checked/` (32 measured, not the 33 the AC text first claimed), `changelogs/2026-08-17-1204-...-ge-119-acs-1200-...md`, the `GE-119b` references in `ACS-1200.yaml` / `ACS-1200a.yaml` / `ACS-1200a-2.yaml`, and the bare `GE-119` at `ACS-1200a-2.yaml:127` ("exactly the failure GE-119 exists to close"), which means the goal record. The `GE-120*` files reference `GE-119` as the worked example and must keep describing the collision in the past tense rather than being repointed.

## Acceptance Criteria

```gherkin
Given the requirement collection contains two records claiming the identifier "GE-119": a
  goal-level record owning a folder of 33 files, and a single parentless detail-level record at
  the namespace root whose subject is the contract-shrinking guard,
And fifteen test functions across two test modules carry a coverage tag naming "GE-119", all of
  them written for the parentless record,
And a guard source file and two release notes cite "GE-119" — one release note meaning the
  parentless record, the other meaning the goal record,
When the repair is complete,
Then exactly one record in the collection claims "GE-119", and it is the goal record,
And every one of the 33 files in that goal record's folder is unchanged,
And the parentless record claims an identifier in the same namespace that no other artifact
  claims, and that was not previously claimed by any retired artifact,
And it declares a parent that exists, and that parent lists it among its children,
And every one of those fifteen coverage tags, in both modules, names the identifier the moved
  record now claims, so each tag resolves to exactly one record,
And the guard source citation names the moved record's new identifier,
And each remaining citation of "GE-119" in a dated historical record either genuinely refers to
  the goal record, or states the identifier the moved record now carries, so no reader is left
  holding a citation that resolves to the wrong work,
And no citation of either identifier anywhere in the repository resolves to zero records or to
  more than one.

Coverage note: covered by executing the whole-collection uniqueness pass and a repository-wide
citation resolution over the repaired tree, asserting one claimant for the old identifier, one
for the new, and no unresolved or multiply-resolved citation of either. Asserting only that the
file was renamed does not cover it — the references are where a renumbering does its damage.
```

- [ ] AC-1: fifteen test functions across two test modules carry a coverage tag naming "GE-119", all of
- [ ] AC-2: a guard source file and two release notes cite "GE-119" — one release note meaning the
- [ ] AC-3: exactly one record in the collection claims "GE-119", and it is the goal record
- [ ] AC-4: every one of the 33 files in that goal record's folder is unchanged
- [ ] AC-5: the parentless record claims an identifier in the same namespace that no other artifact
- [ ] AC-6: it declares a parent that exists, and that parent lists it among its children
- [ ] AC-7: every one of those fifteen coverage tags, in both modules, names the identifier the moved
- [ ] AC-8: the guard source citation names the moved record's new identifier
- [ ] AC-9: each remaining citation of "GE-119" in a dated historical record either genuinely refers to
- [ ] AC-10: no citation of either identifier anywhere in the repository resolves to zero records or to

## Test Requirements

```yaml
tests:
- name: test_old_identifier_has_exactly_one_claimant_after_repair
  file: unit_tests/commit_guardian/test_ge_122e_1.py
  covers:
  - GE-122e-1
  asserts: Over the repaired tree, exactly one record claims GE-119 and it is the
    goal-level record; the whole-collection uniqueness pass reports no finding for
    that identifier.
  framework: unittest
  type: behavioral
- name: test_moved_record_claims_a_free_identifier_with_a_real_parent
  file: unit_tests/commit_guardian/test_ge_122e_1.py
  covers:
  - GE-122e-1
  asserts: The moved record claims an identifier no other artifact claims and that
    no citation refers to, declares a parent that exists, and appears in that parent's
    covered_by list.
  framework: unittest
  type: behavioral
- name: test_goal_records_thirty_three_files_are_unchanged
  file: unit_tests/commit_guardian/test_ge_122e_1.py
  covers:
  - GE-122e-1
  asserts: Every file in the goal record's folder is byte-identical to its pre-repair
    content — the cheap direction was actually taken.
  framework: unittest
  type: behavioral
- name: test_every_coverage_tag_resolves_to_exactly_one_record
  file: unit_tests/commit_guardian/test_ge_122e_1.py
  covers:
  - GE-122e-1
  asserts: The test tree is grepped for coverage tags naming either identifier; all
    fifteen (9 in test_ge_119_contract_shrinking_rename_aware.py, 6 in test_contract_shrinking_ignores_mentions.py)
    name the moved record's new identifier, and each resolves to exactly one record.
    The test discovers tags by scanning, so it fails if a tag exists in a module nobody
    thought to look in — an assertion hard-coded to one filename would have passed
    the defective nine-tag repair.
  framework: unittest
  type: behavioral
- name: test_no_citation_of_either_identifier_resolves_to_zero_or_many
  file: unit_tests/commit_guardian/test_ge_122e_1.py
  covers:
  - GE-122e-1
  asserts: A repository-wide citation resolution over both identifiers finds no citation
    resolving to zero records and none resolving to more than one — the assertion
    the coverage note says a rename alone does not satisfy.
  framework: unittest
  type: behavioral
- name: test_dated_historical_records_are_not_repointed
  file: unit_tests/commit_guardian/test_ge_122e_1.py
  covers:
  - GE-122e-1
  asserts: The release note that legitimately means the goal record is unchanged,
    and the one meaning the moved record carries a clarifying statement rather than
    a rewritten citation — so a blanket search-and-replace fails this test.
  framework: unittest
  type: behavioral
```

## Implementation Notes

```yaml
- 'CORRECTED 2026-08-17 — THE TAG COUNT IS FIFTEEN, IN TWO FILES, NOT NINE IN ONE.
  Measured: `unit_tests/commit_guardian/test_ge_119_contract_shrinking_rename_aware.py`
  carries 9 `# covers: GE-119` comments and `unit_tests/commit_guardian/test_contract_shrinking_ignores_mentions.py`
  carries 6 more, all fifteen written for the parentless record. The BA and IT PO
  both recorded nine-in-one-file; the second module was missed because its filename
  does not name the identifier. This correction is load-bearing: the done-proof oracle
  resolves these tags, so repairing only the nine that live in the obviously-named
  file would leave six silently re-pointed at the goal record — manufacturing exactly
  the false coverage claim this AC exists to prevent, inside the repair itself. Enumerate
  tags by grepping the whole test tree for the identifier, never by trusting a filename.'
- 'The identifier chosen must satisfy ADR-029''s operative rule as applied to this
  namespace: free means NEITHER a file NOR a citation, not merely ''no file exists''.
  Reusing a number that text still cites converts a dangling reference into a confidently
  wrong one, which is worse than the ambiguity being repaired.'
- 'WHEN LISTING TAKEN IDENTIFIERS, scan the loose files as well as the folders. Verified
  2026-08-17: `GE-114-1.yaml` through `GE-114-4.yaml`, `GE-115.yaml` and the `GE-119.yaml`
  being moved all sit at the namespace root alongside the feature folders, so a folder-only
  listing under-reports what is taken and is a plausible way to mint a second collision
  while repairing the first. GE-121 is free by both tests as of 2026-08-17.'
- 'PARENT SELECTION — THE OBVIOUS CHOICE IS FORBIDDEN, AND THE AC FORBIDS IT ONLY
  IMPLICITLY. The most natural semantic parent for a record about the contract-shrinking
  guard is GE-119 itself (`Trust that a green check actually checked something`, 4
  children against a cap of 7): a gate so false-positive-prone that it must be bypassed
  with SKIP is precisely a green check that stopped checking. It is nonetheless RULED
  OUT, because parenting under it requires adding the child to GE-119.yaml''s covered_by,
  and GE-119.yaml is one of the 33 files this AC requires to be byte-identical afterwards.
  Verify the survivor set is untouched BEFORE assuming a parent is available. Verified
  2026-08-17, guardrail-engine L0s and their child counts against the cap of 7: GE-104
  (1), GE-111 (5), GE-113 (3), GE-116 (3), GE-117 (5), GE-118 (2), GE-119 (4, excluded),
  GE-122 (5, this tree — GE-120 at authoring time; renumbered after an identifier
  collision with origin/main). Leading candidate is GE-111 `Refactoring can never silently
  break the link between your requirements and your code` — the contract-shrinking
  guard exists to stop a refactor from silently removing test coverage, and the record
  documents that guard misfiring — but the choice is the implementer''s to justify
  against the criterion, not a directive.'
- 'The AC-store gates will police the move and must be satisfied, not bypassed: `check-ac-schema`,
  `check-ac-parent-covered-by` (the new parent must list the moved record in `covered_by`),
  `check-ac-tree-limits` (the chosen parent must have room under the child cap), `check-ac-circular-deps`
  and `check-done-proof` all run on `docs/acceptance-criteria/**/*.yaml` at commit
  time and again in the `AC store valid` CI job. Choosing a parent that is already
  at its child cap turns a one-file move into a tree restructure.'
- 'Rewrite citations with the three-way treatment `scripts/adr_refs.py` already implements
  rather than a blanket search-and-replace: qualified citations are rewritten mechanically,
  bare ambiguous ones are examined individually because a bare ''GE-119'' today returns
  citations meaning BOTH records, and dated historical records are deliberately left
  alone because repointing them would make the record claim something that did not
  happen. A blanket replace across the repository would rewrite the release note that
  legitimately means the goal record.'
- Changelog entries and release notes are dated historical records. The one meaning
  the moved record gets a clarifying note stating the new identifier rather than a
  rewritten citation; the one meaning the goal record is untouched.
- 'Idempotency: re-running any scripted portion of this repair over the already-repaired
  tree must make no further change and must not re-rewrite an already-rewritten citation.
  Verify by running twice and diffing.'
- Do not delete either claimant. Both describe real, wanted work — the goal record
  is an active five-child tree, the parentless record documents a live defect covered
  by fifteen passing tests.
- This AC's ONLY prerequisite is GE-122e (GE-120e at authoring time); it does not
  depend on the pass being built. Doing it first makes GE-122a-1's own store clean,
  and it supplies GE-122c-3 with a real repair to write its procedure from.
```

## Agent Contracts

### documentation-expert

Existing docs to update / cross-link:

- docs/acceptance-criteria/guardrail-engine/GE-119.yaml (relationship: modifies | status: exists | relevance: The parentless detail-level claimant — the artifact that moves. Single file, no children, authored by /quick-fix from a different working copy.)
- docs/acceptance-criteria/guardrail-engine/GE-119-green-means-checked/GE-119.yaml (relationship: context | status: exists | relevance: The goal-level claimant — the artifact that keeps the identifier. Its 33-file tree must not be touched.)
- scripts/adr_refs.py (relationship: context | status: exists | relevance: A worked precedent for the reference half of a renumbering: it rewrites slug-qualified citations mechanically, refuses to rewrite bare ambiguous ones, and deliberately excludes dated historical records. The same three-way treatment applies here.)
- docs/conventions/adr-numbering.md (relationship: context | status: exists | relevance: The only written account in this repository of what a renumbering actually costs — the 2026-08-13 decision-number repair: six renames, roughly 430 citation rewrites across about 200 files, one gap filled, two records written retroactively. Read it before estimating this one.)
- docs/architecture/adrs/ADR-029-adr-number-collision-prevention.md (relationship: context | status: exists | relevance: Carries the operative free-number rule this repair must obey: a number is free only when it has NEITHER a file NOR a citation. GE-121 must be checked against both.)
- docs/reference/ac-schema.md (relationship: context | status: exists | relevance: The identifier format, the allocation algorithm and the never-reuse-a-retired-slot rule for this namespace, plus the parent back-link requirement the moved record must satisfy.)

- [ ] AC-1: [(unspecified genre)] docs/acceptance-criteria/guardrail-engine/GE-119.yaml — exactly one record in the collection claims "GE-119", and it is the goal record

## Sign-offs

- [x] test-writer — 2026-08-17 19:52
- [ ] python-coder
- [ ] test-runner
- [ ] documentation-expert
- [ ] ac-validator
- [ ] ac-fulfillment-gate
- [ ] documentation-verifier
- [ ] commit
- [ ] pull-request

## Comments

### 2026-08-17 19:52 — test-writer (status: ok)
feedback-id: fb_2026-08-17_97f1b212
completion_manifest:
  six_tests_written: true
  red_baseline_captured: true
  covers_tags_applied: true
  ac_coverage_mapped_in_comment: true

## Test Writer — Completion Report

### Tests Written
| File | Directory | Framework | Status |
|---|---|---|---|
| test_ge_120e_1.py | unit_tests/commit_guardian/ | unittest | written (6 tests, all tagged `# covers: GE-120e-1`) |

All six tests come straight from the ticket's `## Test Requirements` (and the AC's
`test_spec`), asserted against the REAL `docs/acceptance-criteria/` store, the REAL
`unit_tests/` tree, the REAL guard source, and the REAL changelogs — no synthetic
fixtures, per design constraint 1 and the repo's "Real-artifact behavioral spot-check"
convention. The repo root is resolved by walking up from `__file__` (never cwd),
matching `unit_tests/commit_guardian/conftest.py`'s own convention.

### Verification Run
- Command: `AC_ENFORCE_STRICT=1 python3 -m pytest unit_tests/commit_guardian/test_ge_120e_1.py -v`
  (also verified via `python3 -m unittest unit_tests.commit_guardian.test_ge_120e_1 -v`,
  0.267s total, to get the true per-test cost without the AC-enforcement plugin's ~9s
  AC-store-load overhead — see repo memory `project_pytest_ac_enforcement_xfail_mask`:
  plain `pytest` masks not-done-AC failures as `xfail` unless `AC_ENFORCE_STRICT=1` is set).
- Result: **RED — 5 failed, 1 passed** (6 collected). This is the expected and correct
  pre-repair state; see per-test notes below.
- Summary line: `5 failed, 1 passed in 9.57s` (pytest, AC_ENFORCE_STRICT=1) /
  `Ran 6 tests in 0.267s — FAILED (failures=5)` (unittest).
- `ruff check --select E722,BLE001,TRY` and `ruff check` (full default set): all checks
  passed. `python3 -m py_compile`: clean.

### Per-test status
| Test | Status | Reason |
|---|---|---|
| `test_old_identifier_has_exactly_one_claimant_after_repair` | RED | `AssertionError: 2 != 1` — GE-119 is currently claimed by both `GE-119.yaml` and `GE-119-green-means-checked/GE-119.yaml`. |
| `test_moved_record_claims_a_free_identifier_with_a_real_parent` | RED | `AssertionError: 'GE-119' == 'GE-119'` — the moved record has not been given a new identifier yet. |
| `test_goal_records_thirty_three_files_are_unchanged` | **PASSING PRE-REPAIR (known non-constraining)** | Nothing has touched the goal folder yet, so "unchanged" trivially holds. Flagged explicitly per the dispatch instructions — this is not a defect in the test, it becomes a real constraint the moment the coder phase edits the tree, since the assertion is byte-for-byte against a pinned pre-repair git baseline (see below), not against a moving `HEAD`. |
| `test_every_coverage_tag_resolves_to_exactly_one_record` | RED | `AssertionError: 'GE-119' == 'GE-119'` — the moved record has not been given a new identifier yet (all 15 tags still say `GE-119`). |
| `test_no_citation_of_either_identifier_resolves_to_zero_or_many` | RED | Same reason — moved record's identifier has not changed yet. |
| `test_dated_historical_records_are_not_repointed` | RED | Same reason — moved record's identifier has not changed yet. |

### Notes

**A real, independently-verified factual correction (beyond the ticket's own).**
`find docs/acceptance-criteria/guardrail-engine/GE-119-green-means-checked -type f | wc -l`
measures **32 files**, not the 33 the AC/ticket text states. This is the same class of
error the ticket's own Implementation Notes already caught once (the "nine tags in one
file" claim that was actually fifteen across two files) — recorded by assumption, not
verified against the real artifact. Per design constraint 1 (assert on the real store,
not a hand-typed number), `test_goal_records_thirty_three_files_are_unchanged` asserts
the **verified** count (32, via the `GOAL_FOLDER_EXPECTED_FILE_COUNT` constant with an
explanatory comment) rather than the stated 33 — a test hard-coded to a wrong "33" could
never go green under a correct repair, since a correct repair also leaves exactly 32
real files in place. The test function's NAME keeps the ticket's own
"thirty_three" wording (per the Test Requirements naming contract); only the assertion
value differs from the ticket's prose. Recommend the AC's "33 files" language be
corrected in a follow-up (not done here — out of scope for test-writer, and the ticket
explicitly says not to edit the AC YAML from this phase).

**33-files-unchanged baseline.** Compared against the fixed pre-repair commit
`4a83c82803786514d580826c2752bfcd08585e0c` (this branch's HEAD at authoring time,
confirmed via `git diff HEAD -- <goal-folder>` = empty) via `git show <sha>:<path>`,
not against `origin/main` — `origin/main` does not contain the
`GE-119-green-means-checked/` tree at all on this AC-authoring branch (it was authored
directly on this branch and has not merged), so `git show origin/main:<path>` would
fail with "path does not exist" for every one of the files, not produce a meaningful
comparison. Documented inline in the test module's `BASELINE_COMMIT` comment.

**Moved-record lookup and no-hardcoded-new-id.** `_find_moved_record()` locates the
parentless record by an exact grep on its distinctive TITLE across
`docs/acceptance-criteria/`, excluding the goal folder — never by filename or by
assuming the new id is `GE-121`. This is deliberate per design constraint 3: the
ticket's own Context section documents that both the BA and the IT PO independently
missed six of the fifteen coverage tags precisely because they searched by filename: a
test hard-coded to `GE-121` (or to the two known filenames for the tag-scan) would have
passed the same defective repair those two agents nearly shipped.

**AC-1..AC-10 mapping (global AC list — no `### test-writer` subsection exists under
`## Agent Contracts`, so per the AC Mapping Rule the six ticket-specified tests below
are mapped against the ticket's top-level Gherkin `AC-1`..`AC-10` checkboxes instead of
being individually re-named `test_ac1_...`; the ticket's own `## Test Requirements`
names take precedence per direct dispatch instruction):**
- AC-1 (fifteen tags), AC-7 (tags repointed) → `test_every_coverage_tag_resolves_to_exactly_one_record`
- AC-2 (guard source + release notes cite GE-119), AC-8 (guard citation), AC-9 (dated records) → `test_dated_historical_records_are_not_repointed` + `test_no_citation_of_either_identifier_resolves_to_zero_or_many` (guard source is a `.py` file under the repo-wide scan)
- AC-3 (exactly one claimant, goal record) → `test_old_identifier_has_exactly_one_claimant_after_repair`
- AC-4 (33/32 files unchanged) → `test_goal_records_thirty_three_files_are_unchanged`
- AC-5 (free identifier), AC-6 (real parent) → `test_moved_record_claims_a_free_identifier_with_a_real_parent`
- AC-10 (no citation resolves to zero/many) → `test_no_citation_of_either_identifier_resolves_to_zero_or_many`

No `## AC Coverage` table exists in this ticket body (only `## Agent Contracts` →
`### documentation-expert`), so §2c.3 of the signoff skill does not apply here; this
prose mapping is offered in its place for traceability.

Handing off to `python-coder`: make the five RED tests green (and keep the sixth green)
by performing the repair described in the ticket's Context/Implementation Notes.

### python-coder + test-runner (status: ok) — 2026-08-17

Repair complete. `docs/acceptance-criteria/guardrail-engine/GE-119.yaml` was `git mv`'d to
`GE-111-traceability-stays-honest/GE-111f.yaml` with `id: GE-111f`, `level: L1` (matching its
sibling shape), `depends_on: [GE-111]`, and an `amended_by` entry; `criteria` and `notes` are
byte-identical. `GE-111.yaml` gained `GE-111f` in `covered_by` (6 children, cap 7). All 15
`# covers:` tags repointed across both modules — verified 0 stale remaining. The guard comment at
`check_contract_shrinking.py:54` names the new id. The 2026-08-14 changelog keeps its original
`GE-119` citation and gains a clarifying paragraph; the 2026-08-17 changelog, the `ACS-1200`
`GE-119b` references, the bare `GE-119` at `ACS-1200a-2.yaml`, and all 32 files under
`GE-119-green-means-checked/` are untouched (empty `git status` on that folder).

Evidence: `test_ge_120e_1.py` 6 passed. Full `unit_tests/commit_guardian/` suite 1142 passed,
4 skipped, 1 xfailed, 0 failed. `validate_ac_schema.py` OK on both changed AC files (explicit
paths). Independent store-wide scan: 2,997 AC files, 2,997 distinct ids, **0 duplicates** — the
store is fully unambiguous for the first time. `GE-119` now resolves to exactly one record.

ONE TEST WAS CORRECTED, and the reason matters. `test_moved_record_claims_a_free_identifier_with_a_real_parent`
asserted `^GE-\d{3,}$`, requiring a ROOT-format id. That regex was written while the AC still
recommended `GE-121`; the target was corrected to `GE-111f` afterwards, so the assertion carried a
superseded assumption. `python-coder` correctly refused to edit it and escalated rather than
bending the implementation to a stale test. The regex was widened to accept suffix-shaped ids.
This does not weaken the test: the parent's existence and its `covered_by` back-link are asserted
separately and are what actually carry the criterion. The root-format requirement was in fact
harmful — `check_ac_parent_covered_by.py` and `scan_ac_orphans.py` both derive a parent from id
SHAPE via `derive_parent_id()`, which returns `None` for a root id, so `GE-121` would have been
classified "root-level, no parent" and skipped by both gates: a parent link no gate can police,
which is the exact paper-only-parent defect GE-120 exists to end.

PHASES NOT RUN — recorded rather than silently skipped. `documentation-expert`,
`documentation-verifier`, `ac-validator`, `ac-fulfillment-gate`, `commit` and `pull-request`
remain `needed`. This ticket was driven directly under human supervision rather than through
`ticket-supervisor`, because GitHub's API was returning 503 and the `pull-request` phase would
have failed. Their frontmatter status is left as `needed` so nothing claims a sign-off that did
not happen.
