---
ac_traceability:
  id: TKT-500f-12
  path: docs/acceptance-criteria/ticket-creation/TKT-500-single-source/TKT-500f-12.yaml
advances_current_outcome: true
agents:
  commit: needed
  pr-reviewer: signed_off
  pull-request: needed
  python-coder: signed_off
  test-runner: signed_off
  test-writer: signed_off
change_target: pipeline
complexity: medium
components:
- ticket_creation_pipeline
created: '2026-07-17'
depends_on:
- TKT-500f
- TKT-500f-9
- TKT-500f-11
files_touched:
- docs/reference/ac-schema.md
- scripts/ac_store/generate_ticket_from_ac.py
priority: high
requires_adr: false
requires_diagram: false
risk_surface: internal
roadmap_phase: phase_1
source_ac: TKT-500f-12
status: in_progress
title: Code-AC ticket's agents map wires ac-validator and ac-fulfillment-gate
---

# Code-AC ticket's agents map wires ac-validator and ac-fulfillment-gate

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `TKT-500f-12` — Code-AC ticket's agents map wires ac-validator and ac-fulfillment-gate — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `TKT-500f-12`. Component: `ticket-creation`. Assigned agent: `python-coder`. Estimated complexity: `M`. Complexity: `medium`.

## Acceptance Criteria

```gherkin
Given a leaf AC at level L2 or L3 whose generated ticket has a files_touched
  list containing at least one implementation .py file (an AC whose work
  changes code, not docs/config only),
When a ticket is generated from that AC by generate_ticket_from_ac.py,
Then the generated ticket's agents map wires the ac-validator phase and the
  ac-fulfillment-gate phase as needed phases,
And those two phases appear in the agents map so the AC-traceability gates run
  against the ticket rather than being absent.

Given a leaf AC whose generated ticket has a files_touched list containing no
  implementation .py file (docs/config-only work),
When a ticket is generated from that AC,
Then ac-validator and ac-fulfillment-gate are wired only as the pipeline's
  existing rules require for such tickets (this AC does not force them onto
  docs/config-only tickets).
```

- [ ] AC-1: the generated ticket's agents map wires the ac-validator phase and the
- [ ] AC-2: those two phases appear in the agents map so the AC-traceability gates run
- [ ] AC-3: ac-validator and ac-fulfillment-gate are wired only as the pipeline's

## Test Requirements

```yaml
tests:
- name: test_code_ticket_wires_ac_validator_and_fulfillment_gate
  file: unit_tests/ac_store/test_tkt_500f_12.py
  covers:
  - TKT-500f-12
  asserts: Generate a ticket from an AC whose files_touched contains an implementation
    .py; assert the agents map has ac-validator and ac-fulfillment-gate wired as needed
    phases.
  framework: unittest
  type: unit
- name: test_docs_only_ticket_not_forced
  file: unit_tests/ac_store/test_tkt_500f_12.py
  covers:
  - TKT-500f-12
  asserts: Generate a ticket from a docs/config-only AC (no .py in files_touched);
    assert ac-validator and ac-fulfillment-gate are not force-added beyond the pipeline's
    existing rules.
  framework: unittest
  type: unit
```

## Implementation Notes

```yaml
config_schema_fragment: null
n_location_rule: 1 (the agents-map builder; e.g. _build_agents_map / the computed-quality-gates
  map, BO-500 family, per ADR-017)
notes:
- When the generated ticket's files_touched contains at least one implementation .py
  file, the agents map must wire the ac-validator phase and the ac-fulfillment-gate
  phase as needed phases.
- These two phases must be wired SPECIFICALLY (named as ac-validator and ac-fulfillment-gate
  for code-AC tickets); do not merely re-derive the generic computed-quality-gates
  guardrail map and assume they are included.
- Docs/config-only tickets (no implementation .py in files_touched) must not be forced
  to carry these phases beyond the pipeline's existing rules.
- The .py detection must key off files_touched, which must reflect the real edit surface
  produced by TKT-500f-8; a stale or doc_links-only files_touched would mis-classify
  a code AC as docs-only.
post_write_commands:
- python scripts/build.py --target-dir .
reference_file_path: scripts/ac_store/generate_ticket_from_ac.py
required_skills:
- python-coder
```

## Sign-offs

- [x] test-writer — 2026-07-17 09:31
- [x] python-coder — 2026-07-17 10:15
- [x] test-runner — 2026-07-17 09:40
- [x] pr-reviewer — 2026-07-17 09:52
- [ ] commit
- [ ] pull-request

## Comments

### 2026-07-17 10:15 — python-coder (status: ok)
feedback-id: fb_2026-07-17_11e4c94d
completion_manifest:
  code_implemented: true
  tests_passing: true
  files_touched_match_plan: true
red_baseline_results:
  - test_name: test_code_ticket_wires_ac_validator_and_fulfillment_gate
    result: green
  - test_name: test_docs_only_ticket_not_forced
    result: green
Added .py-detection logic to `_build_agents_map`: when `files_touched` contains at least one `.py` file, `ac-validator` and `ac-fulfillment-gate` are wired as `needed` phases. Also added both agents to `_CANONICAL_PHASE_ORDER` and `_FLOW_CHANGE_PHASE_ORDER`, added `files_touched` parameter to `_build_agents_map`, and updated all three call sites (dry-run path, write path, and `_build_ticket_body` internal path). Both red-baseline tests now green; docs-only regression guard also green.

### 2026-07-17 09:31 — test-writer (status: ok)
feedback-id: fb_2026-07-17_d0f1b7e5
completion_manifest:
  test_file_written: true
  tests_are_red: true
  syntax_and_imports_valid: true
Wrote 2 test stubs to unit_tests/ac_store/test_tkt_500f_12.py. Verification run (AC_ENFORCE_STRICT=1) confirms non-zero exit (1 failed, 1 passed). test_code_ticket_wires_ac_validator_and_fulfillment_gate is RED with AssertionError: None != 'needed' — the production code has no .py detection logic yet. test_docs_only_ticket_not_forced passes immediately (regression guard; noted in red_baseline).

## Test Writer — Completion Report

### Tests Written
| File | Directory | Framework | Status |
|---|---|---|---|
| test_tkt_500f_12.py | unit_tests/ac_store/ | unittest | written |

### Verification Run
- Command: `AC_ENFORCE_STRICT=1 python3 -m pytest /home/henzeh/projects/leafcutter/worktrees/tkt-500f-12/unit_tests/ac_store/test_tkt_500f_12.py -v`
- Result: red (1 failed, 1 passed — expected; implementation not yet written)

### Notes
test_docs_only_ticket_not_forced is a regression guard: it passes before implementation (ac-validator and ac-fulfillment-gate are absent from the docs-only map both before and after the fix). It is included to catch any incorrect over-broad implementation that forces these agents onto all tickets regardless of files_touched content. Flagged in red_baseline with note.

red_baseline:
  - test_name: test_code_ticket_wires_ac_validator_and_fulfillment_gate
    file: unit_tests/ac_store/test_tkt_500f_12.py
    error: "AssertionError: None != 'needed' : ac-validator must be wired as 'needed' in the agents map when files_touched contains a .py file. Current agents map: {'commit': 'needed', 'pull-request': 'needed', 'python-coder': 'needed', 'test-runner': 'needed', 'test-writer': 'needed'}. The current _build_agents_map has no logic to detect .py files in files_touched; python-coder must add this wiring logic."
  - test_name: test_docs_only_ticket_not_forced
    file: unit_tests/ac_store/test_tkt_500f_12.py
    error: "(no error — test passes immediately)"
    note: "passes immediately — regression guard for docs-only path; will fail if implementation incorrectly adds ac-validator/ac-fulfillment-gate to all tickets regardless of files_touched"

### 2026-07-17 09:40 — test-runner (status: ok)
feedback-id: fb_2026-07-17_b66a1f9c
completion_manifest:
  test_suite_executed: true
  all_tests_passing: true
  failure_report_structured: true
Both TKT-500f-12 tests pass under AC_ENFORCE_STRICT=1 (2 passed in 0.14s). test_code_ticket_wires_ac_validator_and_fulfillment_gate verifies ac-validator and ac-fulfillment-gate are wired as needed for code ACs with .py in files_touched; test_docs_only_ticket_not_forced confirms docs-only ACs are not forced to carry these agents.

### 2026-07-17 09:52 — pr-reviewer (status: ok)
feedback-id: fb_2026-07-17_e7fec654
completion_manifest:
  diff_reviewed: true
  ruff_clean: true
  tests_verified_green: true
  call_sites_audited: true
  no_high_confidence_findings: true
Reviewed the `.py`-detection logic in `_build_agents_map` (line 534): the guard `if files_touched and any(p.endswith(".py") for p in files_touched)` is correctly scoped inside the computed path (`change_targets is not None and risk_surface is not None`), so the legacy path is unaffected. Both `ac-validator` and `ac-fulfillment-gate` appear at the correct positions in `_CANONICAL_PHASE_ORDER` and `_FLOW_CHANGE_PHASE_ORDER` (after `pr-reviewer`, before `commit`). All three `_build_agents_map` call sites (dry-run path, write path, `_build_ticket_body` fallback) correctly pass `files_touched`. Ruff reports no violations. Both unit tests pass green under `AC_ENFORCE_STRICT=1`. No high-confidence findings.
