---
ac_traceability:
  id: UXP-700e-3-i
  path: docs/acceptance-criteria/ux-prototyping/UXP-700-truthful-project-record/UXP-700e-3-i.yaml
advances_current_outcome: true
agents:
  ac-fulfillment-gate: needed
  ac-validator: needed
  architect-review: needed
  commit: needed
  documentation-expert: needed
  documentation-verifier: needed
  frontend-coder: not_needed
  pr-reviewer: needed
  pull-request: not_needed
  python-coder: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
  user-surface-smoker: not_needed
change_target:
- code
- schema
complexity: medium
components:
- ux_prototyping
created: '2026-09-09'
depends_on: []
documentation_required: true
files_touched:
- docs/product-truth/schemas/flow.schema.json
priority: medium
requires_adr: false
requires_diagram: false
risk_surface: contract_boundary
roadmap_phase: phase_1
source_ac: UXP-700e-3-i
status: todo
title: A field being reshaped is read in both shapes and written in only the new one
---

# A field being reshaped is read in both shapes and written in only the new one

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `UXP-700e-3-i` — A field being reshaped is read in both shapes and written in only the new one — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `UXP-700e-3-i`. Component: `ux-prototyping`. Assigned agent: `python-coder`. Estimated complexity: `M`. Complexity: `medium`.

## Acceptance Criteria

```gherkin
Given three journeys in which the expansion reference is written as a single value where a
  list is wanted, and no journey anywhere carries a branch outcome kind,
When the record is read,
Then a single value is accepted and read as a list holding one item,
And a list is accepted as written,
And an absent branch outcome kind is accepted while that field is being introduced, and is
  reported as a field to be filled rather than as a violation,
And anything the record writes back uses only the new shape, so the older shape disappears as
  artifacts are rewritten rather than by one migration rewriting them all at once.
```

- [ ] AC-1: a single value is accepted and read as a list holding one item
- [ ] AC-2: a list is accepted as written
- [ ] AC-3: an absent branch outcome kind is accepted while that field is being introduced, and is
- [ ] AC-4: anything the record writes back uses only the new shape, so the older shape disappears as

## Test Requirements

```yaml
tests:
- name: test_single_value_expansion_reference_is_read_as_a_list_of_one
  file: unit_tests/product_truth/test_uxp_700e_3_i.py
  covers:
  - UXP-700e-3-i
  asserts: A journey whose expansion reference is a bare string is read as a one-item
    list.
  framework: unittest
  type: unit
  angle: boundary
- name: test_list_expansion_reference_is_read_unchanged
  file: unit_tests/product_truth/test_uxp_700e_3_i.py
  covers:
  - UXP-700e-3-i
  asserts: A journey whose expansion reference is already a list is read as written.
  framework: unittest
  type: unit
  angle: criterion
- name: test_absent_branch_outcome_kind_is_reported_as_to_be_filled_not_violated
  file: unit_tests/product_truth/test_uxp_700e_3_i.py
  covers:
  - UXP-700e-3-i
  asserts: A branch with no outcome kind produces a to-be-filled finding, not a violation.
  framework: unittest
  type: unit
  angle: boundary
- name: test_rewritten_artifact_is_serialised_only_in_the_new_shape
  file: unit_tests/product_truth/test_uxp_700e_3_i.py
  covers:
  - UXP-700e-3-i
  asserts: Reading a single-value artifact and writing it back yields a list on disk.
  framework: unittest
  type: integration
  angle: real_artifact
- name: test_uxp_700e_3_i_reachable_from_entry_point
  file: unit_tests/product_truth/test_uxp_700e_3_i.py
  covers:
  - UXP-700e-3-i
  angle: reachability
  asserts: 'REQUIRED — invoke the production entry point (CLI, hook, slash command,
    workflow dispatch, or main()) as a subprocess/dispatch and assert the new behaviour
    actually occurs. Do NOT satisfy this by importing the function directly. The AC
    authored no test_spec, so the entry point is not declared: resolve it before writing
    this test, and do not delete this entry.'
```

## Agent Contracts

### documentation-expert

Existing docs to update / cross-link:

- docs/product-truth/schemas/flow.schema.json (relationship: specifies | status: exists)

- [ ] AC-1: (unspecified genre) | docs/product-truth/schemas/flow.schema.json | a single value is accepted and read as a list holding one item

### Expects From

- **AC:** UXP-700e-1-i
- **Contract:** The shape-version transition window this record's read-both behaviour lives inside.

## Sign-offs

- [ ] architect-review
- [ ] test-writer
- [ ] python-coder
- [ ] test-runner
- [ ] documentation-expert
- [ ] pr-reviewer
- [ ] ac-validator
- [ ] ac-fulfillment-gate
- [ ] documentation-verifier
- [ ] commit

## Comments
