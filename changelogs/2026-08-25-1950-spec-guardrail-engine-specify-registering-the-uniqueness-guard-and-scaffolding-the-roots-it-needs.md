---
title: "spec(guardrail-engine): specify registering the uniqueness guard, and scaffolding the roots it needs"
date: "2026-08-25"
time: "19:50"
type: manual
components: 
  - guardrail_engine
  - build_pipeline
  - commit_guardian
  - ac_store
summary: "A safety check built to stop two things being given the same ID number turned out never to have been switched on. This writes down exactly what switching it on requires, and in what order, so it cannot be done in a way that breaks new installs."
description: "Spec only -- no production code. The GE-122 whole-collection uniqueness pass was built, tested to 3772 green tests and adversarially reviewed six times, yet appears in neither commit_guardian.json nor the deployed .pre-commit-config.yaml and is invoked by no CI workflow: it has never run. The epic's own Master_Plan predicted this ('a guard that is built, tested green, and registered nowhere') and its Success Criteria require the pass be registered and reachable through its production entry point; that criterion is not met. Five ACs authored under already-approved parents, zero new L0/L1: GE-122d-6 (the check is really registered and fires on a real commit, verified by committing rather than by asserting a config key exists), GE-122d-6-i (the deregistered run -- the same clean commit with the check unwired, so 'the guard ran and found nothing' and 'the guard was never reached' become two observations rather than one silence), GE-122d-3-ii (an absent namespace root must not block a new adopter's first commit, and every root resolves through DECLARED path configuration so a wrong path is still an unresolved declared root and still blocks), GE-120c-6 (a deployed-but-unregistered guard is reported by name and fails the sweep), BP-900h-6 (the consumer simulation uses the install rather than only building it). Ordering is encoded in depends_on and is not advisory: build.py creates neither docs/architecture/adrs/ nor docs/architecture/diagrams/, so under the fail-closed contract registering the hook first blocks every commit in every fresh install -- measured on both documented install paths including --seed-docs. GE-122d-6 depends on BP-900h-6 rather than the reverse, because 're-run the consumer job' is an act and a job that does not exist cannot be re-run. Three corrections made during authoring: the AC was first written as BP-900h-4, which collides with a live approved unrelated record on main (the third occurrence of that exact collision -- main's own BP-1500a records an earlier one) and was renumbered to BP-900h-6 with all 11 references updated; the L2 child cap is 5 rather than the 7 both the coverage sweep and the PO pass reported, so all three target parents were already full and carry child_limit_override: 6 on explicit user decision, recorded as debt to retire alongside BP-900's existing override; and a planned one-line cross-reference to BP-900e-3 was dropped because staging it made check-ac-schema validate the whole file, which failed on a pre-existing defect (approved with no test contract) that is invisible until someone touches the file. Also adds docs/reference/architecture-docs-layout.md, recording that 11 of the 24 files in docs/architecture/diagrams/ are a binding permanent exemption from the numbering rule and that config/paths.json has no architecture_diagrams key, so that one root is hardcoded in the check. Running the fixed pass over this branch: acceptance-criteria 3385 inspected, 0 findings; work-items 5 findings, all pre-existing duplicate tickets on main that GE-122e-2 repairs on the unmerged PR #495 -- so main would currently fail its own gate."
breaking: false
---

## Entry
