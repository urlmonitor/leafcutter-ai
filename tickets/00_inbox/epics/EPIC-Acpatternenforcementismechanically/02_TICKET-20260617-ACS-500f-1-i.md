---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  pr-reviewer: needed
  pull-request: needed
  python-coder: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
components:
- ac-store
created: '2026-06-17'
depends_on:
- ACS-500f-1
files_touched:
- docs/reference/claude-code-hooks.md
- scripts/commit_guardian/commit_guardian.json
priority: high
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: ACS-500f-1-i
status: todo
title: Schema hook fails open and never blocks an unrelated commit on its own error
---

# Schema hook fails open and never blocks an unrelated commit on its own error

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `ACS-500f-1-i` — Schema hook fails open and never blocks an unrelated commit on its own error — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `ACS-500f-1-i`. Component: `ac-store`. Assigned agent: `python-coder`. Estimated complexity: `S`.

## Acceptance Criteria

```gherkin
Given the now-registered AC store schema hook encounters an unexpected internal
  error during execution (e.g. an AC YAML file has unparseable YAML, or the
  git diff command the hook runs fails),
When the hook handles that error,
Then it exits with code 0 (fail-open per project convention),
And the commit is NOT blocked by this hook,
And a diagnostic message identifying the hook and the error is written to stderr.

Given no staged file under docs/acceptance-criteria/ contains an
  implements_pattern field and no staged AC file violates the store schema,
When the commit is attempted,
Then the schema hook exits with code 0,
And the commit proceeds without blocking.
```

## Sign-offs

- [ ] python-coder
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit
- [ ] pull-request

## Comments
