---
ac_traceability:
  id: GE-120c-4
  path: docs/acceptance-criteria/guardrail-engine/GE-120-green-means-checked/GE-120c-4.yaml
advances_current_outcome: true
agents:
  ac-fulfillment-gate: needed
  ac-validator: needed
  commit: needed
  pull-request: not_needed
  test-runner: needed
  test-writer: needed
change_target: code
complexity: medium
components:
- testing_quality
- ac_store
created: '2026-08-25'
depends_on:
- 12_TICKET-20260825-GE-120c-1.md
files_touched:
- config/ac_store_schema.json
- docs/acceptance-criteria
- templates/scripts/commit_guardian/check_ac_schema.py
priority: high
requires_adr: false
requires_diagram: false
risk_surface: internal
roadmap_phase: phase_1
source_ac: GE-120c-4
status: todo
title: The unverified count of files that only looked clean is replaced with a measured
  one
---

# The unverified count of files that only looked clean is replaced with a measured one

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `GE-120c-4` — The unverified count of files that only looked clean is replaced with a measured one — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `GE-120c-4`. Component: `guardrail-engine`. Assigned agent: `test-writer`. Estimated complexity: `M`. Complexity: `medium`.

## Acceptance Criteria

```gherkin
Given the report that 26 schema-violating AC files looked clean under the reduced manual
  field validation — a figure recorded as reported by a parallel session and never
  independently reproduced,
When the harness validates the AC store twice from the same working copy, once with the
  authoritative schema and once with the reduced manual field validation,
Then it reports the actual number of files accepted by the reduced validation and rejected
  by the authoritative one, and lists those files by path,
And it reports which authoritative rule each listed file violates, so the finding is
  actionable rather than a count,
And the measured number is what is cited from that point on, whether or not it equals 26,
And a measured result of zero is reported as the finding rather than treated as a broken
  measurement,
And the measurement is repeatable on demand against the store as it stands, not captured
  once as a static number in a document.

Coverage note: the two validations must both be executed against the real on-disk AC store,
not a synthetic fixture. A fixture built by an author who already knows which rules the
reduced validation omits reproduces that author's assumptions and cannot surprise them
(CLAUDE.md, "Real-artifact behavioral spot-check before declaring done").
```

- [ ] AC-1: it reports the actual number of files accepted by the reduced validation and rejected
- [ ] AC-2: it reports which authoritative rule each listed file violates, so the finding is
- [ ] AC-3: the measured number is what is cited from that point on, whether or not it equals 26
- [ ] AC-4: a measured result of zero is reported as the finding rather than treated as a broken
- [ ] AC-5: the measurement is repeatable on demand against the store as it stands, not captured

## Implementation Notes

```yaml
- RUN BOTH VALIDATIONS AGAINST THE REAL ON-DISK AC STORE (docs/acceptance-criteria/),
  never a synthetic fixture. A fixture authored by someone who already knows which
  rules the manual fallback omits reproduces their assumptions and cannot surprise
  them — the exact bias that let the phantom-done parser pass on indented fixtures
  while being a no-op on every real ticket.
- 'The two modes are: authoritative jsonschema validation against config/ac_store_schema.json,
  and the reduced manual field validation at templates/scripts/commit_guardian/check_ac_schema.py
  ~line 623, selected via the same GE-112 schema_validated path (DECISION HISTORY
  ~lines 29-37). Run both FROM ONE WORKING COPY so the only variable is the validation
  mode — varying the copy as well would confound this measurement with GE-120b-3''s.'
- 'OUTPUT IS THREE THINGS, not one: the count, the file paths, and per file WHICH
  authoritative rule it violates. A bare number is not the deliverable and is not
  actionable.'
- A MEASURED ZERO IS THE FINDING, reported as such — not a broken measurement, not
  a reason to re-run against different inputs until a more alarming number appears.
  State that a zero means the reduced validation happens to cover the rules the current
  store exercises, which weakens the urgency argument for GE-120a-3 while leaving
  its correctness untouched, because the divergence is a property of the store on
  the day it was measured, not of the check.
- REPEATABLE ON DEMAND against the store as it stands. Do not write the number into
  a document as a static value — a number in a doc is stale the next day; a measurement
  that can be re-run answers the question again after the store grows. This is a harness
  capability, not an investigation.
- 'EVIDENCE DISCIPLINE, which is the actual subject of this AC: until this runs, the
  figure 26 is cited only with its label (REPORTED BY A PARALLEL SESSION, NOT VERIFIED).
  After it runs, the measured number is cited. Do not retro-edit the label out of
  the sibling ACs in this tree — supersede the figure with the measurement and say
  when it was measured.'
- 'SCALE AND SCOPE: the real store is large (the guardrail-engine GE-120 folder alone
  holds 32 files, and the store spans many components), so a naive double validation
  is slow. This is a marked-slow, on-demand measurement, never a per-commit gate.'
```

## Agent Contracts

### Expects From

- **AC:** GE-120c-1
- **Contract:** Execution of the AC-schema check from a controlled working copy under each validation mode.

## Sign-offs

- [ ] test-writer
- [ ] test-runner
- [ ] ac-validator
- [ ] ac-fulfillment-gate
- [ ] commit

## Comments
