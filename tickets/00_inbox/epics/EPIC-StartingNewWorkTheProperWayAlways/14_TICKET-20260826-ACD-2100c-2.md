---
ac_coverage: 6/6
ac_traceability:
  id: ACD-2100c-2
  path: docs/acceptance-criteria/ac-driven-dev/ACD-2100-entry-point-unblocked/ACD-2100c-2.yaml
advances_current_outcome: true
agents:
  ac-fulfillment-gate: signed_off
  ac-validator: signed_off
  architect-review: signed_off
  commit: signed_off
  documentation-expert: not_needed
  documentation-verifier: not_needed
  pr-reviewer: signed_off
  pull-request: not_needed
  python-coder: signed_off
  test-runner: signed_off
  test-writer: signed_off
change_target: pipeline
complexity: medium
components:
- ac_driven_dev
created: '2026-08-26'
depends_on:
- tickets/00_inbox/epics/EPIC-StartingNewWorkTheProperWayAlways/05_TICKET-20260826-ACD-2100a-4.md
documentation_required: false
files_touched:
- docs/architecture/adrs/ADR-024-interactive-pause-resume.md
- templates/workflows-js/plan-feature.js
priority: medium
requires_adr: false
requires_diagram: false
risk_surface: safety
roadmap_phase: phase_1
source_ac: ACD-2100c-2
status: done
title: A run that cannot reach a person waits with a durable record instead of proceeding
  or discarding
---

# A run that cannot reach a person waits with a durable record instead of proceeding or discarding

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `ACD-2100c-2` — A run that cannot reach a person waits with a durable record instead of proceeding or discarding — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `ACD-2100c-2`. Component: `ac-driven-dev`. Assigned agent: `python-coder`. Estimated complexity: `M`. Complexity: `medium`.

## Acceptance Criteria

```gherkin
Given a run started with no channel by which a person can answer -- an unattended
  invocation with no interactive session attached,
And the run is started with its process working directory set to a git worktree
  of the project,
And drafted work from the steps before the decision point is present on disk,
When the run reaches a decision point,
Then no step after that decision point runs,
And a record naming the run and that decision point is present on disk after the
  process has exited,
And the run reports that it is waiting for an answer,
And the drafted work is still present on disk unchanged.
```

- [x] AC-1: the run is started with its process working directory set to a git worktree
- [x] AC-2: drafted work from the steps before the decision point is present on disk
- [x] AC-3: no step after that decision point runs
- [x] AC-4: a record naming the run and that decision point is present on disk after the
- [x] AC-5: the run reports that it is waiting for an answer
- [x] AC-6: the drafted work is still present on disk unchanged.

## Test Requirements

```yaml
tests:
- name: test_unattended_run_from_a_worktree_pauses_with_a_record_on_disk
  file: unit_tests/workflows/test_acd_2100c_2.py
  covers:
  - ACD-2100c-2
  asserts: An unattended invocation with no interactive session, driven from a real
    git worktree of the project with drafted work already on disk; after the process
    exits a record naming the run and the decision point is present in the project's
    store and the run reported that it is waiting.
  framework: unittest
  type: integration
  angle: criterion
- name: test_no_step_after_the_decision_point_runs_when_no_person_can_answer
  file: unit_tests/workflows/test_acd_2100c_2.py
  covers:
  - ACD-2100c-2
  asserts: The run's record of the steps it executed ends at the decision point --
    no later step appears, and no authoring output from a later step exists on disk.
  framework: unittest
  type: integration
  angle: reachability
- name: test_drafted_work_is_byte_identical_after_the_pause
  file: unit_tests/workflows/test_acd_2100c_2.py
  covers:
  - ACD-2100c-2
  asserts: The drafted artifacts on disk before the run are compared byte-for-byte
    with the same files after the process exits; nothing was rewritten, relocated
    or removed.
  framework: unittest
  type: integration
  angle: real_artifact
- name: test_pause_written_from_the_worktree_is_readable_from_the_project_root
  file: unit_tests/workflows/test_acd_2100c_2.py
  covers:
  - ACD-2100c-2
  asserts: A second fresh process started with the project root as its working directory
    lists the run as waiting -- the worktree-written record read by a reader that
    starts somewhere else.
  framework: unittest
  type: integration
  angle: seam
```

## Implementation Notes

```yaml
- The change must land under templates/. The deployed workflow is build output.
- The pause record must be durable before the process exits and must be written to
  the project's own store, not beside the caller. Both properties come from ACD-2100a-4;
  until it lands this behaviour degrades to a persist failure when run from a worktree,
  which is where the original loss was observed.
- '''Waiting'' must be established by the record on disk, not by the reported outcome.
  A run that reports waiting while having written nothing is not paused, it is lost,
  and it satisfies every assertion made against the return value.'
- No step after the decision point may run on any path out of an unreachable channel
  -- including a timeout path. A timeout that proceeds is the same loss with a delay.
- The drafted work must be left byte-identical. The pause must not normalise, rewrite
  or relocate anything the earlier steps produced.
- 'Must satisfy the repository error-handling policy: the record write is external
  I/O; a failed write must be logged at WARNING or higher and must not be reported
  as a successful pause.'
- 'Must be idempotent: reaching the same decision point twice for the same run leaves
  one record.'
```

## Agent Contracts

### documentation-expert

Existing docs to update / cross-link:

- docs/architecture/diagrams/c3-006-plan-feature-decision-gate-sequence.md (relationship: describes | status: planned | relevance: The sequence diagram authored by ACD-2100e-1 -- the unattended branch that ends in a pause is one of the paths it depicts.)
- docs/known-issues/ac-driven-dev.md (relationship: describes | status: exists | relevance: KI-ACD-005 -- 'A gate that cannot reach a user should pause and persist a resumable state.')
- docs/architecture/adrs/ADR-024-interactive-pause-resume.md (relationship: implements | status: exists | relevance: The pause and resume contract, including re-entry via a supplied answer.)
- templates/workflows-js/plan-feature.js (relationship: implements | status: exists | relevance: Canonical source. resolveGate at 1571-1582 already pauses on a null or unparseable answer; the residual destructive default is line 2086.)
- docs/architecture/components/interactive-pause-resume-substrate.md (relationship: describes | status: exists | relevance: Component doc for the pause substrate this behaviour writes into.)

This record owns none of the documents above -- every entry is a cross-link to a
document another record produces. The two contract lines that stood here both
demanded `docs/architecture/diagrams/c3-006-plan-feature-decision-gate-sequence.md`,
which ACD-2100e-1 creates; this ticket could never have satisfied them. Removed
per BO-2200c-5-i and -iii; the list above is retained as reading for the
implementer.

### Delivers To

- **Agent:** python-coder
- **Contract:** A durable pause record naming the run and the decision point it stopped at, present in the project's own store after the process exits and readable by a later process started from any working directory -- the state ACD-2100c-3 resumes from.
- **Agent:** python-coder
- **Contract:** An unreachable-channel outcome that is a pause: the drafted work remains on disk and the run's recorded outcome is 'waiting', never a stop attributed to a person.

### Expects From

- **AC:** ACD-2100a-4
- **Contract:** A pause store whose read, write and read-back all resolve to the project's own store rather than to a location relative to the caller's working directory, with the write durable before process exit and its reported success matching what is on disk.

## Sign-offs

- [x] architect-review — 2026-09-08 10:47
- [x] test-writer — 2026-09-08 11:20
- [x] python-coder — 2026-09-08 11:12
- [x] test-runner — 2026-09-08 12:05
- [x] pr-reviewer — 2026-09-08 11:24
- [x] ac-validator — 2026-09-08 12:30
- [x] ac-fulfillment-gate — 2026-09-08 11:38
- [x] commit — 2026-09-08 11:46

## Comments

### 2026-09-08 10:47 — architect-review (status: ok)
feedback-id: fb_2026-09-08_b9785a1f
completion_manifest:
  blast_radius_reviewed: true
  rubric_classification_recorded: true
  stale_implementation_note_flagged: true
Classified SMALL: files_touched is `docs/architecture/adrs/ADR-024-interactive-pause-resume.md`
(cross-link/reference only, per the documentation-expert contract block — not an edit
target; `status: exists`, `documentation-expert: not_needed`) and
`templates/workflows-js/plan-feature.js` (implementation, already implements the pause
contract). No always-large trigger fires: no Alembic migration, no hypertable change,
no FastAPI surface change, and no ADR *contract* change (ADR-024 already documents this
exact pause/resume behavior; this ticket implements against it, it does not alter the
decision). Single component (`ac_driven_dev`), 2 files, no cross-module boundary crossed
— all three threshold criteria land on "small". `requires_adr: false` and
`requires_diagram: false` in the existing frontmatter are confirmed correct; no ADR or
diagram suggestion is warranted (the sequence diagram this behavior appears in,
c3-006-plan-feature-decision-gate-sequence.md, is owned by sibling ticket ACD-2100e-1).

Design note for the implementer: I read `plan-feature.js` end-to-end around the areas
this ticket names. `resolveGate()` (~line 1517) and `pauseAtGate()` (~line 1590) already
implement the described contract — unattended path skips `liveGateFn` entirely, persists
via an agent-mediated `pause_store.py` write with a repo-anchored `--store-dir`
(ACD-2100a-4), verifies the write by reading it back before reporting
`paused_awaiting_input`, and every non-proceed `resolveGate()` outcome triggers an
immediate `return` at the call site (e.g. line ~3120 for the final gate) before any
later step can run — this appears to already satisfy AC-1 through AC-5. One design
concern: the ticket's Implementation Notes claim "the residual destructive default is
line 2086" — that line number is now stale (it currently holds
`resolveRepoAnchoredScriptPath`, an unrelated helper added by later work). I searched
the file for remaining hardcoded terminal defaults and found exactly one candidate,
`const finalDecision = _finalGateResult || { action: "defer" };` (~line 3124) — but it
is unreachable dead code: `resolveGate()` never returns a falsy value, and the three
lines immediately above it already `return` on every pause-related status before this
line executes. Recommend the test-writer/python-coder pair verify this reachability
claim directly (a test that forces the unattended path and asserts the `||` branch is
never taken) rather than chasing the stale line-number pointer as if it names a live
bug — the remaining risk here is in the INTEGRATION test coverage (worktree cwd,
byte-identical drafted-work, cross-process readability) that this ticket's Test
Requirements call for, not in a residual defect in the gate-resolution code itself.

Acceptance-criteria adjustments: none — the six checklist items and four integration
tests as written are sufficient and appropriately scoped to prove behavior the code
already appears to implement.

### 2026-09-08 11:20 — test-writer (status: ok)
feedback-id: fb_2026-09-08_80022537
completion_manifest:
  tests_written: true
  red_baseline_verified:
    result: false
    reason: "All 4 tests pass immediately (AC_ENFORCE_STRICT=1, exit 0) against
      HEAD -- no red state was achievable. ACD-2100c-2's behaviour is already
      fully implemented by three prior, now-done tickets in this same epic:
      ACD-2100a-4 (repository-anchored pause_store.py write/read/verify),
      ACD-2100c-1 (resolveGate() never invokes liveGateFn -- every gate always
      pauses headless, with an immediate `return` at every call site), and
      ACD-2100b-5 (landed 2026-09-07, AFTER a4 and c-1, moved workspace-setup
      permission resolution out of an in-workflow agent() dispatch into a
      pre-computed `args.workspace_setup_permission` verdict). This last
      change is why the sibling, already-signed-off test_acd_2100a_4.py also
      now fails at HEAD when re-run as-is (verified: it still mocks the
      removed 'resolve-workspace-setup-permission' label, so every one of its
      4 tests now halts at the very first dispatch with 'No workspace-setup
      permission pre-flight verdict was supplied in args' -- a real,
      pre-existing regression from ACD-2100b-5 landing after a4 was signed
      off, out of this ticket's scope to fix). My own 4 tests supply
      `args.workspace_setup_permission: {permits: true}` directly and confirm
      the run reaches final-gate and pauses cleanly. I also targeted the one
      residual concern architect-review flagged above (the
      `_finalGateResult || {action: \"defer\"}` fallback at ~line 3124): both
      test_unattended_run_from_a_worktree_pauses_with_a_record_on_disk and
      test_no_step_after_the_decision_point_runs_when_no_person_can_answer
      assert the terminal status is exactly 'paused_awaiting_input' (never
      'ok'/'defer', which is what that fallback would produce if it were ever
      reached) -- it is not reached."
    remediation: "python-coder: there is no red assertion to turn green on
      THIS ticket. Per the TDD-Order convention (CLAUDE.md 'TDD Order --
      test-writer Must Precede python-coder'), document this ticket as a
      verified-already-satisfied AC rather than TDD-compliant new work, and
      re-run `AC_ENFORCE_STRICT=1 python3 -m pytest
      unit_tests/workflows/test_acd_2100c_2.py -v` after any change to
      confirm all 4 stay green. Optional cleanup only: the confirmed-dead
      `_finalGateResult || {action: \"defer\"}` fallback at ~line 3124 may be
      removed for clarity, but removing it is not required by any of these
      4 tests. Separately, and out of this ticket's own scope: flag to the
      epic owner that test_acd_2100a_4.py needs a follow-up fix for the
      ACD-2100b-5 permission-mechanism change (see reason above) -- it is a
      real, pre-existing regression this investigation surfaced, not caused
      by this ticket's work."
cross_layer_seam_answer:
  result: covered
  producing_side: "pauseAtGate()'s real pause_store.py write dispatch
    (templates/workflows-js/plan-feature.js), executed for real via a Node
    child_process against a real, verbatim, on-disk
    .leafcutter/scripts/pause_store.py, from a process started inside a real
    git worktree holding no .leafcutter/ of its own"
  consuming_side: "a second, independent, real `pause_store.py read`
    subprocess started from the project root
    (test_pause_written_from_the_worktree_is_readable_from_the_project_root)"
reachability_entry_point_answer:
  result: resolved
  entry_point: "the /plan-feature slash command
    (templates/commands/plan-feature.md) dispatches
    Workflow(\"plan-feature\", { userInput: $ARGUMENTS }), which executes
    templates/workflows-js/plan-feature.js's top-level body. The Workflow
    tool itself has no invocable test seam, so
    test_no_step_after_the_decision_point_runs_when_no_person_can_answer (and
    every sibling test in this directory: test_acd_2100a_4.py,
    test_bo_2300_pause_resume.py) drives that body directly via a real Node
    subprocess -- this directory's established convention -- and asserts the
    pause outcome is CONSUMED in control flow (the run's own terminal
    result), not merely computed and discarded."

Wrote unit_tests/workflows/test_acd_2100c_2.py (4 tests) per this ticket's
Test Requirements table, one per named angle: (1) criterion -- an unattended
run from a real git worktree with drafted work already on disk leaves a
durable, run-and-gate-naming record and reports 'paused_awaiting_input'; (2)
reachability -- the run's own captured dispatch record ends at
pause-persist-verify with nothing after it, and no unexpected file appears
anywhere under the project; (3) real_artifact -- the drafted work file's
bytes, read from disk before and after the real run, are byte-identical; (4)
seam -- a second, independent `pause_store.py read` process started from the
project root finds the exact record the worktree-started process wrote and
reports it as waiting. All 4 real-execute a real `git worktree add` fixture
and a verbatim copy of scripts/pause_store.py (2h.2 Fixture Authenticity
Rule), mirroring test_acd_2100a_4.py's own harness conventions.

RED BASELINE COULD NOT BE ESTABLISHED: all 4 tests pass immediately at HEAD
(verified with `AC_ENFORCE_STRICT=1` to bypass the AC-status xfail mask --
see `unit_tests/ac_store/` conftest hook). This is not a weak-assertion
problem: I deliberately targeted the architect's own flagged residual concern
and the strictest available invariants (exact dispatch-list-ends-at-verify,
exact terminal status, exact byte comparison) and all still pass. Per the
Outcome Handling table I do not sign off silently on an all-green result --
seeing this documented in `## Comments` before hand-off is the point.
python-coder's job on this ticket is therefore verification/cleanup, not new
red-to-green implementation; see `remediation` above.

### 2026-09-08 11:12 — python-coder (status: ok)
feedback-id: fb_2026-09-08_2c97ada7
completion_manifest:
  red_baseline_reconfirmed: true
  behaviour_already_satisfies_ac: true
  dead_fallback_removed:
    result: true
  regression_suite_green: true
Re-ran `AC_ENFORCE_STRICT=1 python3 -m pytest unit_tests/workflows/test_acd_2100c_2.py
-v` and confirmed all 4 named tests (criterion, reachability, real_artifact, seam) pass
against HEAD, matching test-writer's finding that ACD-2100c-2's behaviour is already
fully implemented via ACD-2100a-4 + ACD-2100c-1. I independently re-verified the one
open design concern architect-review and test-writer both flagged -- the
`_finalGateResult || { action: "defer" }` fallback at (then) ~line 3124 -- by reading
`resolveGate()` end to end (templates/workflows-js/plan-feature.js:1517-1673): every
return path out of it is either one of the four pause-related `status` values already
handled by the `return` at the final-gate call site (lines 3120-3122), or a decision
object from `applyAnswerByType()`; `resolveGate()` never returns a falsy value, so the
`||` fallback was confirmed unreachable dead code, not a live defect. Per the
remediation's "optional cleanup" note I removed the fallback
(`const finalDecision = _finalGateResult;`) and replaced it with a comment recording
this reachability proof, since dead code that looks like a live destructive default is
itself a readability hazard for the next person auditing this gate. This is a
no-behaviour-change edit: re-ran the same 4 tests plus the 8 ACD-2100c-1 tests and all
26 tests across `test_bo_2300_pause_resume.py` /
`test_bo_2300a_1_refusal_not_decision.py` / `test_bo_2300a_2_cancel_status_distinct.py`
(the other suites exercising `resolveGate`/`pauseAtGate`) -- all 38 tests green, no
regressions. `node --check` confirms the file still parses. ADR-024 was read as
cross-link-only per the documentation-expert contract block and architect-review's
classification (`status: exists`, not an edit target) and was left unmodified. No
`.py` files were touched (this ticket's implementation target is a `.js` workflow
file), so `doc-enforcer` (Python/SQL docstring enforcement) and `complexity-reduction`
do not apply; no complexity-reduction skill exists in this package's skill set at all.
Per the TDD-Order convention, documenting explicitly: this ticket is a
verified-already-satisfied AC plus a small dead-code cleanup, not new red-to-green
implementation -- consistent with test-writer's finding. Flagging forward (not this
ticket's scope): `test_acd_2100a_4.py` needs a follow-up fix for the ACD-2100b-5
`workspace_setup_permission` regression that test-writer identified above.

### 2026-09-08 12:05 — test-runner (status: ok)
feedback-id: fb_2026-09-08_9edc9493
completion_manifest:
  test_suite_executed: true
  all_tests_passing: true
  failure_report_structured: true
Ran `AC_ENFORCE_STRICT=1 python3 -m pytest unit_tests/workflows/test_acd_2100c_2.py -v`
(the ticket's own Test Requirements suite, added by test-writer) — all 4 named tests
(criterion, reachability, real_artifact, seam) passed. Also ran `node --check` on the
edited `templates/workflows-js/plan-feature.js` (exit 0, no syntax errors) and the three
sibling pause/resume regression suites python-coder cited
(`test_bo_2300_pause_resume.py`, `test_bo_2300a_1_refusal_not_decision.py`,
`test_bo_2300a_2_cancel_status_distinct.py`) — all 26 tests green, no regressions. No
failures to report; ADR-024 was cross-link-only per the documentation-expert contract
and was not re-verified against a test surface.

### 2026-09-08 11:24 — pr-reviewer (status: ok)
feedback-id: fb_2026-09-08_10b39eec
completion_manifest:
  diff_reviewed: true
  high_confidence_findings: 0
  medium_confidence_findings: 0
  cross_file_contract_traced: true
  tests_reconfirmed_green: true
Reviewed the actual diff for this ticket's `files_touched`: `ADR-024-interactive-pause-resume.md`
has zero lines changed (confirmed via `git diff HEAD`), consistent with its
documentation-expert-contract role as cross-link-only, and `plan-feature.js` carries
exactly one 12-line hunk — replacing `const finalDecision = _finalGateResult || { action:
"defer" };` with `const finalDecision = _finalGateResult;` plus an explanatory comment.
Independently re-traced reachability rather than taking the removal on trust: read
`resolveGate()` end-to-end (lines 1517-1569) and confirmed every return path yields a
truthy object (four pause-status literals, or `applyAnswerByType()`'s object, which always
sets an `action` key even when the caller's fields are absent), so `_finalGateResult` can
never be falsy and the removed `||` fallback was genuinely dead. No high- or
medium-confidence findings.

Performed the cross-file contract trace required by this ticket's `## Agent Contracts`
block: (1) *Expects From* ACD-2100a-4 — traced `pauseAtGate()`'s write (line ~1617),
`resolveGate()`'s resume-check read (line ~1536), and the write-verify read (line ~1638)
and confirmed all three route through the same `buildPauseStoreCommand()` helper with an
explicit `--store-dir`, so the "read/write/read-back all resolve to the project's own
store" contract from ACD-2100a-4 cannot disagree across call sites. (2) *Delivers To*
ACD-2100c-3 — read that ticket (`15_TICKET-20260826-ACD-2100c-3.md`, still `status: todo`,
no consuming code yet exists); confirmed the persisted record (`{run_id, gate_id, question,
context, status}`) already carries both fields ACD-2100c-3 will need to resume from
(`run_id`, `gate_id`), so no forward-looking gap is visible from the producer side today.

Re-executed the evidence independently rather than trusting prior sign-offs: `AC_ENFORCE_STRICT=1
python3 -m pytest unit_tests/workflows/test_acd_2100c_2.py -v` (4/4 passed), `node --check`
on the edited file (exit 0), `ruff check` on the new test file (clean), and the three cited
sibling regression suites (`test_bo_2300_pause_resume.py`,
`test_bo_2300a_1_refusal_not_decision.py`, `test_bo_2300a_2_cancel_status_distinct.py` —
26/26 passed). The new test suite is real-effect (writes a real pause record via a real
subprocess and reads it back from a second independent process), not dispatch-topology-only,
so it satisfies the durable-side-effect coverage bar. `unit_tests/_workflow_engine_harness.py`
and ticket 19 (`ACD-2100d-1`) also show as modified in this shared worktree but are outside
this ticket's `files_touched` and were not reviewed here. No blockers; signing off `ok`.

### 2026-09-08 12:30 — ac-validator (status: ok)
feedback-id: (submit-failed)
completion_manifest:
  all_acs_evidenced: true
  store_alignment_check_passed: true
  cross_file_contract_traced: true
`submit_feedback.py` rejected the write — `ac-validator` is entirely absent from
`allowed_writers` in `config/feedback_categories.yaml` for every category, the same
gap previously found and fixed for `user-surface-smoker` and `documentation-verifier`
(see that file's own decision history); using `(submit-failed)` per §2a step 4 and
proceeding, flagging the gap via knowledge capture.

All 6 checklist ACs covered (both implementation and test evidence found for each):

AC-1 (worktree cwd) covered:
  implementation: `templates/workflows-js/plan-feature.js` — `pauseAtGate()`/`resolveGate()`
    persist and read via `buildPauseStoreCommand()` (line 2051), which passes an explicit
    `--store-dir` in the same dispatched command rather than a caller-relative path, so
    behaviour does not depend on process cwd (read at lines 1517-1650, 2051-2065).
  test: `test_unattended_run_from_a_worktree_pauses_with_a_record_on_disk`
    (unit_tests/workflows/test_acd_2100c_2.py) — drives a real `git worktree add` fixture
    as the process cwd; PASSED (`AC_ENFORCE_STRICT=1 pytest ... -v`, re-run independently).

AC-2 (drafted work present before decision point) covered:
  implementation: `pauseAtGate()` (plan-feature.js:1590-1650) touches only the pause-store
    record via agent dispatch — no code path reads or writes arbitrary drafted-work paths.
  test: same criterion test's fixture setup + `test_drafted_work_is_byte_identical_after_the_pause`.

AC-3 (no step after decision point runs) covered:
  implementation: `plan-feature.js` final-gate call site (~3117-3123, visible in the staged
    diff) — `if ([...].includes(_finalGateResult.status)) { return _finalGateResult; }`
    returns immediately for every pause status before any later step executes.
  test: `test_no_step_after_the_decision_point_runs_when_no_person_can_answer` — PASSED.

AC-4 (durable record naming run + gate present after exit) covered:
  implementation: `pauseAtGate()` builds `rec = {run_id, gate_id, question, context, status}`
    and persists it via `buildPauseStoreCommand("write ...")` (line 1604-1619), then verifies
    the write by reading it back (line ~1621 onward) before reporting success.
  test: `test_unattended_run_from_a_worktree_pauses_with_a_record_on_disk` (criterion) +
    `test_pause_written_from_the_worktree_is_readable_from_the_project_root` (seam) — both PASSED.

AC-5 (run reports waiting for an answer) covered:
  implementation: `pauseAtGate()` sets `status: "paused_awaiting_input"`; the removed
    `_finalGateResult || {action:"defer"}` dead fallback (confirmed unreachable — every
    `resolveGate()` return path is truthy) cannot mask this into a false "defer"/"ok".
  test: `test_unattended_run_from_a_worktree_pauses_with_a_record_on_disk` asserts terminal
    status is exactly `paused_awaiting_input` — PASSED.

AC-6 (drafted work byte-identical after pause) covered:
  implementation: same as AC-2 — the pause path never touches drafted-work files.
  test: `test_drafted_work_is_byte_identical_after_the_pause` — PASSED.

Cross-file contract trace (Step 2d): *Expects From* ACD-2100a-4 (status: `done`) —
confirmed `buildPauseStoreCommand()` (plan-feature.js:2051) is the single write/read/
read-back path all three call sites route through, satisfying "resolve to the project's
own store, write durable before exit, reported success matches disk." *Delivers To*
ACD-2100c-3 (status: `todo` — consumer not yet built) — read that ticket; no consuming
code exists yet, so I verified the producer-side record shape instead: `pauseAtGate()`'s
persisted `rec` already carries both `run_id` and `gate_id` (plan-feature.js:1604-1608),
the fields the contract text names as what ACD-2100c-3 will resume from. No gap found on
the producer side; the consumer's own future implementation is out of this ticket's scope.

Store alignment: `check_v2_ac_store_alignment.py --ticket <this ticket>` exited 0, no
ERROR lines.

Ticket has no `## AC Coverage` table (pre-dates that template section) and no
`### ac-validator` block under `## Agent Contracts`, so §2c.2/§2c.3 of the signoff skill
were silently skipped per their own "no such heading exists" rule; I instead flipped the
6 top-level `- [ ] AC-N:` checkboxes to `[x]` and set `ac_coverage: 6/6` in frontmatter
per `ac-validator.md`'s own Step 4 procedure. Commit phase may proceed.

### 2026-09-08 11:46 — commit (status: ok)
feedback-id: fb_2026-09-08_b5ab7ffd
completion_manifest:
  pre_commit_hooks_pass: true
  commit_message_valid: true
  ticket_staged: true
Auto-authorized commit gate (supervised path, ticket_path provided — no interactive
prompt issued per this agent's supervised-path protocol): subject "chore(ac-driven-dev):
close out ACD-2100c-2, drop dead gate fallback"; staged files: `docs/acceptance-criteria/
ac-driven-dev/ACD-2100-entry-point-unblocked/ACD-2100c-2.yaml`, `templates/workflows-js/
plan-feature.js`, `tickets/00_inbox/epics/EPIC-StartingNewWorkTheProperWayAlways/
14_TICKET-20260826-ACD-2100c-2.md`, `unit_tests/workflows/test_acd_2100c_2.py`.

Pre-commit hook probe (`verify_precommit_active.py --json`, run from the worktree root)
returned `failing_checks: []` before committing — binary, config, git_hook, and canary all
passed. Ran `classify_staged_files()` / `detect_mixed_set()` against exactly this
ticket's four target paths (not the full, shared, dirty worktree index) per
`commit_classifier.py`: it flagged the ticket+code+test combination as a mixed set
(TICKETS + IMPLEMENTATION_CODE + TESTS is not on the exemption list, which only exempts
IMPLEMENTATION_CODE+TESTS). Reviewed and judged intentional: bundling one ticket's own
implementation, its test, its AC-store record, and its own sign-off history into a single
commit is this repository's established per-ticket commit convention (confirmed against
`git log`, e.g. `fcaef7afd chore(ac-driven-dev): close out ACD-2100c-1 sign-offs and AC
store links`) — not an accidental mixing of unrelated work — so I proceeded rather than
splitting.

Committed with `git commit -m "..." -- <4 explicit paths>` (never `git add -A`), per the
"Commit into a dirty shared tree" convention: the shared worktree also carries unrelated,
concurrently in-flight changes from other tickets (`scripts/build_phases.py`,
`tickets/.../19_TICKET-20260826-ACD-2100d-1.md`, `unit_tests/_workflow_engine_harness.py`,
and three untracked files) which were deliberately left untouched and unstaged — verified
via `git status --short` before and after, and confirmed HEAD moved
(`a8b0c75e9089a28d3a13852ec3893ce5b8e7f2ae`) with `git log -1`. Confirmed `ADR-024-
interactive-pause-resume.md` carries zero diff (matching pr-reviewer's and architect-
review's cross-link-only finding) so it was correctly excluded from the commit's pathspec.
No pre-commit hook failures; no autofix cycle needed. This sign-off's own ticket-file edit
will be staged and committed separately per the commit agent's own "Commit-Agent Sign-off
Step" protocol.

### 2026-09-08 11:38 — ac-fulfillment-gate (status: ok)
feedback-id: (submit-failed)
completion_manifest:
  ac_store_fields_verified: true
  auto_fix_applied: true
  schema_validated_after_fix: true
`submit_feedback.py` rejected the write — `ac-fulfillment-gate` is entirely absent from
`allowed_writers` in `config/feedback_categories.yaml` for the `complete` category (same
gap ac-validator flagged above for itself); using `(submit-failed)` per §2a step 4 and
proceeding, flagging the gap via knowledge capture.

Ran the shared resolver (`python3 .leafcutter/scripts/ac_store/ac_coverage_resolver.py
--ticket <this ticket>`) which resolved exactly one AC (`ACD-2100c-2`, `level: L2`, via the
two-key `ac_traceability` block) and reported `failures: [work_status, covered_by]` against
`docs/acceptance-criteria/ac-driven-dev/ACD-2100-entry-point-unblocked/ACD-2100c-2.yaml`:
`work_status` was `todo` and `covered_by` was `[]` despite six prior signed-off phases.

Gathered branch diff evidence via `git diff main...HEAD --name-only` (committed history)
unioned with `git diff HEAD --name-only` (staged/uncommitted work, since this ticket's own
test file and implementation edit are staged but not yet committed at this pre-commit
phase) and intersected with `files_touched`: `templates/workflows-js/plan-feature.js` is in
the diff; `docs/architecture/adrs/ADR-024-interactive-pause-resume.md` is not (0 lines
changed, confirmed cross-link-only per pr-reviewer's own finding above).

Auto-fixed (append-only, all three per §3):
- `work_status: todo` → `done` (files_touched ∩ diff non-empty).
- `implemented_by`: appended `templates/workflows-js/plan-feature.js` (the prior sole
  entry was only the ticket file itself, which does not satisfy the diff-intersection
  requirement for an L2 AC).
- `covered_by`: grepped `tests/` and `unit_tests/` for `# covers: ACD-2100c-2` and found
  four genuine tagged tests in `unit_tests/workflows/test_acd_2100c_2.py` (lines 412, 462,
  531, 569); appended that file path.

Ran `python3 scripts/commit_guardian/check_ac_schema.py <ac_yaml_path>` after the edit —
exit 0 (fell back to manual field validation with a warning that
`config/ac_store_schema.json` is not present at this workspace root; not a blocker). Re-ran
the resolver: `ok: true`, `failures: []`, `verified_count: 1`. AC store fulfillment complete
for this ticket's one resolved AC. Commit phase may proceed.

## Escalation

Branch: none
Reason: 2 files touched, 1 component (ac_driven_dev), no cross-module boundary; no
always-large trigger fired (no migration, no hypertable change, no public API change,
and no ADR *contract* change — ADR-024 is referenced as an existing, already-accepted
contract this ticket implements, not one it alters).
