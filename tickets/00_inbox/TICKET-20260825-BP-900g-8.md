---
ac_coverage: 5/5
ac_traceability:
  id: BP-900g-8
  path: docs/acceptance-criteria/build_pipeline/BP-900-deployment-completeness/BP-900g-8.yaml
advances_current_outcome: true
agents:
  ac-fulfillment-gate: signed_off
  ac-validator: signed_off
  architect-review: signed_off
  commit: signed_off
  documentation-expert: signed_off
  documentation-verifier: not_needed
  pr-reviewer: signed_off
  pull-request: needed
  python-coder: signed_off
  test-runner: signed_off
  test-writer: signed_off
change_target: pipeline
complexity: high
components:
- build_pipeline
created: '2026-08-25'
depends_on: []
documentation_required: true
files_touched:
- scripts/build.py
- scripts/build_phases.py
- scripts/build_referential_integrity.py
priority: high
requires_adr: false
requires_diagram: false
risk_surface: contract_boundary
roadmap_phase: phase_1
source_ac: BP-900g-8
status: todo
title: A deployed script's own intra-package imports are deployed with it, proven
  in the deployed tree
---

# A deployed script's own intra-package imports are deployed with it, proven in the deployed tree

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BP-900g-8` — A deployed script's own intra-package imports are deployed with it, proven in the deployed tree — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BP-900g-8`. Component: `build-pipeline`. Assigned agent: `python-coder`. Estimated complexity: `L`. Complexity: `high`.

## Acceptance Criteria

```gherkin
Given any script the build deploys into a consumer install,
And that script resolving, importing, or executing another module belonging to the
  same package — whether by import statement, by path construction relative to its
  own location, or by dynamic loader,
When the build runs its deploy-completeness preflight,
Then every such intra-package dependency is itself deployed, or the build exits
  non-zero naming the deployed script, the dependency it needs, and the deploy
  phase that would have to carry it,
And the dependency set is DERIVED from what the deployed script actually resolves,
  so a module added to a deployed script tomorrow is covered without anyone editing
  a list — a hand-maintained manifest of dependencies does not satisfy this
  criterion even when it happens to be complete on the day it is written,
And the closure is transitive: a dependency of a dependency is subject to the same
  rule, so a chain that is correct at its first hop and broken at its second fails
  the build rather than passing it,
And the demonstration runs the build into a temporary target directory and then
  exercises the DEPLOYED copy in a fresh subprocess, with a negative control that
  withholds one dependency from the deploy and must be shown to fail the build —
  a check performed against the source tree does not satisfy this criterion,
  because the source tree contains every module by construction and is therefore
  structurally blind to the defect,
Because at HEAD 339b0981c scripts/ac_store/generate_ticket_from_ac.py is deployed
  by the build_ac_store phase and resolves its sibling
  scripts/ac_store/_component_migration_map.py, which that phase does not deploy —
  so in every consumer install the sibling is absent. That deploy map already
  carries THREE separate comments warning about this exact ModuleNotFoundError
  failure mode, for done_proof.py, test_enforcement.py and ac_parent_id.py, and
  still contains a live fourth instance. A hand-maintained list annotated with
  warnings about the way it fails is not a mechanism; it is a record of the
  mechanism's absence.
```

- [x] AC-1: that script resolving, importing, or executing another module belonging to the
- [x] AC-2: every such intra-package dependency is itself deployed, or the build exits
- [x] AC-3: the dependency set is DERIVED from what the deployed script actually resolves
- [x] AC-4: the closure is transitive: a dependency of a dependency is subject to the same
- [x] AC-5: the demonstration runs the build into a temporary target directory and then

## Test Requirements

```yaml
tests:
- name: test_bp_900g_8_derived_closure_includes_the_sibling_the_deploy_map_omits
  file: unit_tests/test_bp_900g_8.py
  covers:
  - BP-900g-8
  asserts: Compute the closure for scripts/ac_store/generate_ticket_from_ac.py from
    the code and assert _component_migration_map.py is in it, and that it is reported
    as NOT covered by the build_ac_store declaration. Asserts the closure's contents
    and the containment verdict — never that a name appears in a list literal in build_phases.py.
  framework: unittest
  type: unit
  angle: criterion
- name: test_bp_900g_8_build_subprocess_blocks_when_a_resolved_dependency_is_withheld_from_the_deploy
  file: unit_tests/test_bp_900g_8.py
  covers:
  - BP-900g-8
  asserts: 'PRODUCTION ENTRY POINT with the must_block modifier, plus its paired control.
    Copy the package tree to a temp dir, withhold one intra-package dependency of
    a deployed script from the deploy declaration, run `python scripts/build.py --target-dir
    <tmp_target>` as a SUBPROCESS: assert non-zero exit and that the output names
    the deployed script, the missing dependency, and the phase that would have to
    carry it. Then run the SAME command against the unmodified tree: exit zero. Both
    halves through the same command — a positive-path build alone cannot tell a working
    guard from an absent one.'
  framework: unittest
  type: integration
  angle: reachability
- name: test_bp_900g_8_deployed_tree_contains_the_full_closure_of_every_deployed_script
  file: unit_tests/test_bp_900g_8.py
  covers:
  - BP-900g-8
  asserts: Run build.py into a temp target, then walk the DEPLOYED tree (not the source
    tree) and assert that for every deployed Python script, every module in its derived
    closure is present in the deployed tree. The source tree contains every module
    by construction, so the same assertion made there is structurally incapable of
    failing — this is the BP-811 distinction between the copy tier and the reachability
    tier.
  framework: unittest
  type: integration
  angle: deployed
- name: test_bp_900g_8_deployed_generate_ticket_cold_imports_in_a_fresh_subprocess
  file: unit_tests/test_bp_900g_8.py
  covers:
  - BP-900g-8
  asserts: 'From the temp target produced by the harness, execute the DEPLOYED copy
    of generate_ticket_from_ac.py in a genuinely fresh `python` subprocess with the
    package source tree absent from sys.path, and assert it reaches its entry point
    without a ModuleNotFoundError. importlib.reload does NOT satisfy this: it re-executes
    inside an already-populated namespace and masks cold-import failures — that is
    exactly how the GenReviewFixes H-2 forward-reference NameError survived its tests.'
  framework: unittest
  type: integration
  angle: real_artifact
```

## Implementation Notes

```yaml
config_schema_fragment:
  description: The three sets this AC and BP-900g-5 together govern, named explicitly
    so an implementer cannot satisfy one by collapsing it into another. The whole
    defect class is these three being assumed equal while never being compared.
  properties:
    deploy_declaration:
      description: 'SET B — the deploy_map: what a phase declares it ships. Remains
        an explicit, human-readable declaration. It is CHECKED against set A, not
        generated from it.'
      items:
        type: string
      type: array
    guard_manifest:
      description: 'SET C — _manifest_ac_store_scripts: the guard''s model of what
        has been deployed. Per BP-900g-5''s fifth it_requirement this must derive
        FROM set B. It must never be the source set B is derived from.'
      items:
        type: string
      type: array
    invariant:
      description: B must contain closure(A), enforced by a build failure; C is derived
        from B. Deriving B from A automatically, or deriving C from anything other
        than B, does not satisfy this AC.
      type: string
    resolved_closure:
      description: SET A — the transitive set of intra-package modules a deployed
        script actually resolves, DERIVED from the code by static analysis of imports,
        relative path construction, and dynamic loader calls. Never hand-maintained.
        This is the new artifact this AC introduces.
      items:
        type: string
      type: array
  required:
  - resolved_closure
  - deploy_declaration
  - guard_manifest
  type: object
constraints:
- 'THE BINDING DIRECTION RULE (resolves the conflict the BA escalated). Three sets,
  two edges, and the direction of each edge is fixed. Edge 1: B must CONTAIN closure(A)
  — checked at build time, failing the build when it does not. Edge 2: C DERIVES FROM
  B — this is BP-900g-5''s fifth it_requirement and it stays. What is FORBIDDEN: (a)
  generating B from A, which silently deploys whatever a typo''d import names and
  leaves BP-900g-9 with no declaration to check; (b) deriving the checker''s view
  from B, which makes the guard and the thing it polices agree by construction and
  detect nothing — this is the failure the AC''s own criteria name; (c) deriving C
  from A, which restores the divergence BP-900g-5 was closing. Full rationale in this
  AC''s notes under IT-PO RULING.'
- 'DERIVED MEANS DERIVED FROM THE CODE. The closure must be computed from what the
  deployed script resolves — import statements, paths built relative to __file__,
  and dynamic loader calls — not read from any list, map, allowlist, or annotation.
  The test that proves this is the newly-added-module case in BP-900g-8-i: add a module
  inside the test, have an already-deployed script resolve it, edit NO list, and require
  the build to either deploy it or fail naming it. An implementation that passes every
  other test and fails that one is an enumeration wearing the word ''derived''.'
- TRANSITIVE, AND SAY SO IN THE FAILURE. The closure follows dependencies of dependencies.
  When it fails at the second hop the message must name the module that is actually
  missing, not only the top-level deployed script, or the author fixes the wrong hop.
- 'EVERY DEPLOY PHASE, NOT ONLY build_ac_store. n_location_rule is ''all'': the live
  instance is in build_ac_store, but any phase in scripts/build_phases.py that ships
  intra-package Python modules is subject to the same closure. A fix scoped to the
  one phase where the defect was found reproduces the history the criteria cite —
  three prior instances, each fixed individually, each annotated with a warning comment,
  and a fourth still live.'
- DO NOT SATISFY THIS BY ADDING _component_migration_map.py TO THE DEPLOY MAP. That
  makes the live symptom disappear and leaves the mechanism exactly as it was. Adding
  it is necessary and is not sufficient; the closure check must independently fail
  on a tree where it is withheld.
- OUT-OF-PACKAGE MODULES ARE NOT OURS TO SHIP. The standard library, installed third-party
  distributions, and host-project paths the consumer owns must produce no finding.
  A closure that demands the build ship the standard library gets an allowlist, and
  an allowlist is a hand-maintained list wearing a different name. Respect BP-900g-5's
  narrowed output-root prefix bound and keep BP-900g-6's negative-control tests green
  — that bound exists because BP-900g-4 mis-normalised a host-project path (debugging/scripts/...)
  into a deploy key.
- STATIC ANALYSIS HAS A KNOWN BLIND SPOT AND IT MUST BE DECLARED, NOT PAPERED OVER.
  A dynamic loader that builds a module path from a runtime value cannot always be
  resolved statically. Where the closure cannot decide, it must report the unresolvable
  reference for a human rather than assume it is external — an unknown silently classified
  as not-ours is the same fail-open in a new place. BP-900g-10 is the runtime backstop
  for whatever the closure still misses; it is not a licence for the closure to guess.
- IDEMPOTENCY AND BUDGET. The closure computation must be deterministic and byte-stable
  across runs on an unchanged tree, and must run inside the existing build preflight's
  budget — static analysis of the package's own Python files, no network, no per-module
  subprocess.
n_location_rule: all
post_write_commands:
- python scripts/build.py --target-dir .
- python -m pytest unit_tests/ -q
reference_file_path: scripts/build_phases.py
required_skills:
- python-coder
```

## Agent Contracts

### documentation-expert

Existing docs to update / cross-link:

- docs/acceptance-criteria/build_pipeline/BP-900-deployment-completeness/BP-900g.yaml (relationship: parent | status: exists)
- scripts/build_phases.py (relationship: modifies | status: exists | relevance: Holds the build_ac_store deploy_map (~lines 851-873) carrying the live fourth instance and the three prior warning comments. The manifest that must become derived rather than hand-maintained.)
- scripts/ac_store/generate_ticket_from_ac.py (relationship: describes | status: exists | relevance: The deployed script with the undeployed dependency (~line 328 resolves and execs its sibling). The concrete instance, not the criterion.)
- scripts/ac_store/_component_migration_map.py (relationship: describes | status: exists | relevance: Present in source, absent from the deploy map — the dependency whose absence is invisible to every source-tree check.)
- scripts/build_referential_integrity.py (relationship: modifies | status: exists | relevance: The existing reference guard; BP-900g-6 established its output-root prefix bound, which this AC must not widen.)
- docs/testing/test-angles.md (relationship: describes | status: exists | relevance: Angle vocabulary for the acceptance demonstration (deployed + reachability + must_block).)
- docs/acceptance-criteria/build_pipeline/BP-900-deployment-completeness/BP-900g-5.yaml (relationship: describes | status: exists | relevance: IT-PO ADDITION — READ THIS BEFORE TOUCHING build_phases.py. Its fifth it_requirement asks that _manifest_ac_store_scripts derive FROM the deploy_map. That is a different edge of the same triangle and it is NOT in conflict with this AC, but it can be satisfied in a way that IS. The ruling on which direction wins is in this AC's notes and is binding on the implementer.)
- scripts/build.py (relationship: modifies | status: exists | relevance: IT-PO ADDITION. The process the preflight must run inside and whose exit code the criterion asserts on, and the command the deployed-tree harness invokes (`python scripts/build.py --target-dir <tmp>`). The harness this AC builds is reused by BP-900g-8-i and BP-900g-10.)
- CLAUDE.md (relationship: describes | status: exists | relevance: IT-PO ADDITION. 'New Hook / Gate Dependencies Must Be in the Build Deploy-Manifest' is the prose statement of this defect after the done_proof.py incident. It records the rule as a convention for humans to remember, which is precisely what this AC replaces with a mechanism — the convention was written and the fourth instance still shipped.)

- [x] AC-1: reference | docs/acceptance-criteria/build_pipeline/BP-900-deployment-completeness/BP-900g.yaml | every such intra-package dependency is itself deployed, or the build exits non-zero naming the deployed script, the dependency, and the phase that would carry it <!-- signed: documentation-expert -->

<!--
FORMAT FIX, 2026-08-26 (main loop, not documentation-expert).
This line was emitted by generate_ticket_from_ac.py as
`[(unspecified genre)] <path> — <text>` with zero `|` characters, because BP-900g
carries no documentation_triggers (the generator warned about exactly this at
generation time: "Parent AC of 'BP-900g-8' has no documentation_triggers;
emitting (unspecified genre) marker (BO-2200c-3-i)").

documentation-verifier's Step 2 parser requires
`- [ ] AC-N: <genre> | <target_path> | <content_constraint>`, so it could not
extract target_path and fail-closed with a parse blocker rather than a
docs finding. Its refusal was correct.

Rewritten by hand into the pipe-delimited form so the verifier can parse it.
The genre is `reference` — the target is an AC YAML record, which is lookup
material. The underlying generator defect is filed separately; fixing the line
here unblocks this ticket, it does not fix the emitter.
-->

### Delivers To

- **Agent:** python-coder
- **Contract:** Two artifacts. (1) The derived transitive intra-package dependency closure — a function over the package tree returning, per deployed script, the set of intra-package modules it resolves, computed from the code and never from a list, together with the build-time check that set B (the deploy declaration) contains it. (2) The deployed-tree test harness: run `python scripts/build.py --target-dir <tmp>` into a temporary target and exercise the DEPLOYED copy in a genuinely fresh subprocess with the source tree off sys.path. Consumed by BP-900g-8-i (which drives three surface shapes through the same harness) and by BP-900g-10 (which removes a dependency from that same deployed tree and asserts the runtime behaviour). The harness is shared, not rebuilt per AC.

## Sign-offs

- [x] architect-review — 2026-08-25 17:02
- [x] test-writer — 2026-08-25 17:45
- [x] python-coder — 2026-08-25 18:40
- [x] test-runner — 2026-08-25 23:05
- [x] documentation-expert — 2026-08-26 06:54
- [ ] pr-reviewer — failed 2026-08-26 07:42
- [x] ac-validator — 2026-08-26 08:15
- [x] ac-fulfillment-gate — 2026-08-26 07:48
- [ ] documentation-verifier — failed 2026-08-26 07:50
- [x] commit — 2026-08-26 09:10
- [ ] pull-request — failed 2026-08-26 09:15

## Comments

### 2026-08-25 17:02 — architect-review (status: ok)
feedback-id: fb_2026-08-25_d4e745db
completion_manifest:
  blast_radius_reviewed: true
  rubric_applied: true
  escalation_decision_made: true
Impact classification: SMALL. Files touched (scripts/build.py, scripts/build_phases.py, scripts/build_referential_integrity.py) = 3 (≤5), all within the single `build_pipeline` component, no cross-module boundary crossing. None of the always-large triggers fired: no Alembic migration, no hypertable change, no FastAPI public-API surface touched, and no `docs/architecture/adrs/ADR-*` file modified — the binding-direction rule (Set A resolved_closure / Set B deploy_declaration / Set C guard_manifest, and B-contains-closure(A) / C-derives-from-B) was already adjudicated by IT-PO under BP-900g-5, so this ticket implements a previously-settled contract rather than opening a new cross-component one; `requires_adr: false` and `requires_diagram: false` in frontmatter are confirmed correct.

Design note: the change adds an AST-based static-analysis closure computation plus a build-time containment check inside the existing `build.py` preflight, applied to every deploy phase in `build_phases.py` (`n_location_rule: all`), not just `build_ac_store`. Two layering risks to watch during implementation: (1) the preflight ordering in `build.py` must run early enough to fail the whole `--target-dir` invocation before any phase partially deploys, and must run for every code path that reaches deployment, not only the default CLI flags; (2) per the ticket's own "STATIC ANALYSIS HAS A KNOWN BLIND SPOT" constraint, any unresolvable dynamic-loader reference must be surfaced as an explicit human-facing finding rather than silently classified as external — this is the fail-open seam most likely to regress under time pressure. Respect BP-900g-6's narrowed output-root prefix bound in `build_referential_integrity.py`; do not widen it while adding the new closure check.

Acceptance-criteria adjustments: none — the Gherkin scenario and the four constraints (binding-direction, derived-not-enumerated, transitive-with-named-hop, every-deploy-phase) are already precise enough to implement and test against directly.

### 2026-08-25 17:45 — test-writer (status: ok)
feedback-id: fb_2026-08-25_e44a5101
completion_manifest:
  tests_written: true
  red_baseline_confirmed: true
  ac1_ac5_mapped: true
Wrote `unit_tests/test_bp_900g_8.py` with the four tests specified in `## Test Requirements`, covering AC-1 through AC-5 (derived closure computation, build-time containment check with a synthetic withheld declaration, a real `python scripts/build.py` subprocess must_block + positive-control pair, a deployed-tree transitive-closure walk, and a fresh-subprocess cold-import check of the deployed `generate_ticket_from_ac.py`). No `### test-writer` subsection exists under `## Agent Contracts` and no `## AC Coverage` table exists in this ticket body, so per signoff SKILL.md §2c.2 the AC-checkbox-flip and coverage-table steps are skipped (out of scope for this agent on this ticket). All four tests were run via `AC_ENFORCE_STRICT=1 python -m pytest unit_tests/test_bp_900g_8.py -v` and confirmed red for the right reasons: two fail with `AttributeError` against the not-yet-implemented `compute_intra_package_closure`/`find_uncovered_closure_dependencies` functions, one fails because the real `build.py` currently exits 0 with the dependency withheld (no guard exists yet), and one fails because the deployed `generate_ticket_from_ac.py` currently logs `Cannot load MIGRATION_MAP from ...` in a fresh subprocess (silent degradation, confirmed empirically via a probe build before authoring the assertion, to avoid an under-specified exit-0-only check). `git status --short scripts/build_phases.py` confirmed the test's on-disk withhold/restore cycle leaves the source tree clean.
red_baseline:
  - test_name: test_bp_900g_8_derived_closure_includes_the_sibling_the_deploy_map_omits
    file: unit_tests/test_bp_900g_8.py
    error: "AttributeError: module 'build_referential_integrity' has no attribute 'compute_intra_package_closure'"
  - test_name: test_bp_900g_8_build_subprocess_blocks_when_a_resolved_dependency_is_withheld_from_the_deploy
    file: unit_tests/test_bp_900g_8.py
    error: "AssertionError: build.py --target-dir exited 0 while a resolved intra-package dependency (_component_migration_map.py, resolved by generate_ticket_from_ac.py) was withheld from the build_ac_store deploy declaration."
  - test_name: test_bp_900g_8_deployed_tree_contains_the_full_closure_of_every_deployed_script
    file: unit_tests/test_bp_900g_8.py
    error: "AttributeError: module 'build_referential_integrity' has no attribute 'compute_intra_package_closure'"
  - test_name: test_bp_900g_8_deployed_generate_ticket_cold_imports_in_a_fresh_subprocess
    file: unit_tests/test_bp_900g_8.py
    error: "AssertionError: The deployed generate_ticket_from_ac.py logged 'Cannot load MIGRATION_MAP from ...' -- proof that its sibling _component_migration_map.py did not load from the DEPLOYED tree in a fresh subprocess."

### 2026-08-25 18:40 — python-coder (status: ok)
feedback-id: fb_2026-08-25_80c1516b
completion_manifest:
  closure_computation_implemented: true
  build_time_guard_wired: true
  binding_direction_rule_respected: true
  n_location_rule_all_applied: true
  red_baseline_all_green: true
  full_unit_suite_green: true
red_baseline_results:
  - test_name: test_bp_900g_8_derived_closure_includes_the_sibling_the_deploy_map_omits
    result: green
  - test_name: test_bp_900g_8_build_subprocess_blocks_when_a_resolved_dependency_is_withheld_from_the_deploy
    result: green
  - test_name: test_bp_900g_8_deployed_tree_contains_the_full_closure_of_every_deployed_script
    result: green
  - test_name: test_bp_900g_8_deployed_generate_ticket_cold_imports_in_a_fresh_subprocess
    result: green
No `### python-coder` subsection exists under `## Agent Contracts` (only `### documentation-expert` and `### Delivers To` prose) and there is no `## Implementation Tasks` section in this ticket, so per signoff SKILL.md §1.5 and §2c.2 the task-checkbox and AC-checkbox-flip steps are skipped, matching test-writer's prior sign-off on this same ticket.

Implemented both deliverables named in `### Delivers To`. (1) `build_referential_integrity.compute_intra_package_closure(script, root)` — AST-based static analysis that walks a script's `import`/`from...import` statements (including relative imports), plus `importlib.util.spec_from_file_location(name, path)` dynamic loads where the `path` expression reduces to a concrete file via a function-scoped local-variable-aware evaluator (`_eval_static_path`) recognising `Path(__file__).resolve().parent / "..."`-style chains — transitively, with existence-under-*root* as the sole external/internal discriminator (no allowlist). An unresolvable dynamic-loader expression (e.g. `goal_to_epic.py`'s runtime source/deployed-layout ternary) is logged as a WARNING naming the file/line rather than silently treated as external, per the ticket's blind-spot constraint. `find_uncovered_closure_dependencies(script_rel_path, root, declared)` is the Set-B-contains-closure(A) containment check. (2) `build.py._check_intra_package_closure_guard`, wired into `main()` alongside the existing script-reference and tracked-source preflights (before `_run_phases()` writes anything), applied uniformly across every deploy category `_get_source_deployable_scripts` covers (`n_location_rule: all`) — not scoped to `build_ac_store` alone.

Per the binding-direction rule: Set B (`build_phases.AC_STORE_DEPLOY_MAP`, extracted from the previously-inline `deploy_map` local so it can be shared) stays an explicit, human-authored declaration — never generated from Set A. Set C (`build._manifest_ac_store_scripts`) now derives FROM `AC_STORE_DEPLOY_MAP` instead of scanning every file physically present in `scripts/ac_store/` (BP-900g-5's fifth it_requirement, previously unimplemented despite that AC's `work_status: done`) — this was necessary for the guard to be meaningful at all, since checking Set A against a Set C that was really "whatever exists in source" made containment trivially true by construction. `_get_source_paths_for_guard`'s ac_store section was updated the same way to keep `test_guard_source_paths_match_deployable_set` (cardinality parity) green.

Added `_component_migration_map.py` to `AC_STORE_DEPLOY_MAP` (the AC's cited instance — necessary, not sufficient per the ticket's explicit constraint). Running the new guard against the real deploy map (before that entry existed) also surfaced a SECOND, previously-unknown instance of the same defect class: `validate_ac_schema.py` does `from _ac_components import components_field_errors, load_registry_ids`, and `_ac_components.py` was likewise present in source but absent from the map — concrete evidence the mechanism is derived, not an enumeration of the one cited case. Both entries are now in `AC_STORE_DEPLOY_MAP`.

Verification: all 4 tests in `unit_tests/test_bp_900g_8.py` pass (`AC_ENFORCE_STRICT=1`). The real `python scripts/build.py --target-dir .` run against this worktree's own deployment target exits 0. Full `python -m pytest unit_tests/ -q` (the ticket's `post_write_commands`) — 3984 passed, 7 skipped, 2 xfailed, 0 failed (an initial run surfaced one pre-existing-pattern regression, `test_guard_source_paths_match_deployable_set`, from the Set-C fix described above; fixed by updating `_get_source_paths_for_guard` in the same commit, then reconfirmed green). `ruff check scripts/build.py scripts/build_phases.py scripts/build_referential_integrity.py` passes with no findings.

### 2026-08-25 23:05 — test-runner (status: ok)
feedback-id: (recorded-by-proxy)
completion_manifest:
  suite_run: true
  failure_triaged: true
  recorded_by: main-loop proxy, not the driving session
Full suite re-run independently from a settled tree:
`python3 -m pytest unit_tests -q --no-header -p no:cacheprovider`
→ **3983 passed, 1 failed, 7 skipped, 2 xfailed, 467 subtests passed in 479.89s.**
All four `unit_tests/test_bp_900g_8.py` tests pass.

The single failure is
`unit_tests/commit_guardian/test_ge_122e_1.py::TestGE122e1::test_goal_record_claims_a_new_id_and_its_folder_matches_origin_main`:
`AssertionError: Lists differ: ['GE-120c-6'] != []`.

**Triaged as branch staleness, not a regression, and verified rather than assumed.**
That test compares this working tree's `GE-120-green-means-checked/` folder against the
same folder at `origin/main`. This worktree sits at `12716f07` (an older main);
`GE-120c-6.yaml` was added to `origin/main` afterwards. Confirmed both directions:
`git ls-tree -r origin/main` lists `GE-120c-6.yaml`, and `ls` in this worktree does not.
So the test correctly reports an id present on main and absent locally — a consequence of
the branch being behind, in a `guardrail-engine` AC folder, with no relationship to
`build.py`, `build_phases.py` or `build_referential_integrity.py`. It will clear when this
branch takes `origin/main`, which must happen before merge in any case.

**Why this sign-off is written by a proxy.** The driving session recorded in
`debugging/logs/feedback.jsonl` at 18:20Z that it was "unable to write ticket sign-off
because Edit/Write tools are disabled for this session" — a tooling blocker, not a test
failure. Three concurrent full-suite runs were observed against this worktree, which is
the retry loop that produced: the suite passes, the sign-off cannot be recorded, the phase
is retried. That is also a live hazard, because this AC's own tests withhold and restore
`scripts/build_phases.py` on disk; concurrent runs can interleave and leave it withheld.
The runs have since exited and the tree was verified intact (`AC_STORE_DEPLOY_MAP` present,
file parses, two independent pre/post snapshots byte-identical) before this was written.

I did not re-derive the earlier phases' work and have signed off only this phase.

### 2026-08-26 06:54 — documentation-expert (status: ok)
feedback-id: fb_2026-08-26_fdcfcd70
completion_manifest:
  doc_written: true
  cross_links_added: true
  diataxis_genre_correct: true
Read the ticket's Agent Contracts cross-link list (`BP-900g.yaml` parent, `test-angles.md`,
`BP-900g-5.yaml`, `CLAUDE.md`) for context; of those, only `docs/reference/false-green-mechanisms.md`
(not itself in the list, but the doc M2 in that catalogue is verbatim the failure class this AC
fixes) was the correct genre-appropriate target — it is an existing **reference** doc, not a
new artifact, so no ADR/architecture-diagram dispatch was warranted (`requires_adr: false`,
`requires_diagram: false` both hold). The files tagged `relationship: describes` in the Agent
Contracts list (`CLAUDE.md`, `BP-900g-5.yaml`, `generate_ticket_from_ac.py`,
`_component_migration_map.py`, `test-angles.md`) were read for context only, per that tag's
convention (vs. `modifies`, used only for the three already-implemented code files) — none were
edited. Added an "Update — BP-900g-8" paragraph to the M2 section documenting the new
`build._check_intra_package_closure_guard` / `build_referential_integrity.compute_intra_package_closure`
/ `find_uncovered_closure_dependencies` mechanism verified against the actual function names and
call site in the diff (`scripts/build.py:1056-1093,1682`), the residual dynamic-loader blind spot
the mechanism still declares rather than papers over, cross-linked to `BP-900g-8.yaml` from the
reference doc (frontmatter `related_docs` + a `See Also` entry), and bumped `last_updated` /
added `build_pipeline` to `components:`. Did not edit the AC YAML store (`BP-900g.yaml`/`BP-900g-8.yaml`)
itself for the reverse cross-link — that surface is schema/hook-governed and outside this agent's
remit; flagging as an open item below. Glossary coverage lint: `glossary_detector.py` ran
against the edited file and returned ~30 candidates, all pre-existing snake_case code-symbol
identifiers (e.g. `compute_intra_package_closure`, `validate_ac_schema`, `run_agent_eval`) matching
the doc's established convention of citing exact function/constant names rather than project
jargon; no `glossary-triage` agent dispatch was available in this session (no Agent-tool sub-dispatch
in this environment) so none were formally triaged — none appear to be candidate glossary terms.
No new terms added to `docs/glossary.md` or `docs/glossary_blacklist.md`.

**Open item:** `BP-900g.yaml`/`BP-900g-8.yaml` `doc_links`/`doc_links` fields do not point back at
the updated reference doc (one-directional cross-link only, from doc → AC). If AC-store
`doc_links` maintenance is expected of this phase on future tickets, that should be made an
explicit `modifies`-tagged target rather than `parent`/`describes`, since AC YAML edits carry
their own store-hygiene rules (stage-the-parent, schema validation) that this agent did not
apply here.

### 2026-08-26 07:42 — pr-reviewer (status: blocker)
feedback-id: fb_2026-08-26_51ad00ba
completion_manifest:
  diff_reviewed: true
  tests_run: true
  full_suite_verified: true
  sign_off_chain_reconciled:
    result: false
    reason: "Substantial working-tree changes (a real functional fix to build_referential_integrity.py plus 3 new regression tests in unit_tests/test_bp_900g_8.py) exist uncommitted, unstaged, and are not recorded by any agent's sign-off comment or feedback-id."
    remediation: "Respawn test-writer and python-coder to formally attest (sign-off comment + completion_manifest) to the current working-tree state of unit_tests/test_bp_900g_8.py and scripts/build_referential_integrity.py, then commit both together in the same commit as the already-committed BP-900g-8 work."

This ticket's committed work (`ea83b3b7`) and the four originally-specified tests are
correct: `python -m pytest unit_tests/test_bp_900g_8.py -q` — all 4 pass;
`python scripts/build.py --target-dir <tmp>` exits 0 cleanly against this tree with no
closure-guard findings; `ruff check scripts/build.py scripts/build_phases.py
scripts/build_referential_integrity.py` is clean; the diff to `build_referential_integrity.py`
is purely additive (BP-900g-6's output-root prefix bound is untouched); and a full
`python -m pytest unit_tests/ -q` run (3986 passed, 1 pre-existing unrelated
`test_ge_122e_1.py` branch-staleness flake already triaged by test-runner, 7 skipped, 2
xfailed) confirms no regression.

**[H-1] Uncommitted, unattested implementation work — blocks sign-off.** The working tree
(as of this review) contains 248 unstaged lines added to `scripts/build_referential_integrity.py`
(`_extract_syspath_directories`, `_is_syspath_mutation_call`, `_syspath_pushed_path_node`, an
`ast.IfExp` branch in `_eval_static_path`, and a module-level assignment fallback in
`_build_local_assignments`) plus 3 new tests in `unit_tests/test_bp_900g_8.py`
(`test_bp_900g_8_syspath_pushed_import_resolves_in_the_real_goal_to_epic_script`,
`test_bp_900g_8_unresolvable_syspath_push_logs_a_warning`,
`test_bp_900g_8_validate_ac_schema_closure_still_resolves_ac_components`). The in-file
"DECISION HISTORY" comment dates this as a "2026-08-26 [python-coder/BP-900g-8 review-fix]"
pass, and file mtimes (test file 06:52, implementation file 06:55) place it right around
`documentation-expert`'s 06:54 sign-off — but no ticket comment, no `feedback-id`, and no
sign-off records it, and `git status` shows the ticket + docs file staged while these two
files are unstaged. I independently verified the fix is real and needed (it closes a
silent-drop gap in `compute_intra_package_closure` for `sys.path.insert()`-based sibling
imports — worse than the documented dynamic-loader blind spot, since the pre-fix behaviour
produced zero log output at any level for `scripts/goal_to_epic.py`'s
`from scan_ac_store import traverse_ac_tree` after
`sys.path.insert(0, str(_sibling_dir))`) and that all 7 tests now pass. The problem is
purely one of attestation and commit hygiene: if the `commit` phase stages `files_touched`
literally (`scripts/build.py`, `scripts/build_phases.py`,
`scripts/build_referential_integrity.py` — note `unit_tests/test_bp_900g_8.py` is NOT in
`files_touched`), the fix could land without its own regression tests in the same commit,
or the unstaged work could be silently dropped/lost entirely. Per this repo's own
"Commit message matches the diff" / "stage the parent alongside the child" conventions,
this must not proceed to `commit` until `test-writer` and `python-coder` have formally
signed off on this exact working-tree state and it is staged/committed as one unit.

**[medium] Residual disclosure gap in the new `sys.path` handling.**
`_is_syspath_mutation_call` recognises only `sys.path.insert(...)` and `sys.path.append(...)`;
`sys.path.extend(...)` or a slice-assignment form (`sys.path[0:0] = [...]`) would be silently
un-scanned (zero log output), which is the same "unknown silently classified as external"
failure mode the AC explicitly forbids for the shapes it does recognise. No instance of
either alternate shape currently exists in this codebase (confirmed by running the guard
against the full source tree: `rc 0`, only genuine `spec_from_file_location`/`sys.path`
blind spots logged), so this is latent rather than a live defect — worth a follow-up
constraint or an explicit note in the docstring rather than a blocker.

**Suppressed (low, not itemised):** minor doc-genre observations already self-flagged by
`documentation-expert` (the one-directional `doc_links` cross-link open item); the AC
store's `work_status: todo` on `BP-900g-8.yaml` (owned by `ac-fulfillment-gate`/
`ac-validator`, not this phase); a stray pre-existing background `pytest` process observed
mid-review interleaving with this worktree's own withhold/restore test cycle — a live hazard
`test-runner` already documented, not a new finding, and it exited clean.

**Recommendation:** respawn `test-writer` (to formally attest to the 3 new tests) and
`python-coder` (to formally attest to the `sys.path` fix) against the current working-tree
state, then commit `scripts/build_referential_integrity.py` and
`unit_tests/test_bp_900g_8.py` together with the already-staged `docs/reference/false-green-mechanisms.md`
and ticket updates. Re-run `pr-reviewer` after that commit lands — I expect a clean `ok`
once the sign-off chain reflects the code actually shipping.

### 2026-08-26 08:15 — ac-validator (status: ok)
feedback-id: (submit-failed — `ac-validator` is absent from every category's
`allowed_writers` list in `config/feedback_categories.yaml`; `submit_feedback.py`
exits 1. Tooling gap, not a phase failure; noting here for a future fix pass
analogous to the documentation-verifier / user-surface-smoker additions recorded
in that file's own decision history.)
completion_manifest:
  acs_parsed: true
  evidence_gathered_per_ac: true
  store_alignment_checked: true
  ac_checkboxes_flipped: true
  ac_coverage_frontmatter_updated: true

pr-reviewer's H-1 (uncommitted, unattested `build_referential_integrity.py` sys.path fix
+ 3 tests) is resolved on disk: commit `4627c634` ("fix(build-pipeline): close the
sys.path-insert blind spot in the closure guard (BP-900g-8)") now carries that exact
work — `scripts/build.py`, `scripts/build_referential_integrity.py`, and
`unit_tests/test_bp_900g_8.py` — on top of `ea83b3b7`. `git status --short` shows only
`docs/reference/false-green-mechanisms.md` and this ticket file modified (no unstaged
implementation/test residual). `python -m pytest unit_tests/test_bp_900g_8.py -q` →
7 passed (all four originally-specified tests plus the three review-fix regression
tests). `check_v2_ac_store_alignment.py --ticket <this ticket>` → exit 0, no violations.

All 5 ACs classify **covered** (implementation + test evidence both found):

- AC-1 (script resolves/imports/execs a sibling module) covered:
  implementation: `scripts/build_referential_integrity.py` — `compute_intra_package_closure` (line 893), AST-based import/relative-import/dynamic-loader/`sys.path`-push resolution (`_extract_syspath_directories` line 729, `_closure_walk` line 857)
  test: `test_bp_900g_8_derived_closure_includes_the_sibling_the_deploy_map_omits` (unit_tests/test_bp_900g_8.py:104) and `test_bp_900g_8_syspath_pushed_import_resolves_in_the_real_goal_to_epic_script` (unit_tests/test_bp_900g_8.py:406)
- AC-2 (undeployed dependency fails the build naming script/dependency/phase) covered:
  implementation: `scripts/build.py` — `_check_intra_package_closure_guard` (line 1079), wired into `main()` before `_run_phases()`, message names deployed script, missing dependency, and `_phase_for_deploy_path` result
  test: `test_bp_900g_8_build_subprocess_blocks_when_a_resolved_dependency_is_withheld_from_the_deploy` (unit_tests/test_bp_900g_8.py:177) — real subprocess must_block + positive-control pair
- AC-3 (dependency set DERIVED, never hand-maintained) covered:
  implementation: `compute_intra_package_closure`/`find_uncovered_closure_dependencies` (build_referential_integrity.py:893,930) compute Set A from AST, checked against the separately-declared `AC_STORE_DEPLOY_MAP` (build_phases.py) — never generated from it
  test: `test_bp_900g_8_derived_closure_includes_the_sibling_the_deploy_map_omits` (unit_tests/test_bp_900g_8.py:104) and `test_bp_900g_8_validate_ac_schema_closure_still_resolves_ac_components` (unit_tests/test_bp_900g_8.py:509)
- AC-4 (closure is transitive) covered:
  implementation: `_closure_walk` (build_referential_integrity.py:857) recurses into each resolved module's own dependencies
  test: `test_bp_900g_8_deployed_tree_contains_the_full_closure_of_every_deployed_script` (unit_tests/test_bp_900g_8.py:271) — walks the DEPLOYED tree, not source
- AC-5 (temp-target build + fresh-subprocess deployed-copy exercise + negative control) covered:
  implementation: `build.py --target-dir <tmp>` harness invoked by the tests; `_check_intra_package_closure_guard` runs inside that same process
  test: `test_bp_900g_8_build_subprocess_blocks_when_a_resolved_dependency_is_withheld_from_the_deploy` (unit_tests/test_bp_900g_8.py:177, must_block + paired positive control) and `test_bp_900g_8_deployed_generate_ticket_cold_imports_in_a_fresh_subprocess` (unit_tests/test_bp_900g_8.py:334, genuinely fresh subprocess, source off sys.path)

Flipped all five `- [ ] AC-N:` checkboxes under `## Acceptance Criteria` to `- [x]` and set
`ac_coverage: 5/5` in frontmatter. No `## AC Coverage` table and no `### ac-validator`
subsection exist under `## Agent Contracts` in this ticket, so per signoff SKILL.md §2c.1/
§2c.3 those steps are skipped (matching the precedent already recorded by test-writer and
python-coder's sign-offs on this same ticket).

**Anomaly carried forward, not mine to fix:** pr-reviewer's frontmatter/Sign-offs rows
still read `failed` for the H-1 finding that is now resolved on disk. Re-running
`pr-reviewer` against the current tree (commit `4627c634`) is recommended before this
ticket proceeds past `ac-fulfillment-gate`/`commit`, since AC coverage and pr-review are
independent gates and this sign-off only clears the AC-coverage gate.

### 2026-08-26 07:48 — ac-fulfillment-gate (status: ok)
feedback-id: (submit-failed — `ac-fulfillment-gate` is absent from every category's
`allowed_writers` list in `config/feedback_categories.yaml`; `submit_feedback.py`
exits 1. Tooling gap, not a phase failure; same gap already noted by `ac-validator`'s
prior sign-off on this ticket.)
completion_manifest:
  ac_traceability_resolved: true
  ac_yaml_loaded_and_verified: true
  auto_fix_applied: true
  schema_validated_after_fix: true
  reresolved_ok: true

`ac_traceability:` present (two-key `{id, path}` form). Ran
`ac_coverage_resolver.py --ticket <this ticket>`: resolved exactly one AC,
`BP-900g-8`, at `docs/acceptance-criteria/build_pipeline/BP-900-deployment-completeness/BP-900g-8.yaml`
(level L2, so it is checked, not skipped). First pass reported `ok: false`,
`failures: [{ac_id: BP-900g-8, field: work_status}]`. Loaded the AC YAML: `work_status:
todo`, `implemented_by: [tickets/00_inbox/TICKET-20260825-BP-900g-8.md]` (no source-file
evidence — the ticket path itself is not in this ticket's own `files_touched`),
`covered_by: [BP-900g-8-i, BP-900g-8-ii, BP-900g-8-iii]` (non-empty — child
technical-constraint AC links, already satisfying the L2 non-empty check).

`git diff main...HEAD --name-only` shows `scripts/build.py`, `scripts/build_phases.py`,
`scripts/build_referential_integrity.py`, `tickets/00_inbox/TICKET-20260825-BP-900g-8.md`,
`unit_tests/test_bp_900g_8.py` — intersected with frontmatter `files_touched`
(`scripts/build.py`, `scripts/build_phases.py`, `scripts/build_referential_integrity.py`),
all three are diff-evidenced. Auto-fixed append-only: (1) `work_status: todo` → `done`
(diff evidence non-empty); (2) appended all three `files_touched ∩ diff` paths to
`implemented_by` (none were previously listed — only the ticket path was there). Ran
`scripts/commit_guardian/check_ac_schema.py` against the edited AC YAML: exit 0 (fell
back to manual field validation with a WARNING that `config/ac_store_schema.json` is
absent at the workspace root — not a schema-invalidity finding). Re-ran the resolver:
`ok: true`, `verified_count: 1`, `failures: []`,
`"All 1 AC(s) verified. work_status, implemented_by, and covered_by fields are accurate."`

Commit phase may proceed on the AC-store dimension. Not itself a re-adjudication of
`pr-reviewer`'s `failed` status (H-1, resolved on disk per `ac-validator`'s comment) —
that gate is independent of this one and is flagged here only for visibility, not
re-litigated by this phase.

### 2026-08-26 07:50 — documentation-verifier (status: blocker)
feedback-id: fb_2026-08-26_2a4976db
completion_manifest:
  required_docs_list_parsed:
    result: false
    reason: "The ## Agent Contracts / ### documentation-expert block contains exactly
      one AC line: '- [x] AC-1: [(unspecified genre)]
      docs/acceptance-criteria/build_pipeline/BP-900-deployment-completeness/BP-900g.yaml
      — every such intra-package dependency is itself deployed, or the build exits
      <!-- signed: documentation-expert -->'. Per the documentation-verifier template's
      Step 2 parsing algorithm this line must be pipe-delimited
      (- [ ] AC-N: <genre> | <target_path> | <content_constraint>); it contains zero
      '|' characters (confirmed via grep -o '|' on the line, count 0), so the
      target_path field cannot be extracted. This is a fail-closed parse failure, not
      an absent-docs finding."
    remediation: "Respawn documentation-expert (or whichever agent authors the Agent
      Contracts block) to rewrite the AC-1 line under ### documentation-expert into
      the pipe-delimited format the template requires, e.g.:
      '- [ ] AC-1: reference |
      docs/acceptance-criteria/build_pipeline/BP-900-deployment-completeness/BP-900g.yaml
      | <content constraint>'. Once the line is pipe-delimited, re-dispatch
      documentation-verifier."
  all_required_docs_present_in_diff: false
  no_placeholder_content_in_changed_docs: false
Fail-closed per the documentation-verifier template's Step 2: the single AC line under
`## Agent Contracts` -> `### documentation-expert` has no pipe separators, so the
required target_path cannot be parsed and Step 5/6 coverage checks cannot run.
Malformed line verbatim (ticket line 263):
  - [x] AC-1: [(unspecified genre)] docs/acceptance-criteria/build_pipeline/BP-900-deployment-completeness/BP-900g.yaml — every such intra-package dependency is itself deployed, or the build exits <!-- signed: documentation-expert -->
Expected format: `- [ ] AC-N: <genre> | <target_path> | <content_constraint>`. Per the
fail-closed posture (mirrors BP-1100 phantom-done enforcement, not folded into that
family), an ambiguous parse result must never produce `(status: ok)`. Responsible
agent: documentation-expert (respawn to reformat the AC-1 line as pipe-delimited).

### 2026-08-26 09:10 — commit (status: ok)
feedback-id: fb_2026-08-26_b01f4a9a
completion_manifest:
  pre_commit_hooks_pass: true
  commit_message_valid: true
  ticket_staged: true

Step 0a probe (`verify_precommit_active.py --json` run with cwd set to this
worktree root) returned `failing_checks: []` — all four checks (binary, config,
git_hook, canary) pass; an initial run from the wrong cwd falsely reported
`git_hook: false`, corrected by invoking with `env --chdir=<worktree>`. The
three implementation files named in this ticket's `files_touched`
(`scripts/build.py`, `scripts/build_phases.py`,
`scripts/build_referential_integrity.py`) plus their tests were already
committed by `python-coder`/reviewers at `ea83b3b7` and `4627c634` before this
phase ran — `git status --short` at the start of this phase showed nothing
outstanding for those paths. This commit phase staged and committed the three
remaining artifact-tracking changes that accumulated during sign-off:
`docs/acceptance-criteria/build_pipeline/BP-900-deployment-completeness/BP-900g-8.yaml`
(ac-fulfillment-gate's `work_status: done` + `implemented_by` reconciliation),
`docs/reference/false-green-mechanisms.md` (documentation-expert's M2 update),
and this ticket file (the accumulated sign-off/comment chain). Ran via
`classify_staged_files`/`detect_mixed_set` (`scripts/commit_classifier.py`):
primary group `docs`, `is_mixed: false` — no pattern-learner dispatch needed
(`specific_pattern_matched: true`). Commit created with
`COMMIT_AGENT_MODE=1 git commit`; `git log -1` confirmed HEAD moved to the new
SHA (not silently dropped).

**Anomaly carried forward, not resolved by this phase.** `pr-reviewer` and
`documentation-verifier` remain `failed` in this ticket's frontmatter/Sign-offs.
Per `ac-validator`'s and `ac-fulfillment-gate`'s own prior comments, each gate is
independent and not re-adjudicated by a different phase; this phase follows that
same established precedent and does not re-litigate either finding. Substantively:
(1) `pr-reviewer`'s H-1 (uncommitted `sys.path` fix) was resolved on disk at
`4627c634` per `ac-validator`'s verification, but `pr-reviewer` itself has not
re-run to confirm — recommend re-dispatching `pr-reviewer` against the current
HEAD before `pull-request` proceeds. (2) `documentation-verifier`'s blocker (the
non-pipe-delimited AC-1 line under `### documentation-expert` in `## Agent
Contracts`) is a ticket-body formatting defect untouched by this commit —
recommend respawning `documentation-expert` to reformat that line, then
re-dispatching `documentation-verifier`, before this ticket is treated as fully
closed out.

### 2026-08-26 09:15 — pull-request (status: blocker)
feedback-id: fb_2026-08-26_5ecbf404
completion_manifest:
  remote_precondition_checked: true
  pr_drafted:
    result: false
    reason: "Two upstream gate agents remain in `failed` status at the point pull-request was dispatched: pr-reviewer (H-1, 2026-08-26 07:42) and documentation-verifier (fail-closed parse blocker, 2026-08-26 07:50). Neither has been re-run since remediation, and the commit phase's own 09:10 comment explicitly recommends against proceeding: 'recommend re-dispatching pr-reviewer against the current HEAD before pull-request proceeds' and 'recommend respawning documentation-expert ... then re-dispatching documentation-verifier, before this ticket is treated as fully closed out.'"
    remediation: "Re-dispatch documentation-expert to reformat the non-pipe-delimited AC-1 line under `### documentation-expert` in `## Agent Contracts` (ticket line ~263) into `- [ ] AC-N: <genre> | <target_path> | <content_constraint>` format, then re-dispatch documentation-verifier. Separately, re-dispatch pr-reviewer against current HEAD (commit `4627c634` plus this phase's ticket-only edits) to confirm its H-1 finding is actually closed (ac-validator asserts it is resolved on disk, but pr-reviewer itself has not re-verified). Once both read signed_off, re-dispatch pull-request."
  push_performed: false
  pr_created: false
Did not draft, push, or open a PR. This ticket's own frontmatter and `## Sign-offs` show
`pr-reviewer: failed` and `documentation-verifier: failed`, both unresolved as of dispatch —
not stale artifacts from before a fix, since neither agent has re-run since the fixes each
gate's finding names. My contract's Merge-State Poll Gate can lead toward `gh pr merge` once
a PR is open, so opening one now risks progressing an unreviewed, doc-gate-failing change
past the point the reviewing agents themselves said it should stop. The immediately-preceding
`commit` phase (2026-08-26 09:10) reached the same conclusion and named this exact next step
in its own comment. Per signoff SKILL.md §4, marking `pull-request: failed` (not `signed_off`,
not silently proceeding) so the supervisor's failure-adjudication ladder routes correctly:
respawn `documentation-expert` → `documentation-verifier`, and independently re-run
`pr-reviewer`, before re-dispatching `pull-request`. No git remote issue (origin is
configured and reachable); this is a review-chain gate, not a tooling precondition failure.

## Escalation

Branch: none
Reason: 3 files in one component (build_pipeline/); no always-large trigger fired (no Alembic migration, no hypertable change, no FastAPI public-API change, no ADR file touched).

### 2026-08-26 — main loop (not a phase agent): gate reconciliation

Recorded by the operator's main loop, NOT by any phase agent. No agent sign-off is
forged here; the two status changes above are adjudications with their evidence
stated, and anyone may reverse them by pointing at better evidence.

**pr-reviewer: failed -> signed_off.** Its 07:42 H-1 asked to be re-run against
current HEAD once the uncommitted work was committed. It was re-run and returned
**PR-READY**, verifying rather than trusting: the 248 lines and 3 tests it named
are in `4627c634`, all five constructs reachable at HEAD, substance unchanged.
It also discharged the merge concern by showing the two sides of `8223808a`
touched a disjoint file set (7 branch-side, 432 main-side, empty intersection),
so no hunk could have been dropped, and cleared the 22 `build.py` deletions as a
tightening — `_manifest_ac_store_scripts` now derives from the deploy map it
actually deploys from, instead of over-claiming five files that were never
deployed. It carried three non-blocking mediums; see below.

**documentation-verifier: failed -> not_needed.** It was dispatched outside its
own conditional. Two independent checks:

- The agent is documented as conditional on `requires_documentation_verification`
  being non-null in ticket frontmatter. That field is **absent** from this
  ticket — `grep` returns nothing.
- `BP-900g.yaml:23-24` carries `documentation_triggers: []` and the explicit
  rationale *"Internal build-time deploy-integrity guardrail — surfaces only as a
  build error; no new user-facing capability or command is introduced."* The AC
  deliberately requires no documentation.

Its own blocker was therefore correct twice over: the Agent Contracts AC-1 line
really was unparseable (zero `|` characters), and it fail-closed rather than
guessing. Repairing that line, which I did in `a7a7f09f`, converts a parse
blocker into an absent-doc blocker for `BP-900g.yaml` — a file the diff does not
and should not contain. Re-running it as-is would fail again, on a document the
AC says must not exist. The honest resolution is that the contract line is a
generator fallback artifact rather than a requirement, so the gate does not apply
to this ticket. It is NOT that a doc is owed.

**Carried forward, not blocking, filed separately:**

1. `_is_syspath_mutation_call` handles `.insert` and `.append` but not
   `sys.path.extend` or slice assignment; both return an empty closure with zero
   log output at any level — the same silent shape `4627c634` fixed, re-opened
   for siblings. No live instance exists (grepped `scripts/` and `templates/`),
   so it is a regression vector rather than a present defect.
2. `4627c634`'s message claims "no route remains ... produces no output", which
   item 1 contradicts. Recorded rather than force-pushed; downstream should not
   rely on that sentence.
3. `generate_ticket_from_ac.py` emits an unparseable Agent Contracts line when the
   parent AC has no `documentation_triggers`. It warns at generation time and
   emits it anyway, producing a ticket that a fail-closed gate cannot process.
