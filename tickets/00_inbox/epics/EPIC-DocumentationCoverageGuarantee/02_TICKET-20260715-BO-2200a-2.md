---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  pr-reviewer: failed
  pull-request: needed
  python-coder: signed_off
  sql-coder: not_needed
  test-runner: needed
  test-writer: signed_off
complexity: low
components:
- build_orchestration
change_target: code
created: '2026-07-15'
depends_on:
- 01_TICKET-20260715-BO-2200a-1.md
risk_surface: internal
files_touched:
- scripts/ac_store/generate_ticket_from_ac.py
priority: medium
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: BO-2200a-2
status: todo
title: A boundary, safety, auth, or privacy risk surface also demands documentation
---

# A boundary, safety, auth, or privacy risk surface also demands documentation

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BO-2200a-2` — A boundary, safety, auth, or privacy risk surface also demands documentation — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BO-2200a-2`. Component: `build-orchestration`. Assigned agent: `python-coder`. Estimated complexity: `S`. Complexity: `low`.

## Acceptance Criteria

```gherkin
Given the declarative `documentation_gates` policy and the ticket generator building
  the agents map for a leaf AC whose `change_target` is `code`,
When the AC's `risk_surface` is `contract_boundary`,
Then the generated agents map includes `documentation-expert`.
And the same result holds when `risk_surface` is `safety`, `auth`, or `privacy`
  (each of these values, on its own, causes `documentation-expert` to be required
  even when the `change_target` alone would not have triggered it).
And the risk-surface trigger is evaluated independently of the change-target trigger,
  so a documentation demand is raised if EITHER dimension matches its triggering set.
```

## Test Requirements

```yaml
tests:
- name: test_risk_surface_contract_boundary_triggers_doc_expert
  file: unit_tests/test_bo_2200a_2.py
  covers:
  - BO-2200a-2
  asserts: change_target=code, risk_surface=contract_boundary yields documentation-expert
    in the generated agents map.
  framework: unittest
  type: behavioral
- name: test_each_triggering_risk_surface_requires_doc_expert
  file: unit_tests/test_bo_2200a_2.py
  covers:
  - BO-2200a-2
  asserts: risk_surface in {safety, auth, privacy} each independently triggers documentation-expert
    even when change_target=code would not.
  framework: unittest
  type: behavioral
- name: test_change_target_and_risk_surface_are_ORed
  file: unit_tests/test_bo_2200a_2.py
  covers:
  - BO-2200a-2
  asserts: A non-triggering change_target combined with a triggering risk_surface
    still yields documentation-expert (independent OR evaluation).
  framework: unittest
  type: behavioral
```

## Implementation Notes

```yaml
config_schema_fragment:
  risk_surface_triggers:
    items:
      enum:
      - contract_boundary
      - safety
      - auth
      - privacy
      type: string
    type: array
constraints:
- Extend the documentation_gates policy evaluation in _build_agents_map so the triggering
  risk_surface set {contract_boundary, safety, auth, privacy} raises the documentation
  demand independently of change_target.
- 'OR semantics between the two dimensions: documentation-expert is required if EITHER
  change_target OR risk_surface matches its triggering set.'
n_location_rule: '1'
post_write_commands: []
reference_file_path: scripts/ac_store/generate_ticket_from_ac.py
required_skills:
- python-coder
```

## Sign-offs

- [x] python-coder — 2026-07-17 16:45
- [x] test-writer — 2026-07-17 15:00
- [ ] test-runner
- [ ] pr-reviewer — failed 2026-07-17 17:00
- [ ] commit
- [ ] pull-request

## Comments

### 2026-07-17 15:00 — test-writer (status: ok)
feedback-id: fb_2026-07-17_c3c83c4e
completion_manifest:
  tests_written: true
  tests_red: true
  covers_ac: true
red_baseline:
  - test_name: test_risk_surface_contract_boundary_triggers_doc_expert
    file: unit_tests/test_bo_2200a_2.py
    error: "AssertionError: 'needed' != None : Expected documentation-expert='needed' when risk_surface='contract_boundary' is in documentation_gates.risk_surface_triggers."
  - test_name: test_each_triggering_risk_surface_requires_doc_expert
    file: unit_tests/test_bo_2200a_2.py
    error: "AssertionError: 'needed' != None : Expected documentation-expert='needed' when risk_surface='safety' is in documentation_gates.risk_surface_triggers."
  - test_name: test_change_target_and_risk_surface_are_ORed
    file: unit_tests/test_bo_2200a_2.py
    error: "AssertionError: 'needed' != None : Expected documentation-expert='needed' when change_target='code' (non-triggering) + risk_surface='auth' (triggering via risk_surface_triggers)."
Written 3 red unittest stubs in unit_tests/test_bo_2200a_2.py covering BO-2200a-2. All 3 fail with AssertionError (exit 1) because _build_agents_map does not yet read documentation_gates.risk_surface_triggers. Minimal temp config with no flow_change_gates isolates the risk_surface path under test.

### 2026-07-17 17:00 — pr-reviewer (status: blocker)
feedback-id: fb_2026-07-17_ea373f21
completion_manifest:
  bo_2200a_2_tests_green: true
  implementation_correct: true
  ruff_clean: true
  test_suite_clean:
    result: false
    reason: "unit_tests/test_generate_ticket_from_ac.py::TestFlowChangeSequencing::test_ac4_documentation_expert_before_coder_for_flow_change_pair fails RED (exit 1). The test asserts documentation-expert appears BEFORE python-coder for flow-change pairs, but HEAD commit 49e1af78 (BO-2200d-1) changed ordering to post-coder unconditionally without updating this test."
    remediation: "BO-2200d-1 ticket owner must either (a) update the failing test to assert post-coder ordering, (b) delete it if the old ordering contract is dropped, or (c) add a # covers: BO-2200d-1 annotation so the AC enforcement plugin masks it while BO-2200d-1 is pending."

The BO-2200a-2 implementation is correct: `risk_surface_triggers` is read from `documentation_gates` config, OR semantics are implemented, all 3 dedicated tests are green, and ruff is clean. However, the test suite exits with code 1 due to a failing test not caused by BO-2200a-2 but bundled in the same commit (BO-2200d-1 changed ordering to post-coder but did not update `test_ac4_documentation_expert_before_coder_for_flow_change_pair`). Recommend resolving that test under BO-2200d-1 before merge. Medium findings also noted: `_FLOW_CHANGE_PHASE_ORDER` is now dead code (module-level constant, not caught by ruff), the guardrail_gates.yaml comment still says risk_surface_triggers is "not yet read by the generator" (stale after BO-2200a-2), and BO-2200a-2/BO-2200d-1/BO-2200b-3 were bundled in one commit.

### 2026-07-17 16:45 — python-coder (status: ok)
feedback-id: fb_2026-07-17_3ce29e6e
completion_manifest:
  risk_surface_triggers_read_path: true
  or_semantics_implemented: true
  ruff_clean: true
  all_three_red_baseline_tests_green: true
red_baseline_results:
  - test_name: test_risk_surface_contract_boundary_triggers_doc_expert
    result: green
  - test_name: test_each_triggering_risk_surface_requires_doc_expert
    result: green
  - test_name: test_change_target_and_risk_surface_are_ORed
    result: green
Added `risk_surface_triggers` read-path to `_build_agents_map` in `scripts/ac_store/generate_ticket_from_ac.py`. After the existing `change_target_triggers` check (BO-2200a-1), a new block reads `documentation_gates.risk_surface_triggers` from config and adds `documentation-expert` to `guardrail_set` when `risk_surface` intersects the trigger set. OR semantics: documentation-expert is required if EITHER dimension matches. The trigger set `{contract_boundary, safety, auth, privacy}` was already present in `config/guardrail_gates.yaml` — no config change required. Also cleaned up the now-unused `is_flow_change_pair` variable (ruff F841) as a side effect of the BO-2200d-1 staged work that changed `phase_order` to `_CANONICAL_PHASE_ORDER`. All 81 tests in `test_generate_ticket_from_ac.py` pass except `test_ac4_documentation_expert_before_coder_for_flow_change_pair`, which was already failing before this ticket due to the pre-existing BO-2200d-1 staged YAML change (documentation-expert removed from `flow_change_gates.mandatory_agents`).

