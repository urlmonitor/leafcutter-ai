---
ac_traceability:
  id: BO-1000a-2
  path: docs/acceptance-criteria/build-orchestration/BO-1000-live-automation-progress/BO-1000a-2.yaml
advances_current_outcome: true
agents:
  commit: needed
  pr-reviewer: needed
  pull-request: needed
  python-coder: needed
  test-runner: needed
  test-writer: needed
change_target: pipeline
complexity: low
components:
- build_orchestration
created: '2026-07-20'
depends_on:
- BO-1000a
files_touched:
- templates/workflows-js/finalize-feature.js
priority: medium
requires_adr: false
requires_diagram: false
risk_surface: internal
roadmap_phase: phase_1
source_ac: BO-1000a-2
status: todo
title: The step count N in the announcements is stable and correct across the run
---

# The step count N in the announcements is stable and correct across the run

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BO-1000a-2` — The step count N in the announcements is stable and correct across the run — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BO-1000a-2`. Component: `build-orchestration`. Assigned agent: `python-coder`. Estimated complexity: `S`. Complexity: `low`.

## Acceptance Criteria

```gherkin
Given the finalize-feature workflow announces "Step X of N" as each step
  begins,
When the start-of-step progress lines from a single run are collected,
Then the value of N is identical in every start-of-step line within that run,
And N equals the number of steps in the finalize sequence that the workflow
  declares it will run,
And the position X advances monotonically as the run progresses, with no
  duplicated or skipped position number for the steps that actually execute.
```

- [ ] AC-1: the value of N is identical in every start-of-step line within that run
- [ ] AC-2: N equals the number of steps in the finalize sequence that the workflow
- [ ] AC-3: the position X advances monotonically as the run progresses, with no

## Test Requirements

```yaml
tests:
- name: test_n_identical_across_all_start_of_step_lines
  file: unit_tests/workflows/test_bo_1000a_2.py
  covers:
  - BO-1000a-2
  asserts: Collecting the start-of-step lines from a single run, the N value in the
    'Step X of N' framing is identical in every line.
  framework: unittest
  type: behavioral
- name: test_n_equals_declared_step_count
  file: unit_tests/workflows/test_bo_1000a_2.py
  covers:
  - BO-1000a-2
  asserts: The announced N equals the number of steps in the finalize sequence the
    workflow declares it will run.
  framework: unittest
  type: behavioral
- name: test_position_x_advances_monotonically_without_gaps_or_duplicates
  file: unit_tests/workflows/test_bo_1000a_2.py
  covers:
  - BO-1000a-2
  asserts: Position X advances monotonically across the executed steps, with no duplicated
    and no skipped position number.
  framework: unittest
  type: behavioral
```

## Implementation Notes

```yaml
config_schema_fragment: null
n_location_rule: '1'
notes: "Policy constraints:\n- N must be derived from a single source of truth for\
  \ the step sequence\n  (one declared list/count), never hard-coded per line, so\
  \ every\n  start-of-step line in a run reports an identical N and N cannot drift\
  \ from\n  the number of steps the workflow actually declares.\n- X must advance\
  \ monotonically with no duplicate or skipped position for the\n  steps that execute\
  \ (skips are still counted and reported — see BO-1000a-3);\n  derive X from the\
  \ same ordered sequence, not an independent counter.\n- Shares the narration helper\
  \ introduced in BO-1000a-1.\n- No new config key. ADR-001 build.py round-trip parity.\n"
post_write_commands:
- python scripts/build.py --target-dir .
reference_file_path: templates/workflows-js/finalize-feature.js
required_skills:
- python-coder
```

## Sign-offs

- [ ] test-writer
- [ ] python-coder
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit
- [ ] pull-request

## Comments
