---
ac_traceability:
  id: ACD-2100c-3-i
  path: docs/acceptance-criteria/ac-driven-dev/ACD-2100-entry-point-unblocked/ACD-2100c-3-i.yaml
advances_current_outcome: true
ac_coverage: 4/4
agents:
  ac-fulfillment-gate: signed_off
  ac-validator: signed_off
  architect-review: signed_off
  commit: signed_off
  documentation-expert: not_needed
  documentation-verifier: not_needed
  pr-reviewer: signed_off
  pull-request: not_needed
  python-coder: signed_off
  test-runner: signed_off
  test-writer: signed_off
change_target: pipeline
complexity: low
components:
- ac_driven_dev
created: '2026-08-26'
depends_on:
- tickets/00_inbox/epics/EPIC-StartingNewWorkTheProperWayAlways/15_TICKET-20260826-ACD-2100c-3.md
documentation_required: false
files_touched:
- templates/workflows-js/plan-feature.js
priority: medium
requires_adr: false
requires_diagram: false
risk_surface: safety
roadmap_phase: phase_1
source_ac: ACD-2100c-3-i
status: done
title: An answer naming a different decision than the one being waited on is not applied
---

# An answer naming a different decision than the one being waited on is not applied

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `ACD-2100c-3-i` — An answer naming a different decision than the one being waited on is not applied — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `ACD-2100c-3-i`. Component: `ac-driven-dev`. Assigned agent: `python-coder`. Estimated complexity: `S`. Complexity: `low`.

## Acceptance Criteria

```gherkin
Given a run paused at one decision point with drafted work on disk,
When the route is started again supplying an answer that names a different
  decision point,
Then the answer is not applied to the decision being waited on,
And the run remains paused with its record still on disk,
And it reports which decision it is waiting on,
And the drafted work is unchanged and nothing is discarded.
```

- [x] AC-1: the answer is not applied to the decision being waited on
- [x] AC-2: the run remains paused with its record still on disk
- [x] AC-3: it reports which decision it is waiting on
- [x] AC-4: the drafted work is unchanged and nothing is discarded.

## Test Requirements

```yaml
tests:
- name: test_answer_naming_a_different_decision_is_not_applied
  file: unit_tests/workflows/test_acd_2100c_3_i.py
  covers:
  - ACD-2100c-3-i
  asserts: A run paused at one decision point; a resume supplying a well-formed answer
    that names a different decision point takes neither branch of the waiting decision
    -- no step after it executes.
  framework: unittest
  type: integration
  angle: criterion
- name: test_run_remains_paused_with_its_record_intact_after_a_mismatch
  file: unit_tests/workflows/test_acd_2100c_3_i.py
  covers:
  - ACD-2100c-3-i
  asserts: After the mismatched resume the record is still present on disk and still
    names the original decision point, and a fresh process listing waiting runs still
    lists it.
  framework: unittest
  type: integration
  angle: failure
- name: test_mismatch_report_names_the_decision_actually_being_waited_on
  file: unit_tests/workflows/test_acd_2100c_3_i.py
  covers:
  - ACD-2100c-3-i
  asserts: The mismatched resume reports the decision point the run is waiting on,
    not the one the supplied answer named.
  framework: unittest
  type: integration
  angle: criterion
- name: test_mismatched_resume_discards_nothing
  file: unit_tests/workflows/test_acd_2100c_3_i.py
  covers:
  - ACD-2100c-3-i
  asserts: The drafted artifacts are byte-identical before and after the mismatched
    resume -- including when the supplied answer names a decision whose choices include
    discarding.
  framework: unittest
  type: integration
  angle: real_artifact
- name: test_acd_2100c_3_i_reachable_from_entry_point
  file: unit_tests/workflows/test_acd_2100c_3_i.py
  covers:
  - ACD-2100c-3-i
  angle: reachability
  asserts: 'REQUIRED — invoke the production entry point (CLI, hook, slash command,
    workflow dispatch, or main()) as a subprocess/dispatch and assert the new behaviour
    actually occurs. Do NOT satisfy this by importing the function directly. The AC
    authored no test_spec, so the entry point is not declared: resolve it before writing
    this test, and do not delete this entry.'
```

## Implementation Notes

```yaml
- The change must land under templates/. The deployed workflow is build output.
- A mismatch must leave the run paused rather than fail it. The person is still owed
  the question; a failure that clears the record makes the recovery from a mistyped
  resume a full re-run.
- The record must be unchanged on disk after the mismatch -- not rewritten, not re-timestamped
  in a way that loses which decision is outstanding.
- The report must name the decision the run is actually waiting on, so the operator
  can correct the answer without inspecting the store by hand.
- The subject binding must be checked before any part of the answer is acted on. Checking
  after the branch has been taken is not a check.
- Must satisfy the repository error-handling policy.
- 'Must be idempotent: repeated mismatched resumes leave the run in the same waiting
  state each time.'
```

## Agent Contracts

### documentation-expert

Existing docs to update / cross-link:

- docs/how-to/resume-a-paused-plan-feature-run.md (relationship: describes | status: planned | relevance: The how-to authored by ACD-2100e-2 must tell an operator what a mistyped resume looks like and that the run survives it.)
- docs/architecture/adrs/ADR-024-interactive-pause-resume.md (relationship: describes | status: exists | relevance: The re-entry path an out-of-band answer arrives on.)
- templates/workflows-js/plan-feature.js (relationship: implements | status: exists | relevance: Canonical source. The re-entry path that binds a supplied answer to the gate the run is paused at.)
- docs/architecture/components/interactive-pause-resume-substrate.md (relationship: describes | status: exists | relevance: Component doc for the record that must survive the mismatch.)

This record owns none of the documents above -- every entry is a cross-link to a
document another record produces. The contract line that stood here demanded
`docs/how-to/resume-a-paused-plan-feature-run.md`, which ACD-2100e-2 creates;
this ticket could never have satisfied it. Removed per BO-2200c-5-i and -iii;
the list above is retained as reading for the implementer.

### Expects From

- **AC:** ACD-2100c-3
- **Contract:** A resume path in which the supplied answer and the decision point the run is paused at are separate bindings, both available before the answer is acted on.

## Sign-offs

- [x] architect-review — 2026-09-09 15:30
- [x] test-writer — 2026-09-09 16:10
- [x] python-coder — 2026-09-09 16:05
- [x] test-runner — 2026-09-09 16:20
- [x] pr-reviewer — 2026-09-09 18:20
- [x] ac-validator — 2026-09-09 18:35
- [x] ac-fulfillment-gate — 2026-09-09 18:44
- [x] commit — 2026-09-09 19:05

## Comments

### 2026-09-09 15:30 — architect-review (status: ok)
feedback-id: fb_2026-09-09_5056a9da
completion_manifest:
  blast_radius_analyzed: true
  rubric_classification_recorded: true
  design_concern_documented: true
Small classification (1 file: `templates/workflows-js/plan-feature.js`; 1 component:
`ac_driven_dev`; no always-large trigger — no alembic, no hypertable, no public API,
and the change implements ADR-024's existing contract rather than altering it). Read
the pipeline loop and `resolveGate()`/`applyAnswerByType()` directly: the bug this
ticket must fix is that `skipAuthorOnResume` (around `templates/workflows-js/plan-feature.js`
line ~3015) is derived from a naive string comparison of `args.resume_answer.gate_id`
to the current step's `stepGateId` BEFORE `resolveGate()` ever consults the persisted
pause record — so on a mismatched resume the authoring agent for the currently-paused
step is still re-dispatched (line ~3033) ahead of the point where `resolveGate()` would
correctly detect the mismatch and re-pause. That violates the Implementation Notes'
"subject binding must be checked before any part of the answer is acted on" requirement
and is the most likely source of AC-4 (drafted work unchanged) failing if left as-is.
No ADR/diagram is warranted; recommend `python-coder` restructure so the "is this the
gate we're actually paused at" determination happens via the durable record (or by
deferring to `resolveGate()`) before entering the authoring dispatch branch.

### 2026-09-09 16:10 — test-writer (status: ok)
feedback-id: fb_2026-09-09_f7753174
completion_manifest:
  tests_written: true
  red_baseline_confirmed: true
  cross_layer_seam_answer:
    result: covered
    producing_side: "plan-feature.js's real, on-disk pause_store.py write dispatch (resolveGate()/pauseAtGate(), executed via a real child_process against a real .leafcutter/scripts/pause_store.py copy)"
    consuming_side: "an independent, freshly-started `python3 pause_store.py read` subprocess (test_run_remains_paused_with_its_record_intact_after_a_mismatch's _read_via_fresh_process), never plan-feature.js again"
  reachability_entry_point_answer:
    result: resolved
    entry_point: "real Node subprocess executing the actual on-disk templates/workflows-js/plan-feature.js top-level body (no CLI wrapper exists for this E2 workflow script; identical resolution to sibling ticket ACD-2100c-3's test_acd_2100c_3.py, carried through verbatim per Step 0 of the Reachability Entry-Point Resolution procedure)"
red_baseline:
  - test_name: test_answer_naming_a_different_decision_is_not_applied
    file: unit_tests/workflows/test_acd_2100c_3_i.py
    error: "AssertionError: AC-1: a mismatched resume must not cause the step whose decision is being waited on to be re-executed. ... Labels from the mismatched-resume process: [..., 'stage-itpo-author', ...] -- assert 1 == 0"
  - test_name: test_run_remains_paused_with_its_record_intact_after_a_mismatch
    file: unit_tests/workflows/test_acd_2100c_3_i.py
    error: "AssertionError: AC-2 read together with the Implementation Notes ... an intact record is not enough on its own if the mismatch still caused the authoring step to re-run. Labels: [..., 'stage-itpo-author', ...] -- assert 1 == 0"
  - test_name: test_mismatch_report_names_the_decision_actually_being_waited_on
    file: unit_tests/workflows/test_acd_2100c_3_i.py
    error: "AssertionError: AC-3: reporting the correct decision point is undermined if the mismatch already caused the authoring step to re-run. Labels: [..., 'stage-itpo-author', ...] -- assert 1 == 0"
  - test_name: test_mismatched_resume_discards_nothing
    file: unit_tests/workflows/test_acd_2100c_3_i.py
    error: "AssertionError: AC-4: the drafted artifact present when the run paused must be byte-identical after a mismatched resume ... before=b'drafted-at:1788961805504:0.3986497224232879\\n' after=b'drafted-at:1788961805670:0.2742540968442897\\n'"
  - test_name: test_acd_2100c_3_i_reachable_from_entry_point
    file: unit_tests/workflows/test_acd_2100c_3_i.py
    error: "AssertionError: The mismatched resume, driven through the real production entry point, must not re-execute the step before the decision it is waiting on. Labels: [..., 'stage-itpo-author', ...] -- assert 1 == 0"
Wrote unit_tests/workflows/test_acd_2100c_3_i.py (5 tests: criterion x2, failure x1,
real_artifact x1, reachability x1), following test_acd_2100c_3.py's self-contained
real-execution-harness convention (real git repo + real worktree + real,
verbatim scripts/pause_store.py; every pause_store.py-embedding dispatch actually
executed; every other dispatch stubbed).

Investigated whether the persisted pause record itself (the file at
`.leafcutter/paused_runs/<run_id>.json`) could serve as the AC-4 "real artifact":
it cannot — `write_record()` in scripts/pause_store.py is idempotent keyed on
`gate_id` alone, so a re-write for the SAME gate_id is a byte-for-byte no-op on
disk regardless of what `context` the caller passed, meaning the record file
cannot observe the redispatch bug either way. Instead, the harness's
"stage-itpo-author" dispatch was extended (self-contained to this test file
only) to actually write a fresh-nonce file to disk on every invocation,
simulating a real (non-deterministic) authoring LLM's output — this makes the
redispatch bug directly observable as changed bytes on disk, and is the
mechanism behind the AC-1/AC-3/AC-4/reachability tests' RED status below.

Verified all 5 new tests are genuinely RED, not merely xfail-masked: this repo's
pytest_ac_enforcement plugin downgrades AC-tagged failures to `xfail` when the
AC's `work_status != done` (per project memory: pytest xfail-masking). A plain
`pytest` run showed `5 xfailed`; re-running with `AC_ENFORCE_STRICT=1` surfaced
all 5 as real `AssertionError`s (not `ImportError`/`SyntaxError`/collection
errors), each failing exactly where predicted: the mismatched resume
re-dispatches `stage-itpo-author` (labels2.count == 1, not 0), which is the
direct AC-1/AC-2/AC-3/reachability failure mode, and produces different nonce
bytes on disk for AC-4's real-artifact test. `test_run_remains_paused_...`
(angle: failure) and `test_mismatch_report_names_...` (angle: criterion) were
strengthened past their originally-passing form (record presence / gate_id
correctness alone) by folding in the same dispatch-count assertion, since those
alone would have passed immediately against current code. No entry needed in
`red_baseline` with a "passes immediately" note — every test in this file
failed on the strict run.
No ADR store change; no architecture doc change (this ticket is `documentation_required: false`).

### 2026-09-09 16:05 — python-coder (status: ok)
feedback-id: fb_2026-09-09_c0afde20
completion_manifest:
  red_baseline_all_green: true
  no_new_regressions: true
  error_handling_policy_followed: true
  file_size_limit_respected: true
Fixed `templates/workflows-js/plan-feature.js` per architect-review's finding:
`skipAuthorOnResume` (was ~line 3015) previously derived `isResumingThisStep`
from a naive per-step comparison of `args.resume_answer.gate_id` to this
step's own `stepGateId`, so a MISMATCHED resume (answer names a different
gate) left `isResumingThisStep` false and let the authoring dispatch for the
step actually being waited on run unconditionally, before `resolveGate()`
ever got a chance to check the subject binding against the durable record.
Replaced the per-step match with `isResuming = !!(args && args.resume_answer)`
— since every pipeline stage before the current one is already skipped via
`committedStageKeys`, the presence of ANY resume_answer by the time control
reaches this (first uncommitted) step means this step's own gate is
necessarily the one the run could be paused at, regardless of what gate_id
the answer names. `skipAuthorOnResume` is now `isResuming && _resumeAction
!== "edit"`, where `_resumeAction` is still derived ONLY from a genuinely
matching answer (so a mismatched "edit" is never read for its action/choice
and is treated as skip, exactly like any other non-matching answer). The
mismatch itself is still correctly detected and re-reported by the existing,
untouched `resolveGate()` — this change only stops the authoring dispatch
from running ahead of that check.

red_baseline_results:
  - test_name: test_answer_naming_a_different_decision_is_not_applied
    result: green
  - test_name: test_run_remains_paused_with_its_record_intact_after_a_mismatch
    result: green
  - test_name: test_mismatch_report_names_the_decision_actually_being_waited_on
    result: green
  - test_name: test_mismatched_resume_discards_nothing
    result: green
  - test_name: test_acd_2100c_3_i_reachable_from_entry_point
    result: green

Verified via `AC_ENFORCE_STRICT=1 python3 -m pytest unit_tests/workflows/test_acd_2100c_3_i.py -q`:
5 passed (previously 5 failed against the red baseline test-writer recorded).
Regression check: re-ran `test_acd_2100c_1.py`, `test_acd_2100c_2.py`,
`test_acd_2100c_3.py` (14 passed, 15 subtests passed) and the full
`unit_tests/workflows/` + `unit_tests/portability/test_acd_2100d_{1,3}.py`
suite (620 items) before and after the change (via `git stash`/`stash pop`):
22 failures are byte-identical pre-existing baseline failures unrelated to
this change (worktree-agent permission-registry and startup-check tests
under `test_acd_2100a_{1,3,4,5}.py`, `test_acd_2100b_4.py`, `test_bo_1500f_1*.py`
— all about workspace-setup permission plumbing, none touching the
resume/gate loop this ticket edits); no new failures introduced. `node --check`
passes on the edited file. No `.py`/`.sql` files touched — only
`templates/workflows-js/plan-feature.js` (a 23-line diff; well under the
400-line new-file limit and not a new file), so `doc-enforcer` and
`complexity-reduction` (Python-only skills) are not applicable. No I/O added
(pure control-flow condition change), so the repository error-handling
policy is unaffected. Flipped this ticket's four `## Acceptance Criteria`
checkboxes to `[x]` (AC-1 through AC-4), matching sibling ticket
`15_TICKET-20260826-ACD-2100c-3.md`'s convention (no `### python-coder`
heading exists under `## Agent Contracts` on this ticket, so signoff skill
§2c is skipped per its own "no such heading" carve-out).

### 2026-09-09 16:20 — test-runner (status: ok)
feedback-id: fb_2026-09-09_1e6b0fa4
completion_manifest:
  test_suite_executed: true
  all_tests_passing: true
  failure_report_structured: true
Ran `unit_tests/workflows/test_acd_2100c_3_i.py` (the file `test-writer` added
for this ticket) with `AC_ENFORCE_STRICT=1 python3 -m pytest`: all 5 tests
pass (`test_answer_naming_a_different_decision_is_not_applied`,
`test_run_remains_paused_with_its_record_intact_after_a_mismatch`,
`test_mismatch_report_names_the_decision_actually_being_waited_on`,
`test_mismatched_resume_discards_nothing`,
`test_acd_2100c_3_i_reachable_from_entry_point`) — this repo's routing table
covers `live_trader`/`sql_functions`/`trading_model` paths, none of which
match this repo's `templates/workflows-js/` + `unit_tests/workflows/` layout,
so I routed to the ticket's own target test file directly instead of guessing
a mismatched suite. Also ran the full `unit_tests/workflows/` directory
(629 items: 607 passed, 22 failed) and independently confirmed via
`git stash push -u -- templates/workflows-js/plan-feature.js
unit_tests/workflows/test_acd_2100c_3_i.py` / `stash pop` that the same 22
failures (worktree-agent permission-registry + startup-check tests under
`test_acd_2100a_{1,3,4,5}.py`, `test_acd_2100b_4.py`, `test_bo_1500f_1*.py`)
are byte-identical with this ticket's diff removed — pre-existing baseline
noise, not a regression from this change. No failures attributable to this
ticket's diff.

### 2026-09-09 16:27 — pr-reviewer (status: blocker)
feedback-id: fb_2026-09-09_89c77aab
completion_manifest:
  review_performed: true
  high_confidence_blocker_found:
    result: false
    reason: "A high-confidence regression was found in the fix itself (see [H-1] below); this is the blocker being reported, not a failure of the review process."
    remediation: "n/a — see comment body for the actual remediation to respawn python-coder with."
[H-1] templates/workflows-js/plan-feature.js:3031-3032 — `isResuming` is
      computed as `!!(args && args.resume_answer)`, i.e. true for EVERY step
      still remaining in the `for (const step of pipeline)` loop for the rest
      of this process invocation, not just for the one step the run actually
      paused at. The architect-review comment's own diagnosis ("this step's
      own gate is necessarily the one decision point the run could actually
      be paused at, regardless of what gate_id the supplied answer names")
      is true ONLY for the FIRST uncommitted step reached in a resumed
      invocation — it is false for every step after that, because on the
      "strategic" (po→ba→itpo), "behavioral" (ba→itpo), and
      ptFlowProduced-forced (ba→itpo) routes, a successful mid-pipeline
      resume (e.g. a resume_answer naming "gate-ba") lets the `for` loop
      continue in the SAME process to the next pipeline step ("itpo") after
      committing "ba" (see the "approve" branch at ~line 3200-3236, which
      sets `approved = true` and lets the outer `for` loop advance — no
      `return` in between). At that point `args.resume_answer` is still the
      (now-resolved) answer for "gate-ba"; `stepGateId` for "itpo" is
      "final-gate", so `_resumeAnswerForThisStep`/`_resumeAction` are
      correctly null for this step — but `isResuming` is STILL true (it does
      not check gate_id at all), so `skipAuthorOnResume` evaluates true and
      the itpo authoring dispatch (`stage-itpo-author`) is wrongly skipped
      (`written = []`) even though itpo has never been authored or paused in
      this run. Before this ticket's change, the per-step comparison
      (`isResumingThisStep = !!_resumeAnswerForThisStep`) correctly evaluated
      false for itpo in this scenario, so authoring ran normally — this is a
      regression this ticket's fix introduces, not a pre-existing bug.
      Effect in production: any resume of a paused strategic/behavioral run
      at its non-final ("gate-ba") gate silently skips real IT-PO authoring
      for the next stage and presents an empty/near-empty gate to the user.
      Not caught by this ticket's own tests (which, per the test file's own
      module docstring, only exercise the single-stage "technical" route —
      "every test below reaches and pauses at [`final-gate`]") nor by sibling
      ticket ACD-2100c-3's `test_h2_duplicate_ac_ids_when_multistage_route_resumes_at_final_gate`
      (its "ba" stage is pre-committed via the crash-resume/`committedStageKeys`
      path from a PRIOR session, not resumed live via this loop in the SAME
      invocation, so it never reaches this code path with two live,
      uncommitted steps in one invocation).
      Suggested fix direction: `isResuming` must be scoped to "the specific
      step whose gate this invocation is/was actually paused at" (e.g. only
      true for the first non-committed step processed in this invocation,
      or derive it from whether `resolveGate()`/the durable record names
      THIS step's gate — matching architect-review's original guidance to
      defer to the durable record — rather than "any resume_answer object is
      present in `args`, unconditionally, for the rest of the loop").
      Sub-skill: pr-reviewer (direct code read; `pr-review-toolkit:review-pr`
      sub-skill dispatch was not available in this invocation environment,
      so this review was performed by direct reading of
      `templates/workflows-js/plan-feature.js` lines 2850-3320 and the
      existing `unit_tests/workflows/test_acd_2100c_3.py` /
      `test_acd_2100c_3_i.py` test suites).

Recommend respawning `python-coder` with this finding: re-scope `isResuming`
so it applies only to the one step actually being resumed (not to every
subsequent step for the rest of the invocation), then have `test-writer`/
`test-runner` add a regression test that resumes a "behavioral" or
"strategic" route AT ITS NON-FINAL GATE (e.g. `gate-ba`) within a single
process invocation and asserts the following step's own authoring dispatch
(`stage-itpo-author`) IS still invoked exactly once. No medium-confidence
findings met the >3 escalation threshold, so Opus was not invoked. Escalation
branch: none — this is a single high-confidence finding, not a cluster.

### 2026-09-09 16:41 — ac-validator (status: blocker)
feedback-id: (submit-failed)
completion_manifest:
  ac_coverage_checked: true
  all_acs_covered:
    result: false
    reason: "AC coverage itself is complete, but pr-reviewer's H-1 finding on the same diff is unaddressed."
    remediation: "Do not proceed to commit until python-coder is respawned per pr-reviewer's recommendation and the ticket's pr-reviewer checkbox is re-signed."
AC coverage for all 4 declared ACs is fully evidenced and independently
verified — not a coverage gap:
- Implementation: `templates/workflows-js/plan-feature.js` lines 3004-3033
  (unstaged working-tree diff against `e85204c37`) — the `isResuming =
  !!(args && args.resume_answer)` / `skipAuthorOnResume` rewrite plus the
  unchanged `resolveGate()` subject-binding check together implement AC-1
  (mismatch not applied), AC-2 (`resolveGate()` re-pauses, record intact),
  AC-3 (`resolveGate()` reports the true waiting gate), and AC-4 (authoring
  dispatch skipped so drafted work is untouched).
- Tests: `unit_tests/workflows/test_acd_2100c_3_i.py`, all 5 tests present
  and named exactly per the ticket's Test Requirements
  (`test_answer_naming_a_different_decision_is_not_applied`,
  `test_run_remains_paused_with_its_record_intact_after_a_mismatch`,
  `test_mismatch_report_names_the_decision_actually_being_waited_on`,
  `test_mismatched_resume_discards_nothing`,
  `test_acd_2100c_3_i_reachable_from_entry_point`). Independently re-ran
  `AC_ENFORCE_STRICT=1 python3 -m pytest unit_tests/workflows/test_acd_2100c_3_i.py -q`
  myself: 5 passed. Also re-ran the full `unit_tests/workflows/` directory
  (629 items): 22 failed / 607 passed — the same pre-existing baseline
  failures test-runner already reported (worktree-agent permission-registry
  + startup-check tests), confirming no new regression is currently
  test-visible.
- Store alignment: `scripts/commit_guardian/check_v2_ac_store_alignment.py
  --ticket <this ticket>` exited 0, no violations.
- `## AC Coverage` table: none exists in this ticket body to fill (§2c.3's
  table-fill step is a no-op here — nothing to update).

I am declining to sign off `ok` regardless, because `pr-reviewer`'s
2026-09-09 16:27 finding ([H-1], `templates/workflows-js/plan-feature.js:3031-3032`)
is a real, unaddressed high-confidence regression in the exact lines this
ticket's coverage evidence rests on: I re-read the live diff and confirmed
`isResuming = !!(args && args.resume_answer)` is still present unchanged —
the fix pr-reviewer flagged as unsafe for the "behavioral"/"strategic"
multi-stage resume routes has not been applied, its sign-off checkbox is
still unchecked (`- [ ] pr-reviewer — failed`), and no test in this
ticket's own suite (single-stage route only, per the test file's own
module docstring) or in the sibling suite exercises the multi-stage case
that would catch it — my own full-directory run above confirms it is not
currently causing any red test. This is not an AC-coverage gap for THIS
ticket's 4 ACs; it is a correctness regression pr-reviewer already
identified and recommended fixing via a `python-coder` respawn. Signing
`ac-validator` off `ok` here would let a known-broken diff pass the final
gate ahead of commit. Suggested remediation: respawn `python-coder` with
pr-reviewer's [H-1] finding, then `test-writer`/`test-runner` add the
non-final-gate multi-stage regression test pr-reviewer specified, then
re-run `pr-reviewer` and `ac-validator` in sequence.

Process note (file, not fix, per this agent's remit): `submit_feedback.py`
rejected `ac-validator` for both the `quality-concern` and `blocker`
categories — `ac-validator` is entirely absent from every category's
`allowed_writers` list in `.leafcutter/config/feedback_categories.yaml` /
`templates/config/feedback_categories.yaml`, the same gap previously found
and fixed for `user-surface-smoker` and `documentation-verifier`. Feedback
recorded as `(submit-failed)` per the signoff skill's fallback; this gap
should be filed as its own fix.

### 2026-09-09 16:50 — ac-fulfillment-gate (status: blocker)
feedback-id: (submit-failed)
completion_manifest:
  ac_store_fields_verified: true
  implemented_by_autofix_applied: true
  covered_by_autofix_applied: true
  work_status_autofix_applied:
    result: false
    reason: "Mechanically this AC's own 5 tests are green and files_touched ∩ diff is non-empty, so work_status: done would be auto-fix-eligible per the letter of this gate's recipe. But pr-reviewer's 16:27 [H-1] finding is an unaddressed, unfixed, high-confidence regression in the exact same unstaged diff (templates/workflows-js/plan-feature.js), and ac-validator's 16:41 comment explicitly declined to sign off ok for that reason, warning that doing so 'would let a known-broken diff pass the final gate ahead of commit.' Flipping work_status to done now -- even though this AC's own narrow scope (single-stage mismatched resume) is genuinely covered -- would misrepresent the diff backing it as safely complete while a known regression against a related route (multi-stage resume at a non-final gate) sits unfixed on the same lines. That is the phantom-done pattern this gate exists to prevent, one level removed: correct-for-its-own-AC evidence used to certify a diff that is not yet safe to ship."
    remediation: "Respawn python-coder with pr-reviewer's [H-1] finding (re-scope `isResuming` to the one step actually being resumed, not every remaining step in the loop), then test-writer/test-runner add the non-final-gate multi-stage regression test pr-reviewer specified, then re-run pr-reviewer and ac-validator. Once both re-sign off ok, re-dispatch ac-fulfillment-gate to complete the work_status auto-fix and covered_by/implemented_by will already be in place from this pass."
Verified AC coverage resolution via `ac_coverage_resolver.py`: 1 AC resolved
(ACD-2100c-3-i, level L3) from the two-key traceability block. Auto-fixed
(append-only) two fields on
`docs/acceptance-criteria/ac-driven-dev/ACD-2100-entry-point-unblocked/ACD-2100c-3-i.yaml`,
both independently true regardless of the H-1 regression's outcome:
`implemented_by` gained `templates/workflows-js/plan-feature.js` (it was
diff- and files_touched-intersecting but missing -- only the ticket path was
previously listed), and `covered_by` gained
`unit_tests/workflows/test_acd_2100c_3_i.py` (a genuine `# covers:
ACD-2100c-3-i` tag was found at 5 locations in that diff-added file via
`grep -rn` over `tests/` and `unit_tests/`, satisfying BO-202's no-level-
qualifier auto-fix criterion for this L3 AC). `check_ac_schema.py` ran
clean after the edit (config/ac_store_schema.json absent in this worktree,
so it fell back to manual field validation and exited 0). `work_status`
was deliberately left at `todo` -- see the manifest item above. This is a
narrower blocker than pr-reviewer's/ac-validator's: it is scoped to one
field on one AC YAML file, not to the code fix itself, and resolves
automatically as soon as those two upstream gates re-sign off ok (no further
action needed from this gate at that point beyond re-running it).

### 2026-09-09 17:01 — commit (status: blocker)
feedback-id: fb_2026-09-09_255c4864
completion_manifest:
  pre_commit_hooks_pass:
    result: false
    reason: "Not attempted -- did not reach `git commit`. `pr-reviewer` (16:27) reported a high-confidence regression ([H-1]: `isResuming = !!(args && args.resume_answer)` at templates/workflows-js/plan-feature.js:3031-3032 is true for every remaining pipeline step in the invocation, not just the step actually being resumed, so a mid-pipeline resume at a non-final gate -- e.g. `gate-ba` on the strategic/behavioral routes -- silently skips the next stage's real authoring dispatch), and both `ac-validator` (16:41) and `ac-fulfillment-gate` (16:50) explicitly declined to sign off `ok` and explicitly recommended NOT proceeding to commit until python-coder is respawned with that finding, a regression test is added, and pr-reviewer/ac-validator re-run."
    remediation: "Respawn python-coder with pr-reviewer's [H-1] finding (re-scope `isResuming` to only the one step actually being resumed, not every remaining step in the loop); have test-writer/test-runner add the non-final-gate multi-stage regression test pr-reviewer specified (resume a 'behavioral' or 'strategic' route at `gate-ba` within one process invocation and assert the following stage's authoring dispatch still runs exactly once); re-run pr-reviewer and ac-validator; re-dispatch commit only after both re-sign off ok."
  commit_message_valid:
    result: false
    reason: "No commit message was drafted since no commit was attempted."
    remediation: "n/a until the upstream blocker above is resolved."
  ticket_staged:
    result: false
    reason: "Working tree changes (templates/workflows-js/plan-feature.js, the AC YAML, the new test file, and this ticket) remain unstaged/uncommitted by design -- committing a diff three upstream gates have refused to approve would land the known [H-1] regression."
    remediation: "Stage and commit once python-coder's respawned fix + the new regression test are in place and pr-reviewer/ac-validator re-sign off ok."
Declined to run `git commit` for this ticket. Three upstream sign-offs on
record (`pr-reviewer`: failed/blocker, `ac-validator`: failed/blocker,
`ac-fulfillment-gate`: failed/blocker) all point at the same unresolved
regression in the working-tree diff of `templates/workflows-js/plan-feature.js`
and all explicitly instruct holding the commit until it is fixed and
re-reviewed; committing now would ship a known-broken diff, which is exactly
the phantom-done failure mode the commit-agent gate exists to prevent.
Recommended remediation: respawn `python-coder` with pr-reviewer's [H-1]
finding, add the multi-stage non-final-gate regression test pr-reviewer
specified, then re-run `pr-reviewer` and `ac-validator` before re-dispatching
`commit`.

### 2026-09-09 18:06 — python-coder (status: ok)
feedback-id: fb_2026-09-09_04e664e9
completion_manifest:
  h1_regression_fixed: true
  red_baseline_all_green: true
  no_new_regressions: true
  test_h1_still_green: true
  error_handling_policy_followed: true
Fixed pr-reviewer's [H-1] finding. Root cause: `isResuming` (renamed here to
`isPausedAtThisStep`) was being derived either from a naive per-step
`gate_id` string comparison against the SUPPLIED `resume_answer` (the
original ACD-2100c-3-i defect this ticket targets), or — in the intervening
attempt pr-reviewer flagged — from `!!(args && args.resume_answer)` alone
(true for every remaining step in the loop, the H-1 regression). Both
versions decide "should I skip authoring?" by inspecting the SUPPLIED
answer, never the actual persisted pause state, so neither can tell apart
"a later step in the same invocation, never paused, no answer for its own
gate" (must author) from "the step actually being waited on, receiving an
answer for a DIFFERENT decision" (must not author).

Fix: added `peekPausedGateId(runId)` (~line 1496), a read-only helper that
consults the durable pause record directly (same `pause_store.py read`
contract `resolveGate()` already uses) and returns the gate_id the run is
GENUINELY paused at right now, or `null`. The per-step decision
(~line 3050) now computes `isPausedAtThisStep` from that real, on-disk
state — not from the supplied answer's own `gate_id` — before
`skipAuthorOnResume` is derived. The three states are now distinguished
structurally:
1. Resumed at THIS step's gate, answer matches: `isPausedAtThisStep` is
   true from the peek, `_resumeAction !== "edit"` → skip authoring (original
   ACD-2100c-3 behaviour, unchanged).
2. A later/fresh step in the same invocation: the peek finds no persisted
   record naming THIS step's gate (either none exists, or an earlier
   stage's record was already cleared by `resolveGate()`'s own clear-on-move
   -past logic) → `isPausedAtThisStep` is false → author normally. This is
   the H-1 fix: the peek is anchored to durable state, not to whether SOME
   `resume_answer` object happens to be present in `args`.
3. Mismatch (answer names a different decision than the one being waited
   on): the peek finds the record's `gate_id` DOES equal this step's gate
   (the run is genuinely paused here) regardless of what the supplied
   answer names → `isPausedAtThisStep` is true → skip authoring. The
   mismatch is then independently (and correctly, unchanged) re-detected by
   `resolveGate()`, which falls through to `pauseAtGate()` and re-reports
   the real waiting gate — but now nothing has been acted on before that
   detection, per the Implementation Notes' ordering requirement.
`peekPausedGateId()` is fail-closed toward "not paused here" (any absent,
stale, or unreadable record resolves to `null`) so a transient read failure
cannot silently re-suppress a genuinely fresh step's authoring dispatch —
the same failure mode as H-1 by a different path.

Verified: `unit_tests/workflows/test_acd_2100c_3_i.py` — all 5 of this
ticket's tests (previously RED per test-writer's baseline) now pass:
`AC_ENFORCE_STRICT=1 python3 -m pytest unit_tests/workflows/test_acd_2100c_3_i.py -q`
→ 5 passed. `unit_tests/workflows/test_acd_2100c_3.py` — still 7/7,
including `test_h1_itpo_author_skipped_when_resume_answer_targets_an_earlier_gate`
(the H-1 regression test test-writer had already added to this file) and
`test_drafted_work_carried_forward_is_the_work_that_was_present_at_pause`.
Full `unit_tests/workflows/` directory:
`AC_ENFORCE_STRICT=1 python3 -m pytest unit_tests/workflows/ -q` → 22
failed / 608 passed (630 items) — the 22 failures are byte-identical to the
pre-existing baseline test-runner/ac-validator already documented
(worktree-agent permission-registry + startup-check tests under
`test_acd_2100a_{1,3,4,5}.py`, `test_acd_2100b_4.py`, `test_bo_1500f_1*.py`
— confirmed by name match against their prior comments), landing exactly at
the documented 22-failure baseline with zero new regressions. `node --check
templates/workflows-js/plan-feature.js` passes. No `.py`/`.sql` files
touched — only `templates/workflows-js/plan-feature.js` — so
`doc-enforcer`/`complexity-reduction` (Python-only skills) do not apply;
the new `peekPausedGateId()` helper carries a full JSDoc header consistent
with this file's other helpers (`resolveGate`, `pauseAtGate`). No new I/O
primitive introduced (the peek reuses the existing `agent()`/
`buildPauseStoreCommand()` dispatch pattern every neighbouring function in
this file already uses), so the repository error-handling policy is
unaffected. Recommend re-running `pr-reviewer` and `ac-validator` next.

### 2026-09-09 18:20 — pr-reviewer (status: ok)
feedback-id: fb_2026-09-09_106606f2
completion_manifest:
  review_performed: true
  high_confidence_blocker_found:
    result: false
    reason: "No high-confidence blocker remains. python-coder's 18:06 fix (peekPausedGateId reading the durable pause record directly) resolves the 16:27 [H-1] finding correctly."
red_baseline_verified: true
Re-reviewed the working diff of `templates/workflows-js/plan-feature.js`
(65 lines) and `unit_tests/workflows/test_acd_2100c_3.py` (+145 lines,
including the new `test_h1_itpo_author_skipped_when_resume_answer_targets_an_earlier_gate`
regression test) against my own 16:27 [H-1] finding. Read the full diff
directly (`pr-review-toolkit:review-pr` dispatch is unavailable in this
invocation environment, same as the prior pass): `peekPausedGateId()`
(new, ~line 1496) consults the durable on-disk pause record via the same
`pause_store.py read` contract `resolveGate()` already uses, fail-closed
toward "not paused here" on any absent/stale/unreadable record. The
per-step decision (~line 3050) now derives `isPausedAtThisStep` from that
real state, not from whether ANY `resume_answer` object is present in
`args` (the H-1 regression) nor from the supplied answer's own `gate_id`
alone (the original ACD-2100c-3-i defect) — this correctly distinguishes
all three cases: (1) genuinely paused here + matching answer → skip
authoring (unchanged ACD-2100c-3 behaviour); (2) a later/fresh step in the
same invocation, never paused (its gate's record already cleared by
`resolveGate()`'s own clear-on-match logic before the loop reaches this
step) → author normally, fixing H-1; (3) genuinely paused here + mismatched
answer → skip authoring, satisfying this ticket's AC-1/AC-4.

Independently ran, from a fresh shell: `AC_ENFORCE_STRICT=1 python3 -m
pytest unit_tests/workflows/test_acd_2100c_3.py
unit_tests/workflows/test_acd_2100c_3_i.py -q` → 12 passed (includes the
H-1 regression test). Independently confirmed the pre-existing 22-failure
baseline in the full `unit_tests/workflows/` directory is unchanged by
`git stash push -u -- templates/workflows-js/plan-feature.js
unit_tests/workflows/test_acd_2100c_3.py unit_tests/workflows/test_acd_2100c_3_i.py`
and re-running a subset of the 22 (`test_acd_2100a_1.py`,
`test_bo_1500f_1.py`): the same 7 of those 22 fail identically with this
ticket's diff removed — pre-existing, unrelated (worktree-agent
permission-registry / startup-check plumbing), then `stash pop` restored
the diff cleanly.

One non-blocking observation for the record, not raised as a finding: when
a resume's supplied answer names THIS step's gate but the durable record
happens to be flagged `stale` (a state orthogonal to this ticket's mismatch
scope), `peekPausedGateId()` returns `null` (fail-closed), so
`isPausedAtThisStep` is `false` and authoring would run rather than skip —
whereas `resolveGate()`'s own matching-answer branch independently handles
staleness via `unresumable_stale`. This edge case is not covered by any of
this ticket's 4 ACs or Implementation Notes and existed in essentially the
same shape before this ticket (staleness has always been a distinct
concept from the mismatch this AC targets); flagging only so a future
staleness-focused AC has this cross-reference. No test regression traces
to it.

AC-1 through AC-4 all verified green via the ticket's own 5 tests plus the
sibling suite's H-1 regression test. Recommend proceeding to
`ac-validator` next.

### 2026-09-09 18:35 — ac-validator (status: ok)
feedback-id: (submit-failed)
completion_manifest:
  ac_coverage_checked: true
  all_acs_covered: true
  h1_regression_resolution_reviewed: true
Re-running this gate after the 16:41 decline: pr-reviewer's [H-1] finding is
now fixed and independently re-confirmed (pr-reviewer 18:20, status: ok), so
the reason I withheld sign-off no longer applies. Re-verified from scratch
rather than trusting the upstream sign-offs alone:

- Implementation: `git diff -- templates/workflows-js/plan-feature.js`
  (unstaged, base `e85204c37`) shows the new `peekPausedGateId()` helper
  (~line 1496) reading the durable pause record directly via the same
  `pause_store.py read` contract `resolveGate()` uses, fail-closed to `null`
  on any absent/stale/unreadable record; and the per-step decision at
  ~line 3050-3073 now derives `isPausedAtThisStep` from that real, on-disk
  state (`_actualPausedGateId === stepGateId`) rather than from whether ANY
  `resume_answer` is present in `args` (the H-1 regression) or from the
  supplied answer's own `gate_id` alone (the original AC-1..AC-4 defect).
  `skipAuthorOnResume = isPausedAtThisStep && _resumeAction !== "edit"`.
  This implements AC-1 (mismatch not applied — the peek is independent of
  what the answer names), AC-2/AC-3 (unchanged `resolveGate()` still
  re-pauses and reports the true waiting gate), and AC-4 (authoring
  dispatch correctly skipped only when genuinely paused at this step, so
  drafted work is untouched, while a later/fresh step in the same
  invocation is NOT wrongly skipped).
- Tests: independently re-ran (not reusing prior agents' output)
  `AC_ENFORCE_STRICT=1 python3 -m pytest
  unit_tests/workflows/test_acd_2100c_3_i.py
  unit_tests/workflows/test_acd_2100c_3.py -q` myself: 12 passed, including
  all 5 of this ticket's own tests
  (`test_answer_naming_a_different_decision_is_not_applied`,
  `test_run_remains_paused_with_its_record_intact_after_a_mismatch`,
  `test_mismatch_report_names_the_decision_actually_being_waited_on`,
  `test_mismatched_resume_discards_nothing`,
  `test_acd_2100c_3_i_reachable_from_entry_point`, all in
  `unit_tests/workflows/test_acd_2100c_3_i.py`) plus the sibling suite's
  new H-1 regression test in `test_acd_2100c_3.py`. Also independently
  re-ran the full `unit_tests/workflows/` directory (630 items):
  22 failed / 608 passed — same file/test names as the previously
  documented pre-existing baseline (`test_acd_2100a_{1,3,4,5}.py`,
  `test_acd_2100b_4.py`, `test_bo_1500f_1*.py`), confirming no new
  regression.
- Store alignment: re-ran
  `scripts/commit_guardian/check_v2_ac_store_alignment.py --ticket <this
  ticket>` myself: exit 0, no violations.
- Cross-file/AC contract: this AC's `expects_from` names ACD-2100c-3's
  contract ("the supplied answer and the decision point the run is paused
  at are separate bindings, both available before the answer is acted
  on"). `peekPausedGateId()` reads the durable pause-store record as one
  binding, independently of `args.resume_answer` (the second binding),
  and only combines them (`_actualPausedGateId === stepGateId` +
  `_resumeAction`) after both are available — satisfying the contract.
  `resolveGate()`, the function that implements ACD-2100c-3 itself, is
  unchanged by this diff.
- `## AC Coverage` table: still does not exist in this ticket body — no
  columns to fill (unchanged from the 16:41 finding).
- `ac_coverage: 4/4` in frontmatter already correct; all 4
  `## Acceptance Criteria` checkboxes already `[x]` from python-coder's
  16:05 sign-off — no further edits needed there.

All 4 ACs covered, both implementation and test evidence found for each,
and the cross-AC contract with ACD-2100c-3 is satisfied. Commit phase may
proceed.

Process note (unchanged from the 16:41 finding, re-confirmed, filing not
repeated): `submit_feedback.py` still rejects `ac-validator` for the
`complete` category — `ac-validator` remains absent from every category's
`allowed_writers` in `feedback_categories.yaml`. Feedback recorded as
`(submit-failed)` per the signoff skill's fallback.

### 2026-09-09 18:44 — ac-fulfillment-gate (status: ok)
feedback-id: (submit-failed)
completion_manifest:
  ac_store_fields_verified: true
  implemented_by_autofix_applied: false
  covered_by_autofix_applied: false
  work_status_autofix_applied: true
Re-ran this gate after the 16:50 decline. That decline was scoped narrowly to
`work_status` only — `implemented_by` and `covered_by` were already correctly
auto-fixed (append-only) in that same pass and needed no further change here;
re-verified both are still intact and unmodified. The condition that blocked
`work_status` no longer holds: `pr-reviewer` re-reviewed the fixed diff and
signed off `ok` at 18:20 (the [H-1] regression is resolved via the new
`peekPausedGateId()` durable-record read), and `ac-validator` independently
re-verified from scratch and signed off `ok` at 18:35. Re-ran
`ac_coverage_resolver.py`: 1 AC resolved (`ACD-2100c-3-i`, level L3, two-key
traceability block). Confirmed `files_touched` (`templates/workflows-js/plan-feature.js`)
intersects both `git diff main...HEAD --name-only` (committed history) and
`git diff HEAD --name-only` (uncommitted working-tree diff, which is where
this ticket's actual edit lives). Auto-fixed `work_status: todo` → `done` on
`docs/acceptance-criteria/ac-driven-dev/ACD-2100-entry-point-unblocked/ACD-2100c-3-i.yaml`
(append-only in spirit — a scalar field flip from a non-`done` value to
`done`, no list entries touched). Ran `check_ac_schema.py` after the edit:
exit 0 (`config/ac_store_schema.json` absent in this worktree, so it fell
back to manual field validation, which also passed). Re-ran the resolver a
second time post-edit to confirm: `"ok": true`, `"failures": []`, `"message":
"All 1 AC(s) verified. work_status, implemented_by, and covered_by fields are
accurate."` No `## Agent Contracts` heading named `ac-fulfillment-gate` exists
on this ticket, so signoff skill §2c.2/§2c.3 are skipped per their own
"no such heading" carve-out (same as noted by `python-coder` and
`ac-validator` for their own passes). Commit phase may proceed.

Process note (same gap as `ac-validator`'s, filed there already, not
re-filed): `submit_feedback.py` rejects `ac-fulfillment-gate` for the
`complete` category — it is entirely absent from every category's
`allowed_writers` in `feedback_categories.yaml`. Feedback recorded as
`(submit-failed)` per the signoff skill's fallback.

### 2026-09-09 19:05 — commit (status: ok)
feedback-id: fb_2026-09-09_4141bbd3
completion_manifest:
  pre_commit_hooks_pass: true
  commit_message_valid: true
  ticket_staged: true
Auto-authorized commit gate: subject "fix(ac-driven-dev): check paused gate
before acting on resume answer"; staged files:
`docs/acceptance-criteria/ac-driven-dev/ACD-2100-entry-point-unblocked/ACD-2100c-3-i.yaml`,
`templates/workflows-js/plan-feature.js`,
`tickets/00_inbox/epics/EPIC-StartingNewWorkTheProperWayAlways/16_TICKET-20260826-ACD-2100c-3-i.md`,
`unit_tests/workflows/test_acd_2100c_3.py`,
`unit_tests/workflows/test_acd_2100c_3_i.py`. The prior 17:01 `blocker`
sign-off on this same gate held for the blocker it was raised against
(pr-reviewer/ac-validator/ac-fulfillment-gate all `blocker` on the
unresolved [H-1] regression); all three have since re-signed `ok` (18:20,
18:35, 18:44) after python-coder's 18:06 fix, so that condition no longer
applies. Verified the probe (`scripts/commit_guardian/verify_precommit_active.py
--json`, run with cwd set to this worktree via `env --chdir`) reports
`failing_checks: []` before proceeding — the first attempt from the wrong
cwd falsely reported `git_hook: false`, resolved by running from the
worktree root. Unlike `ac-validator`/`ac-fulfillment-gate` above, `commit`
IS present in `feedback_categories.yaml`'s `complete` category
`allowed_writers`, so `submit_feedback.py` succeeded normally here.
