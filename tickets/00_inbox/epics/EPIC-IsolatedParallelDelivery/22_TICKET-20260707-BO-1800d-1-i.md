---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  pr-reviewer: needed
  pull-request: needed
  python-coder: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
components:
- build_orchestration
created: '2026-07-07'
depends_on:
- BO-1800d-1
files_touched:
- docs/architecture/adrs/ADR-018-agent-isolation-topology.md
priority: medium
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: BO-1800d-1-i
status: todo
title: No background auto-gc fires mid-drive even after the loose-object threshold
  is passed
---

# No background auto-gc fires mid-drive even after the loose-object threshold is passed

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BO-1800d-1-i` — No background auto-gc fires mid-drive even after the loose-object threshold is passed — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BO-1800d-1-i`. Component: `build-orchestration`. Assigned agent: `python-coder`. Estimated complexity: `S`.

## Acceptance Criteria

```gherkin
Given a drive clone with `gc.auto` set to 0 and a drive that produces enough loose
  objects to exceed git's normal auto-gc threshold (~6700 loose objects),
When the drive keeps committing past that threshold,
Then no automatic (background/detached) gc or repack runs against the clone during
  the drive,
And the drive's commits continue without a concurrent repack touching its object
  store.
```

## Sign-offs

- [ ] python-coder
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit
- [ ] pull-request

## Comments
