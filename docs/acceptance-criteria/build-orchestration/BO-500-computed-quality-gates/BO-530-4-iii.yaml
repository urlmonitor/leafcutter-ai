id: BO-530-4-iii
title: "A locally-passing render or ruff check records auditable evidence in the sign-off"
component: build-orchestration
level: L3
status: active
req_status: active
work_status: todo
readiness: draft
priority: medium
created_by: docs/acceptance-criteria/build-orchestration/BO-500-computed-quality-gates/BO-530-4.yaml
criteria: |
  Given the dev-render smoke check (BO-530-4) or the python ruff check
    (BO-530-5) ran locally and passed for a ticket,
  When the agent records its sign-off,
  Then the sign-off captures evidence that the check actually ran and what it
    found, including:
    - which check ran (dev-render smoke check or repo-ruff check),
    - the surface or file set it was run against (the changed frontend surface,
      or the changed Python files),
    - the outcome (passed),
  And an auditor reading the sign-off afterwards can tell that the check was
    genuinely executed, not merely asserted,
  And a sign-off that claims a passed check WITHOUT recording this evidence is
    not acceptable for these two gates.

  Given a check was skipped (no applicable files) or inconclusive
    (tooling unavailable),
  When the sign-off is recorded,
  Then the evidence distinguishes skipped and inconclusive from passed,
  And the recorded reason makes the non-pass outcome auditable.
depends_on: [BO-530-4]
doc_links:
  - path: templates/skills/signoff/SKILL.md
    relationship: describes
    status: exists
  - path: templates/agents/frontend-coder.md
    relationship: describes
    status: exists
  - path: templates/agents/python-coder.md
    relationship: describes
    status: exists
assigned_agent: llm-expert
estimated_complexity: S
it_requirements:
  - "Both new gates (BO-530-4 dev-render, BO-530-5 ruff) must record machine- and human-readable evidence in the sign-off when they pass, so the gate is auditable and a passing claim can be distinguished from an unrun check."
  - "Evidence must encode the outcome state (passed / skipped / inconclusive) and the surface or file set checked, so audit can separate 'ran clean' from 'nothing to check' from 'could not check'."
delivers_to: null
expects_from:
  ac_id: BO-530-4
  contract: "The dev-render and ruff sign-off gates whose passing outcomes this AC requires to be evidenced and audited"
origin_agent: business-analyst-v3
created: 2026-06-24
amended_by: []
superseded_by: null
covered_by: []
implemented_by: []
notes: |
  Evidence requirement spans BOTH new gates. Filed as a single L3 under BO-530-4
  (rather than duplicated under BO-530-4 and BO-530-5) to avoid duplicate
  coverage; criteria reference both BO-530-4 and BO-530-5 explicitly.
