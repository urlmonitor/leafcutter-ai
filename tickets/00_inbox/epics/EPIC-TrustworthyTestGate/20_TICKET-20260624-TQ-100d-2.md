---
advances_current_outcome: true
agents:
  architecture-diagram-author: needed
  commit: needed
  documentation-expert: not_needed
  pr-reviewer: needed
  pull-request: not_needed
  sql-coder: not_needed
  test-runner: not_needed
  test-writer: not_needed
components:
- testing_quality
created: '2026-06-24'
depends_on:
- TQ-100d-1
files_touched:
- docs/architecture/diagrams/tq100d-allowlist-lifecycle.md
priority: high
requires_adr: false
requires_diagram: true
roadmap_phase: phase_1
source_ac: TQ-100d-2
status: todo
title: State diagram of an allowlist entry's added-tracked-flagged lifecycle
---

# State diagram of an allowlist entry's added-tracked-flagged lifecycle

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `TQ-100d-2` — State diagram of an allowlist entry's added-tracked-flagged lifecycle — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `TQ-100d-2`. Component: `testing-quality`. Assigned agent: `architecture-diagram-author`. Estimated complexity: `S`.

## Acceptance Criteria

```gherkin
Given the allowlist behavior defined by TQ-100d-1 (valid entry exempts) and its
  edge cases (expired, now-passing, missing required fields),
When a state diagram is authored for an allowlist entry's lifecycle,
Then the diagram shows the states an entry can occupy (at least: tracked/valid,
  expired, now-passing, and rejected/invalid)
And it shows the transitions and triggers (expiry date passing, the named test
  starting to pass, a required field being absent)
And a maintainer can read from the diagram exactly when an entry stops
  exempting its test and gets flagged for removal.
```

## Sign-offs

- [ ] architecture-diagram-author
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit

## Comments
