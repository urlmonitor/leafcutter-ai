---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  pr-reviewer: needed
  pull-request: not_needed
  python-coder: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
components:
- build_pipeline
created: '2026-06-22'
depends_on:
- BP-900e
- BP-900e-1
files_touched:
- docs/acceptance-criteria/build_pipeline/BP-900-deployment-completeness/BP-900e.yaml
- scripts/commit_guardian/commit_guardian.json
- docs/acceptance-criteria/build_pipeline/BP-1000-source-template-parity/BP-1000d-1.yaml
- docs/architecture/adrs/ADR-001-self-hosting-boundary.md
- docs/architecture/adrs/ADR-013-portable-skill-script-deployment-boundary.md
priority: high
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: BP-900e-3
status: todo
title: A source-only script that is neither registered nor referenced is never flagged
---

# A source-only script that is neither registered nor referenced is never flagged

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BP-900e-3` — A source-only script that is neither registered nor referenced is never flagged — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BP-900e-3`. Component: `build-pipeline`. Assigned agent: `python-coder`. Estimated complexity: `S`.

## Acceptance Criteria

```gherkin
Given the source tree contains package-development-only scripts such as
  scripts/build.py and scripts/epic_lock.py
And neither script is registered in the commit-guardian hook registry
And neither script is referenced by any deployable agent or skill template
And neither script has a copy under templates/scripts/
When the registry-completeness check runs at the finalize merge gate
Then neither scripts/build.py nor scripts/epic_lock.py appears in the
  promised-but-undeployed list
And the check does not fail the merge gate on account of these source-only
  scripts
And the only scripts ever evaluated by this check are those that are
  registered in the hook registry OR referenced by a deployable template —
  a script that is promised by neither source is out of scope and is never
  flagged
```

## Sign-offs

- [ ] python-coder
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit

## Comments
