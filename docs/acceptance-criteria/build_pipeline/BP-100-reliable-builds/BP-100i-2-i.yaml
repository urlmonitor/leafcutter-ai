id: BP-100i-2-i
components:
  - build_pipeline
readiness: approved
priority: high
title: "Disabled hooks in the legacy manifest still require canonical presence"
component: build-pipeline
level: L3
status: active
req_status: active
work_status: todo
created_by: "docs/acceptance-criteria/build_pipeline/BP-100-reliable-builds/BP-100i.yaml"
criteria: |
  Given the legacy template commit_guardian.json registers a hook:
    - id: "check-future-feature"
    - enabled: false
  And the canonical template commit_guardian.json does NOT contain
    an entry with id "check-future-feature"
  When the parity check runs at pre-commit time
  Then the commit is blocked with exit code 1
  And the error message names "check-future-feature" as a hook present
    in the legacy manifest (with enabled: false) but absent from the
    canonical manifest
  And the error makes clear that disabled hooks still require parity
    because build.py reads the canonical manifest regardless of enabled state
depends_on:
  - BP-100i-2
doc_links:
  - path: "docs/acceptance-criteria/build_pipeline/BP-100-reliable-builds/BP-100i-2.yaml"
    relationship: parent
    status: exists
  - path: "docs/build-drift-hook.md"
    relationship: describes
    status: exists
  - path: "docs/pre-commit-hooks.md"
    relationship: describes
    status: exists
  - path: "docs/how-to/managing-pre-commit-hooks.md"
    relationship: describes
    status: exists
  - path: "docs/build-pipeline.md"
    relationship: describes
    status: exists
assigned_agent: python-coder
estimated_complexity: S
it_requirements:
  - "Must extract hook IDs from the hooks_manifest.hooks[] array regardless of the enabled field value -- both enabled:true and enabled:false entries must be included in the comparison set"
  - "Error messages for disabled-hook violations must include the enabled state so the developer understands why a disabled hook still requires parity"
delivers_to: null
expects_from:
  ac_id: BP-100i-2
  contract: "The manifest-comparison function that extracts hook IDs from hooks_manifest.hooks[] in each commit_guardian.json, treating all entries identically regardless of their enabled field value"
origin_agent: business-analyst-v3
created: 2026-06-05
amended_by:
  - it-po-v3
superseded_by: null
covered_by: []
implemented_by: []
change_target: code
risk_surface: internal
