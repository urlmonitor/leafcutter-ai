id: PER-100a-3
components:
  - persona_management
title: "Persona schema validation rejects malformed persona files"
component: persona-management
level: L2
status: active
req_status: active
work_status: todo
readiness: approved
priority: medium
criteria: |
  Given a persona.yaml file that is missing the required "identity" section
  When the persona validation script is run against the file
  Then it exits with a non-zero status
  And the error message names the missing section: "identity"
  And the error message includes the file path that failed validation

  Given a persona.yaml file where the "goals" section is present but empty (null value)
  When the persona validation script is run against the file
  Then it exits with a non-zero status
  And the error message states that "goals" must be a non-empty list
depends_on:
  - PER-100a
  - PER-100a-1
doc_links:
  - path: docs/reference/ac-schema.md
    relationship: describes
    status: exists
assigned_agent: python-coder
estimated_complexity: S
it_requirements:
  - "Must handle errors gracefully per the project error handling policy"
  - "Validation errors must be structured: include file path, field name, and violation description"
  - "Must return distinct exit codes for schema violations vs YAML parse failures"
  - "Must complete validation of a single file in <100ms"
delivers_to: null
expects_from:
  ac_id: PER-100a-1
  contract: "Persona YAML JSON Schema at config/persona_schema.json that lists required sections"
origin_agent: business-analyst-v3
created: 2026-06-08
amended_by: []
superseded_by: null
covered_by:
  - PER-100a-3-i
implemented_by: []
change_target: code
risk_surface: internal
