---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  frontend-coder: needed
  pr-reviewer: needed
  pull-request: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
complexity: low
components:
- build_orchestration
created: '2026-07-10'
depends_on:
- BO-2100a-3
files_touched:
- templates/workflows-js/build-ticket.js
priority: medium
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: BO-2100a-3-i
status: todo
title: Relative phase order 11.5 < 11.8 < 12 is preserved after insertion
---

# Relative phase order 11.5 < 11.8 < 12 is preserved after insertion

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BO-2100a-3-i` — Relative phase order 11.5 < 11.8 < 12 is preserved after insertion — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BO-2100a-3-i`. Component: `build-orchestration`. Assigned agent: `frontend-coder`. Estimated complexity: `S`. Complexity: `low`.

## Acceptance Criteria

```gherkin
Given the `phaseOrder` array in templates/workflows-js/build-ticket.js after
  `live-surface-tester` has been inserted
When the index of each of `user-surface-smoker`, `live-surface-tester`, and
  `commit` is read from that array
Then the index of `user-surface-smoker` is strictly less than the index of
  `live-surface-tester`
  And the index of `live-surface-tester` is strictly less than the index of
  `commit`
  And no pre-existing entry's relative order is changed by the insertion.
```

## Test Requirements

```yaml
tests:
- name: test_bo_2100a_3_i_the_index_of_user_surface_smoker_is_strictly
  file: unit_tests/test_bo_2100a_3_i.py
  covers:
  - BO-2100a-3-i
  asserts: the index of `user-surface-smoker` is strictly less than the index of
```

## Implementation Notes

```yaml
- Ordering assertions must be relative (index comparisons), not tied to the absolute
  numeric priority value, so a future insertion between phases does not break the
  check.
- The check must confirm no pre-existing entry moved relative to its neighbours as
  a side effect of the insertion.
```

## Sign-offs

- [ ] frontend-coder
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit
- [ ] pull-request

## Comments
