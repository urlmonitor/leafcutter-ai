---
ac_traceability:
  id: BO-1000c-2-i
  path: docs/acceptance-criteria/build-orchestration/BO-1000-live-automation-progress/BO-1000c-2-i.yaml
advances_current_outcome: true
agents:
  commit: needed
  llm-expert: needed
  pr-reviewer: needed
  pull-request: needed
change_target: pipeline
complexity: low
components:
- build-orchestration
created: '2026-07-20'
depends_on:
- BO-1000c-2
files_touched:
- templates/commands/finalize-feature.md
priority: medium
requires_adr: false
requires_diagram: false
risk_surface: internal
roadmap_phase: phase_1
source_ac: BO-1000c-2-i
status: todo
title: On a mid-flight halt the last conversation line reflects the halting step,
  live
---

# On a mid-flight halt the last conversation line reflects the halting step, live

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BO-1000c-2-i` — On a mid-flight halt the last conversation line reflects the halting step, live — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BO-1000c-2-i`. Component: `build-orchestration`. Assigned agent: `llm-expert`. Estimated complexity: `S`. Complexity: `low`.

## Acceptance Criteria

```gherkin
Given the background finalize run halts partway through the sequence,
When the user reads the main conversation,
Then the most recent progress line surfaced into the conversation reflects the
  step that was underway when the run halted,
And the user learns where the run stopped from the live conversation stream,
  not only from the terminal halt result returned at the end.
```

- [ ] AC-1: the most recent progress line surfaced into the conversation reflects the
- [ ] AC-2: the user learns where the run stopped from the live conversation stream

## Implementation Notes

```yaml
- 'Surface: the /finalize-feature launch contract (templates/commands/finalize-feature.md
  + main-loop poll/relay instruction) — llm-expert, same relay surface as BO-1000c-2.'
- The relay must surface the halting step's line into the conversation from the journal
  BEFORE (or independently of) the terminal halt result, so the last conversation
  line reflects the step that was underway at the halt — the user learns where it
  stopped from the live stream, not only the returned halt payload.
- Relies on BO-1000a-1-i (the halting step's start-of-step line is emitted before
  the failure) and BO-1000c-1a (that line reaches the journal); the relay must flush
  any remaining unrelayed journal lines when it detects the run has halted, so the
  halting-step line is not dropped.
- Keep the /finalize-feature command doc and its deployed copy in parity via build.py
  (ADR-001).
```

## Agent Contracts

### Expects From

- **AC:** BO-1000c-1a
- **Contract:** The run-progress journal (from BO-1000c-1a), including the halting step's start-of-step line (guaranteed emitted before failure by BO-1000a-1-i), which the relay surfaces so the last conversation line reflects the halting step.

## Sign-offs

- [ ] pr-reviewer
- [ ] llm-expert
- [ ] commit
- [ ] pull-request

## Comments
