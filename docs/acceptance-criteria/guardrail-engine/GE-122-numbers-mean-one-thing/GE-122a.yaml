id: GE-122a
title: "Every number identifies exactly one piece of work"
component: guardrail-engine
components:
  - commit_guardian
level: L1
status: active
req_status: draft
work_status: todo
readiness: approved
priority: high
roadmap_phase: phase_1
criteria: |
  One number, one thing — never two things wearing the same number.

  Whichever kind of work you are numbering — a requirement, a ticket, a recorded
  decision, a diagram — the number you give it is yours alone, and looking it up
  always lands on a single, unambiguous piece of work. Two unrelated things can
  no longer end up sharing a number, and one piece of work can no longer exist as
  two copies of itself that disagree about where it stands.
depends_on:
  - GE-122
doc_links:
  - path: docs/architecture/components/commit-guardian.md
    relationship: related
    status: exists
delivers_to: null
expects_from: null
documentation_triggers:
  - reference-doc
origin_agent: BrainCandy
created: 2026-08-17
amended_by: []
superseded_by: null
covered_by:
  - GE-122a-1
  - GE-122a-2
  - GE-122a-3
  - GE-122a-4
implemented_by: []
change_target: code
risk_surface: contract_boundary
notes: |
  This is the invariant L1. All four numbered kinds of work belong here, because
  for THIS guarantee they are genuinely the same requirement applied four times —
  splitting them would produce four near-identical ACs. What differs per kind is
  the motive, and the BA should carry that difference into the L2 rationale:
    - requirement numbers and ticket numbers are being REPAIRED — drift exists
      today (see GE-122e for the repair itself);
    - decision-record numbers are currently healthy, so guarding them LOCKS IN a
      good state and protects it from the same erosion. Do not write them up as
      if a defect existed;
    - diagram sequence numbers have no collisions today, but a large opt-out
      population, which is GE-122b's problem, not this one's.

  TWO FAILURE SHAPES, ONE PROMISE. This L1 covers both (a) two unrelated
  artifacts claiming one number and (b) one number appearing on two files in
  different lifecycle locations that are then free to disagree about status. Both
  break the same user-facing promise — "this number identifies one thing" — so
  they belong together even though the mechanics differ.

  THE UNIT OF INSPECTION IS THE POINT. The reason none of this was ever caught is
  that today's validation opens one file at a time. A per-file check cannot, even
  in principle, observe that a second file elsewhere claims the same number. The
  L2 decomposition must change the unit of inspection to the whole collection —
  adding another per-file rule cannot satisfy this AC. The reference-doc trigger
  exists because the numbering rules for all four kinds of work are currently
  scattered or unwritten, and a rule that four namespaces must obey has to live
  somewhere a future author will find it.

  BA DECOMPOSITION NOTE (2026-08-17) — how the two failure shapes were split.
  GE-122a-1 carries the collision shape and covers the three namespaces where it
  is possible: requirement identifiers, decision numbers and diagram sequence
  numbers. GE-122a-2 carries the two-copies shape and covers the work-item
  namespace. The split is not by namespace for its own sake — it is because the
  collision shape is structurally impossible for work items, whose identifier IS
  the filename, so two different work items cannot share one in a folder. The two
  L2s together discharge this L1's four-namespace scope; neither alone does.
  GE-122a-1-i (L3) pins the whole-collection inspection scope against a
  changed-files implementation.

  BA ADDENDUM (2026-08-25) — the allocation side was missing entirely.
  Every child above is a DETECTOR. None placed an obligation on the component that
  CHOOSES a new identifier, even though this L1's own criteria are written in the
  allocation voice ("the number you give it is yours alone"). GE-122a-4 adds it:
  a minting component must enumerate every identifier already taken — at any depth
  beneath the namespace root, and counting identifiers that are cited but no longer
  declared — and must return one that is in neither set. GE-122a-4-i pins the
  condition under which a correct-looking allocator still mints a duplicate: an
  enumeration that could not be completed must yield no identifier at all.
  Prompted by two live incidents sixteen and twenty-four hours apart (KI-ACD-008
  and the GE-120 collision recorded in the component PROJECT_CONTEXT.md), both from
  a root-only scan that could not see identifiers owned by feature folders.
