---
advances_current_outcome: true
agents:
  commit: signed_off
  documentation-expert: not_needed
  pr-reviewer: needed
  pull-request: needed
  python-coder: signed_off
  sql-coder: not_needed
  test-runner: needed
  test-writer: signed_off
components:
- build_pipeline
created: '2026-06-11'
depends_on: []
files_touched:
- scripts/build.py
- scripts/build_phases.py
- docs/architecture/components/template-compiler.md
priority: critical
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: BP-900a-1
status: todo
title: build.py deploys all ac_store scripts to the consumer project
---

# build.py deploys all ac_store scripts to the consumer project

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BP-900a-1` — build.py deploys all ac_store scripts to the consumer project — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BP-900a-1`. Component: `build_pipeline`. Assigned agent: `python-coder`. Estimated complexity: `M`.

## Acceptance Criteria

```gherkin
Given build.py is invoked with --target-dir pointing to a consumer project,
When the build completes successfully,
Then the following 13 files exist under {target}/.leafcutter/scripts/ac_store/:
  ac_prioritizer.py, generate_ticket_from_ac.py, scan_ac_store.py,
  mark_ac_done.py, validate_ac_schema.py, ac_triage.py,
  create_ac_workflow.py, cross_reference_audit.py, backfill_readiness.py,
  ac_parent_id.py, scan_ac_orphans.py, fix_ac_orphans.py, __init__.py
And each deployed file is byte-identical to its source in templates/scripts/ac_store/
```

## Sign-offs

- [x] python-coder — 2026-08-17 19:20
- [x] test-writer — 2026-08-17 19:05
- [ ] test-runner
- [ ] pr-reviewer
- [x] commit — 2026-08-17 20:10
- [ ] pull-request

## Comments

### 2026-08-17 19:05 — test-writer (status: ok)
feedback-id: fb_2026-08-17_455515f8
completion_manifest:
  test_requirements_block_present: false
  ac_store_fallback_used: true
  tests_written: true
  tests_confirmed_red: true
  real_artifact_behavioral_test_written: true
red_baseline:
  - test_name: test_ac1_all_13_ac_store_files_are_deployed
    file: unit_tests/test_bp_900a_1.py
    error: "AssertionError: 7 of the 13 required ac_store scripts were NOT deployed to <tmp>/.leafcutter/scripts/ac_store: ['__init__.py', 'ac_triage.py', 'backfill_readiness.py', 'create_ac_workflow.py', 'cross_reference_audit.py', 'fix_ac_orphans.py', 'validate_ac_schema.py']."
  - test_name: test_ac1_deployed_files_are_byte_identical_to_source
    file: unit_tests/test_bp_900a_1.py
    error: "AssertionError: Deployed ac_store scripts are not byte-identical to their sources: validate_ac_schema.py, ac_triage.py, create_ac_workflow.py, cross_reference_audit.py, backfill_readiness.py, fix_ac_orphans.py, __init__.py — not deployed."
  - test_name: test_ac1_deployed_ac_store_is_idempotent_on_rebuild
    file: unit_tests/test_bp_900a_1.py
    error: "FileNotFoundError: [Errno 2] No such file or directory: '<tmp>/.leafcutter/scripts/ac_store/validate_ac_schema.py'"
This ticket's `## Test Requirements` section is absent (a code ticket with python-coder needed), so per the AC-derivation fallback I resolved `source_ac: BP-900a-1` in `docs/acceptance-criteria/build_pipeline/BP-900-deployment-completeness/BP-900a-1.yaml` and derived tests directly from its `criteria` field. Wrote `unit_tests/test_bp_900a_1.py` with three real-artifact behavioral tests (per the Real-Artifact Behavioral Test Mandate): all three run the actual `build.py --target-dir` into a fresh `tmp_path`, then read the deployed `<output_root>/scripts/ac_store/` directory back off disk — no mocking of the copy call. Confirmed red via `pytest unit_tests/test_bp_900a_1.py` (1 failed + 2 xfailed under the repo's default AC-enforcement masking, non-zero exit; with `AC_ENFORCE_STRICT=1` all 3 fail with real AssertionError/FileNotFoundError, listing the 7 of 13 required files currently missing from `build_ac_store()`'s deploy_map in scripts/build_phases.py: `validate_ac_schema.py`, `ac_triage.py`, `create_ac_workflow.py`, `cross_reference_audit.py`, `backfill_readiness.py`, `fix_ac_orphans.py`, `__init__.py`). Note for python-coder: the AC's prose names "templates/scripts/ac_store/" as the source location, but that directory is currently empty — the test instead asserts byte-identity against the already-established `scripts/ac_store/` source convention used by the existing (incomplete) `build_ac_store()` phase; flag to a human if `templates/scripts/ac_store/` was intended as the true canonical source.

### 2026-08-17 19:20 — python-coder (status: ok)
feedback-id: fb_2026-08-17_ab1ed778
completion_manifest:
  deploy_map_completed: true
  tests_green: true
  byte_identical_verified: true
  idempotency_verified: true
  build_py_change_needed:
    result: false
    reason: "scripts/build.py's manifest helpers (_manifest_ac_store_scripts, _get_source_paths_for_guard) already scan scripts/ac_store/ generically via .iterdir() with no hardcoded filenames, so they required no edit to pick up the 7 newly-deployed files."
    remediation: "None — confirmed by running the full build.py --target-dir pipeline and the ac_store-focused unit_tests/ subset, both green."
  template_compiler_doc_updated:
    result: false
    reason: "docs/architecture/components/template-compiler.md does not enumerate ac_store script names or counts (checked via grep for 'ac_store', 'seven', '13'), so its existing content already correctly describes build.py/build_phases.py as the entry points; no stale claim needed correcting."
    remediation: "None — content verified still accurate."
red_baseline_results:
  - test_name: test_ac1_all_13_ac_store_files_are_deployed
    result: green
  - test_name: test_ac1_deployed_files_are_byte_identical_to_source
    result: green
  - test_name: test_ac1_deployed_ac_store_is_idempotent_on_rebuild
    result: green
Added the 7 missing scripts (validate_ac_schema.py, ac_triage.py, create_ac_workflow.py, cross_reference_audit.py, backfill_readiness.py, fix_ac_orphans.py, __init__.py) to `build_ac_store()`'s `deploy_map` in `scripts/build_phases.py`, closing the deploy_map completeness gap per the AC yaml's SOURCE-PATH CORRECTION note (source remains `scripts/ac_store/`, not `templates/scripts/ac_store/`, which is empty). All 3 red_baseline tests in `unit_tests/test_bp_900a_1.py` are now green under `AC_ENFORCE_STRICT=1`; `ruff check scripts/build_phases.py` passes clean; a broader `-k "manifest or deploy_map or ac_store or bp_900"` unit_tests run shows 593 passed with 9 pre-existing failures/1 pre-existing error unrelated to this change (commit_guardian hook-manifest and `link_feedback` import tests that require a self-build shim step this worktree hasn't run — confirmed identical failure mode via `git stash`/`stash pop` before touching build_phases.py). No changes were needed in `scripts/build.py` or `docs/architecture/components/template-compiler.md`.

### 2026-08-17 20:10 — commit (status: ok)
feedback-id: (submit-failed)
completion_manifest:
  pre_commit_hooks_pass: true
  commit_message_valid: true
  ticket_staged: true
  depends_on_autofix:
    result: false
    reason: "Ticket's depends_on: BP-900a referenced a parent ticket file that was never created (pre-existing ticket-generator gap, unrelated to this ticket's own work), which blocked the check-doc-frontmatter pre-commit hook."
    remediation: "Corrected depends_on to [] as a mechanical fix (same class of fix already precedented in TICKET-20260721-TKT-500f-15.md); no other epic tickets sharing this defect were touched."
Auto-authorized commit gate (supervised path, ticket_path provided): committed subject "feat(build-pipeline): close ac_store deploy_map gap for BP-900a-1" covering scripts/build_phases.py, unit_tests/test_bp_900a_1.py, and this ticket file. Did not stage the 14 unrelated modified docs/acceptance-criteria/build_pipeline/BP-900-deployment-completeness/*.yaml files present in the working tree — those are outside this ticket's files_touched and appear to be concurrent AC-authoring work in the shared worktree.
