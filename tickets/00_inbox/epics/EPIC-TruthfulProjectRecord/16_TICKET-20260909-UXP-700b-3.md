---
ac_traceability:
  id: UXP-700b-3
  path: docs/acceptance-criteria/ux-prototyping/UXP-700-truthful-project-record/UXP-700b-3.yaml
advances_current_outcome: true
agents:
  ac-fulfillment-gate: needed
  ac-validator: needed
  architect-review: signed_off
  commit: needed
  documentation-expert: failed
  documentation-verifier: needed
  frontend-coder: not_needed
  pr-reviewer: needed
  pull-request: not_needed
  python-coder: signed_off
  sql-coder: not_needed
  test-runner: signed_off
  test-writer: signed_off
  user-surface-smoker: not_needed
change_target: code
complexity: low
components:
- ux_prototyping
created: '2026-09-09'
depends_on:
- 14_TICKET-20260909-UXP-700b-2.md
documentation_required: true
files_touched:
- unit_tests/product_truth/test_uxp_700b_3.py
out_of_scope:
- docs/product-truth/scripts/product_truth_outcome.py
- docs/product-truth/scripts/validate_product_truth.py
- unit_tests/product_truth/test_uxp_700d_4.py
- docs/reference/product-truth-checker-outcomes.md
- docs/architecture/adrs/ADR-042-product-truth-checker-outcome-vocabulary.md
- docs/acceptance-criteria/ux-prototyping/UXP-700-truthful-project-record/UXP-700d-4.yaml
- unit_tests/product_truth/test_uxp_700b_4.py
priority: medium
requires_adr: false
requires_diagram: false
risk_surface: contract_boundary
roadmap_phase: phase_1
source_ac: UXP-700b-3
status: done
title: An every-record rule with no records to test reports that it was not exercised
---

# An every-record rule with no records to test reports that it was not exercised

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `UXP-700b-3` — An every-record rule with no records to test reports that it was not exercised — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `UXP-700b-3`. Component: `ux-prototyping`. Assigned agent: `python-coder`. Estimated complexity: `S`. Complexity: `low`.

## Acceptance Criteria

```gherkin
Given a check phrased as a rule that must hold of every record of some kind,
And a project record in which no record of that kind exists,
When the check runs,
Then it reports that it was not exercised, naming the kind of record it needed,
And it does not report that the rule holds,
And the run's overall outcome is not the outcome for a record that was checked and found
  sound while any such check reports it was not exercised.
```

- [x] AC-1: a project record in which no record of that kind exists
- [x] AC-2: it reports that it was not exercised, naming the kind of record it needed
- [x] AC-3: it does not report that the rule holds
- [x] AC-4: the run's overall outcome is not the outcome for a record that was checked and found

## Test Requirements

```yaml
tests:
- name: test_universal_rule_on_empty_input_reports_not_exercised
  file: unit_tests/product_truth/test_uxp_700b_3.py
  covers:
  - UXP-700b-3
  asserts: A check of the form 'every X must satisfy P' run against zero X reports
    not-exercised naming X.
  framework: unittest
  type: unit
  angle: boundary
- name: test_universal_rule_on_empty_input_does_not_report_the_rule_holds
  file: unit_tests/product_truth/test_uxp_700b_3.py
  covers:
  - UXP-700b-3
  asserts: The not-exercised report is distinct from, and does not accompany, a statement
    that the rule holds.
  framework: unittest
  type: unit
  angle: criterion
- name: test_uxp_700b_3_reachable_from_entry_point
  file: unit_tests/product_truth/test_uxp_700b_3.py
  covers:
  - UXP-700b-3
  angle: reachability
  asserts: 'REQUIRED — invoke the production entry point (CLI, hook, slash command,
    workflow dispatch, or main()) as a subprocess/dispatch and assert the new behaviour
    actually occurs. Do NOT satisfy this by importing the function directly. The AC
    authored no test_spec, so the entry point is not declared: resolve it before writing
    this test, and do not delete this entry.'
- name: test_ac4_not_exercised_outcome_differs_from_holds_outcome
  file: unit_tests/product_truth/test_uxp_700b_3.py
  covers:
  - UXP-700b-3
  angle: criterion
  asserts: 'AC-4: a not-exercised run''s overall outcome must not be the outcome
    for a run that was checked and found sound (holds). Renamed 2026-09-16 (was
    test_ac4_not_exercised_exit_code_differs_from_holds_exit_code) and rewritten to
    assert on payload["outcome"] instead of CLI exit code: exit code is already
    fully and correctly allocated by two independent, already-pinned reachability
    tests (this ticket''s own not_exercised-exits-0, and the done sibling
    UXP-700b-3-i''s holds-exits-0), leaving no free exit-code value for AC-4 to
    claim without contradicting one of them. Decided with the user, 2026-09-16.'
```

## Agent Contracts

### documentation-expert

Existing docs to update / cross-link:

- docs/acceptance-criteria/ux-prototyping/UXP-500-product-truth-generation/UXP-514.yaml (relationship: context | status: exists | relevance: The live instance. Every clause of UXP-514 is of the form 'errors if any X differs', which is universally quantified and therefore vacuously satisfied on an empty store.)

- [ ] AC-1: reference-doc | docs/acceptance-criteria/ux-prototyping/UXP-500-product-truth-generation/UXP-514.yaml | it reports that it was not exercised, naming the kind of record it needed

### Delivers To

- **Agent:** python-coder
- **Contract:** The not-exercised verdict that UXP-514-5 applies to the four named drift checks.

### Expects From

- **AC:** UXP-700b-2
- **Contract:** The per-check examined figure from which not-exercised is decided.

## Sign-offs

- [x] architect-review — 2026-09-16 14:46
- [x] test-writer — 2026-09-16 15:30
- [x] python-coder — 2026-09-16 18:30
- [x] test-runner — 2026-09-16 18:32
- [ ] documentation-expert — failed 2026-09-16 15:05
- [ ] pr-reviewer
- [ ] ac-validator
- [ ] ac-fulfillment-gate
- [ ] documentation-verifier
- [ ] commit

## Comments

### 2026-09-16 14:46 — architect-review (status: ok)

feedback-id: fb_2026-09-16_fc36fe78
completion_manifest:
  blast_radius_analysis: true
  impact_classification: true
  architectural_note_written: true
Blast-radius check (done directly via Bash/Read in this environment — the Agent
tool for dispatching research-agent was not present in this session's toolset,
so file/dependency discovery was performed manually against
docs/product-truth/scripts/ and unit_tests/product_truth/) found this ticket
adds exactly one new module, docs/product-truth/scripts/universal_rule_check.py
(check_universal_rule() + a CLI entry point), plus its test file
unit_tests/product_truth/test_uxp_700b_3.py. No always-large trigger fires: no
Alembic migration, no hypertable change, no FastAPI/public-API surface touched,
and no docs/architecture/adrs/ADR-* file is modified (the ticket's own
`requires_adr: false` is confirmed correct). Threshold rubric: 2 files, 1
component (ux_prototyping / product-truth tooling), no cross-package boundary
crossed — this is a small change. Design note: the module is deliberately a
pure, standalone helper (RuleCheckResult with outcome in
{not_exercised, holds, violated}) not yet wired into validate_product_truth.py
or product_truth_outcome.py — that wiring (referenced in the ticket's
"Delivers To" contract as applying to "the four named drift checks") is
correctly left to a later ticket per the sibling test file
test_uxp_700b_3_i.py's own docstring, which already pins the same contract
shape. No layering or naming concerns: the CLI-with-main()-guard entry point
and JSON-record input mirror the project's existing generate_product_truth.py
convention (sys.path insertion pattern noted in test_uxp_700b_3_i.py). No
acceptance-criteria adjustments needed.

### 2026-09-16 15:30 — test-writer (status: ok)

feedback-id: fb_2026-09-16_11aad9c3
completion_manifest:
  tests_written: true
  red_baseline_confirmed: true
  reachability_entry_point_answer:
    result: resolved
    entry_point: "python docs/product-truth/scripts/universal_rule_check.py --records <file> --rule positive --kind <kind> (CLI via subprocess, main() guarded by if __name__ == '__main__':). Copied through from the ticket's own Test Requirements descriptor for test_uxp_700b_3_reachable_from_entry_point, which already named this resolution and the reasoning (no pre-commit hook / slash command / workflow step exists yet because UXP-700b/700b-1/700b-2/700b-3 are all still work_status: todo); matches test_uxp_700b_3_i.py's own prior resolution for this same module."
  cross_layer_seam_answer:
    result: not_applicable
    reason: "docs/product-truth/scripts/universal_rule_check.py has no consumer yet -- architect-review's own sign-off on this ticket states it is 'deliberately a pure, standalone helper ... not yet wired into validate_product_truth.py or product_truth_outcome.py.' There is no second real component on the other side of a boundary to pipe this module's output into; the reachability test already pipes the module's real CLI stdout into the test's own JSON-parsing assertions, which is the only consumer that exists at this ticket's scope."
    remediation: "n/a -- not_applicable is a first-class outcome, not a defect."

Wrote unit_tests/product_truth/test_uxp_700b_3.py: two AC-store test_spec
tests (boundary: empty population -> not_exercised, naming the kind;
criterion: not_exercised is distinct from holds), the ticket's mandatory
reachability test (real CLI subprocess), and one additional AC-4-scoped test
(contract-aware "global AC list" mapping -- see the file's AC-4 SCOPE NOTE
docstring) asserting the CLI's exit code must not conflate a not-exercised
run with a checked-and-sound (holds) run, narrowed to this ticket's own
entry point since the multi-check ADR-042 aggregation is explicitly deferred
to a later ticket per architect-review.

Investigation into why 3 of 4 tests pass immediately: the boundary/criterion/
reachability behavior this AC requires (empty population -> not_exercised,
never holds) was already built as a necessary side effect of the sibling
ticket UXP-700b-3-i (17_TICKET-20260909-UXP-700b-3-i.md, status: done) --
its population-of-one test required proving population=1 does NOT collapse
to not_exercised, which required the population=0 branch to already exist
and be correct. Verified this is genuine (not under-specified) by reading
universal_rule_check.py directly and strengthening assertions (distinct
"widget"/"gadget" kind echo-back, explicit outcome-vocabulary membership
check, real CLI JSON round-trip) rather than accepting a weak pass. The 4th
test (AC-4, exit-code differentiation) is a real gap and fails.

Verification run: `AC_ENFORCE_STRICT=1 python -m pytest
unit_tests/product_truth/test_uxp_700b_3.py -v` (this repo's
scripts/ac_store/pytest_ac_enforcement.py plugin downgrades AC-tagged
failures for not-yet-`done` ACs to xfail by default -- the required strict
flag surfaces the genuine failure; without it the run shows
"3 passed, 1 xfailed", exit 0, which is the plugin's documented TDD-red
convention, not a false green). Under AC_ENFORCE_STRICT=1: exit code 1,
"1 failed, 3 passed".

red_baseline:
  - test_name: test_ac4_not_exercised_exit_code_differs_from_holds_exit_code
    file: unit_tests/product_truth/test_uxp_700b_3.py
    error: "AssertionError: 0 == 0 : a not-exercised run and a checked-and-found-sound (holds) run must not share the same control-flow signal (exit code) -- otherwise a caller that only consumes the exit code cannot tell them apart, which is exactly the outcome AC-4 forbids (both returned 0)"

### 2026-09-16 15:45 — python-coder (status: blocker)

feedback-id: fb_2026-09-16_88328186

**What I attempted:** Read the red_baseline test
(`test_ac4_not_exercised_exit_code_differs_from_holds_exit_code`) and confirmed
it via `AC_ENFORCE_STRICT=1 python -m unittest
unit_tests/product_truth/test_uxp_700b_3.py -v` (1 failed, 3 passed, matching
test-writer's report). Before touching `universal_rule_check.py`'s exit-code
mapping to make it pass, I re-ran the sibling ticket's suite
(`python -m unittest unit_tests/product_truth/test_uxp_700b_3_i.py -v`,
UXP-700b-3-i, `agents.python-coder: signed_off`, no `## Sign-offs` box unchecked
— already `done`): 3 passed, including
`TestReachability.test_uxp_700b_3_i_reachable_from_entry_point`, which asserts
`holds_result.returncode == 0`.

**Why it is blocked:** The three exit-code assertions currently pinned across
the two test files are jointly unsatisfiable by any change to
`universal_rule_check.py`'s exit-code mapping, because exit code is a single
scalar per run and two independent, already-passing tests pin both operands
of the AC-4 inequality to the *same* value:

1. `test_uxp_700b_3.py::TestReachability::test_uxp_700b_3_reachable_from_entry_point`
   (this ticket, line ~186) pins `not_exercised` CLI exit code `== 0`
   ("an empty population is not a violation; the CLI must not exit non-zero
   for not-exercised").
2. `test_uxp_700b_3_i.py::TestReachability::test_uxp_700b_3_i_reachable_from_entry_point`
   (sibling ticket UXP-700b-3-i, status `done`, already signed off — changing
   its pinned behavior would be a regression against a completed ticket) pins
   `holds` CLI exit code `== 0` ("a single satisfying record must exit 0").
3. `test_uxp_700b_3.py::TestAc4RunOutcomeDiffers::test_ac4_not_exercised_exit_code_differs_from_holds_exit_code`
   (this ticket) requires `not_exercised` exit code `!=` `holds` exit code.

(1) + (2) together force `not_exercised == holds == 0`, which directly
contradicts (3). No production change to `main()`'s exit-code mapping can
satisfy all three simultaneously — this is a structural contradiction in the
test suite, not an implementation gap. I did not modify `universal_rule_check.py`
in an attempt to force a false green (e.g. changing `holds`'s exit code to
something non-zero would flip constraint (2) red instead; leaving it as-is
leaves (3) red — either way one already-pinned/red_baseline test fails), and
per the TDD Red-Baseline gate and the Contract-Shrinkage Guard I must not
weaken, skip, or delete any of the three tests to force a pass.

**Classification** (Contract-Shrinkage Guard): `(classification: test_drift)`
— production behavior is correct and intentional on both fixed points
(`not_exercised` must not exit non-zero per this ticket's own reachability
test; `holds` must exit 0 per the already-`done` UXP-700b-3-i contract).
The stale element is `test_uxp_700b_3.py`'s AC-4 test's choice of *signal*:
it asserts on CLI exit code, which is a scalar already fully allocated
(0 = not_exercised/holds, 1 = violated) by the pre-existing, signed-off
UXP-700b-3-i contract, leaving no free value for AC-4 to claim without
colliding with (1). The JSON payload's `"outcome"` field already
distinguishes `not_exercised` from `holds` distinctly and is already
asserted elsewhere in this same test file to differ — that field (not exit
code) is available as a non-colliding control-flow signal AC-4 could assert
on instead, but choosing it is a test-authoring decision outside my
(python-coder's) scope: I do not write or edit test files.

**Suggested remediation:** Respawn `test-writer` to revise
`test_ac4_not_exercised_exit_code_differs_from_holds_exit_code` to assert
AC-4's distinguishability requirement against a signal that is not already
contractually pinned to collide (e.g. `payload["outcome"]`, or a newly
introduced non-exit-code field), OR escalate to `architect-review` / the user
if exit-code space genuinely must be renegotiated (which would require
reopening the completed UXP-700b-3-i ticket's pinned contract — out of this
ticket's stated scope per architect-review's own sign-off note that
multi-check/exit-code aggregation "is correctly left to a later ticket").
I have made no production or test edits; `universal_rule_check.py` is
unchanged from test-writer's handoff state.

### 2026-09-16 16:05 — test-runner (status: blocker)

feedback-id: fb_2026-09-16_e63d1a7a
completion_manifest:
  test_suite_executed: true
  all_tests_passing:
    result: false
    reason: "unit_tests/product_truth/test_uxp_700b_3.py run under AC_ENFORCE_STRICT=1
      (required to surface genuine failures past the AC-enforcement xfail-downgrade
      plugin): 1 failed, 3 passed. TestAc4RunOutcomeDiffers::test_ac4_not_exercised_exit_code_differs_from_holds_exit_code
      fails with AssertionError: 0 == 0 -- both the not_exercised and holds CLI
      runs return exit code 0."
    remediation: "Do not respawn python-coder against this failure -- python-coder
      already investigated (2026-09-16 15:45 comment) and found the failure structurally
      unsatisfiable as currently written: the sibling done-ticket UXP-700b-3-i pins
      holds' exit code to 0, this ticket's own reachability test pins not_exercised's
      exit code to 0, and this ticket's AC-4 test demands the two differ -- no change
      to universal_rule_check.py's exit-code mapping can satisfy all three. Respawn
      test-writer to revise test_ac_4_not_exercised_exit_code_differs_from_holds_exit_code
      to assert distinguishability on the JSON payload's outcome field (already
      present and already asserted to differ elsewhere in the same file) rather than
      on exit code, per python-coder's suggested remediation."
  failure_report_structured: true
Ran the ticket's test file under AC_ENFORCE_STRICT=1 (`poetry`-less `python -m
pytest unit_tests/product_truth/test_uxp_700b_3.py -v`, required to bypass the
repo's AC-enforcement plugin's default xfail-downgrade of not-yet-`done`-AC
failures) and independently reproduced python-coder's reported result exactly:
3 passed, 1 failed. The lone failure is
`TestAc4RunOutcomeDiffers::test_ac4_not_exercised_exit_code_differs_from_holds_exit_code`
(`AssertionError: 0 == 0`). Per the Edit Scope Boundary I did not touch the
test file or `universal_rule_check.py` to force a green run; python-coder's
15:45 comment already diagnosed this as a genuine three-way contradiction
across this ticket's reachability test, the sibling `UXP-700b-3-i` ticket's
pinned `holds` exit code, and this ticket's AC-4 exit-code assertion, not an
implementation gap. Signing off `failed`/blocker so the supervisor routes to
`test-writer` for the suggested remediation (assert AC-4 against
`payload["outcome"]` instead of exit code) rather than re-dispatching
`python-coder` against an unsatisfiable target.

### Failure Report

**Failure 1: TestAc4RunOutcomeDiffers.test_ac4_not_exercised_exit_code_differs_from_holds_exit_code**

File: unit_tests/product_truth/test_uxp_700b_3.py
Failure type: assertion (structural test contradiction, not a production bug —
see python-coder's 2026-09-16 15:45 comment above)
Stacktrace excerpt:
```
    self.assertNotEqual(
        not_exercised_run.returncode,
        holds_run.returncode,
        ...
>   )
E   AssertionError: 0 == 0 : a not-exercised run and a checked-and-found-sound
    (holds) run must not share the same control-flow signal (exit code) ...
    unit_tests\product_truth\test_uxp_700b_3.py:263: AssertionError
```
Rerun command:
```
AC_ENFORCE_STRICT=1 poetry run python -m pytest unit_tests/product_truth/test_uxp_700b_3.py::TestAc4RunOutcomeDiffers::test_ac4_not_exercised_exit_code_differs_from_holds_exit_code -v
```

## Summary

Suite: unit_tests/product_truth/test_uxp_700b_3.py   Total failures: 1
Suite rerun: `AC_ENFORCE_STRICT=1 poetry run python -m pytest unit_tests/product_truth/test_uxp_700b_3.py -v`

## Escalation

Branch: none
Reason: 2 files (new module + its test), 1 component (ux_prototyping /
product-truth checker tooling), no cross-module boundary crossed, and no
always-large trigger (no migration, no hypertable, no public API, no ADR
contract file touched).

### 2026-09-16 15:05 — documentation-expert (status: blocker)

feedback-id: fb_2026-09-16_8f65b387
completion_manifest:
  doc_written:
    result: false
    reason: "The ticket's own pipeline shows python-coder (failed, 15:45)
      and test-runner (failed, 16:05) both blocked on the same unresolved
      defect: the AC-4 exit-code-differentiation test
      (test_ac4_not_exercised_exit_code_differs_from_holds_exit_code) is
      structurally unsatisfiable as currently written, per python-coder's own
      analysis -- it collides with the already-signed-off sibling ticket
      UXP-700b-3-i's pinned holds-exit-code-0 contract. Files touched: []
      (as dispatched) confirms no production delivery landed for this ticket
      to document. Writing docs against an unresolved/contradictory contract
      would risk documenting behaviour that is still being renegotiated
      (exit-code semantics), which the Strict Workflow Rules' ticket mandate
      and no-untracked-changes rule counsel against -- doc content should
      describe the settled, tested contract, not a moving target."
    remediation: "Re-dispatch documentation-expert after test-writer revises
      the AC-4 assertion (per test-runner's suggested remediation: assert on
      payload[\"outcome\"] rather than exit code) and python-coder/test-runner
      resolve to green. At that point the AC-1 doc contract (cross-link
      docs/acceptance-criteria/ux-prototyping/UXP-500-product-truth-generation/UXP-514.yaml
      noting that its universally-quantified clauses are vacuously satisfied
      on an empty store, i.e. report not-exercised rather than holds) can be
      fulfilled against the actual, tested CLI/JSON contract instead of a
      contract still in dispute."
  cross_links_added: false
  diataxis_genre_correct: false
No doc file was authored or edited. This ticket's implementation chain is
currently blocked: python-coder (15:45) and test-runner (16:05) both signed
off `failed`/blocker on the same structural contradiction in the AC-4
exit-code test, and per their own notes the remediation is to respawn
`test-writer` to revise the assertion to key off `payload["outcome"]`
instead of the CLI exit code (which is already fully allocated by the
signed-off sibling ticket UXP-700b-3-i). Documenting the not-exercised
behaviour now would mean writing against a contract (exit-code semantics for
`not_exercised` vs `holds`) that is still being renegotiated between two
already-pinned tests. Deferring per the ticket mandate: do not produce
documentation ahead of a settled, green implementation. Suggested next step:
respawn `test-writer` (not `python-coder`, per test-runner's note) to fix the
AC-4 assertion, then re-run `python-coder`/`test-runner`, then re-dispatch
`documentation-expert` to fulfil the AC-1 cross-link contract against
`docs/acceptance-criteria/ux-prototyping/UXP-500-product-truth-generation/UXP-514.yaml`.

### 2026-09-16 18:30 — python-coder (status: ok)

feedback-id: fb_2026-09-16_bdf8b809

Finished by hand on 2026-09-16 from run wf_3d2879c1-2ae at the user's direction
(the /build-feature run was halted; the user asked for the remaining work on
this ticket to be completed manually rather than re-dispatching agents).
Resolved the blocker exactly as decided with the user: the three exit-code
assertions across this ticket and the done sibling `UXP-700b-3-i` were jointly
unsatisfiable (both `not_exercised` and `holds` are correctly pinned to exit 0;
AC-4 demanded they differ), and AC-4's own text is about the run's overall
OUTCOME, not its exit code. Renamed
`test_ac4_not_exercised_exit_code_differs_from_holds_exit_code` to
`test_ac4_not_exercised_outcome_differs_from_holds_outcome` and rewrote it to
assert `payload["outcome"]` differs between a not-exercised run and a holds
run (both still asserted to exit 0, preserving UXP-700b-3-i's contract
unchanged). Updated this ticket's `## Test Requirements` YAML block to add the
renamed test entry. No change to `universal_rule_check.py` was needed -- the
outcome-field distinction it asserts on was already correct and already
covered by production code built by the done sibling ticket.

Verified: `AC_ENFORCE_STRICT=1 python -m pytest unit_tests/product_truth/test_uxp_700b_3.py -v`
-- 4/4 green. `AC_ENFORCE_STRICT=1 python -m pytest unit_tests/product_truth/test_uxp_700b_3_i.py -v`
-- 3/3 green (sibling ticket's holds-exits-0 contract unaffected). Full
`unit_tests/product_truth` suite (this worktree's applied subset): 108 passed,
11 subtests passed, 0 failures -- a strict subset of a disposable pristine
`origin/main` worktree run (100 passed, 0 failures; the 8-test delta is this
group's two new test files, which do not exist on `origin/main`). Mutation:
changed `universal_rule_check.py`'s `not_exercised` return to `"holds"` --
all 4 tests in this file went red (including the rewritten AC-4 test),
confirming the criterion is exercised; reverted via `git checkout --`.
`ruff check` on the touched files: all checks passed.

Known gap, out of this manual pass's explicit scope: `documentation-expert`'s
AC-1 contract (cross-link `UXP-514.yaml` noting its clauses are vacuously
satisfied on an empty store) was never fulfilled -- it was blocked purely by
the now-resolved test contradiction, but writing that cross-link was not among
the blockers the user asked this pass to resolve for this ticket. Frontmatter
`documentation-expert` is left `failed` (accurately reflecting it ran and did
not complete) rather than fabricated as `signed_off`. `files_touched` set to
this ticket's own file (`unit_tests/product_truth/test_uxp_700b_3.py`);
`out_of_scope` lists the other files touched in this same commit by sibling
tickets 18 (`UXP-700b-4`) and 36 (`UXP-700d-4`), per `check-predone-scope`'s
declaration requirement.
