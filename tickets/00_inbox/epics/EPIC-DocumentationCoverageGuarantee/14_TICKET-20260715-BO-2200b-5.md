---
advances_current_outcome: true
agents:
  commit: signed_off
  documentation-expert: not_needed
  pr-reviewer: needed
  pull-request: signed_off
  python-coder: signed_off
  sql-coder: not_needed
  test-runner: needed
  test-writer: signed_off
complexity: medium
components:
- build_orchestration
change_target: code
created: '2026-07-15'
depends_on:
- 13_TICKET-20260715-BO-2200b-4.md
files_touched:
- scripts/ac_store/generate_ticket_from_ac.py
priority: medium
requires_adr: false
requires_diagram: false
risk_surface: internal
roadmap_phase: phase_1
source_ac: BO-2200b-5
status: todo
title: Once triggered, the writer and verifier cannot be suppressed by a not_needed
  override
---

# Once triggered, the writer and verifier cannot be suppressed by a not_needed override

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BO-2200b-5` — Once triggered, the writer and verifier cannot be suppressed by a not_needed override — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BO-2200b-5`. Component: `build-orchestration`. Assigned agent: `python-coder`. Estimated complexity: `M`. Complexity: `medium`.

## Acceptance Criteria

```gherkin
Given a ticket whose classification triggered a documentation demand, so
  `documentation-expert` and `documentation-verifier` were injected,
When an override attempts to set either agent's status to `not_needed`,
Then the override does not take effect and both agents remain `needed`.
And this non-suppressibility is enforced the same way the existing TDD-mandatory
  protection keeps `test-writer` and `test-runner` from being stripped once required.
```

## Test Requirements

```yaml
tests:
- name: test_not_needed_override_does_not_strip_doc_expert_or_verifier
  file: unit_tests/test_bo_2200b_5.py
  covers:
  - BO-2200b-5
  asserts: After the trigger fires, attempting to set documentation-expert or documentation-verifier
    to not_needed leaves both at needed.
  framework: unittest
  type: behavioral
- name: test_doc_agents_protected_like_tdd_mandatory
  file: unit_tests/test_bo_2200b_5.py
  covers:
  - BO-2200b-5
  asserts: documentation-expert/documentation-verifier are protected by the same mandatory-agent
    mechanism that protects test-writer/test-runner.
  framework: unittest
  type: behavioral
```

## Implementation Notes

```yaml
config_schema_fragment:
  documentation_mandatory_agents:
    description: Protected set (analogue of _TDD_MANDATORY) whose members cannot be
      downgraded to not_needed once the trigger fires.
    items:
      enum:
      - documentation-expert
      - documentation-verifier
      type: string
    type: array
constraints:
- 'Make documentation-expert and documentation-verifier non-suppressible once the
  trigger fires: a not_needed override must not strip them.'
- Locate the protection at the same layer that already guards the TDD agents (the
  _TDD_MANDATORY protected set in _build_agents_map, ~line 454) so the two behave
  identically.
n_location_rule: '1'
post_write_commands: []
reference_file_path: scripts/ac_store/generate_ticket_from_ac.py
required_skills:
- python-coder
```

## Sign-offs

- [x] python-coder — 2026-07-21 11:00
- [x] test-writer — 2026-07-21 10:45
- [ ] test-runner
- [ ] pr-reviewer
- [x] commit — 2026-07-21 11:15
- [x] pull-request — 2026-07-21 11:30

## Comments

### 2026-07-21 11:00 — python-coder (status: ok)
feedback-id: fb_2026-07-20_ae198ef6
completion_manifest:
  red_baseline_turned_green: true
  doc_mandatory_protection_implemented: true
  no_regression_in_existing_tests: true
  frontmatter_guard_violations_fixed: true

Added `_DOC_MANDATORY = frozenset({"documentation-expert", "documentation-verifier"})` and `doc_protected` to `_build_agents_map` in `scripts/ac_store/generate_ticket_from_ac.py`. The protection mirrors `_TDD_MANDATORY` exactly: doc agents are excluded from the override-discard loop and always set to `"needed"` in the phase-order loop when they were in `all_needed` before overrides ran. Both red_baseline tests (`test_not_needed_override_does_not_strip_doc_expert_or_verifier`, `test_doc_agents_protected_like_tdd_mandatory`) are now green; all 83 existing `generate_ticket_from_ac` tests remain green. Pre-existing frontmatter guard violations (`depends_on` AC-IDs, missing `change_target`/`risk_surface`) were also corrected.

red_baseline_results:
  - test_name: test_not_needed_override_does_not_strip_doc_expert_or_verifier
    result: green
  - test_name: test_doc_agents_protected_like_tdd_mandatory
    result: green

### 2026-07-21 11:15 — commit (status: ok)
feedback-id: fb_2026-07-20_f06251d7
completion_manifest:
  pre_commit_hooks_pass: true
  commit_message_valid: true
  ticket_staged: true
Auto-authorized commit gate: subject "feat(build-orchestration): protect doc agents from not_needed override (BO-2200b-5)"; staged files: docs/glossary_blacklist.md, docs/reference/documentation-coverage-guarantee.md, scripts/ac_store/generate_ticket_from_ac.py, tickets/00_inbox/epics/EPIC-DocumentationCoverageGuarantee/14_TICKET-20260715-BO-2200b-5.md, tickets/00_inbox/epics/EPIC-DocumentationCoverageGuarantee/24_TICKET-20260715-BO-2200c-6.md, unit_tests/test_bo_2200b_5.py.

### 2026-07-21 10:45 — test-writer (status: ok)
feedback-id: fb_2026-07-20_0641bf46
completion_manifest:
  tests_written: true
  tests_red_baseline_confirmed: true
  covers_ac_bo_2200b_5: true

Wrote 2 red failing test stubs in `unit_tests/test_bo_2200b_5.py` covering AC BO-2200b-5. Both tests fail with `AssertionError: 'not_needed' != 'needed'` confirming the doc-agent override protection is absent until python-coder implements the `_DOC_MANDATORY` protected set in `_build_agents_map`.

red_baseline:
  - test_name: test_not_needed_override_does_not_strip_doc_expert_or_verifier
    file: unit_tests/test_bo_2200b_5.py
    error: "AssertionError: 'not_needed' != 'needed' : Expected documentation-expert='needed' (override must be suppressed by the mandatory-agent protection), but the override took effect: got 'not_needed'. Implementation required: add documentation-expert to a doc-mandatory protected set in _build_agents_map (BO-2200b-5)."
  - test_name: test_doc_agents_protected_like_tdd_mandatory
    file: unit_tests/test_bo_2200b_5.py
    error: "AssertionError: 'not_needed' != 'needed' : documentation-expert was stripped by a not_needed override even though the documentation trigger fired — the doc-mandatory protection is absent. Got: 'not_needed'. Expected: 'needed' (same outcome as test-writer under TDD-mandatory)."

### 2026-07-21 11:30 — pull-request (status: ok)
feedback-id: fb_2026-07-21_9c92383a
completion_manifest:
  branch_pushed: true
  pr_created: true
  pr_body_complete: true
Epic branch feature/BO-2200-doc-coverage is pushed to origin; existing PR #337 (feat(config): add documentation coverage guarantee (BO-2200)) covers this ticket's commit c3f4221cf. PR is currently CONFLICTING with main — merge conflicts will be resolved at epic finalization. test-runner and pr-reviewer remain needed; status not flipped to done.
