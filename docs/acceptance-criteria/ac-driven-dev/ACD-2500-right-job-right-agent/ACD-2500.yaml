id: ACD-2500
components:
  - ac_driven_dev
  - ac_store
  - testing_quality
readiness: approved
priority: high
title: "Every part of a requirement is written by whoever can actually get it right"
component: ac-driven-dev
level: L0
status: active
req_status: draft
work_status: todo
criteria: |
  Each part of a requirement comes from the one able to get it right.

  Today one planning step writes almost every technical part of a requirement,
  including parts it cannot see enough to write well: how a behaviour should be
  tested, and which files a piece of work really touches. It also spends effort
  re-checking rules that never need judgment. After this goal, judgment calls stay
  with the planner, rule checks run the same way every time without anyone having to
  remember them, and anything that depends on the actual code is decided by
  something that has looked at the code. The result is requirements that are
  cheaper to produce, more consistent, and whose test guidance can be trusted.
depends_on: []
doc_links:
  - path: docs/analysis/2026-09-25-test-writers-prove-failure-not-discrimination.md
    relationship: related
    status: exists
    relevance: >-
      THE WORK THIS GOAL MUST PRECEDE. The analysis proposes a way to state which
      plausible wrong implementations a test must catch. That statement can only be
      written by something that sees the code, so it needs the code-aware test-design
      step this goal creates (ACD-2500c). Any L0 authored for that analysis should
      list ACD-2500 in depends_on.
  - path: docs/acceptance-criteria/testing-quality/TQ-500-checks-that-can-fail/TQ-500.yaml
    relationship: related
    status: exists
    relevance: >-
      Neighbouring test-strength goal. TQ-500 asks that a passing check could have
      failed; this goal decides WHO writes the test design those checks follow. No
      overlap in promises, but the BA should keep the two vocabularies aligned.
  - path: docs/acceptance-criteria/ac-driven-dev/ACD-1700-role-scoped-context/ACD-1700.yaml
    relationship: related
    status: exists
    relevance: >-
      ACD-1700 gives each agent the brief for its role and ACD-1700b checks that the
      assigned specialist matches the deliverable. This goal redraws which role owns
      which part of the requirement; ACD-1700's role scoping must follow the new split.
delivers_to: null
expects_from: null
origin_agent: product-owner
created: 2026-09-25
amended_by: []
superseded_by: null
covered_by:
  - ACD-2500a
  - ACD-2500b
  - ACD-2500c
  - ACD-2500d
  - ACD-2500e
implemented_by: []
change_target:
  - prompt
  - code
risk_surface: contract_boundary
notes: |
  ORIGIN. The user's brief targets the technical-planning agent (IT PO), which today
  covers routing and splitting, sizing and technical requirements, integration
  contracts, the test contract, store housekeeping, and the knowledge loop. Four
  problems were named: it authors test design without being allowed to read code or
  tests; two of its decisions need file visibility it lacks; many of its rules are
  deterministic and belong in a check that runs after writing; and its own declared
  profile misstates what it changes and produces.

  FRAMING CHANGES I MADE. The request reads as one refactor; it is five separable
  outcomes, each independently shippable, so it is framed as an L0 with five L1s.
  The profile correction is a one-line tactical fix on its own; it is folded into
  ACD-2500e rather than given its own L1.

  SEQUENCING IS A HARD CONSTRAINT. This goal must land before the discrimination /
  must_catch work from the 2026-09-25 analysis, because that work's field lives in
  the test design ACD-2500c relocates. Ordering inside this goal: ACD-2500b and
  ACD-2500c ship together (intent without a filler leaves the test writer with
  nothing); ACD-2500a can ship first and independently.

  DECIDED AT THE PO GATE (user, 2026-09-25): the test design is filled by a
  code-aware planning step BEFORE the build (test-planner with source access), not
  by the test writer at build time. Reason: the design then exists before approval,
  a reviewer can see it before tests are written, and the existing readiness rule
  keeps working (see next paragraph).

  CONFLICT TO RESOLVE IN BA/IT PO: ACD-1600c-1 / ACD-1600c-2 and the check-ac-schema
  guard require an approved leaf code AC to carry a test contract. Once the IT PO
  records only test intent, the code-aware step must run before the AC reaches
  `readiness: approved`, or those rules must accept "intent recorded, design
  pending" as a distinct not-yet-buildable state. Do not weaken the rule silently.

  KNOWLEDGE LOOP. The brief lists it as a current duty but does not relocate it. I
  have assumed it stays with the IT PO and have not created an L1 for it.
