---
ac_traceability:
  id: BO-1000c-2
  path: docs/acceptance-criteria/build-orchestration/BO-1000-live-automation-progress/BO-1000c-2.yaml
advances_current_outcome: true
agents:
  commit: needed
  llm-expert: needed
  pr-reviewer: needed
  pull-request: needed
change_target: pipeline
complexity: medium
components:
- build-orchestration
created: '2026-07-20'
depends_on:
- BO-1000c
- BO-1000c-1a
- BO-1000c-1b
files_touched:
- templates/commands/finalize-feature.md
priority: medium
requires_adr: false
requires_diagram: false
risk_surface: internal
roadmap_phase: phase_1
source_ac: BO-1000c-2
status: todo
title: Surfaced progress reflects the in-flight step, arriving over time rather than
  only at the end
---

# Surfaced progress reflects the in-flight step, arriving over time rather than only at the end

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BO-1000c-2` — Surfaced progress reflects the in-flight step, arriving over time rather than only at the end — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BO-1000c-2`. Component: `build-orchestration`. Assigned agent: `llm-expert`. Estimated complexity: `M`. Complexity: `medium`.

## Acceptance Criteria

```gherkin
Given the finalize-feature workflow is running in the background over several
  steps,
When progress is surfaced into the main conversation,
Then a progress line for a step appears in the conversation while the run is
  still in flight — before the run has finished — reflecting the step
  currently underway,
And multiple distinct progress updates arrive in the conversation over the
  course of the run, rather than a single batch delivered only after the run
  completes.
```

- [ ] AC-1: a progress line for a step appears in the conversation while the run is
- [ ] AC-2: multiple distinct progress updates arrive in the conversation over the

## Implementation Notes

```yaml
- 'Surface: the /finalize-feature launch contract (templates/commands/finalize-feature.md
  + main-loop poll/relay instruction) — the observable ''updates arrive in the conversation
  over time'' is produced by the launcher relaying incrementally, so this is llm-expert
  prose, not a finalize-feature.js edit.'
- The relay must deliver a progress line for the in-flight step BEFORE the run finishes
  — the launcher surfaces new journal lines as they appear rather than waiting for
  run completion.
- Multiple distinct updates must reach the conversation across the run (at least one
  per executed/skipped step that emits a line), not a single end-of-run batch. Poll
  cadence and dedup must ensure over-time delivery without repeating already-relayed
  lines.
- This behavior depends on BO-1000c-1a writing the journal incrementally (append-as-you-go)
  and BO-1000c-1b's poll/relay loop; do not re-implement the journal or the relay
  here — this AC asserts their combined over-time delivery guarantee.
- Keep the /finalize-feature command doc and its deployed copy in parity via build.py
  (ADR-001).
```

## Agent Contracts

### Expects From

- **AC:** BO-1000c-1a
- **Contract:** The incrementally-written, order-preserving run-progress journal (from BO-1000c-1a) which, when polled/relayed by BO-1000c-1b, yields in-flight progress lines arriving in the conversation over the course of the run.

## Sign-offs

- [ ] pr-reviewer
- [ ] llm-expert
- [ ] commit
- [ ] pull-request

## Comments
