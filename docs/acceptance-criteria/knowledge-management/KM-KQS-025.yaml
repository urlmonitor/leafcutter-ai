id: KM-KQS-025
title: "Combined edge improvements produce at least 600 valid edges from the real repository"
component: knowledge-management
level: L2
status: active
work_status: todo
criteria: |
  Given the repository with all four edge improvements applied:
    - components frontmatter produces component_membership edges
    - depends_on file paths are resolved to node IDs
    - docs/adrs/components surfaces have related_docs and components edge_fields
    - phantom targets are filtered out
  When running the knowledge query with --format json against the real repository
  Then the output JSON "edges" array contains at least 600 entries
  And every edge target in the output corresponds to an existing node id in the "nodes" array
  And no edge has target "user" or target "__ticket_phase_agents__"
  And the edges array contains at least one edge with type "component_membership"
  And the edges array contains at least one edge with type "depends_on" whose target was resolved from a file path
assigned_agent: python-coder
estimated_complexity: S
it_requirements:
  - "Must complete the full graph build in <5 seconds for the real repository"
  - "Must exit with code 0 when the edge count threshold is met"
  - "Must produce machine-parseable JSON output via the --format json flag"
  - "This AC serves as an integration gate — all four prerequisite improvements must be working together"
delivers_to: null
expects_from:
  ac_id: KM-KQS-021
  contract: "component_membership edges produced by the extract_edges function for nodes with components frontmatter"
doc_links:
  - path: templates/skills/knowledge-query/SKILL.md
    relationship: describes
    status: exists
  - path: docs/architecture/agent_knowledge_system.md
    relationship: describes
    status: exists
origin_agent: "business-analyst-v3"
created_by_ticket: "tickets/00_inbox/epics/EPIC-KnowledgeGraphQueryLayer/05a_edge_connectivity_improvements.md"
superseded_by: null
amended_by: []
covered_by: []
implemented_by: []
