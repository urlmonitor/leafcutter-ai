---
title: "<Project Name> — System Context"
type: architecture
status: draft
created: YYYY-MM-DD
last_updated: YYYY-MM-DD
components: []
flight_level: L1-Context
diagram_type: context
root: true
parent: null
children: []
related_adrs: []
related_code: []
---

# <Project Name> — System Context

<!-- HINT: Replace <Project Name> with your project name.
     Rename this file to c1-001-system-context.md (drop the .template extension).
     Fill in the mermaid diagram below with your system boundary, external actors,
     and the relationships between them.
     Update the frontmatter: set your components, today's date, and status: active
     when the diagram is ready for review. -->

This document is the L1 system-context diagram for this project. It shows the
system boundary, external actors (users, third-party services, upstream data
sources), and the primary flows between them.

```mermaid
C4Context
  title System Context: <Project Name>

  Person(user, "User", "Primary user of the system")

  System_Boundary(sys, "<Project Name>") {
    System(core, "Core System", "Main application logic")
  }

  System_Ext(ext1, "External Service 1", "Description of external dependency")

  Rel(user, core, "Uses")
  Rel(core, ext1, "Integrates with")
```

## System Boundary

<!-- Describe what is inside the system boundary and what is outside it.
     Keep this to 3–5 bullet points. -->

- **Inside**: Core application logic, data storage, background processing.
- **Outside**: External APIs, third-party services, end users.

## External Actors

<!-- List every external actor shown in the diagram. -->

| Actor | Type | Description |
|-------|------|-------------|
| User | Human | Primary end user |
| External Service 1 | Third-party system | Description |

## See Also

- `FRONTMATTER.md` — full frontmatter field reference
- `README.md` — architecture folder conventions
- `adrs/README.md` — ADR folder guide
