---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  pr-reviewer: needed
  pull-request: needed
  python-coder: signed_off
  sql-coder: not_needed
  test-runner: needed
  test-writer: signed_off
change_target: config
complexity: medium
components:
- build_orchestration
created: '2026-07-15'
depends_on: []
files_touched:
- config/agent_registry.json
priority: medium
requires_adr: false
requires_diagram: false
risk_surface: internal
roadmap_phase: phase_1
source_ac: BO-2200b-1
status: todo
title: A documentation-verifier phase runs after writing and before commit
---

# A documentation-verifier phase runs after writing and before commit

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BO-2200b-1` — A documentation-verifier phase runs after writing and before commit — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BO-2200b-1`. Component: `build-orchestration`. Assigned agent: `python-coder`. Estimated complexity: `M`. Complexity: `medium`.

## Acceptance Criteria

```gherkin
Given the build's ordered set of phase agents for a ticket,
When the phase order is resolved,
Then a phase agent `documentation-verifier` exists with priority `11.9`.
And its priority places it AFTER the coder, the test-runner, and
  `documentation-expert`, and BEFORE the commit phase.
And on a ticket where `documentation-verifier` is `needed`, it is dispatched in that
  slot — never at the very end after commit.
```

## Test Requirements

```yaml
tests:
- name: test_documentation_verifier_registered_at_priority_11_9
  file: unit_tests/test_bo_2200b_1.py
  covers:
  - BO-2200b-1
  asserts: config/agent_registry.json contains a documentation-verifier phase agent
    with priority 11.9, conditional:true, and signoff:true.
  framework: unittest
  type: unit
- name: test_documentation_verifier_sorts_after_doc_expert_before_commit
  file: unit_tests/test_bo_2200b_1.py
  covers:
  - BO-2200b-1
  asserts: In the resolved phase order, documentation-verifier's index is greater
    than documentation-expert and less than commit.
  framework: unittest
  type: behavioral
```

## Implementation Notes

```yaml
config_schema_fragment:
  documentation-verifier:
    properties:
      conditional:
        const: true
      is_ticket_phase:
        const: true
      priority:
        const: 11.9
      signoff:
        const: true
    type: object
constraints:
- 'Register a new phase agent documentation-verifier in config/agent_registry.json
  at priority 11.9 (clone the user-surface-smoker entry ~priority 11.5: conditional:true,
  conditional_field, selection_criteria.trigger_conditions, default_status, skills_used:[signoff])
  so it sorts after the coder / test-runner / documentation-expert and before commit.'
- This is the documentation analogue of the BP-1100 phantom-done enforcement — cite
  that family, do NOT fold this into it.
- Also add documentation-verifier to templates/config/feedback_categories.yaml allowed_writers
  (omission caused a production incident for user-surface-smoker).
- The documentation-verifier agent template (templates/agents/documentation-verifier.md)
  is authored under BO-2200b-2 (llm-expert); its frontmatter priority must match this
  registry entry.
n_location_rule: '1'
post_write_commands: []
reference_file_path: config/agent_registry.json
required_skills:
- python-coder
```

## Sign-offs

- [x] python-coder — 2026-07-17 15:30
- [x] test-writer — 2026-07-17 14:00
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit
- [ ] pull-request

## Comments

### 2026-07-17 15:30 — python-coder (status: ok)
feedback-id: fb_2026-07-17_4efd6950

## Completion Report

### Files changed
- `config/agent_registry.json`: Added `documentation-verifier` entry at priority 11.9 (after `live-surface-tester` at 11.8, before `commit` at 12), with `conditional: true`, `conditional_field: "requires_documentation_verification"`, `is_ticket_phase: true`, `skills_used: ["signoff"]`.
- `templates/config/feedback_categories.yaml`: Added `documentation-verifier` to `allowed_writers` in `complete`, `knowledge-gap`, `tooling-issue`, `convention-ambiguity`, `blocker`, and `success-pattern` categories.
- `config/feedback_categories.yaml`: Mirrored same allowed_writers additions to keep parity with the template source.

### Skills run
- doc-enforcer: not applicable (no Python source files created or modified; config-only change)
- complexity-reduction: not applicable (no Python functions written)
- collector-enforcer: not applicable
- research-agent: not needed

### Tests
- Command: `python -m unittest /home/henzeh/projects/leafcutter/worktrees/BO-2200-doc-coverage/unit_tests/test_bo_2200b_1.py -v`
- Result: 2 tests PASS (both previously red_baseline tests now green)

### red_baseline_results
- test_name: test_documentation_verifier_registered_at_priority_11_9
  result: green
- test_name: test_documentation_verifier_sorts_after_doc_expert_before_commit
  result: green

### Notes
- Behavioral spot-check confirmed: priority=11.9, conditional=True, is_ticket_phase=True, skills_used=['signoff'], and phase ordering de_idx(11) < dv_idx(21) < commit_idx(22) verified against the real on-disk artifact.
- The agent template at `templates/agents/documentation-verifier.md` is out of scope for this ticket (BO-2200b-2 / llm-expert).

### 2026-07-17 14:00 — test-writer (status: ok)
feedback-id: fb_2026-07-17_507bc46e
completion_manifest:
  tests_written: true
  tests_red_confirmed: true
  covers_tag_present: true
  no_syntax_errors: true

## Test Writer — Completion Report

### Tests Written
| File | Directory | Framework | Status |
|---|---|---|---|
| test_bo_2200b_1.py | unit_tests/ | unittest | verified existing — red |

### Verification Run
- Command: `python -m unittest /home/henzeh/projects/leafcutter/worktrees/BO-2200-doc-coverage/unit_tests/test_bo_2200b_1.py -v`
- Result: red (2 failures — expected; `documentation-verifier` not yet registered in `config/agent_registry.json`)

### Notes
The test file `unit_tests/test_bo_2200b_1.py` was already present in the worktree. Both tests import and parse the live `config/agent_registry.json` and assert that a `documentation-verifier` entry with `priority=11.9`, `conditional=true`, `is_ticket_phase=true`, and `"signoff" in skills_used` exists and sorts correctly among phase agents. Both fail with `AssertionError` because the entry does not yet exist.

red_baseline:
  - test_name: test_documentation_verifier_registered_at_priority_11_9
    file: unit_tests/test_bo_2200b_1.py
    error: "AssertionError: Agent 'documentation-verifier' not found in agent_registry.json. Available IDs: ['epic-supervisor', 'ticket-supervisor', ...]"
  - test_name: test_documentation_verifier_sorts_after_doc_expert_before_commit
    file: unit_tests/test_bo_2200b_1.py
    error: "AssertionError: 'documentation-verifier' not found in ['status-checker', 'adr-author', ..., 'commit', 'pull-request'] : 'documentation-verifier' not found among is_ticket_phase agents with numeric priorities."
