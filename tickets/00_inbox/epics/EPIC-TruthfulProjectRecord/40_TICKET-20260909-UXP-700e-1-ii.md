---
ac_traceability:
  id: UXP-700e-1-ii
  path: docs/acceptance-criteria/ux-prototyping/UXP-700-truthful-project-record/UXP-700e-1-ii.yaml
advances_current_outcome: true
agents:
  ac-fulfillment-gate: needed
  ac-validator: needed
  architect-review: needed
  commit: needed
  documentation-expert: needed
  documentation-verifier: needed
  frontend-coder: not_needed
  pr-reviewer: needed
  pull-request: not_needed
  python-coder: needed
  sql-coder: not_needed
  status-checker: needed
  test-runner: needed
  test-writer: needed
  user-surface-smoker: not_needed
change_target:
- code
- config
complexity: medium
components:
- ux_prototyping
created: '2026-09-09'
depends_on: []
documentation_required: true
files_touched:
- docs/product-truth/README.md
priority: medium
requires_adr: false
requires_diagram: false
risk_surface: contract_boundary
roadmap_phase: phase_1
source_ac: UXP-700e-1-ii
status: todo
title: Enforcement tightens only when nothing is left on the older shape
---

# Enforcement tightens only when nothing is left on the older shape

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `UXP-700e-1-ii` — Enforcement tightens only when nothing is left on the older shape — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `UXP-700e-1-ii`. Component: `ux-prototyping`. Assigned agent: `python-coder`. Estimated complexity: `M`. Complexity: `medium`.

## Acceptance Criteria

```gherkin
Given a bound in its warning period,
When every artifact in the record declares the shape version in which that bound took effect,
Then the bound may be tightened so that exceeding it stops the work that triggered the check,
And while any artifact still declares an older shape version, tightening is refused and the
  refusal names the artifacts still on the older version,
And the decision to tighten is taken from what the record contains, so a date passing does not
  tighten a bound and a hand-edited flag cannot tighten it either.
```

- [ ] AC-1: the bound may be tightened so that exceeding it stops the work that triggered the check
- [ ] AC-2: while any artifact still declares an older shape version, tightening is refused and the
- [ ] AC-3: the decision to tighten is taken from what the record contains, so a date passing does not

## Test Requirements

```yaml
tests:
- name: test_tightening_is_refused_while_any_artifact_is_on_the_older_version
  file: unit_tests/product_truth/test_uxp_700e_1_ii.py
  covers:
  - UXP-700e-1-ii
  asserts: With one artifact left on the older shape version, the attempt to tighten
    is refused and names that artifact.
  framework: unittest
  type: integration
  angle: failure
- name: test_tightening_is_permitted_once_every_artifact_declares_the_new_version
  file: unit_tests/product_truth/test_uxp_700e_1_ii.py
  covers:
  - UXP-700e-1-ii
  asserts: With the backfill complete, the bound tightens and an exceeding artifact
    stops the caller.
  framework: unittest
  type: integration
  angle: criterion
- name: test_a_date_alone_does_not_tighten_a_bound
  file: unit_tests/product_truth/test_uxp_700e_1_ii.py
  covers:
  - UXP-700e-1-ii
  asserts: Advancing the clock past any configured date does not tighten a bound while
    artifacts remain on the older version.
  framework: unittest
  type: unit
  angle: boundary
- name: test_uxp_700e_1_ii_reachable_from_entry_point
  file: unit_tests/product_truth/test_uxp_700e_1_ii.py
  covers:
  - UXP-700e-1-ii
  angle: reachability
  asserts: 'REQUIRED — invoke the production entry point (CLI, hook, slash command,
    workflow dispatch, or main()) as a subprocess/dispatch and assert the new behaviour
    actually occurs. Do NOT satisfy this by importing the function directly. The AC
    authored no test_spec, so the entry point is not declared: resolve it before writing
    this test, and do not delete this entry.'
```

## Agent Contracts

### documentation-expert

Existing docs to update / cross-link:

- docs/product-truth/README.md (relationship: specifies | status: exists)

- [ ] AC-1: (unspecified genre) | docs/product-truth/README.md | the bound may be tightened so that exceeding it stops the work that triggered the check

### Expects From

- **AC:** UXP-700e-1-i
- **Contract:** The per-artifact shape version this record reads to decide readiness to tighten.

## Sign-offs

- [ ] architect-review
- [ ] test-writer
- [ ] python-coder
- [ ] test-runner
- [ ] documentation-expert
- [ ] pr-reviewer
- [ ] ac-validator
- [ ] ac-fulfillment-gate
- [ ] documentation-verifier
- [ ] status-checker
- [ ] commit

## Comments
