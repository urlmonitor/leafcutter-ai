---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  pr-reviewer: needed
  pull-request: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
  workflow-architect: needed
complexity: medium
components:
- build-orchestration
created: '2026-07-08'
depends_on:
- BO-1900a
files_touched:
- docs/architecture/agent_delivery_workflows.md
- templates/skills/building-epics/SKILL.md
- docs/architecture/components/build-ticket-workflow-dispatch.md
priority: high
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: BO-1900a-1
status: todo
title: Preflight runs before spawn and holds back an unfit ticket with a reason
---

# Preflight runs before spawn and holds back an unfit ticket with a reason

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BO-1900a-1` — Preflight runs before spawn and holds back an unfit ticket with a reason — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BO-1900a-1`. Component: `build-orchestration`. Assigned agent: `workflow-architect`. Estimated complexity: `M`. Complexity: `medium`.

## Acceptance Criteria

```gherkin
Given a ticket that has been read from the inbox and is next in the dispatch loop,
  and the ticket fails at least one fitness check,
When the supervisor reaches the point where it would spawn the phase agent,
Then the preflight validation stage runs first and returns an unfit verdict,
And no phase agent is spawned for that ticket,
And the ticket is recorded as held-back with a reason that names the ticket id
  and the specific failing check.
```

## Sign-offs

- [ ] workflow-architect
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit
- [ ] pull-request

## Comments
