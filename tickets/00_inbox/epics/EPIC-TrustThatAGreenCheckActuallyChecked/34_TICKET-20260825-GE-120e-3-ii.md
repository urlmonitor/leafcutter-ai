---
ac_traceability:
  id: GE-120e-3-ii
  path: docs/acceptance-criteria/guardrail-engine/GE-120-green-means-checked/GE-120e-3-ii.yaml
advances_current_outcome: true
agents:
  architect-review: needed
  commit: needed
  documentation-expert: needed
  documentation-verifier: needed
  pr-reviewer: needed
  pull-request: needed
  python-coder: needed
  test-runner: needed
  test-writer: needed
change_target: code
complexity: medium
components:
- commit_guardian
- git_vcs_operations
created: '2026-08-25'
depends_on: []
documentation_required: true
files_touched: []
priority: high
requires_adr: false
requires_diagram: false
risk_surface: contract_boundary
roadmap_phase: phase_1
source_ac: GE-120e-3-ii
status: todo
title: A merge whose own resolution introduces the fault is still blocked, and no
  check treats a merge as grounds to skip
---

# A merge whose own resolution introduces the fault is still blocked, and no check treats a merge as grounds to skip

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `GE-120e-3-ii` — A merge whose own resolution introduces the fault is still blocked, and no check treats a merge as grounds to skip — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `GE-120e-3-ii`. Component: `guardrail-engine`. Assigned agent: `test-writer`. Estimated complexity: `M`. Complexity: `medium`.

## Acceptance Criteria

```gherkin
Given a merge commit in which the author's own resolution introduces exactly the fault a
  check exists to catch — a test file removed by the resolution rather than by either
  incoming state, and frontmatter the author typed into a conflicted region that the check
  rejects,
When that check runs on the commit,
Then it objects and the commit is blocked,
And its objection names the author's resolution and does not name the carried-in work that
  arrived alongside it,
And the identical content arriving unchanged from a state the commit is built on, with no
  resolution of the author's own, is not blocked, in the same verification against the
  same repository,
And no check skips its inspection, in whole or in part, on the grounds that a merge is
  under way — a merge changes which content is attributed to the author, never whether the
  author's content is inspected.

Coverage note: both arms must run in the same test against the same repository, because
the difference between them IS the criterion. A test that only asserts the untouched case
is not blocked passes on a check that has been switched off for merges entirely, which is
the exact regression this AC exists to prevent.
```

- [ ] AC-1: it objects and the commit is blocked
- [ ] AC-2: its objection names the author's resolution and does not name the carried-in work that
- [ ] AC-3: the identical content arriving unchanged from a state the commit is built on, with no
- [ ] AC-4: no check skips its inspection, in whole or in part, on the grounds that a merge is

## Test Requirements

```yaml
tests:
- name: test_ge120e3ii_test_deleted_by_the_authors_resolution_still_blocks
  file: unit_tests/portability/test_ge_120e_3_ii.py
  covers:
  - GE-120e-3-ii
  asserts: A test file removed by the author's own conflict resolution trips check_contract_shrinking
    and the commit is blocked.
  framework: unittest
  type: behavioral
- name: test_ge120e3ii_frontmatter_typed_during_resolution_still_blocks
  file: unit_tests/portability/test_ge_120e_3_ii.py
  covers:
  - GE-120e-3-ii
  asserts: Frontmatter the author typed into a conflicted region trips check_doc_frontmatter
    and the commit is blocked.
  framework: unittest
  type: behavioral
- name: test_ge120e3ii_objection_names_the_resolution_and_not_the_carried_in_work
  file: unit_tests/portability/test_ge_120e_3_ii.py
  covers:
  - GE-120e-3-ii
  asserts: The blocking objection identifies the author's resolution and mentions
    none of the work that arrived alongside it.
  framework: unittest
  type: behavioral
- name: test_ge120e3ii_identical_content_arriving_unchanged_is_not_blocked_in_the_same_case
  file: unit_tests/portability/test_ge_120e_3_ii.py
  covers:
  - GE-120e-3-ii
  asserts: In the same test against the same repository, the identical bytes arriving
    from a state the commit is built on pass — authorship, not the operation, is the
    discriminator.
  framework: unittest
  type: behavioral
- name: test_ge120e3ii_no_check_skips_inspection_while_a_merge_is_in_progress
  file: unit_tests/portability/test_ge_120e_3_ii.py
  covers:
  - GE-120e-3-ii
  asserts: With a merge genuinely in progress, every self-deriving check still inspects
    the author's own content and still objects to a fault in it.
  framework: unittest
  type: behavioral
- name: test_ge_120e_3_ii_reachable_from_entry_point
  file: unit_tests/portability/test_ge_120e_3_ii.py
  covers:
  - GE-120e-3-ii
  angle: reachability
  asserts: 'REQUIRED — invoke the production entry point (CLI, hook, slash command,
    workflow dispatch, or main()) as a subprocess/dispatch and assert the new behaviour
    actually occurs. Do NOT satisfy this by importing the function directly. The AC
    authored no test_spec, so the entry point is not declared: resolve it before writing
    this test, and do not delete this entry.'
```

## Implementation Notes

```yaml
- BOTH ARMS IN ONE CASE AGAINST ONE REPOSITORY. Do not split the blocked arm and the
  not-blocked arm into separate tests. The difference between identical bytes blocked
  in one arm and passed in the other IS the criterion; two independent tests can both
  pass against a check that has been switched off for merges, which is the regression
  this AC exists to prevent.
- 'TWO REAL SUBJECTS, NOT ONE. Arm A(i): a test file removed by the author''s own
  conflict resolution must still trip check_contract_shrinking. Arm A(ii): frontmatter
  the author typed into a conflicted region must still trip check_doc_frontmatter.
  Both were the observed false-positive sources; both must be shown still catching
  the real thing.'
- THE OBJECTION MUST NAME THE AUTHOR'S RESOLUTION AND MUST NOT NAME THE CARRIED-IN
  WORK. Assert on the objection text, not only on the exit status — a check that blocks
  for the right reason and a check that blocks for the old wrong reason both exit
  non-zero.
- COVER THE NO-SKIP CLAUSE BEHAVIOURALLY, NOT BY GREP. Do not assert the absence of
  the string MERGE_HEAD in any source file. Assert instead that with a merge genuinely
  in progress the author's own faulty content is still blocked — a check that consults
  the marker to decide WHETHER to inspect fails that assertion, and a grep passes
  on dead code (CLAUDE.md, 'Gate / Workflow ACs — Verify Behaviorally, Not by Grep').
- MERGE_HEAD IS PRESENT ONLY WHILE THE MERGE IS IN PROGRESS. The fixture must execute
  the check at that point in the operation — after staging the resolution, before
  the merge commit is created — because that is when the commit path runs and it is
  the only window in which the marker exists at all.
- 'RECORD THE ACCEPTED NARROWING EXPLICITLY, in the test module docstring and in the
  sign-off: under GE-120e-1''s definition, resolving a conflict by taking an incoming
  deletion verbatim matches an incoming state and is therefore not the author''s,
  so an author who accepts an incoming test deletion and separately edits production
  code is no longer blocked by contract-shrinking. That is the observed false positive
  removed on purpose. If it is later judged too wide, the fix belongs in GE-120e-1''s
  definition of authored content, never in an exemption clause here.'
- REUSE GE-120c-1'S OUT-OF-PROCESS HARNESS and the deployed layout, so what is under
  test is the check as the commit path runs it.
```

## Agent Contracts

### documentation-expert

Existing docs to update / cross-link:

- templates/scripts/commit_guardian/check_contract_shrinking.py (relationship: related | status: exists | relevance: First arm's subject — a test deleted by the resolution must still trip it.)
- templates/scripts/commit_guardian/check_doc_frontmatter.py (relationship: related | status: exists | relevance: Second arm's subject — frontmatter written during resolution must still trip it.)
- docs/acceptance-criteria/ac-store/ACS-1200-parked-ideas/ACS-1200c-3.yaml (relationship: related | status: exists | relevance: The placement precedent: the guarantee that must survive a future relaxation of an exemption is filed with the enforcement branch, not with the exemption it constrains.)

- [ ] AC-1: [(unspecified genre)] templates/scripts/commit_guardian/check_contract_shrinking.py — it objects and the commit is blocked

### Expects From

- **AC:** GE-120e-3
- **Contract:** The differential pair mechanics — two arms over one repository with per-arm status and objection text.

## Sign-offs

- [ ] architect-review
- [ ] test-writer
- [ ] python-coder
- [ ] test-runner
- [ ] documentation-expert
- [ ] pr-reviewer
- [ ] documentation-verifier
- [ ] commit
- [ ] pull-request

## Comments
