---
ac_traceability:
  id: FIN-100g-2
  path: docs/acceptance-criteria/build_pipeline/FIN-100-pre-merge-safety-gate/FIN-100g-2.yaml
advances_current_outcome: true
agents:
  ac-fulfillment-gate: needed
  ac-validator: needed
  architect-review: needed
  commit: needed
  pr-reviewer: needed
  pull-request: needed
  python-coder: needed
  test-runner: needed
  test-writer: needed
change_target: pipeline
complexity: low
components:
- build_pipeline
created: '2026-07-21'
depends_on: []
files_touched:
- templates/workflows-js/finalize-feature.js
- templates/workflows/finalize-feature.md
- unit_tests/workflows/test_finalize_feature_preflight.py
priority: high
requires_adr: false
requires_diagram: false
risk_surface: safety
roadmap_phase: phase_1
source_ac: FIN-100g-2
status: todo
title: Pre-flight accepts its target as a bare string OR an object with a target/target_branch
  key
---

# Pre-flight accepts its target as a bare string OR an object with a target/target_branch key

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `FIN-100g-2` — Pre-flight accepts its target as a bare string OR an object with a target/target_branch key — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `FIN-100g-2`. Component: `finalize`. Assigned agent: `python-coder`. Estimated complexity: `S`. Complexity: `low`.

## Acceptance Criteria

```gherkin
Given the finalize workflow is invoked with its target supplied as a bare
string "ge-116a-1"
When the pre-flight normalizes its arguments
Then it resolves the target branch to "ge-116a-1"
And given the same workflow is instead invoked with its target supplied as an
object {target: "ge-116a-1"} (or equivalently {target_branch: "ge-116a-1"})
When the pre-flight normalizes its arguments
Then it resolves the target branch to "ge-116a-1" — identically to the
bare-string invocation
And the object form does NOT collapse to an empty target (the previous
typeof-string-only check that silently dropped object arguments is removed)
And in both invocation forms the pre-flight proceeds into worktree resolution
(FIN-100g-1) carrying the same resolved target
```

- [ ] AC-1: it resolves the target branch to "ge-116a-1"
- [ ] AC-2: given the same workflow is instead invoked with its target supplied as an
- [ ] AC-3: it resolves the target branch to "ge-116a-1" — identically to the
- [ ] AC-4: the object form does NOT collapse to an empty target (the previous
- [ ] AC-5: in both invocation forms the pre-flight proceeds into worktree resolution

## Test Requirements

```yaml
tests:
- name: test_preflight_accepts_bare_string_target
  file: unit_tests/workflows/test_fin_100g_2.py
  covers:
  - FIN-100g-2
  asserts: Invoked with the bare string "ge-116a-1", the pre-flight resolves the target
    branch to "ge-116a-1".
  framework: pytest
  type: unit
- name: test_preflight_accepts_object_target_key
  file: unit_tests/workflows/test_fin_100g_2.py
  covers:
  - FIN-100g-2
  asserts: 'Invoked with {target: "ge-116a-1"}, the pre-flight resolves the target
    branch to "ge-116a-1", identically to the bare-string form.'
  framework: pytest
  type: unit
- name: test_preflight_accepts_object_target_branch_key
  file: unit_tests/workflows/test_fin_100g_2.py
  covers:
  - FIN-100g-2
  asserts: 'Invoked with {target_branch: "ge-116a-1"}, the pre-flight resolves the
    target branch to "ge-116a-1", identically to the bare-string form.'
  framework: pytest
  type: unit
- name: test_preflight_object_form_does_not_collapse_to_empty
  file: unit_tests/workflows/test_fin_100g_2.py
  covers:
  - FIN-100g-2
  asserts: The object form does not collapse to an empty target; the pre-flight proceeds
    into worktree resolution (FIN-100g-1) carrying the same resolved target as the
    bare-string form.
  framework: pytest
  type: unit
```

## Implementation Notes

```yaml
"- SURFACE: templates/workflows-js/finalize-feature.js pre-flight argument\n  normalization\
  \ — the branch that currently gates on `typeof args === 'string'`\n  and silently\
  \ drops object arguments. This is workflow-body logic (python-coder),\n  not an\
  \ agent template.\n- Must accept BOTH argument shapes and reduce them to a single\
  \ normalized target\n  string: a bare string, AND an object carrying a `target`\
  \ OR `target_branch`\n  key. Both shapes must yield the identical resolved target\
  \ handed unchanged to\n  the worktree-resolution step (FIN-100g-1).\n- Must NOT\
  \ regress existing bare-string callers (backward compatible).\n- The object form\
  \ must never collapse to an empty target purely because the old\n  typeof-string-only\
  \ check rejected non-strings.\n- E2 determinism: normalization must be pure and\
  \ deterministic — the resolved\n  target is a function of the args only (no Date.now,\
  \ no ambient CWD read in the\n  normalization step itself).\n- A regression test\
  \ SHOULD assert bare-string and object forms resolve to the\n  identical target\
  \ and that the object form does not collapse to empty.\n"
```

## Agent Contracts

### Delivers To

- **Agent:** python-coder
- **Contract:** a normalized target branch string extracted from either a bare-string OR an object ({target}/{target_branch}) argument, consumed unchanged by the worktree-resolution step (FIN-100g-1)

## Sign-offs

- [ ] architect-review
- [ ] test-writer
- [ ] python-coder
- [ ] test-runner
- [ ] pr-reviewer
- [ ] ac-validator
- [ ] ac-fulfillment-gate
- [ ] commit
- [ ] pull-request

## Comments
