id: FIN-100c
readiness: reviewed
priority: medium
components:
  - finalize
title: "Test failures are triaged into regression, pre-existing, or flaky"
component: finalize
level: L1
status: active
# Temporary waiver, 2026-08-17. FIN-100c has carried 14 L2 children since before
# the 2026-08-17 reconciliation; the default L1 cap is 5, so check-ac-limits
# (ACS-100c-1) blocks ANY commit that stages one of them — including a
# work_status-only reconciliation of a single child that adds no new child.
# Scoped to this parent, and 14 is the current count, so a 15th child still blocks.
# NOT a resolution. This L1 is genuinely overcrowded (the null-baseline recovery
# family -4..-15 grew inside it) and wants ac-tree-split Pattern C. Remove this
# line when that lands.
child_limit_override: 14
criteria: |
  Every test failure after the merge-main step is classified so that the halt
  decision is based on actual regressions, not noise.

  Given the test suite has been run on the merged worktree (Step 3),
  When one or more tests fail,
  Then each failure is compared against the baseline snapshot (from Step 0),
  And failures present in both the baseline and the merged run are classified
  as "pre-existing",
  And failures present only in the merged run are classified as "regression",
  And failures that passed in the baseline but fail intermittently may be
  classified as "flaky" if retry logic confirms inconsistency,
  And the triage result is reported clearly with per-test classification.
depends_on:
  - FIN-100a
  - FIN-100b
doc_links: []
origin_agent: product-owner-v3
created: 2026-06-05
created_by_ticket: "tickets/99_done/TICKET-20260604-FinalizeFeatureStepReorder.md"
amended_by: []
superseded_by: null
covered_by:
  - FIN-100c-1
  - FIN-100c-2
  - FIN-100c-3
  - FIN-100c-4
  - FIN-100c-5
  - FIN-100c-6
  - FIN-100c-7
  - FIN-100c-8
  - FIN-100c-9
  - FIN-100c-10
  - FIN-100c-11
  - FIN-100c-12
  - FIN-100c-13
  - FIN-100c-14
  - FIN-100c-15
implemented_by:
  - scripts/finalize-feature.js
change_target: pipeline
risk_surface: contract_boundary
product_truth:
  - flow: leafcutter/finalize-feature
    node: postmerge
    node_kind: step
    flow_kind: data
    screen: null
    mock_data: null
    entities: []
    source: real
    asof: '2026-07-20'
