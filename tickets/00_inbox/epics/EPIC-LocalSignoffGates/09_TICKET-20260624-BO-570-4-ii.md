---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  llm-expert: needed
  pr-reviewer: needed
  pull-request: not_needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
components:
- build_orchestration
created: '2026-06-24'
depends_on:
- BO-570-4
files_touched:
- templates/agents/python-coder.md
- templates/skills/signoff/SKILL.md
- CLAUDE.md
priority: medium
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: BO-570-4-ii
status: todo
title: Ruff tooling unavailable in the worktree surfaces clearly and never silently
  passes
---

# Ruff tooling unavailable in the worktree surfaces clearly and never silently passes

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BO-570-4-ii` — Ruff tooling unavailable in the worktree surfaces clearly and never silently passes — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BO-570-4-ii`. Component: `build-orchestration`. Assigned agent: `llm-expert`. Estimated complexity: `S`.

## Acceptance Criteria

```gherkin
Given a ticket whose changed surface includes Python files,
  and the ruff tooling needed to lint them is not available in the worktree
  (for example ruff is not installed, or the repository ruff configuration
  cannot be located from the worktree root),
When the sign-off gate attempts the repo-ruff check,
Then the gate does NOT record the ruff check as passed,
And it records the check as inconclusive, naming the reason the lint could
  not be performed (tooling or config unavailable),
And the sign-off cannot reach an "ok" verdict on Python files that were
  never actually linted,
And the inconclusive outcome is surfaced to the supervisor for resolution
  rather than being swallowed silently.
```

## Sign-offs

- [ ] llm-expert
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit

## Comments
