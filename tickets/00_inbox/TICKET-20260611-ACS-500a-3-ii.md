---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  pr-reviewer: needed
  pull-request: needed
  python-coder: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
components:
- ac_store
created: '2026-06-11'
depends_on:
- ACS-500a-3
files_touched:
- docs/reference/ac-schema.md
- docs/architecture/adrs/ADR-007-ac-store-schema-id-format-enforcement.md
- config/ac_store_schema.json
priority: high
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: ACS-500a-3-ii
status: todo
title: implements_pattern referencing a deprecated pattern is rejected
---

# implements_pattern referencing a deprecated pattern is rejected

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `ACS-500a-3-ii` — implements_pattern referencing a deprecated pattern is rejected — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `ACS-500a-3-ii`. Component: `ac-store`. Assigned agent: `python-coder`. Estimated complexity: `S`.

## Acceptance Criteria

```gherkin
Given a pattern AC (PTN-001) has status: deprecated,
And a new AC is authored with implements_pattern: "PTN-001",
When the schema validator runs at commit time,
Then the commit is blocked with an error stating:
  "implements_pattern references deprecated pattern PTN-001;
  use its successor (see PTN-001 superseded_by field) or remove the reference"
And the error names the consuming AC file path.
```

## Sign-offs

- [ ] python-coder
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit
- [ ] pull-request

## Comments
