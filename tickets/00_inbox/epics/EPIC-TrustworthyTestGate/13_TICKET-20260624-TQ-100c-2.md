---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  pr-reviewer: needed
  pull-request: not_needed
  python-coder: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
components:
- testing_quality
created: '2026-06-24'
depends_on:
- TQ-100b-1-ii
- TQ-100b-2
files_touched:
- scripts/ac_store/test_enforcement.py
- templates/scripts/commit_guardian/check_test_enforcement_integrity.py
- unit_tests/commit_guardian/test_check_test_enforcement_integrity.py
priority: high
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: TQ-100c-2
status: todo
title: A done-AC test has no in-test path to downgrade itself to informational
---

# A done-AC test has no in-test path to downgrade itself to informational

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `TQ-100c-2` — A done-AC test has no in-test path to downgrade itself to informational — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `TQ-100c-2`. Component: `testing-quality`. Assigned agent: `python-coder`. Estimated complexity: `M`.

## Acceptance Criteria

```gherkin
Given a test function tagged "# covers: <AC-ID>" whose AC's work_status is done,
When the test author attempts to make that test non-blocking by editing only
  the test (for example adding a skip/xfail marker or otherwise self-declaring
  it informational),
Then the test remains enforced because its covering AC is done
And the informational/enforced decision is taken from the covering AC's
  work_status, never from a marker the test sets on itself
And the only sanctioned way to accept a failing done-AC test is the tracked
  allowlist (TQ-100d), not an in-test downgrade.
```

## Test Requirements

- `test_in_test_marker_cannot_downgrade_done_ac`: A done-AC test that adds its own skip/xfail marker remains enforced because work_status is authoritative over in-test markers.
- `test_integrity_check_runs_pre_session_with_shared_loader`: The pre-pytest linkage-integrity check reads the AC store once via the shared scan_ac_store loader and fails loudly (non-zero) naming the offending AC/test.

## Sign-offs

- [ ] python-coder
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit

## Comments
