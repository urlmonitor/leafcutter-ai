---
ac_coverage: 4/4
ac_traceability:
  id: ACD-2100d-2-i
  path: docs/acceptance-criteria/ac-driven-dev/ACD-2100-entry-point-unblocked/ACD-2100d-2-i.yaml
advances_current_outcome: true
agents:
  ac-fulfillment-gate: signed_off
  ac-validator: signed_off
  architect-review: signed_off
  commit: needed
  documentation-expert: not_needed
  documentation-verifier: not_needed
  pr-reviewer: signed_off
  pull-request: not_needed
  python-coder: signed_off
  test-runner: signed_off
  test-writer: signed_off
change_target: pipeline
complexity: low
components:
- ac_driven_dev
- build_pipeline
created: '2026-08-26'
depends_on: []
documentation_required: false
files_touched:
- scripts/build.py
- scripts/build_phases.py
- unit_tests/build_guards/test_acd_2100d_2_i.py
priority: medium
requires_adr: false
requires_diagram: false
risk_surface: contract_boundary
roadmap_phase: phase_1
source_ac: ACD-2100d-2-i
status: todo
title: An install that replaces a locally changed generated file says so rather than
  replacing it silently
---

# An install that replaces a locally changed generated file says so rather than replacing it silently

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `ACD-2100d-2-i` — An install that replaces a locally changed generated file says so rather than replacing it silently — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `ACD-2100d-2-i`. Component: `ac-driven-dev`. Assigned agent: `python-coder`. Estimated complexity: `S`. Complexity: `low`.

## Acceptance Criteria

```gherkin
Given an installed route file that has been changed since it was installed so
  that its contents differ from what the installer would produce,
When the installer runs,
Then it states in its output for that run that a local change is being replaced
  and names the file,
And the file's contents after the install are what the installer produces,
And a file that has not been changed since it was installed produces no such
  statement.
```

- [x] AC-1: it states in its output for that run that a local change is being replaced
- [x] AC-2: names the file
- [x] AC-3: the file's contents after the install are what the installer produces
- [x] AC-4: a file that has not been changed since it was installed produces no such

## Test Requirements

```yaml
tests:
- name: test_install_announces_a_locally_changed_generated_file_by_name
  file: unit_tests/build_guards/test_acd_2100d_2_i.py
  covers:
  - ACD-2100d-2-i
  asserts: The installer is run into a temporary target, one generated file is changed,
    and the installer is run again; its output for the second run states that a local
    change is being replaced and names exactly that file.
  framework: unittest
  type: integration
  angle: criterion
- name: test_unchanged_generated_files_produce_no_announcement
  file: unit_tests/build_guards/test_acd_2100d_2_i.py
  covers:
  - ACD-2100d-2-i
  asserts: Across the same two runs, the statement appears exactly once and names
    only the changed file -- no statement is produced for any file that was not changed.
  framework: unittest
  type: integration
  angle: boundary
- name: test_installed_contents_after_the_announced_replacement_are_the_installers_own
  file: unit_tests/build_guards/test_acd_2100d_2_i.py
  covers:
  - ACD-2100d-2-i
  asserts: After the second run the file's bytes equal what the installer produces
    for a clean install of that file -- the announcement does not come with a preservation
    or a merge.
  framework: unittest
  type: integration
  angle: real_artifact
- name: test_announcement_is_produced_by_running_the_real_installer
  file: unit_tests/build_guards/test_acd_2100d_2_i.py
  covers:
  - ACD-2100d-2-i
  asserts: The statement is observed in the real installer's captured output in a
    fresh process, not from a helper called directly.
  framework: unittest
  type: integration
  angle: reachability
```

## Implementation Notes

```yaml
- The install still wins. This does not make the installer refuse, preserve the local
  change, or prompt -- build output is generated and must be regenerated. The requirement
  is that the loss is announced, not prevented.
- The statement must fire only for files that actually diverged. Every generated file
  is replaced on every install; a message for each one is noise that gets filtered
  and then ignored, which is the same as no message.
- Divergence must be determined by the same computation ACD-2100d-2's delivery check
  uses. Two independent determinations will disagree, and a disagreement here means
  the check reports clean while the installer silently discards a repair -- the 2026-08-18
  incident exactly.
- The statement must name the file. A count of replaced files does not let the operator
  recover the change they are about to lose.
- The statement must appear in the installer's own output for that run, on a stream
  an operator running the installer normally sees -- not only in a log file that has
  to be sought out.
- 'Must satisfy the repository error-handling policy: reading the pre-install contents
  is external I/O and a failure to read must not silently suppress the statement.'
- Must not measurably slow a clean install -- the common case is that nothing diverged.
- 'THE INSTALLER HAS FOUR SEPARATE COMPARE-BEFORE-WRITE BRANCHES AND THE ANNOUNCEMENT
  MUST FIRE FROM EVERY ONE OF THEM. They are write_file in scripts/build.py (text,
  equality compare); _write in scripts/build_phases.py (text, equality compare); _files_content_identical
  in scripts/build_phases.py, used ahead of shutil.copy2 at roughly fourteen call sites
  (binary, SHA-256); and the workflow-script phase in scripts/build_phases.py, which
  performs its own inline SHA-256 comparison and does NOT route through _files_content_identical.
  That fourth branch is load-bearing for this epic: it is the path the route''s own
  deployed copy takes, so an implementation that instruments only the shared text helper
  is silent for exactly the file the epic is about while passing any test written against
  the text path.'
- CONSUME ACD-2100d-2's DETERMINATION, DO NOT AUTHOR ONE, AND DO NOT EDIT THE FILES
  THAT COMPUTE IT. The installer-derived mapping and the per-file divergence verdict
  are ACD-2100d-2's deliverable and arrive here through the expects_from contract.
  This record's surface is the write paths where the announcement is emitted; the determination's
  own surface belongs to the parent and is deliberately absent from this record's files_touched.
  If implementing this reveals that the determination needs to change, that is a change
  to ACD-2100d-2, not an extension of this record's scope.
- A COMPARE-BEFORE-WRITE BRANCH ALONE CANNOT SATISFY THE THIRD THEN CLAUSE, WHICH IS
  WHY THE SHARED DETERMINATION IS NOT OPTIONAL. Each branch knows only that what is
  on disk differs from what is about to be written -- which is equally true after an
  ordinary source change with no local edit at all. Announcing on that signal alone
  would fire on every legitimate template change and would be filtered within a week,
  which is the failure the second requirement above forbids. The parent's determination
  is what distinguishes the two cases.
```

## Agent Contracts

### documentation-expert

Existing docs to update / cross-link:

- docs/reference/plan-feature-layout-and-startup-checks.md (relationship: describes | status: planned | relevance: The reference page authored by ACD-2100d-4 must tell a reader that a local edit to a generated file is announced and then lost.)
- docs/known-issues/ac-driven-dev.md (relationship: describes | status: exists | relevance: KI-ACD-004's dead workaround -- the evaporation was silent, which is why it was mistaken for a fix.)
- docs/build-pipeline.md (relationship: describes | status: exists | relevance: The installer whose output carries the statement.)
- docs/architecture/adrs/ADR-001-self-hosting-boundary.md (relationship: describes | status: exists | relevance: Why build output is regenerated and why the loss is correct even though it must be announced.)

This record owns none of the documents above -- every entry is a cross-link to a
document another record produces. The contract line that stood here demanded
`docs/reference/plan-feature-layout-and-startup-checks.md`, which ACD-2100d-4
creates; this ticket could never have satisfied it. Removed per BO-2200c-5-i
and -iii; the list above is retained as reading for the implementer.

### Expects From

- **AC:** ACD-2100d-2
- **Contract:** An installer-derived source-to-installed mapping plus a per-file divergence determination -- the same computation, so the delivery check and the install-time announcement cannot disagree about which files diverged. Consumed as a callable determination; this record does not modify the files that compute it.

## Sign-offs

- [x] architect-review — 2026-08-31 22:05
- [x] test-writer — 2026-08-31 20:23
- [x] python-coder — 2026-08-31 20:48
- [x] test-runner — 2026-08-31 22:30
- [x] pr-reviewer — 2026-08-31 23:10
- [x] ac-validator — 2026-08-31 21:11
- [x] ac-fulfillment-gate — 2026-08-31 23:45
- [ ] commit

## Comments

### 2026-08-31 19:20 — note (dependency relaxed, not satisfied)
feedback-id: fb_2026-08-31_22a4a1a6

`depends_on` was `[20_TICKET-20260826-ACD-2100d-2.md]` and is now `[]`, by
explicit user decision, to break a deadlock:

```
21 depends_on 20                     21 cannot start until 20 is done
ticket 20 done  requires ACD-2100d-2 done      (check-ticket-ac-status-parity)
ACD-2100d-2 done requires ACD-2100d-2-i done   (parent/child composite rule)
ACD-2100d-2-i is delivered by ticket 21        20 cannot finish until 21 does
```

What this record needs from ACD-2100d-2 is its CODE -- the installer-derived
mapping and the per-file divergence determination, which its own `Expects From`
block describes as "consumed as a callable determination". That code is
committed (cdd8ea320) and on this branch. The dependency was modelling ticket
lifecycle status when what it actually required was an artifact, and the
artifact is present.

So this is not a dependency being waived while unmet. It is a dependency that
was expressed against the wrong thing. Verify the determination is importable
and behaves before building on it; do not assume it because this note says so.

Ordering that still holds and is NOT waived: this record must not modify the
files that compute the determination. That boundary is stated in ACD-2100d-2's
own implementation notes ("ACD-2100d-2-i reads it and does not modify the files
that compute it") and is unaffected by the depends_on change.

### 2026-08-31 22:05 — architect-review (status: ok)
feedback-id: fb_2026-08-31_83d0463f
completion_manifest:
  blast_radius_reviewed: true
  rubric_applied: true
  small_large_classification: true
Confirmed the four compare-before-write branches named in Implementation Notes
(`write_file` in scripts/build.py; `_write`, `_files_content_identical`
[~14 shutil.copy2 call sites], and the inline SHA-256 comparison in the
workflow-script phase, all in scripts/build_phases.py). None of the
always-large triggers fire: no Alembic migration, no hypertable change, no
FastAPI/public-API surface, and no ADR-contract file is touched (this record's
own `Expects From` explicitly forbids editing ACD-2100d-2's determination
files, so the boundary is self-enforcing). Threshold check: 3 files touched
(≤5), and although `components:` lists two entries (`ac_driven_dev`,
`build_pipeline`), both are facets of the single installer subsystem living
entirely under `scripts/` — there is no cross-package boundary crossing (e.g.
no live_trader/models-style split) and no third component, so this does not
meet the ≥3-component or cross-module-boundary large trigger. Classified
SMALL. Design concern: because the announcement must fire identically from
all four branches per the Implementation Notes, prefer extracting one small
shared "announce if diverged" helper called from each site rather than
duplicating the log/print statement four times — four independent inline copies
is the layering risk most likely to cause exactly the kind of silent
inconsistency (one branch instrumented, one not) the 2026-08-18 incident this
ticket exists to prevent. No acceptance-criteria adjustments — the ACs and
Test Requirements already correctly require the announcement be observed from
a fresh-process run of the real installer (angle: reachability), which is the
right test shape to catch a missed branch. requires_adr remains `false`
(confirmed, no always-large trigger and fewer than 2 architecturally distinct
components).

```json
{
  "architectural_note": "Small change confined to scripts/build.py and scripts/build_phases.py (installer/build-pipeline subsystem) plus its test file. No always-large trigger fires (no Alembic migration, hypertable change, public API surface, or ADR-contract edit) and file/component counts stay under threshold (3 files; components ac_driven_dev + build_pipeline are both facets of one installer subsystem, not a cross-package boundary). Design note: implement the announcement as one shared helper invoked from all four named compare-before-write branches (write_file in build.py; _write, _files_content_identical's ~14 call sites, and the workflow-script phase's inline SHA-256 branch in build_phases.py) rather than four duplicated inline statements, since branch-by-branch drift is the exact failure mode (2026-08-18 incident) this ticket exists to close.",
  "acceptance_adjustments": [],
  "escalation": "none",
  "escalation_reason": "",
  "suggested_adr": null,
  "suggested_diagrams": []
}
```

### 2026-08-31 20:23 — test-writer (status: ok)
feedback-id: fb_2026-08-31_7d13268c
completion_manifest:
  tests_written: true
  red_baseline_captured: true
  cross_layer_seam_answer: true
cross_layer_seam_answer:
  result: covered
  producing_side: "the REAL scripts/build.py CLI, invoked as a subprocess exactly as a consumer install runs it (never write_file/_write/_files_content_identical/build_workflow_scripts called directly)"
  consuming_side: "the on-disk generated files under a temporary self-hosting-layout target (.claude/workflows/*.js written by the workflow-script phase's own inline SHA-256 branch, and .claude/agents/*.md written by the _write text-equality branch), read back off disk after each real subprocess run"

Wrote unit_tests/build_guards/test_acd_2100d_2_i.py: four integration tests
(criterion, boundary, real_artifact, reachability -- matching the AC's own
test_spec angles verbatim) that each drive the REAL `scripts/build.py`
CLI as a fresh subprocess, twice, against a synthetic self-hosting-layout
copy of this repo's own real templates/scripts/config trees (reusing
`test_bp_100k_2.py`'s `_build_synthetic_full_package` helper by file-path
import, per this repo's existing precedent in
`unit_tests/build_guards/test_acd_2100d_2.py` -- never a hand-typed
fixture). The self-hosting layout (`pkg_root.parent == target_root`) is
required: verified empirically while authoring this file that an ordinary
`--target-dir` pointed away from the package root makes
`_compute_output_mappings()` raise a path-arithmetic `ValueError`. captured
as `output_mappings_error` and leaves `output_mappings` empty -- exactly
the shape that would make ACD-2100d-2's divergence determination unable to
see anything, so a test built on that layout could never distinguish a real
fix from a no-op.

Each test mutates one already-installed generated file after the first
install (never its source template, exactly as KI-ACD-004 describes) and
re-runs the real installer over the same target, asserting on the second
run's captured stdout+stderr. To prevent an implementation that
instruments only one compare-before-write branch from passing every test
in this module by accident, the four tests deliberately spread across TWO
of this AC's four named branches: `.claude/workflows/build-epic.js` (the
workflow-script phase's own inline SHA-256 compare in
`build_workflow_scripts` -- the branch this AC's own it_requirements name
as load-bearing, since it does not route through `_files_content_identical`)
for the criterion/boundary/real_artifact descriptors, and
`.claude/agents/README.md` (the `_write` text-equality branch via
`build_agents`) for the reachability descriptor.

RED BASELINE VERIFIED (hand-confirmed before authoring, then re-confirmed
by running the file): a fresh synthetic install, a one-line marker
appended to `.claude/workflows/build-epic.js`, then a second real
`build.py --target-dir` run over the same target already silently
overwrites the marker back to the installer's own content (the
compare-before-write branch and the overwrite already work) but neither
run's combined output contains the phrase "local change is being
replaced" anywhere -- the announcement does not exist yet. Ran the new
file with `AC_ENFORCE_STRICT=1 python -m pytest
unit_tests/build_guards/test_acd_2100d_2_i.py -v` (without the strict flag
this repo's `pytest_ac_enforcement` plugin downgrades not-done-AC failures
to `xfail`, masking the real result -- see
`project_pytest_ac_enforcement_xfail_mask` precedent) and confirmed exit
code 1, all 4 tests FAILED (not xfailed, not error) with the identical
assertion: `AssertionError: 'local change is being replaced' not found in
<captured build.py subprocess output>`. No import errors, no syntax
errors, no test passed by accident.

red_baseline:
  - test_name: test_install_announces_a_locally_changed_generated_file_by_name
    file: unit_tests/build_guards/test_acd_2100d_2_i.py
    error: "AssertionError: 'local change is being replaced' not found in <combined stdout+stderr of the second real build.py subprocess run -- the phrase is absent because the announcement does not exist yet; the file was silently overwritten back to the installer's own content instead>"
  - test_name: test_unchanged_generated_files_produce_no_announcement
    file: unit_tests/build_guards/test_acd_2100d_2_i.py
    error: "AssertionError: 0 != 1 : Exactly one file diverged this run, so the announcement must appear exactly once ... Matching lines: []"
  - test_name: test_installed_contents_after_the_announced_replacement_are_the_installers_own
    file: unit_tests/build_guards/test_acd_2100d_2_i.py
    error: "AssertionError: 'local change is being replaced' not found in <combined stdout+stderr> -- setup-sanity assertion fails first because the announcement is not yet produced (the byte-equality assertion later in the test is therefore never reached)"
  - test_name: test_announcement_is_produced_by_running_the_real_installer
    file: unit_tests/build_guards/test_acd_2100d_2_i.py
    error: "AssertionError: 'local change is being replaced' not found in <combined stdout+stderr of the real build.py subprocess run against the _write/build_agents branch (.claude/agents/README.md)>"

### 2026-08-31 20:48 — python-coder (status: ok)
feedback-id: fb_2026-08-31_d7000a59
completion_manifest:
  red_baseline_turned_green: true
  four_named_branches_instrumented: true
  determination_reused_not_authored: true
  regression_suite_green: true
red_baseline_results:
  - test_name: test_install_announces_a_locally_changed_generated_file_by_name
    result: green
  - test_name: test_unchanged_generated_files_produce_no_announcement
    result: green
  - test_name: test_installed_contents_after_the_announced_replacement_are_the_installers_own
    result: green
  - test_name: test_announcement_is_produced_by_running_the_real_installer
    result: green

Implemented the announcement in scripts/build_phases.py: a new
`set_local_change_baseline(target_root, output_root)` reads the PREVIOUS
run's `.build_manifest.json` `output_mappings` before any phase writes a
file (called from build.py's `main()` right after `reset_uptodate_count()`);
`announce_if_local_change_replaced(target)` compares a file's current
on-disk hash (CRLF-normalised, matching `check_output_drift._sha256_of_file`
exactly) against the previously recorded `expected_output_hash` for its
canonical output_mappings key (resolved via
`build_helpers._canonicalize_output_path`, imported rather than
reimplemented) and prints `"[build] NOTICE: a local change is being
replaced: <key>"` only when they diverge. All four named branches call it
right before an overwrite: `_write` and `_files_content_identical` (single
instrumentation point covering all ~14 `shutil.copy2` call sites) in
build_phases.py, `build_workflow_scripts`'s own inline SHA-256 compare
(the load-bearing fourth branch), and `write_file` in build.py (imports the
same function from build_phases). Per the ticket's Implementation Notes and
its `Expects From` contract, the divergence verdict is entirely consumed
from ACD-2100d-2's own installer-derived mapping/hash computation
(`build_helpers._compute_output_mappings`) — no independent determination
was authored, and neither `build_helpers.py` nor `check_output_drift.py`
was modified. Ran the four red_baseline tests
(`AC_ENFORCE_STRICT=1 python -m pytest
unit_tests/build_guards/test_acd_2100d_2_i.py -v`): all 4 now PASS. Also
re-ran `test_acd_2100d_2.py` (4 passed), the full `unit_tests/build_guards/`
suite (141 passed, 4 subtests passed), and the broader
`bp_900*`/`build_package`/`build_tracked_source`/`build_version`/
`build_changelog` test set (76 passed) — no regressions. `ruff check` on
both edited files passes clean. Appended DECISION HISTORY entries to both
files per doc-enforcer; both already had module docstrings and per-function
docstrings, extended consistently for every new function.

### 2026-08-31 22:30 — test-runner (status: ok)
feedback-id: fb_2026-08-31_526eb4fe
completion_manifest:
  test_suite_executed: true
  all_tests_passing: true
  failure_report_structured: true

Ran `AC_ENFORCE_STRICT=1 python -m pytest unit_tests/build_guards/test_acd_2100d_2_i.py -v`:
all 4 tests (`test_install_announces_a_locally_changed_generated_file_by_name`,
`test_unchanged_generated_files_produce_no_announcement`,
`test_installed_contents_after_the_announced_replacement_are_the_installers_own`,
`test_announcement_is_produced_by_running_the_real_installer`) PASSED. Also
ran the broader `unit_tests/build_guards/` suite under the same strict flag
as a regression check: 141 passed, 4 subtests passed, 0 failures, 0 errors —
no regressions from this change.

### 2026-08-31 23:10 — pr-reviewer (status: ok)
feedback-id: fb_2026-08-31_1d54efd4
completion_manifest:
  four_named_branches_verified: true
  red_baseline_spot_checked: true
  ruff_clean: true
  tests_reran_and_green: true

Re-ran the four tests in `unit_tests/build_guards/test_acd_2100d_2_i.py` fresh
(`AC_ENFORCE_STRICT=1 python -m pytest ... -v`): 4 passed. `ruff check
scripts/build.py scripts/build_phases.py`: clean. Traced all four named
compare-before-write branches by reading the diff against the surrounding
function bodies (not just the hunks): `write_file` (build.py), `_write` and
`_files_content_identical` (build_phases.py, ~14 `shutil.copy2` call sites
via one instrumentation point), and `build_workflow_scripts`'s own inline
SHA-256 compare all call `announce_if_local_change_replaced` immediately
before their overwrite, and `main()` calls `set_local_change_baseline`
before any phase writes a file and after `reset_uptodate_count()` — wiring
is correct. Confirmed `write_build_manifest()` runs near the end of
`main()`, after every phase, so the baseline genuinely reflects the
*previous* run, not this run's own not-yet-written manifest. Confirmed the
"expected hash" side of the comparison is reused from
`build_helpers._compute_output_mappings` (not re-authored), per this
ticket's own `Expects From` contract.

No blocking findings — the ticket's own four Test Requirements pass and the
contract boundary (consume, don't author, the divergence determination) is
respected. Two findings surfaced for follow-up, both out of this ticket's
scope to fix per its own Implementation Notes:

**[H-1] Coverage gap — announcement never fires for the generic `scripts/`
deploy family.** `_compute_output_mappings` (build_helpers.py) has no
section for files deployed by `build_workflow_tools`, `_copy_agent_support_file`,
or `build_template_standalone_scripts` (e.g. `scripts/setup_ticket_worktree.py`,
`scripts/port_registry.py`, `scripts/live_surface_startup.py`,
`scripts/generate_doc_index.py`, and top-level `templates/scripts/*.py`
copies). Those files ARE written via `_files_content_identical`, which DOES
call `announce_if_local_change_replaced` on divergence — but the lookup
against `_previous_output_mappings` finds no entry for their output key
(that family was never recorded), so the announcement silently no-ops for
this whole class of deployed generated files even though the overwrite still
happens. This means a hand-patched fix to e.g. the deployed
`scripts/setup_ticket_worktree.py` would still be silently discarded on the
next install with no announcement — the coverage gap traces back to
`ACD-2100d-2`'s own determination (`_compute_output_mappings`), which this
ticket correctly does not modify per its `Expects From` contract. Recommend
a follow-up AC against `ACD-2100d-2` to extend `_compute_output_mappings`
with a generic `scripts/` family, or to explicitly scope-note the gap.

**[H-2] KI-ACD-004 citation does not match its own content.** New code
comments in `build_phases.py` and the new test module's docstring cite
`KI-ACD-004` as "the 2026-08-18 incident" where "a hand-applied repair was
patched directly into a deployed copy" and "the next `build.py` run
silently discarded it." The actual `KI-ACD-004` entry in
`docs/known-issues/ac-driven-dev.md` describes an unrelated bug (`/plan-feature`
failing to start in the self-hosting layout because `setup_ticket_worktree.py`
resolves git from the untracked workspace). No `KI-ACD-*` entry in that file
matches the "hand-patched deployed copy silently discarded" narrative. This
citation is repeated across every AC in the `ACD-2100d` subtree
(`ACD-2100d`, `ACD-2100d-2`, `ACD-2100d-2-i`, `ACD-2100d-4`), so it is
inherited, not introduced by this ticket — but this ticket's own diff is the
first to bake it into *source code comments* (previously it was confined to
AC YAML `relevance:` fields), which propagates the mismatch further.
Recommend either correcting the citation at its origin (`ACD-2100d`) or
filing the actual 2026-08-18 incident as a new `KI-ACD-*` entry.

Also noted, medium confidence (not blocking, no escalation — count is 3,
at the ≤3 threshold): (1) the "current on-disk hash" side of the comparison
(`_hash_for_local_change_check`) duplicates `check_output_drift._sha256_of_file`'s
exact SHA-256+CRLF-normalise logic rather than importing it, which is
structurally forced by the self-hosting boundary (`check_output_drift.py`
must stay a standalone deployed hook with no cross-package imports) but is
still a drift risk if either copy's normalisation ever changes independently;
(2) the announcement fires during `--dry-run` for the
`_files_content_identical` and workflow-script branches but not for
`_write`/`write_file` (whose `dry_run` early-return precedes the
compare-before-write block) — a real behavioural inconsistency across the
four branches, though not covered by any stated AC; (3) the new module-level
globals (`_previous_output_mappings`, `_local_change_target_root`,
`_local_change_output_root`) in `build_phases.py` are set once per `main()`
call and never reset otherwise — a latent test-isolation risk for any
future in-process (non-subprocess) caller of `_write`/`_files_content_identical`
that bypasses `main()`, though no such caller exists today and the pattern
mirrors the pre-existing `_uptodate_count` global.

Spot-checked that `write_file()` in build.py (one of the four named
branches) has no production callers (`grep -rn "\bwrite_file(" scripts/*.py`
finds only its own definition and DECISION HISTORY comments) — it is a
test-only utility per its own comment ("used by tests via _build.write_file").
Instrumenting it satisfies the ticket's literal instruction but this branch
protects no real install path today; not a defect introduced by this ticket
(the function predates it), noted for awareness only — not surfaced as a
numbered finding above since it has zero behavioural impact.

### 2026-08-31 21:11 — ac-validator (status: ok)
feedback-id: (submit-failed)
completion_manifest:
  all_acs_have_implementation_evidence: true
  all_acs_have_test_evidence: true
  store_alignment_checked: true
  cross_file_contract_traced: true

`submit_feedback.py` rejected phase `ac-validator` for every category
("not an allowed writer" — `ac-validator` is absent from
`.leafcutter/config/feedback_categories.yaml` entirely); recorded
`(submit-failed)` per the signoff skill's non-blocking fallback and
proceeded. `check_v2_ac_store_alignment.py --ticket <this ticket>` exited 0
(no output) — no store-alignment violations. Ran
`AC_ENFORCE_STRICT=1 python -m pytest unit_tests/build_guards/test_acd_2100d_2_i.py -v`
fresh: 4 passed, 0 failed, 0 xfailed.

AC-1 covered (states in output that a local change is being replaced):
  implementation: scripts/build_phases.py — `announce_if_local_change_replaced()` prints `"[build] NOTICE: a local change is being replaced: <key>"`, called from `_write`, `_files_content_identical`, and the workflow-script phase's inline SHA-256 branch; scripts/build.py `write_file()` calls the same function.
  test: `test_install_announces_a_locally_changed_generated_file_by_name` (unit_tests/build_guards/test_acd_2100d_2_i.py:188-244) — PASSED, asserts the phrase appears in the real `build.py` subprocess's second-run output.

AC-2 covered (names the file):
  implementation: `announce_if_local_change_replaced()` (scripts/build_phases.py) interpolates the resolved output_mappings key (e.g. `.claude/workflows/build-epic.js`) into the printed statement via `_local_change_output_key()`.
  test: `test_install_announces_a_locally_changed_generated_file_by_name` and `test_unchanged_generated_files_produce_no_announcement` (unit_tests/build_guards/test_acd_2100d_2_i.py:245-319) both assert the statement names exactly the one mutated file and no other — PASSED.

AC-3 covered (file's contents after install are what the installer produces):
  implementation: the announcement fires immediately before the pre-existing unconditional overwrite in all four branches; no branch was changed to skip, preserve, or merge the write.
  test: `test_installed_contents_after_the_announced_replacement_are_the_installers_own` (unit_tests/build_guards/test_acd_2100d_2_i.py:320-394) — PASSED, asserts post-install bytes equal a clean install's output.

AC-4 covered (unchanged file produces no statement):
  implementation: the divergence check in `announce_if_local_change_replaced()` compares the current on-disk hash against the previous run's recorded `expected_output_hash` (via `_hash_for_local_change_check()` and `_local_change_output_key()`) and only prints when they diverge — an unchanged file's hash matches and no statement fires.
  test: `test_unchanged_generated_files_produce_no_announcement` (unit_tests/build_guards/test_acd_2100d_2_i.py:245-319) — PASSED, asserts the statement appears exactly once (only for the one mutated file) across the run's full output.

Cross-file contract (`Expects From` ACD-2100d-2): traced `build_phases.py`'s
new code to confirm it imports `_canonicalize_output_path` from
`build_helpers.py` (unmodified by this diff) and reads `output_mappings` /
`expected_output_hash` from the manifest `build_helpers._compute_output_mappings`
populates — the divergence verdict is consumed, not re-authored, satisfying
this record's own "CONSUME, DO NOT AUTHOR" boundary. No contract gap found.

All 4 ACs covered. Implementation evidence found for each; test evidence
found for each (all 4 tests independently re-run and green). Store
alignment clean. Commit phase may proceed.

### 2026-08-31 23:45 — ac-fulfillment-gate (status: ok)
feedback-id: (submit-failed)
completion_manifest:
  resolver_ran: true
  auto_fix_applied: true
  schema_validation_passed: true
  reverified_green: true

`submit_feedback.py` rejected phase `ac-fulfillment-gate` for category
`complete` ("not an allowed writer" — same gap `ac-validator` hit above;
`ac-fulfillment-gate` is absent from `feedback_categories.yaml`'s allowed
writers list). Recorded `(submit-failed)` per the signoff skill's
non-blocking fallback and proceeded.

Ran `.leafcutter/scripts/ac_store/ac_coverage_resolver.py --ticket <this
ticket>`: resolved exactly one AC (`ACD-2100d-2-i`, `level: L3`) via the
two-key `{id, path}` traceability block. Initial run reported `ok: false`
with two failing fields (`work_status`, `covered_by`).

Gathered branch diff evidence via both `git diff main...HEAD --name-only`
(shows this branch's prior committed tickets/tests, including
`unit_tests/build_guards/test_acd_2100d_2_i.py`) and `git diff HEAD
--name-only` (shows the still-uncommitted working-tree edits to
`scripts/build.py` and `scripts/build_phases.py` that python-coder,
test-runner, and pr-reviewer already verified above) — this ticket's own
implementation sits in the worktree ahead of the `commit` phase that has
not yet run, so both sources were needed to see the real `files_touched`
evidence this gate exists to verify. Union of both diffs ∩ `files_touched`
= all three files (`scripts/build.py`, `scripts/build_phases.py`,
`unit_tests/build_guards/test_acd_2100d_2_i.py`).

Auto-fixed the AC YAML
(`docs/acceptance-criteria/ac-driven-dev/ACD-2100-entry-point-unblocked/ACD-2100d-2-i.yaml`),
append-only:
- `work_status: todo` → `done` (diff-evidenced files_touched intersection
  non-empty).
- `implemented_by`: appended `scripts/build.py`, `scripts/build_phases.py`,
  `unit_tests/build_guards/test_acd_2100d_2_i.py` (the pre-existing entry,
  the ticket's own path, was left untouched).
- `covered_by`: was `[]`; grepped `tests/` and `unit_tests/` for `# covers:
  ACD-2100d-2-i` per BO-202 (no level qualifier) and found four genuine
  covering tags in `unit_tests/build_guards/test_acd_2100d_2_i.py` (lines
  192, 249, 328, 409) — appended that one file path.

Ran `scripts/commit_guardian/check_ac_schema.py` on the edited YAML: exit 0
(fell back to manual field validation with a warning that
`config/ac_store_schema.json` is absent — non-fatal per the gate's own
"skip silently if absent" rule; treated the manual-validation pass as
sufficient since the script itself did not error).

Re-ran the resolver: `ok: true`, `verified_count: 1`, `failures: []`. All
AC store fields (`work_status`, `implemented_by`, `covered_by`) verified
accurate. Commit phase may proceed.

## Escalation

Branch: none
Reason: 3 files in one component-family (scripts/ installer subsystem: ac_driven_dev + build_pipeline); no always-large trigger fired (no migration, hypertable, public-API, or ADR-contract change).
