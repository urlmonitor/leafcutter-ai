---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  pr-reviewer: needed
  pull-request: needed
  python-coder: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
components:
- build-orchestration
created: '2026-07-06'
depends_on:
- BO-1600d-3
files_touched:
- templates/scripts/git_recovery.py
- tests/test_git_recovery.py
- docs/acceptance-criteria/build-orchestration/BO-1600-safe-concurrent-dispatch/BO-1600d.yaml
priority: high
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: BO-1600d-3-iii
status: todo
title: Recovery refuses to run on a shallow or bare clone
---

# Recovery refuses to run on a shallow or bare clone

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BO-1600d-3-iii` — Recovery refuses to run on a shallow or bare clone — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BO-1600d-3-iii`. Component: `build-orchestration`. Assigned agent: `python-coder`. Estimated complexity: `S`.

## Acceptance Criteria

```gherkin
Given the repository is a shallow clone or a bare clone
When the recovery entry point is started against it
Then recovery refuses to proceed and reports that the repository is a shallow or
  bare clone that this recovery does not support, before taking any repair action
And it performs no object removal, no ref reset, and no index rebuild in this case
```

## Sign-offs

- [ ] python-coder
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit
- [ ] pull-request

## Comments
