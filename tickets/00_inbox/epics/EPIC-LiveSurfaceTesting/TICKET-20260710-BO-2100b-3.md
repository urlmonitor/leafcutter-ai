---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  llm-expert: needed
  pr-reviewer: needed
  pull-request: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
complexity: low
components:
- build_orchestration
created: '2026-07-10'
depends_on:
- BO-2100b
files_touched:
- scripts/config_loader.py
- templates/skills/ticket-authoring/SKILL.md
- docs/architecture/adrs/ADR-020-live-surface-tester.md
priority: medium
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: BO-2100b-3
status: todo
title: Both toggles on means the check runs
---

# Both toggles on means the check runs

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BO-2100b-3` — Both toggles on means the check runs — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BO-2100b-3`. Component: `build-orchestration`. Assigned agent: `llm-expert`. Estimated complexity: `S`. Complexity: `low`.

## Acceptance Criteria

```gherkin
Given a project `skills_config.json` whose `live_surface_testing.enabled` is `true`
  And a ticket whose frontmatter sets `live_surface_test: true`
When the ticket is authored and driven
Then the `live-surface-tester` phase is added to the ticket's `agents:` map as needed
  And the phase is dispatched during the drive
  And the live-app check actually runs.
```

## Implementation Notes

```yaml
- 'When both toggles are true the agents-map entry must be present as `live-surface-tester:
  needed`; this is the authoring-side guarantee.'
- The end-to-end 'phase actually dispatched and check runs' guarantee is proven by
  the real-run behavioral gate BO-2100e-1; this AC must not be marked satisfied by
  authoring alone if the wiring downstream is absent.
```

## Sign-offs

- [ ] llm-expert
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit
- [ ] pull-request

## Comments
