id: ACD-1900c-5
components:
  - ac_driven_dev
title: "State diagram shows the emit/enforce states and legal transitions"
component: ac-driven-dev
level: L2
status: active
req_status: draft
work_status: todo
readiness: reviewed
priority: medium
roadmap_phase: phase_acbuild_1_foundation
criteria: |
  Given the {emit, enforce} control has four states and a restricted set of legal
    transitions (enforce = true reachable only when zero v1 records remain),
  When the state diagram is authored,
  Then it shows all four states, the legal transitions between them, the guard on the
    transition into enforce = true, and the rollback transitions,
  And every transition drawn corresponds to a flag change a reader can reproduce.
depends_on:
  - ACD-1900c
doc_links:
  - path: docs/architecture/adrs/ADR-026-ac-driven-build-v2-phased-migration.md
    relationship: describes
    status: exists
assigned_agent: architecture-diagram-author
estimated_complexity: S
delivers_to: null
expects_from:
  ac_id: ACD-1900c-3
  contract: "The implemented {emit, enforce} state machine and the guard on the transition into enforce=true (zero v1 records) so the diagram's states, transitions, and guard match the code."
it_requirements:
  - "Canonical source: a state diagram under docs/architecture/ (Mermaid stateDiagram). Must show all four {emit, enforce} states, the legal transitions, the guard on the transition into enforce=true (zero schema_version < 2 records remaining), and the rollback transitions (enforce->false, emit->false)."
  - "Every transition drawn must correspond to a reproducible flag change; the diagram must not depict a transition the enforce-precondition (ACD-1900c-3) forbids."
test_required: false
origin_agent: BrainCandy
created: 2026-08-12
amended_by: []
superseded_by: null
covered_by: []
implemented_by: []
change_target: docs
risk_surface: internal
