---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  pr-reviewer: needed
  pull-request: needed
  python-coder: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
components:
- ac-store
created: '2026-06-22'
depends_on: []
files_touched:
- scripts/commit_guardian/check_components_integrity.py
- templates/scripts/commit_guardian/check_components_integrity.py
- docs/components.json
priority: high
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: ACS-300g-6
status: todo
title: Component integrity hook resolves REPO_ROOT to the actual repository top-level
---

# Component integrity hook resolves REPO_ROOT to the actual repository top-level

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `ACS-300g-6` — Component integrity hook resolves REPO_ROOT to the actual repository top-level — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `ACS-300g-6`. Component: `ac-store`. Assigned agent: `python-coder`. Estimated complexity: `S`.

## Acceptance Criteria

```gherkin
Given check_components_integrity.py is invoked through the `.leafcutter`
  symlink install path (so that
  `Path(__file__).resolve().parents[2]` resolves to
  `<repo>/.leafcutter` rather than the real repository root),
And a component entry in docs/components.json has a detail_ref pointing
  at an existing file under `<repo>/docs/...`,
When the hook resolves the project root in order to validate that
  detail_ref's existence,
Then REPO_ROOT must equal the actual repository top-level as reported by
  `git rev-parse --show-toplevel`,
And the detail_ref existence check resolves against
  `<repo>/docs/...` (where the file exists) rather than
  `<repo>/.leafcutter/docs/...`,
And a detail_ref pointing at an existing `<repo>/docs/...` file passes
  instead of false-failing as "detail_ref file does not exist".
```

## Sign-offs

- [ ] python-coder
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit
- [ ] pull-request

## Comments
