---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  pr-reviewer: needed
  pull-request: needed
  python-coder: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
components:
- testing-quality
created: '2026-06-24'
depends_on:
- TQ-100d-1
files_touched:
- scripts/ac_store/known_failing_tests.py
- tests/testing_quality/test_known_failing_allowlist.py
priority: high
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: TQ-100d-1-iii
status: todo
title: An allowlist entry missing its ticket reference or expiry date is rejected
---

# An allowlist entry missing its ticket reference or expiry date is rejected

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `TQ-100d-1-iii` — An allowlist entry missing its ticket reference or expiry date is rejected — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `TQ-100d-1-iii`. Component: `testing-quality`. Assigned agent: `python-coder`. Estimated complexity: `S`.

## Acceptance Criteria

```gherkin
Given a known-failing-tests allowlist that contains an entry lacking a ticket
  reference, or lacking an expiry date, or lacking both,
When the allowlist check runs,
Then the incomplete entry is rejected with a message naming the entry and the
  missing required field(s)
And the check fails (non-passing outcome) rather than accepting the
  under-specified entry
And an entry can only exempt a test if it carries both a ticket reference and
  an expiry date.
```

## Test Requirements

- `test_allowlist_entry_missing_required_fields_rejected`: An allowlist entry missing its ticket_ref or expiry date is rejected.

## Sign-offs

- [ ] python-coder
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit
- [ ] pull-request

## Comments
