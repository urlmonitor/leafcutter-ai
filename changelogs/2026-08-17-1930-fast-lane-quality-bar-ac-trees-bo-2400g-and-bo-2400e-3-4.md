---
title: "Fast-lane quality-bar ACs — BO-2400g (review + reachability) and BO-2400e-3/-4 (durable work-record writes)"
date: "2026-08-17"
time: "19:30"
type: manual
components:
  - build_orchestration
  - review_system
  - ac_store
summary: "The fast build path skips three things the thorough path does: it never asks work to prove it really runs, nothing reads the change before it is committed, and the way it records progress can destroy the record it is updating. All three are now written down as requirements."
description: "1 commit on PR #452 — 11 new AC files plus two additive parent edits, no production code. Authored by the business-analyst against verified evidence, in response to a session finding that the migration's own foundation ACs were about to be built through a lane missing the machinery the migration exists to install. GAP 1 (BO-2400g-1, -1-i): PR #451 established a mandatory angle: reachability descriptor on every derived test plan, but it lives in exactly two places and both are ticket-shaped — generate_ticket_from_ac.py appends it and neither fast-lane workflow ever calls the generator (zero grep hits for generate_ticket_from_ac, test_spec, angle or reachability), while templates/agents/test-writer.md carries a fallback mandate whose entry condition is 'Code ticket (a coder agent is needed)' and whose skip rule reads the agents: map, source_ac frontmatter and the ## Test Requirements block, none of which exist on a lane with no ticket. What test-writer actually receives is the ad-hoc prompt at fast-lane-ship.js:333, 'write a minimal failing test that asserts the AC behavior' — precisely the AC-literal-only shape #451 was written to eliminate. This deliberately does not duplicate BO-2900g-1, whose surface is reference_file_path: generate_ticket_from_ac.py with n_location_rule '1' and whose discriminating test introduces a third descriptor-producing route inside the generator: it closes the gap between plan-producing routes within one program, whereas the fast lane produces no plan at all and never loads that program, so BO-2900g-1 built exactly to spec leaves it uncovered. The boundary is recorded in both directions and BO-2400g-1 depends_on BO-2900g-1, because the 'one source, not a lane-local copy' clause is only satisfiable once g-1 has established the single attachment point. GAP 2 (BO-2400g-2, -2-i, -3, -4, -4-i): Coder at fast-lane-ship.js:395 is followed directly by Commit at 471, while the thorough lane runs pr-reviewer (11), change-scope-reviewer, user-surface-smoker (11.5), ac-validator (11.5), ac-fulfillment-gate (11.7), live-surface-tester (11.8) and documentation-verifier (11.9) in that slot. The requirement is deliberately not 'add a review phase': pr-reviewer.md:562 makes sign-off conditional so the agent can run ticketless, but Contract-Aware Mode (102) keys off a ## Agent Contracts section, the contract trace is skipped when that section is absent (193), the delivers_to/expects_from cross-file trace (136) keys off ticket ACs, and the completion_manifest scope_verified check (517) compares the change set against the ticket's files_touched — so dropped in unchanged, every rich mode evaluates to 'skip' silently and leaves a diff skim wearing the name of a review, which is phantom-done one level up and worse than no phase at all because it reads as coverage. The ACs therefore bind the reading to the two inputs the lane already holds at that moment (the resolved connected id set from resolve_connected_build_set at fast_lane.py:498, surfaced as batchIds, and the working diff) and require that any examination which cannot run is named in the run's result rather than quietly skipped. BO-2400g-3 adds the design half of the reading (find-design-smells / find-structural-smells, with the depth-1 dispatch constraint noted), BO-2400g-4 gives findings a consequence by reusing the lane's existing release-on-failure path, and BO-2400g-4-i carries non-halting findings onto the pull request. BO-2400g-2 deliberately does NOT depend on BO-2400g-1 — they attach at different phases and neither consumes the other's output — recorded explicitly so the IT PO does not add the edge on aesthetic grounds. GAP 3 (BO-2400e-3, -3-i, -4) placed under the existing git/durability L1 rather than the new one: fast_lane.py:98-125 _update_ac_work_status opens the AC YAML with mode 'w' which truncates at open, so an interrupted run leaves a zero-byte source-of-truth file, and yaml.safe_dump defaults to sort_keys=True so every write alphabetically re-keys the whole file; it is called three times per AC per run (lines 203, 261, 376), meaning an 11-leaf build set triggers roughly 30 whole-file rewrites. Both stated as behaviour, never as an implementation instruction. Parent edits are additive: BO-2400.yaml gains BO-2400g in covered_by and its child_limit_override moves 6 to 7 (7 is also the default _MAX_L1_PER_L0, so the L0 is now full by both measures and an eighth L1 needs a tree split — recorded in the comment); BO-2400e.yaml gains BO-2400e-3 and BO-2400e-4. The commit also merges origin/main (PR #451) into the branch, which is what makes BO-2900g exist to depend on. Verified independently of the authoring agent: 11 new files counted individually via git status --untracked-files=all; validate_ac_schema.py over an explicit glob reports all 64 files in the tree valid (53 pre-existing plus 11, confirming the new files were really in the set rather than the silent no-op the validator performs on a bare directory argument); check_ac_limits, check_ac_circular_deps and check_ac_parent_covered_by all exit 0 against the staged index; review_system confirmed a real component id at docs/components.json:373; both parent files were rewritten with Write rather than Edit and were diffed in full to confirm no field was dropped. Recorded but explicitly out of scope: the fast lane also skips ac-validator, ac-fulfillment-gate, user-surface-smoker, live-surface-tester and documentation-verifier, noted in BO-2400g's notes as an open question with the observation that user-surface-smoker's absence may already contradict declares_side_effect and BP-1100f-5. Nothing is readiness: approved and none of it carries assigned_agent or it_requirements — the IT PO enrichment pass has not run, so none of it is buildable yet."
pr: 452
commits:
  - af363475a
breaking: false
---

## Entry

See description.
