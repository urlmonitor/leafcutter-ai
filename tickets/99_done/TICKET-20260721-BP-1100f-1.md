---
ac_traceability:
  id: BP-1100f-1
  path: docs/acceptance-criteria/build_pipeline/BP-1100-phantom-done-prevention/BP-1100f-1.yaml
advances_current_outcome: true
agents:
  architect-review: signed_off
  commit: signed_off
  llm-expert: signed_off
  pr-reviewer: signed_off
  pull-request: needed
change_target: pipeline
complexity: low
components:
- build_pipeline
created: '2026-07-21'
depends_on: []
files_touched:
- templates/agents/pr-reviewer.md
priority: high
requires_adr: false
requires_diagram: false
risk_surface: contract_boundary
roadmap_phase: phase_1
source_ac: BP-1100f-1
status: done
title: A durable-change dispatch that carries no instruction is flagged before it
  is accepted
---

# A durable-change dispatch that carries no instruction is flagged before it is accepted

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BP-1100f-1` — A durable-change dispatch that carries no instruction is flagged before it is accepted — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BP-1100f-1`. Component: `build-pipeline`. Assigned agent: `llm-expert`. Estimated complexity: `S`. Complexity: `low`.

## Acceptance Criteria

```gherkin
Given a workflow step dispatches an automated helper to produce a durable change
  (a file written to disk, or an equivalent lasting artifact),
And the dispatch request's first argument is a bare data payload — an object or an
  empty value — carrying no instruction text,
When the pre-implementation dispatch review examines the step,
Then the step is flagged and is not accepted, and the finding names the dispatch as
  carrying no actionable instruction,
And a dispatch whose first argument is a non-empty instruction string describing the
  change to make passes this review unflagged.
```

- [ ] AC-1: the dispatch request's first argument is a bare data payload — an object or an
- [ ] AC-2: the step is flagged and is not accepted, and the finding names the dispatch as
- [ ] AC-3: a dispatch whose first argument is a non-empty instruction string describing the

## Implementation Notes

```yaml
- 'Implementation surface is the pr-reviewer agent template prose (templates/agents/pr-reviewer.md):
  add a dispatch-review lens, not code.'
- The lens flags any agent()-style dispatch whose first argument is not a non-empty
  instruction string (a bare object, a null/empty value, or an omitted first argument)
  as carrying no actionable instruction; a first argument that is a non-empty instruction
  string describing the change passes unflagged.
- State the check in PORTABLE terms per ADR-001 — the rule must live in the packaged
  template and reach any consumer that runs build.py; it must NOT depend on the leafcutter
  repo's own root CLAUDE.md.
- Maintain SPEC PARITY with the harness-level predicate in BP-1100f-4 (the same 'instruction-less
  first argument' definition). The .md lens and the .py harness cannot share code,
  so mirror the single normative definition and note the drift risk rather than implying
  a code import bridges them.
- 'Deployed/template parity per ADR-001: edit the packaged template source and verify
  via the build.py round-trip; do not edit only the deployed copy.'
```

## Sign-offs

- [x] architect-review
- [x] pr-reviewer
- [x] llm-expert — 2026-07-21 14:30
- [x] commit
- [ ] pull-request

## Comments

### 2026-07-21 14:30 — llm-expert (status: ok)
feedback-id: fb_2026-07-21_12d063bf
completion_manifest:
  template_written: true
  prompt_quality_checklist_passed: true
  convention_violations_resolved: true
Added Step 2b — Dispatch Instruction Lens to `templates/agents/pr-reviewer.md`. The lens classifies any `agent()`-style dispatch aimed at a durable change whose first argument is a bare data object, null/omitted, empty string, or whitespace-only string as a high-confidence finding; a non-empty non-whitespace instruction string passes unflagged. Covers BP-1100f-1 (bare-object / omitted) and BP-1100f-1-i (whitespace-only edge case) in one lens block. Spec-parity note with the BP-1100f-4 harness included. Pre-existing hook violation: `depends_on: BP-1100f` in this ticket references a file absent from the folder — this is a ticket-generator artefact, not introduced by this edit.
