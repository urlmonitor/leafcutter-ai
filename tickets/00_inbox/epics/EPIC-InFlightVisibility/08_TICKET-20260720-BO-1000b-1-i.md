---
ac_traceability:
  id: BO-1000b-1-i
  path: docs/acceptance-criteria/build-orchestration/BO-1000-live-automation-progress/BO-1000b-1-i.yaml
advances_current_outcome: true
agents:
  commit: needed
  pr-reviewer: needed
  pull-request: needed
  python-coder: needed
  test-runner: needed
  test-writer: needed
change_target: pipeline
complexity: low
components:
- build-orchestration
created: '2026-07-20'
depends_on:
- BO-1000b-1
files_touched:
- templates/workflows-js/finalize-feature.js
priority: medium
requires_adr: false
requires_diagram: false
risk_surface: internal
roadmap_phase: phase_1
source_ac: BO-1000b-1-i
status: todo
title: A skipped step records a skipped outcome so the per-step record has no gaps
---

# A skipped step records a skipped outcome so the per-step record has no gaps

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BO-1000b-1-i` — A skipped step records a skipped outcome so the per-step record has no gaps — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BO-1000b-1-i`. Component: `build-orchestration`. Assigned agent: `python-coder`. Estimated complexity: `S`. Complexity: `low`.

## Acceptance Criteria

```gherkin
Given a step is skipped because its outcome was already satisfied,
When finalize records that step's result,
Then the step's recorded outcome is "skipped" together with the reason it was
  already satisfied,
And every step in the sequence has a recorded outcome — executed or skipped —
  so the collected per-step outcome record contains no gaps.
```

- [ ] AC-1: the step's recorded outcome is "skipped" together with the reason it was
- [ ] AC-2: every step in the sequence has a recorded outcome — executed or skipped —

## Test Requirements

```yaml
tests:
- name: test_skipped_step_records_skipped_outcome_with_reason
  file: unit_tests/workflows/test_bo_1000b_1_i.py
  covers:
  - BO-1000b-1-i
  asserts: When a step is skipped because its outcome was already satisfied, its recorded
    outcome is 'skipped' together with the already-satisfied reason.
  framework: unittest
  type: behavioral
- name: test_per_step_record_has_no_gaps
  file: unit_tests/workflows/test_bo_1000b_1_i.py
  covers:
  - BO-1000b-1-i
  asserts: After a run, every step in the declared sequence has exactly one recorded
    outcome (executed or skipped) — the collected per-step record has no missing positions
    and no duplicates.
  framework: unittest
  type: behavioral
```

## Implementation Notes

```yaml
config_schema_fragment: null
n_location_rule: all
notes: "Policy constraints:\n- A skipped step (BO-1000a-3 branch) must write a \"\
  skipped\" entry to the same\n  in-order per-step record produced by BO-1000b-1,\
  \ carrying the\n  already-satisfied reason.\n- The completeness invariant: after\
  \ a run, every step in the declared\n  sequence has exactly one recorded outcome\
  \ (executed or skipped) — the\n  record has no gaps and no duplicate entries. Assert\
  \ this against the\n  declared step count (the same source of truth as BO-1000a-2's\
  \ N).\n- No new config key. ADR-001 build.py round-trip parity.\n"
post_write_commands:
- python scripts/build.py --target-dir .
reference_file_path: templates/workflows-js/finalize-feature.js
required_skills:
- python-coder
```

## Agent Contracts

### Expects From

- **AC:** BO-1000b-1
- **Contract:** The in-order per-step outcome record from BO-1000b-1, into which the skipped entry (with reason) is written so no step position is left without an outcome.

## Sign-offs

- [ ] test-writer
- [ ] python-coder
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit
- [ ] pull-request

## Comments
