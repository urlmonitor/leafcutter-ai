---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  pr-reviewer: needed
  pull-request: needed
  python-coder: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
components:
- ac_store
created: '2026-06-11'
depends_on:
- ACS-500a-3
files_touched:
- docs/reference/ac-schema.md
- docs/architecture/adrs/ADR-007-ac-store-schema-id-format-enforcement.md
- config/ac_store_schema.json
priority: high
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: ACS-500a-3-i
status: todo
title: pattern_bindings with missing keys are rejected at commit time
---

# pattern_bindings with missing keys are rejected at commit time

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `ACS-500a-3-i` — pattern_bindings with missing keys are rejected at commit time — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `ACS-500a-3-i`. Component: `ac-store`. Assigned agent: `python-coder`. Estimated complexity: `S`.

## Acceptance Criteria

```gherkin
Given a pattern AC (PTN-001) has criteria containing three parameterized
  slots: {entity_type}, {columns}, and {default_sort},
And a consuming AC has implements_pattern: "PTN-001" with pattern_bindings
  containing only two keys: {entity_type: "orders", columns: ["id", "date"]},
When the schema validator runs at commit time,
Then the commit is blocked with an error stating:
  "pattern_bindings missing required key 'default_sort' for pattern PTN-001"
And the error names both the consuming AC file and the missing key.
```

## Sign-offs

- [ ] python-coder
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit
- [ ] pull-request

## Comments
