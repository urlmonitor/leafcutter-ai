---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  pr-reviewer: needed
  pull-request: needed
  python-coder: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
components:
- build_pipeline
created: '2026-06-22'
depends_on:
- BP-900e
- BP-900e-1
files_touched:
- docs/acceptance-criteria/build_pipeline/BP-900-deployment-completeness/BP-900e.yaml
- templates/workflows-js/finalize-feature.js
- docs/acceptance-criteria/build_pipeline/BP-1000-source-template-parity/BP-1000b.yaml
- docs/acceptance-criteria/build_pipeline/BP-900-deployment-completeness/BP-900b.yaml
- docs/architecture/adrs/ADR-001-self-hosting-boundary.md
- docs/architecture/adrs/ADR-013-portable-skill-script-deployment-boundary.md
priority: high
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: BP-900e-5
status: todo
title: The registry-completeness check fires at the finalize merge gate, not the build
  preflight
---

# The registry-completeness check fires at the finalize merge gate, not the build preflight

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BP-900e-5` — The registry-completeness check fires at the finalize merge gate, not the build preflight — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BP-900e-5`. Component: `build-pipeline`. Assigned agent: `python-coder`. Estimated complexity: `M`.

## Acceptance Criteria

```gherkin
Given an epic branch is being finalized for merge
And the branch contains a hook registered in commit_guardian.json with no
  deploy-source copy under templates/scripts/
When the finalize-feature merge flow runs its gates
Then the registry-completeness check runs in the same enforcement position as
  the existing source-to-template parity gate (after merge-origin/main, before
  the pull-request merge step)
And on a registry-completeness violation the finalize flow HALTs and surfaces
  the violation to the user, never reaching the pull-request merge step
And the registry-completeness check is NOT run as part of the build.py
  preflight (the build.py preflight script-reference guard, which scans
  template references against the deployable manifest, remains a separate
  PREFLIGHT check that does not read commit_guardian.json)
And when the branch has no registry-completeness violation the finalize flow
  proceeds past this gate to the pull-request merge step
```

## Sign-offs

- [ ] python-coder
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit
- [ ] pull-request

## Comments
