---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  pr-reviewer: needed
  pull-request: needed
  python-coder: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
components:
- testing-quality
created: '2026-06-24'
depends_on: []
files_touched:
- scripts/ac_store/test_enforcement.py
- tests/testing_quality/test_ac_linkage.py
priority: high
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: TQ-100c-1
status: todo
title: A test with no covers tag is enforced by default, requiring no backfill
---

# A test with no covers tag is enforced by default, requiring no backfill

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `TQ-100c-1` — A test with no covers tag is enforced by default, requiring no backfill — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `TQ-100c-1`. Component: `testing-quality`. Assigned agent: `python-coder`. Estimated complexity: `S`.

## Acceptance Criteria

```gherkin
Given a test function that carries no "# covers: <AC-ID>" tag at all,
When the suite is run and that test fails,
Then the test is enforced (its failure counts as a real failure and drives the
  overall run outcome non-passing)
And no tag has to be added to the test for it to be enforced
And turning the AC-status-gated enforcement on does not require any bulk
  re-tagging of the existing untagged suite for those tests to keep blocking.
```

## Test Requirements

- `test_untagged_test_enforced_by_default`: A failing test with no '# covers:' tag is enforced by default, requiring no backfill tag.

## Sign-offs

- [ ] python-coder
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit
- [ ] pull-request

## Comments
