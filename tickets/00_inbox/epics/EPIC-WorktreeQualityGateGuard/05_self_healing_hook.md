---
title: "Self-Healing Hook"
status: todo
components:
  - build-orchestration
created: 2026-07-02
depends_on:
  - 02_probe_core_canary_git_common_dir.md
priority: high
ac_traceability:
  - BO-1700c-1
  - BO-1700c-1-i
  - BO-1700c-1-ii
  - BO-1700c-1-iii
  - BO-1700c-1-iv
files_touched:
  - scripts/commit_guardian/ensure_precommit_config.py
  - templates/scripts/commit_guardian/ensure_precommit_config.py
  - scripts/commit_guardian/commit_guardian.json
  - templates/scripts/commit_guardian/commit_guardian.json
  - unit_tests/commit_guardian/test_ensure_precommit_config.py
agents:
  python-coder: needed
  test-writer: needed
  pr-reviewer: needed
---

# 05: Self-Healing Hook

## Goal
Implement ensure_precommit_config.py, register it as the first (index-0) hook in the commit-guardian manifest, and guarantee that any commit re-materializes a missing .pre-commit-config.yaml via symlink or copy.

## Context
Stub generated by create-epic. This ticket implements the self-healing mechanism:
- ensure_precommit_config.py: re-materializes .leafcutter symlink or copies .pre-commit-config.yaml
- Registered at manifest index 0 (FIRST, before all other hooks)
- Atomic and idempotent (safe to call repeatedly)
- Uses git-common-dir resolver from ticket 02 to locate main tree
- Templates mirror for ADR-001 parity
- This is the SINGLE live config-establishment path (supersedes interim SKILL.md slice)

## Acceptance Criteria
(To be filled in by create-ticket hardening pass.)

## Sign-offs
- [ ] python-coder
- [ ] test-writer
- [ ] pr-reviewer

## Comments

## Implementation Tasks

### python-coder
- [ ] Create ensure_precommit_config.py (scripts/commit_guardian/)
  - On any commit, check if .pre-commit-config.yaml resolves to a readable file
  - If missing/unreadable:
    - Try to create a .leafcutter symlink to the main tree's .leafcutter
      (using git-common-dir resolution from ticket 02)
    - If symlink fails (NTFS/Windows), copy the main tree's .pre-commit-config.yaml instead
  - Exit 0 only if config is resolvable after the hook runs
  - Exit non-zero if re-materialization fails
  - Idempotent: safe to call multiple times (config already exists → no-op, exit 0)
  - Atomic: symlink/copy operation must complete fully or fail completely
- [ ] Register in commit_guardian.json at index 0 (FIRST hook)
  - Canonical source is templates/scripts/commit_guardian/commit_guardian.json
  - MUST be listed before all other hooks
  - Assert manifest index 0 in tests
- [ ] Create templates/ mirror: templates/scripts/commit_guardian/ensure_precommit_config.py
  - Verify byte-for-byte parity via build.py round-trip per ADR-001
- [ ] Integrate with build_precommit.py
  - ensure_precommit_config entry is emitted into generated .pre-commit-config.yaml

### test-writer
- [ ] Add unit tests in unit_tests/commit_guardian/test_ensure_precommit_config.py
  - Test config already exists → no-op, exit 0
  - Test config missing, symlink succeeds → .leafcutter created, exit 0
  - Test config missing, symlink fails, copy succeeds → config copied, exit 0
  - Test config missing, both symlink and copy fail → exit non-zero
  - Test idempotency: call twice, both succeed
  - Test atomicity: if operation fails halfway, state is clean
  - Test manifest index 0: assert ensure_precommit_config is registered first

## Risk & Safety
- Touches money? No.
- Touches data? No.
- Reversibility? Yes — hook only adds missing config; removes itself from manifest on disable.
