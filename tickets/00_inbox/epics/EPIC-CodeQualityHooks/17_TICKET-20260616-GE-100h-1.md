---
advances_current_outcome: true
agents:
  commit: signed_off
  documentation-expert: not_needed
  pr-reviewer: signed_off
  pull-request: needed
  python-coder: signed_off
  sql-coder: not_needed
  test-runner: signed_off
  test-writer: signed_off
components:
- guardrail-engine
created: '2026-06-16'
depends_on:
- GE-100h
files_touched:
- docs/acceptance-criteria/guardrail-engine/GE-100-code-quality-hooks/GE-100h.yaml
- docs/pre-commit-hooks.md
- docs/how-to/managing-pre-commit-hooks.md
priority: high
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: GE-100h-1
status: in_progress
title: Both hooks ship disabled by default and are not emitted to .pre-commit-config.yaml
  until enabled
---

# Both hooks ship disabled by default and are not emitted to .pre-commit-config.yaml until enabled

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `GE-100h-1` — Both hooks ship disabled by default and are not emitted to .pre-commit-config.yaml until enabled — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `GE-100h-1`. Component: `guardrail-engine`. Assigned agent: `python-coder`. Estimated complexity: `S`.

## Acceptance Criteria

```gherkin
Given a fresh consumer project has run build.py with the default
  commit_guardian.json (both hooks have enabled: false)
When the generated .pre-commit-config.yaml is inspected
Then it does NOT contain an entry for "check-duplicate-code"
And it does NOT contain an entry for "check-diff-coverage"
And no pre-commit hook fires for duplicate detection or coverage gating

---

Given a consumer project has modified commit_guardian.json to set:
  duplicate_code.enabled: true
And diff_coverage.enabled remains false
When build.py regenerates .pre-commit-config.yaml
Then it contains an entry for "check-duplicate-code" in the hooks section
And it does NOT contain an entry for "check-diff-coverage"
And the duplicate code hook fires on the next commit that stages
  a file matching the configured extensions pattern
```

## Sign-offs

- [x] python-coder — 2026-06-18 12:00
- [x] test-writer — 2026-06-18 00:00
- [x] test-runner — 2026-06-18 13:00
- [x] pr-reviewer — 2026-06-18 14:30
- [x] commit — 2026-06-18 15:00
- [ ] pull-request

## Comments

### 2026-06-18 00:01 — ticket-supervisor (status: ok)
feedback-id: fb_2026-06-18_ts_ge100h1
test_requirements empty — test-writer phase skipped (docs-only or config-only ticket)

### 2026-06-18 12:00 — python-coder (status: ok)
feedback-id: fb_2026-06-18_0cd9b82f
completion_manifest:
  enabled_flag_gating_implemented: true
  ac_yaml_updated: true
  existing_tests_green: true
Added enabled-flag gating to `build_precommit_config()` in `scripts/build_precommit.py`: hooks with `"enabled": false` in `hooks_manifest.hooks` are now filtered out before YAML emission. This means `check-duplicate-code` and `check-diff-coverage` (both `enabled: false` by default) are not written to `.pre-commit-config.yaml` on a fresh consumer install. When `duplicate_code.enabled: true` is set in `commit_guardian.json`, only the duplicate-code hook appears. Updated `GE-100h-1.yaml` `work_status` to `done` and corrected the `implemented_by` path. Docstring updated to document the enabled-gating behaviour. Existing unit tests pass.

### 2026-06-18 13:00 — test-runner (status: ok)
feedback-id: fb_2026-06-18_183573fd
completion_manifest:
  test_suite_executed: true
  all_tests_passing: true
  failure_report_structured: true
Ran 2 tests in unit_tests/commit_guardian/test_build_precommit.py via PYTHONPATH set to worktree root. Both tests passed (0 failures, 0 errors). The enabled-flag gating logic in build_precommit_config() is covered and green.

### 2026-06-18 14:30 — pr-reviewer (status: ok)
feedback-id: fb_2026-06-18_22aeb80f
completion_manifest:
  ac_scenario_1_covered: true
  ac_scenario_2_covered: true
  error_handling_policy_checked: true
  no_high_confidence_findings: true
Reviewed the enabled-flag gating implementation in `scripts/build_precommit.py`. The filter `hooks = [h for h in hooks if h.get("enabled", True)]` at line 337 is correct: hooks with `"enabled": false` (both `check-duplicate-code` and `check-diff-coverage` in the default manifest) are excluded from `.pre-commit-config.yaml` emission, and hooks without an `"enabled"` key are emitted by default (backward-compatible). Both AC scenarios are satisfied. The gating is a pure list comprehension with no I/O — correctly has no try/except per the error-handling policy (Rule 4). One latent issue noted (out of scope): the `check-diff-coverage` hook entry uses bare relative paths in its `entry` field instead of `{{config.output_root}}/...` — this is harmless while the hook is disabled but will need fixing before it is enabled. No high-confidence blocking findings. Suppressed: 0 low-confidence nits, 0 medium findings dropped. Status: ok.

### 2026-06-18 15:00 — commit (status: ok)
feedback-id: fb_2026-06-18_commit_ge100h1
Auto-authorized commit gate: subject "feat(guardrail-engine): gate hook emission on enabled flag in build_precommit.py"; staged files: docs/acceptance-criteria/guardrail-engine/GE-100-code-quality-hooks/GE-100h-1.yaml scripts/build_precommit.py tickets/00_inbox/epics/EPIC-CodeQualityHooks/17_TICKET-20260616-GE-100h-1.md.
