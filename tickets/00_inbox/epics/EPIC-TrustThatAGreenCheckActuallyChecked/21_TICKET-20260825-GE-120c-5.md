---
ac_traceability:
  id: GE-120c-5
  path: docs/acceptance-criteria/guardrail-engine/GE-120-green-means-checked/GE-120c-5.yaml
advances_current_outcome: true
agents:
  architecture-diagram-author: needed
  commit: needed
  documentation-expert: needed
  documentation-verifier: needed
  pull-request: not_needed
change_target: docs
complexity: low
components:
- documentation_system
- testing_quality
created: '2026-08-25'
depends_on:
- 12_TICKET-20260825-GE-120c-1.md
documentation_required: true
files_touched:
- docs/architecture/components/commit-guardian.md
- docs/architecture/components/worktree-manager.md
- templates/scripts/commit_guardian/commit_guardian.json
- unit_tests/commit_guardian
- unit_tests/portability
priority: high
requires_adr: false
requires_diagram: false
risk_surface: internal
roadmap_phase: phase_1
source_ac: GE-120c-5
status: todo
title: The new verification surface is drawn as an architecture component
---

# The new verification surface is drawn as an architecture component

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `GE-120c-5` — The new verification surface is drawn as an architecture component — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `GE-120c-5`. Component: `guardrail-engine`. Assigned agent: `architecture-diagram-author`. Estimated complexity: `S`. Complexity: `low`.

## Acceptance Criteria

```gherkin
Given the harness introduced by GE-120c-1 is a new architectural piece rather than a change
  to an existing one,
When a reader opens the guardrail engine's architecture documentation,
Then a component diagram shows the harness, the separate working copy it stands up, the
  deployed check processes it invokes, and the hook manifest it compares its coverage
  against,
And the diagram distinguishes the harness's out-of-process invocation path from the existing
  in-process unit tests that import checks from the source tree,
And it states which of those two paths can observe the defect class this tree addresses and
  which cannot,
And the boundary that the source tree does not cross into the process under test is shown,
  since that boundary is the property the harness exists to enforce,
And the diagram is reachable from the guardrail engine's component documentation rather than
  filed only under testing.
```

- [ ] AC-1: a component diagram shows the harness, the separate working copy it stands up, the
- [ ] AC-2: the diagram distinguishes the harness's out-of-process invocation path from the existing
- [ ] AC-3: it states which of those two paths can observe the defect class this tree addresses and
- [ ] AC-4: the boundary that the source tree does not cross into the process under test is shown
- [ ] AC-5: the diagram is reachable from the guardrail engine's component documentation rather than

## Implementation Notes

```yaml
- 'HOME: docs/architecture/components/commit-guardian.md, with the diagram reachable
  from that component page — not filed only under a testing document, which is the
  criterion''s last clause. docs/architecture/components/worktree-manager.md is the
  related page for the copy-creation path and should cross-link.'
- 'SATISFY THE DIAGRAM GATES IN THE SAME CHANGE: check_mermaid_drift, check_mermaid_parent_link,
  check_mermaid_complexity and check_diagram_naming all fire on diagram commits. Parent-link
  and naming conventions are enforced mechanically, so a diagram that reads well but
  breaks them will not land.'
- 'NODES THE DIAGRAM MUST CONTAIN, drawn from the harness AS BUILT rather than as
  intended: the harness in unit_tests/portability/; the second working copy it creates;
  build.py producing the .leafcutter deployed layout inside that copy; the run_hook.py
  subprocess invocation of each deployed check (plus the direct-script shape used
  by precommit-canary); and hooks_manifest.hooks[] in templates/scripts/commit_guardian/commit_guardian.json
  as the coverage source.'
- THE LOAD-BEARING ELEMENT IS THE CONTRAST. Show the existing in-process unit tests
  (unit_tests/commit_guardian/, importing from the source tree) BESIDE the out-of-process
  path, and mark explicitly which one can observe this defect class and which cannot.
  A diagram without that contrast does not satisfy the criterion — the reason the
  defect survived is that a reasonable engineer saw a directory full of passing tests
  and concluded the checks were covered.
- Draw the source-tree boundary that does not cross into the process under test as
  an EXPLICIT boundary element, since that boundary is the single property the harness
  exists to enforce.
- SEQUENCE AFTER GE-120c-1 IS BUILT. A diagram of the author's intention has to be
  corrected later, and in practice is not.
```

## Agent Contracts

### documentation-expert

Existing docs to update / cross-link:

- docs/architecture/components/commit-guardian.md (relationship: modifies | status: exists)
- docs/architecture/components/worktree-manager.md (relationship: related | status: exists)
- docs/architecture/adrs/ADR-001-self-hosting-boundary.md (relationship: context | status: exists | relevance: The template / runtime / deployed split the diagram must render, since the harness exists to test the deployed side.)

- [ ] AC-1: component-diagram | docs/architecture/components/commit-guardian.md | a component diagram shows the harness, the separate working copy it stands up, the

### Expects From

- **AC:** GE-120c-1
- **Contract:** The harness's actual structure — copy creation, invocation path, and manifest comparison — to draw rather than to guess at.

## Sign-offs

- [ ] documentation-expert
- [ ] documentation-verifier
- [ ] architecture-diagram-author
- [ ] commit

## Comments
