---
advances_current_outcome: true
agents:
  commit: signed_off
  documentation-expert: not_needed
  pr-reviewer: needed
  pull-request: signed_off
  python-coder: signed_off
  sql-coder: not_needed
  test-runner: signed_off
  test-writer: signed_off
change_target: code
complexity: low
components:
- build_orchestration
created: '2026-07-15'
depends_on:
- 01_TICKET-20260715-BO-2200a-1.md
files_touched:
- scripts/ac_store/generate_ticket_from_ac.py
priority: medium
requires_adr: false
requires_diagram: false
risk_surface: internal
roadmap_phase: phase_1
source_ac: BO-2200a-4
status: todo
title: A list-valued change target triggers documentation if any element triggers
  (union semantics)
---

# A list-valued change target triggers documentation if any element triggers (union semantics)

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BO-2200a-4` — A list-valued change target triggers documentation if any element triggers (union semantics) — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BO-2200a-4`. Component: `build-orchestration`. Assigned agent: `python-coder`. Estimated complexity: `S`. Complexity: `low`.

## Acceptance Criteria

```gherkin
Given the declarative `documentation_gates` policy and the ticket generator building
  the agents map for a leaf AC whose `change_target` is a list,
When `change_target` is `[config, code]` (no element in the triggering set) and
  `risk_surface` is `internal`,
Then the generated agents map does NOT include `documentation-expert`.
And when `change_target` is `[config, ui]` (at least one element, `ui`, is in the
  triggering set), the generated agents map DOES include `documentation-expert`.
And the policy evaluates a list-valued `change_target` as the union of its elements:
  the documentation demand is raised when ANY element is in the triggering set.
```

## Test Requirements

```yaml
tests:
- name: test_list_change_target_no_triggering_element_no_doc_expert
  file: unit_tests/test_bo_2200a_4.py
  covers:
  - BO-2200a-4
  asserts: change_target=[config, code] with risk_surface=internal yields no documentation-expert.
  framework: unittest
  type: behavioral
- name: test_list_change_target_any_triggering_element_requires_doc_expert
  file: unit_tests/test_bo_2200a_4.py
  covers:
  - BO-2200a-4
  asserts: change_target=[config, ui] yields documentation-expert (union semantics
    — any element in the triggering set triggers).
  framework: unittest
  type: behavioral
```

## Implementation Notes

```yaml
config_schema_fragment:
  change_target:
    oneOf:
    - type: string
    - items:
        type: string
      type: array
constraints:
- change_target may be a scalar or a list per the AC schema; normalise a scalar to
  a single-element list before evaluating.
- Apply union semantics across list elements so a multi-target AC is handled without
  crashing and triggers documentation-expert when ANY element is in the triggering
  set.
n_location_rule: '1'
post_write_commands: []
reference_file_path: scripts/ac_store/generate_ticket_from_ac.py
required_skills:
- python-coder
```

## Sign-offs

- [x] python-coder — 2026-07-20 20:48
- [x] test-writer — 2026-07-20 15:00
- [x] test-runner — 2026-07-20 20:58
- [ ] pr-reviewer
- [x] commit — 2026-07-20 21:05
- [x] pull-request — 2026-07-20 21:20

## Comments

### 2026-07-20 20:48 — python-coder (status: ok)
feedback-id: fb_2026-07-20_2a2fd878
completion_manifest:
  union_semantics_fix_implemented: true
  red_baseline_tests_green: true
  doc_enforcer_pass: true
  real_artifact_spot_check: true
red_baseline_results:
  - test_name: test_list_change_target_any_triggering_element_requires_doc_expert
    result: green
  - test_name: test_list_change_target_no_triggering_element_no_doc_expert
    result: green
Fixed the union-semantics bug in `_build_agents_map` Dimension 3 (non_triggering_classifications): replaced the old per-entry `entry_ct in change_targets` check with a set-intersection approach that collects all `suppressed_targets` (change_target values covered by non_triggering entries for the current risk_surface) and discards `documentation-expert` only when EVERY triggering change_target element (those in `doc_change_triggers`) is covered by a suppressed_targets entry. A non_triggering match for one list element (e.g. config/internal) can no longer cancel the trigger raised by a different element (e.g. ui). Also added MODULE/GOAL/BUSINESS CONTEXT/ARCHITECTURE doc-enforcer headers to the module docstring and appended a DECISION HISTORY entry. Real-artifact spot-check verified against the actual guardrail_gates.yaml: [config,code]/internal produces no documentation-expert, [config,ui]/internal produces documentation-expert=needed.

### 2026-07-20 15:00 — test-writer (status: ok)
feedback-id: fb_2026-07-20_4a99c93f
completion_manifest:
  tests_written: true
  tests_red_verified: true
  red_baseline_captured: true

## Test Writer — Completion Report

### Tests Written
| File | Directory | Framework | Status |
|---|---|---|---|
| test_bo_2200a_4.py | unit_tests/ | unittest | written |

### Verification Run
- Command: `python -m unittest /home/henzeh/projects/leafcutter/worktrees/BO-2200-doc-coverage/unit_tests/test_bo_2200a_4.py -v`
- Result: red (1 failure — expected; union-semantics fix not yet implemented)

### Notes
- `test_list_change_target_no_triggering_element_no_doc_expert` passes immediately (PASS) — negative case where neither element in [config, code] is a trigger; flagged as under-specified in red_baseline.
- `test_list_change_target_any_triggering_element_requires_doc_expert` fails (RED) — exposes the union-semantics bug: non_triggering_classifications incorrectly discards documentation-expert for [config, ui]/internal when config is in non_triggering, even though ui independently triggers it.
- Also fixed ticket frontmatter: added missing `change_target: code` and `risk_surface: internal`, corrected `depends_on` to reference the actual ticket file `01_TICKET-20260715-BO-2200a-1.md` (was using AC IDs, which the frontmatter guard rejects).

red_baseline:
  - test_name: test_list_change_target_any_triggering_element_requires_doc_expert
    file: unit_tests/test_bo_2200a_4.py
    error: "AssertionError: 'needed' != None : Expected documentation-expert='needed' when change_target=['config', 'ui'] because 'ui' IS in change_target_triggers [ui, schema, pipeline, docs]. The union-semantics rule requires that a non_triggering entry for (config, internal) must NOT cancel the trigger raised by the separate 'ui' element."
  - test_name: test_list_change_target_no_triggering_element_no_doc_expert
    file: unit_tests/test_bo_2200a_4.py
    error: "passes immediately"
    note: "passes immediately — negative case (no element triggers); under-specified for the union aspect but included as regression guard"

### 2026-07-20 20:58 — test-runner (status: ok)
feedback-id: fb_2026-07-20_4370113c
completion_manifest:
  test_suite_executed: true
  all_tests_passing: true
  failure_report_structured: true
Ran 2 tests in unit_tests/test_bo_2200a_4.py (unittest): both PASS. Union-semantics fix confirmed green — test_list_change_target_any_triggering_element_requires_doc_expert and test_list_change_target_no_triggering_element_no_doc_expert both pass against the fixed _build_agents_map implementation.

### 2026-07-20 21:05 — commit (status: ok)
feedback-id: fb_2026-07-20_a0bdc85b
completion_manifest:
  pre_commit_hooks_pass: true
  commit_message_valid: true
  ticket_staged: true
Auto-authorized commit gate: subject "fix(build-orchestration): list change_target union semantics in doc gate"; staged files: scripts/ac_store/generate_ticket_from_ac.py, tickets/00_inbox/epics/EPIC-DocumentationCoverageGuarantee/05_TICKET-20260715-BO-2200a-4.md, tickets/00_inbox/epics/EPIC-DocumentationCoverageGuarantee/10_TICKET-20260715-BO-2200b-2-i.md, tickets/00_inbox/epics/EPIC-DocumentationCoverageGuarantee/12_TICKET-20260715-BO-2200b-3-i.md, unit_tests/test_bo_2200a_4.py.

### 2026-07-20 21:20 — pull-request (status: ok)
feedback-id: fb_2026-07-20_a5eb0709
completion_manifest:
  branch_pushed: true
  pr_created: true
  pr_body_complete: true
Pushed commit 26cc75a5 ("fix(build-orchestration): list change_target union semantics in doc gate") to existing epic PR #337 (feature/BO-2200-doc-coverage → main). PR url: https://github.com/urlmonitor/leafcutter-ai/pull/337. Merge state: MERGEABLE/BLOCKED (CI checks pending — expected). urlmonitor account active.
