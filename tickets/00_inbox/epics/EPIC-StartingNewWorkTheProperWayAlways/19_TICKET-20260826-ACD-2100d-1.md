---
ac_traceability:
  id: ACD-2100d-1
  path: docs/acceptance-criteria/ac-driven-dev/ACD-2100-entry-point-unblocked/ACD-2100d-1.yaml
advances_current_outcome: true
agents:
  ac-fulfillment-gate: needed
  ac-validator: needed
  architect-review: needed
  commit: needed
  documentation-expert: needed
  documentation-verifier: needed
  pr-reviewer: needed
  pull-request: needed
  python-coder: needed
  test-runner: needed
  test-writer: needed
change_target: pipeline
complexity: high
components:
- ac_driven_dev
- build_pipeline
created: '2026-08-26'
depends_on:
- 06_TICKET-20260826-ACD-2100a-5.md
documentation_required: true
files_touched: []
priority: medium
requires_adr: false
requires_diagram: false
risk_surface: contract_boundary
roadmap_phase: phase_1
source_ac: ACD-2100d-1
status: todo
title: The installed copy of the route reaches the first user question exactly as
  the source copy does
---

# The installed copy of the route reaches the first user question exactly as the source copy does

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `ACD-2100d-1` — The installed copy of the route reaches the first user question exactly as the source copy does — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `ACD-2100d-1`. Component: `ac-driven-dev`. Assigned agent: `python-coder`. Estimated complexity: `L`. Complexity: `high`.

## Acceptance Criteria

```gherkin
Given a project into which the route has been installed by running the installer
  from the current source,
When a run is started in that project using the installed copy and a second run
  is started in the source checkout using the source copy under the same starting
  directory conditions,
Then both runs reach the same point -- the first question the route puts to the
  user,
And neither run stops at a check performed before that point,
And the finding rests on what the two runs did rather than on any comparison of
  the two files.
```

- [ ] AC-1: both runs reach the same point -- the first question the route puts to the
- [ ] AC-2: neither run stops at a check performed before that point
- [ ] AC-3: the finding rests on what the two runs did rather than on any comparison of

## Test Requirements

```yaml
tests:
- name: test_installed_and_source_copies_both_reach_the_first_user_question
  file: unit_tests/portability/test_acd_2100d_1.py
  covers:
  - ACD-2100d-1
  asserts: The installer is run into a temporary target project; a run started there
    against the installed copy and a run started in the source checkout against the
    source copy, under identical starting directory conditions, both reach the first
    question the route puts to the user.
  framework: unittest
  type: e2e
  angle: deployed
- name: test_neither_copy_halts_at_a_check_before_the_first_question
  file: unit_tests/portability/test_acd_2100d_1.py
  covers:
  - ACD-2100d-1
  asserts: Neither run's recorded outcome is a halt at a startup check, and the assertion
    is made against each run's own recorded outcome rather than against its exit code
    alone.
  framework: unittest
  type: e2e
  angle: failure
- name: test_parity_finding_is_produced_by_running_both_copies_not_by_diffing_them
  file: unit_tests/portability/test_acd_2100d_1.py
  covers:
  - ACD-2100d-1
  asserts: The deployed copy is invoked through the entry point a consumer actually
    uses, and the run advances past the startup checks -- so a deployed artifact that
    is byte-identical and still cannot run fails this test.
  framework: unittest
  type: e2e
  angle: reachability
```

## Implementation Notes

```yaml
- Evidence must come from executing the deployed artifact in the deployed layout in
  a fresh process. A file diff, a checksum comparison or a build-manifest assertion
  is explicitly not evidence and does not satisfy this record.
- The installer must be run into a temporary target rather than against a developer's
  own project, so the check is repeatable and leaves nothing behind.
- Both runs must be started under the same directory conditions. The only variable
  is which copy is running -- ACD-2100a's behaviours are directory-sensitive and varying
  two things at once makes a failure unattributable.
- Both runs must complete their startup path with no network available, so a transport
  failure in one run cannot be read as a parity difference.
- This record must not restate what ACD-2100a, b or c require. Its subject is that
  the copy the user runs is the copy that does it; a child that reads as 'sibling
  behaviour X but in the deployed layout' duplicates coverage and splits one behaviour's
  evidence across two L1s.
- Must satisfy the repository error-handling policy in any harness code that ships
  under scripts/ or unit_tests/.
- Any dependency the deployed route needs at runtime must be in the build deploy manifest.
  A dependency that is not deployed raises at runtime while every source-tree test
  passes -- this repo has that failure twice over in CLAUDE.md.
```

## Agent Contracts

### documentation-expert

Existing docs to update / cross-link:

- docs/reference/plan-feature-layout-and-startup-checks.md (relationship: describes | status: planned | relevance: The reference page authored by ACD-2100d-4 states which copy runs where; this record is the behaviour that makes the two copies agree.)
- docs/known-issues/ac-driven-dev.md (relationship: describes | status: exists | relevance: The three blockers this parity behaviour has to hold for.)
- docs/architecture/adrs/ADR-001-self-hosting-boundary.md (relationship: describes | status: exists | relevance: The source-versus-deployed boundary: templates/ is the source and .leafcutter/ is build output.)
- docs/build-pipeline.md (relationship: describes | status: exists | relevance: The installer whose output this record exercises rather than inspects.)
- docs/testing/test-angles.md (relationship: describes | status: exists | relevance: The deployed angle and the BP-811 case it cites -- an AC that asserted the copy tier and never the reachability tier is the exact mistake this record's last clause forbids.)

- [ ] AC-1: [reference-doc] docs/reference/plan-feature-layout-and-startup-checks.md — both runs reach the same point -- the first question the route puts to the

### Delivers To

- **Agent:** python-coder
- **Contract:** A repeatable harness that installs into a temporary target project and starts a run there against the installed copy, observing how far the run gets -- the measurement ACD-2100d-3 takes before and after an install.
- **Agent:** documentation-expert
- **Contract:** The confirmed behaviour of the installed copy on the startup path, which the reference page states as fact rather than as an expectation.

## Sign-offs

- [ ] architect-review
- [ ] test-writer
- [ ] python-coder
- [ ] test-runner
- [ ] documentation-expert
- [ ] pr-reviewer
- [ ] ac-validator
- [ ] ac-fulfillment-gate
- [ ] documentation-verifier
- [ ] commit
- [ ] pull-request

## Comments
