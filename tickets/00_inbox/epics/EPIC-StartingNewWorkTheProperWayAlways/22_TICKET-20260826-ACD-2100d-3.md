---
ac_coverage: 2/3
ac_traceability:
  id: ACD-2100d-3
  path: docs/acceptance-criteria/ac-driven-dev/ACD-2100-entry-point-unblocked/ACD-2100d-3.yaml
advances_current_outcome: true
agents:
  ac-fulfillment-gate: failed
  ac-validator: failed
  architect-review: signed_off
  commit: failed
  documentation-expert: not_needed
  documentation-verifier: not_needed
  pr-reviewer: failed
  pull-request: not_needed
  python-coder: signed_off
  test-runner: signed_off
  test-writer: signed_off
change_target: pipeline
complexity: medium
components:
- ac_driven_dev
- build_pipeline
created: '2026-08-26'
depends_on:
- tickets/00_inbox/epics/EPIC-StartingNewWorkTheProperWayAlways/19_TICKET-20260826-ACD-2100d-1.md
documentation_required: false
files_touched:
- scripts/ci/check_consumer_install.py
- unit_tests/_installed_route_probe.py
- unit_tests/portability/test_acd_2100d_3.py
priority: medium
requires_adr: false
requires_diagram: false
risk_surface: contract_boundary
roadmap_phase: phase_1
source_ac: ACD-2100d-3
status: todo
title: Installing into a project where the route starts leaves the route still able
  to start
---

# Installing into a project where the route starts leaves the route still able to start

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `ACD-2100d-3` — Installing into a project where the route starts leaves the route still able to start — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `ACD-2100d-3`. Component: `ac-driven-dev`. Assigned agent: `python-coder`. Estimated complexity: `M`. Complexity: `medium`.

## Acceptance Criteria

```gherkin
Given a project in which a run reaches the first question the route puts to the
  user before the installer is run,
When the current installer is run against that project,
Then a run started in that project afterwards still reaches that first question,
And it does not stop at any check that did not stop it before the install,
And the finding comes from starting a run in that project after the install
  rather than from inspecting what the installer copied.
```

- [x] AC-1: a run started in that project afterwards still reaches that first question
- [ ] AC-2: it does not stop at any check that did not stop it before the install
- [x] AC-3: the finding comes from starting a run in that project after the install

## Test Requirements

```yaml
tests:
- name: test_the_route_reaches_the_first_question_before_and_after_the_install
  file: unit_tests/portability/test_acd_2100d_3.py
  covers:
  - ACD-2100d-3
  asserts: A temporary target project is measured first -- a run started there reaches
    the first question the route puts to the user -- the installer is then run against
    that same project and the measurement repeated. The assertion is over the PAIR
    of recorded observations; a test that inspects only the second one does not satisfy
    this descriptor.
  framework: unittest
  type: e2e
  angle: criterion
- name: test_a_project_already_broken_before_the_install_does_not_pass_the_guarantee
  file: unit_tests/portability/test_acd_2100d_3.py
  covers:
  - ACD-2100d-3
  asserts: THE TEST THAT MAKES THE BEFORE-MEASUREMENT LOAD-BEARING. Seed a target
    project in which a run halts at a startup check BEFORE the installer runs, install,
    and assert the verdict is precondition-not-met and is distinguishable from a pass.
    Assert in the same test that an after-state-only assertion over this fixture would
    have reported a pass -- that contrast is the evidence the before-measurement is
    doing work rather than being computed and discarded.
  framework: unittest
  type: e2e
  angle: boundary
- name: test_a_startup_halt_the_project_did_not_have_before_the_install_is_reported
  file: unit_tests/portability/test_acd_2100d_3.py
  covers:
  - ACD-2100d-3
  asserts: 'THE LIVE HAZARD, RUN THE WRONG WAY ROUND. Install an artifact that carries
    a startup check the project''s own copy did not carry, and assert the verdict
    is a regression that names the check that now stops the run. This is the currently-true
    case: the deployed workflow has no charter gate and the source does.'
  framework: unittest
  type: e2e
  angle: failure
- name: test_both_findings_come_from_runs_started_in_the_project_not_from_the_copy_list
  file: unit_tests/portability/test_acd_2100d_3.py
  covers:
  - ACD-2100d-3
  asserts: Both measurements are taken by invoking the entry point a consumer uses,
    in a fresh process -- so an install whose copied files are impeccable and whose
    deployed route still cannot start fails this test.
  framework: unittest
  type: e2e
  angle: reachability
```

## Implementation Notes

```yaml
- THE VERDICT MUST BE A RELATION BETWEEN TWO RECORDED OBSERVATIONS of the same project
  -- one taken before the installer runs and one after. It must not be an assertion
  about the after-state alone. An implementation that measures only the after-state
  and compares it to a constant passes on a project that was already broken, which
  is the one outcome this record exists to exclude.
- A PROJECT THAT DOES NOT REACH THE FIRST QUESTION BEFORE THE INSTALL MUST NOT PRODUCE
  A PASS. The Given is a precondition, so the correct verdict there is precondition-not-met,
  reported as such and distinguishable in the output from a pass. This is the mechanically
  checkable form of the requirement above -- it is the single case that separates
  a real before-and-after from an after-only check wearing its name.
- Reuse ACD-2100d-1's harness -- install into a temporary target project and observe
  how far a run started there gets -- rather than writing a second one. Two harnesses
  observing 'how far the run got' by different means will eventually disagree, and
  when they do one of these two records reports green on the condition the other reports
  red. The temporary target must be created and torn down by the harness so the check
  is repeatable and leaves nothing behind; it must never be run against a developer's
  own project.
- Both observations must be taken on the same project under the same starting directory
  conditions. The only variable is that the installer ran in between -- ACD-2100a's
  behaviours are directory-sensitive and varying two things at once makes a regression
  unattributable.
- The observation must be the run's own recorded stopping point, not its exit code.
  A run can exit zero having halted at a startup check, and which check stopped it
  is this record's entire subject.
- Both runs must complete their startup path with no network available, so a transport
  failure in one of the two measurements cannot be read as an install-induced regression.
- The finding must be produced by starting a run in the project after the install.
  An assertion over what the installer copied, a file manifest or a checksum is not
  evidence -- the same real-artifact rule ACD-2100d-1 is held to, for the same reason.
- STANDING GUARANTEE MEANS IT HAS TO KEEP RUNNING. Wire the check where a change to
  the startup path or to the installer will re-run it, and have its verdict read by
  whatever decides that change is deliverable. A script that proved the property once
  and is never invoked again satisfies the words and not the record -- the inert-gate
  shape this repo keeps rediscovering.
- Must satisfy the repository error-handling policy in any harness code that ships
  under scripts/ or unit_tests/.
- Any dependency the check needs at runtime must be in the build deploy manifest.
  A check that runs only from the source tree is not the check a consumer's install
  would run, and this record is about what happens to a consumer.
- 'THE GATE THAT ACTUALLY RUNS ON AN INSTALLER CHANGE ALREADY EXISTS -- WIRE INTO IT
  RATHER THAN ADDING A SECOND ONE. scripts/ci/check_consumer_install.py already creates
  a temporary target project, runs the real installer into it as a subprocess, and
  has its exit code consumed by the consumer-install simulation job in the repository''s
  CI workflow. That is the deliverability decision the standing-guarantee requirement
  above names. Extend that script with the before-and-after startup measurement so
  the verdict is read where an installer change is already gated. The CI workflow definition
  itself needs no edit, because the job already invokes this script -- and a new standalone
  script that nothing invokes is the inert-gate shape the requirement forbids.'
- 'THE DEPLOY-MANIFEST REQUIREMENT IS ABOUT THE ROUTE, NOT ABOUT THE CHECKER, AND THE
  TWO MUST NOT BE CONFLATED. Neither the CI check directory nor the test tree is deployed
  into a target install, and that is correct: both are package-development tooling.
  What must be present in a consumer install is everything THE ROUTE reads at startup,
  and closing that gap is ACD-2100d-1''s surface. If reusing ACD-2100d-1''s probe from
  the CI check turns out to be awkward because the probe lives under the test tree,
  resolve it by having the consumer-install job reach the probe through the test runner,
  or by relocating the single shared probe. Do NOT resolve it by writing a second probe
  -- that is the exact outcome the reuse requirement above exists to prevent.'
```

## Agent Contracts

### documentation-expert

Existing docs to update / cross-link:

- docs/known-issues/ac-driven-dev.md (relationship: describes | status: exists | relevance: KI-ACD-009 -- the charter gate that halts the run. The deployed workflow does not carry it yet, so installing adds it.)
- docs/architecture/adrs/ADR-001-self-hosting-boundary.md (relationship: describes | status: exists | relevance: The install path this guarantee is stated over.)
- docs/build-pipeline.md (relationship: describes | status: exists | relevance: The installer whose run sits between this record's two measurements.)
- docs/testing/test-angles.md (relationship: describes | status: exists | relevance: The deployed and reachability angles this record's evidence has to satisfy -- the same BP-811 case ACD-2100d-1 cites.)
- docs/reference/plan-feature-layout-and-startup-checks.md (relationship: describes | status: planned | relevance: The reference page authored by ACD-2100d-4 names the startup checks; this record is the guarantee that an install adds none of them to a project that had none.)

This record owns none of the documents above -- every entry is a cross-link to a
document another record produces or maintains. The contract line that stood here
demanded `docs/known-issues/ac-driven-dev.md`, which this record only
`describes`; the register is maintained by whoever files an issue, not produced
by this ticket. Removed per BO-2200c-5-i and -iii; the list above is retained as
reading for the implementer.

This ticket also carries the store's clearest example of why `status` cannot be
trusted as a decision input: its cross-link to
`docs/reference/plan-feature-layout-and-startup-checks.md` reads `status:
planned`, authored before that page existed in anyone's plan of record, and
nothing refreshes it (BO-2200c-5-vi).

### Expects From

- **AC:** ACD-2100d-1
- **Contract:** A repeatable harness that installs into a temporary target project and starts a run there in a fresh process, returning that run's own recorded stopping point -- reached-the-first-question, or the named startup check that halted it. This record calls that harness twice against one project and compares the two results; the observation vocabulary must therefore be stable enough to compare.

## Sign-offs

- [x] architect-review — 2026-09-08 15:27
- [x] test-writer — 2026-09-08 15:50
- [x] python-coder — 2026-09-08 16:13
- [x] test-runner — 2026-09-08 16:30
- [ ] pr-reviewer — failed 2026-09-08 16:32
- [ ] ac-validator — failed 2026-09-08 16:45
- [ ] ac-fulfillment-gate — failed 2026-09-08 16:47
- [ ] commit — failed 2026-09-08 16:53

## Comments

### 2026-09-08 15:27 — architect-review (status: ok)

feedback-id: fb_2026-09-08_02f740c1
completion_manifest:
  blast_radius_reviewed: true
  impact_classified: true
  requires_adr_confirmed: true
Blast-radius check (no research-agent/Agent tool available in this runtime, so
verified directly against the worktree source, following the same fallback
this epic's ACD-2100d-1 architect-review used): `files_touched` is 3 files —
`scripts/ci/check_consumer_install.py` (existing, 406 lines; already the
consumer-install-simulation job's entry point per
`.github/workflows/ci.yml:552-557`, `python leafcutter-ai/scripts/ci/check_consumer_install.py
--package-dir leafcutter-ai --target-dir . --use-install`), `unit_tests/_installed_route_probe.py`
(existing, 236 lines; ACD-2100d-1's own principal deliverable — already
exports `STOPPING_POINT_FIRST_QUESTION`, `RouteProbeResult`,
`install_route_into_temp_target()`, `probe_installed_route()`,
`probe_source_route()`, exactly the shared harness this ticket's own
Implementation Notes instruct it to reuse rather than duplicate), and
`unit_tests/portability/test_acd_2100d_3.py` (new). 3 files, well under the
≤5-file small threshold. No always-large trigger fires: no Alembic migration
in this repo, no TimescaleDB hypertable, no FastAPI/public-API surface in
this repo, and no `docs/architecture/adrs/ADR-*` file is touched — the ticket
only cross-links ADR-001 as background reading and explicitly does not alter
`.github/workflows/ci.yml` (confirmed: that file is not in `files_touched`,
and the ticket's own notes state "The CI workflow definition itself needs no
edit, because the job already invokes this script"). Threshold table:
files=3 (small); components tagged = `ac_driven_dev`, `build_pipeline` — the
same two-tag pairing ACD-2100d-1 carried, describing one cohesive change
(extend the existing CI gate with a before/after startup measurement, reusing
the existing shared probe) rather than two independently-scoped changes; well
below the ≥3-component large trigger. No cross-package-boundary concern: the
change stays within this repo's CI/test-tooling surface (`scripts/ci/` +
`unit_tests/`), which is exactly the boundary the ticket's own Implementation
Notes flag as intentionally NOT deployed to consumers (distinct from the
route's own runtime surface, which is ACD-2100d-1's concern, already closed).

Design note: the two ALL-CAPS Implementation Notes items are legitimate and
load-bearing — (1) "wire into the gate that already runs" (extend
`check_consumer_install.py` rather than add a second standalone script) is
the correct scope-limiter, since a new unwired script would recreate the
inert-gate failure mode this repo's CLAUDE.md already documents twice; (2)
the reuse instruction for ACD-2100d-1's probe is real and already satisfiable
— `unit_tests/_installed_route_probe.py` exists with exactly the stable
stopping-point vocabulary (`STOPPING_POINT_FIRST_QUESTION`) this ticket needs
to compare two observations against each other, so no second probe or second
harness should be authored. The before/after ordering requirement (install
runs strictly between the two probe calls, same temp target, same directory
conditions, no network) is a control-flow constraint on `python-coder`'s
implementation inside `check_consumer_install.py`'s existing `main()`, not an
architectural fork — no new module boundary is introduced.

requires_adr: false (confirmed, matches existing frontmatter) — judgment
call: the two component tags (`ac_driven_dev`, `build_pipeline`) describe one
cohesive extension of an existing, already-wired CI check; no new
cross-cutting policy decision, abstraction, or cross-component contract is
introduced — the ticket implements ACD-2100d-1's already-established
before/after-probe pattern for a second observation point (install-time
regression detection) rather than establishing a new one. requires_diagram:
false (confirmed) — this is a test/CI-harness extension with no new
pipeline, container, or user-facing surface; `suggested_diagrams: []`.

Acceptance-criteria adjustments: none — the four test requirements already
correctly separate (a) the paired before/after criterion test, (b) the
precondition-not-met boundary case (already-broken project must not read as
a pass), (c) the live-hazard failure case (install introduces a startup check
the project didn't have — currently true per the ticket's own note that "the
deployed workflow has no charter gate and the source does"), and (d) the
reachability angle (both measurements via a fresh-process run, not a file
manifest), matching the ticket's own explicit prohibitions on after-only and
file-diff evidence.

```json
{
  "architectural_note": "Small change: 3 files (scripts/ci/check_consumer_install.py [existing, already the wired CI entry point at .github/workflows/ci.yml:552-557] + unit_tests/_installed_route_probe.py [existing, ACD-2100d-1's shared probe/vocabulary] + one new test file), 2 tagged components (ac_driven_dev, build_pipeline) describing one cohesive extension of an already-wired gate. No always-large trigger fires (no Alembic migration, no hypertable change, no public API in this repo, no ADR-* file altered — the ticket cross-links ADR-001 as background only and explicitly leaves .github/workflows/ci.yml untouched). Confirmed via direct source inspection (research-agent/Agent tool unavailable in this runtime) that the probe module's stable stopping-point vocabulary (STOPPING_POINT_FIRST_QUESTION, RouteProbeResult, install_route_into_temp_target, probe_installed_route, probe_source_route) already exists from ACD-2100d-1 and is reusable as-is, so the reuse instruction in this ticket's Implementation Notes is satisfiable without a second harness. Design concern: python-coder must keep the before/after ordering (install strictly between the two probe calls, same temp target and directory conditions, no network available for either measurement) as a control-flow constraint inside check_consumer_install.py's existing main(), not as a new module or a second entry point -- the ticket's own notes already forbid a second probe or a second CI job, and this review confirms no design deviation is needed to satisfy that.",
  "acceptance_adjustments": [],
  "escalation": "none",
  "escalation_reason": "",
  "suggested_adr": null,
  "suggested_diagrams": []
}
```

### 2026-09-08 15:50 — test-writer (status: ok)

feedback-id: fb_2026-09-08_ed2a30c4
completion_manifest:
  tests_written: true
  red_baseline_confirmed: true
  cross_layer_seam_answer:
    result: covered
    producing_side: "scripts/ci/check_consumer_install.py's own main() (the wired consumer-install-simulation CI entry point) as the producer of a before/after route-start verdict"
    consuming_side: "the test's own subprocess assertions on that CLI's real stdout/stderr and exit code -- the same consumption the CI job (.github/workflows/ci.yml:552-557) performs on this script's exit code"
  reachability_entry_point_answer:
    result: resolved
    entry_point: "python scripts/ci/check_consumer_install.py --package-dir <pkg> --target-dir <target> (CLI via subprocess) -- the wired consumer-install-simulation CI entry point at .github/workflows/ci.yml:552-557"
red_baseline:
  - test_name: test_the_route_reaches_the_first_question_before_and_after_the_install
    file: unit_tests/portability/test_acd_2100d_3.py
    error: "AssertionError: the verdict must report BOTH the before and the after stopping point -- an after-only check does not satisfy this record. expected \"before='first_question:covered-route-gate'\" and \"after='first_question:covered-route-gate'\" in output: 'CONSUMER INSTALL SIMULATION OK: deployed output root at <tmp>/project/.leafcutter\\n'"
  - test_name: test_a_project_already_broken_before_the_install_does_not_pass_the_guarantee
    file: unit_tests/portability/test_acd_2100d_3.py
    error: "AssertionError: a project that halted BEFORE the installer ran must be reported precondition-not-met (exit 3), never a plain pass (0) or an undifferentiated failure: got returncode=0, output='CONSUMER INSTALL SIMULATION OK: deployed output root at <tmp>/project/.leafcutter\\n'"
  - test_name: test_a_startup_halt_the_project_did_not_have_before_the_install_is_reported
    file: unit_tests/portability/test_acd_2100d_3.py
    error: "AssertionError: install introduced a startup check the project did not halt at before -- expected exit 1 (regression), got 0: 'CONSUMER INSTALL SIMULATION OK: deployed output root at <tmp>/project/.leafcutter\\n'"
  - test_name: test_both_findings_come_from_runs_started_in_the_project_not_from_the_copy_list
    file: unit_tests/portability/test_acd_2100d_3.py
    error: "AssertionError: a deployed route that copied perfectly but cannot start must still fail this CLI's own exit code: got 0, output='CONSUMER INSTALL SIMULATION OK: deployed output root at <tmp>/project/.leafcutter\\n'"

Wrote unit_tests/portability/test_acd_2100d_3.py (4 e2e tests, one per Test
Requirements row, tags `# covers: ACD-2100d-3` + matching `# angle:`). All 4
drive the EXISTING `scripts/ci/check_consumer_install.py` CLI as a real
subprocess (never a second script), per the ticket's own "wire into the gate
that already runs" instruction, and reuse ACD-2100d-1's shared
`unit_tests/_installed_route_probe.py` harness (never a second probe) both to
seed fixtures and to independently verify them. Verified empirically this
session (ad-hoc, uncommitted scripts) before writing assertions: (1) a
minimal no-checks stub JS reaches `STOPPING_POINT_FIRST_QUESTION` via the
real harness; (2) a synchronously-throwing stub is classified
`halted:no_terminal_payload`; (3) a genuinely non-git `target_dir` makes the
REAL current source's workspace-setup-permission pre-flight halt with
`result.status=="error"` ("No repository could be resolved"), while the
no-checks stub does not perform that check at all -- giving a real,
deterministic install-introduces-a-check regression fixture without needing
to check out any prior git revision of the package; (4) `build.py`'s
`{{config.output_root}}` placeholder substitution means the deployed copy is
NOT byte-identical to source even on a clean install, so the reachability
test asserts on functional content markers instead of byte equality. Ran
only these 4 new tests (`pytest .../test_acd_2100d_3.py -v`); confirmed all 4
fail with `AssertionError` at assertion time (not at collection) under
`AC_ENFORCE_STRICT=1` (plain `pytest` without that flag downgrades them to
`xfail` via this repo's known AC-enforcement masking -- see
`red_baseline` above for the real, unmasked failures). No production code
was touched. The test file's own docstring specifies the exact exit-code
(0 / 1 / 3) and marker-substring (`"PRECONDITION NOT MET"`,
`"route-start regression"`, `before=.../after=...`) contract
`check_consumer_install.py` must satisfy to turn these green.

### 2026-09-08 16:13 — python-coder (status: ok)

feedback-id: fb_2026-09-08_ea9727c8
completion_manifest:
  red_baseline_all_green: true
  no_new_probe_or_harness: true
  gate_wired_into_existing_entry_point: true
  fresh_scratch_install_still_green: true
  file_size_docstring_complexity_gates_pass: true
red_baseline_results:
  - test_name: test_the_route_reaches_the_first_question_before_and_after_the_install
    result: green
  - test_name: test_a_project_already_broken_before_the_install_does_not_pass_the_guarantee
    result: green
  - test_name: test_a_startup_halt_the_project_did_not_have_before_the_install_is_reported
    result: green
  - test_name: test_both_findings_come_from_runs_started_in_the_project_not_from_the_copy_list
    result: green

Extended `scripts/ci/check_consumer_install.py`'s existing `main()` with
ACD-2100d-3's before/after route-start regression check, wired into the
already-gated consumer-install-simulation CI entry point rather than a
second script, per the ticket's own instruction. Before `_maybe_run_build()`
runs, probes `target_dir`'s pre-existing deployed
`.leafcutter/workflows/plan-feature.js` (if any) via ACD-2100d-1's shared
`unit_tests/_installed_route_probe.py` harness, reused as-is (no second
probe authored). After `_verify_deployed_tree()` succeeds, probes the same
target again and classifies the pair: before never reaching the first
question -> `PRECONDITION NOT MET`, exit 3 (never a pass); before did but
after does not -> `route-start regression`, exit 1; both reach it -> exit 0
with `before=...`/`after=...` printed. A fresh scratch install with nothing
deployed yet skips the whole check (verified manually against a genuine
empty `--target-dir`: unchanged `CONSUMER INSTALL SIMULATION OK`, exit 0,
no before/after markers). The heavy probe/verdict logic was split into a
new sibling module `scripts/ci/_route_start_regression_check.py` (mirroring
the existing `_use_install_step.py` split) to stay within
`check_consumer_install.py`'s own line-size budget under
`check_file_size.py`'s content-line rule (340 lines vs the 400 limit; new
sibling file 88 lines). Ran `check_docstrings.py`, `check_complexity.py`,
and `check_file_size.py` against both files directly (all pass) and `ruff
check` (clean). All 4 of test-writer's red-baseline tests now pass
(`AC_ENFORCE_STRICT=1 pytest unit_tests/portability/test_acd_2100d_3.py`,
50.81s), and ACD-2100d-1's own 3 tests remain green (no regression to the
shared probe, which was not modified). No SQL files touched;
`unit_tests/_installed_route_probe.py` was reused unmodified, not edited,
consistent with `files_touched` listing it as a reused dependency rather
than an edit target.
cross_layer_seam_answer:
  result: not_applicable
  reason: "python-coder does not author the cross-layer seam declaration; that is test-writer's Rule 3 artifact, already recorded in this ticket's own 2026-09-08 15:50 test-writer comment."
  remediation: "n/a -- not_applicable is a first-class outcome for a non-test-writer phase."

### 2026-09-08 16:30 — test-runner (status: ok)

feedback-id: fb_2026-09-08_f387c9ec
completion_manifest:
  test_suite_executed: true
  all_tests_passing: true
  failure_report_structured: true
Ran the ticket's own target suite directly (`git diff --name-only HEAD` shows
only CI/test-tooling paths that match none of the fixed routing-table suites
in this agent's dispatch spec, so I routed to the specific test file the
prior test-writer/python-coder phases already established as the target):
`AC_ENFORCE_STRICT=1 python3 -m pytest unit_tests/portability/test_acd_2100d_3.py -v`
— all 4 tests pass (`test_the_route_reaches_the_first_question_before_and_after_the_install`,
`test_a_project_already_broken_before_the_install_does_not_pass_the_guarantee`,
`test_a_startup_halt_the_project_did_not_have_before_the_install_is_reported`,
`test_both_findings_come_from_runs_started_in_the_project_not_from_the_copy_list`),
39.89s, under strict AC enforcement so no failure is masked as xfail. Also
re-ran ACD-2100d-1's own 3 tests
(`unit_tests/portability/test_acd_2100d_1.py`, the shared-probe dependent
suite python-coder's sign-off claimed was unaffected) to confirm no
regression: all 3 pass, 20.56s. No test file, source file, or fixture was
edited by this phase.

### 2026-09-08 16:32 — pr-reviewer (status: blocker)

feedback-id: fb_2026-09-08_f48984d1
completion_manifest:
  reviewed_ticket_diff: true
  ran_ticket_own_tests: true
  ran_cross_cutting_regression_suite:
    result: false
    reason: "Ran the wider unit_tests/portability/ suite (not just this ticket's own 4 tests + ACD-2100d-1's 3) and found the new before/after route-start check regresses 5 pre-existing, already-`done` AC tests that share this same entry point."
    remediation: "python-coder must rework the before-measurement so it only fires when the Given precondition is genuinely meaningful for THIS invocation, or the callers that legitimately re-invoke check_consumer_install.py against an already-populated, non-git target_dir must be exempted/accounted for. See finding below."

[H-1] scripts/ci/check_consumer_install.py:436-458 (main()) — new
before/after route-start check masks pre-existing downstream checks on any
target_dir that already carries a deployed (non-git) route

Confirmed by direct execution (not inspection only), all reproducible right
now on this worktree:

- `AC_ENFORCE_STRICT=1 python3 -m pytest unit_tests/portability/test_acd_2100d_3.py -v`
  — 4/4 green, as claimed by test-writer/python-coder/test-runner.
- `python3 -m pytest unit_tests/portability/test_consumer_simulation.py -v`
  — `test_consumer_simulation_detects_unresolved_reference` (AC BP-900h-1,
  already `done`) now FAILS. It runs `check_consumer_install.py` once to
  build a project into a plain `tempfile`-style `tmp_path` (not
  git-initialized), deletes a deployed script the compiled templates
  reference, then re-invokes the CLI with `--skip-build` and asserts the
  missing path is named on stderr with a non-zero exit. Instead it now gets
  exit 3 with `CONSUMER INSTALL SIMULATION PRECONDITION NOT MET: ... before
  ='halted:error'` — the new route-start probe finds the already-deployed
  route halts (because `target_dir` has no `.git` ancestry, the same
  repository-resolution pre-flight hazard this ticket's own test-writer
  documented) and returns early via `verdict_after_install()`'s `exit 3`
  path in `main()`, before `_check_unresolved_references()` ever runs. The
  unresolved-reference regression the test exists to catch is never
  detected; BP-900h-1's own guarantee is now silently unenforced whenever a
  non-git target_dir is reused this way.
- `python3 -m pytest unit_tests/portability/test_bp_900h6i.py unit_tests/portability/test_bp_900h6ii.py -v`
  — 4 additional tests fail for the identical reason
  (`test_bp900h6i_an_entitled_disposable_target_still_completes`,
  `test_bp900h6ii_a_narrowed_run_and_a_full_run_do_not_produce_the_same_verdict`,
  `test_bp900h6ii_a_silent_or_unaccounted_narrowing_fails_the_job`,
  `test_bp900h6ii_narrowed_run_names_every_withheld_guard_and_states_the_deployed_count`):
  each re-invokes `check_consumer_install.py --skip-build --use-install`
  against a `target_dir` that already has a route deployed from an earlier
  invocation in the same test, and the new precondition-not-met short-circuit
  fires before `run_use_install_and_report()`'s own EXECUTED/UNACCOUNTED/
  DEPLOYED-GUARDS reporting ever runs, so all four assertions about that
  reporting fail against a truncated `PRECONDITION NOT MET` line instead.

Total: 5 previously-green, already-`done`-AC tests newly broken by this
ticket's diff (confirmed via the `CONSUMER INSTALL SIMULATION PRECONDITION
NOT MET` marker string in every failure's own output — that string exists
nowhere in the codebase before this diff, so causation is unambiguous, no
`git stash`/revert needed to confirm it). This is exactly the "Full test
suite... at epic-finalize" gap this repo's own CLAUDE.md names: test-writer,
python-coder, and test-runner each ran only this ticket's own 4 tests plus
ACD-2100d-1's 3, never the wider `unit_tests/portability/` suite that shares
the same entry point, so the regression was invisible to every prior sign-off
despite all of them being individually accurate about what they did run.

This is not a case of "the new check correctly refuses to compare" — it is
that `check_consumer_install.py` is reused by several existing tests as a
multi-invocation CLI against the SAME `target_dir` for purposes unrelated to
ACD-2100d-3's Given (re-checking references after deleting a file;
re-running `--use-install` after a first build), and none of those existing
callers git-initialize `target_dir`. The new probe now fires on every one of
them and reports a manufactured "precondition not met" that has nothing to
do with what those tests are actually checking.

Suggested remediation (for python-coder, not performed by this read-only
review): scope the new before/after probe so a target whose "before" route
halts for a reason unrelated to this ticket's Given does not swallow the
rest of `main()`'s pipeline for callers that never claimed that Given holds
— e.g. only engage the check when explicitly requested (a new flag), or run
it in parallel with (not gating ahead of) `_check_unresolved_references()`
and the `--use-install` step rather than returning early, or fix the
underlying non-git-ancestry `halted:error` false-halt in the shared probe's
target so a reused scratch directory does not spuriously trip the Given.
Whichever path is chosen, add all 5 of the tests named above (plus a full
`unit_tests/portability/` run) to the ticket's own red/green verification
before re-requesting pr-review.

### 2026-09-08 16:45 — ac-validator (status: blocker)

feedback-id: (submit-failed) — ac-validator is not present in
`config/feedback_categories.yaml`'s `allowed_writers` for any category
(`blocker` rejected the call with "Phase 'ac-validator' is not an allowed
writer"; the allowed-writers list for every category in the file omits
ac-validator entirely). Per signoff SKILL.md §2a this is not a phase
failure; proceeding with the fallback value.
completion_manifest:
  ac_1_evidence_found: true
  ac_2_evidence_found:
    result: false
    reason: "Both implementation (scripts/ci/_route_start_regression_check.py _classify()'s regression branch, exit 1) and test evidence (test_a_startup_halt_the_project_did_not_have_before_the_install_is_reported) exist and pass in isolation, but the guarantee AC-2 states -- 'it does not stop at any check that did not stop it before the install' -- is independently falsified elsewhere in the repository's own already-shipped, already-done ACs by this exact diff."
    remediation: "Respawn python-coder with pr-reviewer's own H-1 finding (2026-09-08 16:32 comment above) as input; scope the new before/after probe so it does not fire for callers that never claimed ACD-2100d-3's Given holds, per pr-reviewer's suggested remediation."
  ac_3_evidence_found: true
  unresolved_pr_reviewer_blocker_reproduced: true
AC coverage evidence, cited concretely: AC-1 covered — implementation at
`scripts/ci/_route_start_regression_check.py:107-136` (`_classify()`'s pass
branch, `before == after == first_question`) + test
`test_the_route_reaches_the_first_question_before_and_after_the_install`
(`unit_tests/portability/test_acd_2100d_3.py:239`), both green per
test-runner's 2026-09-08 16:30 comment. AC-3 covered — implementation at
`scripts/ci/check_consumer_install.py:436-458` (`main()`'s before/after
probe calls, both invoking `check_consumer_install.py` as a real subprocess
against the real target, never a file manifest) + test
`test_both_findings_come_from_runs_started_in_the_project_not_from_the_copy_list`
(`unit_tests/portability/test_acd_2100d_3.py:377`). AC-2 has the same two
evidence channels — implementation at `_route_start_regression_check.py`'s
`_classify()` regression branch (exit 1 when `before == first_question` and
`after != first_question`) + test
`test_a_startup_halt_the_project_did_not_have_before_the_install_is_reported`
(`unit_tests/portability/test_acd_2100d_3.py:327`), also green — but I
independently reproduced pr-reviewer's H-1 finding by running
`python3 -m pytest unit_tests/portability/test_consumer_simulation.py -v -k
test_consumer_simulation_detects_unresolved_reference` against this exact
diff just now: it fails with `CONSUMER INSTALL SIMULATION PRECONDITION NOT
MET ... before='halted:error'` instead of naming the deleted script
`scripts/ac_store/ac_prioritizer.py` on stderr, because this ticket's new
probe now stops that already-`done` AC's (BP-900h-1) re-invocation of
`check_consumer_install.py` at a check that did not stop it before this
diff landed — the live counter-example to AC-2's own wording, in a case
this ticket's own 4 tests do not exercise. I did not re-run the other 4
tests pr-reviewer named (`test_bp900h6i`/`test_bp900h6ii`) since one
independent reproduction is sufficient to confirm the finding is live and
unresolved (git diff unchanged since python-coder's 16:13 comment, verified
via `git status --short`). AC store alignment check
(`check_v2_ac_store_alignment.py --ticket <this ticket>`) exits 0 — no
store-alignment violations. Not signing off `ac-validator` as `signed_off`:
AC-2's evidence is contradicted by a reproducible counter-example, and the
ticket already carries an unresolved `pr-reviewer: failed` blocker from the
same root cause. Suggested remediation: respawn `python-coder` per
pr-reviewer's own remediation, then re-run `test-runner` on the FULL
`unit_tests/portability/` suite (not just this ticket's own 4 tests) before
re-requesting `pr-reviewer` and `ac-validator`.

### 2026-09-08 16:47 — ac-fulfillment-gate (status: blocker)

feedback-id: (submit-failed) — ac-fulfillment-gate is not present in
`config/feedback_categories.yaml`'s `allowed_writers` for the `blocker`
category (same gap ac-validator's 2026-09-08 16:45 comment hit). Per signoff
SKILL.md §2a this is not a phase failure; proceeding with the fallback value.
completion_manifest:
  resolver_invoked: true
  ac_yaml_loaded: true
  work_status_check:
    result: false
    reason: "AC ACD-2100d-3 (level L2) carries work_status: todo. Per this
      gate's own auto-fix rule (Step 3a) this is mechanically eligible for
      auto-fix to done, since files_touched intersects the current diff
      (scripts/ci/check_consumer_install.py modified,
      unit_tests/portability/test_acd_2100d_3.py added -- confirmed via `git
      status --short` and `git diff HEAD --name-only`). I deliberately did
      NOT perform that auto-fix: this same ticket's own record already
      carries two unresolved, independently-reproduced blockers
      (pr-reviewer failed 16:32, ac-validator failed 16:45) showing a live,
      reproducible counter-example to this AC's own AC-2 clause ('it does
      not stop at any check that did not stop it before the install') --
      the new before/after probe in this diff regresses 5 pre-existing,
      already-done AC tests (BP-900h-1, BP-900h6i, BP-900h6ii) by
      short-circuiting main() with a manufactured PRECONDITION NOT MET
      before their own checks run. Marking work_status: done over an
      unresolved, reproduced regression on the very AC being verified is
      the phantom-done shape this gate exists to prevent -- not a case
      where 'evidence cannot be determined', but one where the strongest
      available evidence says the guarantee does not yet hold."
    remediation: "Respawn python-coder per pr-reviewer's own remediation
      (scope the new before/after probe so it does not fire for callers
      that never claimed ACD-2100d-3's Given holds), re-run test-runner on
      the FULL unit_tests/portability/ suite, then re-request pr-reviewer
      and ac-validator. Only once both are signed off should
      ac-fulfillment-gate flip work_status: done."
  implemented_by_fixed: true
  covered_by_fixed: true
  schema_validated_after_edit: true
Resolved AC coverage via `ac_coverage_resolver.py` (two-key
`{id, path}` block, `ACD-2100d-3.yaml`, level L2). Auto-fixed two
append-only, evidence-backed gaps and verified both with the resolver
re-run and `check_ac_schema.py` (exit 0): `covered_by` was `[]` though
`unit_tests/portability/test_acd_2100d_3.py` carries four genuine
`# covers: ACD-2100d-3` tags (confirmed via `grep -rn` under `unit_tests/`)
-- appended that path. `implemented_by` listed only this ticket's own path,
which is not itself in `files_touched`, so per Step 2c's
files_touched-intersect-diff rule it had no valid evidence -- appended
`scripts/ci/check_consumer_install.py` and
`unit_tests/portability/test_acd_2100d_3.py`, both confirmed present in
`git status --short` as staged changes against this AC's `files_touched`.
Left `implemented_by`'s pre-existing ticket-path entry and `work_status`
untouched (append-only constraint; see completion_manifest for why
`work_status` was not flipped to `done`). Re-ran the resolver after the
edit: only `work_status` remains a failed field. This AC's overall
verdict is `blocker`, so the ticket-level verdict per Step 5 is also
`blocker` -- do not proceed to `commit` until pr-reviewer's H-1 finding is
resolved and re-verified.

### 2026-09-08 16:53 — commit (status: blocker)

feedback-id: fb_2026-09-08_3358d0b7
completion_manifest:
  pre_commit_hooks_pass:
    result: false
    reason: "Not reached — refused before staging/commit because the ticket's
      own record shows three unresolved, independently-reproduced blockers
      (pr-reviewer failed 16:32, ac-validator failed 16:45,
      ac-fulfillment-gate failed 16:47) all pointing at the same live
      regression, and the diff has not changed since python-coder's 16:13
      commit."
    remediation: "Respawn python-coder per pr-reviewer's own remediation
      (scope the new before/after probe in
      scripts/ci/_route_start_regression_check.py so it does not fire for
      callers that never claimed ACD-2100d-3's Given holds), re-run
      test-runner on the FULL unit_tests/portability/ suite, then
      re-request pr-reviewer and ac-validator before re-dispatching commit."
  commit_message_valid: false
  ticket_staged: false
Independently re-reproduced the regression before refusing to commit (not a
re-statement of the prior three sign-offs): ran
`python3 -m pytest unit_tests/portability/test_consumer_simulation.py -v -k
test_consumer_simulation_detects_unresolved_reference` against this exact
worktree state just now — it still fails, with stderr reading `CONSUMER
INSTALL SIMULATION PRECONDITION NOT MET: the project did not reach the first
question BEFORE the installer ran (before='halted:error') -- ACD-2100d-3's
Given precondition was not met, so no before/after comparison can be made.`
instead of naming the deleted `scripts/ac_store/ac_prioritizer.py` on
stderr as AC BP-900h-1 (already `done`) requires. `git status --short`
confirms the diff is unchanged since python-coder's 16:13 sign-off (no new
commits from python-coder have landed to address pr-reviewer's H-1 finding).
Committing `scripts/ci/check_consumer_install.py` and
`unit_tests/portability/test_acd_2100d_3.py` in this state would ship a
known, reproducible regression against an already-`done` AC's test — the
`--no-verify` bypass policy and this ticket's own escalation chain both
forbid treating that as a routine commit-phase authorization. Did not stage
or commit any of this ticket's `files_touched`. This worktree's git index
also currently carries unrelated staged changes from sibling tickets (15,
19) plus `docs/INDEX.md`, `scripts/pause_store.py`, and
`templates/workflows-js/plan-feature.js` — out of scope for this ticket and
left untouched. Recommend respawning `python-coder` with pr-reviewer's own
16:32 remediation, then re-running `test-runner` on the full
`unit_tests/portability/` suite, then re-requesting `pr-reviewer` and
`ac-validator` before re-dispatching `commit`.

## Escalation

Branch: none
Reason: 3 files touched (well under the ≤5-file threshold), 2 tagged components
(ac_driven_dev, build_pipeline) describing one cohesive extension of an
already-wired CI check rather than independently-scoped work (well under the
≥3-component large trigger); no always-large trigger fires (no Alembic
migration, no hypertable change, no public API surface in this repo, no
ADR-* file altered — the ticket implements ACD-2100d-1's existing
before/after-probe pattern for a second observation point).
