---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  pr-reviewer: needed
  pull-request: needed
  python-coder: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
components:
- build-pipeline
created: '2026-06-08'
depends_on:
- BP-700c
files_touched:
- config/agent_registry.json
- docs/architecture/adrs/ADR-005-frontend-coder-agent.md
priority: medium
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: BP-700c-4
status: todo
title: Agent registry entry preserves all existing selection criteria and metadata
---

# Agent registry entry preserves all existing selection criteria and metadata

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BP-700c-4` — Agent registry entry preserves all existing selection criteria and metadata — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BP-700c-4`. Component: `build-pipeline`. Assigned agent: `python-coder`. Estimated complexity: `S`.

## Acceptance Criteria

```gherkin
Given the current frontend-coder entry in agent_registry.json has:
  - id: "frontend-coder"
  - tier: "phase"
  - priority: 8
  - owns_file_extensions: [".tsx", ".jsx", ".vue", ".svelte", ".html", ".css", ".scss"]
  - spawn_allowlist: ["research-agent"]
  - requires_verification: true
  - category: "implementation"
When the unified frontend agent replaces the old entry
Then all the fields listed above are preserved with the same values
And the skills_invoked list no longer references "frontend-design" as a
  conditional skill (since it is now embedded)
And the skills_used list no longer includes "frontend-design"
And the skills_invoked list still includes "webapp-testing" as conditional
  and "signoff" as conditional
```

## Sign-offs

- [ ] python-coder
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit
- [ ] pull-request

## Comments
