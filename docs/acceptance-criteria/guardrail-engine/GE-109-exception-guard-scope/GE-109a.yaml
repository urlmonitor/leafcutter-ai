id: GE-109a
readiness: approved
priority: medium
title: "Exception-handling guard skips test files"
component: guardrail-engine
level: L2
status: active
req_status: active
work_status: done
created_by: "docs/acceptance-criteria/guardrail-engine/GE-109-exception-guard-scope/GE-109.yaml"
criteria: |
  Given the check-exception-handling pre-commit hook receives a staged Python file,
  And the file is a test file — its path contains a `tests` or `unit_tests`
    component, OR its basename matches `test_*.py`, `*_test.py`, or `conftest.py`,
  When the hook runs,
  Then the file is skipped without AST analysis,
  And no E722, BLE001, or IO-001 violation is emitted for that file,
  And the hook exit code is not raised to 1 on account of that file's contents,
  And a non-test production `.py` file with the same violations is still
    blocked (exit 1), so the exemption never widens to production code.
depends_on:
  - GE-109
doc_links:
  - path: "docs/acceptance-criteria/guardrail-engine/GE-109-exception-guard-scope/GE-109.yaml"
    relationship: parent
    status: exists
  - path: "templates/scripts/commit_guardian/check_exception_handling.py"
    relationship: specifies
    status: exists
assigned_agent: python-coder
estimated_complexity: S
it_requirements:
  - "Test-file detection is path-based: any path component equal to `tests` or `unit_tests`, or a basename of `conftest.py` / `test_*.py` / `*_test.py`."
  - "Exemption must short-circuit before AST analysis so a test file never contributes a violation."
  - "Production files (normal directory, normal name) must remain subject to the full E722/BLE001/IO-001 check."
delivers_to: null
expects_from:
  ac_id: GE-109
  contract: "The check_exception_handling hook's per-file processing loop in main()"
origin_agent: business-analyst
created: 2026-06-17
amended_by: []
superseded_by: null
covered_by:
- unit_tests/commit_guardian/test_check_exception_handling.py::TestGE109aTestFileE722Exempt
- unit_tests/commit_guardian/test_check_exception_handling.py::TestGE109aTestFileBLE001Exempt
- unit_tests/commit_guardian/test_check_exception_handling.py::TestGE109aTestFileIO001Exempt
- unit_tests/commit_guardian/test_check_exception_handling.py::TestGE109aProductionFileStillFlagged
- unit_tests/commit_guardian/test_check_exception_handling.py::TestGE109aPathComponentTestsDir
- unit_tests/commit_guardian/test_check_exception_handling.py::TestGE109aBasenameDetection
implemented_by:
- templates/scripts/commit_guardian/check_exception_handling.py#is_test_file
- tickets/00_inbox/TICKET-20260617-GE-109a.md
notes: |
  Originally implemented only in the now-deprecated tree
  templates/commit-guardian/check_exception_handling.py (see TICKET-20260617-GE-109a).
  The exemption was later ported to the canonical tree
  templates/scripts/commit_guardian/check_exception_handling.py under AC GE-110
  (the copy build.py actually deploys); implemented_by now points at the canonical
  tree to match this AC's doc_links. See GE-110 for the canonical-tree coverage.
change_target: code
risk_surface: internal
