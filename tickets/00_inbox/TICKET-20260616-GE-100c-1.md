---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  pr-reviewer: needed
  pull-request: needed
  python-coder: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
components:
- commit_guardian
created: '2026-06-16'
depends_on:
- GE-100c
files_touched:
- docs/acceptance-criteria/guardrail-engine/GE-100-code-quality-hooks/GE-100c.yaml
- docs/pre-commit-hooks.md
priority: high
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: GE-100c-1
status: todo
title: jscpd hook exits fail-open when the jscpd subprocess exceeds the 30-second
  timeout
---

# jscpd hook exits fail-open when the jscpd subprocess exceeds the 30-second timeout

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `GE-100c-1` — jscpd hook exits fail-open when the jscpd subprocess exceeds the 30-second timeout — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `GE-100c-1`. Component: `guardrail-engine`. Assigned agent: `python-coder`. Estimated complexity: `S`.

## Acceptance Criteria

```gherkin
Given the commit guardian configuration has the duplicate_code section with:
  enabled: true
  strict: true
And the jscpd binary is installed (version 3.x)
And the jscpd subprocess takes longer than 30 seconds to complete
  (e.g. scanning a very large working tree)
When the duplicate code hook runs at pre-commit time
Then the hook terminates the jscpd subprocess after 30 seconds
And it emits a warning to stderr stating that jscpd timed out
And the hook exits with code 0 (fail-open despite strict mode)
And the commit proceeds without duplicate checking
```

## Sign-offs

- [ ] python-coder
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit
- [ ] pull-request

## Comments
