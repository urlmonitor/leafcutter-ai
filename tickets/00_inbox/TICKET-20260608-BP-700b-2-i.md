---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  pr-reviewer: needed
  pull-request: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
components:
- build-pipeline
created: '2026-06-08'
depends_on:
- BP-700b-2
files_touched:
- config/agent_registry.json
- docs/architecture/adrs/ADR-005-frontend-coder-agent.md
priority: medium
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: BP-700b-2-i
status: todo
title: LLM trigger fires for tickets describing UI work without frontend file extensions
  in files_touched
---

# LLM trigger fires for tickets describing UI work without frontend file extensions in files_touched

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BP-700b-2-i` — LLM trigger fires for tickets describing UI work without frontend file extensions in files_touched — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BP-700b-2-i`. Component: `build-pipeline`. Assigned agent: `test-writer`. Estimated complexity: `S`.

## Acceptance Criteria

```gherkin
Given a ticket has files_touched containing only ".ts" files (not .tsx)
But the ticket body says "create a React component for the settings page"
When the dispatch system evaluates the unified frontend agent's trigger conditions
Then the LLM-type trigger condition fires ("ticket involves creating or
  modifying frontend/UI components, markup, or styles")
And the agent is dispatched with status "needed"
And the dispatch system does not rely solely on the DSL file-extension check
```

## Sign-offs

- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit
- [ ] pull-request

## Comments
