id: BO-1100b-1
title: "Staged files spanning two or more routing groups trigger a warning before message composition"
component: build-orchestration
level: L2
status: active
readiness: approved
priority: medium
req_status: active
work_status: todo
criteria: |
  Given the routing config classifies staged files into groups,
    and the staged files are:
      - tickets/02_in_progress/TICKET-001.md  (group: "tickets")
      - scripts/commit_guardian/new_hook.py    (group: "implementation")
  When the commit agent finishes classification and detects 2 distinct groups,
  Then the commit agent pauses before composing any message,
  And the commit agent presents a warning to the user indicating:
    "Mixed commit detected: staged files belong to 2 groups."
  And the warning is presented BEFORE any subject line is drafted.
depends_on: [BO-1100b, BO-1100a-1]
doc_links:
  - path: templates/agents/commit.md
    relationship: describes
    status: exists
assigned_agent: llm-expert
estimated_complexity: M
it_requirements:
  - "Must present the warning before any message composition step begins"
  - "Must not proceed to message drafting without user acknowledgment"
  - "Must work within the existing commit agent confirmation gate pattern"
delivers_to: null
expects_from:
  ac_id: BO-1100a-1
  contract: "JSON classification result: {matched: true, groups: [{group: string, files: string[]}, {group: string, files: string[]}]} with 2+ group entries indicating a mixed commit"
origin_agent: business-analyst-v3
created: 2026-06-05
amended_by: [it-po-v3]
superseded_by: null
covered_by: [BO-1100b-1-i]
implemented_by: []
