---
ac_traceability:
  id: GE-120d-2
  path: docs/acceptance-criteria/guardrail-engine/GE-120-green-means-checked/GE-120d-2.yaml
advances_current_outcome: true
agents:
  ac-fulfillment-gate: needed
  ac-validator: needed
  architect-review: needed
  commit: needed
  documentation-expert: needed
  documentation-verifier: needed
  pr-reviewer: needed
  pull-request: needed
  python-coder: needed
  test-runner: needed
  test-writer: needed
change_target: code
complexity: medium
components:
- worktree_manager
- precommit_hooks
created: '2026-08-25'
depends_on:
- 22_TICKET-20260825-GE-120d-1.md
documentation_required: true
files_touched:
- scripts/build.py
- templates/scripts
- templates/scripts/commit_guardian
- templates/scripts/setup_ticket_worktree.py
priority: high
requires_adr: false
requires_diagram: false
risk_surface: contract_boundary
roadmap_phase: phase_1
source_ac: GE-120d-2
status: todo
title: Set-up refuses to hand over a working copy whose protection it could not establish
---

# Set-up refuses to hand over a working copy whose protection it could not establish

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `GE-120d-2` — Set-up refuses to hand over a working copy whose protection it could not establish — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `GE-120d-2`. Component: `guardrail-engine`. Assigned agent: `python-coder`. Estimated complexity: `M`. Complexity: `medium`.

## Acceptance Criteria

```gherkin
Given the set-up command cannot locate the script that verifies the commit-time checks are
  active, or the script that installs the commit-time shims,
When it reaches those steps,
Then it does not continue with the behaviour observed today — a warning reading "not found
  ... graceful no-op" followed by a successfully created working copy,
And it names the missing scripts and states the protection they would have established,
And it fails the set-up, because the alternative is handing the caller a working copy in
  which the commit-time checks do not run,
And the working copy it leaves behind, if any, is reported as unprotected so the caller
  cannot mistake it for a prepared one,
And this block-rather-than-announce outcome is recorded as the deliberate per-case
  disposition required by GE-120a-2, with the stated reason that the artifact handed over is
  otherwise unprotected.

Coverage note: cover this by running the real set-up command in a workspace where those
scripts are genuinely unreachable, and asserting on the observed failure and the state of any
copy left behind. This is the case that was observed live while creating the working copy
this tree was authored in, so it reproduces without contrivance.
```

- [ ] AC-1: it does not continue with the behaviour observed today — a warning reading "not found
- [ ] AC-2: it names the missing scripts and states the protection they would have established
- [ ] AC-3: it fails the set-up, because the alternative is handing the caller a working copy in
- [ ] AC-4: the working copy it leaves behind, if any, is reported as unprotected so the caller
- [ ] AC-5: this block-rather-than-announce outcome is recorded as the deliberate per-case

## Test Requirements

```yaml
tests:
- name: test_ge120d2_missing_protection_scripts_fail_the_setup
  file: unit_tests/setup/test_ge_120d_2.py
  covers:
  - GE-120d-2
  asserts: 'Real set-up command run where verify_precommit_active.py and install_pre_commit_shims.py
    are genuinely unreachable: the set-up fails.'
  framework: unittest
  type: behavioral
- name: test_ge120d2_graceful_no_op_outcome_no_longer_produced
  file: unit_tests/setup/test_ge_120d_2.py
  covers:
  - GE-120d-2
  asserts: The observed not-found graceful-no-op warning followed by a successfully
    created working copy is no longer the outcome.
  framework: unittest
  type: behavioral
- name: test_ge120d2_failure_names_scripts_and_the_protection_they_establish
  file: unit_tests/setup/test_ge_120d_2.py
  covers:
  - GE-120d-2
  asserts: The failure output names the missing scripts and states the protection
    they would have established.
  framework: unittest
  type: behavioral
- name: test_ge120d2_leftover_copy_reported_as_unprotected
  file: unit_tests/setup/test_ge_120d_2.py
  covers:
  - GE-120d-2
  asserts: Any working copy left behind is reported as unprotected with its path,
    so the caller cannot mistake it for a prepared one.
  framework: unittest
  type: behavioral
- name: test_ge120d2_caller_does_not_treat_the_copy_as_prepared
  file: unit_tests/setup/test_ge_120d_2.py
  covers:
  - GE-120d-2
  asserts: The caller-visible contract reflects the refusal — a non-zero result the
    caller acts on, not just an exit code nothing reads.
  framework: unittest
  type: behavioral
- name: test_ge_120d_2_reachable_from_entry_point
  file: unit_tests/setup/test_ge_120d_2.py
  covers:
  - GE-120d-2
  angle: reachability
  asserts: 'REQUIRED — invoke the production entry point (CLI, hook, slash command,
    workflow dispatch, or main()) as a subprocess/dispatch and assert the new behaviour
    actually occurs. Do NOT satisfy this by importing the function directly. The AC
    authored no test_spec, so the entry point is not declared: resolve it before writing
    this test, and do not delete this entry.'
```

## Implementation Notes

```yaml
- 'TARGET: templates/scripts/setup_ticket_worktree.py, the create-ac-worktree path,
  which today warns ''not found ... graceful no-op'' for verify_precommit_active.py
  and install_pre_commit_shims.py and then reports a successfully created working
  copy. VERIFIED 2026-08-14 that both scripts DO exist at templates/scripts/commit_guardian/
  — the failure is resolution, not absence, which is why GE-120d-3 is the paired fix.'
- 'BLOCK IS THE RECORDED DISPOSITION for this case per GE-120a-2, with the stated
  reason: the artifact handed over is otherwise unprotected and is then used for hours
  by someone who was not watching set-up run. Record it WHERE GE-120a-2 records dispositions,
  not only in prose on this AC — a disposition that lives only in an AC note is not
  reviewable as a set.'
- 'FAIL THE SET-UP MEANS THE CALLER MUST SEE IT: non-zero exit AND the callers (worktree-agent,
  the /feature path, build-epic.js) must not treat the copy as prepared. Verify the
  caller-visible contract, not just the exit code — a refusal the caller ignores is
  the same no-op one layer up, which is precisely the shape of this defect.'
- ANY WORKING COPY LEFT BEHIND must be reported as unprotected in the failure output,
  naming its path, so it cannot be mistaken for a prepared one. Decide EXPLICITLY
  whether it is removed or left with a marker, and state which — leaving that question
  to the reader recreates the ambiguity the AC closes. If it is left, leaving it in
  a state a later run can detect is preferable to silent removal of someone's checkout.
- 'REPRODUCE WITHOUT CONTRIVANCE: this exact condition was observed live while creating
  the working copy this tree was authored in. Make the scripts genuinely unreachable
  rather than stubbing the not-found branch — a stub tests the branch the author wrote,
  not the resolution failure that occurred.'
- 'ADR-001 deployed/template parity across all three copies of setup_ticket_worktree.py
  (templates/scripts/, scripts/, .leafcutter/scripts/): edit the template, run ''python
  scripts/build.py --target-dir .'', confirm all three match. Callers run the deployed
  copy.'
- 'Error-handling policy: the not-found condition is a caught specific exception (OSError
  / FileNotFoundError) logged at WARNING and escalated to a refusal — never swallowed
  into a no-op, which is the behaviour being removed.'
```

## Agent Contracts

### documentation-expert

Existing docs to update / cross-link:

- docs/architecture/components/worktree-manager.md (relationship: describes | status: exists)
- docs/how-to/managing-pre-commit-hooks.md (relationship: related | status: exists)
- templates/scripts/setup_ticket_worktree.py (relationship: modifies | status: exists)
- templates/scripts/commit_guardian/verify_precommit_active.py (relationship: related | status: exists | relevance: One of the two scripts observed reported as 'not found ... graceful no-op'; verified present in the package.)
- templates/scripts/commit_guardian/install_pre_commit_shims.py (relationship: related | status: exists | relevance: The second observed no-op target; verified present in the package.)

- [ ] AC-1: [how-to] docs/architecture/components/worktree-manager.md — it does not continue with the behaviour observed today — a warning reading "not found

### Expects From

- **AC:** GE-120d-1
- **Contract:** The per-step protection outcome record this criterion escalates on.

## Sign-offs

- [ ] architect-review
- [ ] test-writer
- [ ] python-coder
- [ ] test-runner
- [ ] documentation-expert
- [ ] pr-reviewer
- [ ] ac-validator
- [ ] ac-fulfillment-gate
- [ ] documentation-verifier
- [ ] commit
- [ ] pull-request

## Comments
