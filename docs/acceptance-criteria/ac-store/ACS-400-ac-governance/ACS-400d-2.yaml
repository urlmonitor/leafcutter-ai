id: ACS-400d-2
readiness: reviewed
priority: medium
components:
  - ac-store
title: "Governance rules are included by default with no opt-in required"
component: ac-store
level: L2
status: active
req_status: active
work_status: todo
criteria: |
  Given a consumer project runs build.py with default options
    (no flags to enable or disable governance),
  When the build completes,
  Then the governance rules are present in the deployed output,
  And there is no configuration flag, environment variable, or prompt
    that the user must set to activate governance,
  And the governance enforcement hook is registered in the deployed
    settings alongside existing hooks (check_ac_schema, check_test_ac_tags).
depends_on: [ACS-400d]
doc_links:
  - path: docs/reference/claude-code-hooks.md
    relationship: describes
    status: exists
  - path: docs/architecture/adrs/ADR-007-ac-store-schema-id-format-enforcement.md
    relationship: describes
    status: exists
assigned_agent: python-coder
estimated_complexity: S
it_requirements:
  - "Must register the governance hook in templates/settings.json so build.py deploys it automatically"
  - "Must follow the same hook registration pattern as existing hooks (check_ac_schema, check_test_ac_tags)"
  - "Must not require any new configuration keys in commit_guardian.json for basic governance enforcement"
  - "Must be deployable without changes to the build.py invocation interface"
delivers_to: null
expects_from:
  ac_id: ACS-400d-1
  contract: "Governance rules text block and hook script registered in the templates/ directory, ready for build.py to compile into the consumer project's .claude/ directory"
origin_agent: business-analyst-v3
created: 2026-06-05
amended_by: []
superseded_by: null
covered_by: [ACS-400d-2-i]
implemented_by: []
change_target: config
risk_surface: contract_boundary
