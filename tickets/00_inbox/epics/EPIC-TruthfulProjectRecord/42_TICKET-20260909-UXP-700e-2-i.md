---
ac_traceability:
  id: UXP-700e-2-i
  path: docs/acceptance-criteria/ux-prototyping/UXP-700-truthful-project-record/UXP-700e-2-i.yaml
advances_current_outcome: true
agents:
  ac-fulfillment-gate: needed
  ac-validator: needed
  architect-review: not_needed
  commit: needed
  documentation-expert: not_needed
  documentation-verifier: not_needed
  frontend-coder: not_needed
  pr-reviewer: not_needed
  pull-request: not_needed
  python-coder: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
  user-surface-smoker: not_needed
change_target: code
complexity: low
components:
- ux_prototyping
created: '2026-09-09'
depends_on: []
files_touched:
- docs/product-truth/README.md
priority: medium
requires_adr: false
requires_diagram: false
risk_surface: internal
roadmap_phase: phase_1
source_ac: UXP-700e-2-i
status: todo
title: The derived short form is bounded, reproducible, and not editable in place
---

# The derived short form is bounded, reproducible, and not editable in place

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `UXP-700e-2-i` — The derived short form is bounded, reproducible, and not editable in place — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `UXP-700e-2-i`. Component: `ux-prototyping`. Assigned agent: `python-coder`. Estimated complexity: `S`. Complexity: `low`.

## Acceptance Criteria

```gherkin
Given a journey whose authoritative description is 938 characters long,
When the shorter form is derived,
Then the derived form is no longer than the declared bound for the place it is shown,
And deriving it twice from the same description produces the same text,
And an edit made directly to the derived form is reported, naming the authoritative
  description as the place to edit instead,
And the derived form ends in a way that shows a reader it was shortened, so nobody reads it
  as the whole description.
```

- [ ] AC-1: the derived form is no longer than the declared bound for the place it is shown
- [ ] AC-2: deriving it twice from the same description produces the same text
- [ ] AC-3: an edit made directly to the derived form is reported, naming the authoritative
- [ ] AC-4: the derived form ends in a way that shows a reader it was shortened, so nobody reads it

## Test Requirements

```yaml
tests:
- name: test_derived_short_form_respects_the_declared_bound
  file: unit_tests/product_truth/test_uxp_700e_2_i.py
  covers:
  - UXP-700e-2-i
  asserts: Deriving from a 938-character description yields text no longer than the
    bound.
  framework: unittest
  type: unit
  angle: boundary
- name: test_derivation_is_reproducible
  file: unit_tests/product_truth/test_uxp_700e_2_i.py
  covers:
  - UXP-700e-2-i
  asserts: Deriving twice from the same input yields identical text.
  framework: unittest
  type: unit
  angle: criterion
- name: test_editing_the_derived_form_in_place_is_reported
  file: unit_tests/product_truth/test_uxp_700e_2_i.py
  covers:
  - UXP-700e-2-i
  asserts: A hand edit to the derived form is reported and names the authoritative
    description.
  framework: unittest
  type: integration
  angle: failure
- name: test_uxp_700e_2_i_reachable_from_entry_point
  file: unit_tests/product_truth/test_uxp_700e_2_i.py
  covers:
  - UXP-700e-2-i
  angle: reachability
  asserts: 'REQUIRED — invoke the production entry point (CLI, hook, slash command,
    workflow dispatch, or main()) as a subprocess/dispatch and assert the new behaviour
    actually occurs. Do NOT satisfy this by importing the function directly. The AC
    authored no test_spec, so the entry point is not declared: resolve it before writing
    this test, and do not delete this entry.'
```

## Agent Contracts

### Expects From

- **AC:** UXP-700e-2
- **Contract:** The derivation this record constrains.

## Sign-offs

- [ ] test-writer
- [ ] python-coder
- [ ] test-runner
- [ ] ac-validator
- [ ] ac-fulfillment-gate
- [ ] commit

## Comments
