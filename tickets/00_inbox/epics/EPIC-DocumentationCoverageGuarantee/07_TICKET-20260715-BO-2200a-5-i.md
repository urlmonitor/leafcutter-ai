---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  pr-reviewer: needed
  pull-request: needed
  python-coder: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
complexity: low
components:
- build_orchestration
created: '2026-07-15'
depends_on:
- BO-2200a-5
files_touched:
- scripts/ac_store/validate_ac_schema.py
priority: medium
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: BO-2200a-5-i
status: todo
title: A valid documentation_triggers enum value on a non-L1 AC is still rejected
  for being off-level
---

# A valid documentation_triggers enum value on a non-L1 AC is still rejected for being off-level

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BO-2200a-5-i` — A valid documentation_triggers enum value on a non-L1 AC is still rejected for being off-level — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BO-2200a-5-i`. Component: `build-orchestration`. Assigned agent: `python-coder`. Estimated complexity: `S`. Complexity: `low`.

## Acceptance Criteria

```gherkin
Given the agent-side AC schema validator with both the enum-value check and the new
  L1-only check active,
When it validates an L2 AC whose `documentation_triggers` contains only values that
  are themselves valid enum members (for example `reference-doc`),
Then validation still fails, because the field is present on a non-L1 AC — a valid
  enum value does not exempt an off-level AC from the L1-only rule.
And when it validates an L1 AC whose `documentation_triggers` contains an invalid
  value (for example `tutorial`), validation fails on the enum check even though the
  level is permitted.
And the two rules are independent, so neither masks the other.
```

## Test Requirements

```yaml
tests:
- name: test_valid_enum_on_l2_still_fails_off_level
  file: unit_tests/ac_store/test_bo_2200a_5_i.py
  covers:
  - BO-2200a-5-i
  asserts: An L2 AC whose documentation_triggers is a valid enum value (reference-doc)
    still fails on the L1-only rule.
  framework: unittest
  type: unit
- name: test_invalid_enum_on_l1_fails_enum_check
  file: unit_tests/ac_store/test_bo_2200a_5_i.py
  covers:
  - BO-2200a-5-i
  asserts: An L1 AC whose documentation_triggers contains an invalid value (tutorial)
    fails on the enum check even though the level is permitted.
  framework: unittest
  type: unit
```

## Implementation Notes

```yaml
config_schema_fragment:
  independence_rule:
    description: The level check and the enum check for documentation_triggers must
      both run and report independently.
    type: object
constraints:
- The level check and the enum check must both run and report independently — an AC
  can fail one, the other, or both.
- Do not short-circuit the enum check when the level check passes, and do not let
  a valid enum value suppress the level check.
n_location_rule: '1'
post_write_commands: []
reference_file_path: scripts/ac_store/validate_ac_schema.py
required_skills:
- python-coder
```

## Sign-offs

- [ ] python-coder
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit
- [ ] pull-request

## Comments
