---
ac_traceability:
  id: BO-2600a-4
  path: docs/acceptance-criteria/build-orchestration/BO-2600-connected-build-set/BO-2600a-4.yaml
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: needed
  documentation-verifier: needed
  llm-expert: needed
  pr-reviewer: needed
  pull-request: needed
change_target: pipeline
complexity: medium
components:
- build_orchestration
created: '2026-08-11'
depends_on:
- 02_TICKET-20260811-BO-2600a-2.md
documentation_required: true
files_touched:
- templates/agents/build-ac.md
priority: high
requires_adr: false
requires_diagram: false
risk_surface: internal
roadmap_phase: phase_1
source_ac: BO-2600a-4
status: todo
title: build-ac emits a dependency-ordered epic when the connected build set has more
  than one AC
---

# build-ac emits a dependency-ordered epic when the connected build set has more than one AC

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BO-2600a-4` — build-ac emits a dependency-ordered epic when the connected build set has more than one AC — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BO-2600a-4`. Component: `build-orchestration`. Assigned agent: `llm-expert`. Estimated complexity: `M`. Complexity: `medium`.

## Acceptance Criteria

```gherkin
Given /build-ac processing a leaf AC in its single-ticket path (Step 2b),
And the coordinator resolves the connected build set via the select_connected
  CLI with --exclude-structural-parent,
When the resolved set contains more than one AC,
Then the coordinator emits a dependency-ordered epic folder (not a single ticket)
  by reusing the goal_to_epic machinery — one ticket per AC in the set, ordered
  prerequisites-first, with each ticket's depends_on wired to the co-located
  prerequisite tickets (via resolve_leaf_dependencies + topological_sort) so the
  ticket_frontmatter_guard passes,
And every member of the resolved set (the target AC and its baked-in prerequisites
  and un-built children) appears as a ticket in the epic folder,
And the Step 3 confirmation prompt adapts to the epic form
  ("Build this epic now? (yes / review / skip)"),
And dependency cycles are handled by the existing drain-cycles-with-warning
  behavior rather than aborting.
```

- [ ] AC-1: the coordinator resolves the connected build set via the select_connected
- [ ] AC-2: the coordinator emits a dependency-ordered epic folder (not a single ticket)
- [ ] AC-3: every member of the resolved set (the target AC and its baked-in prerequisites
- [ ] AC-4: un-built children) appears as a ticket in the epic folder
- [ ] AC-5: the Step 3 confirmation prompt adapts to the epic form
- [ ] AC-6: dependency cycles are handled by the existing drain-cycles-with-warning

## Implementation Notes

```yaml
- Edit Step 2b of templates/agents/build-ac.md so that when the resolved connected
  build set has more than one member, the coordinator routes to epic generation (reuse
  the goal_to_epic path already wired for goal-mode) instead of the single-ticket
  generator.
- 'Epic tickets must be dependency-ordered and cross-wired: reuse goal_to_epic''s
  resolve_leaf_dependencies + topological_sort so each generated ticket''s depends_on
  references co-located ticket files, satisfying ticket_frontmatter_guard (which requires
  depends_on to reference existing tickets in the same epic folder).'
- Do NOT emit flat cross-referencing tickets in tickets/00_inbox — the epic folder
  is the output shape.
- 'Verify behaviorally: a multi-member set must actually produce an epic folder whose
  tickets pass ticket_frontmatter_guard, and whose depends_on edges match the resolved
  dependency order — not a grep-only assertion (CLAUDE.md ''Gate / Workflow ACs —
  Verify Behaviorally, Not by Grep'').'
- 'Real validating case: GE-113c-3 resolves to {GE-113c-3, GE-113c-3-i, GE-113c-3-ii,
  GE-113c-3-iii, GE-113c-3-iv} (structural parent GE-113c excluded), so /build-ac
  must emit an epic of those five, not a single ticket.'
```

## Agent Contracts

### documentation-expert

Existing docs to update / cross-link:

- templates/agents/build-ac.md (relationship: modifies | status: exists)
- scripts/ac_store/goal_to_epic.py (relationship: related | status: exists)

- [ ] AC-1: [(unspecified genre)] templates/agents/build-ac.md — the coordinator emits a dependency-ordered epic folder (not a single ticket)

### Expects From

- **AC:** BO-2600a-2
- **Contract:** select_connected --exclude-structural-parent --ac <id> prints the JSON ordered id list of the connected build set.

## Sign-offs

- [ ] documentation-expert
- [ ] pr-reviewer
- [ ] documentation-verifier
- [ ] llm-expert
- [ ] commit
- [ ] pull-request

## Comments
