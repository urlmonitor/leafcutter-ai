id: ACD-1700b-1
components:
  - ac_driven_dev
readiness: draft
priority: medium
title: "An assigned_agent that mismatches the deliverable surface is flagged"
component: ac-driven-dev
level: L2
status: active
req_status: draft
work_status: todo
roadmap_phase: phase_acbuild_1_foundation
criteria: |
  Given an AC with a deliverable surface (file path + extension) and an assigned_agent,
    and a surface->craft map that resolves each surface to the craft that owns it
    (path-aware, not extension-alone),
  When the assignment-quality check evaluates the AC,
  Then the assignment is flagged as a mismatch ONLY when assigned_agent differs from the
    craft the surface->craft map resolves for that deliverable surface,
  And a legitimate mapping is NOT flagged — in particular a .js/.mjs WORKFLOW body under
    templates/workflows-js/ assigned to python-coder passes (Node orchestration is
    python-coder's craft per project convention),
  And a genuine mismatch (e.g. a frontend .jsx/.tsx/.vue/.svelte deliverable assigned to
    python-coder, or a .sql deliverable assigned to python-coder) IS flagged,
  And the flag names the deliverable surface, the craft the map resolved, and the
    mismatched agent,
  And the AC is held for reassignment before dispatch.
depends_on: [ACD-1700b]
doc_links:
  - path: docs/reference/ac-schema.md
    relationship: describes
    status: exists
assigned_agent: python-coder
estimated_complexity: M
delivers_to: null
expects_from: null
origin_agent: BrainCandy
created: 2026-08-11
amended_by: []
superseded_by: null
covered_by:
  - ACD-1700b-1-i
implemented_by: []
change_target: pipeline
risk_surface: contract_boundary
notes: |
  Recurring documented failure (memory/feedback_itpo_agent_assignment_by_surface.md):
  the BA tends to stamp python-coder uniformly. Positive mapping in ACD-1700b-2.

  DECISION RESOLVED (2026-08-11, BrainCandy) — the check is MAP-based, not
  extension-based, to avoid false-flagging legitimate python-coder .js-workflow ACs.
  Pinned surface->craft map (path-aware; first match wins):
    - *.py                                             -> python-coder
    - templates/workflows-js/**/*.{js,mjs}             -> python-coder  (Node workflow/orchestration)
    - *.sql                                            -> sql-coder
    - *.jsx, *.tsx, *.vue, *.svelte                    -> frontend-coder
    - web *.js/*.ts/*.css/*.html under a web-app path  -> frontend-coder
    - templates/agents/*.md, templates/skills/**/SKILL.md, workflow/command prose -> llm-expert
  Criteria above reframed to reference this map. Home for the map to be pinned by IT-PO at
  enrichment: a dedicated config (e.g. config/surface_craft_map.yaml) or surface globs on
  each agent in config/agent_registry.json, so the check and any generator/assignment
  tooling read one source. This is the ACD-1700b resolution of the "just-a-.js" tension.
