---
ac_traceability:
  id: UXP-700a-4-i
  path: docs/acceptance-criteria/ux-prototyping/UXP-700-truthful-project-record/UXP-700a-4-i.yaml
advances_current_outcome: true
agents:
  ac-fulfillment-gate: needed
  ac-validator: needed
  architect-review: not_needed
  commit: needed
  documentation-expert: needed
  documentation-verifier: needed
  frontend-coder: not_needed
  pr-reviewer: needed
  pull-request: not_needed
  python-coder: failed
  sql-coder: not_needed
  test-runner: needed
  test-writer: signed_off
  user-surface-smoker: not_needed
change_target:
- code
- pipeline
complexity: low
components:
- build_pipeline
- ux_prototyping
created: '2026-09-09'
depends_on: []
documentation_required: true
files_touched: []
priority: medium
requires_adr: false
requires_diagram: false
risk_surface: internal
roadmap_phase: phase_1
source_ac: UXP-700a-4-i
status: todo
title: Removing one required file turns this check red and no other check
---

# Removing one required file turns this check red and no other check

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `UXP-700a-4-i` — Removing one required file turns this check red and no other check — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `UXP-700a-4-i`. Component: `ux-prototyping`. Assigned agent: `python-coder`. Estimated complexity: `S`. Complexity: `low`.

## Acceptance Criteria

```gherkin
Given the build's from-scratch record check is passing,
When exactly one file that the checker reads before checking begins is removed from what the
  install writes,
Then the build fails and the failure names that file,
And no other check in the build changes its verdict,
And restoring the file returns the build to passing with no other change.
```

- [ ] AC-1: the build fails and the failure names that file
- [ ] AC-2: no other check in the build changes its verdict
- [ ] AC-3: restoring the file returns the build to passing with no other change.

## Test Requirements

```yaml
tests:
- name: test_removing_one_required_file_fails_the_build_naming_it
  file: unit_tests/build_guards/test_uxp_700a_4_i.py
  covers:
  - UXP-700a-4-i
  asserts: With one start-up input removed from the install, the build fails and the
    message names that file.
  framework: unittest
  type: integration
  angle: failure
- name: test_removing_one_required_file_leaves_other_build_checks_green
  file: unit_tests/build_guards/test_uxp_700a_4_i.py
  covers:
  - UXP-700a-4-i
  asserts: The verdicts of the build's other checks are unchanged by the removal.
  framework: unittest
  type: integration
  angle: boundary
- name: test_uxp_700a_4_i_reachable_from_entry_point
  file: unit_tests/build_guards/test_uxp_700a_4_i.py
  covers:
  - UXP-700a-4-i
  angle: reachability
  asserts: 'REQUIRED — invoke the production entry point (CLI, hook, slash command,
    workflow dispatch, or main()) as a subprocess/dispatch and assert the new behaviour
    actually occurs. Do NOT satisfy this by importing the function directly. The AC
    authored no test_spec, so the entry point is not declared: resolve it before writing
    this test, and do not delete this entry.'
```

## Agent Contracts

### documentation-expert

Existing docs to update / cross-link:

- docs/acceptance-criteria/build_pipeline/BP-900-deployment-completeness/BP-900b-2.yaml (relationship: context | status: exists)

- [ ] AC-1: (unspecified genre) | docs/acceptance-criteria/build_pipeline/BP-900-deployment-completeness/BP-900b-2.yaml | the build fails and the failure names that file

### Expects From

- **AC:** UXP-700a-4
- **Contract:** The build check whose sensitivity this proves.

## Sign-offs

- [x] test-writer — 2026-09-09 16:45
- [ ] python-coder — failed 2026-09-09 17:15
- [ ] test-runner
- [ ] documentation-expert
- [ ] pr-reviewer
- [ ] ac-validator
- [ ] ac-fulfillment-gate
- [ ] documentation-verifier
- [ ] commit

## Comments

### 2026-09-09 16:45 — test-writer (status: ok)
feedback-id: (submit-failed)
completion_manifest:
  tests_written: true
  red_baseline_verified: true
  cross_layer_seam_answer:
    result: covered
    producing_side: "scripts/build.py::main() — the real CLI entry point that will host UXP-700a-4's from-scratch record check"
    consuming_side: "the eventual from-scratch record check's pass/fail verdict as observed through build.py's own exit code and stdout/stderr, asserted by all three tests via _build.main() with real argv (not an inner helper call)"
  reachability_entry_point_answer:
    result: resolved
    entry_point: "python scripts/build.py --target-dir <dir> (invoked in-process as _build.main([\"--target-dir\", str(dir)]) per the established pattern in unit_tests/test_bp_900g_4.py and unit_tests/test_build_guard_real_package.py) — named directly by UXP-700a-4's own criteria text: \"When the tooling's own build runs...\""
IMPORTANT CONTEXT: as of this sign-off, none of this ticket's AC-store
dependency chain (UXP-700a, UXP-700a-1, UXP-700a-4 — tickets 02/08 of this
epic) has any agent sign-off; UXP-700a-4's "from-scratch record check" does
not exist in the codebase yet. Per the test-writer skip rule this is not a
reason to withhold tests — I wrote genuine failing stubs grounded in real,
already-existing production code (scripts/build.py's real main()/CLI,
scripts/build_phases.py::build_product_truth(), and
docs/product-truth/scripts/validate_product_truth.py's documented mandatory
schema-validation dependency) rather than a synthetic/guessed API, and
verified them red by actually running them. Flagging this dependency gap so
the epic driver can confirm tickets 02-08 land before python-coder is
dispatched on this one; python-coder may otherwise have nothing concrete to
attach the isolation/naming behaviour to.

Wrote unit_tests/build_guards/test_uxp_700a_4_i.py (unittest, 3 tests, one
per Test Requirements row / AC). Ran the file directly:
`python -m unittest unit_tests.build_guards.test_uxp_700a_4_i -v` — 2 of 3
FAILED (red, as required): AC-1 (failure) and AC-1/AC-3 (reachability) both
assert build.py must react to a missing required file, and today it exits 0
regardless. AC-2 (boundary/isolation) passed immediately, which is expected
for an isolation control today (nothing has broken it), flagged below with a
note per the under-specified-test protocol. All three tests are marked
`_MANUAL` (module docstring explains why: a real `build.py --target-dir` run
takes ~8.5s locally, over the 5s default budget) and are real-artifact
integration tests against the actual repo, restoring every mutated file in a
`finally` block (confirmed via `git status --porcelain` clean after the run).

red_baseline:
  - test_name: test_removing_one_required_file_fails_the_build_naming_it_MANUAL
    file: unit_tests/build_guards/test_uxp_700a_4_i.py
    error: "AssertionError: 0 == 0 : build.py exited 0 with docs/product-truth/schemas/flow.schema.json removed from the install. UXP-700a-4's from-scratch record check must fail the build when a file its checker reads before checking begins was not installed (UXP-700a-4-i AC-1)."
  - test_name: test_removing_one_required_file_leaves_other_build_checks_green_MANUAL
    file: unit_tests/build_guards/test_uxp_700a_4_i.py
    error: "(passes today — no error)"
    note: "passes immediately — may be under-specified. This is an isolation/boundary control asserting an UNRELATED existing guard (_check_script_reference_guard) is unaffected by the mutation; it is naturally green today because nothing yet exists to break isolation. It becomes a real regression guard once UXP-700a-4 gives the record check its own verdict."
  - test_name: test_uxp_700a_4_i_reachable_from_entry_point_MANUAL
    file: unit_tests/build_guards/test_uxp_700a_4_i.py
    error: "AssertionError: 0 == 0 : build.py --target-dir produced the SAME exit code (0) whether or not docs/product-truth/schemas/flow.schema.json was present. The real CLI entry point a consumer install actually runs must observably react to the missing start-up input — an internal function that would detect this but is never called from main() does not satisfy UXP-700a-4-i."

### 2026-09-09 17:15 — python-coder (status: blocker)
feedback-id: (submit-failed)
completion_manifest:
  upstream_dependency_verified:
    result: false
    reason: "This ticket's own '### Expects From' contract names AC UXP-700a-4
      (\"The build check whose sensitivity this proves\") as the thing
      UXP-700a-4-i proves the sensitivity of. That check does not exist:
      scripts/build_phases.py::build_product_truth() copies
      docs/product-truth/{scripts/*.py,schemas/*.json} into a consumer
      install but performs no post-deploy record check and has no per-file
      failure mode — build.py exits 0 with a required schema file missing.
      test-writer's own 2026-09-09 16:45 sign-off comment on this ticket
      states the identical finding in its 'IMPORTANT CONTEXT' paragraph and
      its test module's IMPLEMENTATION STATUS docstring, and flags that
      tickets 02-08 of this epic (UXP-700a, UXP-700a-1, UXP-700a-4) carry
      zero agent sign-offs as of that writing. I independently confirmed via
      `git log --all` for unit_tests/build_guards/test_uxp_700a_4_i.py and by
      reading scripts/build_phases.py that no from-scratch record check has
      landed since. Implementing 'isolation and naming' behaviour on top of a
      check that does not exist would mean inventing the check myself, which
      is UXP-700a-4's scope, not this ticket's (UXP-700a-4-i) — out of scope
      per this agent's contract, and the ticket's own AC gherkin presupposes
      \"Given the build's from-scratch record check is passing\", a
      precondition that is currently false."
    remediation: "Dispatch python-coder (or the assigned implementer) on
      UXP-700a, UXP-700a-1, and UXP-700a-4 first (tickets 02-08 of this epic,
      per test-writer's note), then re-dispatch python-coder on this ticket
      (09) once UXP-700a-4's from-scratch record check exists in
      scripts/build.py / scripts/build_phases.py and its own tests are
      green."
  worktree_dispatch_matches_ticket_location:
    result: false
    reason: "I was dispatched with worktree_path
      C:\\Users\\Hendrik\\Code\\leafcutter\\EPIC-TruthfulProjectRecord, but
      that worktree (branch EPIC-TruthfulProjectRecord, HEAD 639a047f) is
      checked out near main tip and contains none of this epic's ticket
      files or test-writer's artifacts — it has no
      tickets/00_inbox/epics/EPIC-TruthfulProjectRecord/ directory at all.
      The actual ticket file and the red-baseline test file
      (unit_tests/build_guards/test_uxp_700a_4_i.py) both live in a
      different worktree, C:\\Users\\Hendrik\\Code\\leafcutter\\leafcutter-ai,
      on branch epic/uxp-700-product-truth. I performed this sign-off
      against the real ticket file in leafcutter-ai (the only place it
      exists) rather than guess which worktree is authoritative or copy
      files across worktrees without instruction."
    remediation: "Confirm with the user/ticket-supervisor which worktree is
      the intended dispatch target for EPIC-TruthfulProjectRecord tickets
      before the next agent is dispatched; if leafcutter-ai /
      epic/uxp-700-product-truth is correct, update the dispatcher's
      worktree_path so future phase agents land in the same tree
      test-writer used."
Blocked before writing any code: this ticket's own 'Expects From' contract
and the AC gherkin's Given-clause both presuppose UXP-700a-4's from-scratch
record check already exists and passes, but per test-writer's own sign-off
comment and my independent verification it has not been implemented (zero
sign-offs on tickets 02-08 of this epic). Separately, the worktree_path I was
given (EPIC-TruthfulProjectRecord) does not contain this ticket, its
siblings, or test-writer's test file at all — everything actually lives in
the leafcutter-ai worktree on branch epic/uxp-700-product-truth, where I
performed this sign-off. No production or test files were touched. Suggested
remediation: land tickets 02-08 (UXP-700a / UXP-700a-1 / UXP-700a-4) first,
resolve the worktree-dispatch mismatch, then re-dispatch python-coder on this
ticket.
