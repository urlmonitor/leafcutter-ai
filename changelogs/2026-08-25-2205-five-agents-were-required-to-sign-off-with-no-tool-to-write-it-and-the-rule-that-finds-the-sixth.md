---
title: "Five agents were required to sign off with no tool to write it, and the rule that finds the sixth"
date: "2026-08-25"
time: "22:05"
type: manual
components: 
  - agent_registry
  - build_orchestration
summary: "A live build halted because test-runner completed all its work and then had no tool with which to record it. Fixes the five templates carrying a sign-off obligation they could not discharge, and adds a derived rule so the next one is reported on arrival rather than at the next stalled build."
description: "Workflow run wf_394d2b76-014, driving TICKET-20260825-BP-900g-8, halted at the test-runner phase. The agent had done everything asked of it -- four target tests passed, full suite 3984 passed and 0 failed, build.py exit 0 -- and then returned a blocker because it could not record any of it. templates/agents/test-runner.md declared tools: Bash, Read while carrying a mandatory sign-off obligation, and sign-off is defined by the signoff skill as an atomic mutation of the ticket markdown file. The only workarounds are the sed -i, python -c and heredoc forms the global conventions hard-forbid because they can truncate untracked, unrecoverable ticket files. The agent's conduct was correct: it refused the unsafe mutation and failed closed with full evidence. The template was wrong. The failure ladder then escalated through mechanical retry to brainstorm-lead and re-dispatched the same agent, which could not succeed, because the tool allowlist is fixed at dispatch. The incident named three templates. Writing the check as a rule over every template rather than as a test of the three immediately surfaced two more, research-agent and worktree-agent, carrying the identical contradiction; an enumerated test would have closed the criterion while two live instances remained. Obligation is detected structurally in two independent ways, frontmatter signoff: true and the injected Sign-off heading, which select the same five violators and cleanly exclude the five templates that already carry Edit. A substring search for the phrase sign-off was measured and rejected because it matches all thirteen read-only templates that merely mention another agent's sign-off in prose. The five split into two groups with different correct fixes, decided from the registry rather than by intuition. The three genuine ticket phases -- test-runner, live-surface-tester and user-surface-smoker, each tier phase with is_ticket_phase true and present in the build-ticket phaseOrder -- were granted Edit together with the mandatory requires_verification pairing, and keep their obligation, because removing it would convert a loud correct blocker into a silent gap, which is the phantom-done signature KI-BO-007 and the BO-2900f-1 family exist to detect. Each also gained an Edit Scope Boundary section confining Edit to the ticket record, since an agent that measures and can also edit what it measures can manufacture the false green it exists to catch. The two internal utilities -- research-agent and worktree-agent, both tier utility with is_ticket_phase false, in no phaseOrder, dispatched only by other agents or behind a JSON schema -- had the obligation removed instead, because research-agent's own Hard Rule 2 reads that it must not write files and is read-only, sitting directly above a section demanding an atomic write, and arming the single hub holding Grep, Glob, jcodemunch, serena and context7 would widen a search tool into a write tool across the whole fleet. Both now carry an in-body justification so the obligation is not reinstated by a future editor. The real deliverable is the test: it derives both obligation and capability from the templates themselves, carries a synthetic-violator case so it can still fail once the five real instances are fixed, and a control case so the rule cannot be satisfied by demanding Edit everywhere. Bash does not count as write-capable and neither does Agent, which delegates to a separate dispatch with its own fixed allowlist. Verified with the target test moving from one failed to three passed under AC_ENFORCE_STRICT, a full suite of 4000 passed and 0 failed, ruff clean, and build.py --force-breaking exiting 0, which is the only thing that exercises the registry validator rule behind the requires_verification pairing. Deferred and recorded rather than hidden: config/agent_registry.json still lists skills_invoked signoff for both utilities, producing two advisory build warnings alongside roughly fourteen pre-existing warnings of the same class; the registry is outside this change's scope and nothing is injected into the deployed body, so no agent is instructed to sign off."
breaking: false
---

## Entry
