---
ac_traceability:
  id: BO-2500e-3
  path: docs/acceptance-criteria/build-orchestration/BO-2500-mechanical-done-proof/BO-2500e-3.yaml
advances_current_outcome: true
agents:
  ac-fulfillment-gate: needed
  ac-validator: needed
  commit: needed
  documentation-expert: needed
  documentation-verifier: needed
  pr-reviewer: needed
  pull-request: needed
  python-coder: needed
  test-runner: needed
  test-writer: needed
change_target: pipeline
complexity: low
components:
- build_orchestration
- testing_quality
created: '2026-08-12'
depends_on:
- BO-2500e
documentation_required: true
files_touched:
- scripts/ac_store/done_proof.py
priority: medium
requires_adr: false
requires_diagram: false
risk_surface: internal
roadmap_phase: phase_1
source_ac: BO-2500e-3
status: todo
title: A JS-covered AC whose front-end test fails cannot be marked done
---

# A JS-covered AC whose front-end test fails cannot be marked done

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BO-2500e-3` — A JS-covered AC whose front-end test fails cannot be marked done — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BO-2500e-3`. Component: `build-orchestration`. Assigned agent: `python-coder`. Estimated complexity: `S`. Complexity: `low`.

## Acceptance Criteria

```gherkin
Given an active AC whose "// covers:<id>" test in a .ts/.tsx file FAILS when the JS test runner
  (vitest) executes it,
When the done-status of that AC is evaluated,
Then the AC cannot be marked done,
And the verdict names the failing linked test and the AC id as the reason,
So a front-end AC is never marked done on a failing test — the JS path fails closed.
```

- [ ] AC-1: the AC cannot be marked done
- [ ] AC-2: the verdict names the failing linked test and the AC id as the reason

## Test Requirements

```yaml
tests:
- name: test_failing_vitest_blocks_js_ac
  file: unit_tests/ac_store/test_bo_2500e_3.py
  covers:
  - BO-2500e-3
  asserts: An active AC whose '// covers:<id>' vitest test fails cannot be marked
    done.
  framework: unittest
  type: unit
- name: test_failing_js_verdict_names_test_and_ac
  file: unit_tests/ac_store/test_bo_2500e_3.py
  covers:
  - BO-2500e-3
  asserts: The blocking verdict names the failing linked test and the AC id as the
    reason.
  framework: unittest
  type: unit
```

## Implementation Notes

```yaml
config_schema_fragment:
  done_proof_js_failing_test_blocks:
    description: Verdict that blocks an AC from being marked done when its '// covers:<id>'
      vitest test fails; the verdict names the failing linked test and the AC id as
      the reason (fails closed).
    type: object
constraints:
- When the JS test runner (vitest) executes a '// covers:<id>' test and it FAILS,
  the AC must not be marked done — the JS path must fail closed, mirroring BO-2500a-2
  for the Python path.
- The blocking verdict must be machine-readable and name the specific failing linked
  test together with the AC id, per the error handling policy.
- Verdict must be idempotent for a given store + test tree.
n_location_rule: '1'
post_write_commands: []
reference_file_path: scripts/ac_store/done_proof.py
required_skills:
- python-coder
```

## Agent Contracts

### documentation-expert

Existing docs to update / cross-link:

- docs/architecture/components/build-orchestration.md (relationship: describes | status: exists)
- docs/architecture/adrs/ADR-004-tdd-workflow-enforcement.md (relationship: describes | status: exists)
- docs/architecture/adrs/ADR-003-test-source-of-truth-discipline.md (relationship: describes | status: exists)

- [ ] AC-1: [how-to] docs/architecture/components/build-orchestration.md — the AC cannot be marked done
- [ ] AC-2: [sequence-diagram] docs/architecture/components/build-orchestration.md — the AC cannot be marked done

### Expects From

- **AC:** BO-2500e-1
- **Contract:** JS covers-tag linkage (ac_id -> [.ts/.tsx test locations]) from the shared seam that this fail-closed verdict runs vitest against.

## Sign-offs

- [ ] test-writer
- [ ] python-coder
- [ ] test-runner
- [ ] documentation-expert
- [ ] pr-reviewer
- [ ] ac-validator
- [ ] ac-fulfillment-gate
- [ ] documentation-verifier
- [ ] commit
- [ ] pull-request

## Comments
