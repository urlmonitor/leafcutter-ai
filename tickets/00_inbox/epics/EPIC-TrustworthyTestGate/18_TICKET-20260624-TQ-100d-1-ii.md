---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  pr-reviewer: needed
  pull-request: needed
  python-coder: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
components:
- testing-quality
created: '2026-06-24'
depends_on:
- TQ-100d-1
files_touched:
- scripts/ac_store/known_failing_tests.py
- tests/testing_quality/test_known_failing_allowlist.py
priority: high
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: TQ-100d-1-ii
status: todo
title: An allowlisted test that has started passing is flagged so the stale entry
  is removed
---

# An allowlisted test that has started passing is flagged so the stale entry is removed

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `TQ-100d-1-ii` — An allowlisted test that has started passing is flagged so the stale entry is removed — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `TQ-100d-1-ii`. Component: `testing-quality`. Assigned agent: `python-coder`. Estimated complexity: `S`.

## Acceptance Criteria

```gherkin
Given a known-failing-tests allowlist entry (valid and unexpired) for a test
  that is now passing,
When the allowlist check runs,
Then the now-passing allowlisted entry is flagged with a message naming the
  entry and its test
And the flag indicates the stale exemption should be removed
And the check signals this as a violation (so a now-passing test cannot keep a
  permanent exemption).
```

## Test Requirements

- `test_now_passing_allowlisted_test_flagged`: An allowlisted test that has started passing is flagged so the stale entry can be removed.

## Sign-offs

- [ ] python-coder
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit
- [ ] pull-request

## Comments
