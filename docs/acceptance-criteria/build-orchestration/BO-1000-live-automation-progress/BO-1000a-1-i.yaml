id: BO-1000a-1-i
components:
  - build_orchestration
title: "The in-flight step is identifiable from the start line even when its sub-agent errors"
component: build-orchestration
level: L3
status: active
req_status: draft
work_status: todo
readiness: approved
priority: medium
roadmap_phase: phase_1
criteria: |
  Given a step's sub-agent returns an error or a malformed result partway
    through the finalize run,
  When the failure surfaces,
  Then the start-of-step progress line for that step was already emitted before
    the failure occurred, so the step that was in flight at the moment of
    failure is identifiable from the progress output alone,
  And identifying the in-flight step does not depend on the error branch
    emitting its own separate diagnostic line.
depends_on: [BO-1000a-1]
doc_links:
  - path: docs/architecture/components/finalize.md
    relationship: describes
    status: exists
  - path: docs/architecture/adrs/ADR-017-dual-engine-workflow-support.md
    relationship: describes
    status: exists
assigned_agent: python-coder
estimated_complexity: S
it_requirements:
  config_schema_fragment: null
  reference_file_path: "templates/workflows-js/finalize-feature.js"
  n_location_rule: "all"
  required_skills:
    - "python-coder"
  post_write_commands:
    - "python scripts/build.py --target-dir ."
  notes: |
    This is the ordering guarantee of BO-1000a-1 stated as an edge case; same
    surface (templates/workflows-js/finalize-feature.js), same python-coder owner.
    Policy constraints:
    - The start-of-step line must be flushed/emitted before the step dispatches
      its sub-agent, so a subsequent error or malformed-result return cannot
      pre-empt it. The in-flight step must be recoverable from the progress
      stream WITHOUT relying on the error branch printing its own line.
    - Applies to every step that dispatches a sub-agent or otherwise can fail,
      including the intermediate closure step (see BO-1000a-2-i).
    - No new config key. ADR-001 build.py round-trip parity.
delivers_to: null
expects_from: null
test_required: true
test_spec:
  - name: test_start_line_emitted_before_step_failure
    target_dir: unit_tests/workflows/
    framework: unittest
    type: behavioral
    description: "When a step's sub-agent returns an error or malformed result, the start-of-step line for that step already appears in the progress output before the failure surfaces."
  - name: test_in_flight_step_identifiable_without_error_branch_line
    target_dir: unit_tests/workflows/
    framework: unittest
    type: behavioral
    description: "The step in flight at the moment of failure is identifiable from its start-of-step line alone, with the error branch emitting no separate diagnostic line."
origin_agent: business-analyst-v3
created: 2026-07-10
amended_by:
  - reason: it-po-v3-technical-enrichment
    note: "Assigned python-coder (workflow-body surface), added structured it_requirements + doc_links; criteria unchanged."
  - reason: it-po-test-contract
    note: "Added test_spec (2 behavioral tests derived from the Gherkin Then-clauses) and test_required: true; criteria and all prior fields unchanged."
superseded_by: null
covered_by: []
implemented_by:
- tickets/00_inbox/epics/EPIC-InFlightVisibility/02_TICKET-20260720-BO-1000a-1-i.md
change_target: pipeline
risk_surface: internal
