---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  pr-reviewer: needed
  pull-request: needed
  python-coder: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
components:
- build-orchestration
created: '2026-06-18'
depends_on:
- BO-1300d
- BO-1300a-2
- BO-1300b-1
files_touched:
- docs/acceptance-criteria/build-orchestration/BO-1300-independent-spot-check/BO-1300d.yaml
- docs/architecture/components/build-ticket-workflow-dispatch.md
- docs/architecture/adrs/ADR-006-flatten-supervisor-chain.md
priority: high
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: BO-1300d-1
status: todo
title: The same three-reviewer spot-check runs automatically as the closing step of
  a build
---

# The same three-reviewer spot-check runs automatically as the closing step of a build

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BO-1300d-1` — The same three-reviewer spot-check runs automatically as the closing step of a build — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BO-1300d-1`. Component: `build-orchestration`. Assigned agent: `python-coder`. Estimated complexity: `M`.

## Acceptance Criteria

```gherkin
Given a feature has been built through /build-feature and all of its tickets are
  implemented and merged
When the build reaches its closing steps
Then the same three-reviewer spot-check pass used by the on-demand command runs
  automatically against the just-built feature, without the user having to ask for it
And the spot-check pass runs AFTER the feature's tickets are implemented and merged
And the spot-check pass runs BEFORE the feature is declared done
And the automatic run dispatches the same three distinct-angle reviewers exercising the
  feature (it reuses the shared spot-check pass rather than a separate copy of the logic)
```

## Sign-offs

- [ ] python-coder
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit
- [ ] pull-request

## Comments
