id: BP-1300c-1
components:
  - build-pipeline
title: "Self-description enforcement fails the drive instead of warning"
component: build-pipeline
level: L2
status: active
req_status: active
work_status: todo
readiness: approved
priority: high
roadmap_phase: phase_1
criteria: |
  Given the self-description enforcement check, which is warn-only today, and a violation is
    present (an agent or skill is missing its required self-description),
  When the check runs inside an automated drive,
  Then the check reports the violation as a failure and returns a non-zero exit,
  And the drive stops on that check rather than continuing past a warning,
  And the failure names the artifact missing its self-description.
depends_on:
  - BP-1300c
doc_links:
  - path: docs/how-to/managing-pre-commit-hooks.md
    relationship: related
    status: exists
  - path: docs/architecture/components/commit-guardian.md
    relationship: describes
    status: exists
assigned_agent: python-coder
estimated_complexity: M
it_requirements:
  - "In automated-drive mode the self-description check must escalate a violation from warning to failure (non-zero exit)."
  - "The failure must name the artifact that lacks a self-description."
  - "Escalation must be applied to the existing self-description validation path (validate_agent_self_description in scripts/build_phases.py), not a new parallel check."
delivers_to:
  agent: python-coder
  contract: "Self-description enforcement that fails during automated drives"
expects_from: null
origin_agent: business-analyst
created: 2026-07-07
amended_by:
  - it-po-v3
superseded_by: null
covered_by:
  - BP-1300c-1-i
implemented_by: []
