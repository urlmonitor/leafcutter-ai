---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  pr-reviewer: needed
  pull-request: needed
  python-coder: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
complexity: low
components:
- build-orchestration
created: '2026-07-15'
depends_on:
- BO-2200a-3
files_touched:
- scripts/ac_store/generate_ticket_from_ac.py
priority: medium
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: BO-2200a-3-i
status: todo
title: A cost risk surface and an unclassified AC do not demand documentation
---

# A cost risk surface and an unclassified AC do not demand documentation

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BO-2200a-3-i` — A cost risk surface and an unclassified AC do not demand documentation — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BO-2200a-3-i`. Component: `build-orchestration`. Assigned agent: `python-coder`. Estimated complexity: `S`. Complexity: `low`.

## Acceptance Criteria

```gherkin
Given the declarative `documentation_gates` policy and the ticket generator building
  the agents map for a leaf AC,
When the AC's `change_target` is `code` and its `risk_surface` is `cost`,
Then the generated agents map does NOT include `documentation-expert`
  (the `cost` risk surface is explicitly excluded from the triggering set).
And when the AC carries neither a `change_target` nor a `risk_surface` field at all
  (an AC that predates the computed-gates classification), the generator completes
  without error and does NOT force `documentation-expert` from the documentation
  policy on the basis of absent classification.
```

## Test Requirements

```yaml
tests:
- name: test_risk_surface_cost_does_not_trigger_doc_expert
  file: unit_tests/test_bo_2200a_3_i.py
  covers:
  - BO-2200a-3-i
  asserts: change_target=code, risk_surface=cost yields no documentation-expert.
  framework: unittest
  type: behavioral
- name: test_unclassified_ac_completes_without_forcing_doc_expert
  file: unit_tests/test_bo_2200a_3_i.py
  covers:
  - BO-2200a-3-i
  asserts: An AC with neither change_target nor risk_surface generates without error
    and does not force documentation-expert from the doc policy.
  framework: unittest
  type: behavioral
```

## Implementation Notes

```yaml
config_schema_fragment:
  edge_negatives:
    description: risk_surface=cost is excluded from the triggering set; a missing/None
      change_target and risk_surface must not raise or force a documentation demand.
    type: object
constraints:
- 'risk_surface: cost must be excluded from the triggering set.'
- A missing/None classification must not raise KeyError and must not default to a
  documentation demand — fail safe toward no-demand when there is nothing to classify.
n_location_rule: '1'
post_write_commands: []
reference_file_path: scripts/ac_store/generate_ticket_from_ac.py
required_skills:
- python-coder
```

## Sign-offs

- [ ] python-coder
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit
- [ ] pull-request

## Comments
