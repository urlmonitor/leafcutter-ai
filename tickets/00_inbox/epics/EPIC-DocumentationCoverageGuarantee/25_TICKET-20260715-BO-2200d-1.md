---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  pr-reviewer: needed
  pull-request: needed
  python-coder: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
complexity: medium
components:
- build_orchestration
created: '2026-07-15'
depends_on:
- BO-2200d
- BO-2200a-1
files_touched:
- config/guardrail_gates.yaml
priority: medium
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: BO-2200d-1
status: todo
title: documentation-expert is added via the post-coder surface path, not the pre-coder
  flow-change slot
---

# documentation-expert is added via the post-coder surface path, not the pre-coder flow-change slot

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BO-2200d-1` — documentation-expert is added via the post-coder surface path, not the pre-coder flow-change slot — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BO-2200d-1`. Component: `build-orchestration`. Assigned agent: `python-coder`. Estimated complexity: `M`. Complexity: `medium`.

## Acceptance Criteria

```gherkin
Given the ticket generator injects `documentation-expert` for a doc-triggering ticket,
When it decides through which path to add the agent,
Then `documentation-expert` is added via the per-surface (canonical-order) path, which
  orders it after the coder -- not via the pre-coder flow-change gate slot.
And `documentation-expert` is absent from all four flow-change-gate mandatory-agents
  lists.
And `architect-review` remains present in the pre-coder flow-change-gate lists (the
  removal of documentation-expert is surgical and does not remove other pre-coder gates).
```

## Test Requirements

```yaml
tests:
- name: test_documentation_expert_added_via_post_coder_path
  file: unit_tests/test_bo_2200d_1.py
  covers:
  - BO-2200d-1
  asserts: For a doc-triggering flow-change ticket, documentation-expert is injected
    via the per-surface canonical-order path (ordered after the coder), not the pre-coder
    flow-change slot.
  framework: unittest
  type: behavioral
- name: test_documentation_expert_absent_from_flow_change_gates
  file: unit_tests/test_bo_2200d_1.py
  covers:
  - BO-2200d-1
  asserts: documentation-expert is absent from all four flow_change_gates mandatory_agents
    lists.
  framework: unittest
  type: behavioral
- name: test_architect_review_retained_in_flow_change_gates
  file: unit_tests/test_bo_2200d_1.py
  covers:
  - BO-2200d-1
  asserts: architect-review remains in the pre-coder flow_change_gates lists after
    the surgical removal.
  framework: unittest
  type: behavioral
```

## Implementation Notes

```yaml
config_schema_fragment:
  flow_change_gates:
    description: documentation-expert removed from all four code|schema x contract_boundary|safety
      flow_change_gates mandatory_agents entries; architect-review retained; documentation-expert
      added via the per-surface canonical-order (post-coder) path instead.
    type: object
constraints:
- 'Remove documentation-expert from the pre-coder flow_change_gates mandatory_agents
  lists (all four: code|schema x contract_boundary|safety, ~lines 266-327) and add
  it via the per-surface canonical-order path (_CANONICAL_PHASE_ORDER) so it is ordered
  after the coder.'
- architect-review must stay in the flow_change_gates lists (pre-coder).
- 'Audit for double-injection: documentation-expert must appear exactly once in the
  resolved agents map.'
- The existing tests TestFlowChangeSequencing / TestBO540FlowChangeOrdering assert
  doc-expert BEFORE the coder — they must be updated to reflect the post-coder placement.
n_location_rule: all
post_write_commands: []
reference_file_path: config/guardrail_gates.yaml
required_skills:
- python-coder
```

## Sign-offs

- [ ] python-coder
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit
- [ ] pull-request

## Comments
