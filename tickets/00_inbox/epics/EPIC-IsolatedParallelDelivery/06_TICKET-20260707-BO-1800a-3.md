---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  pr-reviewer: needed
  pull-request: needed
  python-coder: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
components:
- build_orchestration
created: '2026-07-07'
depends_on:
- BO-1800a
files_touched:
- docs/architecture/adrs/ADR-018-agent-isolation-topology.md
- docs/architecture/components/worktree-manager.md
priority: medium
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: BO-1800a-3
status: todo
title: Removing a drive deletes both its working copy and its feature branch
---

# Removing a drive deletes both its working copy and its feature branch

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BO-1800a-3` — Removing a drive deletes both its working copy and its feature branch — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BO-1800a-3`. Component: `build-orchestration`. Assigned agent: `python-coder`. Estimated complexity: `S`.

## Acceptance Criteria

```gherkin
Given a drive was created as an independent clone with its own feature branch,
When the drive is removed via the lifecycle `remove` operation,
Then the drive's entire working-copy directory (including its own `.git`) is
  deleted from disk,
And the drive's feature branch is deleted,
And no `git worktree remove` / `git worktree prune` bookkeeping against a shared
  repository is required, because the drive was a standalone clone rather than a
  worktree.
```

## Sign-offs

- [ ] python-coder
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit
- [ ] pull-request

## Comments
