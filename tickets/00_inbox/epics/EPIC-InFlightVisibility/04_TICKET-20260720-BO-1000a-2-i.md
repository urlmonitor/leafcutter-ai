---
ac_traceability:
  id: BO-1000a-2-i
  path: docs/acceptance-criteria/build-orchestration/BO-1000-live-automation-progress/BO-1000a-2-i.yaml
advances_current_outcome: true
agents:
  commit: needed
  pr-reviewer: needed
  pull-request: needed
  python-coder: needed
  test-runner: needed
  test-writer: needed
change_target: pipeline
complexity: low
components:
- build_orchestration
created: '2026-07-20'
depends_on:
- BO-1000a-2
files_touched:
- templates/workflows-js/finalize-feature.js
priority: medium
requires_adr: false
requires_diagram: false
risk_surface: internal
roadmap_phase: phase_1
source_ac: BO-1000a-2-i
status: todo
title: The intermediate closure step and early pre-flight aborts do not break the
  X-of-N counting
---

# The intermediate closure step and early pre-flight aborts do not break the X-of-N counting

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BO-1000a-2-i` — The intermediate closure step and early pre-flight aborts do not break the X-of-N counting — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BO-1000a-2-i`. Component: `build-orchestration`. Assigned agent: `python-coder`. Estimated complexity: `S`. Complexity: `low`.

## Acceptance Criteria

```gherkin
Given the finalize sequence contains an intermediate closure step that runs
  between the tests step and the PR-merge step,
When the start-of-step positions are announced,
Then that intermediate step is announced with a position that fits the
  "Step X of N" scheme without breaking the monotonic ordering and without
  making the announced N inconsistent with the other steps' N,
And when the run aborts during pre-flight before the first numbered step
  begins, no start-of-step line claims a position for a step that never
  started.
```

- [ ] AC-1: that intermediate step is announced with a position that fits the
- [ ] AC-2: when the run aborts during pre-flight before the first numbered step

## Test Requirements

```yaml
tests:
- name: test_intermediate_closure_step_gets_monotonic_position_without_changing_n
  file: unit_tests/workflows/test_bo_1000a_2_i.py
  covers:
  - BO-1000a-2-i
  asserts: The intermediate closure step (between the tests step and the PR-merge
    step) is announced with a 'Step X of N' position that keeps the ordering monotonic
    and leaves N consistent with the other steps' N.
  framework: unittest
  type: behavioral
- name: test_preflight_abort_emits_no_numbered_start_line
  file: unit_tests/workflows/test_bo_1000a_2_i.py
  covers:
  - BO-1000a-2-i
  asserts: When the run aborts during pre-flight before the first numbered step begins,
    no start-of-step line claims a 'Step X of N' position for a step that never started.
  framework: unittest
  type: behavioral
```

## Implementation Notes

```yaml
config_schema_fragment: null
n_location_rule: all
notes: "Policy constraints:\n- The intermediate closure step (between the tests step\
  \ and the PR-merge\n  step) must be included in the single declared step sequence\
  \ so it receives\n  a monotonic position and does not perturb N for the other steps.\n\
  - Pre-flight aborts occur BEFORE the first numbered step: no start-of-step\n  line\
  \ may be emitted for a step that never began. Numbered narration must\n  start only\
  \ once the numbered sequence starts; pre-flight failures use a\n  distinct non-numbered\
  \ message.\n- Reuse the same declared-sequence source of truth as BO-1000a-2 (do\
  \ not\n  introduce a second counter for the closure step).\n- No new config key.\
  \ ADR-001 build.py round-trip parity.\n"
post_write_commands:
- python scripts/build.py --target-dir .
reference_file_path: templates/workflows-js/finalize-feature.js
required_skills:
- python-coder
```

## Sign-offs

- [ ] test-writer
- [ ] python-coder
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit
- [ ] pull-request

## Comments
