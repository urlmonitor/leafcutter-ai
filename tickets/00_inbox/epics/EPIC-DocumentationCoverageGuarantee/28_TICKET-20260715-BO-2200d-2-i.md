---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  pr-reviewer: needed
  pull-request: needed
  python-coder: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
complexity: low
components:
- build-orchestration
created: '2026-07-15'
depends_on:
- BO-2200d-2
files_touched:
- scripts/ac_store/generate_ticket_from_ac.py
priority: medium
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: BO-2200d-2-i
status: todo
title: With multiple coders, documentation-expert is ordered after the last coder
---

# With multiple coders, documentation-expert is ordered after the last coder

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BO-2200d-2-i` — With multiple coders, documentation-expert is ordered after the last coder — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BO-2200d-2-i`. Component: `build-orchestration`. Assigned agent: `python-coder`. Estimated complexity: `S`. Complexity: `low`.

## Acceptance Criteria

```gherkin
Given a documentation-required ticket whose phase order includes more than one coder --
  for example both `python-coder` and `frontend-coder` -- alongside `documentation-expert`,
When the phase order is evaluated,
Then the index of `documentation-expert` is greater than the index of EVERY coder in the
  ticket (it is ordered after the last coder, not merely after the first).
And `documentation-verifier` still runs after `documentation-expert` and last before commit.
```

## Test Requirements

```yaml
tests:
- name: test_doc_expert_ordered_after_last_coder
  file: unit_tests/test_bo_2200d_2_i.py
  covers:
  - BO-2200d-2-i
  asserts: With python-coder and frontend-coder both present, index(documentation-expert)
    is greater than the index of every coder (after the last coder).
  framework: unittest
  type: behavioral
- name: test_verifier_after_doc_expert_and_last_before_commit_multi_coder
  file: unit_tests/test_bo_2200d_2_i.py
  covers:
  - BO-2200d-2-i
  asserts: documentation-verifier still runs after documentation-expert and immediately
    before commit in the multi-coder case.
  framework: unittest
  type: behavioral
```

## Implementation Notes

```yaml
config_schema_fragment:
  multi_coder_ordering:
    description: 'index(documentation-expert) > max(index of every coder present:
      python-coder, sql-coder, frontend-coder); documentation-verifier after documentation-expert
      and last before commit.'
    type: object
constraints:
- The post-coder placement must hold against the LAST coder when several coders are
  present (python-coder, sql-coder, frontend-coder).
- A placement that only clears the first coder would let docs be written before another
  coder finishes — assert against the maximum coder index.
n_location_rule: '1'
post_write_commands: []
reference_file_path: scripts/ac_store/generate_ticket_from_ac.py
required_skills:
- python-coder
```

## Sign-offs

- [ ] python-coder
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit
- [ ] pull-request

## Comments
