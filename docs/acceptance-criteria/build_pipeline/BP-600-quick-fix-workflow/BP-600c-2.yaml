id: BP-600c-2
components:
  - build_pipeline
title: "Quick-fix workflow runs the new test and confirms it fails (red phase)"
component: build-pipeline
level: L2
status: active
readiness: reviewed
priority: medium
work_status: done
criteria: |
  Given the test-writer has produced a test file for the diagnosed bug,
  When the workflow reaches the red-phase verification step,
  Then it dispatches the test-runner agent targeting the new test file,
  And the test-runner reports at least one FAILED result for the new test,
  And if the test unexpectedly passes the workflow halts with a warning:
    "The test passes before the fix was applied -- the diagnosis may be
    incorrect or the bug is already fixed."
depends_on:
  - BP-600c
  - BP-600c-1
doc_links:
  - path: docs/architecture/agent_delivery_workflows.md
    relationship: describes
    status: exists
  - path: docs/architecture/adrs/ADR-006-flatten-supervisor-chain.md
    relationship: describes
    status: exists
assigned_agent: workflow-architect
estimated_complexity: M
it_requirements:
  - "Must operate at depth 0 per ADR-006 — phase agents dispatched at depth 1 via the Agent tool"
  - "Requires Claude Code >= 2.1.154 for JS workflow script support"
  - "Must handle errors gracefully per the project error handling policy"
  - "Must parse test-runner output to distinguish PASSED, FAILED, and ERROR outcomes"
  - "Must halt the workflow (not just warn) if the test passes before the fix is applied"
delivers_to:
  agent: workflow-architect
  contract: "Red-phase result: { test_file: string, outcome: 'FAILED'|'PASSED'|'ERROR', failure_message: string|null, assertion_location: string|null }"
expects_from:
  ac_id: BP-600c-1
  contract: "Test file absolute path (string) written by the test-writer agent"
origin_agent: business-analyst-v3
created: 2026-06-05
amended_by: []
superseded_by: null
covered_by: [BP-600c-2-i]
  - "unit_tests/workflows/test_quick_fix_workflow.py"
implemented_by: []
change_target: pipeline
risk_surface: contract_boundary
