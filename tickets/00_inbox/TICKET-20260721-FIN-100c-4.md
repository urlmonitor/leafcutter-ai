---
ac_traceability:
  id: FIN-100c-4
  path: docs/acceptance-criteria/build_pipeline/FIN-100-pre-merge-safety-gate/FIN-100c-4.yaml
advances_current_outcome: true
agents:
  ac-fulfillment-gate: needed
  ac-validator: needed
  architect-review: needed
  commit: needed
  pr-reviewer: needed
  pull-request: needed
  python-coder: needed
  test-runner: needed
  test-writer: needed
change_target: pipeline
complexity: medium
components:
- build_pipeline
created: '2026-07-21'
depends_on: []
files_touched:
- templates/workflows-js/finalize-feature.js
- templates/workflows/finalize-feature.md
- unit_tests/workflows/test_finalize_feature_preflight.py
priority: high
requires_adr: false
requires_diagram: false
risk_surface: contract_boundary
roadmap_phase: phase_1
source_ac: FIN-100c-4
status: todo
title: Null baseline triggers a targeted main-HEAD rerun instead of blanket regression
---

# Null baseline triggers a targeted main-HEAD rerun instead of blanket regression

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `FIN-100c-4` — Null baseline triggers a targeted main-HEAD rerun instead of blanket regression — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `FIN-100c-4`. Component: `finalize`. Assigned agent: `python-coder`. Estimated complexity: `M`. Complexity: `medium`.

## Acceptance Criteria

```gherkin
Given Step 0 failed to capture a baseline (baseline_failures is null)
And the post-merge test run reports a non-empty set of failing tests
  (for example the 3 deploy-dependent tests observed on 2026-07-15:
   "test_plan_feature_sha_matches", "test_plan_feature_size_matches",
   "test_commit_guardian_imports")
When the finalize workflow reaches the regression-triage stage
Then it does NOT immediately classify every post-merge failure as a regression
And it establishes a checkout of main HEAD
And it runs the project build/deploy step against that checkout so that
  deploy-dependent tests are evaluated under the same build/deploy conditions
  as the post-merge run
And it re-executes the failing tests against main HEAD to determine which of
  them also fail there
```

- [ ] AC-1: the post-merge test run reports a non-empty set of failing tests
- [ ] AC-2: it does NOT immediately classify every post-merge failure as a regression
- [ ] AC-3: it establishes a checkout of main HEAD
- [ ] AC-4: it runs the project build/deploy step against that checkout so that
- [ ] AC-5: it re-executes the failing tests against main HEAD to determine which of

## Test Requirements

```yaml
tests:
- name: test_null_baseline_with_failures_does_not_blanket_regress
  file: unit_tests/workflows/test_fin_100c_4.py
  covers:
  - FIN-100c-4
  asserts: With baseline_failures=null and a non-empty post-merge failure set, the
    workflow does NOT immediately mark every failure regression; it enters the recovery
    branch instead of the blanket-regression path.
  framework: pytest
  type: unit
- name: test_null_baseline_establishes_main_head_checkout
  file: unit_tests/workflows/test_fin_100c_4.py
  covers:
  - FIN-100c-4
  asserts: The recovery branch establishes a detached checkout of origin/main HEAD
    before re-running the failing tests.
  framework: pytest
  type: unit
- name: test_null_baseline_runs_build_before_rerun
  file: unit_tests/workflows/test_fin_100c_4.py
  covers:
  - FIN-100c-4
  asserts: The recovery branch runs `python3 scripts/build.py --target-dir <checkout>`
    against the main-HEAD checkout BEFORE executing the tests, matching the Step 0
    / Step 3 build/deploy step (deploy parity with FIN-100a-4).
  framework: pytest
  type: unit
- name: test_null_baseline_reexecutes_failing_tests_on_main
  file: unit_tests/workflows/test_fin_100c_4.py
  covers:
  - FIN-100c-4
  asserts: The recovery branch re-executes the post-merge failing tests against main
    HEAD and records each test's pass/fail result on main.
  framework: pytest
  type: unit
```

## Implementation Notes

```yaml
"- SURFACE: this is finalize-feature.js workflow-body logic (python-coder), NOT\n\
  \  the triage agent — the triage agent (templates/agents/test-failure-triage.md)\n\
  \  is read-only and MUST NOT re-run tests. The rerun lives entirely in the\n  workflow;\
  \ only classification stays in the triage agent (FIN-100c-7/8).\n- TRIGGER: the\
  \ recovery branch must fire ONLY when baselineFailures === null AND\n  post_merge_failures\
  \ is non-empty. A null baseline with zero post-merge failures\n  still proceeds\
  \ normally (no rerun, no halt) — do not regress the existing\n  zero-failure fast\
  \ path (~L713).\n- INSERTION POINT: place the branch between post-merge failure\
  \ collection\n  (finalize-feature.js ~L713) and the triage dispatch (~L747), so\
  \ the recovered\n  baseline is available before the triage set-difference runs.\n\
  - BUILD/DEPLOY PARITY: the main-HEAD checkout must run the SAME build/deploy step\n\
  \  used by Step 0 (~L440) and Step 3 (~L677) — `python3 scripts/build.py\n  --target-dir\
  \ <checkout>` — BEFORE re-executing tests, so deploy-dependent tests\n  (e.g. scripts/commit_guardian/*)\
  \ are judged under identical build/deploy state\n  (parity with FIN-100a-4). If\
  \ Step 0/Step 3 downgrade self-description\n  enforcement, the rerun must use the\
  \ same level.\n- WORKTREE LIFECYCLE: reuse the existing temporary-worktree pattern\
  \ — create via\n  `git worktree add --detach` against origin/main, and guarantee\
  \ cleanup on every\n  exit path (success, halt, error) with no /tmp leak (mirror\n\
  \  cleanupBaselineWorktree).\n- DETERMINISM (E2): no Date.now() (banned in E2);\
  \ derive any temp path from\n  args.baseline_ts, exactly as the Step 0 baseline\
  \ worktree path does (~L266).\n- The null baseline alone MUST NOT halt finalization;\
  \ the halt decision defers to\n  triage over the recovered baseline (FIN-100c-6/7/8).\
  \ The conservative halt is\n  reserved for the rerun-unavailable fallback (FIN-100c-9).\n\
  - ERROR HANDLING: checkout/build/rerun I/O must be wrapped and logged at WARNING+\n\
  \  per the project error-handling policy; any failure routes to the FIN-100c-9\n\
  \  fallback, never crashes finalize.\n"
```

## Agent Contracts

### Delivers To

- **Agent:** test-failure-triage
- **Contract:** provides a recovered baseline derived from a main-HEAD rerun in place of the null baseline, so triage can distinguish pre-existing from new failures

### Expects From

- **Contract:** provides baseline_failures=null and a non-empty post_merge_failures list
- **Contract:** returns the post-merge failure list with test IDs in file::test_name format

## Sign-offs

- [ ] architect-review
- [ ] test-writer
- [ ] python-coder
- [ ] test-runner
- [ ] pr-reviewer
- [ ] ac-validator
- [ ] ac-fulfillment-gate
- [ ] commit
- [ ] pull-request

## Comments
