id: KM-KGS-100e-7
readiness: approved
priority: high
title: "How-to guide: declare a component on a knowledge item and query it back"
component: knowledge-management
components:
  - knowledge-management
level: L2
status: active
req_status: draft
work_status: todo
roadmap_phase: phase_1
created_by: "docs/acceptance-criteria/knowledge-management/KM-KGS-100-knowledge-graph-surfaces/KM-KGS-100e.yaml"
criteria: |
  Given a reader who wants to make a knowledge item (an acceptance criterion, a
    ticket, a doc, an agent, a skill, or a roadmap entry) discoverable by component
  When they follow the how-to guide
  Then the guide shows how to declare the item's component membership so it is
    accepted at commit time
  And the guide shows how to query a component through the knowledge map to get its
    criteria and, through them, the delivering code and tests
  And the guide states which items are required to declare a component and which
    surface is exempt because it declares no component-membership relationship
depends_on:
  - KM-KGS-100e
  - KM-KGS-100e-1
  - KM-KGS-100e-6
doc_links:
  - path: templates/skills/knowledge-query/SKILL.md
    relationship: describes
    status: exists
  - path: docs/reference/ac-schema.md
    relationship: context
    status: exists
  - path: config/paths.json
    relationship: context
    status: exists
assigned_agent: documentation-expert
estimated_complexity: S
it_requirements:
  - "Author the how-to under docs/how-to/ following the project doc-frontmatter conventions (type: how-to plus the required frontmatter fields) AND — per this feature's own rule — include a non-empty `components: [knowledge-management]` frontmatter list so the guide itself passes the KM-KGS-100e-2 doc-frontmatter check."
  - "Document both directions: (a) how to declare a knowledge item's `components` membership so it passes the commit-time check (KM-KGS-100e-1/-2/-3), and (b) how to run the component query (KM-KGS-100e-6) to get a component's criteria and, through them, the delivering code and tests."
  - "State which surfaces are REQUIRED to declare a component and that the glossary is exempt because its config/paths.json edge_fields is empty — describe the rule generically (driven by paths.json), not as a fixed named list."
  - "Keep the guide consistent with the reconciled docs/reference/ac-schema.md: the `components` LIST is the authoritative membership field the graph reads and the checks enforce."
delivers_to: null
expects_from:
  ac_id: KM-KGS-100e-6
  contract: "A working component-query path whose invocation and output the guide documents"
origin_agent: business-analyst-v3
created: 2026-07-08
amended_by: []
superseded_by: null
covered_by: []
implemented_by: []
change_target: docs
risk_surface: internal
