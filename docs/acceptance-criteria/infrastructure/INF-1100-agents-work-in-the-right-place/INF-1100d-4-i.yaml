id: INF-1100d-4-i
components:
  - infrastructure
  - testing_quality
title: "The address check flags real-looking addresses, allows obvious placeholders, and leaves unshipped history alone"
component: infrastructure
level: L3
status: active
req_status: draft
work_status: todo
readiness: draft
priority: medium
roadmap_phase: phase_1
criteria: |
  Given six shipped files, each containing one of these lines —
      S1: postgresql://localhost:5403/LIVE          (no credentials),
      S2: postgres://app:app@127.0.0.1:5432/app     (other scheme spelling),
      S3: postgresql://<user>:<password>@<host>:<port>/<database>,
      S4: ${DB_CONNECTION_TEST},
      S5: {{config.testing_context.db_connection_test}},
      S6: the prose "set testing_context.db_connection_test to your test
          database's address",
    and S7, a shipped documentation page that quotes
      postgresql://trader:trader@localhost:5403/LIVE as history (for example
      an analysis page recounting the incident),
    and one unshipped project record — a requirement record in the AC store
      that quotes the same address,
  When the shipped-address check runs,
  Then S1 and S2 are flagged — a concrete host and port is an address even
      without a password, and both scheme spellings count,
    And S3, S4, S5 and S6 are not flagged — angle-bracket placeholders,
      environment-variable references, settings placeholders and prose that
      names the setting are how the guidance should read,
    And S7 is flagged like any other shipped file — being history is no
      exemption, and the check has no list of excluded paths; the remedy is
      to replace the address in the page with a placeholder, after which S7
      passes,
    And the unshipped project record is not flagged, because it is not part
      of what a build puts into an install.
depends_on:
  - INF-1100d-4
doc_links:
  - path: docs/reference/skills-config-fields.md
    relationship: relates-to
    status: exists
  - path: docs/analysis/2026-09-25-test-writers-prove-failure-not-discrimination.md
    relationship: relates-to
    status: exists
    relevance: "A shipped history page that quotes the address (the S7 case). Its quotes are replaced with a placeholder by INF-1100d-5."
assigned_agent: python-coder
estimated_complexity: S
delivers_to: null
expects_from:
  ac_id: INF-1100d-4
  contract: "The shipped-address check and its definition of 'shipped'."
origin_agent: business-analyst
created: 2026-09-25
amended_by: []
superseded_by: null
covered_by: []
implemented_by: []
change_target: code
risk_surface: safety
notes: |
  EDGE CASES on both sides of the line: S1/S2 stop a narrow literal match from
  passing; S3-S6 stop an over-broad check from forbidding the guidance
  INF-1100d-5 needs to write.

  USER DECISION AT THE BA GATE (Q5): shipped documentation that quotes the
  address as history is edited to use a placeholder. It is never exempted by
  path. The check therefore has NO exclusion list. S7 is the case that proves
  it: it must fail before the edit and pass after. The doc edits themselves are
  owned by INF-1100d-5.

  No shipped code needs a literal of the old address (the user dropped
  stale-default detection at the BA gate, NQ1), so the check has no exceptions
  of any kind.
