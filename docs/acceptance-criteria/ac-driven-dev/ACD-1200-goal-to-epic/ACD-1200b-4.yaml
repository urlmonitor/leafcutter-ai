id: ACD-1200b-4
components:
  - ac_driven_dev
readiness: approved
priority: high
title: "Non-interactive run with an approval flag clears the readiness gate; without a flag and without a TTY it fails clearly"
component: ac-driven-dev
level: L2
status: active
req_status: draft
work_status: done
roadmap_phase: phase_1
created_by: "docs/acceptance-criteria/ac-driven-dev/ACD-1200-goal-to-epic/ACD-1200b.yaml"
criteria: |
  Given goal_to_epic.py is invoked non-interactively (no controlling TTY on
    standard input) with an explicit approval flag (for example --yes or
    --approved-only),
  When execution reaches the readiness gate that would otherwise prompt the user,
  Then the command proceeds past the gate without prompting for input,
  And the command does not exit non-zero merely because no interactive input is
    available,
  And when --approved-only is the chosen flag, generation proceeds with only the
    already-approved leaf ACs and excludes the unapproved ones.

  Given goal_to_epic.py is invoked with no controlling TTY on standard input and
    with no approval flag supplied,
  When execution reaches the readiness gate,
  Then the command exits with a non-zero status and prints a clear message
    stating that approval input is required and naming the flag to pass, rather
    than hanging waiting for input that can never arrive.

  Given goal_to_epic.py is invoked interactively (a TTY is present) with no
    approval flag,
  When execution reaches the readiness gate,
  Then the existing interactive prompt behavior is unchanged.
depends_on: [ACD-1200b]
doc_links:
  - path: scripts/goal_to_epic.py
    relationship: modifies
    status: exists
  - path: docs/reference/ac-schema.md
    relationship: describes
    status: exists
  - path: docs/architecture/components/ac-driven-dev.md
    relationship: describes
    status: exists
  - path: docs/architecture/adrs/ADR-010-ac-store-as-authoritative-backlog.md
    relationship: describes
    status: exists
delivers_to: null
expects_from: null
origin_agent: business-analyst-v3
created: 2026-07-15
amended_by: []
superseded_by: null
covered_by:
- unit_tests/ac_driven_dev/test_acd_1200b_4.py
implemented_by:
- tickets/TICKET-20260720-ACD-1200b-4.md
assigned_agent: python-coder
estimated_complexity: M
it_requirements:
  config_schema_fragment: null
  reference_file_path: "scripts/goal_to_epic.py"
  n_location_rule: "1 (the readiness-gate approve / review-all / cancel prompt site)"
  required_skills:
    - python-coder
  post_write_commands:
    - "python scripts/build.py --target-dir ."
  notes:
    - "A new approval flag (for example --yes / --approved-only) must let a non-interactive (no-TTY) run proceed past the readiness gate without prompting, and must not exit non-zero merely because interactive input is unavailable."
    - "With --approved-only, generation must proceed using only already-approved leaf ACs and exclude unapproved ones."
    - "No TTY and no flag must exit non-zero with a clear message that names the flag to pass, never hang waiting for input that can never arrive."
    - "Interactive (TTY present, no flag) behaviour must remain unchanged."
    - "TTY detection must use the actual controlling-terminal state of standard input, not an environment guess."
test_spec:
  - name: test_no_tty_with_flag_proceeds
    target_dir: unit_tests/ac_driven_dev/
    framework: unittest
    type: unit
    description: "Simulate no controlling TTY plus --yes; assert the command proceeds past the readiness gate and exits zero."
  - name: test_approved_only_excludes_unapproved
    target_dir: unit_tests/ac_driven_dev/
    framework: unittest
    type: unit
    description: "Invoke with --approved-only over a goal with mixed readiness; assert generation includes only already-approved leaves and excludes unapproved ones."
  - name: test_no_tty_no_flag_fails_clearly
    target_dir: unit_tests/ac_driven_dev/
    framework: unittest
    type: unit
    description: "Simulate no TTY and no approval flag; assert a non-zero exit and a message that names the flag to pass (no hang)."
  - name: test_tty_no_flag_prompts_as_before
    target_dir: unit_tests/ac_driven_dev/
    framework: unittest
    type: unit
    description: "Simulate a TTY present with no flag; assert the existing interactive prompt behaviour is unchanged."
change_target: pipeline
risk_surface: internal
