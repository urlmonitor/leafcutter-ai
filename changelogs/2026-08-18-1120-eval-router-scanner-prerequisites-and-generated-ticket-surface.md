---
title: "Route proof by the kind of work — TQ-200c eval router, ACD-400a-5 scanner prerequisites, BP-1100a surface derivation"
date: "2026-08-18"
time: "11:20"
type: manual
components:
  - testing_quality
  - ac_driven_dev
  - build_pipeline
  - build_orchestration
  - ac_store
summary: "Running the build against the real store found three ways the pipeline quietly asks for the wrong thing: prompt work is asked for no proof at all, the scanner throws away real prerequisites, and generated tickets name files the work never touches while omitting the one it does. All three are now written down as requirements."
description: "1 commit, 26 files — 14 new AC records, 3 parent back-link repairs, 7 corrections to records merged the same day, 2 regenerated product-truth files. No production code. TQ-200c (new L1 under the existing TQ-200 eval-coverage L0, 9 records): an AC assigned to llm-expert generates a ticket with no ## Test Requirements block and no test-writer in its agents map, silently discarding an authored test_spec of any size, while the readiness report prints '[PASS] non-code AC — no test contract required' and skips its own would-the-guard-pass check because that check is itself gated on is_code_ac. Measured over the store: 444 ACs assigned to llm-expert, 124 named test contracts on them, 115 of those tests exist nowhere in unit_tests/, and 85 of the missing sit on ACs already work_status: done across 39 records. The cause is NOT the _FALLBACK_PRODUCERS frozenset at generate_ticket_from_ac.py:1073 — that is only reached when the registry cannot be loaded. config/agent_registry.json does populate `produces` across seven values (production_code 9, test_artifact 8, documentation 6, review_verdict 7, orchestration 10, analysis 19, prompt 1) and llm-expert declares produces: 'prompt'; _load_production_code_agents:683 collapses that whole vocabulary to == 'production_code', so 'prompt' is treated identically to 'orchestration', a value that legitimately needs no code proof. The material a correct routing decision needs is already on disk and is discarded at the point of use. TQ-200c states the two-layer split: prompt work has a mechanically checkable half (frontmatter, registry parity, phaseOrder membership, dispatch sequence — already covered by hand-written deterministic tests, which TQ-200c-2-i explicitly forbids converting into evals) and a judgment half that needs the eval harness. c-1 makes 'judged' sayable in one vocabulary both sides share; c-2 derives which layers apply from what the work says about itself; c-3 allows both at once; c-4 lands the judged half as a scorable row in the harness that already exists; c-5 forbids silence. c-2's criteria defeat a list-based fix by construction via an unanticipated-producer clause, rename invariance, and no-free-pass-by-absence. Vocabulary precedes router (c-2 depends_on c-1) because a router built first could only emit unittest | pytest | nothing — the binary it exists to remove. ACD-400a-5 and -5-i: the scanner offers BO-2400g-1-i as ready while its declared prerequisite BO-2400g-1 is held, because _classify_ac (scan_ac_store.py:287-289) drops any depends_on entry that is a transitive ancestor. That is right for L0/L1 composites, which never reach done independently, and wrong for an L2 that carries its own criteria and owner and can be built — keeping such an edge cannot deadlock. The criteria carry two opposed subjects in one scenario so that both the current behaviour and the naive fix fail it: R beneath buildable P must be held naming P, while S beneath composite C must be ready with C named nowhere; deleting the exclusion re-creates ACD-400a-3's original deadlock. BP-1100a-3, -4 and -4-i: generating BO-2400g-2 produced a files_touched omitting templates/workflows-js/fast-lane-ship.js, the file it changes, while inventing scripts/build.py and templates/agents/change-scope-reviewer.md — the latter a file that AC's own criteria forbid touching. _build_files_touched:535 unions two sources; doc_links contribute only when relationship is in _EDIT_SURFACE_RELATIONSHIPS (:104), and list-form it_requirements fall back to _extract_paths_from_prose:496, a lexical token scan that produced both invented paths. So the omission is authoring shape and the invention is a code defect, and the ACs separate them. Corrections to #452: all ten ACs merged there had zero edit-surface doc_links, so their surface came from prose scraping alone; seven are corrected here by marking the real edit surface `modifies` (g-1, g-1-i, g-2 plus pr-reviewer.md, g-2-i, g-3, g-4, g-4-i), while BO-2400e-3, e-3-i and e-4 are deliberately left alone because their structured it_requirements carry reference_file_path and already emit a correct surface. Regenerating BO-2400g-2 now includes fast-lane-ship.js; the two invented paths remain because prose scraping is the defect BP-1100a-4 covers. The five llm-expert leaves gain depends_on: TQ-200c-2 so the scanner holds them until the router exists rather than offering tickets that would carry no tests — verified, all five now report blocked_by TQ-200c-2. The edge is added to each leaf directly rather than through g-2/g-4 because the ancestor defect strips the parent edge: BO-2400g-4-i is held by TQ-200c-2 alone despite declaring BO-2400g-4, which is the defect demonstrating itself. ACD-400a work_status corrected done -> in_progress: staging the parent made check-done-proof evaluate it and surfaced that the composite claimed done while ACD-400a-1 and -2 were both still todo — already false before a-5 existed. todo was tried first and is wrong the other way, deriving impl_status=not_started for the ac-lifecycle 'scan' step, which would claim the scanner does not exist; generate_product_truth.py then propagated in_progress into the flow (scan step done -> in_progress, impl_summary 3/3/4 -> 2/4/4). Its covered_by also read [a-1, a-2] while a-3 and a-4 had existed on disk since 2026-08-12, stale by two, now repaired with a-5; the drift survived because check_ac_parent_covered_by compares only files present in a commit's index. Verified: 3,076 store files parse and zero leaked tool-call literals store-wide (one authoring agent hit silent Write corruption and repaired it, confirmed independently); parent rewrites lossless at key level against HEAD (ACD-400a 27->28 keys, BP-1100a 23->24, TQ-200 22->22, zero lost); validate_ac_schema.py over explicit globs green on all four touched trees; zero dependency cycles across 3,075 nodes by both the repo's check and an independent colouring walk. All three configured agent evals were run locally to satisfy the staleness gate and all passed — pt-classifier 88.89% (16/18, threshold 70), mock-data-author 100% (3/3, threshold 66), flow-author 100% (3/3, threshold 66) — the first eval results this repo has produced. Recorded, not fixed: 144 composites marked done carry no direct covers tag and would fail the BO-2500b presence gate if staged, which disagrees with done_proof's own composite roll-up path (BO-2500a-6); 19 composites are falsely done with at least one unfinished child; the eval staleness gate tells the user to re-stage a result that scripts/evals/results/.gitignore makes unstageable; and 39 done ACs still carry 85 named tests that were never written. Everything new is readiness: reviewed, nothing approved, and no new record carries assigned_agent or it_requirements — the IT PO pass has not run."
pr: null
commits:
  - 60e949825
breaking: false
---

## Entry

See description.
