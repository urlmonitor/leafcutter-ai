---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  llm-expert: needed
  pr-reviewer: needed
  pull-request: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
components:
- ac_store
created: '2026-06-11'
depends_on:
- ACS-500c
- ACS-500a-1
files_touched:
- docs/reference/ac-schema.md
- docs/architecture/adrs/ADR-007-ac-store-schema-id-format-enforcement.md
priority: high
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: ACS-500c-1
status: todo
title: BA agent checks for existing pattern before writing new behavioral AC
---

# BA agent checks for existing pattern before writing new behavioral AC

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `ACS-500c-1` — BA agent checks for existing pattern before writing new behavioral AC — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `ACS-500c-1`. Component: `ac-store`. Assigned agent: `llm-expert`. Estimated complexity: `M`.

## Acceptance Criteria

```gherkin
Given the business-analyst-v3 agent is decomposing an L1 AC into L2 behaviors,
And the store contains a pattern AC defining "sortable-table" behavior,
When the agent encounters a page requirement that includes sortable-table behavior
  (e.g. "the parts list supports column sorting"),
Then the agent produces an L2 AC with:
  implements_pattern referencing the existing sortable-table pattern AC ID,
  pattern_bindings mapping the page-specific values to the pattern's slots,
And the agent does NOT write a new standalone criteria body
  that restates the sorting behavior already defined in the pattern.
```

## Sign-offs

- [ ] llm-expert
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit
- [ ] pull-request

## Comments
