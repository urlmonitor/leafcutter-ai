---
ac_coverage: 3/3
ac_traceability:
  id: UXP-700b-1-ii
  path: docs/acceptance-criteria/ux-prototyping/UXP-700-truthful-project-record/UXP-700b-1-ii.yaml
advances_current_outcome: true
agents:
  ac-fulfillment-gate: needed
  ac-validator: signed_off
  architect-review: not_needed
  commit: needed
  documentation-expert: not_needed
  documentation-verifier: not_needed
  frontend-coder: not_needed
  pr-reviewer: not_needed
  pull-request: not_needed
  python-coder: signed_off
  sql-coder: not_needed
  test-runner: signed_off
  test-writer: signed_off
  user-surface-smoker: not_needed
change_target: code
complexity: low
components:
- ux_prototyping
created: '2026-09-09'
depends_on: []
files_touched: []
priority: medium
requires_adr: false
requires_diagram: false
risk_surface: internal
roadmap_phase: phase_1
source_ac: UXP-700b-1-ii
status: todo
title: Partial emptiness is reported per artifact type, not rounded to sound
---

# Partial emptiness is reported per artifact type, not rounded to sound

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `UXP-700b-1-ii` — Partial emptiness is reported per artifact type, not rounded to sound — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `UXP-700b-1-ii`. Component: `ux-prototyping`. Assigned agent: `python-coder`. Estimated complexity: `S`. Complexity: `low`.

## Acceptance Criteria

```gherkin
Given a record holding three journeys, zero example datasets and zero screens,
When the record's checker runs,
Then it names example data and screens as the types for which it read zero records,
And it does not name journeys,
And the outcome it reports is not the outcome for a record that was checked and found sound.
```

- [x] AC-1: it names example data and screens as the types for which it read zero records
- [x] AC-2: it does not name journeys
- [x] AC-3: the outcome it reports is not the outcome for a record that was checked and found sound.

## Test Requirements

```yaml
tests:
- name: test_mixed_store_names_only_the_empty_artifact_types
  file: unit_tests/product_truth/test_uxp_700b_1_ii.py
  covers:
  - UXP-700b-1-ii
  asserts: With three journeys and no example data or screens, the report names example
    data and screens and does not name journeys.
  framework: unittest
  type: integration
  angle: boundary
- name: test_mixed_store_outcome_is_not_the_clean_pass_outcome
  file: unit_tests/product_truth/test_uxp_700b_1_ii.py
  covers:
  - UXP-700b-1-ii
  asserts: A partially empty store does not report the outcome reserved for a sound
    checked store.
  framework: unittest
  type: integration
  angle: criterion
- name: test_uxp_700b_1_ii_reachable_from_entry_point
  file: unit_tests/product_truth/test_uxp_700b_1_ii.py
  covers:
  - UXP-700b-1-ii
  angle: reachability
  asserts: 'REQUIRED — invoke the production entry point (CLI, hook, slash command,
    workflow dispatch, or main()) as a subprocess/dispatch and assert the new behaviour
    actually occurs. Do NOT satisfy this by importing the function directly. The AC
    authored no test_spec, so the entry point is not declared: resolve it before writing
    this test, and do not delete this entry.'
```

## Agent Contracts

### Expects From

- **AC:** UXP-700b-1
- **Contract:** The per-type zero-record reporting this record exercises on a mixed store.

## Sign-offs

- [x] test-writer — 2026-09-09 15:40
- [x] python-coder — 2026-09-09 16:00
- [x] test-runner — 2026-09-09 16:12
- [x] ac-validator — 2026-09-09 16:30
- [ ] ac-fulfillment-gate
- [ ] commit

## Comments

### 2026-09-09 15:40 — test-writer (status: ok)
feedback-id: fb_2026-09-09_eb7b21fe
completion_manifest:
  tests_written: true
  red_baseline_confirmed: true
  cross_layer_seam_answer:
    result: covered
    producing_side: "docs/product-truth/scripts/generate_product_truth.py main() (real CLI subprocess) — writes index.json"
    consuming_side: "docs/product-truth/scripts/validate_product_truth.py main() (real CLI subprocess) — reads index.json and reports per-artifact-type emptiness"
  reachability_entry_point_answer:
    result: resolved
    entry_point: "python docs/product-truth/scripts/validate_product_truth.py (CLI via subprocess, main() guarded by if __name__ == '__main__': sys.exit(main()))"
Wrote unit_tests/product_truth/test_uxp_700b_1_ii.py (3 tests: boundary, criterion,
reachability — angle-tagged and AC-tagged per test) in worktree
C:\Users\Hendrik\Code\leafcutter\EPIC-TruthfulProjectRecord, since that is the worktree_path
this dispatch named for code/test work (this ticket record itself only exists on the
leafcutter-ai checkout's epic/uxp-700-product-truth branch — noted so a later reader is
not surprised the two live in different worktrees of the same repo).

UXP-700b-1 (this record's own prerequisite — "the per-type zero-record reporting this
record exercises on a mixed store") is still work_status: todo with no implementation
and no prior test-writer contract to inherit, so this test file specifies the minimal
wire contract itself: validate_product_truth.py's main() must print a final stdout JSON
line `{"outcome": <str>, "empty_types": [...]}` on an error-free run. Full rationale is in
the test file's module docstring.

Fixture: a schema-valid product-truth store (3 flow journeys, 0 mock-data, 0 mockups) is
built from scratch, with the REAL generate_product_truth.py and validate_product_truth.py
scripts copied byte-for-byte and run as two real subprocess CLI invocations (generator
first, to derive a self-consistent index.json via the actual single-writer; then
validator) — mirroring unit_tests/test_generate_product_truth_idempotency.py's existing
fixture convention but reached via subprocess instead of monkeypatched import, so all
three tests are simultaneously integration-grade, reachability-grade, and a genuine
generator→validator seam test.

Verification: `AC_ENFORCE_STRICT=1 python -m pytest unit_tests/product_truth/test_uxp_700b_1_ii.py -v`
→ exit 1, "3 failed" — all three assertions fail with a clean, diagnostic AssertionError
naming the missing `{outcome, empty_types}` stdout contract (not a fixture-setup error;
the generator+validator pipeline itself runs cleanly to completion, confirming the
fixture is sound and the failure signal is exactly the missing feature). Without
AC_ENFORCE_STRICT the repo's pytest_ac_enforcement plugin downgrades these to xfail
(exit 0) because UXP-700b-1-ii's own work_status is not yet `done` — this is expected
repo behaviour, not a red-baseline problem; the true red state was confirmed with the
strict flag as shown above.

red_baseline:
  - test_name: test_mixed_store_names_only_the_empty_artifact_types
    file: unit_tests/product_truth/test_uxp_700b_1_ii.py
    error: "AssertionError: validate_product_truth.py produced no stdout output. Expected the final stdout line to be a JSON object {'outcome': <str>, 'empty_types': [<str>, ...]} reporting, per artifact type, which types had zero records read — see this test module's docstring for the exact contract. Not yet implemented (UXP-700b-1 / UXP-700b-1-ii)."
  - test_name: test_mixed_store_outcome_is_not_the_clean_pass_outcome
    file: unit_tests/product_truth/test_uxp_700b_1_ii.py
    error: "AssertionError: validate_product_truth.py produced no stdout output. Expected the final stdout line to be a JSON object {'outcome': <str>, 'empty_types': [<str>, ...]} reporting, per artifact type, which types had zero records read — see this test module's docstring for the exact contract. Not yet implemented (UXP-700b-1 / UXP-700b-1-ii)."
  - test_name: test_uxp_700b_1_ii_reachable_from_entry_point
    file: unit_tests/product_truth/test_uxp_700b_1_ii.py
    error: "AssertionError: validate_product_truth.py produced no stdout output. Expected the final stdout line to be a JSON object {'outcome': <str>, 'empty_types': [<str>, ...]} reporting, per artifact type, which types had zero records read — see this test module's docstring for the exact contract. Not yet implemented (UXP-700b-1 / UXP-700b-1-ii)."

### 2026-09-09 16:00 — python-coder (status: ok)
feedback-id: fb_2026-09-09_096d9602
completion_manifest:
  code_implemented: true
  tests_green: true
  doc_enforcer_pass: true
  complexity_check_pass: true
  cross_layer_seam_answer:
    result: covered
    producing_side: "docs/product-truth/scripts/generate_product_truth.py main() (real CLI subprocess) — writes index.json"
    consuming_side: "docs/product-truth/scripts/validate_product_truth.py main() (real CLI subprocess) — prints the {outcome, empty_types} JSON payload consumed by the 3 red-baseline tests"
red_baseline_results:
  - test_name: test_mixed_store_names_only_the_empty_artifact_types
    result: green
  - test_name: test_mixed_store_outcome_is_not_the_clean_pass_outcome
    result: green
  - test_name: test_uxp_700b_1_ii_reachable_from_entry_point
    result: green

Implemented the per-artifact-type emptiness report in
docs/product-truth/scripts/validate_product_truth.py: main() now computes
`empty_types` (the subset of {flows, mock-data, mockups} with zero records
read) and an `outcome` in {checked-and-sound, degraded, nothing-examined,
failed} — "degraded" whenever some but not all types are empty, so a
partially empty store never rounds up to "checked-and-sound" — and prints
`{"outcome": <str>, "empty_types": [...]}` as the final stdout line on every
run. All 3 red-baseline tests in unit_tests/product_truth/test_uxp_700b_1_ii.py
are green, including under `AC_ENFORCE_STRICT=1`. Also spot-checked against
the REAL project store (not just the fixture): `python
docs/product-truth/scripts/validate_product_truth.py --quiet` now prints
`{"outcome": "checked-and-sound", "empty_types": []}` and exits 0.

Note on the "Expects From UXP-700b-1" contract: this EPIC-TruthfulProjectRecord
worktree is shared across every ticket in the epic. While this ticket was in
flight, UXP-700b-1's own test-writer and python-coder landed concurrently in
the SAME file and confirmed (unit_tests/product_truth/test_uxp_700b_1.py, 4/4
passing) that the vocabulary this ticket's implementation already uses
(empty_types: "flows"/"mock-data"/"mockups"; outcome:
"checked-and-sound"/"nothing-examined") is exactly UXP-700b-1's own
delivers_to.contract vocabulary — no further change was needed to satisfy the
upstream dependency. Also reviewed and confirmed unit_tests/product_truth/
test_uxp_700c_1.py (4/4) and unit_tests/test_generate_product_truth_idempotency.py
(7/7) still pass after this shared file's concurrent edits settled; UXP-300 and
UXP-700a-1-ii tests in the same directory correctly stay xfail-masked (their
own AC work_status is not yet done — unrelated to this ticket).

ANOMALY (not this ticket's to fix): docs/product-truth/scripts/validate_product_truth.py
is a pre-existing, already-oversized file (line-count ratchet: content-line
count grew from a HEAD baseline of 472 lines past several concurrent tickets'
additions today, including this one's). The file-size ratchet
(scripts/commit_guardian/check_file_size.py) will refuse a commit that leaves
the file net-longer than its HEAD baseline. Splitting this module (the
build_phases.py / build_helpers.py precedent) is a structural change spanning
multiple concurrently-active tickets' code in the same shared worktree and is
out of this ticket's authorized scope per the TICKET MANDATE — flagging for the
`commit` phase / a dedicated follow-up ticket rather than attempting an
unscoped split against actively-edited sibling work.

### 2026-09-09 16:12 — test-runner (status: ok)
feedback-id: fb_2026-09-09_a703908f
completion_manifest:
  test_suite_executed: true
  all_tests_passing: true
  failure_report_structured: true
Ran `AC_ENFORCE_STRICT=1 python -m pytest unit_tests/product_truth/test_uxp_700b_1_ii.py -v`
in worktree `C:\Users\Hendrik\Code\leafcutter\EPIC-TruthfulProjectRecord` (the worktree
this ticket's code/test work actually lives in, per test-writer's and python-coder's own
notes above): all 3 tests pass (`3 passed in 1.16s`), including under strict AC enforcement
so the pass is not an xfail-mask artifact. Also ran the full `unit_tests/product_truth`
directory as a sibling-regression check (233s, shared worktree has many concurrent
tickets' processes contending for CPU): 6 failures, all in `test_uxp_300.py`
(`TestArtifactPathsResolveUnderDeclaredStoreDirectories`,
`TestOneCanonicalDatasetPerEntityPerComponent`,
`TestDuplicateSurvivesTheRealGeneratorConsumerSeam`, and 3 more in
`TestTheStorePassesItsOwnValidator`/`TestUxp300ReachableFromEntryPoint`) — these belong to
the separate, not-yet-implemented ticket `01_TICKET-20260909-UXP-300.md` (duplicate-artifact
detection expecting a non-zero exit code the validator does not yet produce) and are
unrelated to this ticket's per-artifact-type emptiness contract; none of this ticket's own
3 tests are among the failures. Rerun command:
`AC_ENFORCE_STRICT=1 python -m pytest unit_tests/product_truth/test_uxp_700b_1_ii.py -v`.

### 2026-09-09 16:30 — ac-validator (status: ok)
feedback-id: (submit-failed)
completion_manifest:
  ac_coverage_computed: true
  store_alignment_checked: true
  cross_file_contract_traced: true
All 3 ACs covered by both implementation and test evidence; verdict ok.

AC-1 covered:
  implementation: docs/product-truth/scripts/validate_product_truth.py —
    `_compute_empty_types()` (returns the subset of {flows, mock-data, mockups}
    with a zero count) and `main()`'s final `print(json.dumps({"outcome":
    outcome, "empty_types": empty_types}))` line (unstaged diff against HEAD,
    hunk starting `+def _compute_empty_types(flows: dict, mocks: dict, mockups: dict)`)
  test: test_mixed_store_names_only_the_empty_artifact_types
    (unit_tests/product_truth/test_uxp_700b_1_ii.py) — asserts
    `empty_types == {"mock-data", "mockups"}` and `"flows" not in empty_types`
    against a real generator+validator subprocess pipeline. Ran
    `AC_ENFORCE_STRICT=1 python -m pytest unit_tests/product_truth/test_uxp_700b_1_ii.py -v`
    myself: 3 passed in 1.31s.

AC-2 covered:
  implementation: same `_compute_empty_types()` — the three-journey fixture
    type ("flows") has a nonzero count and is therefore excluded by
    construction from the returned list.
  test: same test as AC-1 — its `assertNotIn("flows", empty_types, ...)`
    assertion is the AC-2-specific check.

AC-3 covered:
  implementation: `_compute_outcome()` in
    docs/product-truth/scripts/validate_product_truth.py — returns
    `"degraded"` (never `_OUTCOME_CHECKED_AND_SOUND` = `"checked-and-sound"`)
    whenever `empty_types` is non-empty but not all three types, i.e. exactly
    the partial-emptiness case this AC names.
  test: test_mixed_store_outcome_is_not_the_clean_pass_outcome
    (unit_tests/product_truth/test_uxp_700b_1_ii.py) — asserts
    `payload["outcome"] != "checked-and-sound"`. Also independently
    re-confirmed via test_uxp_700b_1_ii_reachable_from_entry_point, which
    re-asserts both AC-1/AC-2's empty_types set and AC-3's outcome inequality
    through the validator's real CLI subprocess entry point (reachability
    angle). All 3 tests pass under `AC_ENFORCE_STRICT=1`.

Cross-file contract trace (Expects From UXP-700b-1 — "the per-type
zero-record reporting this record exercises on a mixed store"): both
UXP-700b-1's and UXP-700b-1-ii's implementations live in the SAME producer
file (docs/product-truth/scripts/validate_product_truth.py) and the SAME
`_compute_empty_types`/`_compute_outcome`/stdout-JSON wire contract. I ran
UXP-700b-1's own test suite directly: `AC_ENFORCE_STRICT=1 python -m pytest
unit_tests/product_truth/test_uxp_700b_1.py -v` → 4 passed in 1.07s. No
contract gap found — the vocabulary (`empty_types` naming
flows/mock-data/mockups; `outcome` in checked-and-sound/nothing-examined/
degraded/failed) is shared and consistent across both tickets' tests.

Store-alignment check: `python
scripts/commit_guardian/check_v2_ac_store_alignment.py --ticket
<this ticket path>` → exit 0, no output (no violations).

Anomaly noted (not this ticket's to fix): `config/feedback_categories.yaml`
has no `ac-validator` entry in `allowed_writers` for any category, so
`submit_feedback.py --phase ac-validator` exits 1 for every category choice.
This mirrors the exact gap the file's own decision history describes being
hit and fixed for `user-surface-smoker`, `frontend-coder`/`llm-expert`, and
`documentation-verifier` previously — `ac-validator` was never added.
Recommend a follow-up ticket adding `ac-validator` to `allowed_writers` for
at least `complete`, `knowledge-gap`, and `blocker`.

Anomaly noted (pre-existing, flagged by python-coder above, not
re-litigated here): `docs/product-truth/scripts/validate_product_truth.py`
is over the file-size ratchet baseline after several concurrent tickets'
additions in this shared worktree; `commit` phase will need to address the
ratchet (split or accept) before this ticket's changes can land.

No `## AC Coverage` table exists in this ticket body to fill (this ticket
predates or was authored outside the `## AC Coverage` table convention
described in the ac-validator template) — `ac_coverage: 3/3` was still added
to frontmatter per that template's Step 4c, and all three AC checkboxes were
already `[x]` from python-coder's/test-writer's own passes, confirmed
correct by my own evidence review above.

### 2026-09-09 16:45 — ac-fulfillment-gate (status: question)
feedback-id: (submit-failed)
completion_manifest:
  ac_yaml_loaded: true
  work_status_verified:
    result: false
    reason: "AC YAML (docs/acceptance-criteria/ux-prototyping/UXP-700-truthful-project-record/UXP-700b-1-ii.yaml) still has work_status: todo, covered_by: [] (L3). This gate's own shared resolver (ac_coverage_resolver.py) independently confirms the same two failing fields."
    remediation: "Resolve the files_touched question below first, then re-run this gate to auto-fix."
  files_touched_evidence_determinable:
    result: false
    reason: "This ticket's frontmatter files_touched: [] (empty). The gate's mandated evidence method is files_touched ∩ (git diff main...HEAD --name-only, falling back to git diff HEAD --name-only only when the first is empty). Here git diff main...HEAD is NON-empty (it lists unrelated committed files from a concurrent ticket, INF-1200 — so the fallback does not trigger by the letter of the rule) and does not include this ticket's own files; the actual uncommitted work for this ticket (docs/product-truth/scripts/validate_product_truth.py, unit_tests/product_truth/test_uxp_700b_1_ii.py) only shows up under the fallback command git diff HEAD --name-only, run directly. With files_touched empty, the intersection is empty regardless of which diff command is authoritative, so no auto-fix evidence can be computed via the mandated method without guessing."
    remediation: "Ask the user/orchestrator: (a) should files_touched on this ticket be populated with [docs/product-truth/scripts/validate_product_truth.py, unit_tests/product_truth/test_uxp_700b_1_ii.py] before this gate re-runs, and (b) which diff command is authoritative when git diff main...HEAD is non-empty but reflects a concurrent sibling ticket's already-committed work rather than this ticket's own (still-uncommitted) changes in this shared worktree."
This ticket's own AC YAML (UXP-700b-1-ii.yaml, L3) still shows work_status: todo
and covered_by: [] despite python-coder's, test-runner's, and ac-validator's
comments above all reporting the implementation and tests as green — the shared
`ac_coverage_resolver.py` independently confirms both fields still fail. Per this
gate's Stop-and-Ask Rule, I am not attempting to infer or auto-fix fulfillment
because the ticket's `files_touched` frontmatter key is empty (`[]`), so the
mandated `files_touched ∩ diff` evidence check cannot be computed — even though
`unit_tests/product_truth/test_uxp_700b_1_ii.py` genuinely contains three
`# covers: UXP-700b-1-ii` tags and `docs/product-truth/scripts/validate_product_truth.py`
is genuinely modified in this shared worktree's uncommitted diff (confirmed via
`git diff HEAD --name-only` and `git status --short`). Suggested remediation:
have `python-coder` (or a supervisor) populate this ticket's `files_touched`
frontmatter with the two files named above, then re-dispatch `ac-fulfillment-gate`
to auto-fix `work_status`, `implemented_by`, and `covered_by` on the AC YAML per
Step 3 of this gate's own protocol. I have made NO edits to any AC YAML file or
to this ticket's frontmatter/Sign-offs/agents map — only this Comments entry —
so `agents.ac-fulfillment-gate` remains `needed` and the Sign-offs checkbox
remains unchecked pending that clarification.
