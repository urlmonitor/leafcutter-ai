---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  pr-reviewer: needed
  pull-request: needed
  python-coder: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
complexity: medium
components:
- build_orchestration
created: '2026-07-15'
depends_on:
- BO-2200c
- BO-2200c-2
files_touched:
- scripts/ac_store/generate_ticket_from_ac.py
priority: medium
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: BO-2200c-3
status: done
title: The genre is sourced from the parent L1's documentation_triggers
---

# The genre is sourced from the parent L1's documentation_triggers

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BO-2200c-3` — The genre is sourced from the parent L1's documentation_triggers — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BO-2200c-3`. Component: `build-orchestration`. Assigned agent: `python-coder`. Estimated complexity: `M`. Complexity: `medium`.

## Acceptance Criteria

```gherkin
Given the generator is building the contract line for a leaf AC and the generator has
  loaded only that single leaf AC record,
When it determines the Diataxis genre for the line,
Then it resolves the leaf AC's parent L1 via the parent/`depends_on` link and reads the
  parent L1's `documentation_triggers` field to source the genre.
And when the parent L1 declares multiple `documentation_triggers` (for example
  `[how-to, sequence-diagram]`), the contract reflects each declared genre.
```

## Test Requirements

```yaml
tests:
- name: test_genre_resolved_from_parent_l1_documentation_triggers
  file: unit_tests/test_bo_2200c_3.py
  covers:
  - BO-2200c-3
  asserts: Generating a contract line for a leaf AC resolves the parent L1 and sources
    the genre from the parent's documentation_triggers.
  framework: unittest
  type: behavioral
- name: test_multiple_parent_triggers_each_reflected
  file: unit_tests/test_bo_2200c_3.py
  covers:
  - BO-2200c-3
  asserts: When the parent L1 declares multiple documentation_triggers, the contract
    reflects each declared genre.
  framework: unittest
  type: behavioral
```

## Implementation Notes

```yaml
config_schema_fragment:
  parent_genre_resolution:
    description: Genre sourced from the parent L1's documentation_triggers (an L1-only
      field), resolved via derive_parent_id/depends_on; multiple triggers each reflected.
    type: object
constraints:
- The generator today loads only the single leaf AC (via _find_ac_by_id / main); it
  must additionally resolve the parent L1 (via ac_parent_id.derive_parent_id / depends_on)
  and read its documentation_triggers, since documentation_triggers is an L1-only
  field.
- Handle multiple triggers declared on the parent L1 — reflect each declared genre
  in the contract.
n_location_rule: '1'
post_write_commands: []
reference_file_path: scripts/ac_store/generate_ticket_from_ac.py
required_skills:
- python-coder
```

## Sign-offs

- [ ] python-coder
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit
- [ ] pull-request

## Comments
