---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  pr-reviewer: needed
  pull-request: not_needed
  python-coder: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
complexity: medium
components:
- build_orchestration
created: '2026-07-08'
depends_on:
- BO-1900b
- TKT-200e
files_touched:
- docs/architecture/agent_delivery_workflows.md
- docs/architecture/components/build-ticket-workflow-dispatch.md
priority: high
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: BO-1900b-1
status: todo
title: A premise that no longer reproduces at dispatch halts the run
---

# A premise that no longer reproduces at dispatch halts the run

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BO-1900b-1` — A premise that no longer reproduces at dispatch halts the run — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BO-1900b-1`. Component: `build-orchestration`. Assigned agent: `python-coder`. Estimated complexity: `M`. Complexity: `medium`.

## Acceptance Criteria

```gherkin
Given a ticket whose motivating claim is "build X fails", captured with the exact
  reproduction command and the observed failing result,
  and the build now succeeds when that command is re-run at dispatch time,
When the preflight premise re-check runs before the phase agent is spawned,
Then the re-check observes that the reproduced result no longer matches the captured
  result,
And the dispatch is halted with a held-back reason that names the stale premise and
  shows the captured-vs-current results,
And no phase agent is spawned.
```

## Test Requirements

```yaml
tests: []
```

## Sign-offs

- [ ] python-coder
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit

## Comments
