---
ac_traceability:
  id: FIN-100c-7
  path: docs/acceptance-criteria/build_pipeline/FIN-100-pre-merge-safety-gate/FIN-100c-7.yaml
advances_current_outcome: true
agents:
  architect-review: needed
  commit: needed
  pr-reviewer: needed
  pull-request: needed
  test-failure-triage: needed
change_target: pipeline
complexity: low
components:
- build_pipeline
created: '2026-07-21'
depends_on: []
files_touched:
- templates/workflows-js/finalize-feature.js
- templates/workflows/finalize-feature.md
- unit_tests/workflows/test_finalize_feature_preflight.py
priority: high
requires_adr: false
requires_diagram: false
risk_surface: contract_boundary
roadmap_phase: phase_1
source_ac: FIN-100c-7
status: todo
title: Failures that also fail on main HEAD are pre_existing; only failures that pass
  on main are regressions
---

# Failures that also fail on main HEAD are pre_existing; only failures that pass on main are regressions

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `FIN-100c-7` — Failures that also fail on main HEAD are pre_existing; only failures that pass on main are regressions — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `FIN-100c-7`. Component: `finalize`. Assigned agent: `test-failure-triage`. Estimated complexity: `S`. Complexity: `low`.

## Acceptance Criteria

```gherkin
Given the triage stage receives a recovered baseline built from the main-HEAD rerun
And the post-merge failure set (for example the 3 deploy-dependent tests from
  2026-07-15, all of which also failed on main HEAD)
When the triage agent compares each post-merge failure against the recovered baseline
Then every failing test that ALSO fails on main HEAD is classified as "pre_existing"
And a failing test that PASSES on main HEAD but fails post-merge is classified as "regression"
And each test's classification appears in the triage report with its category
```

- [ ] AC-1: the post-merge failure set (for example the 3 deploy-dependent tests from
- [ ] AC-2: every failing test that ALSO fails on main HEAD is classified as "pre_existing"
- [ ] AC-3: a failing test that PASSES on main HEAD but fails post-merge is classified as "regression"
- [ ] AC-4: each test's classification appears in the triage report with its category

## Implementation Notes

```yaml
"- NO NEW TRIAGE CODE PATH (instance-specific integration constraint): a recovered\n\
  \  baseline is a non-null list, so the triage agent's Step 1 null-baseline\n  short-circuit\
  \ does NOT fire and the EXISTING Step 2 partition / Step 5\n  pre_existing rule\
  \ (FIN-100c-1) and set-difference regression rule (FIN-100c-2)\n  apply unchanged.\
  \ Do NOT add a special-case branch for the recovered baseline —\n  it must be indistinguishable\
  \ from a normally-captured baseline to the triage\n  agent. (These behavioral rules\
  \ are inherited from FIN-100c-1/FIN-100c-2 and are\n  NOT restated here.)\n- The\
  \ recovered baseline arrives as file::test_name IDs and is compared by exact\n \
  \ string match, identical to the captured-baseline path.\n- OBSERVABLE OUTPUT: each\
  \ post-merge failure appears in triage_report with its\n  category (pre_existing\
  \ | regression), preserving the existing per-test report\n  shape — no schema change.\n"
```

## Agent Contracts

### Delivers To

- **Agent:** finalize-feature-workflow
- **Contract:** provides a triage_report classifying each rerun failure as pre_existing (fails on main) or regression (passes on main)

### Expects From

- **Contract:** provides baseline_failures = the recovered baseline (subset of failing IDs that fail on main HEAD) plus post_merge_failures

## Sign-offs

- [ ] architect-review
- [ ] pr-reviewer
- [ ] test-failure-triage
- [ ] commit
- [ ] pull-request

## Comments
