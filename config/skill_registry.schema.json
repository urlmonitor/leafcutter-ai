{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "skill_registry.schema.json",
  "title": "Skill Registry",
  "description": "Schema for skill_registry.json — the single source of truth for which skills exist, their portability classification, and their declared dependencies. A strict subset of agent_registry.schema.json (same structural pattern; omits agent-only fields: spawn_allowlist, spawned_by, is_ticket_phase, selection_criteria, model).",
  "type": "object",
  "required": ["skills"],
  "properties": {
    "skills": {
      "type": "array",
      "description": "List of all skills in the leafcutter ecosystem.",
      "items": {
        "$ref": "#/definitions/skill"
      },
      "minItems": 1
    }
  },
  "definitions": {
    "skill": {
      "type": "object",
      "required": [
        "id",
        "name",
        "portable",
        "domain",
        "dependencies"
      ],
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique skill identifier — matches the directory name under .claude/skills/ and (for portable skills) leafcutter/templates/skills/.",
          "pattern": "^[a-z][a-z0-9-]*$"
        },
        "name": {
          "type": "string",
          "description": "Human-readable display name."
        },
        "portable": {
          "type": "boolean",
          "description": "True if the skill is domain-agnostic and belongs in the portable package (has a template_path). False for domain-specific skills (domain field non-null)."
        },
        "domain": {
          "type": ["string", "null"],
          "description": "Domain tag (e.g. 'bybit-trader') for domain-specific skills. Null for portable skills."
        },
        "template_path": {
          "type": "string",
          "description": "Relative path (from leafcutter root) to the skill's template directory. Present only for portable skills.",
          "pattern": "^leafcutter/templates/skills/[a-z][a-z0-9-]*/$"
        },
        "dependencies": {
          "type": "array",
          "items": {
            "type": "string",
            "pattern": "^[a-z][a-z0-9-]*$"
          },
          "description": "Skill IDs that this skill invokes at runtime (skill-to-skill dependency graph). May be left [] for the initial pass when dependency mapping is non-trivial."
        },
        "internal": {
          "type": "boolean",
          "description": "When true, this skill is for internal/runtime use only and should be excluded from user-facing skill listings and summary tables. The skill is still copied to .claude/skills/ so runtime agents can invoke it."
        },
        "description": {
          "type": "string",
          "description": "Optional human-readable description of the skill's purpose. Used in summaries and documentation."
        },
        "invocation_surface": {
          "type": "string",
          "description": "Optional slash-command or surface identifier used to invoke this skill (e.g. '/plan-feature'). Present only for skills that expose a user-facing command entry point."
        },
        "workflow_script": {
          "type": "string",
          "description": "Optional relative path (from repo root) to the deployed workflow JS script that drives this skill's slash command (e.g. '.claude/workflows/plan-feature.js'). Present only when the skill has a workflow_script entry point."
        },
        "components": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Optional list of component ids this skill is associated with (component-graph vocabulary). Additive metadata; may be omitted."
        }
      },
      "additionalProperties": false
    }
  }
}
