---
advances_current_outcome: true
agents:
  commit: signed_off
  documentation-expert: not_needed
  pr-reviewer: signed_off
  pull-request: signed_off
  python-coder: signed_off
  sql-coder: not_needed
  test-runner: signed_off
  test-writer: signed_off
components:
- build-orchestration
created: '2026-06-24'
depends_on:
- BO-1500c
- BO-1500c-1
files_touched:
- templates/skills/create-ac/SKILL.md
- scripts/workflows/plan-feature.js
priority: high
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: BO-1500c-3
status: done
title: AC files are never committed directly onto main during authoring
---

# AC files are never committed directly onto main during authoring

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BO-1500c-3` — AC files are never committed directly onto main during authoring — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BO-1500c-3`. Component: `build-orchestration`. Assigned agent: `python-coder`. Estimated complexity: `S`.

## Acceptance Criteria

```gherkin
Given the AC-authoring workflow runs from start through final delivery,
When the run is complete,
Then no commit produced by the workflow was placed directly on the main branch,
And every AC YAML commit the workflow produced lives on the authoring branch,
And the approved AC files reach main only by merging the authoring pull
  request,
And this holds whether the user invoked /create-ac or /plan-feature.
```

## Sign-offs

- [x] python-coder — 2026-06-24 12:25
- [x] test-writer — 2026-06-24 00:00
- [x] test-runner — 2026-06-24 12:30
- [x] pr-reviewer — 2026-06-24 12:35
- [x] commit — 2026-06-24 12:40
- [x] pull-request — 2026-06-24 12:45

## Comments

### 2026-06-24 00:00 — ticket-supervisor (status: ok)
feedback-id: (submit-failed)
test_requirements empty — test-writer phase skipped (docs-only or config-only ticket)

### 2026-06-24 12:25 — python-coder (status: ok)
feedback-id: (submit-failed)
completion_manifest:
  no_main_guard_added_to_plan_feature_js: true
  skill_nm_section_added: true
  decision_history_entry_added: true

Added the AC BO-1500c-3 no-main-commit defensive guard to `commitStageOutput()` in `scripts/workflows/plan-feature.js`. The guard dispatches `git -C <authoringWorktreePath> branch --show-current` before any staging/commit; if the result is "main" (case-insensitive), the function returns a structured error and aborts without touching git. If the branch check itself fails, a `console.warn` is emitted and the commit proceeds (fail-open — infrastructure failure must not block authoring). Also added a JSDoc comment block to `deliverAuthoringBranch()` documenting that approved AC files reach main ONLY via PR merge, never via direct commit (AC BO-1500c-3). In `templates/skills/create-ac/SKILL.md`, added the new `§NM — No-Main-Commit Invariant (AC BO-1500c-3)` section between §D and §C with four subsections (§NM.1 invariant statement, §NM.2 structural mechanism, §NM.3 defensive guard description, §NM.4 scope and limitations). Updated the Related section to cross-reference §NM. Added a DECISION HISTORY entry for this ticket.

### 2026-06-24 12:30 — test-runner (status: ok)
feedback-id: (submit-failed)
completion_manifest:
  no_main_guard_verified: true
  skill_nm_section_verified: true
  diff_stat_confirmed: true

Static analysis confirms all three deliverables are correctly implemented. The no-main guard in `commitStageOutput()` (lines 165–220 of `scripts/workflows/plan-feature.js`) correctly: (1) runs `git -C "<authoringWorktreePath>" branch --show-current` before any staging or committing; (2) aborts with a structured `{status:"error", message:"safety: refusing to commit AC files to main..."}` when the branch is "main" (case-insensitive); and (3) uses `console.warn` and proceeds fail-open when the branch check itself raises an exception or returns a non-zero exit code. The `§NM` section in `templates/skills/create-ac/SKILL.md` is correctly positioned between §D and §C, and documents the invariant (§NM.1), structural mechanism (§NM.2), defensive guard (§NM.3), and scope/limitations (§NM.4). Diff stat confirms exactly the two expected files changed (plus the ticket itself). All 10 behavioral and 9 staging tests for prior-ticket commitStageOutput() changes pass; the 1 known pre-existing template parity failure is unrelated to this ticket's changes.

### 2026-06-24 12:35 — pr-reviewer (status: ok)
feedback-id: (submit-failed)
completion_manifest:
  no_main_guard_correct: true
  fail_open_verified: true
  skill_nm_positioned_correctly: true
  error_handling_compliant: true
  code_style_consistent: true

Reviewed full working diff against AC BO-1500c-3. Guard in `commitStageOutput()` (plan-feature.js lines 178–220): case-insensitive `"main"` check is correct; fail-open is confirmed on both the non-zero exit-code path (else branch with console.warn + fall-through) and the catch path (console.warn + fall-through, no rethrow). §NM section confirmed between §D (line 487) and §C (line 637) in SKILL.md. Error handling follows project policy (named catch variable, console.warn, no silent swallow). Code style consistent with existing async/await + agent-dispatch pattern. One medium-confidence note: `branchCheckErr.message` may be `undefined` for non-Error rejections — harmless, fail-open behavior unaffected. No high-confidence blockers found.

### 2026-06-24 12:40 — commit (status: ok)
feedback-id: (submit-failed)
completion_manifest:
  files_committed: true
  hooks_passed: true
Committed no-main guard in plan-feature.js and §NM section in create-ac/SKILL.md for BO-1500c-3; all pre-commit hooks passed after adding missing feedback-id to ticket-supervisor comment.

### 2026-06-24 12:45 — pull-request (status: ok)
feedback-id: (submit-failed)
completion_manifest:
  branch_pushed: true
  pr_exists: true
PR #165 (EPIC-SafeAcAuthoring → main) updated with commits for BO-1500c-3.
