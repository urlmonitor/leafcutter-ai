---
ac_traceability:
  id: UXP-700a-2
  path: docs/acceptance-criteria/ux-prototyping/UXP-700-truthful-project-record/UXP-700a-2.yaml
advances_current_outcome: true
agents:
  ac-fulfillment-gate: needed
  ac-validator: needed
  architect-review: not_needed
  commit: needed
  documentation-expert: not_needed
  documentation-verifier: not_needed
  frontend-coder: not_needed
  pr-reviewer: not_needed
  pull-request: not_needed
  python-coder: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
  user-surface-smoker: needed
change_target: code
complexity: low
components:
- ux_prototyping
created: '2026-09-09'
declares_side_effect: true
depends_on:
- 02_TICKET-20260909-UXP-700a-1.md
files_touched: []
priority: medium
requires_adr: false
requires_diagram: false
risk_surface: internal
roadmap_phase: phase_1
source_ac: UXP-700a-2
status: todo
title: The record rebuilds itself from zero artifacts with no hand-written index
---

# The record rebuilds itself from zero artifacts with no hand-written index

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `UXP-700a-2` — The record rebuilds itself from zero artifacts with no hand-written index — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `UXP-700a-2`. Component: `ux-prototyping`. Assigned agent: `python-coder`. Estimated complexity: `S`. Complexity: `low`.

## Acceptance Criteria

```gherkin
Given a record whose artifact places exist and are empty and whose store index is absent,
When the record's generator runs,
Then a store index is written declaring zero artifacts, holding an empty entry for every
  derived lookup it maintains,
And running the generator a second time with no artifact added writes nothing further,
And the record's checker then reaches a verdict against that index without any file having
  been authored by hand.
```

- [ ] AC-1: a store index is written declaring zero artifacts, holding an empty entry for every
- [ ] AC-2: running the generator a second time with no artifact added writes nothing further
- [ ] AC-3: the record's checker then reaches a verdict against that index without any file having

## Test Requirements

```yaml
tests:
- name: test_generator_creates_index_from_zero_artifacts
  file: unit_tests/product_truth/test_uxp_700a_2.py
  covers:
  - UXP-700a-2
  asserts: Running the generator against empty artifact directories with no index
    present writes an index declaring zero artifacts.
  framework: unittest
  type: integration
  angle: real_artifact
- name: test_second_generator_run_on_empty_store_writes_nothing
  file: unit_tests/product_truth/test_uxp_700a_2.py
  covers:
  - UXP-700a-2
  asserts: A second run over the same empty store leaves every file unchanged.
  framework: unittest
  type: integration
  angle: criterion
- name: test_uxp_700a_2_reachable_from_entry_point
  file: unit_tests/product_truth/test_uxp_700a_2.py
  covers:
  - UXP-700a-2
  angle: reachability
  asserts: 'REQUIRED — invoke the production entry point (CLI, hook, slash command,
    workflow dispatch, or main()) as a subprocess/dispatch and assert the new behaviour
    actually occurs. Do NOT satisfy this by importing the function directly. The AC
    authored no test_spec, so the entry point is not declared: resolve it before writing
    this test, and do not delete this entry.'
```

## Agent Contracts

### Delivers To

- **Agent:** python-coder
- **Contract:** A zero-artifact store index that the checker in UXP-700b-1 evaluates.

### Expects From

- **AC:** UXP-700a-1
- **Contract:** The empty artifact places the generator scans.

## Sign-offs

- [ ] test-writer
- [ ] python-coder
- [ ] test-runner
- [ ] user-surface-smoker
- [ ] ac-validator
- [ ] ac-fulfillment-gate
- [ ] commit

## Comments
