---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: needed
  pr-reviewer: needed
  pull-request: not_needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
components:
- build_orchestration
created: '2026-07-07'
depends_on:
- BO-1800b
files_touched:
- docs/architecture/adrs/ADR-018-agent-isolation-topology.md
- docs/architecture/agent_delivery_workflows.md
priority: medium
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: BO-1800b-1
status: todo
title: The hub's main accepts changes only through a pull request; direct push, force-push,
  and branch deletion are rejected server-side
---

# The hub's main accepts changes only through a pull request; direct push, force-push, and branch deletion are rejected server-side

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BO-1800b-1` — The hub's main accepts changes only through a pull request; direct push, force-push, and branch deletion are rejected server-side — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BO-1800b-1`. Component: `build-orchestration`. Assigned agent: `documentation-expert`. Estimated complexity: `M`.

## Acceptance Criteria

```gherkin
Given branch protection is configured on the hub (authoritative `origin`) `main`,
When any client attempts to update `main` other than by merging a pull request,
Then a direct `git push` to `main` is rejected by the hub,
And a force-push to `main` is rejected by the hub,
And deletion of the `main` branch is rejected by the hub,
And the rejection happens at the source of truth (server-side), independently of
  whatever local hooks or tooling the client is running.
```

## Sign-offs

- [ ] documentation-expert
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit

## Comments
