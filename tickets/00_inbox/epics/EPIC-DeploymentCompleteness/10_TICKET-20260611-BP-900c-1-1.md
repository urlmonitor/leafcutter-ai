---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  pr-reviewer: needed
  pull-request: needed
  python-coder: signed_off
  sql-coder: not_needed
  test-runner: needed
  test-writer: signed_off
components:
- build_pipeline
created: '2026-06-11'
depends_on:
- 09_TICKET-20260611-BP-900c-1.md
files_touched:
- scripts/build_propagation_audit.py
- docs/architecture/components/template-compiler.md
priority: critical
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: BP-900c-1-1
status: todo
title: Multiple templates referencing the same missing script produce a consolidated
  entry
---

# Multiple templates referencing the same missing script produce a consolidated entry

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BP-900c-1-1` — Multiple templates referencing the same missing script produce a consolidated entry — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BP-900c-1-1`. Component: `build_pipeline`. Assigned agent: `python-coder`. Estimated complexity: `XS`.

## Acceptance Criteria

```gherkin
Given "scripts/ac_store/generate_ticket_from_ac.py" is referenced in both
"agents/build-ac.md" and "skills/ac-scanner/SKILL.md"
And the script is not in the deployable set,
When the guard emits the error report,
Then one entry is emitted for the missing script with both referencing templates
listed (not two separate entries with the same missing path)
And the suggested action appears once for the shared missing path
```

## Sign-offs

- [x] python-coder — 2026-08-18 00:15
- [x] test-writer — 2026-08-18 00:00
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit
- [ ] pull-request

## Comments

### 2026-08-18 00:00 — test-writer (status: ok)
feedback-id: (submit-failed)
completion_manifest:
  tests_written: true
  red_baseline_captured:
    result: false
    reason: "AC BP-900c-1-1 (consolidation of multiple templates referencing the same missing script) is ALREADY fully implemented and ALREADY has passing coverage from a prior ticket: unit_tests/build_guards/test_bp_stragglers_backfill.py::TestBrokenRefConsolidation (written for 06_stragglers_test_coverage.md / EPIC-BuildPipelineTestBackfill against the same AC id). The AC store entry itself (docs/acceptance-criteria/build_pipeline/BP-900-deployment-completeness/BP-900c-1-1.yaml) already carries work_status: done with implemented_by pointing at tickets/00_inbox/TICKET-20260611-BP-900c-1-1.md — a DIFFERENT ticket path than this one, confirming this ticket duplicates already-completed work (see project memory note on ticket-vs-AC redundancy). I added a new cross-layer seam test (unit_tests/build_guards/test_bp900c1_1_real_extraction_consolidation.py) that pipes the REAL producer (build_referential_integrity.extract_script_path_refs_with_sources on the actual templates/ tree) into the REAL consumer (build_propagation_audit.build_broken_ref_report) — prior coverage only used a hand-typed synthetic refs_to_sources dict, so it never proved the producer/consumer seam matches (Source-of-Truth Discipline Rule 3). All 3 new tests pass immediately on first run (verified via `pytest unit_tests/build_guards/test_bp900c1_1_real_extraction_consolidation.py -v`, exit 0). This is a TDD-order violation per project convention (test-writer runs before python-coder and found the suite already green) — flagging explicitly rather than silently signing off as a normal red-to-green cycle. No red baseline exists to capture because production correctness is genuine, not a coverage gap I could close with a stronger assertion."
    remediation: "python-coder should verify AC BP-900c-1-1 is already satisfied by scripts/build_propagation_audit.py (build_broken_ref_report / BrokenRefEntry.referencing_templates) and docs/architecture/components/template-compiler.md's 'Broken-Reference Report Entry Schema' section, sign off with a no-op note rather than modifying working code, and let test-runner confirm the full suite (including the new test file) stays green. Supervisor may wish to close this ticket as a duplicate of tickets/00_inbox/TICKET-20260611-BP-900c-1-1.md at epic finalize."
Wrote unit_tests/build_guards/test_bp900c1_1_real_extraction_consolidation.py (3 tests, all tagged `# covers: BP-900c-1-1`) as real-producer/real-consumer seam coverage for the AC. Verification run was GREEN (0 failures) on first run because the underlying implementation and an existing straggler test file already fully satisfy this AC — see completion_manifest for the TDD-order-violation disclosure and duplicate-ticket finding. Also note: the ticket_frontmatter_guard hook flagged pre-existing frontmatter gaps unrelated to this change (missing `change_target`/`risk_surface`, `depends_on: BP-900c-1` not resolving to a file in this epic folder) — these are AC-generator artifacts (see project memory "build-ac generator quirks") and were not introduced by this edit; left untouched as out of scope for test-writer.

### 2026-08-18 00:15 — python-coder (status: ok)
feedback-id: (submit-failed)
completion_manifest:
  ac_verified_already_satisfied: true
  files_touched_reviewed:
    scripts/build_propagation_audit.py: true
    docs/architecture/components/template-compiler.md: true
  code_changes_needed: false
  red_baseline_results: "none — test-writer captured no red baseline; AC BP-900c-1-1 was already green before this phase (see test-writer's completion_manifest)"
  tests_confirmed_green: true
Read the ticket and test-writer's disclosure, then verified independently rather than trusting the note at face value. `scripts/build_propagation_audit.py` already implements consolidation: `build_broken_ref_report()` groups by `refs_to_sources` (script_path -> set of referencing templates) and emits exactly one `BrokenRefEntry` per missing path with a `referencing_templates` tuple (sorted, deduped) and a single `suggested_action` — this is precisely the Given/When/Then behavior the AC requires. `docs/architecture/components/template-compiler.md` already documents this under "Broken-Reference Report Entry Schema (AC BP-900c-1)", explicitly citing AC BP-900c-1-1's consolidation rule for `referencing_templates`. Re-ran the full test set covering this AC (`unit_tests/build_guards/test_bp900c1_1_real_extraction_consolidation.py` + `unit_tests/build_guards/test_bp_stragglers_backfill.py::TestBrokenRefConsolidation`, 11 tests total) fresh: all pass (`pytest ... -v`, exit 0). No code or doc edits were made — this is a genuine no-op sign-off, not a skipped implementation. Concur with test-writer's remediation: this ticket duplicates already-completed work tracked under a different ticket path (`tickets/00_inbox/TICKET-20260611-BP-900c-1-1.md`); flagging for the supervisor to consider closing as duplicate at epic finalize. The pre-existing frontmatter gaps (`depends_on: BP-900c-1` unresolved, missing `change_target`/`risk_surface`) are out of scope for python-coder per the ticket's `files_touched` and were left untouched.
