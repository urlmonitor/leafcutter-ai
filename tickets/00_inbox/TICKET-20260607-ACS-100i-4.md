---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  pr-reviewer: needed
  pull-request: needed
  python-coder: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
components:
- ac_store
created: '2026-06-07'
depends_on:
- ACS-100i
- ACS-100i-1
files_touched:
- docs/reference/ac-schema.md
- docs/architecture/adrs/ADR-007-ac-store-schema-id-format-enforcement.md
priority: medium
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: ACS-100i-4
status: todo
title: Existing orphaned children are detected and reported by a store-wide scan
---

# Existing orphaned children are detected and reported by a store-wide scan

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `ACS-100i-4` — Existing orphaned children are detected and reported by a store-wide scan — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `ACS-100i-4`. Component: `ac-store`. Assigned agent: `python-coder`. Estimated complexity: `M`.

## Acceptance Criteria

```gherkin
Given the AC store at docs/acceptance-criteria/ contains 80+ AC YAML files,
And at least 4 child ACs exist whose parent's covered_by field does not
  include them (e.g. ACS-300h has covered_by: [] but ACS-300h-1.yaml,
  ACS-300h-2.yaml, ACS-300h-3.yaml exist on disk),
When an operator runs the store-wide integrity scan,
Then the scan reports each orphaned child AC with:
  - the child AC ID
  - the expected parent AC ID
  - the parent file path
And the report groups orphans by parent for readability,
And the scan exits with a non-zero status when orphans are found,
And the scan exits with zero status when every child is listed
  in its parent's covered_by field.
```

## Sign-offs

- [ ] python-coder
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit
- [ ] pull-request

## Comments
