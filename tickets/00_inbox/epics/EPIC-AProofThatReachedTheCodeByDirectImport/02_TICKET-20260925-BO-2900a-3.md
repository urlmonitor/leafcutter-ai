---
ac_traceability:
  id: BO-2900a-3
  path: docs/acceptance-criteria/build-orchestration/BO-2900-runtime-reachability-guard/BO-2900a-3.yaml
advances_current_outcome: true
agents:
  ac-fulfillment-gate: needed
  ac-validator: needed
  architect-review: not_needed
  commit: needed
  documentation-expert: needed
  documentation-verifier: needed
  frontend-coder: not_needed
  pr-reviewer: needed
  pull-request: not_needed
  python-coder: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
  user-surface-smoker: not_needed
change_target: pipeline
complexity: medium
components:
- build_orchestration
- testing_quality
- ac_store
created: '2026-09-25'
depends_on:
- 01_TICKET-20260925-BO-2900a-1.md
documentation_required: true
files_touched:
- scripts/ac_store/done_proof.py
priority: medium
requires_adr: false
requires_diagram: false
risk_surface: internal
roadmap_phase: phase_1
source_ac: BO-2900a-3
status: todo
title: Code that no way of running the product can reach cannot be marked done, however
  many tests pass
---

# Code that no way of running the product can reach cannot be marked done, however many tests pass

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BO-2900a-3` — Code that no way of running the product can reach cannot be marked done, however many tests pass — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BO-2900a-3`. Component: `build-orchestration`. Assigned agent: `python-coder`. Estimated complexity: `M`. Complexity: `medium`.

## Acceptance Criteria

```gherkin
Given an acceptance criterion whose implementing code lives in a unit that exposes
  no runtime way in of its own,
  And no unit that does expose a runtime way in reaches that code,
  And no automation runs that unit as a program,
  And the criterion's proof-of-done test is present, executes the code, and passes,
When the criterion is evaluated for eligibility to be marked done,
Then the criterion is rejected as not eligible,
  And the rejection states that no way of running the product reaches the code, and
    names the unit,
  And adding a further passing test over the same code leaves the verdict unchanged,
  And the verdict changes only when the code is given a way in, or when an exemption
    for that unit is recorded.
```

- [ ] AC-1: no unit that does expose a runtime way in reaches that code
- [ ] AC-2: no automation runs that unit as a program
- [ ] AC-3: the criterion's proof-of-done test is present, executes the code, and passes
- [ ] AC-4: the criterion is rejected as not eligible
- [ ] AC-5: the rejection states that no way of running the product reaches the code, and
- [ ] AC-6: adding a further passing test over the same code leaves the verdict unchanged
- [ ] AC-7: the verdict changes only when the code is given a way in, or when an exemption

## Test Requirements

```yaml
tests:
- name: test_unit_with_no_way_in_is_refused_despite_a_passing_proof
  file: unit_tests/ac_store/test_bo_2900a_3.py
  covers:
  - BO-2900a-3
  asserts: Runs verify_done_eligible over a fixture unit with no main(), no importing
    module that has one and no automation invocation, whose covers-tagged proof passes;
    asserts eligible False, refusal_cause 'no_entry_point_reaches_code' and that the
    message names the unit.
  framework: unittest
  type: behavioral
- name: test_adding_another_passing_test_leaves_the_verdict_unchanged
  file: unit_tests/ac_store/test_bo_2900a_3.py
  covers:
  - BO-2900a-3
  asserts: Adds a second passing covers-tagged test over the same fixture code and
    re-runs the real evaluation; asserts a byte-identical refusal verdict, proving
    the predicate reads no test-derived term.
  framework: unittest
  type: behavioral
- name: test_verdict_flips_only_when_an_entry_point_is_added_or_an_exemption_is_recorded
  file: unit_tests/ac_store/test_bo_2900a_3.py
  covers:
  - BO-2900a-3
  asserts: Two follow-up runs over the same fixture — one after adding a main() that
    reaches the code, one after recording an exemption — each assert eligible True,
    and both are asserted alongside the untouched refusal so a pass-everything implementation
    cannot satisfy the set.
  framework: unittest
  type: behavioral
- name: test_bo_2900a_3_reachable_from_entry_point
  file: unit_tests/ac_store/test_bo_2900a_3.py
  covers:
  - BO-2900a-3
  angle: reachability
  asserts: 'REQUIRED — invoke the production entry point (CLI, hook, slash command,
    workflow dispatch, or main()) as a subprocess/dispatch and assert the new behaviour
    actually occurs. Do NOT satisfy this by importing the function directly. The AC
    authored no test_spec, so the entry point is not declared: resolve it before writing
    this test, and do not delete this entry.'
```

## Implementation Notes

```yaml
config_schema_fragment:
  no_way_in_verdict:
    description: 'Second refusal cause on the eligibility verdict: { eligible: false,
      refusal_cause: ''no_entry_point_reaches_code'', unit: ''<module path>'', clearing_actions:
      [''give the unit an entry point'', ''record an exemption''] }. Emitted only
      when the BO-2900a-1 scope fence excludes the unit.'
    type: object
constraints:
- 'Evaluate exactly the three enumerated conditions and no vaguer predicate: (1) the
  unit''s module defines no module-level main(argv); (2) no module that DOES define
  one reaches the code — established from the import graph built by importing candidate
  modules, never from a text scan of import statements; (3) no automation script runs
  the unit as a program — established from the BO-2900b-3 invocation collection over
  the derived automation-script set. All three must hold for the refusal.'
- 'THE ANTI-RITUAL CLAUSE IS STRUCTURAL: no term in the condition set may reference
  test count, test presence, test outcome, or coverage. Adding another passing test
  must be incapable of changing the verdict, because the predicate never reads those
  facts.'
- 'Exactly two state changes flip the verdict: an entry point appears (condition 1
  or 2 becomes false), or a recorded exemption for that unit exists per BO-2900d-1.
  The exemption check happens here, in the done-proof path, so the released criterion
  genuinely becomes eligible (BO-2900d-1 scenario two asserts that end to end).'
- This refusal and the BO-2900a-1 refusal are mutually exclusive by construction —
  BO-2900a-1 applies only when an entry point resolves, this one only when none does.
  An implementation that can emit both for one criterion has merged the scope fence
  and must be corrected, not reconciled downstream.
- This is the clause that catches the ACTUAL PR-411 regression state (no registered
  capability, no caller, passing tests). The BO-2900b forward check passes vacuously
  there; do not widen either rule to compensate.
- Condition (3) crosses into the commit-guardian package. Import the shared invocation-collection
  seam rather than duplicating it, and add whatever new module done_proof.py gains
  to build_ac_store's deploy_map (scripts/build_phases.py:794) in the same change.
n_location_rule: '1'
post_write_commands:
- ./build-self.sh
reference_file_path: scripts/ac_store/done_proof.py
required_skills:
- python-coder
```

---
Reference material (read-only, do not copy blindly): a prototype static caller finder lives at the absolute path C:/Users/Hendrik/Code/leafcutter/leafcutter-ai/debugging/test_judge/callers.py (untracked, NOT on origin/main — read it from that absolute path; a worktree will not contain it). It counts only AST Call nodes as callers (docstring/log/text mentions do not count — the BO-2900b-3 rule), excludes tests/docs/tickets, and resolves call sites with `git grep` then AST confirmation. On 38 labelled leafcutter incidents it flagged 4/17 vacuous tests with 0/21 false alarms, including the stranded BO-2400f lifecycle functions (claim_build_set, release_claim) and BO-1700's orphaned check_hook_freshness / resolve_hooks_path. Note BO-2900a-2 requires the a-track verdict to come from watching the run, not reading text — the static finder is a cheap pre-filter/evidence source, not a substitute. Evidence: C:/Users/Hendrik/Code/leafcutter/leafcutter-ai/docs/analysis/2026-09-25-jev-test-triage-leafcutter-replication.md, section "Development pass".
---

## Agent Contracts

### documentation-expert

Existing docs to update / cross-link:

- scripts/ac_store/done_proof.py (relationship: describes | status: exists | relevance: The eligibility gate that must gain this second refusal reason.)
- docs/acceptance-criteria/build-orchestration/BO-2400-fast-lane-build/BO-2400f-7.yaml (relationship: related | status: exists | relevance: One of the criteria that was marked done over unreachable code — the regression this AC is calibrated against. Do not amend it here.)
- docs/acceptance-criteria/build-orchestration/BO-2900-runtime-reachability-guard/BO-2900d.yaml (relationship: related | status: exists | relevance: The only legitimate way past this refusal: an explicit, reasoned, reviewable exemption. This AC hands off to it rather than deciding the helper-module case itself.)
- docs/architecture/components/phantom-done-prevention.md (relationship: describes | status: exists | relevance: The component page that must carry the second refusal cause alongside the existing done-proof conditions.)

- [ ] AC-1: how-to | scripts/ac_store/done_proof.py | the criterion is rejected as not eligible
- [ ] AC-2: sequence-diagram | scripts/ac_store/done_proof.py | the criterion is rejected as not eligible

### Delivers To

- **Agent:** python-coder
- **Contract:** Verdict { eligible: false, refusal_cause: 'no_entry_point_reaches_code', unit: str, clearing_actions: [str] } consumed by check_done_proof.py (all modes) and by BO-2900e-1's second message, and released by a recorded exemption from BO-2900d-1.

### Expects From

- **AC:** BO-2900b-3
- **Contract:** collected_invocations(script_paths) -> list[{ script: str, line: int, surface: str, capability: str }] over the derived automation-script set, used to answer condition (3) 'no automation runs that unit as a program'. Collected from executed invocations only, never from a text match.

## Sign-offs

- [ ] test-writer
- [ ] python-coder
- [ ] test-runner
- [ ] documentation-expert
- [ ] pr-reviewer
- [ ] ac-validator
- [ ] ac-fulfillment-gate
- [ ] documentation-verifier
- [ ] commit

## Comments
