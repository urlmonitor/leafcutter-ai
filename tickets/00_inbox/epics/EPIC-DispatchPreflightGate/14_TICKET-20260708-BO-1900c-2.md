---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  pr-reviewer: needed
  pull-request: needed
  python-coder: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
complexity: low
components:
- build_orchestration
created: '2026-07-08'
depends_on:
- BO-1900c
files_touched:
- config/agent_registry.json
- docs/architecture/components/agent-registry.md
priority: high
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: BO-1900c-2
status: todo
title: A correctly-assigned agent passes the charter check
---

# A correctly-assigned agent passes the charter check

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BO-1900c-2` — A correctly-assigned agent passes the charter check — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BO-1900c-2`. Component: `build-orchestration`. Assigned agent: `python-coder`. Estimated complexity: `S`. Complexity: `low`.

## Acceptance Criteria

```gherkin
Given a ticket whose task asks to "run the existing suite and report the results"
  and whose assigned phase agent is test-runner (whose registry charter covers
  executing an existing suite and reporting),
When the preflight charter check runs before spawn,
Then it finds the assigned agent's charter matches the task verb,
And it returns a fit verdict on the charter axis,
And the dispatch proceeds to spawn test-runner (no held-back on the charter axis).
```

## Test Requirements

```yaml
tests: []
```

## Sign-offs

- [ ] python-coder
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit
- [ ] pull-request

## Comments
