---
ac_traceability:
  id: ACD-2100c-3
  path: docs/acceptance-criteria/ac-driven-dev/ACD-2100-entry-point-unblocked/ACD-2100c-3.yaml
advances_current_outcome: true
agents:
  ac-fulfillment-gate: needed
  ac-validator: needed
  architect-review: needed
  commit: needed
  documentation-expert: not_needed
  documentation-verifier: not_needed
  pr-reviewer: needed
  pull-request: needed
  python-coder: needed
  test-runner: needed
  test-writer: needed
change_target: pipeline
complexity: medium
components:
- ac_driven_dev
created: '2026-08-26'
depends_on:
- 14_TICKET-20260826-ACD-2100c-2.md
documentation_required: false
files_touched:
- docs/architecture/adrs/ADR-024-interactive-pause-resume.md
- templates/workflows-js/plan-feature.js
priority: medium
requires_adr: false
requires_diagram: false
risk_surface: safety
roadmap_phase: phase_1
source_ac: ACD-2100c-3
status: todo
title: A paused run picks up at the decision it was waiting on when the answer arrives
---

# A paused run picks up at the decision it was waiting on when the answer arrives

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `ACD-2100c-3` — A paused run picks up at the decision it was waiting on when the answer arrives — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `ACD-2100c-3`. Component: `ac-driven-dev`. Assigned agent: `python-coder`. Estimated complexity: `M`. Complexity: `medium`.

## Acceptance Criteria

```gherkin
Given a run that has paused at a decision point with drafted work on disk and the
  process no longer running,
When the route is started again in a new process supplying the person's answer
  for that decision point,
Then the new run continues from that decision point rather than repeating the
  steps before it,
And the drafted work present when it paused is the work the resumed run carries
  forward unchanged,
And the answer supplied is the one applied to that decision,
And once the resumed run has moved past the decision point no record of it
  waiting remains on disk.
```

- [ ] AC-1: the new run continues from that decision point rather than repeating the
- [ ] AC-2: the drafted work present when it paused is the work the resumed run carries
- [ ] AC-3: the answer supplied is the one applied to that decision
- [ ] AC-4: once the resumed run has moved past the decision point no record of it

## Test Requirements

```yaml
tests:
- name: test_resume_in_a_new_process_continues_from_the_decision_point
  file: unit_tests/workflows/test_acd_2100c_3.py
  covers:
  - ACD-2100c-3
  asserts: A run paused to disk and its process exited; a second process supplying
    the person's answer continues from that decision point and the steps before it
    do not re-execute -- observed from the resumed run's own record of the steps it
    ran.
  framework: unittest
  type: integration
  angle: criterion
- name: test_drafted_work_carried_forward_is_the_work_that_was_present_at_pause
  file: unit_tests/workflows/test_acd_2100c_3.py
  covers:
  - ACD-2100c-3
  asserts: The drafted artifacts are compared byte-for-byte between the moment of
    pause and the moment the resumed run moves past the gate; the resumed run carries
    the same bytes forward.
  framework: unittest
  type: integration
  angle: real_artifact
- name: test_supplied_answer_is_the_one_applied_to_that_decision
  file: unit_tests/workflows/test_acd_2100c_3.py
  covers:
  - ACD-2100c-3
  asserts: Two resumes of equivalent paused runs supplying different valid answers
    take different observable branches -- so the answer is applied rather than a default.
  framework: unittest
  type: integration
  angle: criterion
- name: test_no_waiting_record_remains_after_the_resumed_run_passes_the_gate
  file: unit_tests/workflows/test_acd_2100c_3.py
  covers:
  - ACD-2100c-3
  asserts: After the resumed run moves past the decision point, a fresh process that
    lists waiting runs does not list it -- the real writer's record consumed and cleared,
    read back by the real reader.
  framework: unittest
  type: integration
  angle: seam
- name: test_acd_2100c_3_reachable_from_entry_point
  file: unit_tests/workflows/test_acd_2100c_3.py
  covers:
  - ACD-2100c-3
  angle: reachability
  asserts: 'REQUIRED — invoke the production entry point (CLI, hook, slash command,
    workflow dispatch, or main()) as a subprocess/dispatch and assert the new behaviour
    actually occurs. Do NOT satisfy this by importing the function directly. The AC
    authored no test_spec, so the entry point is not declared: resolve it before writing
    this test, and do not delete this entry.'
```

## Implementation Notes

```yaml
- The change must land under templates/. The deployed workflow is build output.
- The steps before the decision point must not re-execute on resume. Re-running them
  is observable as duplicated or rewritten drafted work and is the failure this record
  exists to prevent.
- The waiting record must be cleared as part of moving past the decision point, not
  on process exit. A record left behind makes the run look permanently paused to anything
  that lists waiting runs, and the next resume then re-applies an answer to a decision
  already made.
- Clearing the record and applying the answer must not be separately observable in
  a way that leaves the run both resumed and still listed as waiting if the process
  dies between them; the later of the two must be the one that commits the run past
  the gate.
- The supplied answer must reach the decision through the same evaluation ACD-2100c-4
  governs -- resume must not be a second, unguarded way to answer a gate.
- 'Must satisfy the repository error-handling policy: reading and removing the record
  are external I/O.'
- 'Resume must be safe to attempt when no record exists: it reports that there is
  nothing to resume rather than starting a fresh run under the old run''s identity.'
```

## Agent Contracts

### documentation-expert

Existing docs to update / cross-link:

- docs/how-to/resume-a-paused-plan-feature-run.md (relationship: describes | status: planned | relevance: The how-to authored by ACD-2100e-2 -- this is the behaviour it walks an operator through.)
- docs/architecture/adrs/ADR-024-interactive-pause-resume.md (relationship: implements | status: exists | relevance: The resume contract; the route already accepts a supplied answer on re-entry.)
- docs/known-issues/ac-driven-dev.md (relationship: describes | status: exists | relevance: KI-ACD-005's fix direction -- 'persist a pause record and exit with a status that says awaiting input, and re-enter'.)
- templates/workflows-js/plan-feature.js (relationship: implements | status: exists | relevance: Canonical source. The re-entry path that accepts a supplied answer and the gate resolution it feeds.)
- docs/architecture/components/interactive-pause-resume-substrate.md (relationship: describes | status: exists | relevance: Component doc for the resume substrate and the record lifecycle this behaviour completes.)

This record owns none of the documents above -- every entry is a cross-link to a
document another record produces. The two contract lines that stood here both
demanded `docs/how-to/resume-a-paused-plan-feature-run.md`, which ACD-2100e-2
creates; this ticket could never have satisfied them. Removed per BO-2200c-5-i
and -iii; the list above is retained as reading for the implementer.

Note the `### Delivers To` entry below addressed to documentation-expert: this
record SUPPLIES the resume invocation and its outcomes, which ACD-2100e-2's
how-to walks an operator through. That is a contract with a downstream record,
not a demand that this ticket write the page.

### Delivers To

- **Agent:** python-coder
- **Contract:** A resume path in which the supplied answer and the decision point the run is paused at are separate bindings, both available at the point the answer is applied -- so a mismatch between them is detectable rather than already collapsed.
- **Agent:** documentation-expert
- **Contract:** The resume invocation and its observable outcomes -- resumed, nothing to resume, mismatched answer -- which the how-to walks an operator through.

### Expects From

- **AC:** ACD-2100c-2
- **Contract:** A durable pause record naming the run and the decision point it stopped at, present in the project's own store after the process exits and readable by a later process started from any working directory.

## Sign-offs

- [ ] architect-review
- [ ] test-writer
- [ ] python-coder
- [ ] test-runner
- [ ] pr-reviewer
- [ ] ac-validator
- [ ] ac-fulfillment-gate
- [ ] commit
- [ ] pull-request

## Comments
