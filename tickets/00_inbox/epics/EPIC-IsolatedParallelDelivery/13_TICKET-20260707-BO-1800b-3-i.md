---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: needed
  pr-reviewer: needed
  pull-request: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
components:
- build_orchestration
created: '2026-07-07'
depends_on:
- BO-1800b-3
files_touched:
- docs/architecture/adrs/ADR-018-agent-isolation-topology.md
priority: medium
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: BO-1800b-3-i
status: todo
title: An admin merge attempt on main is rejected outside the gate
---

# An admin merge attempt on main is rejected outside the gate

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BO-1800b-3-i` — An admin merge attempt on main is rejected outside the gate — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BO-1800b-3-i`. Component: `build-orchestration`. Assigned agent: `documentation-expert`. Estimated complexity: `S`.

## Acceptance Criteria

```gherkin
Given the hub `main` protection is configured with bypass disabled,
When an operator runs an admin-override merge (for example `gh pr merge --admin`)
  on a pull request whose required checks have not passed,
Then the hub rejects the merge and `main` is unchanged,
And landing the change still requires the required checks to pass and the merge
  queue to perform the merge.
```

## Sign-offs

- [ ] documentation-expert
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit
- [ ] pull-request

## Comments
