---
ac_traceability:
  id: UXP-700c-2
  path: docs/acceptance-criteria/ux-prototyping/UXP-700-truthful-project-record/UXP-700c-2.yaml
advances_current_outcome: true
agents:
  ac-fulfillment-gate: signed_off
  ac-validator: needed
  architect-review: signed_off
  commit: needed
  documentation-expert: signed_off
  documentation-verifier: signed_off
  frontend-coder: not_needed
  pr-reviewer: signed_off
  pull-request: not_needed
  python-coder: signed_off
  sql-coder: not_needed
  test-runner: signed_off
  test-writer: signed_off
  user-surface-smoker: not_needed
ac_coverage: 4/4
change_target:
- code
- schema
complexity: high
components:
- ux_prototyping
created: '2026-09-09'
depends_on:
- 19_TICKET-20260909-UXP-700c-1.md
documentation_required: true
files_touched:
- docs/product-truth/schemas/flow.schema.json
- docs/product-truth/scripts/validate_product_truth.py
- docs/product-truth/scripts/product_truth_checks.py
- unit_tests/product_truth/test_uxp_700c_2.py
- unit_tests/product_truth/_uxp_700c_2_fixtures.py
- docs/how-to/authoring-product-truth-artifacts.md
- docs/architecture/components/ux-prototyping.md
- docs/product-truth/README.md
- docs/acceptance-criteria/ux-prototyping/UXP-700-truthful-project-record/UXP-700c-2.yaml
out_of_scope:
- unit_tests/product_truth/test_uxp_700c_2_ii.py
- unit_tests/product_truth/_uxp_700c_2_ii_fixtures.py
- docs/how-to/product-truth-schema-reference.md
- docs/acceptance-criteria/ux-prototyping/UXP-520-atlas-flow-explorer/UXP-591.yaml
priority: medium
requires_adr: false
requires_diagram: false
risk_surface: contract_boundary
roadmap_phase: phase_1
source_ac: UXP-700c-2
status: done
title: The record states what it was last confirmed against, and says so when that
  has moved
---

# The record states what it was last confirmed against, and says so when that has moved

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `UXP-700c-2` — The record states what it was last confirmed against, and says so when that has moved — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `UXP-700c-2`. Component: `ux-prototyping`. Assigned agent: `python-coder`. Estimated complexity: `L`. Complexity: `high`.

## Acceptance Criteria

```gherkin
Given a journey that describes behaviour implemented by a set of acceptance criteria and by
  files in the project,
When the record is checked,
Then each journey states the state of the things it describes at the moment it was last
  confirmed against them,
And a journey whose described things have changed since that moment is reported as behind,
  naming the journey and each described thing that changed,
And a journey whose described things have not changed since that moment is not reported,
And the check states how many journeys it compared, and that figure rises by one when a
  journey is added.
```

- [x] AC-1: each journey states the state of the things it describes at the moment it was last
- [x] AC-2: a journey whose described things have changed since that moment is reported as behind
- [x] AC-3: a journey whose described things have not changed since that moment is not reported
- [x] AC-4: the check states how many journeys it compared, and that figure rises by one when a

## Test Requirements

```yaml
tests:
- name: test_journey_is_reported_behind_after_a_described_thing_changes
  file: unit_tests/product_truth/test_uxp_700c_2.py
  covers:
  - UXP-700c-2
  asserts: Confirming a journey, then changing an acceptance criterion it describes,
    causes the journey to be reported behind naming that criterion.
  framework: unittest
  type: integration
  angle: criterion
- name: test_journey_is_not_reported_when_nothing_it_describes_changed
  file: unit_tests/product_truth/test_uxp_700c_2.py
  covers:
  - UXP-700c-2
  asserts: A confirmed journey whose described things are untouched is absent from
    the report.
  framework: unittest
  type: integration
  angle: boundary
- name: test_compared_journey_count_rises_with_the_number_of_journeys
  file: unit_tests/product_truth/test_uxp_700c_2.py
  covers:
  - UXP-700c-2
  asserts: Adding one journey raises the stated compared count by exactly one.
  framework: unittest
  type: integration
  angle: seam
- name: test_uxp_700c_2_reachable_from_entry_point
  file: unit_tests/product_truth/test_uxp_700c_2.py
  covers:
  - UXP-700c-2
  angle: reachability
  asserts: 'REQUIRED — invoke the production entry point (CLI, hook, slash command,
    workflow dispatch, or main()) as a subprocess/dispatch and assert the new behaviour
    actually occurs. Do NOT satisfy this by importing the function directly. The AC
    authored no test_spec, so the entry point is not declared: resolve it before writing
    this test, and do not delete this entry.'
```

## Agent Contracts

### documentation-expert

Existing docs to update / cross-link:

- docs/acceptance-criteria/ux-prototyping/UXP-500-product-truth-generation/UXP-590.yaml (relationship: context | status: exists | relevance: UXP-590 keeps the record self-consistent. This AC is the record's agreement with the code, which can be false while UXP-590 holds.)
- docs/product-truth/README.md (relationship: related | status: exists)

- [x] AC-1: how-to | docs/how-to/authoring-product-truth-artifacts.md | each journey states the state of the things it describes at the moment it was last <!-- signed: documentation-expert -->
- [x] AC-2: architecture-doc-extension (Lifecycle & Validation section) | docs/architecture/components/ux-prototyping.md | each journey states the state of the things it describes at the moment it was last <!-- signed: documentation-expert -->

### Delivers To

- **Agent:** python-coder
- **Contract:** A per-journey behind/current/never-confirmed verdict consumed by UXP-700c-2-ii and surfaced by UXP-700c-3.

### Expects From

- **AC:** UXP-700c-1
- **Contract:** Pointers already known to resolve, so a behind verdict is not produced for a target that simply vanished.

## Sign-offs

- [x] architect-review — 2026-09-16 14:48
- [x] test-writer — 2026-09-16 15:10
- [x] python-coder — 2026-09-16 16:45
- [x] test-runner — 2026-09-16 15:19
- [x] documentation-expert — 2026-09-16 17:20
- [x] pr-reviewer — 2026-09-16 17:45
- [ ] ac-validator — failed 2026-09-16 15:37
- [x] ac-fulfillment-gate — 2026-09-16 18:05
- [x] documentation-verifier — 2026-09-16 18:20
- [ ] commit — failed 2026-09-16 18:40

## Comments

### 2026-09-16 14:48 — architect-review (status: ok)
feedback-id: fb_2026-09-16_66860ed2
completion_manifest:
  blast_radius_reviewed: true
  rubric_classification_recorded: true
  requires_adr_confirmed: true
```json
{
  "architectural_note": "Classified SMALL. components: [ux_prototyping] is the ticket's sole top-level component; the implementation extends docs/product-truth/scripts/validate_product_truth.py with one new `_check_*`-pattern helper (following the precedent UXP-700c-1's `_check_pointers` already set — count via `record_check_executed`, mutate the shared `errors`/`warnings` lists, wire into `main()` alongside the other checks) plus a schema addition to docs/product-truth/schemas/flow.schema.json for the per-journey confirmation record, and a new unit_tests/product_truth/test_uxp_700c_2.py — at most 3-4 files, well under the 5-file threshold, no second component touched. No always-large trigger fires: no Alembic migration, no TimescaleDB hypertable, no FastAPI surface change. This ticket does not itself edit an ADR file, so has_adr_contract_change does not fire on that narrow reading, BUT it is executing directly inside an existing binding contract: ADR-043 (docs/architecture/adrs/ADR-043-journey-record-carries-its-own-behind-mark.md), authored during sibling ticket 23 (UXP-700c-2-ii), names THIS ticket verbatim as the 'producing sibling' and its §3 is a binding design constraint on exactly the artifact this ticket must produce: 'Whatever shape UXP-700c-2 chooses for the per-journey confirmation record, that record MUST expose a single stable string that identifies the confirmation, and behind.confirmed_against MUST be that string... the checker MUST NOT flatten, hash, or otherwise synthesise an identity at the point of writing the mark.' Design directive for test-writer/python-coder: the confirmation record this ticket adds to flow.schema.json MUST carry an explicit, non-derived string field naming what the journey was last confirmed against (e.g. a commit SHA, an AC-store version tag, or an explicit confirmation id supplied by whoever confirms the journey) -- it must NOT be a hash or fingerprint synthesised by the checker itself, or ticket 23's durable-mark pass breaks. The AC's own notes independently rule out reusing impl_summary.asof/impl_asof (generator-owned, rewritten every run, would make every journey permanently current) as the freshness stamp -- confirmed via direct inspection of generate_product_truth.py's asof-preservation logic. Recommend the same anti-vacuity accounting UXP-700c-1 used (resolved_pointers-style counter, stated unconditionally on every run per this AC's own AC-4) so a run that compared zero journeys is distinguishable from one that compared none because none exist. One secondary concern carried over from sibling ticket 19's own post-hoc failure: that ticket's ### documentation-expert Agent Contracts block named target_path docs/acceptance-criteria/.../UXP-590.yaml for BOTH its AC-1 (how-to) and AC-2 (sequence-diagram) rows -- an AC-store YAML, not a real doc surface -- which caused documentation-verifier to block ticket 19 outright. This ticket's own ### documentation-expert block has the identical shape (both AC-1/AC-2 rows point at the same UXP-590.yaml target_path). See acceptance_adjustments below.",
  "acceptance_adjustments": [
    "### documentation-expert Agent Contracts block: both AC-1 (how-to) and AC-2 (sequence-diagram) rows currently name target_path docs/acceptance-criteria/ux-prototyping/UXP-500-product-truth-generation/UXP-590.yaml, which is an AC-store YAML file, not a documentation surface -- the identical authoring defect that caused documentation-verifier to block sibling ticket 19 (UXP-700c-1) at sign-off. Recommend documentation-expert instead extend docs/how-to/authoring-product-truth-artifacts.md (how-to genre, per ticket 19's precedent) and docs/product-truth/README.md / docs/architecture/components/ux-prototyping.md (the existing Lifecycle & Validation section that already carries this store's diagram) rather than editing UXP-590.yaml, unless a genuine content change to UXP-590.yaml is separately justified.",
    "python-coder/test-writer: the confirmation record's identity field (behind.confirmed_against per ADR-043 §3) must be an explicit, caller-supplied string -- not a hash or synthesised fingerprint -- or ticket 23 (UXP-700c-2-ii) cannot durably mark journeys behind without violating ADR-043."
  ],
  "escalation": "none",
  "escalation_reason": "",
  "suggested_adr": null,
  "suggested_diagrams": []
}
```

### 2026-09-16 15:10 — test-writer (status: ok)
feedback-id: fb_2026-09-16_48a82035
completion_manifest:
  tests_written: true
  red_baseline_confirmed: true
  reachability_entry_point_answer:
    result: resolved
    entry_point: "python docs/product-truth/scripts/validate_product_truth.py (CLI via subprocess, main() guarded by if __name__ == '__main__':) -- the same, already-shipped entry point UXP-700c-1's test_uxp_700c_1.py pins for its own reachability test. This ticket extends that existing script with a further _check_* helper (_check_freshness) per architect-review's explicit instruction; it does not introduce a second CLI."
  cross_layer_seam_answer:
    result: covered
    producing_side: "The REAL generate_product_truth.load_flows() producer, reading on-disk JSON flow files it wrote itself (TestCheckFreshnessSeam), piped into the REAL validate_product_truth._check_freshness consumer."
    reason: "TestReachability additionally pipes the REAL validate_product_truth.py CLI's own schema-validation + freshness-check pipeline against a hand-authored on-disk store, through to the real process's own stdout/exit-code."

## Test Writer — Completion Report

### Tests Written
| File | Directory | Framework | Status |
|---|---|---|---|
| test_uxp_700c_2.py | unit_tests/product_truth/ | unittest (run via pytest) | written |

Five test functions across four classes:
- `TestCheckFreshnessDirect.test_journey_is_reported_behind_after_a_described_thing_changes` (angle: criterion) -- covers AC-1 (states what it was confirmed against, verbatim in the behind verdict) and AC-2 (reported behind, naming the journey and the changed thing).
- `TestCheckFreshnessBoundary.test_journey_is_not_reported_when_nothing_it_describes_changed` (angle: boundary) -- covers AC-3 (unchanged journey produces no warning; verdict is `None`, i.e. current).
- `TestCheckFreshnessBoundary.test_never_confirmed_journey_is_omitted_from_verdicts_and_not_compared` (angle: boundary) -- covers the never-confirmed edge implied by AC-4's "compared" accounting (a never-confirmed journey must not inflate the compared count).
- `TestCheckFreshnessSeam.test_compared_journey_count_rises_with_the_number_of_journeys` (angle: seam) -- covers AC-4 (compared count rises by exactly one when one further confirmed journey is added), round-tripped through the real `generate_product_truth.load_flows()` producer.
- `TestReachability.test_uxp_700c_2_reachable_from_entry_point` (angle: reachability) -- invokes the real `validate_product_truth.py` CLI as a subprocess against two from-scratch fixture stores (current and behind), asserting the real process's own stdout states "compared 1" on every run and names the journey + changed AC when behind.

Two new module-level symbols are specified in the test file's docstring for `python-coder` to add to `docs/product-truth/scripts/validate_product_truth.py`: `_ac_content_signature(ac_record) -> str` and `_check_freshness(flows, ac_records, warnings) -> tuple[dict, int]`, plus a new optional top-level `confirmed` object registered in `flow.schema.json` (closed shape: `against` + `state`). Per architect-review's acceptance_adjustment and ADR-043 SS3, `confirmed.against` is documented as an explicit, caller-supplied string the checker must copy verbatim into `behind.confirmed_against` -- never synthesised by the checker.

### Verification Run
- Command: `python -m pytest unit_tests/product_truth/test_uxp_700c_2.py -v`
- Result: red (4 failed, 1 xfailed via this repo's AC-enforcement pytest plugin which downgrades AC-tagged failures to xfail while the source AC's `work_status != done`; overall exit code is non-zero -- expected, implementation not yet written).

### Notes
`_check_pointers` (UXP-700c-1) is already implemented and used as the structural precedent for `_check_freshness`. The reachability test's fixture pre-computes the exact signature `_ac_content_signature` is documented to produce (sha256 of `json.dumps({work_status, product_truth, implemented_by, covered_by}, sort_keys=True, default=str)`, excluding `path`) since production code does not exist yet -- python-coder must implement that literal recipe or the reachability test's "current" case will spuriously report behind. File-path "described things" (the Gherkin's "and files in the project" clause) are out of scope for this ticket's tests -- only AC ids via `implements` are exercised, mirroring UXP-700c-1's own AC-id-only precedent; architect-review's design note likewise discusses only AC content.

red_baseline:
  - test_name: test_journey_is_reported_behind_after_a_described_thing_changes
    file: unit_tests/product_truth/test_uxp_700c_2.py
    error: "AttributeError: module 'validate_product_truth' has no attribute '_ac_content_signature'"
  - test_name: test_journey_is_not_reported_when_nothing_it_describes_changed
    file: unit_tests/product_truth/test_uxp_700c_2.py
    error: "AttributeError: module 'validate_product_truth' has no attribute '_ac_content_signature'"
  - test_name: test_never_confirmed_journey_is_omitted_from_verdicts_and_not_compared
    file: unit_tests/product_truth/test_uxp_700c_2.py
    error: "AttributeError: module 'validate_product_truth' has no attribute '_check_freshness'"
  - test_name: test_compared_journey_count_rises_with_the_number_of_journeys
    file: unit_tests/product_truth/test_uxp_700c_2.py
    error: "AttributeError: module 'validate_product_truth' has no attribute '_ac_content_signature'"
  - test_name: test_uxp_700c_2_reachable_from_entry_point
    file: unit_tests/product_truth/test_uxp_700c_2.py
    error: "xfail (AC-enforcement plugin downgrades this AC-tagged test while UXP-700c-2 work_status != done); underlying cause is the same AttributeError -- _check_freshness / _ac_content_signature not yet implemented, and flow.schema.json does not yet register the top-level `confirmed` property (additionalProperties: false rejects it)."
    note: "downgraded to xfail by this repo's pytest AC-enforcement plugin, not a pass -- the underlying implementation genuinely does not exist yet"

## Escalation

Branch: none
Reason: Single component (ux_prototyping), estimated 3-4 files touched (validate_product_truth.py, flow.schema.json, new test file, docs) -- under the 5-file threshold. No always-large trigger fires (no Alembic migration, no hypertable change, no FastAPI/public-API change, and this ticket does not itself edit an ADR file). The design is materially constrained by the pre-existing ADR-043 §3 (which names this ticket by id and binds the shape of its confirmed_against identity field), so that constraint is carried into the architectural note verbatim for python-coder/test-writer rather than triggering Opus escalation -- ADR-043 already exists and was authored specifically to close this exact cross-ticket contract risk, so re-litigating it at architect-review-deep would be redundant.
Classified small: single component (ux_prototyping), files well under the 5-file threshold, no always-large trigger fired. Design note: extend validate_product_truth.py's existing `_check_*` pattern (per UXP-700c-1 precedent) for the journey freshness check, and add a per-journey confirmation-record field to flow.schema.json whose identity string is explicit and non-synthesised, per ADR-043 §3's binding constraint on this exact ticket. Flagged a likely documentation-expert target_path authoring defect (mirrors sibling ticket 19's documentation-verifier blocker) as an acceptance adjustment. requires_adr and requires_diagram remain false as already set in frontmatter.

### 2026-09-16 16:45 — python-coder (status: ok)
feedback-id: fb_2026-09-16_9ccce71f
completion_manifest:
  ac1_confirmed_state_stamp: true
  ac2_behind_naming_journey_and_changed: true
  ac3_unchanged_not_reported: true
  ac4_compared_count_states_unconditionally: true
  adr043_ss10_same_module_as_sync_behind_marks: true
  adr043_ss3_against_is_caller_supplied_never_synthesised: true
  red_baseline_all_green: true
  real_store_spot_check: true
  tests_green: true
red_baseline_results:
  - test_name: test_journey_is_reported_behind_after_a_described_thing_changes
    result: green
  - test_name: test_journey_is_not_reported_when_nothing_it_describes_changed
    result: green
  - test_name: test_never_confirmed_journey_is_omitted_from_verdicts_and_not_compared
    result: green
  - test_name: test_compared_journey_count_rises_with_the_number_of_journeys
    result: green
  - test_name: test_uxp_700c_2_reachable_from_entry_point
    result: green
Added `_ac_content_signature` and `_check_freshness` (check 12) directly to
`docs/product-truth/scripts/validate_product_truth.py` — per ADR-043 §10 the
verdict computation MUST live in the same module as `_sync_behind_marks`
(UXP-700c-2-ii), so this was NOT extracted to a sibling `product_truth_*`
module despite that being the file's usual size-ratchet escape valve.
`confirmed.against` is read verbatim, never synthesised (ADR-043 §3), matching
test-writer's pinned `_ac_content_signature` recipe exactly. Registered the new
optional top-level `confirmed` object in `flow.schema.json`. Wired
`_check_freshness` into `run_checks()`/`main()`: feeds the shared `warnings`
list (never `errors` — a stale journey does not itself fail the build) and
`main()` states `"compared N journey(s) for freshness"` unconditionally,
mirroring `_check_pointers`'s "resolved N pointer(s)" convention. All 5
red_baseline tests are green (`unit_tests/product_truth/test_uxp_700c_2.py`,
including the real-subprocess CLI reachability test); the full
`unit_tests/product_truth/` suite (119 passed, 5 pre-existing unrelated
xfails) and the sibling UXP-700c-1 / UXP-700c-2-ii suites stayed green — no
regression. Real-artifact spot-check: ran the real CLI against the committed
store (`python docs/product-truth/scripts/validate_product_truth.py --quiet`)
— exit 0, `"compared 0 journey(s) for freshness"` (no journey carries
`confirmed` yet, expected), outcome unchanged at `checked-and-sound`. This
ticket computes the verdict only; wiring it into `_sync_behind_marks`'s
`verdicts` parameter remains UXP-700c-2-ii's own, separately-scoped follow-up
(already noted in that module's own decision history). No `### python-coder`
sub-heading exists under this ticket's `## Agent Contracts`, so Contract-Aware
Mode / §2c AC-checkbox-flip did not apply (only `### documentation-expert`
has one there); the ticket also has no `## Implementation Tasks` section, so
§1.5 was a no-op. Pre-existing, out-of-scope note for `documentation-verifier`
/ `commit`: `docs/product-truth/scripts/validate_product_truth.py` already
carries several committed (pre-HEAD, not touched by this ticket) DECISION
HISTORY entries missing their `HH:MM` timestamp (e.g. the 2026-09-14
`UXP-700c-1-i` entry); `check_documentation.py` flags the whole file on any
touch. Per doc-enforcer §6 these are legacy entries and MUST NOT be
reformatted by a coder agent, so they were left as-is — this predates and is
unrelated to this ticket's change; `scripts/commit_guardian/transform_decision_history.py`
or a dedicated documentation ticket is the appropriate fix path.

### 2026-09-16 15:19 — test-runner (status: ok)
feedback-id: fb_2026-09-16_d851fadb
completion_manifest:
  test_suite_executed: true
  all_tests_passing: true
  failure_report_structured: true
Ran the ticket-scoped `unit_tests/product_truth/test_uxp_700c_2.py -v`: all
5 tests pass (`TestCheckFreshnessDirect`, `TestCheckFreshnessBoundary` x2,
`TestCheckFreshnessSeam`, `TestReachability` including the real-subprocess
CLI reachability test). Also ran the full `unit_tests/product_truth` suite
as a regression check: 120 passed, 4 xfailed — the xfails are this repo's
AC-enforcement plugin masking two pre-existing, unrelated tickets
(UXP-700b-3, UXP-700d-3, both `work_status != done`), not UXP-700c-2. No
failures, no regressions. Confirms python-coder's claimed green baseline.

### 2026-09-16 17:20 — documentation-expert (status: ok)
feedback-id: fb_2026-09-16_67eeac00
completion_manifest:
  doc_written: true
  cross_links_added: true
  diataxis_genre_correct: true
AC-1 (how-to): extended `docs/how-to/authoring-product-truth-artifacts.md`
with a new "Part 6 — Confirm a journey against what it describes (freshness)"
section (authoring the `confirmed.against`/`confirmed.state` record, the
ADR-043 SS3 non-synthesis rule, and how a `behind` mark clears) plus a
Verification bullet and a `See Also` link to ADR-043. AC-2 (architecture-doc
extension, per `requires_diagram: false` and the same genre substitution
ticket 19 made for its own sequence-diagram AC): extended
`docs/architecture/components/ux-prototyping.md`'s existing "Lifecycle &
Validation" section with the freshness-check paragraph (and, in the same
edit, corrected an already-stale adjacent sentence that still described
UXP-700c-1's pointer check as a warning after it became a hard failure), plus
a Cross-Links entry for ADR-043. Also extended `docs/product-truth/README.md`
(named as `related` context in this ticket's "Existing docs to update /
cross-link" list) with the equivalent freshness paragraph in its own
`## Validation` section, mirroring the pointer-check paragraph already there
for UXP-700c-1, and cross-linked back to the how-to's Part 6 anchor. All
three edits pass `scripts/commit_guardian/check_documentation.py --file`.

**Anomaly / ticket-authoring fix:** per architect-review's `acceptance_adjustments`
(16:48 comment), this ticket's own `### documentation-expert` Agent Contracts
block named `target_path`
`docs/acceptance-criteria/ux-prototyping/UXP-500-product-truth-generation/UXP-590.yaml`
(an AC-store YAML, not a documentation surface) for BOTH AC-1 and AC-2 — the
identical authoring defect that caused `documentation-verifier` to block
sibling ticket 19 (UXP-700c-1) outright. Corrected both `target_path` fields
above in place to the real files touched (rather than leaving the stale
paths for `documentation-verifier` to trip over again), following ticket
19's own after-the-fact precedent of substituting an architecture-doc
extension for AC-2's nominal "sequence-diagram" genre.

### 2026-09-16 17:45 — pr-reviewer (status: ok)
feedback-id: fb_2026-09-16_db79e082
completion_manifest:
  diff_reviewed: true
  high_confidence_findings: 2
  medium_confidence_findings: 1
  opus_escalation:
    result: not_applicable
    reason: "Medium-confidence count (1) is <= 3, below the escalation threshold; no Opus dispatch needed."
    remediation: "n/a"
  contract_pass_run: true
  cross_file_contract_trace: true

**Scope.** No `Skill`/`Agent` tool was present in this session's toolset (the same
anomaly architect-review, documentation-expert and sibling ticket 23's pr-reviewer
already recorded), so `pr-review-toolkit:review-pr` could not be dispatched as a
sub-skill; this review was performed directly via `Bash`/`Read` against this ticket's
own `files_touched` (`flow.schema.json`, `validate_product_truth.py`,
`test_uxp_700c_2.py`), scoped to the `_ac_content_signature` / `_check_freshness` hunks.
The working tree's diff also carries several interleaved, unrelated changes (UXP-700a-3,
UXP-700a-4, UXP-700b-3, UXP-700b-4, UXP-700c-2-ii, UXP-700d-3, UXP-700d-4, ADR-044) not
reviewed here.

**Verification performed:** `pytest unit_tests/product_truth/test_uxp_700c_2.py -q` →
5 passed; `py_compile` on `validate_product_truth.py` → OK; `flow.schema.json` parses
and the new `confirmed` object is additive/optional (not in top-level `required`);
confirmed `_ac_content_signature`'s implementation matches the test docstring's pinned
byte-for-byte recipe exactly.

**Contract-aware pass (v2 ticket).** `Delivers To: python-coder` — "verdict consumed by
UXP-700c-2-ii and surfaced by UXP-700c-3" — see [H-2]. `Expects From: UXP-700c-1` —
"pointers already known to resolve, so a behind verdict is not produced for a target
that simply vanished" — see [H-1], reproduced directly by running `_check_freshness`
with an AC id absent from `ac_records`. This ticket has no `## AC Coverage` table in its
body (same gap ac-validator noted on sibling ticket 23), so §2c.3 is a no-op.

**[H-1] cross-file contract gap — `validate_product_truth.py:_check_freshness` produces
a behind verdict for a vanished target, contradicting this ticket's own declared
contract.** `Expects From: UXP-700c-1` states "a behind verdict is not produced for a
target that simply vanished," but `_check_freshness` has no special case for an AC id
present in `confirmed.state` that is absent from `ac_records` (the AC store as loaded
right now): `ac_records.get(ac_id, {})` falls back to `{}`, whose signature never
matches a real recorded one, so the journey is reported BEHIND naming the vanished AC as
"changed." Reproduced directly: `_check_freshness({flow}, {}, [])` on a flow confirmed
against `AC-GONE-1` returns `verdicts={'...': {'confirmed_against': ..., 'changed':
['AC-GONE-1']}}` — exactly the outcome the ticket's own contract says must not happen.
`_check_pointers` (UXP-700c-1) validates a disjoint set of pointers (`implements`, not
`confirmed.state`), so it provides no actual guarantee here. No test in this ticket's
suite exercises this case. Not a blocker against this ticket's own AC-1..AC-4 (which say
nothing about vanished targets), but it directly contradicts a contract this ticket
itself declares, and durably marking a vanished target "behind" (once UXP-700c-2-ii's
wiring lands) would misrepresent the record — worth a follow-up ticket or amendment
before that wiring ships. Sub-skill: manual cross-file contract trace (Agent/Skill tool
unavailable this session).

**[H-2] cross-file contract gap — `delivers_to UXP-700c-2-ii` still not wired even
though this ticket has now landed.** `Delivers To` contract: "A per-journey
behind/current/never-confirmed verdict consumed by UXP-700c-2-ii." `run_checks()`
computes `freshness_verdicts` (validate_product_truth.py:735) but nothing calls
`_sync_behind_marks` with it — confirmed via `grep -n "_sync_behind_marks"`: only its
own definition, docstring cross-references, and the test file appear. Ticket 23's own
ac-validator already flagged this exact absence as a blocker on 2026-09-16 15:08, with
remediation "land UXP-700c-2 (ticket 21) first, then dispatch python-coder ... to wire
`_sync_behind_marks` into `run_checks()`/`main()`." Ticket 21 (this ticket) has now
landed, but the wiring still has not happened: this ticket's own python-coder comment
defers it back to "UXP-700c-2-ii's own, separately-scoped follow-up." Neither ticket has
picked it up — the end-to-end behaviour ticket 23's own AC-1 describes ("a mark is
written ... when that check completes") still does not occur via the shipped CLI.
Flagged here so a follow-up dispatch closes the loop; each ticket's own unit tests pass
in isolation, which is exactly the case this lens exists to catch. Sub-skill: manual
cross-file contract trace (Agent/Skill tool unavailable this session).

**[M-1] `unit_tests/product_truth/test_uxp_700c_2.py` / `validate_product_truth.py` —
"described things" scoped to AC ids only, not file paths.** The Gherkin text this
ticket implements ("a journey that describes behaviour implemented by a set of
acceptance criteria and by files in the project") names files as a second kind of
described thing, but `_check_freshness` and every test here only exercise AC ids via
`implements`. This is an explicit, transparent scoping decision (architect-review's note
discusses only AC content; test-writer's docstring calls it out and defers file-path
support to a follow-on AC), not a silent omission, so it is not high-confidence — but a
reader following the AC's Gherkin text alone would expect file-path coverage that does
not exist yet.

Suppressed: 0 low-confidence nits.

### 2026-09-16 15:37 — ac-validator (status: blocker)
feedback-id: (submit-failed)
completion_manifest:
  acs_parsed: true
  implementation_evidence_found: true
  test_evidence_found: true
  store_alignment_check_run: true
  cross_file_contract_trace_run: true
  contract_gap_found:
    result: false
    reason: "Expects-From UXP-700c-1 contract ('a behind verdict is not produced for a target that simply vanished') is violated by this ticket's own shipped _check_freshness."
    remediation: "Respawn python-coder to special-case an AC id present in confirmed.state but absent from ac_records (e.g. skip it from `changed`, or route it to a distinct 'unresolvable' bucket mirroring _check_pointers's own unresolvable/broken split) so a vanished target is not reported BEHIND; add a regression test for this exact case to unit_tests/product_truth/test_uxp_700c_2.py."

AC-1..AC-4 are each individually **covered**: concrete implementation and test
evidence exists for all four (cited below). However, this ticket's own declared
`Expects From: UXP-700c-1` contract ("pointers already known to resolve, so a
behind verdict is not produced for a target that simply vanished") is violated
by the code as shipped — reproduced independently (not just re-citing
pr-reviewer's [H-1]):

```
$ python -c "
from validate_product_truth import _check_freshness
flow = {'id': 'flow-x', 'confirmed': {'against': 'commit-abc', 'state': {'AC-GONE-1': 'deadbeef'}}}
verdicts, compared = _check_freshness({'flow-x': flow}, {}, [])
print(verdicts)"
{'flow-x': {'confirmed_against': 'commit-abc', 'changed': ['AC-GONE-1']}}
```

`_check_pointers` (docs/product-truth/scripts/product_truth_checks.py:428-463,
UXP-700c-1) only validates the disjoint `implements` field on flow
nodes/steps — it provides zero guarantee for AC ids named in
`confirmed.state`, the field `_check_freshness`
(docs/product-truth/scripts/validate_product_truth.py:390-439) actually reads.
`ac_records.get(ac_id, {})` (line 438) silently falls back to `{}` for a
vanished AC id, whose signature will essentially never match the recorded
one, so the journey is reported BEHIND naming the vanished id as "changed" —
exactly the outcome this ticket's own `Expects From` contract says must not
happen. Per the ac-validator cross-file contract-trace requirement (Step 2d),
this is a blocker equivalent to a missing AC, regardless of AC-1..AC-4's own
evidence being otherwise complete.

**AC coverage evidence (informational — not the blocking issue):**
- AC-1 covered: implementation `_ac_content_signature` / `confirmed["against"]`
  copied verbatim (docs/product-truth/scripts/validate_product_truth.py:376-438);
  test `test_journey_is_reported_behind_after_a_described_thing_changes`
  (unit_tests/product_truth/test_uxp_700c_2.py:294).
- AC-2 covered: implementation `warnings.append(f"[freshness] {flow_id}: behind --
  changed: {changed}")` (validate_product_truth.py:438); test
  `test_journey_is_reported_behind_after_a_described_thing_changes`
  (unit_tests/product_truth/test_uxp_700c_2.py:294).
- AC-3 covered: implementation `verdicts[flow_id] = None; continue` (no warning
  appended) (validate_product_truth.py:435-436); test
  `test_journey_is_not_reported_when_nothing_it_describes_changed`
  (unit_tests/product_truth/test_uxp_700c_2.py:330).
- AC-4 covered: implementation `logger.warning("compared %d journey(s) for
  freshness", report["compared_freshness"])` stated unconditionally
  (validate_product_truth.py:796); test
  `test_compared_journey_count_rises_with_the_number_of_journeys`
  (unit_tests/product_truth/test_uxp_700c_2.py:385). All 5 tests in
  `unit_tests/product_truth/test_uxp_700c_2.py` pass (`pytest -q` → 5 passed).

Store-alignment check (`check_v2_ac_store_alignment.py --ticket
21_TICKET-20260909-UXP-700c-2.md`) exits 0 — no store-alignment violations.
This ticket has no `## AC Coverage` table in its body, so §2c.3's table-fill
step is a no-op (same gap noted by this ticket's own pr-reviewer on sibling
ticket 23). AC-1..AC-4 checkboxes above have been flipped `[x]` to reflect
their individually-covered status; `ac_coverage: 4/4` recorded in frontmatter.
The overall verdict is `blocker`, not `ok`, because of the cross-file contract
gap above — commit phase must not proceed until it is fixed.

Suggested remediation: Respawn `python-coder` with this finding — special-case
a `confirmed.state` AC id absent from `ac_records` so it is not folded into
`changed` (e.g. treat it the same way `_check_pointers` treats an unresolvable
target: distinct from both "resolved/current" and "broken/behind"), then add a
regression test to `unit_tests/product_truth/test_uxp_700c_2.py` pinning that
behaviour. Once fixed, re-run `ac-validator`.

### 2026-09-16 18:05 — ac-fulfillment-gate (status: ok)
feedback-id: (submit-failed)
completion_manifest:
  ac_traceability_resolved: true
  work_status_verified: true
  implemented_by_verified: true
  covered_by_verified: true
  schema_validated_after_edit: true
Resolved AC `UXP-700c-2` via the shared coverage resolver (two-key
`{id, path}` block). AC YAML `work_status` was `todo`; branch evidence
(`git diff HEAD --name-only`, this ticket's working tree is staged-not-yet-committed)
showed all three `files_touched` present in the diff, so auto-fixed
`work_status: todo -> done` and appended the diff-evidenced paths
(`docs/product-truth/schemas/flow.schema.json`,
`docs/product-truth/scripts/validate_product_truth.py`,
`unit_tests/product_truth/test_uxp_700c_2.py`) to `implemented_by`
(append-only — the existing ticket-path entry was left in place).
`covered_by` (`UXP-700c-2-i`, `UXP-700c-2-ii`) was already non-empty and
valid per the AC schema's direct-child-AC-id convention, so no auto-fix was
needed there. `check_ac_schema.py` passed (exit 0) after the edit. AC store
fields are now accurate; this gate's own scope (work_status,
implemented_by, covered_by) is fully satisfied. Note for the commit phase:
this gate does not adjudicate `ac-validator`'s separately-recorded blocker
(cross-file contract gap in `_check_freshness` vs. the `Expects From:
UXP-700c-1` contract) — that is outside this gate's scope and remains
unresolved in `agents.ac-validator: failed` above.

### 2026-09-16 18:20 — documentation-verifier (status: ok)
feedback-id: fb_2026-09-16_24ac2aca
completion_manifest:
  required_docs_list_parsed: true
  all_required_docs_present_in_diff: true
  no_placeholder_content_in_changed_docs: true
Documentation coverage verified: both required doc(s) present (branch-range diff
union working-tree diff) with real content.

Required docs (2), parsed from `### documentation-expert` Agent Contracts:
- `docs/how-to/authoring-product-truth-artifacts.md` (AC-1, how-to)
- `docs/architecture/components/ux-prototyping.md` (AC-2, architecture-doc-extension)

Integration target resolved via Candidate 1 (`@{upstream}` = `origin/main`; not
self-tracking — current branch is `epic/uxp-700-remaining-2`, not `main`).
`git diff --name-only origin/main...HEAD` was empty; both required paths appear
in the working-tree diff (`git diff HEAD --name-only`), so both are present in
the union.

Placeholder check (all four sub-checks) on both files: helper script scan
(`scan_for_placeholders`) returned zero hits on both; TBD-marker grep returned
zero hits on both; unfilled single-identifier `{token}` grep returned zero hits
on both (note: `grep -P` required `LC_ALL=C.UTF-8` in this shell's default
locale — a local environment quirk, not a template defect); both files Read in
full and contain substantial real prose (Part 6 in the how-to, the Lifecycle &
Validation paragraph in the architecture doc) — neither is empty or
heading-only. No placeholder content detected.

Note (informational, out of this phase's scope): `agents.ac-validator: failed`
above records an unresolved cross-file contract gap (`_check_freshness` vs. the
`Expects From: UXP-700c-1` "vanished target" contract) that blocks the `commit`
phase independently of documentation coverage. Documentation coverage for this
ticket is fully satisfied regardless of that blocker.

### 2026-09-16 18:40 — commit (status: blocker)
feedback-id: fb_2026-09-16_0ad5683b
completion_manifest:
  pre_commit_hooks_pass:
    result: false
    reason: "Commit was not attempted — refused before staging/running hooks."
    remediation: "See below: fix the ac-validator blocker first, then re-invoke commit."
  commit_message_valid:
    result: false
    reason: "No commit message was drafted — the ticket is not eligible for commit yet."
    remediation: "Re-invoke commit after ac-validator's blocker is resolved and re-signed-off."
  ticket_staged:
    result: false
    reason: "Nothing was staged/committed by this phase; this sign-off records the refusal itself."
    remediation: "n/a until the upstream blocker clears."
Refused to commit. `agents.ac-validator: failed` records an unresolved,
independently-reproduced blocker: `_check_freshness`
(`docs/product-truth/scripts/validate_product_truth.py`) reports a journey
BEHIND for an AC id present in `confirmed.state` but absent from the loaded
AC store (a "vanished target"), directly contradicting this ticket's own
declared `Expects From: UXP-700c-1` contract ("a behind verdict is not
produced for a target that simply vanished"). Per the signoff skill §1, a
ticket is done-eligible only when every `agents:` entry is
`{not_needed, signed_off}`; `ac-validator: failed` means this ticket is not
eligible, so `commit` must not stage or run pre-commit hooks against this
working tree. `ac-fulfillment-gate`'s own 18:05 comment likewise notes this
blocker is outside its scope and remains unresolved. Suggested remediation
(per ac-validator's 15:37 comment): respawn `python-coder` to special-case a
`confirmed.state` AC id absent from `ac_records` (skip it from `changed`,
mirroring `_check_pointers`'s unresolvable/broken split) and add a regression
test to `unit_tests/product_truth/test_uxp_700c_2.py` pinning that behaviour;
then re-run `ac-validator`; only then re-invoke `commit`. No files were
staged or modified by this phase beyond this ticket's own sign-off.

### 2026-09-16 22:10 — python-coder (status: ok)
feedback-id: fb_2026-09-16_3f3e7435

Finished by hand from the halted `/build-feature` run `wf_3d2879c1-2ae`'s
backup (`_backups/epic-uxp700-run-wf_3d2879c1-2ae/staged.patch`), on branch
`feature/uxp-700c-2-c-2-ii` off `origin/main`, at the user's direction.

Fixed the ac-validator/commit blocker above: in `_check_freshness`, an AC id
named in a journey's `confirmed.state` that has since vanished from the AC
store fell back to `ac_records.get(ac_id, {})`, an empty record whose
signature can never match the recorded one, so the journey was wrongly
reported BEHIND with the vanished id listed as "changed" — reproduced
verbatim via ac-validator's own repro. Fixed per its own suggested
remediation: a `confirmed.state` id absent from `ac_records` is now routed
to a distinct `[freshness-unresolvable]` warning and never folded into
`changed`, mirroring `_check_pointers`'s unresolvable/broken split. Added
`TestFreshnessSurvivesAVanishedCitationTarget` (`# covers: UXP-700c-2`,
`# angle: failure`) to `unit_tests/product_truth/test_uxp_700c_2.py`,
confirmed RED against the unfixed code for exactly this reason, then GREEN
after the fix (test authored directly by python-coder in this pass — no
test-writer dispatch exists in this by-hand recovery flow).

Tried extracting the freshness/behind-mark functions into a sibling
`product_truth_freshness.py` module (this file's usual ratchet escape
valve); reverted after it reproduced a real bug only under the FULL
`unit_tests/product_truth` suite (never in file-level isolation):
`test_uxp_700a_1.py` pops `validate_product_truth` from `sys.modules` in its
own cleanup, so a later cross-module re-import can bind to a different
module object than the one a test patched `vpt.STORE` on. Kept the five
functions inline instead, per ADR-043 SS10's own binding requirement (the
write/remove helper must live beside the verdict computation in
`validate_product_truth.py`) — the same call the halted run's own
python-coder passes on both this ticket and sibling ticket 23 independently
made. To bring the file back under its 400-content-line ratchet with the
freshness/behind-mark code staying inline, moved OTHER, unrelated,
STORE-independent code out instead: `OUTCOME_BY_COMBO`, `_validate_schema`,
and two new pure functions (`validate_eval_rows`, `check_mock_data_ref`,
extracted from `_check_eval`'s row loop and `run_checks()`'s
`mock_data_ref` cross-check respectively — neither reads nor patches
`STORE`) now live in `docs/product-truth/scripts/product_truth_checks.py`,
re-imported so `vpt._validate_schema`/`vpt.OUTCOME_BY_COMBO` still resolve.
`validate_product_truth.py` measured 398 content lines, `product_truth_checks.py`
396, both via the hook's own `count_content_lines`; `check_file_size.py`
run directly against the staged set passes (exit 0).

Verified: `unit_tests/product_truth/test_uxp_700c_2.py` (6/6) and the full
`unit_tests/product_truth` suite (125 passed, 11 subtests passed, 0
failures) both green — versus a disposable `git worktree add -d` pristine
`origin/main` baseline on this host (113 passed, 11 subtests, 0 failures;
the +12 delta is this group's two new test files' 6+6 tests). Real-artifact
spot-check: `python docs/product-truth/scripts/validate_product_truth.py
--quiet` against the committed store — exit 0, `"compared 0 journey(s) for
freshness"`, outcome unchanged at `checked-and-sound`, zero journey-file
churn (`git status` clean under `docs/product-truth/flows/`). Mutation
check: reverted the fix in place, confirmed
`TestFreshnessSurvivesAVanishedCitationTarget` goes red for the exact
vanished-id reason, restored, confirmed green again.

`agents.ac-validator` and `agents.commit` are reset to `needed` (not
`signed_off`) — those phases were not actually re-dispatched in this by-hand
pass, only independently verified by python-coder directly, per this
project's own established by-hand-completion precedent (see
`16_TICKET-20260909-UXP-700b-3.md` as merged). `status: done` set per the
user's explicit direction for this recovery pass.

## Escalation

Branch: none
Reason: not escalated — medium count was 1 (threshold > 3).
