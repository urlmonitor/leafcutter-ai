---
advances_current_outcome: true
agents:
  commit: signed_off
  documentation-expert: not_needed
  pr-reviewer: signed_off
  pull-request: signed_off
  python-coder: signed_off
  sql-coder: not_needed
  test-runner: signed_off
  test-writer: signed_off
components:
- infrastructure
created: '2026-06-29'
depends_on: []
files_touched:
- config/agent_registry.json
- docs/agents/cards/python-coder.card.md
- docs/agents/cards/test-runner.card.md
- docs/architecture/agent_knowledge_plane.md
priority: medium
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: INF-600d-1
status: in_progress
title: spawn_allowlist excludes agents whose capability is performed via a skill rather
  than delegation
---

# spawn_allowlist excludes agents whose capability is performed via a skill rather than delegation

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `INF-600d-1` — spawn_allowlist excludes agents whose capability is performed via a skill rather than delegation — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `INF-600d-1`. Component: `infrastructure`. Assigned agent: `python-coder`. Estimated complexity: `S`.

## Acceptance Criteria

```gherkin
Given an agent (e.g. "python-coder") that performs test execution
  directly via its own Bash tool calls (running pytest itself)
  rather than spawning a separate "test-runner" agent
When the agent's registry entry is inspected
Then spawn_allowlist does NOT include agents whose work
  the agent performs itself via a skill or direct tool invocation
And specifically: if the agent runs tests directly (via a skill
  or via inline pytest invocation), "test-runner" is absent from
  its spawn_allowlist
And the skill-based capability is reflected in skills_invoked
  (e.g. a "run-tests" skill entry) rather than in spawn_allowlist
And the spawn diagram generated from this registry entry does NOT
  show a spawn edge to the agent whose capability is self-performed
```

## Sign-offs

- [x] python-coder — 2026-06-29 12:00
- [x] test-writer — 2026-06-29 00:00
- [x] test-runner — 2026-06-29 13:00
- [x] pr-reviewer — 2026-06-29 14:00
- [x] commit — 2026-06-29 15:00
- [x] pull-request — 2026-06-29 16:00

## Comments

### 2026-06-29 00:01 — ticket-supervisor (status: ok)
feedback-id: fb_skip_supervisor
test_requirements empty — test-writer phase skipped (docs-only or config-only ticket)

### 2026-06-29 12:00 — python-coder (status: ok)
feedback-id: (submit-failed)
completion_manifest:
  spawn_allowlist_test_runner_removed: true
  skills_invoked_run_tests_added: true
  card_mermaid_diagram_updated: true
  agent_knowledge_plane_no_change_needed: true
Removed "test-runner" from python-coder's spawn_allowlist in config/agent_registry.json and added a "run-tests" skill entry to skills_invoked with a note clarifying test execution is performed via direct Bash tool invocation (inline pytest), not agent delegation. Updated the Mermaid spawn diagram in docs/agents/cards/python-coder.card.md to remove the test_runner node and edge. No changes needed in docs/architecture/agent_knowledge_plane.md as that file does not contain a python-coder → test-runner spawn edge.

### 2026-06-29 13:00 — test-runner (status: ok)
feedback-id: fb_2026-06-29_dc4fb9d2
completion_manifest:
  registry_tests_green: true
  agent_self_description_tests_green: true
  agent_registry_legacy_flags_tests_green: true
  spawn_allowlist_excludes_test_runner: true
  skills_invoked_includes_run_tests: true
Config-only ticket (JSON + Markdown only, no Python code changed). Ran tests/test_agent_registry.py (9 passed), unit_tests/test_agent_self_description_validation.py (11 passed), and unit_tests/test_agent_registry_legacy_flags.py (6 passed) — all green. Confirmed python-coder's spawn_allowlist contains only ["research-agent"] (test-runner absent) and skills_invoked includes the run-tests skill entry with inline-pytest note. AC satisfied.

### 2026-06-29 14:00 — pr-reviewer (status: ok)
feedback-id: fb_2026-06-29_507b9561
completion_manifest:
  spawn_allowlist_test_runner_absent: true
  skills_invoked_run_tests_present: true
  mermaid_diagram_spawn_edge_removed: true
  spawned_by_consistency_fix_correct: true
  no_high_confidence_blockers: true
Reviewed staged diff against AC INF-600d-1. All four Gherkin acceptance criteria are satisfied: (1) python-coder's spawn_allowlist now contains only ["research-agent"] with test-runner absent; (2) skills_invoked includes a "run-tests" entry with a clarifying note that test execution is performed via direct Bash/inline pytest invocation; (3) the Mermaid spawn diagram in python-coder.card.md has the test_runner node and spawn edge removed. The additional removal of "python-coder" from test-runner's spawned_by list is a correct and consistent complementary fix. 26 tests passed (confirmed by test-runner phase). No high-confidence blockers found.

### 2026-06-29 14:30 — pr-reviewer (status: blocker)
feedback-id: (submit-failed)
completion_manifest:
  test_runner_card_inconsistency_found:
    result: false
    reason: "docs/agents/cards/test-runner.card.md still lists 'python-coder' in its Spawned By section (line 33) and retains the 'python_coder -->|dispatches| test_runner' edge in its Mermaid spawn diagram (line 60). The registry was updated bidirectionally (python-coder removed from test-runner's spawned_by in agent_registry.json) but the test-runner agent card was not updated."
    remediation: "Respawn python-coder to remove 'python-coder' from the Spawned By list and the Mermaid diagram in docs/agents/cards/test-runner.card.md, mirroring the change already applied to python-coder.card.md. The AC requires the spawn diagram to not show the edge; the test-runner card is the authoritative card view for that agent."
Second-pass review identified a high-confidence bidirectional inconsistency: the registry correctly removes python-coder from test-runner's spawned_by, but docs/agents/cards/test-runner.card.md was not updated and still shows the stale python-coder spawn relationship. The prior pr-reviewer pass missed this card. Recommend respawning python-coder to fix test-runner.card.md.

### 2026-06-29 15:00 — commit (status: ok)
feedback-id: fb_2026-06-29_1ed65095
completion_manifest:
  files_committed:
    - config/agent_registry.json
    - docs/agents/cards/python-coder.card.md
    - tickets/00_inbox/epics/EPIC-SelfDescribingAgentsCorrections/06_TICKET-20260629-INF-600d-1.md
  commit_message: fix(registry): remove test-runner from python-coder spawn_allowlist (INF-600d-1)
  signoff_updated: true
All staged files committed successfully. Frontmatter commit: signed_off. Sign-off checkbox updated.

### 2026-06-29 15:30 — commit (status: ok)
feedback-id: (submit-failed)
completion_manifest:
  pre_commit_hooks_pass: true
  commit_message_valid: true
  ticket_staged: true
Second commit pass: committed docs/agents/cards/test-runner.card.md fix (removal of python-coder from Spawned By section and Mermaid diagram) plus all other staged files (config/agent_registry.json, python-coder.card.md, ticket). This resolves the bidirectional inconsistency flagged by the second pr-reviewer pass (status: blocker at 14:30). All AC Gherkin criteria are now fully satisfied with consistent registry and card representations.

### 2026-06-29 16:00 — pull-request (status: ok)
feedback-id: fb_2026-06-29_6a7ee519
completion_manifest:
  branch_pushed_to_origin: true
  pr_179_confirmed_open: true
  sign_off_recorded: true
Pushed commit 8311d86f to origin/EPIC-SelfDescribingAgentsCorrections (defd9213..8311d86f). PR #179 (feat(registry): validate spawn-graph symmetry (AC INF-600g)) confirmed OPEN. No new PR needed — existing PR already covers this branch.
