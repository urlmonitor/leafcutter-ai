---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  pr-reviewer: needed
  pull-request: needed
  python-coder: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
components:
- ac-store
created: '2026-06-11'
depends_on:
- ACS-500a
files_touched:
- docs/acceptance-criteria/index.yaml
- docs/reference/ac-schema.md
- docs/architecture/adrs/ADR-007-ac-store-schema-id-format-enforcement.md
priority: high
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: ACS-500a-2
status: todo
title: Pattern definitions live in the existing component registry hierarchy
---

# Pattern definitions live in the existing component registry hierarchy

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `ACS-500a-2` — Pattern definitions live in the existing component registry hierarchy — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `ACS-500a-2`. Component: `ac-store`. Assigned agent: `python-coder`. Estimated complexity: `S`.

## Acceptance Criteria

```gherkin
Given a shared behavior pattern needs to be defined (e.g. "paginated-collection"),
When the pattern AC is created,
Then it is stored as a standard AC YAML file within
  docs/acceptance-criteria/<component>/<feature-folder>/,
And it uses the same component field as the consuming ACs that reference it,
And it appears in the parent AC's covered_by list like any other child,
And no separate "pattern catalog" directory or registry file is created.
```

## Sign-offs

- [ ] python-coder
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit
- [ ] pull-request

## Comments
