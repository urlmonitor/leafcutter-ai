---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  frontend-coder: needed
  pr-reviewer: needed
  pull-request: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
complexity: low
components:
- build-orchestration
created: '2026-07-10'
depends_on:
- BO-2100a
files_touched:
- templates/workflows-js/build-ticket.js
- templates/skills/building-epics/SKILL.md
- docs/architecture/adrs/ADR-020-live-surface-tester.md
priority: medium
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: BO-2100a-3
status: todo
title: build-ticket.js phase ordering includes live-surface-tester after the smoker
  and before commit
---

# build-ticket.js phase ordering includes live-surface-tester after the smoker and before commit

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BO-2100a-3` — build-ticket.js phase ordering includes live-surface-tester after the smoker and before commit — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BO-2100a-3`. Component: `build-orchestration`. Assigned agent: `frontend-coder`. Estimated complexity: `S`. Complexity: `low`.

## Acceptance Criteria

```gherkin
Given the canonical phase ordering array `phaseOrder` in
  templates/workflows-js/build-ticket.js, which currently lists
  `user-surface-smoker` (priority 11.5) immediately before `commit` (priority 12)
When a ticket's `agents:` map includes `live-surface-tester: needed`
Then `live-surface-tester` is a member of `phaseOrder`
  And it is positioned after `user-surface-smoker` and before `commit`
  (priority 11.8), so the phase sort dispatches it once the smoker has run and
  before the commit phase begins.
```

## Test Requirements

```yaml
tests:
- name: test_bo_2100a_3_live_surface_tester_is_a_member_of_phaseorder
  file: unit_tests/test_bo_2100a_3.py
  covers:
  - BO-2100a-3
  asserts: '`live-surface-tester` is a member of `phaseOrder`'
```

## Implementation Notes

```yaml
- The inserted priority (11.8) must preserve the existing relative order of every
  other phase; no pre-existing entry's relative order may change.
- 'The agent key added to phaseOrder must exactly match the key emitted by the ticket-authoring
  skill (BO-2100a-1) and listed in the supervisor allowlist (BO-2100a-2): `live-surface-tester`.'
- Must remain consistent with the building-epics canonical dispatch-order documentation
  (BO-2100a-4) at priority 11.8.
```

## Sign-offs

- [ ] frontend-coder
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit
- [ ] pull-request

## Comments
