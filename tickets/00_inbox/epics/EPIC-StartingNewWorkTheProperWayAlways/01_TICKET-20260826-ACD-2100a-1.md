---
ac_traceability:
  id: ACD-2100a-1
  path: docs/acceptance-criteria/ac-driven-dev/ACD-2100-entry-point-unblocked/ACD-2100a-1.yaml
advances_current_outcome: true
agents:
  ac-fulfillment-gate: needed
  ac-validator: needed
  commit: needed
  documentation-expert: needed
  documentation-verifier: needed
  pr-reviewer: needed
  pull-request: needed
  python-coder: needed
  test-runner: needed
  test-writer: needed
change_target: pipeline
complexity: medium
components:
- ac_driven_dev
- worktree_manager
created: '2026-08-26'
depends_on: []
documentation_required: true
files_touched:
- templates/workflows-js/plan-feature.js
priority: medium
requires_adr: false
requires_diagram: false
risk_surface: internal
roadmap_phase: phase_1
source_ac: ACD-2100a-1
status: todo
title: The worktree step runs the copy of the setup script that belongs to the repository
  being worked on
---

# The worktree step runs the copy of the setup script that belongs to the repository being worked on

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `ACD-2100a-1` — The worktree step runs the copy of the setup script that belongs to the repository being worked on — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `ACD-2100a-1`. Component: `ac-driven-dev`. Assigned agent: `python-coder`. Estimated complexity: `M`. Complexity: `medium`.

## Acceptance Criteria

```gherkin
Given two copies of the worktree setup step exist on the machine -- one inside
  the repository the run will operate on, and one in an untracked parent
  directory that is not itself a repository,
And the two copies differ in an externally observable way, so which one ran can
  be told from the run's output alone,
And the run is started with the process working directory set to that untracked
  parent directory,
When the route creates the authoring worktree,
Then the copy that executes is the one inside the repository being operated on,
And the authoring worktree exists on disk afterwards as a working copy of that
  repository with the expected branch checked out,
And the run's own record of the command it issued names an absolute location
  inside that repository rather than a location relative to the working
  directory.
```

- [ ] AC-1: the two copies differ in an externally observable way, so which one ran can
- [ ] AC-2: the run is started with the process working directory set to that untracked
- [ ] AC-3: the copy that executes is the one inside the repository being operated on
- [ ] AC-4: the authoring worktree exists on disk afterwards as a working copy of that
- [ ] AC-5: the run's own record of the command it issued names an absolute location

## Test Requirements

```yaml
tests:
- name: test_worktree_step_runs_the_in_repo_copy_from_an_untracked_cwd
  file: unit_tests/workflows/test_acd_2100a_1.py
  covers:
  - ACD-2100a-1
  asserts: Two distinguishable copies of the setup step on disk (one inside the target
    repository, one in an untracked parent); the workflow is driven from the untracked
    parent and the marker emitted by the in-repository copy -- not the parent copy
    -- appears in the captured output.
  framework: unittest
  type: integration
  angle: criterion
- name: test_worktree_step_invocation_is_reached_from_the_workflow_entry_point
  file: unit_tests/workflows/test_acd_2100a_1.py
  covers:
  - ACD-2100a-1
  asserts: Driving the real workflow entry point produces the worktree on disk under
    the target repository with the expected branch checked out, and the recorded command
    string is an absolute path rooted inside that repository.
  framework: unittest
  type: integration
  angle: reachability
- name: test_wrong_copy_execution_is_observable_and_blocks
  file: unit_tests/workflows/test_acd_2100a_1.py
  covers:
  - ACD-2100a-1
  asserts: With only the untracked-parent copy present and no copy inside the target
    repository, the step fails and names the location it could not resolve -- it does
    not silently fall back to the parent copy.
  framework: unittest
  type: integration
  angle: failure
```

## Implementation Notes

```yaml
- The change must land under templates/. A repair applied to the deployed copy under
  .leafcutter/ is regenerated away by the next build and does not count as delivered
  (see ACD-2100d).
- The resolved script location must be absolute at the point it is handed to the process
  launcher -- the run must not rely on the caller's working directory to complete
  the path.
- The run's own diagnostic record must carry the resolved absolute location it invoked
  so a wrong-copy execution is diagnosable after the fact without re-running.
- The resolution must be shared with the other four cwd-relative sites (ACD-2100a-3
  and ACD-2100a-4) rather than patched here alone; a per-site fix is what left three
  sites standing after the known issues were filed.
- 'Must satisfy the repository error-handling policy: the resolution is external I/O,
  so a failure to resolve must raise a typed error or log at WARNING and re-raise
  -- never be swallowed into a silent fallback.'
- Must not change the setup step's existing command-line contract; callers that already
  pass an explicit location must behave exactly as they do today.
- The startup path up to and including this step must complete with no network available.
```

## Agent Contracts

### documentation-expert

Existing docs to update / cross-link:

- docs/reference/plan-feature-layout-and-startup-checks.md (relationship: describes | status: planned | relevance: The reference page authored by ACD-2100d-4. This behaviour is one of the layout facts it must state; cross-link rather than restate.)
- docs/known-issues/ac-driven-dev.md (relationship: describes | status: exists | relevance: KI-ACD-004 records the observed failure and the reproduction; its second fix direction is this behaviour.)
- templates/workflows-js/plan-feature.js (relationship: implements | status: exists | relevance: Canonical source of the route. The resolution site is the worktree-creation step around line 1800; the deployed copy under .leafcutter/ is build output and is not the place to fix it.)
- docs/architecture/adrs/ADR-001-self-hosting-boundary.md (relationship: describes | status: exists | relevance: Defines the self-hosting layout in which the deployed copy sits outside the repository it operates on.)
- docs/architecture/components/ac-driven-dev.md (relationship: describes | status: exists | relevance: Component doc for the route; primary_code names templates/workflows-js/plan-feature.js as the surface this behaviour changes.)
- docs/architecture/components/worktree-manager.md (relationship: describes | status: exists | relevance: The worktree-creation surface the route shells out to.)

- [ ] AC-1: [reference-doc] docs/reference/plan-feature-layout-and-startup-checks.md — the copy that executes is the one inside the repository being operated on

### Delivers To

- **Agent:** python-coder
- **Contract:** An absolute repository-anchored location for a named support file under the route's installed support directory, derived from the repository being operated on and not from the caller's working directory. The same resolution ACD-2100a-3 and ACD-2100a-4 consume for the registry and the pause store.

## Sign-offs

- [ ] test-writer
- [ ] python-coder
- [ ] test-runner
- [ ] documentation-expert
- [ ] pr-reviewer
- [ ] ac-validator
- [ ] ac-fulfillment-gate
- [ ] documentation-verifier
- [ ] commit
- [ ] pull-request

## Comments
