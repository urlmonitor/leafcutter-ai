---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  pr-reviewer: needed
  pull-request: not_needed
  python-coder: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
components:
- build_pipeline
created: '2026-06-22'
depends_on:
- BP-900e
- BP-900e-1
files_touched:
- docs/acceptance-criteria/build_pipeline/BP-900-deployment-completeness/BP-900e.yaml
- scripts/build_propagation_audit.py
- docs/architecture/adrs/ADR-001-self-hosting-boundary.md
- docs/architecture/adrs/ADR-013-portable-skill-script-deployment-boundary.md
priority: high
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: BP-900e-4
status: todo
title: The failure report names each undeployed script, where it was promised, and
  the action to resolve it
---

# The failure report names each undeployed script, where it was promised, and the action to resolve it

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BP-900e-4` — The failure report names each undeployed script, where it was promised, and the action to resolve it — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BP-900e-4`. Component: `build-pipeline`. Assigned agent: `python-coder`. Estimated complexity: `S`.

## Acceptance Criteria

```gherkin
Given the registry-completeness check has found two promised-but-undeployed
  scripts: check_test_fixture_bloat.py (promised by a hook registry entry) and
  a script promised by an agent template reference
When the check fails the merge gate
Then the failure report lists every promised-but-undeployed script (not only
  the first one found)
And for each listed script the report states its source path under scripts/
And for each listed script the report states WHERE it was promised — for a
  registry entry, the hook id and the registry file; for a template
  reference, the referencing template file path
And for each listed script the report states the suggested action: promote the
  script to its expected templates/scripts/ path (or remove the promise), and
  names that expected templates/scripts/ path
```

## Sign-offs

- [ ] python-coder
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit

## Comments
