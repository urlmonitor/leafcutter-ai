---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  pr-reviewer: needed
  pull-request: not_needed
  python-coder: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
components:
- build_orchestration
created: '2026-07-07'
depends_on:
- BO-1800a-1
files_touched:
- docs/architecture/adrs/ADR-018-agent-isolation-topology.md
- docs/architecture/components/worktree-manager.md
priority: medium
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: BO-1800a-1-ii
status: todo
title: A drive requested on a non-native filesystem is refused before it can start
---

# A drive requested on a non-native filesystem is refused before it can start

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BO-1800a-1-ii` — A drive requested on a non-native filesystem is refused before it can start — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BO-1800a-1-ii`. Component: `build-orchestration`. Assigned agent: `python-coder`. Estimated complexity: `S`.

## Acceptance Criteria

```gherkin
Given a drive is requested with its working copy located on a filesystem that is
  not a native POSIX filesystem (for example a `/mnt/c` DrvFs mount, a network
  share, or a portable/removable drive),
When the isolation layer prepares the drive,
Then it refuses to create the drive there and reports that isolation requires a
  native POSIX filesystem (ext4/btrfs/xfs), naming the offending path,
And no drive working copy is created on that filesystem.
```

## Sign-offs

- [ ] python-coder
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit

## Comments
