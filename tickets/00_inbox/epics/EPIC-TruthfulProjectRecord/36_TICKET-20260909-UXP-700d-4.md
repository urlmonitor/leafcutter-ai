---
ac_coverage: 4/4
ac_traceability:
  id: UXP-700d-4
  path: docs/acceptance-criteria/ux-prototyping/UXP-700-truthful-project-record/UXP-700d-4.yaml
advances_current_outcome: true
agents:
  ac-fulfillment-gate: signed_off
  ac-validator: signed_off
  architect-review: not_needed
  commit: needed
  documentation-expert: not_needed
  documentation-verifier: not_needed
  frontend-coder: not_needed
  pr-reviewer: not_needed
  pull-request: not_needed
  python-coder: signed_off
  sql-coder: not_needed
  test-runner: signed_off
  test-writer: signed_off
  user-surface-smoker: not_needed
change_target: code
complexity: low
components:
- ux_prototyping
created: '2026-09-09'
depends_on:
- 29_TICKET-20260909-UXP-700d-1.md
files_touched:
- docs/product-truth/scripts/product_truth_outcome.py
- docs/product-truth/scripts/validate_product_truth.py
- unit_tests/product_truth/test_uxp_700d_4.py
out_of_scope:
- docs/reference/product-truth-checker-outcomes.md
- docs/architecture/adrs/ADR-042-product-truth-checker-outcome-vocabulary.md
- unit_tests/product_truth/test_uxp_700b_3.py
- unit_tests/product_truth/test_uxp_700b_4.py
priority: medium
requires_adr: false
requires_diagram: false
risk_surface: internal
roadmap_phase: phase_1
source_ac: UXP-700d-4
status: done
title: An artifact type whose whole population is example content is reported
---

# An artifact type whose whole population is example content is reported

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `UXP-700d-4` — An artifact type whose whole population is example content is reported — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `UXP-700d-4`. Component: `ux-prototyping`. Assigned agent: `python-coder`. Estimated complexity: `S`. Complexity: `low`.

## Acceptance Criteria

```gherkin
Given a project whose record holds at least one artifact of a given type,
When the record is checked,
Then the check states, for each artifact type, how many artifacts of that type belong to the
  project and how many belong to the example product,
And an artifact type whose project count is zero while its example count is not is reported
  by name,
And an example artifact is never counted as satisfying that type for the project,
And a type for which both counts are zero is reported as empty rather than as example-only.
```

- [x] AC-1: the check states, for each artifact type, how many artifacts of that type belong to the
- [x] AC-2: an artifact type whose project count is zero while its example count is not is reported
- [x] AC-3: an example artifact is never counted as satisfying that type for the project
- [x] AC-4: a type for which both counts are zero is reported as empty rather than as example-only.

## Test Requirements

```yaml
tests:
- name: test_type_with_only_example_artifacts_is_reported_by_name
  file: unit_tests/product_truth/test_uxp_700d_4.py
  covers:
  - UXP-700d-4
  asserts: A store whose screens are all example content reports screens as example-only.
  framework: unittest
  type: integration
  angle: criterion
- name: test_per_type_project_and_example_counts_are_both_stated
  file: unit_tests/product_truth/test_uxp_700d_4.py
  covers:
  - UXP-700d-4
  asserts: The report carries two counts per artifact type and they sum to that type's
    population.
  framework: unittest
  type: integration
  angle: criterion
- name: test_type_with_no_artifacts_at_all_is_reported_empty_not_example_only
  file: unit_tests/product_truth/test_uxp_700d_4.py
  covers:
  - UXP-700d-4
  asserts: A type with zero project and zero example artifacts is reported as empty.
  framework: unittest
  type: unit
  angle: boundary
- name: test_uxp_700d_4_reachable_from_entry_point
  file: unit_tests/product_truth/test_uxp_700d_4.py
  covers:
  - UXP-700d-4
  angle: reachability
  asserts: 'REQUIRED — invoke the production entry point (CLI, hook, slash command,
    workflow dispatch, or main()) as a subprocess/dispatch and assert the new behaviour
    actually occurs. Do NOT satisfy this by importing the function directly. The AC
    authored no test_spec, so the entry point is not declared: resolve it before writing
    this test, and do not delete this entry.'
```

## Agent Contracts

### Expects From

- **AC:** UXP-700d-1
- **Contract:** The per-artifact ownership predicate these counts are grouped by.

## Sign-offs

- [x] test-writer — 2026-09-16 14:51
- [x] python-coder — 2026-09-16 15:01
- [x] test-runner — 2026-09-16 16:20
- [x] ac-validator — 2026-09-16 15:16
- [x] ac-fulfillment-gate — 2026-09-16 18:40
- [ ] commit

## Comments

### 2026-09-16 14:51 — test-writer (status: ok)
feedback-id: fb_2026-09-16_0aae279b
completion_manifest:
  tests_written: true
  red_baseline_confirmed: true
  cross_layer_seam_answer:
    result: covered
    producing_side: "docs/product-truth/scripts/validate_product_truth.py main()'s
      stdout outcome payload — the same producer UXP-700b-1's own seam test
      already pins, now asserted to carry this AC's two new keys
      (type_population, example_only_types)"
    consuming_side: "TestExampleOnlyTypesReachableFromEntryPoint, which subprocess-runs
      the real CLI against the real, checked-in product-truth store and reads
      payload['type_population'] / payload['example_only_types'] from its
      actual stdout — never a hand-typed literal"
  reachability_entry_point_answer:
    result: resolved
    entry_point: "python docs/product-truth/scripts/validate_product_truth.py
      (CLI via subprocess, main() guarded by if __name__ == '__main__':) — the
      same entry point UXP-700b-1's own reachability test already resolved and
      proved reachable; this AC's reachability test proves it again for the
      two new stdout-contract fields this AC adds"
Wrote unit_tests/product_truth/test_uxp_700d_4.py: 4 tests (3 in-process +
1 subprocess-CLI reachability) pinning per-type project/example population
counts (compute_type_population / compute_example_only_types, proposed new
functions in docs/product-truth/scripts/product_truth_outcome.py) and two
new fields on validate_product_truth.py's existing stdout outcome contract
line. Verified red by direct execution: `python -m unittest
unit_tests.product_truth.test_uxp_700d_4 -v` — 3 errors (AttributeError,
compute_type_population does not exist yet) + 1 failure (AssertionError,
the real CLI's stdout contract carries no type_population/example_only_types
key yet), exit code non-zero. Tests use the REAL, checked-in
docs/product-truth store throughout (Fixture Authenticity Rule) — VERIFIED
(2026-09-16, direct execution) the real store's "mockups" (10/10) and
"mock-data" (2/2) populations are entirely fern-and-fig today, matching the
AC's own notes and giving the example-only test a genuine, non-synthetic
fixture; only the both-zero boundary test (AC-4) needed a synthetic empty
input, since no real-store type is currently empty.

### 2026-09-16 15:01 — python-coder (status: ok)
feedback-id: fb_2026-09-16_d9b556e6
completion_manifest:
  red_baseline_all_green: true
  ac1_per_type_counts_stated: true
  ac2_example_only_named: true
  ac3_no_example_leakage: true
  ac4_both_zero_reported_empty: true
  upstream_dependency_verified: true
red_baseline_results:
  - test_name: test_type_with_only_example_artifacts_is_reported_by_name
    result: green
  - test_name: test_per_type_project_and_example_counts_are_both_stated
    result: green
  - test_name: test_type_with_no_artifacts_at_all_is_reported_empty_not_example_only
    result: green
  - test_name: test_uxp_700d_4_reachable_from_entry_point
    result: green
Verified upstream `Expects From` deliverable (UXP-700d-1's
product_ownership.is_example_artifact_id) exists in the tree before starting.
Added compute_type_population(flows, mocks, mockups) and
compute_example_only_types(type_population) to
docs/product-truth/scripts/product_truth_outcome.py, both pure functions
built on product_ownership.is_example_artifact_id (imported verbatim, never
re-derived), plus DECISION HISTORY entries. Wired
"type_population"/"example_only_types" onto validate_product_truth.py's
existing single stdout JSON outcome-contract line
(_print_outcome_contract, run_checks(), main()) with matching DECISION
HISTORY entries. Ran
`python -m unittest unit_tests.product_truth.test_uxp_700d_4 -v`: 4/4 green
(all 4 red_baseline tests from test-writer's sign-off now pass). Ran the
full `python -m unittest discover -s unit_tests/product_truth -t . -p
"test_*.py"` suite: exit code 0, no regressions. Ran `ruff check` on both
touched files: all checks passed. Real-artifact spot-check (same run as the
reachability test, real checked-in store): mockups 0 project / 10 example,
mock-data 0 project / 2 example (both example-only), flows 11 project / 3
example (mixed, correctly excluded from example_only_types). No `.sql`
files touched; no files under `collector/`; no cross-file research needed
beyond the pre-flight dependency check. Handing off to test-runner per the
ticket's own agent sequence.

### 2026-09-16 16:20 — test-runner (status: ok)
feedback-id: fb_2026-09-16_13869fb9
completion_manifest:
  test_suite_executed: true
  all_tests_passing: true
  failure_report_structured: true
Ran `python -m unittest unit_tests.product_truth.test_uxp_700d_4 -v`: 4/4
green (test_type_with_only_example_artifacts_is_reported_by_name,
test_per_type_project_and_example_counts_are_both_stated,
test_type_with_no_artifacts_at_all_is_reported_empty_not_example_only,
test_uxp_700d_4_reachable_from_entry_point). Also ran the full
`python -m unittest discover -s unit_tests/product_truth -t . -p
"test_*.py"` regression suite as a sanity check: 124 tests, 5 failures —
all 5 are in sibling in-progress tickets' own test files
(test_uxp_700a_3.py, test_uxp_700b_3.py, test_uxp_700d_3.py), whose
`python-coder`/`test-runner` frontmatter status is `needed`/`failed` on
their own tickets (confirmed by reading those ticket files), i.e. these are
pre-existing red baselines unrelated to this ticket's change, not
regressions caused by it. None of the 5 failures are in test_uxp_700d_4.py.
Notably, the UXP-700d-3 failure stdout itself shows this ticket's new
`type_population` / `example_only_types` fields present and correctly
populated on the real CLI's outcome contract, corroborating UXP-700d-4's
own reachability test. No files edited other than this ticket's sign-off
surfaces.

### 2026-09-16 15:16 — ac-validator (status: ok)
feedback-id: (submit-failed)
completion_manifest:
  all_acs_have_implementation_evidence: true
  all_acs_have_test_evidence: true
  store_alignment_check_clean: true
  cross_file_contract_verified: true
All 4 ACs covered by both implementation and test evidence; ticket checkboxes
and `ac_coverage: 4/4` frontmatter updated. `submit_feedback.py` rejected
`ac-validator` as an unregistered writer for `category=complete` (exit 1,
`ERROR: Phase 'ac-validator' is not an allowed writer for category
'complete'`), so per SKILL.md §2a this comment uses `feedback-id:
(submit-failed)` and proceeds rather than aborting.

AC-1 covered:
  implementation: docs/product-truth/scripts/product_truth_outcome.py —
    compute_type_population(flows, mocks, mockups) (returns per-type
    {"project": int, "example": int}), wired onto validate_product_truth.py's
    stdout outcome-contract line via `"type_population":
    compute_type_population(flows, mocks, mockups)` in
    docs/product-truth/scripts/validate_product_truth.py
  test: test_per_type_project_and_example_counts_are_both_stated
    (unit_tests/product_truth/test_uxp_700d_4.py:209) — asserts every
    artifact type carries both a "project" and "example" key and that they
    sum to that type's real total population; also cross-checks the project
    figure against product_ownership.is_example_artifact_id independently

AC-2 covered:
  implementation: docs/product-truth/scripts/product_truth_outcome.py —
    compute_example_only_types(type_population) (sorted names where
    project == 0 and example != 0)
  test: test_type_with_only_example_artifacts_is_reported_by_name
    (unit_tests/product_truth/test_uxp_700d_4.py:166) — asserts "mockups"
    (real store: 0 project / 10 example) is named in example_only_types

AC-3 covered:
  implementation: docs/product-truth/scripts/product_truth_outcome.py —
    compute_type_population's per-id split uses
    `not is_example_artifact_id(artifact_id)` for the project count and
    `is_example_artifact_id(artifact_id)` for the example count, imported
    verbatim from product_ownership.py (the UXP-700d-1 `expects_from`
    deliverable, confirmed present at
    docs/product-truth/scripts/product_ownership.py:77,
    `def is_example_artifact_id`)
  test: test_per_type_project_and_example_counts_are_both_stated
    (unit_tests/product_truth/test_uxp_700d_4.py:209, lines 239-248) —
    independently re-derives the expected project count from
    product_ownership.is_example_artifact_id and asserts it matches;
    test_type_with_only_example_artifacts_is_reported_by_name
    (unit_tests/product_truth/test_uxp_700d_4.py:166) also asserts "flows"
    (11 project / 3 example) is never reported example-only

AC-4 covered:
  implementation: docs/product-truth/scripts/product_truth_outcome.py —
    compute_example_only_types's condition `counts["project"] == 0 and
    counts["example"] != 0` deliberately excludes the both-zero case, which
    remains _compute_empty_types' vocabulary instead
  test: test_type_with_no_artifacts_at_all_is_reported_empty_not_example_only
    (unit_tests/product_truth/test_uxp_700d_4.py:262) — asserts a
    zero-population type yields example_only == [] and is disjoint from
    _compute_empty_types' output

Cross-file contract (`expects_from` UXP-700d-1): traced —
docs/product-truth/scripts/product_ownership.py:77 defines
`is_example_artifact_id`, and
docs/product-truth/scripts/product_truth_outcome.py imports and calls it
verbatim (`from product_ownership import is_example_artifact_id`, line 22;
used at lines within compute_type_population) — no contract gap.

Ran `python -m unittest unit_tests.product_truth.test_uxp_700d_4 -v`: 4/4
green (re-verified independently of test-runner's prior run). Ran
`python scripts/commit_guardian/check_v2_ac_store_alignment.py --ticket
tickets/00_inbox/epics/EPIC-TruthfulProjectRecord/36_TICKET-20260909-UXP-700d-4.md`:
exit 0, no store-alignment violations. No `## AC Coverage` table exists in
this ticket's body (pre-dates that table format), so §2c.3 was not
applicable; flipped the ticket's own `## Acceptance Criteria` AC-1..AC-4
checkboxes to `- [x]` and set `ac_coverage: 4/4` in frontmatter instead, per
this agent's own Step 4 procedure. Commit phase may proceed.

### 2026-09-16 15:22 — ac-fulfillment-gate (status: blocker)
feedback-id: (submit-failed)
completion_manifest:
  ac_yaml_loaded: true
  covered_by_autofixed:
    result: true
  work_status_verified:
    result: false
    reason: "AC YAML (docs/acceptance-criteria/ux-prototyping/UXP-700-truthful-project-record/UXP-700d-4.yaml, level L2) still has work_status: todo."
    remediation: "See below."
  implemented_by_verified:
    result: false
    reason: "implemented_by holds only a stale ticket-file path (tickets/00_inbox/TICKET-20260909-UXP-700d-4.md, not even this ticket's real path), with no code file, and no entry intersects files_touched (empty) or the branch diff."
    remediation: "See below."
Ran `python3 .leafcutter/scripts/ac_store/ac_coverage_resolver.py --ticket <this
ticket>`: resolved 1 AC (UXP-700d-4, via traceability_block, two-key id/path form),
level L2, initial failures [work_status, covered_by]. Auto-fixed `covered_by`
(Step 3c): grepped `tests/` and `unit_tests/` for `# covers: UXP-700d-4` and found
four genuine tags in `unit_tests/product_truth/test_uxp_700d_4.py` (lines 167, 210,
263, 302 — matching test-writer's own declared test file); appended that path to
`covered_by: []` -> `covered_by: [unit_tests/product_truth/test_uxp_700d_4.py]`
(append-only edit). Ran `scripts/commit_guardian/check_ac_schema.py` on the edited
YAML afterward — exit 0, schema valid. Re-ran the resolver: `covered_by` now passes;
`work_status` remains the sole resolver-flagged failure.

`work_status` and `implemented_by` could NOT be auto-fixed (Steps 3a/3b): both
criteria require `files_touched ∩ diff` to be non-empty, and this ticket's own
frontmatter declares `files_touched: []`. That intersection is empty against any
diff by construction (an empty set intersected with anything is empty, independent
of which diff command — `git diff main...HEAD` or the `git diff HEAD` fallback — is
authoritative), so there is no diff-based evidence this gate is permitted to act on
for either field. Per this gate's own Constraints ("No false positives... If
evidence cannot be determined, record None and emit a blocker — do not infer
fulfillment from descriptions or commit messages"), the rich verification evidence
documented in python-coder's, test-runner's, and ac-validator's comments above
(pointing at real, verified changes to `docs/product-truth/scripts/product_truth_outcome.py`
and `docs/product-truth/scripts/validate_product_truth.py`, both confirmed present in
`git status`/`git diff HEAD --name-only`) cannot substitute for diff evidence tied to
`files_touched`, however credible. This is a determined "zero evidence" outcome for
these two fields, not an ambiguous one, so it is reported as a blocker rather than a
stop-and-ask question.

AC store fulfillment incomplete:
UXP-700d-4: work_status is `todo`, not `done`, and implemented_by holds only a
stale/unrelated ticket-file path with no diff-intersecting entry — Step 3a/3b's
auto-fix precondition (files_touched ∩ diff non-empty) cannot be satisfied because
files_touched is `[]`.
Auto-fix was not possible: no file is declared touched by this ticket for the gate
to correlate against the branch diff.
Suggested remediation: either (a) have a maintainer or the commit phase manually set
`work_status: done` and append the real implementing files
(`docs/product-truth/scripts/product_truth_outcome.py`,
`docs/product-truth/scripts/validate_product_truth.py`) to `implemented_by` in
`docs/acceptance-criteria/ux-prototyping/UXP-700-truthful-project-record/UXP-700d-4.yaml`
once this ticket's verified closure (documented by python-coder/test-runner/
ac-validator above) is accepted as sufficient grounds, or (b) populate this
ticket's `files_touched` frontmatter with those same two files so a future run of
this gate can auto-fix `work_status` and `implemented_by` from real diff evidence.
`submit_feedback.py` rejected `ac-fulfillment-gate` as an unregistered writer for
`category=blocker` (exit 1), so per SKILL.md §2a this comment uses
`feedback-id: (submit-failed)` and proceeds rather than aborting. No edits were made
to any file other than this ticket's own sign-off surfaces and
`UXP-700d-4.yaml`'s `covered_by` field (append-only, schema-validated).

### 2026-09-16 15:26 — ac-fulfillment-gate (status: question)
feedback-id: fb_2026-09-16_fc36fe78
completion_manifest:
  ac_yaml_loaded: true
  covered_by_verified:
    result: true
  work_status_verified:
    result: false
    reason: "AC YAML still has work_status: todo (unchanged since the prior run)."
    remediation: "See below — ambiguity requires a maintainer decision, not a mechanical fix."
  implemented_by_verified:
    result: false
    reason: "implemented_by still holds only a stale ticket-file path, with no diff-intersecting code file."
    remediation: "See below — ambiguity requires a maintainer decision, not a mechanical fix."
Re-ran the resolver: 1 AC resolved (UXP-700d-4, L2, via traceability_block). `covered_by`
remains correctly populated from the prior run's auto-fix
(`unit_tests/product_truth/test_uxp_700d_4.py`) — re-verified, still schema-valid, no
further action needed there. `work_status` (`todo`) and `implemented_by` (stale ticket-path
only) are unchanged from the previous gate run: this ticket's own `files_touched`
frontmatter is still `[]`.

Re-reading this gate's own Stop-and-Ask Rule against that fact: "The `files_touched`
frontmatter key is missing or empty and auto-fix evidence cannot be determined" is listed
as its own named trigger for `(status: question)`, separate from and prior to the Step 5
blocker path. `files_touched: []` is literally true here, and it is what blocks this gate
from computing `files_touched ∩ diff` at all — the real branch diff independently confirms
`docs/product-truth/scripts/product_truth_outcome.py` and
`docs/product-truth/scripts/validate_product_truth.py` were genuinely touched (matching
python-coder's, test-runner's, and ac-validator's own comments above), but this gate has no
declared-files anchor to correlate that diff against, so it cannot mechanically determine
which diff entries are "this ticket's". The prior gate run reached the same facts but
classified this as a determined-blocker rather than applying this named rule; on review,
the rule's own text matches this exact situation, so this run corrects that classification
to `question` rather than repeating the blocker verdict. Frontmatter reset from `failed` to
`needed` accordingly (a `question` halt is not a terminal failure of this gate's own work —
it is unresolved pending clarification), matching the convention already used on
`tickets/00_inbox/epics/EPIC-TruthfulProjectRecord/12_TICKET-20260909-UXP-700b-1-i.md`'s own
prior `ac-fulfillment-gate (status: question)` entry.

The question for the user/maintainer: (a) should `docs/product-truth/scripts/product_truth_outcome.py`
and `docs/product-truth/scripts/validate_product_truth.py` be added to this ticket's
`files_touched` frontmatter now, so a re-run of this gate can auto-fix `work_status` and
`implemented_by` in `UXP-700d-4.yaml` from real diff evidence per Steps 3a/3b — or (b)
should a maintainer directly set `work_status: done` and populate `implemented_by` with
those two files in the AC YAML by hand, treating python-coder's/test-runner's/ac-validator's
already-verified closure as sufficient grounds without going through this gate's diff-based
auto-fix path? Either answer unblocks the commit phase; this gate cannot choose between them
on its own authority per its "no false positives / do not infer fulfillment from descriptions"
constraint.

### 2026-09-16 15:29 — commit (status: question)
feedback-id: fb_2026-09-16_c410cab5
completion_manifest:
  precommit_probe_checked:
    result: false
    reason: "Not reached — halted before Step 0a because a required upstream gate has an unresolved question."
    remediation: "Re-dispatch commit after the ac-fulfillment-gate question below is answered by the user/maintainer."
  staged_changes_committed:
    result: false
    reason: "No commit attempted. This ticket's own `## Sign-offs` checklist shows ac-fulfillment-gate still unchecked, and its latest comment (2026-09-16 15:26, status: question) is an unresolved, unanswered question directed at the user/maintainer — that status tag halts the ticket per the signoff skill's own Status-tags table (§3): \"question — Phase needs user clarification. Halts the ticket; surfaces the question to the user.\""
    remediation: "Answer ac-fulfillment-gate's two-option question in its 2026-09-16 15:26 comment (add the two implementing files to this ticket's `files_touched` frontmatter so the gate can auto-fix, or hand-set `work_status`/`implemented_by` in UXP-700d-4.yaml directly), then re-run ac-fulfillment-gate, and only then re-dispatch commit."
I did not run `git commit` and made no edits to any file other than this ticket's own
sign-off surfaces. This agent's own instructions require the `--no-verify` bypass policy
and hook-respecting operation, but the blocking condition here is upstream of hooks
entirely: `ac-fulfillment-gate`'s frontmatter row is still `needed`, its `## Sign-offs`
checkbox is still `- [ ] ac-fulfillment-gate`, and its own most recent comment is an
explicit, still-open `(status: question)` asking the user/maintainer to choose between two
remediation paths — neither of which this commit agent has the authority to choose on its
own (the same "no false positives / do not infer fulfillment" constraint ac-fulfillment-gate
cited applies equally to me deciding on its behalf). Committing past an unanswered upstream
question would silently paper over that halt. I am deliberately leaving `agents.commit` as
`needed` and the `## Sign-offs` checkbox unchecked (rather than flipping to `failed`) because
this is not a failure of the commit phase's own work — it is a pending clarification, exactly
matching the `question` comment-status semantics in the signoff skill's §3 table. Separately,
note the currently staged tree is large and spans several sibling tickets in this epic (e.g.
`07_TICKET-...UXP-700a-3.md`, `16_TICKET-...UXP-700b-3.md`, `21_TICKET-...UXP-700c-2.md`, and
others) in addition to this ticket's own two implementation files — once the question above is
answered, the mixed-set check in this agent's own Step 2a should be run before drafting any
commit subject.

### 2026-09-16 18:40 — python-coder (status: ok)

feedback-id: fb_2026-09-16_56ffef62

Finished by hand on 2026-09-16 from run wf_3d2879c1-2ae at the user's direction
(the /build-feature run was halted; the user asked for the remaining work on
this ticket to be completed manually). Applied `docs/product-truth/scripts/
product_truth_outcome.py`'s `compute_type_population`/`compute_example_only_types`
additions from the run's `unstaged.patch` (clean -- that file was not part of
the mixed `validate_product_truth.py`/`UXP-700c-2` hunk). `validate_product_truth.py`
itself was NOT re-applied from `staged.patch` wholesale (its diff there mixed
this ticket's own `type_population`/`example_only_types` wiring with unrelated
`UXP-700c-2`/`UXP-700c-2-ii` freshness code -- `_check_freshness`,
`_sync_behind_marks`, `confirmed`/`behind` state). Instead, hand-extracted and
applied only the three hunks this ticket's own tests require: the import of
`compute_example_only_types`/`compute_type_population`, `run_checks()`'s
`"type_population"` entry, and `main()`'s `example_only_types` derivation +
extended `contract` tuple, plus this ticket's own DECISION HISTORY entry.
Confirmed by grep afterward that no `freshness`/`_sync_behind_marks`/`behind`/
`confirmed_against` content is present in `validate_product_truth.py`; `docs/
product-truth/schemas/flow.schema.json` was correctly left untouched (this
ticket needs no schema change).

Resolved `ac-fulfillment-gate`'s open question by the user's direct decision:
set this ticket's `files_touched` to the files actually changed for it
(`product_truth_outcome.py`, `validate_product_truth.py`,
`test_uxp_700d_4.py`) and recorded `implemented_by`
(the ticket path + the two real implementation files, replacing a stale,
non-existent ticket path) and `covered_by` (already auto-fixed by
`ac-fulfillment-gate`'s prior run) directly on
`docs/acceptance-criteria/ux-prototyping/UXP-700-truthful-project-record/UXP-700d-4.yaml`.
`work_status` is left `todo` for `.leafcutter/scripts/ac_store/mark_ac_done.py`
to flip (its own coverage gate re-verifies `covered_by`'s tests actually pass
before writing `done`).

Verified: `AC_ENFORCE_STRICT=1 python -m pytest unit_tests/product_truth/test_uxp_700d_4.py -v`
-- 4/4 green (including the real-CLI-subprocess reachability test against the
real, checked-in store: `mockups` 0 project / 10 example, `mock-data` 0
project / 2 example, both example-only; `flows` 11 project / 3 example, never
example-only). Full `unit_tests/product_truth` suite: 108 passed, 0 failures
-- a strict subset of a disposable pristine `origin/main` worktree run (100
passed, 0 failures). Mutation: changed `compute_example_only_types` to
`return []` -- 2 of 4 tests in this file went red (the AC-2 criterion test and
the reachability test), confirming the criterion is exercised; reverted via
`git checkout --`. `ruff check` on both touched production files: all checks
passed. File-size ratchet (`_file_size_ratchet.py::count_content_lines`
against `git show HEAD:<path>`): `product_truth_outcome.py` 190 -> 215 lines
(+25), `validate_product_truth.py` 380 -> 385 lines (+5) -- both remain well
under the 400-line limit, neither was already over it.
