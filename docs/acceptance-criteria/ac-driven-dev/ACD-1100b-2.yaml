id: ACD-1100b-2
readiness: approved
priority: high
title: "Agent registry entries use canonical names without version suffixes"
component: ac-driven-dev
level: L2
status: active
req_status: approved
work_status: todo
criteria: |
  Given the agent registry previously contained entries with ids
    "product-owner-v3", "business-analyst-v3", and "it-po-v3",
  When the v2.0 migration promotes v3 agents,
  Then config/agent_registry.json contains entries with ids:
    "product-owner" (replacing product-owner-v3)
    "business-analyst" (replacing business-analyst-v3, distinct from the
      removed v1 business-analyst)
    "it-po" (replacing it-po-v3, distinct from the removed v1/v2 it-po)
  And each promoted entry's template_path field references the canonical
    filename (e.g. "templates/agents/product-owner.md")
  And no entry in the entire registry has an "id" field containing the
    substring "-v1", "-v2", or "-v3".
depends_on:
  - ACD-1100b
  - ACD-1100a-2
doc_links:
  - path: docs/reference/agent-template-frontmatter.md
    relationship: describes
    status: exists
  - path: docs/architecture/agent_knowledge_system.md
    relationship: describes
    status: exists
assigned_agent: python-coder
estimated_complexity: S
it_requirements:
  - "Registry file must remain valid JSON after edits"
  - "Must update both the 'id' field and the 'template_path' field for each promoted entry"
  - "Must preserve all other fields in each promoted entry (model, skills_used, spawn_allowlist, etc.) exactly as-is"
  - "Must not create duplicate entries — the old v3 entry is replaced in-place, not duplicated"
delivers_to:
  agent: python-coder
  contract: "config/agent_registry.json with three entries at canonical ids (product-owner, business-analyst, it-po), each with template_path pointing to the canonical .md filename; no -v3 suffix in any id"
expects_from: null
origin_agent: BrainCandy
created: 2026-06-05
amended_by: []
superseded_by: null
covered_by: []
implemented_by: []
change_target: config
risk_surface: contract_boundary
