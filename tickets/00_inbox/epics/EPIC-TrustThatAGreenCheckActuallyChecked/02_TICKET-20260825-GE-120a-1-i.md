---
ac_traceability:
  id: GE-120a-1-i
  path: docs/acceptance-criteria/guardrail-engine/GE-120-green-means-checked/GE-120a-1-i.yaml
advances_current_outcome: true
agents:
  ac-fulfillment-gate: needed
  ac-validator: needed
  architect-review: needed
  commit: needed
  documentation-expert: needed
  documentation-verifier: needed
  pr-reviewer: needed
  pull-request: needed
  python-coder: needed
  test-runner: needed
  test-writer: needed
change_target: code
complexity: low
components:
- commit_guardian
- precommit_hooks
created: '2026-08-25'
depends_on: []
documentation_required: true
files_touched:
- scripts/build.py
- scripts/commit_guardian/hooks
- templates/agents
- templates/scripts/commit_guardian/hooks
- templates/scripts/commit_guardian/hooks/check_agent_verification_consistency.py
priority: high
requires_adr: false
requires_diagram: false
risk_surface: contract_boundary
roadmap_phase: phase_1
source_ac: GE-120a-1-i
status: todo
title: One unparseable input, while the check still ran, remains an ordinary pass
---

# One unparseable input, while the check still ran, remains an ordinary pass

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `GE-120a-1-i` — One unparseable input, while the check still ran, remains an ordinary pass — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `GE-120a-1-i`. Component: `guardrail-engine`. Assigned agent: `python-coder`. Estimated complexity: `S`. Complexity: `low`.

## Acceptance Criteria

```gherkin
Given 5 staged agent definition files, of which exactly 1 cannot be parsed,
And the check that inspects agent definitions can reach every prerequisite it needs,
When the check runs,
Then it inspects the 4 parseable definitions and reports its findings for them,
And it warns about the 1 unparseable definition, naming that file,
And it does not block the commit on account of the unparseable definition,
And its overall result is the ordinary success value, NOT the could-not-check value
  defined by GE-120a-1 — because the check did run,
And the behaviour approved in GE-116a-1-iii ("Unparseable agent definition fails open
  and does not block the commit") is therefore observably unchanged by this feature.

Coverage note: this must be exercised by running the check against a real staged file
set containing a genuinely unparseable definition, not by asserting that a fail-open
branch exists in the source.
```

- [ ] AC-1: the check that inspects agent definitions can reach every prerequisite it needs
- [ ] AC-2: it inspects the 4 parseable definitions and reports its findings for them
- [ ] AC-3: it warns about the 1 unparseable definition, naming that file
- [ ] AC-4: it does not block the commit on account of the unparseable definition
- [ ] AC-5: its overall result is the ordinary success value, NOT the could-not-check value
- [ ] AC-6: the behaviour approved in GE-116a-1-iii ("Unparseable agent definition fails open
- [ ] AC-7: does not block the commit") is therefore observably unchanged by this feature.

## Test Requirements

```yaml
tests:
- name: test_ge120a1i_four_parseable_definitions_inspected_and_reported
  file: unit_tests/commit_guardian/test_ge_120a_1_i.py
  covers:
  - GE-120a-1-i
  asserts: 'Five staged agent definitions with exactly one unparseable: findings are
    reported for the four parseable ones.'
  framework: unittest
  type: behavioral
- name: test_ge120a1i_unparseable_definition_warned_by_name_not_blocking
  file: unit_tests/commit_guardian/test_ge_120a_1_i.py
  covers:
  - GE-120a-1-i
  asserts: The unparseable definition is warned about by filename and does not block
    the commit.
  framework: unittest
  type: behavioral
- name: test_ge120a1i_overall_result_is_ordinary_success_not_could_not_check
  file: unit_tests/commit_guardian/test_ge_120a_1_i.py
  covers:
  - GE-120a-1-i
  asserts: The run's result value is the ordinary success value from the GE-120a-1
    vocabulary, not the could-not-check value — the contradiction guard for the whole
    tree.
  framework: unittest
  type: behavioral
- name: test_ge_120a_1_i_reachable_from_entry_point
  file: unit_tests/commit_guardian/test_ge_120a_1_i.py
  covers:
  - GE-120a-1-i
  angle: reachability
  asserts: 'REQUIRED — invoke the production entry point (CLI, hook, slash command,
    workflow dispatch, or main()) as a subprocess/dispatch and assert the new behaviour
    actually occurs. Do NOT satisfy this by importing the function directly. The AC
    authored no test_spec, so the entry point is not declared: resolve it before writing
    this test, and do not delete this entry.'
```

## Implementation Notes

```yaml
- 'Target identified: templates/scripts/commit_guardian/hooks/check_agent_verification_consistency.py
  — manifest id check-agent-verification-consistency, whose files pattern scopes it
  to staged agent definitions under templates/agents/. That is the guard GE-116a-1-iii
  names as the verification-consistency guard.'
- 'PREREQUISITE STATE — verify before implementing. GE-116a-1-iii carries readiness:
  approved but work_status: todo and implemented_by: [], so the fail-open-on-unparseable
  behaviour it specifies may not exist in the guard yet. If it is absent, this AC''s
  deliverable is the executing regression guard plus the minimum work that makes GE-116a-1-iii''s
  behaviour observable — NOT a re-specification of GE-116a-1-iii and NOT an expansion
  of its scope.'
- This AC must not force a production change. If executing the criteria already yields
  the ordinary success value, the deliverable is the executing regression test plus
  a recorded observation. Do not manufacture a change in order to have something to
  commit.
- The assertion is on the RESULT VALUE from GE-120a-1's shared outcome vocabulary
  (ordinary success, distinct from could-not-check), not on the exit code. A test
  asserting only exit 0 cannot tell the two states apart and does not satisfy the
  criterion.
- 'PRECEDENCE: GE-116a-1-iii carries readiness: approved and outranks every draft
  criterion in this tree. If a GE-120a-1 or GE-120b-2 change makes this AC fail, narrow
  that change; do not weaken this one.'
- 'ADR-001 deployed/template parity: any edit lands in templates/scripts/commit_guardian/hooks/
  (canonical) followed by ''python scripts/build.py --target-dir .''; scripts/commit_guardian/hooks/
  and .leafcutter/scripts/commit_guardian/hooks/ must match afterwards (enforced by
  check_hook_parity).'
```

## Agent Contracts

### documentation-expert

Existing docs to update / cross-link:

- docs/acceptance-criteria/guardrail-engine/GE-116-consistent-agent-config (relationship: context | status: exists | relevance: GE-116a-1-iii is the approved AC this criterion protects from regression.)
- docs/architecture/components/commit-guardian.md (relationship: describes | status: exists)
- templates/scripts/commit_guardian/hooks/check_agent_verification_consistency.py (relationship: related | status: exists | relevance: IT-PO verified: this is the verification-consistency guard GE-116a-1-iii constrains (manifest id check-agent-verification-consistency, scoped to staged templates/agents markdown).)

- [ ] AC-1: (unspecified genre) | docs/acceptance-criteria/guardrail-engine/GE-116-consistent-agent-config | it inspects the 4 parseable definitions and reports its findings for them

### Expects From

- **AC:** GE-120a-1
- **Contract:** The distinction between the ordinary success value and the could-not-check value.

## Sign-offs

- [ ] architect-review
- [ ] test-writer
- [ ] python-coder
- [ ] test-runner
- [ ] documentation-expert
- [ ] pr-reviewer
- [ ] ac-validator
- [ ] ac-fulfillment-gate
- [ ] documentation-verifier
- [ ] commit
- [ ] pull-request

## Comments
