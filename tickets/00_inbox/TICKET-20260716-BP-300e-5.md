---
ac_traceability:
  id: BP-300e-5
  path: docs/acceptance-criteria/build_pipeline/BP-300-workflow-orchestration/BP-300e-5.yaml
advances_current_outcome: true
agents:
  architect-review: signed_off
  commit: needed
  pr-reviewer: needed
  pull-request: needed
  python-coder: needed
  test-runner: needed
  test-writer: needed
change_target: pipeline
complexity: high
components:
- build_pipeline
created: '2026-07-16'
depends_on: []
files_touched:
- templates/workflows-js/plan-feature.js
- templates/workflows-js/build-feature.js
- templates/workflows-js/finalize-feature.js
- templates/workflows-js/build-epic.js
- templates/workflows-js/build-ticket.js
- unit_tests/test_bp_300e_parse_agent_json.py
priority: high
requires_adr: false
requires_diagram: false
risk_surface: contract_boundary
roadmap_phase: phase_1
source_ac: BP-300e-5
status: todo
title: Prose-tolerant reply reading is applied uniformly across every delivery workflow
  script
---

# Prose-tolerant reply reading is applied uniformly across every delivery workflow script

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BP-300e-5` — Prose-tolerant reply reading is applied uniformly across every delivery workflow script — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BP-300e-5`. Component: `build-pipeline`. Assigned agent: `python-coder`. Estimated complexity: `L`. Complexity: `high`.

## Acceptance Criteria

```gherkin
Given the delivery commands are backed by multiple workflow scripts
  (/plan-feature, /build-feature, /finalize-feature, and the epic and ticket
  workflows behind them),
And each script reads structured results from the agents it dispatches at every
  such reply-reading point,
When any of these scripts reads an agent reply that wraps a valid JSON object or
  array in surrounding prose,
Then every reply-reading point in every one of these scripts tolerates the extra
  wording identically — a /build-feature or /finalize-feature run is exactly as
  resilient as a /plan-feature run,
And no reply-reading point retains the brittle behavior of stopping on prose around
  otherwise-valid JSON.
```

## Test Requirements

```yaml
tests:
- name: test_object_with_trailing_prose_parsed
  file: unit_tests/test_bp_300e_parse_agent_json.py
  covers:
  - BP-300e-1
  asserts: parseAgentJson recovers a valid JSON object immediately followed by a trailing
    markdown section (e.g. "## Anomalies"); all fields intact, trailing prose ignored.
  framework: unittest
  type: unit
- name: test_nested_braces_not_truncated
  file: unit_tests/test_bp_300e_parse_agent_json.py
  covers:
  - BP-300e-1-i
  asserts: Extraction stops at the matching top-level closing brace, not the first
    nested one; nested objects recovered whole.
  framework: unittest
  type: unit
- name: test_braces_and_hash_inside_strings_ignored
  file: unit_tests/test_bp_300e_parse_agent_json.py
  covers:
  - BP-300e-1-ii
  asserts: '{ } and # characters inside JSON string values (and escaped quotes) are
    treated as text, not structure/delimiters; object recovered with string intact.'
  framework: unittest
  type: unit
- name: test_already_parsed_value_passthrough
  file: unit_tests/test_bp_300e_parse_agent_json.py
  covers:
  - BP-300e-1-iii
  asserts: A non-string (already-parsed) value is returned unchanged with no re-parse
    attempt.
  framework: unittest
  type: unit
- name: test_surrounding_prose_parsed
  file: unit_tests/test_bp_300e_parse_agent_json.py
  covers:
  - BP-300e-2
  asserts: parseAgentJson recovers a valid JSON object wrapped in prose both before
    the opening brace and after the closing brace.
  framework: unittest
  type: unit
- name: test_multiple_blocks_first_balanced_taken
  file: unit_tests/test_bp_300e_parse_agent_json.py
  covers:
  - BP-300e-2-i
  asserts: When a reply contains more than one top-level JSON block, the FIRST complete
    balanced value is parsed and later blocks ignored (deterministic).
  framework: unittest
  type: unit
- name: test_json_array_with_prose_parsed
  file: unit_tests/test_bp_300e_parse_agent_json.py
  covers:
  - BP-300e-3
  asserts: A valid JSON array surrounded by prose is recovered with every element intact.
  framework: unittest
  type: unit
- name: test_unusable_reply_raises_typed_error_naming_stage_and_agent
  file: unit_tests/test_bp_300e_parse_agent_json.py
  covers:
  - BP-300e-4
  asserts: A prose-only reply with no parseable JSON raises a typed error naming the
    stage and agent; never silently skipped or swallowed.
  framework: unittest
  type: unit
- name: test_empty_or_whitespace_reply_raises_same_typed_error
  file: unit_tests/test_bp_300e_parse_agent_json.py
  covers:
  - BP-300e-4-i
  asserts: An empty or whitespace-only reply raises the same typed stage+agent error,
    never treated as an acceptable/skippable result.
  framework: unittest
  type: unit
- name: test_all_agent_parse_sites_route_through_tolerant_reader
  file: unit_tests/test_bp_300e_parse_agent_json.py
  covers:
  - BP-300e-5
  asserts: 'Structural audit: every enumerated agent-reply parse site across the five
    workflow scripts routes through parseAgentJson; no bare JSON.parse or strict ternary
    on agent output remains (count-agnostic; 0-site scripts trivially compliant).'
  framework: unittest
  type: unit
- name: test_tolerant_reader_body_identical_across_workflow_scripts
  file: unit_tests/test_bp_300e_parse_agent_json.py
  covers:
  - BP-300e-5
  asserts: 'Parity: the inlined parseAgentJson body is byte-identical across every
    workflow script that contains it (guards inline placement against drift).'
  framework: unittest
  type: unit
- name: test_build_feature_run_tolerates_prose_wrapped_reply
  file: unit_tests/test_bp_300e_parse_agent_json.py
  covers:
  - BP-300e-5
  asserts: Replay a prose-wrapped reply through build-feature; the run continues to
    its next stage.
  framework: unittest
  type: integration
- name: test_finalize_feature_run_tolerates_prose_wrapped_reply
  file: unit_tests/test_bp_300e_parse_agent_json.py
  covers:
  - BP-300e-5
  asserts: Replay a prose-wrapped reply through finalize-feature; the run continues
    to its next stage.
  framework: unittest
  type: integration
```

## Implementation Notes

```yaml
- 'Every agent-reply parse site across plan-feature.js, build-feature.js, finalize-feature.js,
  build-epic.js and build-ticket.js must route through the single tolerant reader;
  no residual inline `typeof x === ''string'' ? JSON.parse(x) : x` ternary or bare
  JSON.parse of agent output may remain.'
- Coverage must be asserted by a structural audit test that enumerates agent-reply
  parse sites (not a hard-coded count); the authoring-time surface is 26 JSON.parse
  sites in plan-feature.js, 5 in finalize-feature.js, 1 each in build-epic.js and
  build-ticket.js, and 0 in build-feature.js (it dispatches via schema-enforced agent()
  calls, so it is trivially compliant).
- 'HELPER PLACEMENT (open design decision — recommendation, needs ratification at
  the gate): E2 workflow scripts are self-contained — the engine forbids require()/import
  in the script body — so the tolerant reader cannot be a shared import. Choose either
  (a) inline a byte-identical parseAgentJson into each workflow script, guarded by
  a parity test asserting the helper body is identical across all scripts, or (b)
  have build.py inject a shared prelude at deploy time. RECOMMEND (a): the test harness
  (unit_tests/_plan_feature_e2_runner.py / _workflow_engine_harness.py) executes the
  TEMPLATE source directly via vm.Script, so a deploy-time-only prelude (b) would
  leave templates/workflows-js/*.js non-runnable and break the harness; inlining keeps
  each script independently runnable and neutralizes drift via the parity test, matching
  the repo''s established source<->template parity pattern (BP-1000).'
- Edit only the canonical source templates/workflows-js/*.js; rebuild via build.py
  so every deployed .claude/workflows/*.js copy stays byte-identical (BP-100k drift
  guard).
- Resilience must be verified by per-script runtime behavioral tests (replay a prose-wrapped
  reply through build-feature and finalize-feature and assert each run continues),
  not a source string-scan.
```

## Sign-offs

- [x] architect-review — 2026-07-17 08:55
- [ ] test-writer
- [ ] python-coder
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit
- [ ] pull-request

## Comments

### 2026-07-17 08:55 — architect-review (status: ok)
feedback-id: fb_2026-07-17_db8aa274
completion_manifest:
  blast_radius_analysed: true
  impact_classified: true
  requires_adr_confirmed: true
  sign_off_written: true
Classified as small: 6 files (5 source in templates/workflows-js/ + 1 test file) within the single build_pipeline component; no always-large trigger fired; no cross-module boundary; mechanical file-count threshold of 6 > 5 is exceeded only by the test file, so architectural substance warrants a small ruling on judgment. Design note: the implementation notes have already settled the inline-helper approach (option a) over deploy-time prelude; the coder must ensure the parseAgentJson body is stable before authoring the parity test, since byte-identical matching is sensitive to whitespace drift. No ADR needed (single component, settled design decision documented in implementation notes). No diagrams needed (pure refactor within one component, no new boundary).
