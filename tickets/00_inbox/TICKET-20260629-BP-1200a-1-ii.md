---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  pr-reviewer: needed
  pull-request: needed
  python-coder: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
components:
- build_pipeline
created: '2026-06-29'
depends_on:
- BP-1200a-1
files_touched:
- scripts/build.py
- scripts/build_helpers.py
- scripts/build_phases.py
- unit_tests/test_build_guard_real_package.py
- docs/architecture/adrs/ADR-016-ci-fresh-clone-test-dependencies.md
priority: high
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: BP-1200a-1-ii
status: todo
title: No test fails at collection time because a build-generated dependency is missing
  from the fresh clone
---

# No test fails at collection time because a build-generated dependency is missing from the fresh clone

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BP-1200a-1-ii` — No test fails at collection time because a build-generated dependency is missing from the fresh clone — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BP-1200a-1-ii`. Component: `build-pipeline`. Assigned agent: `python-coder`. Estimated complexity: `S`.

## Acceptance Criteria

```gherkin
Given a brand-new checkout in which the locally-generated build outputs are not
  present (the kind of generated tooling directories that a plain clone omits),
When the documented CI test command is followed through to running the full
  suite,
Then no test is reported as an error during collection or startup due to a
  module or file it imports or opens being absent
And every test that previously failed only because such a generated dependency
  was missing now runs and reports a pass
And the count of tests that error before they can execute is zero.
```

## Sign-offs

- [ ] python-coder
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit
- [ ] pull-request

## Comments
