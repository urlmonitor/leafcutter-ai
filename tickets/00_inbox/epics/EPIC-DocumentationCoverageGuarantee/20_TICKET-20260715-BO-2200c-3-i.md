---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  pr-reviewer: needed
  pull-request: needed
  python-coder: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
complexity: low
components:
- build_orchestration
created: '2026-07-15'
depends_on:
- BO-2200c-3
files_touched:
- scripts/ac_store/generate_ticket_from_ac.py
priority: medium
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: BO-2200c-3-i
status: todo
title: An unresolved or absent parent L1 yields a genre-less contract line, not a
  crash
---

# An unresolved or absent parent L1 yields a genre-less contract line, not a crash

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BO-2200c-3-i` — An unresolved or absent parent L1 yields a genre-less contract line, not a crash — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BO-2200c-3-i`. Component: `build-orchestration`. Assigned agent: `python-coder`. Estimated complexity: `S`. Complexity: `low`.

## Acceptance Criteria

```gherkin
Given the generator is sourcing the Diataxis genre for a leaf AC's contract line,
When the leaf AC's parent L1 cannot be resolved -- the parent id derivation returns
  nothing, the parent file is absent, or the parent carries no `documentation_triggers` --
Then the generator emits the contract line WITHOUT a genre (the path and content
  constraint are still written), rather than raising an exception or aborting the ticket.
And the missing genre is represented explicitly (for example an "(unspecified genre)"
  marker) so the omission is visible to the writer rather than silently blank.
```

## Test Requirements

```yaml
tests:
- name: test_unresolved_parent_yields_genre_less_line_no_crash
  file: unit_tests/test_bo_2200c_3_i.py
  covers:
  - BO-2200c-3-i
  asserts: When the parent L1 cannot be resolved, the generator emits a contract line
    without a genre (path + constraint retained) and does not raise.
  framework: unittest
  type: behavioral
- name: test_missing_genre_marked_explicitly
  file: unit_tests/test_bo_2200c_3_i.py
  covers:
  - BO-2200c-3-i
  asserts: The genre-less line carries an explicit marker (e.g. '(unspecified genre)')
    rather than a silently blank genre.
  framework: unittest
  type: behavioral
```

## Implementation Notes

```yaml
config_schema_fragment:
  parent_resolution_failsoft:
    description: Unresolved parent (derive_parent_id None, missing file, or absent
      documentation_triggers) yields a genre-less line with an explicit marker, not
      a crash.
    type: object
constraints:
- 'Parent resolution must fail soft: derive_parent_id returning None, a missing parent
  file, or an absent documentation_triggers must produce a genre-less line (path and
  content constraint still written), not a crash.'
- Represent the missing genre explicitly (e.g. an '(unspecified genre)' marker) so
  the omission is visible.
- Log at WARNING per the error-handling policy; never swallow silently.
n_location_rule: '1'
post_write_commands: []
reference_file_path: scripts/ac_store/generate_ticket_from_ac.py
required_skills:
- python-coder
```

## Sign-offs

- [ ] python-coder
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit
- [ ] pull-request

## Comments
