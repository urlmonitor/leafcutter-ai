---
advances_current_outcome: true
agents:
  commit: signed_off
  documentation-expert: not_needed
  pr-reviewer: signed_off
  pull-request: needed
  python-coder: signed_off
  sql-coder: not_needed
  test-runner: needed
  test-writer: signed_off
change_target: code
complexity: low
components:
- build_orchestration
created: '2026-07-15'
depends_on:
- 03_TICKET-20260715-BO-2200a-3.md
files_touched:
- scripts/ac_store/generate_ticket_from_ac.py
priority: medium
requires_adr: false
requires_diagram: false
risk_surface: internal
roadmap_phase: phase_1
source_ac: BO-2200a-3-i
status: todo
title: A cost risk surface and an unclassified AC do not demand documentation
---

# A cost risk surface and an unclassified AC do not demand documentation

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BO-2200a-3-i` — A cost risk surface and an unclassified AC do not demand documentation — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BO-2200a-3-i`. Component: `build-orchestration`. Assigned agent: `python-coder`. Estimated complexity: `S`. Complexity: `low`.

## Acceptance Criteria

```gherkin
Given the declarative `documentation_gates` policy and the ticket generator building
  the agents map for a leaf AC,
When the AC's `change_target` is `code` and its `risk_surface` is `cost`,
Then the generated agents map does NOT include `documentation-expert`
  (the `cost` risk surface is explicitly excluded from the triggering set).
And when the AC carries neither a `change_target` nor a `risk_surface` field at all
  (an AC that predates the computed-gates classification), the generator completes
  without error and does NOT force `documentation-expert` from the documentation
  policy on the basis of absent classification.
```

## Test Requirements

```yaml
tests:
- name: test_risk_surface_cost_does_not_trigger_doc_expert
  file: unit_tests/test_bo_2200a_3_i.py
  covers:
  - BO-2200a-3-i
  asserts: change_target=code, risk_surface=cost yields no documentation-expert.
  framework: unittest
  type: behavioral
- name: test_unclassified_ac_completes_without_forcing_doc_expert
  file: unit_tests/test_bo_2200a_3_i.py
  covers:
  - BO-2200a-3-i
  asserts: An AC with neither change_target nor risk_surface generates without error
    and does not force documentation-expert from the doc policy.
  framework: unittest
  type: behavioral
```

## Implementation Notes

```yaml
config_schema_fragment:
  edge_negatives:
    description: risk_surface=cost is excluded from the triggering set; a missing/None
      change_target and risk_surface must not raise or force a documentation demand.
    type: object
constraints:
- 'risk_surface: cost must be excluded from the triggering set.'
- A missing/None classification must not raise KeyError and must not default to a
  documentation demand — fail safe toward no-demand when there is nothing to classify.
n_location_rule: '1'
post_write_commands: []
reference_file_path: scripts/ac_store/generate_ticket_from_ac.py
required_skills:
- python-coder
```

## Sign-offs

- [x] python-coder — 2026-07-20 15:30
- [x] test-writer — 2026-07-20 14:35
- [ ] test-runner
- [x] pr-reviewer — 2026-07-20 18:53
- [x] commit — 2026-07-20 18:59
- [ ] pull-request

## Comments

### 2026-07-20 15:30 — python-coder (status: ok)
feedback-id: fb_2026-07-20_796a565e
completion_manifest:
  config_entry_added: true
  red_baseline_tests_green: true
  python_code_unchanged: true
Config-only change: added `{change_target: code, risk_surface: cost}` to `documentation_gates.non_triggering_classifications` in `config/guardrail_gates.yaml`. No Python generator code required — `_build_agents_map` already reads and applies `non_triggering_classifications` (implemented in BO-2200a-3). Both red_baseline tests now green: `test_risk_surface_cost_does_not_trigger_doc_expert` (was RED — config entry absent) and `test_unclassified_ac_completes_without_forcing_doc_expert` (was passing; regression guard confirmed still green).
red_baseline_results:
  - test_name: test_risk_surface_cost_does_not_trigger_doc_expert
    result: green
  - test_name: test_unclassified_ac_completes_without_forcing_doc_expert
    result: green

### 2026-07-20 14:35 — test-writer (status: ok)
feedback-id: fb_2026-07-20_6dec0dd6
completion_manifest:
  tests_written: true
  tests_red: true
  ruff_clean: true
Wrote `unit_tests/test_bo_2200a_3_i.py` with two unittest tests. `test_risk_surface_cost_does_not_trigger_doc_expert` is RED (exit 1) — it asserts that `{change_target: code, risk_surface: cost}` is present in `documentation_gates.non_triggering_classifications` in `config/guardrail_gates.yaml`, which is currently absent. `test_unclassified_ac_completes_without_forcing_doc_expert` passes immediately (legacy path ignores documentation_gates; included as a regression guard). Fixed three pre-existing frontmatter issues (depends_on AC-ID → ticket filename, added change_target: code, added risk_surface: internal).

red_baseline:
  - test_name: test_risk_surface_cost_does_not_trigger_doc_expert
    file: unit_tests/test_bo_2200a_3_i.py
    error: "AssertionError: False is not true : Expected {change_target: code, risk_surface: cost} in documentation_gates.non_triggering_classifications in config/guardrail_gates.yaml — but the entry was absent."
  - test_name: test_unclassified_ac_completes_without_forcing_doc_expert
    file: unit_tests/test_bo_2200a_3_i.py
    error: "(passes immediately)"
    note: "passes immediately — regression guard only; legacy path ignores documentation_gates for unclassified ACs"

### 2026-07-20 15:35 — pr-reviewer (status: blocker)
feedback-id: fb_2026-07-20_d5163ffc
completion_manifest:
  diff_scoped_correctly:
    result: false
    reason: "Working diff contains templates/workflows-js/build-epic.js (+56 lines) and templates/workflows-js/build-ticket.js (+56 lines) — both out of scope for this ticket and each now has a duplicate parseAgentJson function declaration."
    remediation: "Before commit: stage only config/guardrail_gates.yaml + unit_tests/test_bo_2200a_3_i.py + the ticket file. Do NOT stage build-epic.js or build-ticket.js. The JS changes belong to a different ticket (BP-300e parseAgentJson extraction)."
  implementation_correct: true
  tests_correct: true
Two HIGH-confidence blockers found. The config-only implementation (adding {change_target: code, risk_surface: cost} to non_triggering_classifications in guardrail_gates.yaml) and the two behavioral tests in unit_tests/test_bo_2200a_3_i.py are correct and address the AC. However, the working diff also contains +56-line changes to templates/workflows-js/build-epic.js and templates/workflows-js/build-ticket.js — both completely out of scope for BO-2200a-3-i. Worse, each of these JS files now has two parseAgentJson function declarations in the same scope (the newly inserted ES5-var version at line 180 and the original ES6-const version at line 227); the second definition silently overwrites the first. The commit agent MUST scope staging to the three relevant files only and exclude the JS files. Suggested remediation: commit agent uses explicit git add on only config/guardrail_gates.yaml, unit_tests/test_bo_2200a_3_i.py, and the ticket file — then re-invoke pr-reviewer to confirm the scoped diff is clean.

### 2026-07-20 18:53 — pr-reviewer (status: ok)
feedback-id: fb_2026-07-20_65e4854c
completion_manifest:
  diff_scoped_correctly: true
  implementation_correct: true
  tests_correct: true
  prior_blocker_resolved: true
Prior blocker resolved: the JS files (build-epic.js, build-ticket.js) are now unstaged and will not be included in this commit. The staged diff contains only the three in-scope files: config/guardrail_gates.yaml (adds the explicit {change_target: code, risk_surface: cost} exclusion to non_triggering_classifications), unit_tests/test_bo_2200a_3_i.py (new 324-line behavioral test covering both AC scenarios), and the ticket file. Three medium notes for the commit agent: (1) add ENTROPY_HIGH:unit_tests/test_bo_2200a_3_i.py:* to both worktree and workspace-root .security-allowlist before committing (the guardrail_config_path=_GUARDRAIL_CONFIG keyword-arg pattern is a known check-secrets false positive per worktree CLAUDE.md); (2) the files_touched frontmatter field still lists scripts/ac_store/generate_ticket_from_ac.py though no Python code was changed — metadata-only discrepancy, does not block; (3) the unstaged JS files still have duplicate parseAgentJson declarations and must be remediated in their own BP-300e ticket before staging.

### 2026-07-20 18:59 — commit (status: ok)
feedback-id: fb_2026-07-20_cc7914d1
completion_manifest:
  pre_commit_hooks_pass: true
  commit_message_valid: true
  ticket_staged: true
Auto-authorized commit gate: subject "feat(build-orchestration): add cost risk surface to doc gate exclusions"; staged files: .security-allowlist, config/guardrail_gates.yaml, unit_tests/test_bo_2200a_3_i.py. Security allowlist updated (ENTROPY_HIGH:unit_tests/test_bo_2200a_3_i.py:*) in both worktree and workspace-root allowlists per pr-reviewer note. Mixed-set classifier warning (config + test) noted as expected for this AC — pr-reviewer signed off on scope.
