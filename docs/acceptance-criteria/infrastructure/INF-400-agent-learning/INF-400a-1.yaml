id: INF-400a-1
components:
  - infrastructure
title: "V3 agent templates contain a pre-flight step that loads domain-scoped accumulated context"
component: infrastructure
level: L2
status: active
req_status: active
work_status: todo
priority: high
readiness: reviewed
criteria: |
  Given the product-owner-v3, business-analyst-v3, and it-po-v3 agent templates,
  When an agent is spawned to work on a feature in a specific component
    (e.g., component: infrastructure),
  Then the agent template contains a pre-flight step (before the agent's first
    substantive action) that instructs the agent to:
    1. Identify the component from the L1 AC or ticket it was given,
    2. Read the PROJECT_CONTEXT.md file co-located with any skill the agent loads
       (Channel 3 of the knowledge plane),
    3. Read the per-folder README.md for the component's AC directory
       (Channel 2 of the knowledge plane),
  And the pre-flight step appears before the agent's first numbered working step,
  And the pre-flight step specifies that missing files are skipped without error.
depends_on: [INF-400a]
doc_links:
  - path: docs/architecture/agent_knowledge_plane.md
    relationship: describes
    status: exists
  - path: docs/architecture/agent_knowledge_system.md
    relationship: describes
    status: exists
  - path: templates/agents/product-owner.md
    relationship: constrains
    status: exists
  - path: templates/agents/business-analyst.md
    relationship: constrains
    status: exists
  - path: templates/agents/it-po.md
    relationship: constrains
    status: exists
assigned_agent: llm-expert
estimated_complexity: M
it_requirements:
  - "Pre-flight step must not add more than 5 seconds to agent startup under normal conditions"
  - "Must degrade gracefully when context files are absent, corrupted, or exceed 50KB"
  - "Must not modify any context file during the read phase"
  - "Must be consistent across all three v3 agent templates — same step structure, same skip behavior"
delivers_to:
  agent: llm-expert
  contract: "Three agent templates (product-owner-v3.md, business-analyst-v3.md, it-po-v3.md) each containing a pre-flight section that reads PROJECT_CONTEXT.md (Channel 3) and per-folder README.md (Channel 2) before the first working step"
expects_from: null
origin_agent: business-analyst-v3
created: 2026-06-05
amended_by: []
superseded_by: null
covered_by: []
implemented_by: []
change_target: prompt
risk_surface: contract_boundary
