---
ac_traceability:
  id: GE-120e-2
  path: docs/acceptance-criteria/guardrail-engine/GE-120-green-means-checked/GE-120e-2.yaml
advances_current_outcome: true
agents:
  ac-fulfillment-gate: needed
  ac-validator: needed
  architect-review: needed
  commit: needed
  documentation-expert: needed
  documentation-verifier: needed
  pr-reviewer: needed
  pull-request: needed
  python-coder: needed
  status-checker: needed
  test-runner: needed
  test-writer: needed
change_target:
- code
- config
complexity: medium
components:
- commit_guardian
- precommit_hooks
created: '2026-08-25'
depends_on:
- 28_TICKET-20260825-GE-120e-1.md
documentation_required: true
files_touched:
- scripts/build_precommit.py
- templates/scripts/commit_guardian/change_set_source.py
- templates/scripts/commit_guardian/check_hook_parity.py
- templates/scripts/commit_guardian/commit_guardian.json
- unit_tests/portability/test_ge_120e_2.py
priority: high
requires_adr: false
requires_diagram: false
risk_surface: contract_boundary
roadmap_phase: phase_1
source_ac: GE-120e-2
status: todo
title: Which checks work out their own change set is read from the manifest, not from
  the two that were caught
---

# Which checks work out their own change set is read from the manifest, not from the two that were caught

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `GE-120e-2` — Which checks work out their own change set is read from the manifest, not from the two that were caught — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `GE-120e-2`. Component: `guardrail-engine`. Assigned agent: `python-coder`. Estimated complexity: `M`. Complexity: `medium`.

## Acceptance Criteria

```gherkin
Given the guardrail engine's hook manifest, which lists every check it installs,
When the set of checks that work out their own change set is determined,
Then the candidate set is every entry in the manifest, read at the time of the
  determination,
And each entry records where its change set comes from — handed to it by the commit path,
  or taken from the shared authored-change source — as a value on the entry itself,
And membership is decided from that recorded value, never from a list of check names
  written down by hand and never from the two checks observed misattributing,
And every check recorded as taking its own change set takes it from the shared source,
And a check recorded as taking its own change set that obtains it by any other means is
  named and the determination reports a failure.

Coverage note: exercise this against a manifest fixture, not against today's manifest. A
test asserting that the checks registered today all take the shared source passes forever
and cannot fail on the day the next self-deriving check is added, which is the day this AC
exists for. Execute the determination and assert on the names it reported and the result
status it produced.
```

- [ ] AC-1: the candidate set is every entry in the manifest, read at the time of the
- [ ] AC-2: each entry records where its change set comes from — handed to it by the commit path
- [ ] AC-3: membership is decided from that recorded value, never from a list of check names
- [ ] AC-4: every check recorded as taking its own change set takes it from the shared source
- [ ] AC-5: a check recorded as taking its own change set that obtains it by any other means is
- [ ] AC-6: cannot fail on the day the next self-deriving check is added, which is the day this AC

## Test Requirements

```yaml
tests:
- name: test_ge120e2_candidate_set_is_read_from_the_manifest_at_run_time
  file: unit_tests/commit_guardian/test_ge_120e_2.py
  covers:
  - GE-120e-2
  asserts: Run against a fixture manifest, the determination's candidate set is exactly
    that fixture's entries — no hand-written check list.
  framework: unittest
  type: behavioral
- name: test_ge120e2_recorded_source_decides_membership_not_pass_filenames
  file: unit_tests/commit_guardian/test_ge_120e_2.py
  covers:
  - GE-120e-2
  asserts: A fixture entry with pass_filenames false but a recorded handed-its-files
    source is excluded from the governed set.
  framework: unittest
  type: behavioral
- name: test_ge120e2_entry_with_no_recorded_source_is_named_and_fails
  file: unit_tests/commit_guardian/test_ge_120e_2.py
  covers:
  - GE-120e-2
  asserts: A fixture entry missing the field makes the determination report a failure
    naming that entry — absent is not a default.
  framework: unittest
  type: behavioral
- name: test_ge120e2_self_deriving_entry_not_using_the_shared_source_is_named_and_fails
  file: unit_tests/commit_guardian/test_ge_120e_2.py
  covers:
  - GE-120e-2
  asserts: A fixture entry recorded as self-deriving whose check obtains its change
    set by other means is named and the determination exits non-zero.
  framework: unittest
  type: behavioral
- name: test_ge120e2_determination_honours_an_explicit_manifest_path
  file: unit_tests/commit_guardian/test_ge_120e_2.py
  covers:
  - GE-120e-2
  asserts: Given a fixture manifest path the determination reads that file and not
    the installed manifest — the fail-open argv trap is absent.
  framework: unittest
  type: behavioral
- name: test_ge120e2_result_is_independent_of_the_number_of_manifest_entries
  file: unit_tests/commit_guardian/test_ge_120e_2.py
  covers:
  - GE-120e-2
  asserts: Fixtures with different entry counts produce correct results — no count
    is hard-coded.
  framework: unittest
  type: unit
- name: test_ge120e2_added_field_leaves_precommit_config_generation_and_hook_parity_intact
  file: unit_tests/commit_guardian/test_ge_120e_2.py
  covers:
  - GE-120e-2
  asserts: With the new per-entry key present, build_precommit produces the same .pre-commit-config.yaml
    and check_hook_parity still agrees.
  framework: unittest
  type: integration
- name: test_ge_120e_2_reachable_from_entry_point
  file: unit_tests/commit_guardian/test_ge_120e_2.py
  covers:
  - GE-120e-2
  angle: reachability
  asserts: 'REQUIRED — invoke the production entry point (CLI, hook, slash command,
    workflow dispatch, or main()) as a subprocess/dispatch and assert the new behaviour
    actually occurs. Do NOT satisfy this by importing the function directly. The AC
    authored no test_spec, so the entry point is not declared: resolve it before writing
    this test, and do not delete this entry.'
```

## Implementation Notes

```yaml
- 'WHERE THE FIELD GOES. One new key per entry of hooks_manifest.hooks[] in templates/scripts/commit_guardian/commit_guardian.json.
  build_commit_guardian() deploys that manifest TWICE — to scripts/commit_guardian/commit_guardian.json
  (the runner''s copy) and to config/commit_guardian/commit_guardian.json (the authoritative
  copy). Both are generated from the one template: edit the template only, then run
  build.py and confirm both deployed copies carry the field.'
- 'PASS_FILENAMES IS NOT THE DISCRIMINATOR AND MUST NOT BE USED AS ONE. 52 of the
  54 entries in today''s manifest carry pass_filenames: false while only a handful
  derive a diff at all. A predicate built on it sweeps in dozens of checks with no
  change set to narrow and still misses a check that is handed filenames and derives
  a diff anyway. The source is a declared value, full stop.'
- EVERY ENTRY MUST CARRY THE FIELD; ABSENT IS NOT A DEFAULT. An entry with no recorded
  source is a determination FAILURE, so the next hook added without one is blocked
  when it is added rather than silently classified. Defaulting the field is the same
  decay this AC exists to stop, one release later.
- DO NOT BREAK THE MANIFEST'S EXISTING CONSUMERS. scripts/build_precommit.py generates
  .pre-commit-config.yaml from these entries and templates/scripts/commit_guardian/check_hook_parity.py
  compares the generated config back to the manifest. After adding the key, run build.py
  and confirm .pre-commit-config.yaml is unchanged apart from intended edits, and
  that check-hook-parity still passes.
- THE DETERMINATION MUST ACCEPT AN EXPLICIT MANIFEST PATH AND HONOUR IT. Without that,
  this AC's own coverage note — exercise against a fixture manifest, not today's —
  is untestable. This repository has a recorded history of AC hooks that ignore argv
  and read only the installed artefact, which makes them fail open and impossible
  to exercise; do not repeat it.
- REUSE GE-120c-3'S MANIFEST-READING CALLABLE. Two manifest readers will disagree
  and the disagreement will be settled by whichever is quieter. One reader, two questions.
- DO NOT HARD-CODE THE ENTRY COUNT ANYWHERE — not in the code, not in a test, not
  in a docstring. It was 54 when this AC was written and moves with every addition;
  a pinned number fails for the wrong reason and gets edited to match rather than
  investigated.
- FAILURE OUTPUT NAMES THE OFFENDERS. The determination exits non-zero and names each
  check whose recorded source is missing or contradicted, not just a count.
- LATENCY BUDGET. This runs at commit time. A full manifest pass must complete in
  under 200 ms and must not spawn a subprocess per entry.
```

## Agent Contracts

### documentation-expert

Existing docs to update / cross-link:

- templates/scripts/commit_guardian/commit_guardian.json (relationship: modifies | status: exists | relevance: hooks_manifest.hooks[] is the registry the candidate set is read from, and the entry is where the change-set source is recorded. Note that pass_filenames is NOT the discriminator — almost every entry carries pass_filenames: false, while only a few actually compute a diff, so the source must be recorded rather than inferred.)
- docs/architecture/components/commit-guardian.md (relationship: describes | status: exists)
- docs/how-to/managing-pre-commit-hooks.md (relationship: related | status: exists)

- [ ] AC-1: reference-doc | templates/scripts/commit_guardian/commit_guardian.json | the candidate set is every entry in the manifest, read at the time of the

### Delivers To

- **Agent:** test-writer
- **Contract:** The manifest-derived list of self-deriving checks, which GE-120e-3 runs its differential pair over.

### Expects From

- **AC:** GE-120e-1
- **Contract:** The shared authored-change source that a recorded self-deriving check must be taking its change set from.

## Sign-offs

- [ ] architect-review
- [ ] test-writer
- [ ] python-coder
- [ ] test-runner
- [ ] documentation-expert
- [ ] pr-reviewer
- [ ] ac-validator
- [ ] ac-fulfillment-gate
- [ ] documentation-verifier
- [ ] status-checker
- [ ] commit
- [ ] pull-request

## Comments
