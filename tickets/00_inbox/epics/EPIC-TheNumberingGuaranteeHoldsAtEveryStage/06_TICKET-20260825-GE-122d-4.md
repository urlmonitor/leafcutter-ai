---
ac_traceability:
  id: GE-122d-4
  path: docs/acceptance-criteria/guardrail-engine/GE-122-numbers-mean-one-thing/GE-122d-4.yaml
advances_current_outcome: true
agents:
  ac-fulfillment-gate: needed
  ac-validator: needed
  architecture-diagram-author: needed
  commit: needed
  documentation-expert: needed
  documentation-verifier: needed
  pull-request: needed
change_target: docs
complexity: low
components:
- commit_guardian
- precommit_hooks
- documentation_system
created: '2026-08-25'
depends_on:
- 01_TICKET-20260825-GE-122d-1.md
- 02_TICKET-20260825-GE-122d-2.md
documentation_required: true
files_touched:
- docs/architecture
- docs/architecture/components/commit-guardian.md
- docs/architecture/diagrams
- scripts/next_diagram_seq.py
priority: high
requires_adr: false
requires_diagram: false
risk_surface: internal
roadmap_phase: phase_1
source_ac: GE-122d-4
status: todo
title: The three-stage arrangement is drawn, showing which stage owns which promise
---

# The three-stage arrangement is drawn, showing which stage owns which promise

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `GE-122d-4` — The three-stage arrangement is drawn, showing which stage owns which promise — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `GE-122d-4`. Component: `guardrail-engine`. Assigned agent: `architecture-diagram-author`. Estimated complexity: `S`. Complexity: `low`.

## Acceptance Criteria

```gherkin
Given a reader trying to understand where the numbering rule is enforced and what each stage
  guarantees,
When they read the component diagram,
Then it shows the three stages, the single shared rule evaluation all three invoke, and the
  collection each reads,
And each stage is annotated with what it can do when a clash is found — bring in a specialist,
  stop a commit, or fail a build — so the differing strength of the three promises is visible
  on the drawing,
And each stage is annotated with whether it can be bypassed, and the shared build is
  identified as the one that cannot,
And it shows the rule evaluation as a single element the three stages share, not as three
  parallel copies,
And the diagram carries a sequence number allocated by the project's own allocator and is
  placed where the enrolment rule expects a numbered diagram,
And it is linked from its parent architecture document in both directions.
```

- [ ] AC-1: it shows the three stages, the single shared rule evaluation all three invoke, and the
- [ ] AC-2: each stage is annotated with what it can do when a clash is found — bring in a specialist
- [ ] AC-3: each stage is annotated with whether it can be bypassed, and the shared build is
- [ ] AC-4: it shows the rule evaluation as a single element the three stages share, not as three
- [ ] AC-5: the diagram carries a sequence number allocated by the project's own allocator and is
- [ ] AC-6: it is linked from its parent architecture document in both directions.

## Implementation Notes

```yaml
- 'The sequence number MUST come from `python scripts/next_diagram_seq.py`, not from
  scanning the directory and adding one — the allocator encodes never-reusing a retired
  number and a max+1 scan does not. Coordinate with GE-122c-4, which allocates at
  the same time: two diagrams allocated from one stale scan is a collision minted
  by the tree that exists to prevent collisions.'
- The file goes in `docs/architecture/diagrams/` at level c2 (a container-level structural
  view), named `c2-NNN-<slug>.md`. GE-122c-4 is the c3 interaction view of the same
  arrangement; the two levels are the distinction between them.
- The drawing must show the two DEPLOYED layouts the stages run from — `.claude/hooks/`
  for authoring time and `.leafcutter/scripts/commit_guardian/` for commit time and
  the shared build — because that split is the real structural obstacle to the shared
  element and hiding it makes the diagram describe an easier system than the one that
  was built.
- 'The shared rule evaluation must be ONE element with three inbound arrows. This
  is the diagram''s main assertion and the thing a future contributor is most likely
  to break by adding a stage-specific special case: if the drawing shows three copies,
  three copies is what the next person builds.'
- The bypass annotation must state the mechanism, not just the fact, for each bypassable
  stage, and must identify the shared build as unbypassable ONLY once GE-122d-2's
  required-check step has actually landed. Drawing it as unbypassable while the CI
  gate is still optional documents an intent as if it were a fact.
- '`check-mermaid-parent-link` enforces bidirectional links and `check-mermaid-complexity`
  caps size, both at commit time on `docs/architecture/**`. Add the back-link in `docs/architecture/components/commit-guardian.md`
  in the same change.'
- Must agree with GE-122c-4 on which stage engages the specialist. Structure and interaction
  views that disagree are a contradiction inside the architecture surface itself.
```

## Agent Contracts

### documentation-expert

Existing docs to update / cross-link:

- docs/architecture/components/commit-guardian.md (relationship: modifies | status: exists | relevance: The parent architecture document this diagram must link to and from.)
- scripts/next_diagram_seq.py (relationship: context | status: exists | relevance: The allocator that must supply this diagram's sequence number.)
- docs/architecture/diagrams/c2-001-ac-driven-pipeline.md (relationship: context | status: exists | relevance: An existing correctly-numbered c2 container view in the enrolled location — the level, shape and naming convention to follow.)
- docs/architecture/adrs/ADR-001-self-hosting-boundary.md (relationship: describes | status: exists | relevance: Why the drawing must show the DEPLOYED layouts the three stages actually run from (.claude/hooks/ for authoring time, .leafcutter/scripts/commit_guardian/ for the other two) rather than the templates/ source tree — the two-layout split is the structural fact GE-122d-1's shared element has to bridge.)

- [ ] AC-1: [component-diagram] docs/architecture/components/commit-guardian.md — it shows the three stages, the single shared rule evaluation all three invoke, and the
- [ ] AC-2: [reference-doc] docs/architecture/components/commit-guardian.md — it shows the three stages, the single shared rule evaluation all three invoke, and the

### Expects From

- **AC:** GE-122d-1
- **Contract:** Which element the three stages actually share, so the diagram depicts the arrangement that was built rather than the one that was intended.

## Sign-offs

- [ ] documentation-expert
- [ ] ac-validator
- [ ] ac-fulfillment-gate
- [ ] documentation-verifier
- [ ] architecture-diagram-author
- [ ] commit
- [ ] pull-request

## Comments
