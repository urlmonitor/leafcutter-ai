---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  llm-expert: needed
  pr-reviewer: needed
  pull-request: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
complexity: low
components:
- build_orchestration
created: '2026-07-10'
depends_on:
- BO-2100a
files_touched:
- templates/skills/ticket-authoring/SKILL.md
- docs/architecture/adrs/ADR-020-live-surface-tester.md
priority: medium
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: BO-2100a-1
status: todo
title: Ticket authoring adds live-surface-tester to the agents map when both toggles
  are on
---

# Ticket authoring adds live-surface-tester to the agents map when both toggles are on

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BO-2100a-1` — Ticket authoring adds live-surface-tester to the agents map when both toggles are on — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BO-2100a-1`. Component: `build-orchestration`. Assigned agent: `llm-expert`. Estimated complexity: `S`. Complexity: `low`.

## Acceptance Criteria

```gherkin
Given a ticket whose frontmatter sets `live_surface_test: true`
  And a project `skills_config.json` whose `live_surface_testing.enabled` is `true`
When the business-analyst authors that ticket via the ticket-authoring skill
  (templates/skills/ticket-authoring/SKILL.md)
Then the ticket's `agents:` map contains the entry `live-surface-tester: needed`
  And this mirrors the existing rule that adds `user-surface-smoker: needed`
  when `user_facing_surface` is non-null.
```

## Implementation Notes

```yaml
- The two-toggle check must read the live toggles at authoring time (feature frontmatter
  live_surface_test AND project skills_config live_surface_testing.enabled); it must
  not hardcode an assumption about either.
- 'Must mirror the existing user-surface-smoker: needed insertion rule so the two
  gating patterns stay structurally consistent and maintainable.'
- Must add the entry only when BOTH toggles are true; no partial-toggle combination
  may produce it.
- The emitted agent key must be spelled exactly `live-surface-tester`, in parity with
  the ticket-supervisor Spawn Allowlist (BO-2100a-2) and the build-ticket.js phaseOrder
  (BO-2100a-3).
```

## Sign-offs

- [ ] llm-expert
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit
- [ ] pull-request

## Comments
