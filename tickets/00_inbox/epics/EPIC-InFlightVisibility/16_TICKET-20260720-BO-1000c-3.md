---
ac_traceability:
  id: BO-1000c-3
  path: docs/acceptance-criteria/build-orchestration/BO-1000-live-automation-progress/BO-1000c-3.yaml
advances_current_outcome: true
agents:
  architecture-diagram-author: signed_off
  commit: signed_off
  pr-reviewer: signed_off
  pull-request: needed
change_target: pipeline
complexity: low
components:
- build_orchestration
created: '2026-07-20'
depends_on:
- 12_TICKET-20260720-BO-1000c-1a.md
- 13_TICKET-20260720-BO-1000c-1b.md
files_touched:
- docs/architecture/diagrams
- docs/architecture/diagrams/finalize-progress-relay-sequence.md
priority: medium
requires_adr: false
requires_diagram: false
risk_surface: internal
roadmap_phase: phase_1
source_ac: BO-1000c-3
status: todo
title: Sequence diagram of live progress delivery from background workflow to the
  conversation
---

# Sequence diagram of live progress delivery from background workflow to the conversation

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BO-1000c-3` — Sequence diagram of live progress delivery from background workflow to the conversation — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BO-1000c-3`. Component: `build-orchestration`. Assigned agent: `architecture-diagram-author`. Estimated complexity: `S`. Complexity: `low`.

## Acceptance Criteria

```gherkin
Given the live delivery of finalize progress into the main conversation
  (BO-1000c-1 and BO-1000c-2) is implemented,
When the documentation required by the BO-1000c sequence-diagram trigger is
  produced,
Then a sequence diagram is authored that shows progress travelling from the
  background finalize workflow, through the mechanism that relays it, into the
  main conversation, and on to the user,
And the diagram shows more than two participants and depicts the in-flight
  ordering, so that updates are shown arriving during the run rather than only
  at its end.
```

- [ ] AC-1: a sequence diagram is authored that shows progress travelling from the
- [ ] AC-2: the diagram shows more than two participants and depicts the in-flight

## Implementation Notes

```yaml
- Author the diagram as a Mermaid sequenceDiagram under docs/architecture/diagrams/,
  matching the existing *-sequence.md convention.
- 'Participants must number more than two and cover the full relay path: the background
  finalize workflow, the run-progress journal (BO-1000c-1a), the /finalize-feature
  launcher/poller (BO-1000c-1b), the main conversation, and the user.'
- 'Depict in-flight ordering explicitly (e.g. a loop): lines are journaled and relayed
  into the conversation DURING the run, not batched only at the end — matching BO-1000c-2.'
- 'Include the halt case (BO-1000c-2-i) at least as a note: the halting step''s line
  reaches the conversation via the relay before the terminal halt result.'
```

## Agent Contracts

### Expects From

- **AC:** BO-1000c-1b
- **Contract:** The implemented relay path (background workflow -> run-progress journal -> launcher poll/relay -> main conversation -> user) with in-flight ordering, which the diagram documents.

## Sign-offs

- [x] pr-reviewer — 2026-07-23 08:59
- [x] architecture-diagram-author — 2026-07-23 08:55
- [x] commit — 2026-07-23 09:20
- [ ] pull-request

## Comments

### 2026-07-23 08:55 — architecture-diagram-author (status: ok)
feedback-id: fb_2026-07-23_ef4c6542
completion_manifest:
  diagram_created: true
  flight_level_correct: true
  cross_links_added: true
Authored docs/architecture/diagrams/finalize-progress-relay-sequence.md — an L3-Component Mermaid sequenceDiagram with five participants (user, /finalize-feature launcher, background finalize workflow, run-progress journal, main conversation) tracing the full relay path. In-flight ordering is shown via a par/loop (workflow appends to the journal as it happens per BO-1000c-1a while the launcher polls and relays incrementally per BO-1000c-1b, so updates arrive during the run not only at the end per BO-1000c-2); the mid-flight halt case (BO-1000c-2-i) is an alt branch relaying the halting-step line before the terminal halt result. Both ACs satisfied: AC-1 (full workflow→journal→launcher→conversation→user path) and AC-2 (>2 participants + in-flight ordering). Frontmatter validated (check_doc_frontmatter PASSED); mermaid parent-link and complexity hooks both exit 0. Parent cross-link added to c2-006-feature-to-merged-pr.md (prose Parent: line + children: frontmatter entry). Note: scripts/scaffold/new_arch_doc.py is not deployed in this repo, so — following the committed sibling finalize-progress-narration-sequence.md precedent from this same epic — the file was hand-authored to match the established *-sequence.md convention.

### 2026-07-23 08:59 — pr-reviewer (status: ok)
feedback-id: (submit-failed)
completion_manifest:
  ac1_verified: true
  ac2_verified: true
  contract_verified: true
  no_high_confidence_findings: true
Reviewed BO-1000c-3 diff (156-line sequence diagram + 1-line parent cross-link). Both ACs satisfied: AC-1 (full relay path workflow→journal→launcher→conversation→user present in diagram) and AC-2 (5 participants, in-flight ordering via par/loop structure). Contract (Expects From BO-1000c-1b) verified — diagram documents the exact relay path. One medium finding: BO-1000a-1-i referenced in the halt-case alt branch but absent from depends_on; BO-1000a-1-i is a real AC in the store and the reference is accurate, the depends_on gap is informational. Pre-existing defect noted for commit phase: depends_on contains AC IDs (BO-1000c, BO-1000c-1a, BO-1000c-1b) instead of ticket file paths — ticket_frontmatter_guard fires on every Edit to this ticket; commit phase must fix these references to ticket filenames before the hook will pass (known build-ac generator quirk).

### 2026-07-23 09:20 — commit (status: ok)
feedback-id: fb_2026-07-23_6d663603
completion_manifest:
  pre_commit_hooks_pass: true
  commit_message_valid: true
  ticket_staged: true
Auto-authorized commit gate: subject "docs(diagrams): add finalize progress relay sequence diagram (BO-1000c-3)"; staged files: docs/architecture/diagrams/c2-006-feature-to-merged-pr.md, docs/architecture/diagrams/finalize-progress-relay-sequence.md, templates/commands/finalize-feature.md, tickets/00_inbox/epics/EPIC-InFlightVisibility/14_TICKET-20260720-BO-1000c-2.md, tickets/00_inbox/epics/EPIC-InFlightVisibility/16_TICKET-20260720-BO-1000c-3.md. Fixed depends_on AC ID references to ticket filenames (removed non-existent BO-1000c; mapped BO-1000c-1a→12_TICKET, BO-1000c-1b→13_TICKET) per pr-reviewer finding and build-ac generator quirk.
