---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  llm-expert: needed
  pr-reviewer: needed
  pull-request: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
complexity: low
components:
- build-orchestration
created: '2026-07-10'
depends_on:
- BO-2100b
files_touched:
- scripts/config_loader.py
- templates/skills/ticket-authoring/SKILL.md
- docs/architecture/adrs/ADR-020-live-surface-tester.md
priority: medium
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: BO-2100b-1
status: todo
title: Project toggle off means the check never runs, even when the ticket opts in
---

# Project toggle off means the check never runs, even when the ticket opts in

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BO-2100b-1` — Project toggle off means the check never runs, even when the ticket opts in — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BO-2100b-1`. Component: `build-orchestration`. Assigned agent: `llm-expert`. Estimated complexity: `S`. Complexity: `low`.

## Acceptance Criteria

```gherkin
Given a project `skills_config.json` whose `live_surface_testing.enabled` is `false`
  And a ticket whose frontmatter sets `live_surface_test: true`
When the ticket is authored and driven
Then no `live-surface-tester` phase is added to the ticket's `agents:` map
  And no development server is started for that ticket
  And the live-app check does not run.
```

## Implementation Notes

```yaml
- 'Enablement must be re-derived from the live project config at authoring time: a
  false project toggle must yield no agents-map entry regardless of the feature toggle''s
  value.'
- The gate must be the single decision point for adding the phase, so a false project
  toggle deterministically prevents both the agents-map entry and any downstream server
  start.
```

## Sign-offs

- [ ] llm-expert
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit
- [ ] pull-request

## Comments
