---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  pr-reviewer: needed
  pull-request: needed
  python-coder: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
components:
- ac-store
created: '2026-06-08'
depends_on: []
files_touched:
- docs/components.json
priority: medium
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: ACS-300g-2
status: todo
title: Existing component entries are preserved unmodified during backfill
---

# Existing component entries are preserved unmodified during backfill

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `ACS-300g-2` — Existing component entries are preserved unmodified during backfill — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `ACS-300g-2`. Component: `ac-store`. Assigned agent: `python-coder`. Estimated complexity: `S`.

## Acceptance Criteria

```gherkin
Given docs/components.json already contains entries for
  sync_platforms, build_pipeline, and config_loader,
When 17 new subsystem entries are added to the components object,
Then the three pre-existing entries remain byte-for-byte identical
  (same field values, same field order).
And the total component count equals 20 (3 existing + 17 new).
```

## Sign-offs

- [ ] python-coder
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit
- [ ] pull-request

## Comments
