---
advances_current_outcome: true
agents:
  commit: signed_off
  documentation-expert: not_needed
  pr-reviewer: signed_off
  pull-request: signed_off
  python-coder: signed_off
  sql-coder: not_needed
  test-runner: signed_off
  test-writer: signed_off
components:
- build_pipeline
created: '2026-07-06'
depends_on:
- BP-1100e
- BP-1100e-1
files_touched:
- docs/acceptance-criteria/build_pipeline/BP-1100-phantom-done-prevention/BP-1100e.yaml
- templates/scripts/commit_guardian/commit_guardian.json
- templates/scripts/commit_guardian/hooks/check_files_touched_reconciliation.py
- unit_tests/commit_guardian/test_check_files_touched_reconciliation.py
priority: high
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: BP-1100e-2
status: done
title: The reconciliation is advisory and fails open by default; strict blocking is
  opt-in
---

# The reconciliation is advisory and fails open by default; strict blocking is opt-in

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BP-1100e-2` — The reconciliation is advisory and fails open by default; strict blocking is opt-in — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BP-1100e-2`. Component: `build-pipeline`. Assigned agent: `python-coder`. Estimated complexity: `S`.

## Acceptance Criteria

```gherkin
Given the pre-done reconciliation produces a verdict that a ticket changed source
  files not present in its declared files_touched UNION out_of_scope
When the reconciliation runs in its default configuration
Then it reports the mismatch as an advisory (it names the undeclared source files
  and does not hard-block the ticket from reaching done) — the default is fail-open
And when the strict mode is explicitly opted into via configuration, the same
  verdict blocks the ticket from being marked done until the scope is reconciled
And when the reconciliation itself cannot run (an internal error computing the diff
  or reading frontmatter), it fails open — it does not block the ticket on account
  of its own failure
```

## Sign-offs

- [x] python-coder — 2026-07-07 15:00
- [x] test-writer — 2026-07-07 07:10
- [x] test-runner — 2026-07-07 09:32
- [x] pr-reviewer — 2026-07-07 09:37
- [x] commit — 2026-07-07 15:30
- [x] pull-request — 2026-07-07 15:45

## Comments

### 2026-07-07 07:10 — ticket-supervisor (status: ok)
feedback-id: (submit-failed)
test_requirements empty — test-writer phase skipped (docs-only or config-only ticket)

### 2026-07-07 15:00 — python-coder (status: ok)
feedback-id: fb_2026-07-07_9bc18521
completion_manifest:
  module_docstring_updated: true
  import_json_added: true
  print_advisory_function_added: true
  load_strict_mode_function_added: true
  main_uses_strict_mode: true
  commit_guardian_json_predone_scope_added: true
  hook_comment_updated: true
  files_touched_updated: true
  tests_green: true
Implemented AC BP-1100e-2. The reconciliation hook is now advisory by default (exit 0) and only blocks (exit 1) when `predone_scope.strict: true` is set in commit_guardian.json. Added `_print_advisory()` for non-blocking output, `_load_strict_mode()` to read the config with fail-open semantics, and updated `main()` to branch on the strict setting. Added `predone_scope` section to commit_guardian.json. Extended the test suite with `TestLoadStrictMode` (7 tests) and `TestMainAdvisoryStrictMode` (6 tests) — all 42 tests green.

### 2026-07-07 09:32 — test-runner (status: ok)
feedback-id: (submit-failed)
completion_manifest:
  test_suite_executed: true
  all_tests_passing: true
  failure_report_structured: true
Ran both reconciliation suites. 42 tests passed in test_check_files_touched_reconciliation.py (TestLoadStrictMode: 7 tests, TestMainAdvisoryStrictMode: 6 tests, plus all pre-existing tests) and 12 tests passed in test_check_files_touched_reconciliation_pathcase.py. No failures.

### 2026-07-07 09:37 — pr-reviewer (status: ok)
feedback-id: (submit-failed)
completion_manifest:
  advisory_strict_branching_correct: true
  fail_open_semantics_verified: true
  test_coverage_adequate: true
  no_high_confidence_findings: true
Reviewed working diff (3 files, 354 lines). Advisory/strict branching in main() is correct — _load_strict_mode is called only after errors are found, returns False (fail-open) on missing file, empty root, OSError, and JSONDecodeError (a ValueError subclass). _print_advisory exits 0 unconditionally. Two medium findings: (M-1) missing OSError test case for _load_strict_mode (ValueError/malformed-JSON path tested, OSError not); (M-2) 4-level nested patch.object nesting in _run_main_with_undeclared — cosmetic only. Neither is blocking. All three AC clauses (advisory by default, strict opt-in, fail-open on internal error) are satisfied by the implementation and covered by the new test classes.

### 2026-07-07 15:30 — commit (status: ok)
feedback-id: (submit-failed)
completion_manifest:
  pre_commit_hooks_pass: true
  commit_message_valid: true
  ticket_staged: true
Auto-authorized commit gate (human-authorized batch drive, COMMIT_AGENT_MODE=1). SHA bdaf9b76 — "feat(build-pipeline): BP-1100e-2 — reconciliation advisory by default; strict opt-in". 4 files, 378 insertions. Pre-commit hook autofix applied: added feedback-id line to ticket-supervisor comment heading.

### 2026-07-07 15:45 — pull-request (status: ok)
feedback-id: (submit-failed)
completion_manifest:
  branch_pushed: true
  pr_created: true
  pr_body_complete: true
BP-1100e-2 commits pushed to existing PR #209 (EPIC-PhantomDoneFilesTouched). Branch updated from e3ef343c to 10e5aac2. No new PR opened — PR #209 already exists and is OPEN. All agents signed off; ticket status set to done.
