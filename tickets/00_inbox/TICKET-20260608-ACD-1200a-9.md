---
title: "Fix _dfs_collect_leaves to use level-based leaf detection"
status: open
priority: high
created: 2026-06-08
ac_traceability:
  - id: ACD-1200a-9
    path: docs/acceptance-criteria/ac-driven-dev/ACD-1200-goal-to-epic/ACD-1200a-9.yaml
files_touched:
  - scripts/ac_store/scan_ac_store.py
  - tests/ac_store/test_scan_ac_store.py
out_of_scope:
  - The main() CLI filter path (already correct — uses _is_leaf())
  - build_ac_mode_detection.py (already uses _is_leaf() correctly)
depends_on: []
agents:
  test-writer: needed
  python-coder: needed
  pr-reviewer: not_needed
  commit: not_needed
---

# Fix _dfs_collect_leaves to use level-based leaf detection

## Summary

`_dfs_collect_leaves()` in `scripts/ac_store/scan_ac_store.py` (lines 489-515) uses
`covered_by == []` as its sole leaf signal. But the module already defines
`_LEAF_LEVELS = frozenset({"L2", "L3"})` at line 46, used correctly by `_is_leaf()`
and by `build_ac_mode_detection.py`.

The tree-traversal function ignores the level field and treats any AC with non-empty
`covered_by` as a composite pass-through. This means an L2 AC that has L3 edge-case
children is never emitted as a leaf — only its L3 children are.

## Root Cause

`_dfs_collect_leaves()` line 508: `if not children:` — only emits the node when
`covered_by` is empty. Should instead check `level in _LEAF_LEVELS`.

## Fix

Change `_dfs_collect_leaves()` to:
1. If the node's level is in `_LEAF_LEVELS` (L2/L3): emit it as a leaf AND recurse into its `covered_by` children (so L3 edge cases are also collected).
2. If the node's level is L0/L1: recurse only (pure pass-through composite).
3. If the node has no `covered_by` children and is NOT in `_LEAF_LEVELS`: emit as leaf (backward compat for any edge case).

## Test Requirements

```yaml
tests:
  - name: test_l2_with_covered_by_emitted_as_leaf
    location: tests/ac_store/test_scan_ac_store.py
    type: integration
    covers: ACD-1200a-9
    description: |
      An L2 AC that has covered_by children (L3s) must still be emitted as a leaf
      by traverse_ac_tree(). Create a tree: L0 -> L1 -> L2 (with covered_by: [L3]) -> L3.
      Assert the L2 appears in the returned leaf list.

  - name: test_l3_under_l2_also_collected
    location: tests/ac_store/test_scan_ac_store.py
    type: integration
    covers: ACD-1200a-9
    description: |
      When an L2 has L3 children, both the L2 AND the L3 must appear in the leaf list.
      Create tree: L0 -> L1 -> L2 (covered_by: [L3-a, L3-b]) -> L3-a, L3-b.
      Assert all three (L2, L3-a, L3-b) are in the result.

  - name: test_l0_l1_never_emitted_as_leaf
    location: tests/ac_store/test_scan_ac_store.py
    type: integration
    covers: ACD-1200a-9
    description: |
      L0 and L1 nodes must NEVER appear in the leaf list, even if their covered_by
      is empty. Create an L0 with empty covered_by and an L1 with empty covered_by.
      Assert neither appears in the result.

  - name: test_depth_first_order_preserved_with_level_based_detection
    location: tests/ac_store/test_scan_ac_store.py
    type: integration
    covers: ACD-1200a-9
    description: |
      The order must be depth-first with alphabetical siblings. Create:
      L0 -> L1a (-> L2-alpha, L2-beta(-> L3-gamma)), L1b (-> L2-delta).
      Expected order: [L2-alpha, L2-beta, L3-gamma, L2-delta].
```

## Acceptance Criteria

Given the AC store contains:
  - ROOT-001 (L0, covered_by: [ROOT-001a])
  - ROOT-001a (L1, covered_by: [ROOT-001a-1, ROOT-001a-2])
    - ROOT-001a-1 (L2, covered_by: [])
    - ROOT-001a-2 (L2, covered_by: [ROOT-001a-2-i])
      - ROOT-001a-2-i (L3, covered_by: [])

When `traverse_ac_tree("ROOT-001", ac_store_root)` is called,
Then the result is `["ROOT-001a-1", "ROOT-001a-2", "ROOT-001a-2-i"]`.

L2/L3 nodes are always leaves. L0/L1 nodes are always composites.

## Sign-offs

- [ ] test-writer
- [ ] python-coder
