---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  pr-reviewer: needed
  pull-request: needed
  python-coder: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
complexity: low
components:
- build_orchestration
created: '2026-07-08'
depends_on:
- BO-1900c-1
files_touched:
- config/agent_registry.json
- docs/architecture/components/agent-registry.md
priority: high
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: BO-1900c-1-i
status: todo
title: A task verb with no confidently-matching charter is held back, not passed
---

# A task verb with no confidently-matching charter is held back, not passed

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BO-1900c-1-i` — A task verb with no confidently-matching charter is held back, not passed — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BO-1900c-1-i`. Component: `build-orchestration`. Assigned agent: `python-coder`. Estimated complexity: `S`. Complexity: `low`.

## Acceptance Criteria

```gherkin
Given a ticket whose task verb cannot be confidently matched to any single agent
  charter (the task is ambiguous or spans multiple charters),
When the preflight charter check runs before spawn,
Then it returns an unfit verdict rather than silently allowing the assigned agent,
And the held-back reason states that agent fit could not be confirmed for the task verb,
And no phase agent is spawned.
```

## Test Requirements

```yaml
tests: []
```

## Sign-offs

- [ ] python-coder
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit
- [ ] pull-request

## Comments
