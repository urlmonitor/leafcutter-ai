---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  pr-reviewer: needed
  pull-request: not_needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
  workflow-architect: needed
complexity: low
components:
- build_orchestration
created: '2026-07-08'
depends_on:
- BO-1900a-1
files_touched:
- docs/architecture/agent_delivery_workflows.md
- docs/architecture/components/build-ticket-workflow-dispatch.md
priority: high
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: BO-1900a-1-i
status: todo
title: Preflight that errors internally fails closed and holds the ticket back
---

# Preflight that errors internally fails closed and holds the ticket back

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BO-1900a-1-i` — Preflight that errors internally fails closed and holds the ticket back — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BO-1900a-1-i`. Component: `build-orchestration`. Assigned agent: `workflow-architect`. Estimated complexity: `S`. Complexity: `low`.

## Acceptance Criteria

```gherkin
Given a ticket at the dispatch point,
  and the preflight validation stage itself raises an unexpected error while evaluating
  a fitness check (for example the check subprocess crashes),
When the supervisor runs preflight before spawning,
Then the ticket is held back rather than dispatched,
And the held-back reason states that preflight could not complete and names the check
  that errored,
And the phase agent is never spawned.
```

## Sign-offs

- [ ] workflow-architect
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit

## Comments
