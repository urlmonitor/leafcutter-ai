id: BP-600c-1
components:
  - build_pipeline
title: "Quick-fix workflow dispatches test-writer to create a failing test before the fix"
component: build-pipeline
level: L2
status: active
readiness: reviewed
priority: medium
work_status: done
criteria: |
  Given the quick-fix workflow has created an AC YAML file for the
    diagnosed bug,
  When the workflow reaches the test-writing phase,
  Then it dispatches the test-writer agent with the AC as input,
  And the test-writer produces a test that reproduces the diagnosed bug,
  And the test includes a "# covers: <AC-ID>" tag referencing the
    newly created AC,
  And the test is written to the appropriate test directory before any
    fix code is applied.
depends_on:
  - BP-600c
  - BP-600b-1
doc_links:
  - path: docs/reference/ac-schema.md
    relationship: describes
    status: exists
  - path: docs/architecture/agent_delivery_workflows.md
    relationship: describes
    status: exists
  - path: docs/architecture/adrs/ADR-006-flatten-supervisor-chain.md
    relationship: describes
    status: exists
assigned_agent: workflow-architect
estimated_complexity: M
it_requirements:
  - "Must operate at depth 0 per ADR-006 — phase agents dispatched at depth 1 via the Agent tool"
  - "Requires Claude Code >= 2.1.154 for JS workflow script support"
  - "Must handle errors gracefully per the project error handling policy"
  - "test-writer must be dispatched via the Agent tool at depth 1 with the AC path and diagnosis as prompt context"
  - "Test must be written before any fix code is applied — ordering is enforced by the workflow"
delivers_to:
  agent: workflow-architect
  contract: "Test file absolute path (string) that the red-phase and green-phase verification steps consume as input to the test-runner agent"
expects_from:
  ac_id: BP-600b-1
  contract: "AC YAML file absolute path (string) containing: { id: string, criteria: string, component: string }"
origin_agent: business-analyst-v3
created: 2026-06-05
amended_by: []
superseded_by: null
covered_by:
  - "unit_tests/workflows/test_quick_fix_workflow.py"
implemented_by: []
change_target: pipeline
risk_surface: contract_boundary
