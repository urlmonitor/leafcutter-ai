id: BO-2000c-4
components:
  - build_orchestration
title: "The deterministic dispatch stays thin but tells the agent to read the ticket"
component: build-orchestration
level: L2
status: active
req_status: active
work_status: todo
readiness: approved
priority: high
roadmap_phase: phase_1
criteria: |
  Given the deterministic build-ticket dispatch is about to spawn a phase agent,
  When it assembles the dispatch string,
  Then the string includes an explicit instruction to read the ticket before starting,
  And the string still carries only the thin pointer set (phase name, ticket path,
    files_touched) -- it does not inline the implementation spec from the ticket body.
depends_on:
  - BO-2000c
doc_links:
  - path: templates/workflows-js/build-ticket.js
    relationship: modifies
    status: exists
  - path: docs/architecture/components/build-ticket-workflow-dispatch.md
    relationship: describes
    status: exists
assigned_agent: python-coder
estimated_complexity: S
it_requirements:
  - "build-ticket.js (a .js workflow body -- python-coder surface per the repo convention) must add an explicit 'Read the ticket before starting.' instruction to the dispatch string while keeping it otherwise thin (no inlined Implementation Notes)."
  - "Test coverage: assert the assembled dispatch string contains the read-the-ticket instruction and does NOT contain the ticket's implementation-spec body."
delivers_to:
  agent: ticket-supervisor
  contract: "A thin dispatch string that points the agent at the ticket instead of inlining the spec"
expects_from:
  ac_id: BO-2000c-1
  contract: "The ticket body (with Implementation Notes) that the thin dispatch points to"
origin_agent: BrainCandy
created: 2026-07-08
amended_by: []
superseded_by: null
covered_by: []
implemented_by: []
