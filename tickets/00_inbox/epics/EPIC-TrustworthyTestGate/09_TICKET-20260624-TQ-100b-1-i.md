---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  pr-reviewer: needed
  pull-request: needed
  python-coder: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
components:
- testing_quality
created: '2026-06-24'
depends_on:
- TQ-100b-1
- TQ-100b-2
files_touched:
- scripts/ac_store/test_enforcement.py
- tests/testing_quality/test_ac_linkage.py
priority: high
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: TQ-100b-1-i
status: todo
title: When its AC flips to done, the same test transitions from informational to
  enforced with no test edit
---

# When its AC flips to done, the same test transitions from informational to enforced with no test edit

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `TQ-100b-1-i` — When its AC flips to done, the same test transitions from informational to enforced with no test edit — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `TQ-100b-1-i`. Component: `testing-quality`. Assigned agent: `python-coder`. Estimated complexity: `S`.

## Acceptance Criteria

```gherkin
Given a failing test tagged "# covers: <AC-ID>" whose AC begins with
  work_status not-done, observed in a first run to be reported informationally
  (non-blocking),
When the AC's work_status is changed to done (for example because its feature
  merged) and the suite is run again, with no change made to the test file,
Then in the second run the same test is enforced and its failure makes the
  overall run outcome non-passing
And the transition from informational to enforced is driven solely by the AC's
  work_status change, not by any edit to the test or any manual switch.
```

## Test Requirements

- `test_transition_informational_to_enforced_on_done`: When a covering AC flips from not-done to done, the same unmodified test transitions from informational to enforced with no test edit.

## Sign-offs

- [ ] python-coder
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit
- [ ] pull-request

## Comments
