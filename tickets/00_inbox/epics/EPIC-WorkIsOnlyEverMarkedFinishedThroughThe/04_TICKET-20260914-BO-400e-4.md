---
ac_traceability:
  id: BO-400e-4
  path: docs/acceptance-criteria/build-orchestration/BO-400-ticket-status-source-of-truth/BO-400e-4.yaml
advances_current_outcome: true
agents:
  ac-fulfillment-gate: needed
  ac-validator: needed
  architect-review: needed
  commit: needed
  documentation-expert: needed
  documentation-verifier: needed
  frontend-coder: not_needed
  pr-reviewer: needed
  pull-request: not_needed
  python-coder: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
  user-surface-smoker: not_needed
change_target: pipeline
complexity: medium
components:
- build_orchestration
- supervisor_system
created: '2026-09-14'
depends_on:
- 01_TICKET-20260914-BO-400e-1.md
- 02_TICKET-20260914-BO-400e-2.md
- 03_TICKET-20260914-BO-400e-3.md
documentation_required: true
files_touched:
- templates/workflows-js/build-feature.js
- templates/workflows-js/build-ticket.js
priority: high
requires_adr: false
requires_diagram: false
risk_surface: contract_boundary
roadmap_phase: phase_1
source_ac: BO-400e-4
status: todo
title: Several tickets in the identical state, carried by one run, all get the same
  answer -- and it is the strict one
---

# Several tickets in the identical state, carried by one run, all get the same answer -- and it is the strict one

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BO-400e-4` — Several tickets in the identical state, carried by one run, all get the same answer -- and it is the strict one — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BO-400e-4`. Component: `build-orchestration`. Assigned agent: `python-coder`. Estimated complexity: `M`. Complexity: `medium`.

## Acceptance Criteria

```gherkin
Given one run carrying four tickets,
  And the first three are in the identical state: their records name the same phases as
    needed, every one of those phases carries a sign-off, except one and the same phase
    in all three, which carries none,
  And the fourth ticket's record names those same phases as needed and carries a
    sign-off for every one of them,
When the run reaches the close of each of the four,
Then the first three all receive the same answer as each other,
  And that answer is refusal: none of the three has its recorded state changed, and each
    refusal names the same outstanding phase,
  And the fourth is written finished,
  And the answers do not vary with the order in which the run reached the tickets, nor
    with which of the four was closed first,
And given a second run over the same four tickets in the same state,
When it reaches the close of each,
Then it produces the same four answers as the first run,
So one run cannot give two answers to one question, the answer it gives is the strict
  one, and "the same answer every time" is not obtained by a run that refuses
  everything.
```

- [ ] AC-1: the first three are in the identical state: their records name the same phases as
- [ ] AC-2: the fourth ticket's record names those same phases as needed and carries a
- [ ] AC-3: the first three all receive the same answer as each other
- [ ] AC-4: that answer is refusal: none of the three has its recorded state changed, and each
- [ ] AC-5: the fourth is written finished
- [ ] AC-6: the answers do not vary with the order in which the run reached the tickets, nor
- [ ] AC-7: given a second run over the same four tickets in the same state
- [ ] AC-8: it produces the same four answers as the first run

## Test Requirements

```yaml
tests:
- name: test_four_tickets_in_one_run_receive_one_answer_three_refused_one_written
  file: unit_tests/prompt_assembly/test_bo_400e_4.py
  covers:
  - BO-400e-4
  asserts: 'Drive FOUR tickets through the real workflow in ONE run: three in the
    identical state (same phases named needed, every one signed off except the same
    single phase in all three, which has no sign-off) plus a fourth whose every needed
    phase is signed off. Assert the three receive the same answer as each other, that
    the answer is refusal -- no recorded state changed, the same outstanding phase
    named in each -- and that the fourth is written finished. FOUR SEPARATE SINGLE-TICKET
    RUNS DO NOT REPRODUCE THIS: the defect is the disagreement between tickets inside
    one run, so per-ticket assertions gathered from independent runs pass on the broken
    driver.'
  framework: unittest
  type: integration
  angle: boundary
- name: test_answers_do_not_vary_with_the_order_the_run_reaches_the_four_tickets
  file: unit_tests/prompt_assembly/test_bo_400e_4.py
  covers:
  - BO-400e-4
  asserts: 'Re-drive THE SAME four-tickets-in-one-run fixture with the tickets permuted
    -- including runs in which the control ticket is reached first and last -- and
    assert each ticket''s answer is unchanged by its position. The observed split
    correlated with position in the run (ticket 01 refused, 03 and 20 written), so
    an answer that moves with order has relocated the disagreement rather than removed
    it. Must reuse the one-run fixture: permuting four independent single-ticket runs
    proves nothing.'
  framework: unittest
  type: integration
  angle: criterion
- name: test_a_second_run_over_the_same_four_tickets_produces_the_same_four_answers
  file: unit_tests/prompt_assembly/test_bo_400e_4.py
  covers:
  - BO-400e-4
  asserts: Restore the same four tickets to the same state and drive a SECOND full
    run, asserting the four answers match the first run's four answers exactly. Covers
    the criteria's second-run clause; combined with the control ticket it also proves
    the repeatability is not the trivial repeatability of a writer that refuses everything.
  framework: unittest
  type: integration
  angle: criterion
- name: test_one_answer_per_condition_is_reachable_from_a_real_workflow_run
  file: unit_tests/prompt_assembly/test_bo_400e_4.py
  covers:
  - BO-400e-4
  asserts: 'REQUIRED -- invoke the production entry point: load and execute the real
    workflow script via unit_tests/prompt_assembly/harness_build_ticket_guard.mjs,
    carrying all four tickets in one run, and assert the per-ticket record writes
    the run actually performed. Do NOT satisfy this by importing the completion helper
    and calling it four times: that is the single-ticket shape the record''s own rationale
    rules out, and it passes on the broken driver. AUTHORED DELIBERATELY (it-po):
    because this record carries an authored test_spec, the ticket generator''s own
    reachability floor would otherwise append a generic sentinel whose text asserts
    ''the AC authored no test_spec, so the entry point is not declared'' -- false
    here, and misleading on precisely the record whose entry point and run shape are
    load-bearing.'
  framework: unittest
  type: integration
  angle: reachability
```

## Implementation Notes

```yaml
config_schema_fragment: null
constraints:
- FOUR TICKETS, ONE RUN -- CARDINALITY IS THE WHOLE POINT. A per-ticket assertion
  gathered from four independent single-ticket runs does not reproduce this defect
  and does not satisfy this record. At one ticket the broken driver and the fixed
  driver are indistinguishable, because either branch is defensible in isolation;
  the defect IS the disagreement between tickets inside a single run.
- ORDER-INDEPENDENCE IS A PROPERTY OF THE DECISION, NOT OF THE FIXTURE. No state accumulated
  during a run -- a memo, a cache, a running tally, a set of already-seen phases,
  a batch index -- may influence a later ticket's answer. The observed split correlated
  with position in the run (ticket 01 refused, tickets 03 and 20 written); an implementation
  whose answer depends on how far into the run a ticket sits has not converged, it
  has moved the disagreement somewhere harder to see.
- 'DETERMINISM MUST NOT BE OBTAINED BY REFUSING EVERYTHING. ''All four the same answer''
  is a property a completely broken writer holds trivially. The fourth, fully-signed-off
  ticket is part of the contract, not a testing nicety: only the contrast makes the
  three refusals attributable to the missing sign-off.'
- THE PER-TICKET DECISION MUST BE INDIVIDUALLY OBSERVABLE. The run must expose the
  record write it performed for EACH ticket, separately. A run that reports only an
  aggregate outcome cannot be checked for agreement between its tickets, which is
  exactly the observability gap that made the original defect look like agent caprice
  for a day.
- 'VERIFY BY EXECUTION, NEVER BY GREP. Nothing about this record is visible in the
  source: the broken driver and the fixed one both contain completion code, a phase
  list and a parity check. Only reading back the four records a single run produced
  can tell them apart.'
- BOTH TWINS IN ONE CHANGE, per BO-400a-2-ii's standing constraint.
- TEMPLATES ARE BUILD SOURCES, NOT THE RUNNING ARTEFACT. Keep the deployed copy of
  the drivers in step (check-build-drift is the gate) and confirm the four-ticket
  behaviour through the deployed layout before sign-off.
n_location_rule: 2 -- the completion-write step in build-feature.js, which is the
  only driver that can exhibit the disagreement, and the twin step in build-ticket.js,
  which cannot but must not be allowed to become the second door. Both change together.
post_write_commands:
- python -m unittest discover -s unit_tests/prompt_assembly -p test_bo_400e_4.py
- python -m unittest discover -s unit_tests/prompt_assembly -p test_ticket_done_recording.py
reference_file_path: templates/workflows-js/build-feature.js
required_skills:
- python-coder
```

## Agent Contracts

### documentation-expert

Existing docs to update / cross-link:

- docs/known-issues/build-orchestration.md (relationship: describes | status: exists | relevance: KI-BO-20260831-1932 IS this case: one drive, three tickets in exactly the same state -- every phase signed off except pull-request, with no sign-off entry for it -- and the guard refused one and flipped the other two to done. The reproduction here is that observation with a control ticket added.)
- templates/workflows-js/build-feature.js (relationship: modifies | status: exists | relevance: The epic driver, the surface on which the split outcome was observed -- it is the driver that carries several tickets in one run, so it is the only place the defect is visible. TWIN of build-ticket.js.)
- templates/workflows-js/build-ticket.js (relationship: modifies | status: exists | relevance: The single-ticket twin. It cannot exhibit the disagreement itself, which is precisely why the change must land here too: otherwise the two drivers become the two doors again.)
- unit_tests/prompt_assembly/harness_build_ticket_guard.mjs (relationship: describes | status: exists | relevance: Behavioral harness. It must be driven with FOUR tickets in ONE run and must expose the record write performed for each, individually -- a per-ticket assertion aggregated across four separate runs does not reproduce the defect.)
- docs/acceptance-criteria/build-orchestration/BO-400-ticket-status-source-of-truth/BO-400a-2-iii.yaml (relationship: related | status: exists | relevance: Already uses the three-tickets-in-one-drive shape with a control case, and is the closest precedent for how this must be tested.)
- docs/architecture/adrs/ADR-006-flatten-supervisor-chain.md (relationship: describes | status: exists | relevance: Why the epic driver's own per-ticket loop -- not a supervisor agent -- is the surface that carries several tickets in one run, and therefore the only place the disagreement can occur.)
- docs/architecture/components/build-orchestration.md (relationship: describes | status: exists | relevance: The component document for the drivers -- the boundary this change stays inside.)
- docs/architecture/agent_delivery_workflows.md (relationship: describes | status: exists | relevance: The batching and dispatch topology that determines the order tickets are reached in a run -- the dimension the order-independence clause is about.)

- [ ] AC-1: sequence-diagram | docs/known-issues/build-orchestration.md | the first three all receive the same answer as each other

### Expects From

- **AC:** BO-400e-3
- **Contract:** A single close route through the checking mechanism -- the property that makes one answer per condition possible, since two routes are what produced two answers

## Sign-offs

- [ ] architect-review
- [ ] test-writer
- [ ] python-coder
- [ ] test-runner
- [ ] documentation-expert
- [ ] pr-reviewer
- [ ] ac-validator
- [ ] ac-fulfillment-gate
- [ ] documentation-verifier
- [ ] commit

## Comments
