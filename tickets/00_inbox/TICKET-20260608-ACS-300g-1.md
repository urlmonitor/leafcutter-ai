---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  pr-reviewer: needed
  pull-request: needed
  python-coder: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
components:
- ac_store
created: '2026-06-08'
depends_on: []
files_touched:
- docs/components.json
- docs/architecture/adrs/ADR-008-ac-store-schema-id-format-enforcement.md
priority: medium
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: ACS-300g-1
status: todo
title: Each backfilled component entry satisfies the minimum schema
---

# Each backfilled component entry satisfies the minimum schema

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `ACS-300g-1` — Each backfilled component entry satisfies the minimum schema — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `ACS-300g-1`. Component: `ac-store`. Assigned agent: `python-coder`. Estimated complexity: `S`.

## Acceptance Criteria

```gherkin
Given docs/components.json receives a new component entry for a subsystem
  (e.g. id: "supervisor_system"),
When the entry is validated by check_components_integrity.py,
Then it contains all required fields:
  id (string, snake_case),
  name (string),
  type (one of "infrastructure", "utility", "orchestration", "coding",
        "review", "documentation", "analysis"),
  description (string, >= 10 characters),
  status (one of "active", "reviewed", "planned"),
  and primary_code (array of >= 1 path string).
And detail_ref is either a valid path to an on-disk Markdown file
  or null (indicating no architecture doc exists yet).
```

## Sign-offs

- [ ] python-coder
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit
- [ ] pull-request

## Comments
