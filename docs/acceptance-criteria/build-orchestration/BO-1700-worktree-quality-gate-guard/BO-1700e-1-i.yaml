id: BO-1700e-1-i
components:
  - build-orchestration
readiness: approved
priority: medium
title: "The probe is found when the package is installed in a consumer subdirectory"
component: build-orchestration
level: L3
status: active
req_status: draft
work_status: todo
created_by: "docs/acceptance-criteria/build-orchestration/BO-1700-worktree-quality-gate-guard/BO-1700e.yaml"
criteria: |
  Given the package is installed inside another project in a subdirectory (for
    example `my-project/leafcutter-ai/`),
  When bootstrap resolves the path to the verification probe,
  Then it locates the probe under the subdirectory layout and runs it, rather than
    failing to find the script.
depends_on: [BO-1700e-1]
doc_links:
  - path: docs/architecture/components/worktree-manager.md
    relationship: describes
    status: exists
  - path: docs/architecture/components/bootstrap-installer.md
    relationship: describes
    status: exists
  - path: docs/architecture/adrs/ADR-001-self-hosting-boundary.md
    relationship: describes
    status: exists
assigned_agent: python-coder
estimated_complexity: S
it_requirements: |
  REUSE the existing _bootstrap build.py probe logic that already handles the
  leafcutter-ai/ subdir layout so the guard script is discoverable in a consumer
  install — do not invent a second path-resolution scheme. Implemented in the
  bootstrap path over the deployed script set (BO-1700e-1); ADR-001 deployed/template
  parity applies.
delivers_to: null
expects_from:
  ac_id: BO-1700e-1
  contract: "The deployed probe script path (scripts/commit_guardian/verify_precommit_active.py) this edge case resolves under the subdir layout."
origin_agent: business-analyst-v3
created: 2026-07-01
amended_by: []
superseded_by: null
covered_by: []
implemented_by: []
