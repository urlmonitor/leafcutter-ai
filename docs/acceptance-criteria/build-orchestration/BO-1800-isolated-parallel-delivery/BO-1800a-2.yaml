id: BO-1800a-2
readiness: approved
priority: medium
title: "Drive copies are created by the fastest safe strategy available, in a fixed preference order"
component: build-orchestration
level: L2
status: active
req_status: draft
work_status: todo
roadmap_phase: phase_1
created_by: "docs/acceptance-criteria/build-orchestration/BO-1800-isolated-parallel-delivery/BO-1800a.yaml"
criteria: |
  Given a new drive copy must be created from a warm local clone and a hub remote,
  When the isolation layer creates the copy,
  Then it attempts the creation strategies in this exact preference order and uses
    the first one that succeeds:
    1. a copy-on-write reflink copy of the warm local clone
       (`cp --reflink=auto`),
    2. else `git clone --reference <local-cache> --dissociate <hub>`,
    3. else a plain `git clone <hub>`.
  And whichever strategy is used, the resulting copy has a fully independent object
    store — it borrows no objects from the warm clone or the cache at rest (a
    `--reference` clone is always `--dissociate`d), so it is safe to garbage-collect
    on its own without risking any other copy.
depends_on: [BO-1800a]
doc_links:
  - path: docs/architecture/adrs/ADR-018-agent-isolation-topology.md
    relationship: describes
    status: exists
  - path: docs/architecture/components/worktree-manager.md
    relationship: describes
    status: exists
assigned_agent: python-coder
estimated_complexity: M
it_requirements:
  - "Clone creation MUST attempt strategies in this exact preference order and use the first that succeeds: (1) cp --reflink=auto of the warm local clone, (2) git clone --reference <local-cache> --dissociate <hub>, (3) plain git clone <hub> (ADR-018 Decision §1)."
  - "Whatever strategy is used, the resulting copy MUST have a fully independent object store that borrows no objects at rest (a --reference clone MUST always be --dissociate'd) so it is safe to gc independently."
  - "Strategy selection MUST be deterministic and MUST report which strategy was used (consumed by the fallback edge case BO-1800a-2-i and by telemetry)."
  - "Subprocess and clone I/O failures MUST be handled per the project error handling policy."
  - "The strategy logic MUST live on a portable package surface (scripts/ or templates/scripts/) deployed by build.py."
delivers_to:
  agent: python-coder
  contract: "A created, fully-dissociated independent drive copy plus a signal of which strategy was used (for the fallback edge case and telemetry)."
expects_from: null
origin_agent: it-po
created: 2026-07-06
amended_by: []
superseded_by: null
covered_by:
  - BO-1800a-2-i
implemented_by:
- tickets/00_inbox/epics/EPIC-IsolatedParallelDelivery/04_TICKET-20260707-BO-1800a-2.md
