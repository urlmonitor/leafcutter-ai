---
ac_traceability:
  id: BO-1000b-3
  path: docs/acceptance-criteria/build-orchestration/BO-1000-live-automation-progress/BO-1000b-3.yaml
advances_current_outcome: true
agents:
  commit: signed_off
  pr-reviewer: signed_off
  pull-request: needed
  python-coder: signed_off
  test-runner: needed
  test-writer: signed_off
change_target: pipeline
complexity: low
components:
- build_orchestration
created: '2026-07-20'
depends_on: []
files_touched:
- templates/workflows-js/finalize-feature.js
priority: medium
requires_adr: false
requires_diagram: false
risk_surface: internal
roadmap_phase: phase_1
source_ac: BO-1000b-3
status: todo
title: Step outcomes and the recap carry concrete result data, never a content-free
  'done'
---

# Step outcomes and the recap carry concrete result data, never a content-free 'done'

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BO-1000b-3` — Step outcomes and the recap carry concrete result data, never a content-free 'done' — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BO-1000b-3`. Component: `build-orchestration`. Assigned agent: `python-coder`. Estimated complexity: `S`. Complexity: `low`.

## Acceptance Criteria

```gherkin
Given a finalize step performed work that produced measurable results (counts,
  identifiers, or decisions such as tickets closed, source ACs closed and
  skipped, pre-existing failure counts, the pull request number, or the merge
  result),
When that step's outcome line and the final recap are emitted,
Then the outcome text states the concrete result data for the step rather than
  a content-free acknowledgement,
And a step that did real work never reports its outcome as only "ok" or "done"
  without the accompanying result data.
```

- [x] AC-1: the outcome text states the concrete result data for the step rather than
- [x] AC-2: a step that did real work never reports its outcome as only "ok" or "done"

## Test Requirements

```yaml
tests:
- name: test_outcome_text_states_concrete_result_data
  file: unit_tests/workflows/test_bo_1000b_3.py
  covers:
  - BO-1000b-3
  asserts: For a step that produced measurable results (counts, identifiers, or decisions
    such as tickets/ACs closed, pre-existing failure count, PR number, merge result),
    the outcome line and final recap state that concrete result data rather than a
    content-free acknowledgement.
  framework: unittest
  type: behavioral
- name: test_worked_step_never_reports_bare_ok_or_done
  file: unit_tests/workflows/test_bo_1000b_3.py
  covers:
  - BO-1000b-3
  asserts: A step that did real work never reports its outcome as only 'ok' or 'done'
    without the accompanying result data.
  framework: unittest
  type: behavioral
```

## Implementation Notes

```yaml
config_schema_fragment: null
n_location_rule: all
notes: "Policy constraints:\n- Each step that returns measurable results must surface\
  \ the concrete data in\n  its outcome text (e.g. pre-existing-failure count, PR\
  \ number, tickets/ACs\n  closed and skipped counts, merge result); a step that did\
  \ real work must\n  never record its outcome as only \"ok\"/\"done\".\n- Capture\
  \ the result values from each step's own return payload (not a static\n  string),\
  \ so the outcome and the recap (BO-1000b-2) reflect what actually\n  happened this\
  \ run.\n- This is an invariant over BO-1000b-1's outcome text and BO-1000b-2's recap;\n\
  \  implement it as a shared expectation on the outcome record, not per-step\n  duplicated\
  \ string logic.\n- No new config key. ADR-001 build.py round-trip parity.\n"
post_write_commands:
- python scripts/build.py --target-dir .
reference_file_path: templates/workflows-js/finalize-feature.js
required_skills:
- python-coder
```

## Agent Contracts

### Expects From

- **AC:** BO-1000b-1
- **Contract:** The per-step outcome record from BO-1000b-1; each entry's outcome text must be populated with the concrete result data this AC requires.

## Sign-offs

- [x] test-writer — 2026-07-21 11:30
- [x] python-coder — 2026-07-21 12:30
- [ ] test-runner
- [x] pr-reviewer — 2026-07-21 13:00
- [x] commit — 2026-07-21 13:20
- [ ] pull-request

## Comments

### 2026-07-21 13:20 — commit (status: ok)
feedback-id: fb_2026-07-21_3b70ff66
completion_manifest:
  pre_commit_hooks_pass: true
  commit_message_valid: true
  ticket_staged: true
Auto-authorized commit gate: subject "feat(finalize): make step outcome text carry concrete result data (BO-1000b-3)"; staged files: templates/workflows-js/finalize-feature.js, tickets/00_inbox/epics/EPIC-InFlightVisibility/04_TICKET-20260720-BO-1000a-2-i.md, tickets/00_inbox/epics/EPIC-InFlightVisibility/11_TICKET-20260720-BO-1000b-3.md, unit_tests/workflows/test_bo_1000b_3.py.

### 2026-07-21 13:00 — pr-reviewer (status: ok)
feedback-id: fb_2026-07-21_fbaa651c
completion_manifest:
  all_5_tests_green: true
  step3_pass_path_has_concrete_data: true
  step5_has_concrete_data: true
  no_high_confidence_findings: true
  medium_count_below_opus_threshold: true
All 5 AC tests pass. Step 3 pass-path embeds `baselineFailures.length` (in-scope variable, declared at line 387). Step 5 parses `syncResult` via the existing `parseAgentJson` helper and embeds `headSha`/`headMessage` with `|| 'unknown'` fallbacks on parse failure. One medium finding: `ac-fulfillment-gate.card.md` drops `components: - ac_store` as collateral from the `build.py` post-write step — corrective (registry never had this field), not regressive. No blockers.

### 2026-07-21 12:30 — python-coder (status: ok)
feedback-id: fb_2026-07-21_6651c5e8
completion_manifest:
  step3_pass_path_concrete_outcome: true
  step5_concrete_outcome_with_sha_and_message: true
  all_5_red_baseline_tests_green: true
  depends_on_invalid_reference_fixed: true
Updated finalize-feature.js: (1) Step 3 pass-path now includes `${baselineFailures !== null ? baselineFailures.length : 'N/A'}` so the outcome carries the pre-existing failure count; (2) Step 5 now parses the syncResult JSON to extract headSha and headMessage and embeds them as `${headSha || 'unknown'} — ${headMessage || 'message unknown'}` in the outcome. All 5 red_baseline tests now green. Pre-existing invalid `depends_on: [BO-1000b]` frontmatter reference cleared.
red_baseline_results:
  - test_name: test_ac1_step3_pass_path_includes_concrete_result_data
    result: green
  - test_name: test_ac1_step5_includes_concrete_result_data
    result: green
  - test_name: test_ac2_step5_never_reports_bare_static_string
    result: green
  - test_name: test_ac2_step3_pass_path_never_bare_template_without_variable_data
    result: green
  - test_name: test_ac2_steps_with_real_work_have_dynamic_outcome_text
    result: green

### 2026-07-21 11:30 — test-writer (status: ok)
feedback-id: fb_2026-07-21_d214060d
completion_manifest:
  tests_written: true
  tests_are_red: true
  no_import_or_syntax_errors: true
Wrote 5 failing tests in unit_tests/workflows/test_bo_1000b_3.py targeting the two concrete-result-data violations in finalize-feature.js: (1) Step 3 pass-path uses `Tests passed on post-merge worktree` — a bare backtick string with no ${} interpolation; (2) Step 5 uses the single-quoted static string `'Local main synced to origin/main HEAD'` — no result data. All 5 tests fail with AssertionError; none have syntax or import errors.

## Test Writer — Completion Report

### Tests Written
| File | Directory | Framework | Status |
|---|---|---|---|
| test_bo_1000b_3.py | unit_tests/workflows/ | unittest | written |

### Verification Run
- Command: python -m unittest discover -s unit_tests/workflows -t . -p "test_bo_1000b_3.py" -v
- Result: red (5 failures — expected; implementation not yet written)

### Notes
Tests parse finalize-feature.js as text (same pattern as test_bo_1000b_1.py and test_bo_1000b_2.py). The two root violations are Step 3 pass-path and Step 5; the parametric test (test_ac2_steps_with_real_work_have_dynamic_outcome_text) covers Steps 0, 3, 5, and 6 holistically. Pre-existing ticket `depends_on: [BO-1000b]` hook warning is not caused by these edits.

red_baseline:
  - test_name: test_ac1_step3_pass_path_includes_concrete_result_data
    file: unit_tests/workflows/test_bo_1000b_3.py
    error: "AssertionError: '${' not found in 'Tests passed on post-merge worktree'"
  - test_name: test_ac1_step5_includes_concrete_result_data
    file: unit_tests/workflows/test_bo_1000b_3.py
    error: "AssertionError: False is not true : Step 5 outcome text lacks concrete result data (no ${...} template literal interpolation)."
  - test_name: test_ac2_step5_never_reports_bare_static_string
    file: unit_tests/workflows/test_bo_1000b_3.py
    error: "AssertionError: <re.Match object; span=(625, 688), match=\"outcome('Step 5 of 9', 'Local main synced to orig\"> is not None"
  - test_name: test_ac2_step3_pass_path_never_bare_template_without_variable_data
    file: unit_tests/workflows/test_bo_1000b_3.py
    error: "AssertionError: '${' not found in 'Tests passed on post-merge worktree'"
  - test_name: test_ac2_steps_with_real_work_have_dynamic_outcome_text
    file: unit_tests/workflows/test_bo_1000b_3.py
    error: "AssertionError: Lists differ: ['Step 5 (local main sync): outcome() call has no ${...} interpolation — content-free static string'] != []"
