---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  llm-expert: needed
  pr-reviewer: needed
  pull-request: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
components:
- infrastructure
created: '2026-07-06'
depends_on:
- INF-600l
- INF-600l-1
files_touched:
- docs/acceptance-criteria/infrastructure/INF-600-self-describing-agents/INF-600l.yaml
- templates/agents/pr-reviewer.md
priority: high
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: INF-600l-3
status: todo
title: The pr-reviewer prose backstop for card/registry consistency delegates the
  search to research-agent
---

# The pr-reviewer prose backstop for card/registry consistency delegates the search to research-agent

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `INF-600l-3` — The pr-reviewer prose backstop for card/registry consistency delegates the search to research-agent — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `INF-600l-3`. Component: `infrastructure`. Assigned agent: `llm-expert`. Estimated complexity: `S`.

## Acceptance Criteria

```gherkin
Given a change modifies an agent's registry entry or its generated card, and the
  pr-reviewer is reviewing it as a backstop to the build-time mirror check
When the pr-reviewer's review instructions cover card<->registry consistency
Then the pr-reviewer does not attempt to search the registry and cards itself
  (it has no search tools) -- instead it delegates the card<->registry
  cross-reference to research-agent and reasons over what research-agent returns
And the review instructions frame this consistency check as a BACKSTOP to the
  authoritative build-time check (INF-600l-1), not a replacement for it
And when research-agent reports a card<->registry disagreement, the pr-reviewer
  surfaces it in the review naming both agents and the disagreeing side
```

## Sign-offs

- [ ] llm-expert
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit
- [ ] pull-request

## Comments
