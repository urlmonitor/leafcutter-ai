---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: needed
  pr-reviewer: needed
  pull-request: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
components:
- build-orchestration
created: '2026-07-07'
depends_on:
- BO-1800b-1
files_touched:
- docs/architecture/adrs/ADR-018-agent-isolation-topology.md
priority: medium
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: BO-1800b-1-i
status: todo
title: An agent's direct push to main is rejected at the hub
---

# An agent's direct push to main is rejected at the hub

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BO-1800b-1-i` — An agent's direct push to main is rejected at the hub — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BO-1800b-1-i`. Component: `build-orchestration`. Assigned agent: `documentation-expert`. Estimated complexity: `S`.

## Acceptance Criteria

```gherkin
Given an autonomous drive running in its own clone whose `origin` points at the hub,
When the agent runs `git push origin main` (or `git push origin HEAD:main`) directly,
Then the hub rejects the push,
And `main` on the hub is unchanged,
And the rejection does not depend on any local hook having fired in the clone.
```

## Sign-offs

- [ ] documentation-expert
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit
- [ ] pull-request

## Comments
