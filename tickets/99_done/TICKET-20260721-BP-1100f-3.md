---
ac_traceability:
  id: BP-1100f-3
  path: docs/acceptance-criteria/build_pipeline/BP-1100-phantom-done-prevention/BP-1100f-3.yaml
advances_current_outcome: true
agents:
  architect-review: signed_off
  commit: signed_off
  llm-expert: signed_off
  pr-reviewer: signed_off
  pull-request: needed
change_target: pipeline
complexity: medium
components:
- build_pipeline
created: '2026-07-21'
depends_on: []
files_touched:
- templates/agents/it-po.md
priority: high
requires_adr: false
requires_diagram: false
risk_surface: contract_boundary
roadmap_phase: phase_1
source_ac: BP-1100f-3
status: done
title: A work item whose stated intent contradicts its declared surface is caught
  before implementation
---

# A work item whose stated intent contradicts its declared surface is caught before implementation

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BP-1100f-3` — A work item whose stated intent contradicts its declared surface is caught before implementation — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BP-1100f-3`. Component: `build-pipeline`. Assigned agent: `llm-expert`. Estimated complexity: `M`. Complexity: `medium`.

## Acceptance Criteria

```gherkin
Given a work item whose stated intent assigns a Python implementer whose work is checked
  by a Python test framework,
And the work item's declared surface consists of JavaScript source files,
When the pre-implementation intent-versus-surface consistency check evaluates the work item,
Then it detects the contradiction and surfaces it — naming the mismatch between the Python
  implementer/checker and the JavaScript surface — before any implementer is dispatched,
And a work item whose implementer and checking technology match its declared surface passes
  the check unflagged.
```

- [ ] AC-1: the work item's declared surface consists of JavaScript source files
- [ ] AC-2: it detects the contradiction and surfaces it — naming the mismatch between the Python
- [ ] AC-3: a work item whose implementer and checking technology match its declared surface passes

## Implementation Notes

```yaml
- 'Surface is the pre-dispatch consistency lens in templates/agents/it-po.md (applied
  before the first coder dispatch): compare the work item''s stated intent (assigned
  implementer + its checking test framework) against the declared surface (files_touched
  extensions).'
- Detect a technology CONTRADICTION where the implementer/checker cannot act on the
  declared surface (e.g. a Python implementer checked by a Python test framework aimed
  at a JavaScript surface); surface the mismatch by name before any implementer is
  dispatched. A matching implementer/surface passes unflagged.
- This is DISTINCT from BP-1100a (files_touched contains an executable surface at
  all) and from BP-1100e-1 (post-change declared-vs-actual reconciliation) — do not
  re-derive either; check intent(technology)-versus-surface only.
- Per the selection_criteria DSL limits (no negation / no derived-path correlation),
  express this as an llm/judgment lens in the template, not as a DSL trigger; planning-time
  judgment degrades to advisory, so pair with a commit-time mechanical backstop only
  if a follow-up python-coder AC is authored (see caveat).
- Portable per ADR-001 — the rule lives in the packaged template and reaches any consumer
  that runs build.py; verify via the build.py round-trip.
```

## Sign-offs

- [x] architect-review
- [x] pr-reviewer
- [x] llm-expert — 2026-07-21 14:30
- [x] commit
- [ ] pull-request

## Comments

### 2026-07-21 14:30 — llm-expert (status: ok)
feedback-id: fb_2026-07-21_24581863
completion_manifest:
  template_written: true
  prompt_quality_checklist_passed: true
  convention_violations_resolved: true
Added Section S2.8 — Intent-vs-Surface Consistency Check to `templates/agents/it-po.md`, inserted between S2 Enrichment Protocol and S3 Splitting Protocol. The check partitions the declared surface into matched/unmatched portions for the assigned implementer, flags only the unmatched portion for mixed surfaces (BP-1100f-3-i, no false full-reject), and records a full-contradiction caveat when the entire surface is unmatched. The check is explicitly marked DISTINCT from BP-1100a and BP-1100e-1. Pre-existing hook violation: `depends_on: BP-1100f` in this ticket — ticket-generator artefact, not introduced by this edit.
