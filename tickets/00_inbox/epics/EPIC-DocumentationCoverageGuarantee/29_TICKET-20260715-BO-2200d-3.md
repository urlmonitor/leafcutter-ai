---
advances_current_outcome: true
agents:
  architecture-diagram-author: needed
  commit: needed
  documentation-expert: not_needed
  pr-reviewer: needed
  pull-request: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
complexity: low
components:
- build-orchestration
created: '2026-07-15'
depends_on:
- BO-2200d
- BO-2200d-2
- BO-2200b-1
files_touched: []
priority: medium
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: BO-2200d-3
status: todo
title: A sequence diagram shows the doc-required ticket phase flow through writer
  and verifier to commit
---

# A sequence diagram shows the doc-required ticket phase flow through writer and verifier to commit

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BO-2200d-3` — A sequence diagram shows the doc-required ticket phase flow through writer and verifier to commit — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BO-2200d-3`. Component: `build-orchestration`. Assigned agent: `architecture-diagram-author`. Estimated complexity: `S`. Complexity: `low`.

## Acceptance Criteria

```gherkin
Given the documentation-coverage guarantee changes the phase order for a doc-required
  ticket (documentation-expert after the coder and tests; documentation-verifier last
  before commit),
When a maintainer needs to see the runtime flow,
Then a sequence diagram exists that shows, in order, the doc-required ticket phases:
  coder -> test-runner -> documentation-expert (writes the docs) ->
  documentation-verifier (asserts the docs against the Agent Contracts brief) -> commit,
  including the verifier's blocker path that prevents commit when docs are missing or
  placeholder.
And the diagram is cross-linked from the build-orchestration and doc-compliance
  component docs.
```

## Implementation Notes

```yaml
'Author a C4/mermaid sequence diagram of the doc-required ticket phase flow:

  coder -> test-runner -> documentation-expert -> documentation-verifier -> commit,
  showing

  the verifier''s status: ok (advance to commit) and status: blocker (halt before
  commit)

  branches. This AC satisfies the sequence-diagram documentation_trigger declared
  on the

  BO-2200b and BO-2200d parent L1s (S7b documentation gate). Cross-link the diagram
  from the

  build-orchestration and doc-compliance component docs.

  '
```

## Sign-offs

- [ ] architecture-diagram-author
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit
- [ ] pull-request

## Comments
