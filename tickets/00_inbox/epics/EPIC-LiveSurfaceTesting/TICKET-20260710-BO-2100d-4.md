---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: needed
  pr-reviewer: needed
  pull-request: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
complexity: low
components:
- build_orchestration
created: '2026-07-10'
depends_on:
- BO-2100d
- BO-2100d-1
files_touched:
- docs/architecture/adrs/ADR-020-live-surface-tester.md
- docs/reference/skills-config-fields.md
priority: medium
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: BO-2100d-4
status: todo
title: How-to guide for diagnosing a live-app check that cannot run
---

# How-to guide for diagnosing a live-app check that cannot run

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BO-2100d-4` — How-to guide for diagnosing a live-app check that cannot run — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BO-2100d-4`. Component: `build-orchestration`. Assigned agent: `documentation-expert`. Estimated complexity: `S`. Complexity: `low`.

## Acceptance Criteria

```gherkin
Given an operator whose live-app check reported a loud failure
When they consult the documentation to fix it
Then a how-to guide exists that maps each loud failure to its remedy:
  a missing `requests` prerequisite, an empty or missing `startup_command`,
  and a script-path resolution failure in a worktree,
  And it tells the operator where to look (config block, environment, deployed dir)
  for each case.
```

## Implementation Notes

```yaml
- Must map each of the three loud failures (missing `requests`, empty/missing `startup_command`,
  worktree script-path resolution failure) to a concrete remedy and where to look
  (environment, config block, deployed dir).
- The failure symptoms described must match the actual error messages emitted by BO-2100d-1,
  d-2, and d-3, so the guide stays actionable.
```

## Sign-offs

- [ ] documentation-expert
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit
- [ ] pull-request

## Comments
