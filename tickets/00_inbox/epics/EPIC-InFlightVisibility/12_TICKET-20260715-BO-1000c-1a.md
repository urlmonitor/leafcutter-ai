---
advances_current_outcome: true
agents:
  commit: needed
  pr-reviewer: needed
  pull-request: needed
  python-coder: needed
  test-runner: needed
  test-writer: needed
change_target: pipeline
complexity: medium
components:
- build_orchestration
created: '2026-07-15'
depends_on:
- 01_TICKET-20260715-BO-1000a-1.md
- 07_TICKET-20260715-BO-1000b-1.md
files_touched:
- docs/architecture/components/finalize.md
- docs/architecture/adrs/ADR-017-dual-engine-workflow-support.md
priority: medium
requires_adr: false
requires_diagram: false
risk_surface: internal
roadmap_phase: phase_1
source_ac: BO-1000c-1a
status: todo
title: Background finalize appends each progress line to a durable, pollable run-progress
  journal as it happens
---

# Background finalize appends each progress line to a durable, pollable run-progress journal as it happens

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BO-1000c-1a` — Background finalize appends each progress line to a durable, pollable run-progress journal as it happens — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BO-1000c-1a`. Component: `build-orchestration`. Assigned agent: `python-coder`. Estimated complexity: `M`. Complexity: `medium`.

## Acceptance Criteria

```gherkin
Given the finalize-feature workflow is running in the background while the
  user remains in the main conversation,
When the workflow emits a start-of-step or per-step-outcome progress line,
Then that same line is appended, at the moment it is emitted, to a durable
  run-progress record for the run that a caller outside the background run can
  read while the run is still in flight,
And the record preserves the emission order of the lines and is written
  incrementally over the run rather than only flushed once at the end.
```

## Test Requirements

```yaml
tests:
- name: test_bo_1000c_1a_that_same_line_is_appended_at_the_moment
  file: unit_tests/test_bo_1000c_1a.py
  covers:
  - BO-1000c-1a
  asserts: that same line is appended, at the moment it is emitted, to a durable
```

## Implementation Notes

```yaml
config_schema_fragment: null
n_location_rule: all
notes: "Surface: the .js workflow body (templates/workflows-js/finalize-feature.js)\
  \ —\npython-coder per repo precedent (BP-1000). This is the producer half of the\n\
  split from BO-1000c-1; the launcher/relay half is BO-1000c-1b (llm-expert).\nPolicy\
  \ constraints (state WHAT, coder picks HOW):\n- Every start-of-step (BO-1000a-1)\
  \ and per-step-outcome (BO-1000b-1) line\n  must be appended to a durable run-progress\
  \ journal at the SAME point it is\n  emitted — append-as-you-go, not a single end-of-run\
  \ flush — so an external\n  poller sees lines while the run is in flight.\n- The\
  \ journal must live at a path the launcher can locate deterministically\n  for THIS\
  \ run (e.g. keyed by worktree/run id), be append-only, and preserve\n  emission\
  \ order (line ordering is the contract BO-1000c-1b relays).\n- Journal writes must\
  \ follow the project error-handling policy for file I/O\n  and must never take down\
  \ the finalize run: a journal-write failure degrades\n  to best-effort (log at WARNING)\
  \ rather than aborting the workflow.\n- No new config key is introduced by this\
  \ AC beyond the journal path\n  convention. ADR-001 build.py round-trip parity on\
  \ the template workflow.\n"
post_write_commands:
- python scripts/build.py --target-dir .
reference_file_path: templates/workflows-js/finalize-feature.js
required_skills:
- python-coder
```

## Sign-offs

- [ ] test-writer
- [ ] python-coder
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit
- [ ] pull-request

## Comments
