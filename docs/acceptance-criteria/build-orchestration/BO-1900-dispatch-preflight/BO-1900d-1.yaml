id: BO-1900d-1
title: "A payload of only allowlisted pointers is accepted"
component: build-orchestration
level: L2
status: active
req_status: active
work_status: todo
readiness: approved
priority: high
roadmap_phase: phase_1
criteria: |
  Given a dispatch payload whose keys are exactly the allowlisted pointers
    (ticket_path, worktree_path, branch, files_touched) with well-formed values,
  When the supervisor constructs the payload and preflight validates it before spawn,
  Then the payload is accepted,
  And the phase agent is spawned with only those pointer values,
  And no free-composed prose prompt is attached to the dispatch.
depends_on:
  - BO-1900d
doc_links:
  - path: docs/architecture/agent_delivery_workflows.md
    relationship: describes
    status: exists
  - path: templates/skills/building-epics/SKILL.md
    relationship: describes
    status: exists
  - path: docs/architecture/components/build-ticket-workflow-dispatch.md
    relationship: describes
    status: exists
assigned_agent: workflow-architect
estimated_complexity: M
it_requirements:
  - "The dispatch payload schema must be a closed allowlist: ticket_path, worktree_path, branch, files_touched."
  - "A payload containing only allowlisted keys with well-formed values must be accepted and spawned as-is."
delivers_to:
  agent: ticket-supervisor
  contract: "An allowlisted-pointer dispatch payload that carries no free-composed prompt"
expects_from: null
origin_agent: business-analyst
created: 2026-07-07
amended_by:
  - it-po-v3
superseded_by: null
covered_by:
  - BO-1900d-1-i
implemented_by: []
