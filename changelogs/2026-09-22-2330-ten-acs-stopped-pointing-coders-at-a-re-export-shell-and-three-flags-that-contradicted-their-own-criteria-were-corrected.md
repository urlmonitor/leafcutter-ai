---
title: "Ten ACs stopped pointing coders at a re-export shell, and three flags that contradicted their own criteria were corrected"
date: "2026-09-22"
time: "23:30"
type: manual
components: 
  - ac_store
  - build_orchestration
  - ticket_creation_pipeline
summary: "Ten not-done ACs declared scripts/ac_store/generate_ticket_from_ac.py as their edit surface after it became a re-export shell, so generated tickets sent coders to a file of import assignments. Their surfaces now name the real _gtfa_* siblings. Separately, BO-2900g-1, BO-2900g-4 and BO-2900g-2-i authored declares_side_effect: true against a derived false, and in all three the authored value was wrong."
description: "EDIT SURFACES. generate_ticket_from_ac.py is now a 386-line re-export shell carrying one real def (_sibling) plus a TYPE_CHECKING block; the implementation lives in 23 _gtfa_* siblings. Ten ACs still declared it, via doc_links at an edit-surface relationship (modifies/constrains both feed files_touched through _gtfa_constants._EDIT_SURFACE_RELATIONSHIPS) or via it_requirements.reference_file_path. A generated ticket therefore emitted files_touched naming only the shell, and because files_touched drives the surface the phantom-done guards check, a correct fix touching the real sibling would have read as touching an undeclared file. Each target was re-derived from the AC criteria and verified against the tree, not copied from a filename: BO-2000c-3/-3-i to _gtfa_files_touched.py + _gtfa_body.py; BO-2900g-1 to _gtfa_tests_section.py (the single plan-finalisation return path its n_location_rule 1 designates) plus _gtfa_test_descriptors.py; BO-2900g-4 to both descriptor-emitting modules, with its all-locations constraint rewritten to name both since reference_file_path can only name one; TKT-500f-5/-5-i to _gtfa_agents_map.py + _gtfa_config.py; the TKT-500f-6 family to _gtfa_impl_py.py, _gtfa_tests_section.py and _gtfa_body.py. The shell is deliberately KEPT on the four ACs whose work genuinely touches it, narrowed in each case to its re-export surface, module-docstring sibling list and TYPE_CHECKING block, which is exactly what commit 4dcfd87c changed there. DERIVED FLAGS. Per the 2026-09-21 five-record audit, BO-2900g-1, BO-2900g-4 and BO-2900g-2-i each asserted the content of a produced structure (what a plan contains, whether descriptions use a permitted kind, what a record carries), all satisfiable by a process that writes nothing, so the derived false is honest and the authored true was wrong. Each carries an amended_by entry with reason derived-field-correction. BO-2400g-4 and BO-2400g-4-i stay pinned deliberately: for those the derivation is what is wrong. The BO-2900g-2-ii allowlist shrank from five entries to two in the same change, because its staleness test asserts every pinned id still disagrees and would otherwise fail CI. BO-2900g-2 also had two stale claims corrected in its own notes: that it carries true (it was flipped to false in 9699d501) and that the shell is the only reader of the flag (it is _gtfa_cli.py). TEST CONTRACTS. BO-2000c-3 and BO-2000c-3-i were approved leaf code ACs with no test_spec, which check-ac-schema refuses; real descriptors were authored from their Gherkin rather than taking the reclassify-as-non-code escape, which would have been false for both. No criteria, work_status, readiness or priority were changed anywhere."
breaking: false
---

## Entry
