---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  pr-reviewer: needed
  pull-request: needed
  python-coder: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
components:
- build-orchestration
created: '2026-07-06'
depends_on:
- BO-1600d
- BO-1600d-1
- BO-1600d-3
files_touched:
- docs/acceptance-criteria/build-orchestration/BO-1600-safe-concurrent-dispatch/BO-1600d.yaml
- docs/architecture/adrs/ADR-001-self-hosting-boundary.md
- templates/scripts/git_recovery.py
- templates/docs/how-to/
priority: high
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: BO-1600d-4
status: todo
title: Any project that installs leafcutter and runs build.py gets the recovery helper
  the same way
---

# Any project that installs leafcutter and runs build.py gets the recovery helper the same way

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BO-1600d-4` — Any project that installs leafcutter and runs build.py gets the recovery helper the same way — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BO-1600d-4`. Component: `build-orchestration`. Assigned agent: `python-coder`. Estimated complexity: `M`.

## Acceptance Criteria

```gherkin
Given a fresh consumer project that has installed leafcutter and run
  build.py --target-dir .
When the consumer looks for the guided recovery helper after a corruption halt
Then the recovery entry point and its step engine are present in the consumer
  project from the build, deployed from the package source tree that build.py
  copies from (not left only in the leafcutter repository's own working tree)
And the recovery behavior available to the consumer is the same human-invoked,
  dry-run-first, confirmation-gated behavior defined in BO-1600d-1 through BO-1600d-3
And the recovery guidance a consumer relies on does not depend on the leafcutter
  repository's own root CLAUDE.md, which is not deployed to consumers
```

## Sign-offs

- [ ] python-coder
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit
- [ ] pull-request

## Comments
