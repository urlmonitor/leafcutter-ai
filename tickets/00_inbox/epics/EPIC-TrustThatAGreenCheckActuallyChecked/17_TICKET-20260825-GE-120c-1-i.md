---
ac_traceability:
  id: GE-120c-1-i
  path: docs/acceptance-criteria/guardrail-engine/GE-120-green-means-checked/GE-120c-1-i.yaml
advances_current_outcome: true
agents:
  commit: needed
  pull-request: not_needed
  test-runner: needed
  test-writer: signed_off
change_target: code
complexity: low
components:
- testing_quality
- commit_guardian
created: '2026-08-25'
depends_on: []
files_touched: []
priority: high
requires_adr: false
requires_diagram: false
risk_surface: internal
roadmap_phase: phase_1
source_ac: GE-120c-1-i
status: todo
title: The harness reports its own setup failure instead of passing vacuously
---

# The harness reports its own setup failure instead of passing vacuously

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `GE-120c-1-i` — The harness reports its own setup failure instead of passing vacuously — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `GE-120c-1-i`. Component: `guardrail-engine`. Assigned agent: `test-writer`. Estimated complexity: `S`. Complexity: `low`.

## Acceptance Criteria

```gherkin
Given the harness cannot complete the creation of its separate working copy — for example
  the copy is created but the deployed layout it needs was never built,
When the harness runs,
Then it states that no check was exercised and names the setup step that did not complete,
And it reports a failure rather than reporting that every check passed,
And a run in which zero checks were exercised is never reported as a successful sweep, even
  when no assertion failed,
And the count of checks actually exercised appears in the harness output, so a reader can
  see that the number is not zero without reading the assertions.

Coverage note: exercise this by running the harness against a deliberately incomplete setup
and asserting on the observed failure and message. This case must be run, not reasoned
about — the whole point is that the vacuous path produces no failing assertion of its own.
```

- [ ] AC-1: it states that no check was exercised and names the setup step that did not complete
- [ ] AC-2: it reports a failure rather than reporting that every check passed
- [ ] AC-3: a run in which zero checks were exercised is never reported as a successful sweep, even
- [ ] AC-4: the count of checks actually exercised appears in the harness output, so a reader can
- [ ] AC-5: asserting on the observed failure and message. This case must be run, not reasoned

## Implementation Notes

```yaml
- VACUOUS SUCCESS IS THE TARGET STATE. Zero checks exercised with zero failing assertions
  must be a FAILURE. Assert on the exercised COUNT, not on the absence of assertion
  failures — the whole defect is that the vacuous path produces no failing assertion
  of its own, so a test that waits for one will never fire.
- The exercised-check count must appear in the harness output on EVERY run, including
  successful ones. A hard failure catches a harness that breaks loudly; the printed
  count catches a harness that silently narrows its own subject list, which is the
  slower and more common decay. Compare the count against the manifest set (GE-120c-3)
  rather than against a constant.
- The setup step that did not complete must be named SPECIFICALLY — for example 'build.py
  did not produce .leafcutter/scripts/commit_guardian in <copy>' — not reported as
  a generic setup error. A generic message sends the reader to the wrong layer.
- 'Exercise this against a DELIBERATELY INCOMPLETE SETUP: a second copy created but
  never built. Do not simulate it by stubbing the harness''s own counter or by raising
  a synthetic exception — a stub reproduces the author''s belief about how setup half-fails,
  and a harness that stands up infrastructure has far more ways to half-fail than
  a check does.'
- 'PRECEDENT TO ENCODE, not merely to remember: this project has shipped this exact
  shape twice. fast-lane-build.js passed its grep-only structural tests while never
  executing its red/green gates, and the phantom-done files_touched hook passed its
  unit tests while being a no-op on every real ticket. Both were green sweeps over
  nothing. The interlock must be mechanical.'
- 'The harness is itself subject to GE-120a: a harness that cannot run reports a could-not-check
  outcome, distinguishable from a clean sweep. Reuse the shared outcome vocabulary
  rather than inventing a second one for the test layer.'
```

## Agent Contracts

### Expects From

- **AC:** GE-120c-1
- **Contract:** The harness's per-check execution loop and its reported exercised-check count.

## Sign-offs

- [x] test-writer — 2026-08-25 21:10
- [ ] test-runner
- [ ] commit

## Comments

### 2026-08-25 21:10 — test-writer (status: ok)
feedback-id: fb_2026-08-25_869bc2f7
completion_manifest:
  tests_written: true
  red_baseline_confirmed_via_real_fixture: true
  harness_module_implemented:
    result: false
    reason: "unit_tests/portability/_deployed_check_harness.py (GE-120c-1's deliverable, a separate ticket/12) does not exist yet, so DeployedCheckHarness cannot be imported."
    remediation: "Implement _deployed_check_harness.py per GE-120c-1 (ticket 12) — its run_sweep() must additionally satisfy the setup-failure contract this file documents (success=False, checks_exercised=0, failed_setup_step naming the missing artifact, count visible in message) before this ticket's tests can go green."
red_baseline:
  - test_name: test_ge120c1i_incomplete_setup_reports_failure_not_all_passed
    file: unit_tests/portability/test_ge120c1i_setup_failure_reporting.py
    error: "AssertionError: unit_tests/portability/_deployed_check_harness.py does not exist yet (or does not define DeployedCheckHarness). This module is GE-120c-1's deliverable — GE-120c-1-i (this file) additionally requires that its run_sweep() reports a named setup failure with checks_exercised == 0 and success is False when the second copy's deployed layout was never built."
  - test_name: test_ge120c1i_names_the_setup_step_that_did_not_complete
    file: unit_tests/portability/test_ge120c1i_setup_failure_reporting.py
    error: "AssertionError: unit_tests/portability/_deployed_check_harness.py does not exist yet (or does not define DeployedCheckHarness)."
  - test_name: test_ge120c1i_zero_exercised_checks_is_never_a_successful_sweep
    file: unit_tests/portability/test_ge120c1i_setup_failure_reporting.py
    error: "AssertionError: unit_tests/portability/_deployed_check_harness.py does not exist yet (or does not define DeployedCheckHarness)."
  - test_name: test_ge120c1i_exercised_check_count_present_in_output
    file: unit_tests/portability/test_ge120c1i_setup_failure_reporting.py
    error: "AssertionError: unit_tests/portability/_deployed_check_harness.py does not exist yet (or does not define DeployedCheckHarness)."
Wrote unit_tests/portability/test_ge120c1i_setup_failure_reporting.py with 4 behavioral tests covering GE-120c-1-i's four AC clauses (named setup step, failure-not-success, zero-exercised-is-never-success, count-visible-in-output). Per the AC's own mandate, each test stands up a REAL second working copy via `git worktree add` (deliberately never built — the actual incomplete-setup condition, not a stub) and calls a not-yet-implemented `DeployedCheckHarness.run_sweep()`; this is GE-120c-1's deliverable (ticket 12, still `todo` in this worktree), so all 4 tests are RED with `AssertionError` (module missing) — confirmed via `AC_ENFORCE_STRICT=1 python -m pytest unit_tests/portability/test_ge120c1i_setup_failure_reporting.py`, exit code 1, 4 failed. Note: without `AC_ENFORCE_STRICT=1` these are silently downgraded to `xfail` by `pytest_ac_enforcement` (AC work_status != done) — ran with the strict flag to get the true red baseline, per project memory on this masking behavior. The module docstring documents the full `HarnessResult`/`DeployedCheckHarness` contract GE-120c-1's implementer must satisfy. All git worktree fixtures created during the run were cleaned up (verified via `git worktree list` / `git worktree prune`, no residuals).
