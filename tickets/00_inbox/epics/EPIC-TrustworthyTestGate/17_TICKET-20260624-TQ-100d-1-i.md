---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  pr-reviewer: needed
  pull-request: needed
  python-coder: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
components:
- testing_quality
created: '2026-06-24'
depends_on:
- TQ-100d-1
files_touched:
- scripts/ac_store/known_failing_tests.py
- tests/testing_quality/test_known_failing_allowlist.py
priority: high
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: TQ-100d-1-i
status: todo
title: An allowlist entry whose expiry date has passed is flagged and fails the check
---

# An allowlist entry whose expiry date has passed is flagged and fails the check

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `TQ-100d-1-i` — An allowlist entry whose expiry date has passed is flagged and fails the check — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `TQ-100d-1-i`. Component: `testing-quality`. Assigned agent: `python-coder`. Estimated complexity: `S`.

## Acceptance Criteria

```gherkin
Given a known-failing-tests allowlist containing an entry whose expiry date is
  earlier than the current date,
When the allowlist check runs,
Then the expired entry is flagged with a message naming the entry, its test,
  and its passed expiry date
And the check fails (non-passing outcome) on the presence of the expired entry
And the expired entry no longer exempts its named test from blocking the run.
```

## Test Requirements

- `test_expired_allowlist_entry_flagged`: An allowlist entry whose expiry date has passed is flagged and fails the allowlist check.

## Sign-offs

- [ ] python-coder
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit
- [ ] pull-request

## Comments
