---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  llm-expert: needed
  pr-reviewer: needed
  pull-request: not_needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
components:
- commit_guardian
change_target: prompt
created: '2026-06-17'
depends_on: []
files_touched:
- docs/acceptance-criteria/guardrail-engine/GE-104-enforced-page-docs/GE-104a.yaml
- docs/how-to/managing-pre-commit-hooks.md
- config/agent_registry.json
- scripts/selection_criteria_evaluator.py
priority: high
requires_adr: false
requires_diagram: false
risk_surface: internal
roadmap_phase: phase_1
source_ac: GE-104a-2
status: todo
title: Planning-time trigger flips documentation-expert to needed when a ticket adds
  a new page without its reference doc
---

# Planning-time trigger flips documentation-expert to needed when a ticket adds a new page without its reference doc

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `GE-104a-2` — Planning-time trigger flips documentation-expert to needed when a ticket adds a new page without its reference doc — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `GE-104a-2`. Component: `guardrail-engine`. Assigned agent: `llm-expert`. Estimated complexity: `S`.

## Acceptance Criteria

```gherkin
Given a ticket whose files_touched list contains a new frontend page at
  site/app/<route>/page.tsx
And files_touched does NOT contain a matching reference doc at
  docs/reference/frontend/<name>.md
When agent selection resolves the status of the documentation-expert agent
  against the ticket
Then the documentation-expert agent status resolves to "needed"
  even though its default_status is "not_needed"

---

Given a ticket whose files_touched list contains a new frontend page at
  site/app/<route>/page.tsx
And files_touched ALSO contains the matching reference doc at
  docs/reference/frontend/<name>.md
When agent selection resolves the status of the documentation-expert agent
  against the ticket
Then this new-page-without-doc trigger does NOT fire on that basis
And the documentation-expert agent is not forced to "needed" by this trigger
  (no false-positive nag when the doc is already part of the planned work)

---

Given a ticket whose files_touched list contains no new frontend page at
  site/app/**/page.tsx
When agent selection resolves the status of the documentation-expert agent
  against the ticket
Then this new-page-without-doc trigger does NOT fire
And the documentation-expert agent remains at its default_status of "not_needed"
  unless some other condition independently requires it
```

## Sign-offs

- [ ] llm-expert
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit

## Comments
