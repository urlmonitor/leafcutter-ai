---
ac_traceability:
  id: BO-2500e-5
  path: docs/acceptance-criteria/build-orchestration/BO-2500-mechanical-done-proof/BO-2500e-5.yaml
advances_current_outcome: true
agents:
  ac-fulfillment-gate: needed
  ac-validator: needed
  commit: needed
  documentation-expert: needed
  documentation-verifier: needed
  pr-reviewer: needed
  pull-request: needed
  python-coder: needed
  test-runner: needed
  test-writer: needed
change_target: pipeline
complexity: medium
components:
- build_orchestration
- commit_guardian
created: '2026-08-12'
depends_on:
- BO-2500e
documentation_required: true
files_touched:
- templates/scripts/commit_guardian/check_done_proof.py
priority: medium
requires_adr: false
requires_diagram: false
risk_surface: internal
roadmap_phase: phase_1
source_ac: BO-2500e-5
status: todo
title: The pre-commit proof-of-done check covers JS-covered ACs
---

# The pre-commit proof-of-done check covers JS-covered ACs

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BO-2500e-5` — The pre-commit proof-of-done check covers JS-covered ACs — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BO-2500e-5`. Component: `build-orchestration`. Assigned agent: `python-coder`. Estimated complexity: `M`. Complexity: `medium`.

## Acceptance Criteria

```gherkin
Given a developer commits locally with staged work whose ACs are covered by "// covers:<id>"
  vitest tests,
When the proof-of-done pre-commit check runs,
Then its verdict includes those JS-covered ACs (running the JS test runner as needed) alongside
  any Python-covered ACs, reporting pass or fail for each,
And it remains skippable locally (SKIP=<hook-id> / git --no-verify) as fast feedback,
So local proof-of-done feedback covers front-end work, not only Python work.
```

- [ ] AC-1: its verdict includes those JS-covered ACs (running the JS test runner as needed) alongside
- [ ] AC-2: it remains skippable locally (SKIP=<hook-id> / git --no-verify) as fast feedback

## Test Requirements

```yaml
tests:
- name: test_precommit_verdict_includes_js_covered_acs
  file: unit_tests/commit_guardian/test_bo_2500e_5.py
  covers:
  - BO-2500e-5
  asserts: The pre-commit proof-of-done check reports pass/fail for JS-covered ACs
    (running vitest as needed) alongside Python-covered ACs.
  framework: unittest
  type: unit
- name: test_precommit_js_check_is_skippable
  file: unit_tests/commit_guardian/test_bo_2500e_5.py
  covers:
  - BO-2500e-5
  asserts: The check remains bypassable locally via SKIP=<hook-id> / --no-verify and
    does not hard-block the commit.
  framework: unittest
  type: unit
```

## Implementation Notes

```yaml
config_schema_fragment:
  proof_of_done_precommit_js_coverage:
    description: The pre-commit proof-of-done hook's verdict includes JS-covered ACs
      — it runs the JS test runner (vitest) as needed and reports pass/fail for each
      JS- and Python-covered AC, while remaining skippable locally (SKIP=<hook-id>
      / --no-verify).
    type: object
constraints:
- The hook must consume the JS-inclusive done-proof verdict from the BO-2500e engine
  (done_proof.py) rather than re-deriving covers/pass semantics, so local and CI verdicts
  stay identical.
- 'The hook must remain non-blocking locally: it reports pass/fail and is honored
  by SKIP=<hook-id> and git --no-verify (fast feedback, not the final gate).'
- On failure it must print the specific AC ids and reasons (missing/failing/non-passing
  linked test, unavailable runner) for both JS- and Python-covered ACs, per the error
  handling policy.
- 'Deploy-manifest rule: any module this hook imports (done_proof.py and any new JS-runner
  helper) MUST be present in the build deploy map so the DEPLOYED hook does not crash
  with ModuleNotFoundError; verify by running the deployed hook after python scripts/build.py,
  not only the source-tree unit tests.'
- Invoking vitest must stay within the pre-commit latency budget; scope the JS run
  to the staged/linked tests and do not run the full suite when a scoped check suffices.
n_location_rule: '1'
post_write_commands:
- python scripts/build.py
reference_file_path: templates/scripts/commit_guardian/check_done_proof.py
required_skills:
- python-coder
```

## Agent Contracts

### documentation-expert

Existing docs to update / cross-link:

- docs/architecture/components/build-orchestration.md (relationship: describes | status: exists)
- docs/architecture/components/commit-guardian.md (relationship: describes | status: exists)
- docs/architecture/adrs/ADR-004-tdd-workflow-enforcement.md (relationship: describes | status: exists)

- [ ] AC-1: [how-to] docs/architecture/components/build-orchestration.md — its verdict includes those JS-covered ACs (running the JS test runner as needed) alongside
- [ ] AC-2: [sequence-diagram] docs/architecture/components/build-orchestration.md — its verdict includes those JS-covered ACs (running the JS test runner as needed) alongside

### Expects From

- **AC:** BO-2500e-2
- **Contract:** Per-AC JS eligibility/pass-fail verdict { ac_id, can_be_done, reason } from the JS-inclusive done-proof engine, which the pre-commit hook renders as local pass/fail feedback.

## Sign-offs

- [ ] test-writer
- [ ] python-coder
- [ ] test-runner
- [ ] documentation-expert
- [ ] pr-reviewer
- [ ] ac-validator
- [ ] ac-fulfillment-gate
- [ ] documentation-verifier
- [ ] commit
- [ ] pull-request

## Comments
