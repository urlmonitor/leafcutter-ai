---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  pr-reviewer: needed
  pull-request: not_needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
  workflow-architect: needed
complexity: medium
components:
- build_orchestration
created: '2026-07-08'
depends_on:
- BO-1900d-2
files_touched:
- docs/architecture/agent_delivery_workflows.md
- docs/architecture/components/build-ticket-workflow-dispatch.md
priority: high
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: BO-1900d-2-i
status: todo
title: A premise injected inside an allowlisted pointer value is rejected
---

# A premise injected inside an allowlisted pointer value is rejected

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BO-1900d-2-i` — A premise injected inside an allowlisted pointer value is rejected — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BO-1900d-2-i`. Component: `build-orchestration`. Assigned agent: `workflow-architect`. Estimated complexity: `M`. Complexity: `medium`.

## Acceptance Criteria

```gherkin
Given a dispatch payload whose keys are all allowlisted but where a pointer value has
  been overloaded to smuggle a prose instruction or a failure premise (for example a
  files_touched entry that embeds a sentence, or a branch value carrying appended text),
When preflight validates the payload before spawn,
Then the payload is rejected because the pointer value does not match the expected shape
  for that pointer (a path, a branch name, or a path list),
And the held-back reason names the malformed pointer,
And no phase agent is spawned.
```

## Sign-offs

- [ ] workflow-architect
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit

## Comments
