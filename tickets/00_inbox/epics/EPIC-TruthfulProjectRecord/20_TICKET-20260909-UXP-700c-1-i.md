---
ac_traceability:
  id: UXP-700c-1-i
  path: docs/acceptance-criteria/ux-prototyping/UXP-700-truthful-project-record/UXP-700c-1-i.yaml
advances_current_outcome: true
agents:
  ac-fulfillment-gate: needed
  ac-validator: needed
  architect-review: needed
  commit: needed
  documentation-expert: needed
  documentation-verifier: needed
  frontend-coder: not_needed
  pr-reviewer: needed
  pull-request: not_needed
  python-coder: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
  user-surface-smoker: not_needed
change_target: code
complexity: low
components:
- ux_prototyping
created: '2026-09-09'
depends_on: []
documentation_required: true
files_touched: []
priority: medium
requires_adr: false
requires_diagram: false
risk_surface: contract_boundary
roadmap_phase: phase_1
source_ac: UXP-700c-1-i
status: todo
title: A pointer the checker cannot classify is reported as unresolvable, never as
  sound
---

# A pointer the checker cannot classify is reported as unresolvable, never as sound

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `UXP-700c-1-i` — A pointer the checker cannot classify is reported as unresolvable, never as sound — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `UXP-700c-1-i`. Component: `ux-prototyping`. Assigned agent: `python-coder`. Estimated complexity: `S`. Complexity: `low`.

## Acceptance Criteria

```gherkin
Given a pointer whose target lies outside the kinds of target the checker knows how to
  resolve,
When the record is checked,
Then that pointer is reported as not resolvable, naming the artifact that holds it and the
  reason the target could not be classified,
And it is excluded from the stated count of pointers resolved,
And it is not reported as a broken pointer,
And the run's outcome is not the outcome for a record that was checked and found sound while
  any pointer is unresolvable.
```

- [ ] AC-1: that pointer is reported as not resolvable, naming the artifact that holds it and the
- [ ] AC-2: it is excluded from the stated count of pointers resolved
- [ ] AC-3: it is not reported as a broken pointer
- [ ] AC-4: the run's outcome is not the outcome for a record that was checked and found sound while

## Test Requirements

```yaml
tests:
- name: test_unclassifiable_pointer_is_reported_as_unresolvable
  file: unit_tests/product_truth/test_uxp_700c_1_i.py
  covers:
  - UXP-700c-1-i
  asserts: A pointer of an unknown target kind is reported as unresolvable with a
    stated reason.
  framework: unittest
  type: unit
  angle: boundary
- name: test_unresolvable_pointer_is_in_neither_the_resolved_nor_the_broken_set
  file: unit_tests/product_truth/test_uxp_700c_1_i.py
  covers:
  - UXP-700c-1-i
  asserts: The unresolvable pointer is excluded from the resolved count and absent
    from the broken list.
  framework: unittest
  type: unit
  angle: boundary
- name: test_uxp_700c_1_i_reachable_from_entry_point
  file: unit_tests/product_truth/test_uxp_700c_1_i.py
  covers:
  - UXP-700c-1-i
  angle: reachability
  asserts: 'REQUIRED — invoke the production entry point (CLI, hook, slash command,
    workflow dispatch, or main()) as a subprocess/dispatch and assert the new behaviour
    actually occurs. Do NOT satisfy this by importing the function directly. The AC
    authored no test_spec, so the entry point is not declared: resolve it before writing
    this test, and do not delete this entry.'
```

## Agent Contracts

### documentation-expert

Existing docs to update / cross-link:

- docs/acceptance-criteria/guardrail-engine/GE-120-green-means-checked/GE-120.yaml (relationship: context | status: exists)

- [ ] AC-1: (unspecified genre) | docs/acceptance-criteria/guardrail-engine/GE-120-green-means-checked/GE-120.yaml | that pointer is reported as not resolvable, naming the artifact that holds it and the

### Expects From

- **AC:** UXP-700c-1
- **Contract:** The pointer verdict this record adds a third value to.

## Sign-offs

- [ ] architect-review
- [ ] test-writer
- [ ] python-coder
- [ ] test-runner
- [ ] documentation-expert
- [ ] pr-reviewer
- [ ] ac-validator
- [ ] ac-fulfillment-gate
- [ ] documentation-verifier
- [ ] commit

## Comments
