---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  pr-reviewer: needed
  pull-request: needed
  python-coder: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
components:
- guardrail-engine
created: '2026-06-16'
depends_on:
- GE-100
- GE-100d
files_touched:
- docs/acceptance-criteria/guardrail-engine/GE-100-code-quality-hooks/GE-100.yaml
- docs/pre-commit-hooks.md
- docs/how-to/managing-pre-commit-hooks.md
priority: high
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: GE-100e
status: todo
title: diff-cover hook reports uncovered lines in changed files against the configured
  threshold
---

# diff-cover hook reports uncovered lines in changed files against the configured threshold

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `GE-100e` — diff-cover hook reports uncovered lines in changed files against the configured threshold — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `GE-100e`. Component: `guardrail-engine`. Assigned agent: `python-coder`. Estimated complexity: `M`.

## Acceptance Criteria

```gherkin
Given the commit guardian configuration has the diff_coverage section with:
  enabled: true
  strict: false
  min_coverage_percent: 80
  coverage_xml_path: "coverage.xml"
  compare_branch: "origin/main"
  max_age_seconds: 3600
And the diff-cover binary is installed
And a valid coverage.xml exists that was generated less than 3600 seconds ago
And the developer has staged Python files whose diff coverage against
  origin/main measures at 65% (below the 80% threshold)
When the diff-cover hook runs at pre-commit time
Then the hook emits a warning listing the files with insufficient coverage
  and their individual coverage percentages
And the warning states the overall diff coverage (65%) vs the threshold (80%)
And because strict is false, the hook exits with code 0 (warn-only)
```

## Sign-offs

- [ ] python-coder
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit
- [ ] pull-request

## Comments
