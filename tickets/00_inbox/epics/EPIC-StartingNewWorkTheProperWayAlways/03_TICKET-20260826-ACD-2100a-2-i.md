---
ac_coverage: 4/4
ac_traceability:
  id: ACD-2100a-2-i
  path: docs/acceptance-criteria/ac-driven-dev/ACD-2100-entry-point-unblocked/ACD-2100a-2-i.yaml
advances_current_outcome: true
agents:
  ac-fulfillment-gate: signed_off
  ac-validator: signed_off
  commit: signed_off
  documentation-expert: not_needed
  documentation-verifier: not_needed
  pr-reviewer: signed_off
  pull-request: needed
  python-coder: signed_off
  test-runner: signed_off
  test-writer: signed_off
change_target: pipeline
complexity: low
components:
- ac_driven_dev
- worktree_manager
created: '2026-08-26'
depends_on:
- 02_TICKET-20260826-ACD-2100a-2.md
documentation_required: false
files_touched:
- templates/scripts
- templates/scripts/setup_ticket_worktree.py
priority: medium
requires_adr: false
requires_diagram: false
risk_surface: internal
roadmap_phase: phase_1
source_ac: ACD-2100a-2-i
status: todo
title: An ambiguous repository location stops the setup step instead of being guessed
---

# An ambiguous repository location stops the setup step instead of being guessed

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `ACD-2100a-2-i` — An ambiguous repository location stops the setup step instead of being guessed — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `ACD-2100a-2-i`. Component: `ac-driven-dev`. Assigned agent: `python-coder`. Estimated complexity: `S`. Complexity: `low`.

## Acceptance Criteria

```gherkin
Given a copy of the worktree setup step placed in a directory that is not inside
  any git repository,
And two or more git repositories exist among the immediate subdirectories of
  that directory,
When the setup step is executed from that directory with no repository location
  supplied by the caller,
Then it exits with a failure status and creates no worktree under any of the
  candidate repositories,
And its failure names every candidate it found and states that it will not
  choose between them,
And no branch and no directory is left behind in any candidate repository by the
  attempt.
```

- [x] AC-1: two or more git repositories exist among the immediate subdirectories of
- [x] AC-2: it exits with a failure status and creates no worktree under any of the
- [x] AC-3: its failure names every candidate it found and states that it will not
- [x] AC-4: no branch and no directory is left behind in any candidate repository by the

## Test Requirements

```yaml
tests:
- name: test_two_candidate_repositories_refuse_and_create_nothing
  file: unit_tests/ac_driven_dev/test_acd_2100a_2_i.py
  covers:
  - ACD-2100a-2-i
  asserts: A non-repository directory with two real git repositories among its immediate
    subdirectories; the subprocess exits non-zero and no worktree directory exists
    under either candidate afterwards.
  framework: unittest
  type: integration
  angle: criterion
- name: test_refusal_names_every_candidate_it_found
  file: unit_tests/ac_driven_dev/test_acd_2100a_2_i.py
  covers:
  - ACD-2100a-2-i
  asserts: The failure output contains the location of both candidate repositories
    and states that the step will not choose between them.
  framework: unittest
  type: integration
  angle: criterion
- name: test_ambiguous_refusal_leaves_no_branch_in_any_candidate
  file: unit_tests/ac_driven_dev/test_acd_2100a_2_i.py
  covers:
  - ACD-2100a-2-i
  asserts: After the refusal, `git branch` in each candidate repository lists exactly
    the branches it had before the attempt, and no new directory is present in either.
  framework: unittest
  type: integration
  angle: failure
- name: test_refusal_is_reached_through_the_command_line_entry_point
  file: unit_tests/ac_driven_dev/test_acd_2100a_2_i.py
  covers:
  - ACD-2100a-2-i
  asserts: The refusal is produced by running the script's real command-line entry
    point in a fresh process, and its non-zero exit status is what the caller observes.
  framework: unittest
  type: integration
  angle: reachability
```

## Implementation Notes

```yaml
- The change must land under templates/scripts/ alongside ACD-2100a-2's resolver;
  the deployed copy is build output.
- 'Fail closed: an ambiguous resolution is a non-zero exit, never a warning that proceeds.
  A worktree created in the wrong project is silent and expensive to unpick.'
- 'The refusal must be atomic with respect to the filesystem: the resolution must
  complete before any branch is created or any directory is written, so a partial
  attempt cannot leave residue in a candidate repository.'
- The refusal message must enumerate every candidate found. A bare 'could not determine
  the repository' sends the operator back to the same search by hand and is not distinguishable
  by a test from a resolver that simply failed.
- 'Must satisfy the repository error-handling policy: raise a typed error rather than
  returning a sentinel that a caller can ignore.'
- 'Must be idempotent: rerunning after the refusal must produce the same refusal,
  not a different outcome caused by residue from the first attempt.'
```

## Agent Contracts

### documentation-expert

Existing docs to update / cross-link:

- docs/reference/plan-feature-layout-and-startup-checks.md (relationship: describes | status: planned | relevance: The reference page authored by ACD-2100d-4 must tell an operator what an ambiguous-layout refusal means and how to resolve it.)
- docs/known-issues/ac-driven-dev.md (relationship: describes | status: exists | relevance: KI-ACD-004's fix direction names the exactly-one rule: 'accept the result only when exactly one is found; otherwise re-raise'.)
- templates/scripts/setup_ticket_worktree.py (relationship: implements | status: exists | relevance: Canonical source of the resolver whose ambiguous case this pins.)
- docs/architecture/components/worktree-manager.md (relationship: describes | status: exists | relevance: Component doc for the surface that must refuse rather than guess.)

This record owns none of the documents above -- every entry is a cross-link to a
document another record produces. The contract line that stood here demanded
`docs/reference/plan-feature-layout-and-startup-checks.md`, which ACD-2100d-4
creates; this ticket could never have satisfied it. Removed per BO-2200c-5-i
and -iii; the list above is retained as reading for the implementer.

### Expects From

- **AC:** ACD-2100a-2
- **Contract:** A repository resolver whose search step returns its full candidate set rather than a single first hit, so that 'two or more candidates' is a state this record can act on rather than one the resolver has already collapsed.

## Sign-offs

- [x] test-writer — 2026-08-31 09:30
- [x] python-coder — 2026-08-31 10:15
- [x] test-runner — 2026-08-31 11:00
- [x] pr-reviewer — 2026-08-31 14:58
- [x] ac-validator — 2026-08-31 15:30
- [x] ac-fulfillment-gate — 2026-08-31 16:10
- [x] commit — 2026-08-31 16:45
- [ ] pull-request

## Comments

### 2026-08-31 09:30 — test-writer (status: ok)
feedback-id: fb_2026-08-31_0eb1bc9d
completion_manifest:
  tests_written: true
  red_baseline_confirmed: true
  ac_store_test_spec_used: true
red_baseline:
  - test_name: test_refusal_names_every_candidate_it_found
    file: unit_tests/ac_driven_dev/test_acd_2100a_2_i.py
    error: "AssertionError: 'not choose' not found in \"ERROR: subprocess failed: Failed to resolve a repository: the anchor at .../script-location is not inside a git repository, and a bounded search of the immediate subdirectories of ... found 2 candidate repositories (need exactly 1): [PosixPath('.../candidate-repo-a'), PosixPath('.../candidate-repo-b')]\\n\" : the failure must state that the step will not choose between the candidates it found (a bare candidate count or 'need exactly 1' is not a refusal-to-choose statement)"
Wrote 4 real-subprocess integration tests in `unit_tests/ac_driven_dev/test_acd_2100a_2_i.py` per the AC store `test_spec` for ACD-2100a-2-i (source_ac), constructing a genuinely non-repository tmp base directory with TWO real, independently-committed git repositories among its immediate subdirectories and invoking the real CLI entry point (`create-only`) as a subprocess with no `--repo-root` -- no internal resolver is imported or mocked. **TDD-order finding (per this repo's "TDD Order" CLAUDE.md convention):** verified with `AC_ENFORCE_STRICT=1 python -m pytest` (the default run silently downgrades these to xfail via the AC-enforcement plugin since ACD-2100a-2-i is `work_status: todo`) that 3 of the 4 tests are ALREADY GREEN — `test_two_candidate_repositories_refuse_and_create_nothing`, `test_ambiguous_refusal_leaves_no_branch_in_any_candidate`, and `test_refusal_is_reached_through_the_command_line_entry_point` all pass against the current `templates/scripts/setup_ticket_worktree.py`, because `_resolve_repository_with_search_fallback()` (added by the sibling ticket `ACD-2100a-2`, which python-coder's own sign-off comment there already anticipated would handle "zero or multiple candidates raise, surfacing the full candidate set") already refuses non-1-candidate results with a `subprocess.SubprocessError` before any branch or worktree is created, and `main()` already converts that into a clean non-zero exit naming both candidate paths. I did not weaken these three tests to force an artificial red — per the AC's own `test_rationale` ("this is not covered at all" refers to the *absence* of a dedicated assertion, not to today's implementation being wrong) they are exactly the regression tests this record exists to add: they will catch a future regression to a first-hit-wins search, which is the "dangerous implementation" the AC text calls out. Only `test_refusal_names_every_candidate_it_found` is genuinely RED: the current refusal message enumerates the candidate count and raw candidate list ("need exactly 1: [...]") but contains no phrase stating the step will not choose between the candidates, which AC-3 requires verbatim ("states that it will not choose between them") — see `red_baseline` above for the exact assertion text from the strict run. Flagging for python-coder: the fix is a wording change to the `raise subprocess.SubprocessError(...)` message inside `_resolve_repository_with_search_fallback()`'s ambiguous branch (the `except subprocess.SubprocessError:` block, `len(candidates) != 1` path) — add an explicit refusal-to-choose clause (e.g. containing the substring "will not choose") alongside the existing candidate enumeration; no other behavioural change is required by this ticket's four tests. No `### test-writer` heading exists under `## Agent Contracts` and no `## AC Coverage` table exists in this ticket body, so §2c of the signoff skill does not apply.

### 2026-08-31 10:15 — python-coder (status: ok)
feedback-id: fb_2026-08-31_e35eea89
completion_manifest:
  fix_applied: true
  red_baseline_all_green: true
  no_other_tests_regressed: true
red_baseline_results:
  - test_name: test_refusal_names_every_candidate_it_found
    result: green
  - test_name: test_two_candidate_repositories_refuse_and_create_nothing
    result: green (was already green; regression guard per test-writer's note)
  - test_name: test_ambiguous_refusal_leaves_no_branch_in_any_candidate
    result: green (was already green; regression guard per test-writer's note)
  - test_name: test_refusal_is_reached_through_the_command_line_entry_point
    result: green (was already green; regression guard per test-writer's note)
Fixed the single red test per test-writer's flagged finding: added an explicit
refusal-to-choose clause ("and will not choose between them") to the
`subprocess.SubprocessError` message raised in
`_resolve_repository_with_search_fallback()`'s ambiguous branch
(`templates/scripts/setup_ticket_worktree.py`), alongside the existing candidate
enumeration -- no other behavioural change. Verified with
`AC_ENFORCE_STRICT=1 python3 -m pytest unit_tests/ac_driven_dev/test_acd_2100a_2_i.py -q`:
all 4 tests pass. No `### python-coder` heading exists under `## Agent Contracts`
and no `## AC Coverage` table exists in this ticket body, so §2c of the signoff
skill does not apply (confirmed per test-writer's same finding). doc-enforcer and
complexity-reduction skills were not invocable as dedicated tools in this session
(only Bash/Read/Edit/Write/Agent available); manually verified the change is a
single-line string-literal edit inside an already-documented, already-simple
function with no new functions, no docstring changes, and no complexity increase
(same branch structure, same line count class).

### 2026-08-31 14:58 — pr-reviewer (status: ok)
feedback-id: fb_2026-08-31_5e7e71ff
completion_manifest:
  review_run: true
  high_confidence_findings: 0
  medium_confidence_findings: 0
Reviewed the staged diff (single-line refusal-message wording change in
`_resolve_repository_with_search_fallback()`'s ambiguous branch, plus the 4 new
real-subprocess integration tests in `unit_tests/ac_driven_dev/test_acd_2100a_2_i.py`).
Verified independently: `AC_ENFORCE_STRICT=1 python3 -m pytest unit_tests/ac_driven_dev/`
(27 passed, includes this ticket's 4 tests and the sibling ACD-2100a-2 suite), `ruff
check` on both changed files (clean), confirmed `main()` actually surfaces the
`subprocess.SubprocessError` message to stderr with exit 1 (not swallowed), and
grepped for stale assertions on the old "need exactly 1" wording (none found). No
high- or medium-confidence findings; this is a minimal, well-scoped, fully
test-verified change. No `### pr-reviewer` heading exists under `## Agent Contracts`
and this ticket's contracts contain no checkbox `AC-N` lines, so contract-aware mode
and §2c do not apply (v1-style review).

### 2026-08-31 11:00 — test-runner (status: ok)
feedback-id: fb_2026-08-31_885fce9a
completion_manifest:
  test_suite_executed: true
  all_tests_passing: true
  failure_report_structured: true
Ran `unit_tests/ac_driven_dev/test_acd_2100a_2_i.py` with `AC_ENFORCE_STRICT=1
python3 -m pytest` (this repo does not match the generic live_trader/sql_functions
routing table, so I targeted the ticket's own test file plus its named sibling
`test_acd_2100a_2.py` for a regression check) -- all 4 tests in this ticket's
file pass, and the 4 sibling tests remain green (8 passed, 0 failed, 0.61s /
1.44s combined). No `### test-runner` heading exists under `## Agent Contracts`
and no `## AC Coverage` table exists in this ticket body, so §2c does not apply
(same finding as test-writer and python-coder).

### 2026-08-31 15:30 — ac-validator (status: ok)
feedback-id: (submit-failed)
completion_manifest:
  acs_covered: 4/4
  acs_partial: 0
  acs_missing: 0
  store_alignment_check: pass (exit 0)
All 4 ACs covered with concrete implementation and test evidence found in the
staged diff (`git diff --cached`):

AC-1 (two or more git repositories among the immediate subdirectories) covered:
  implementation: `templates/scripts/setup_ticket_worktree.py` —
    `_search_immediate_subdirectory_repos()` / `_resolve_repository_with_search_fallback()`
    (lines 196-226, 229-282) build the full `candidates: list[Path]` set from
    immediate subdirectories rather than a first hit — this is exactly the
    contract this ticket's "Expects From" entry names as delivered by sibling
    ticket ACD-2100a-2.
  test: `_AmbiguousTwoCandidateScenarioTestCase.setUp` in
    `unit_tests/ac_driven_dev/test_acd_2100a_2_i.py:140-185` constructs two
    real, independently-committed git repos (`candidate-repo-a`,
    `candidate-repo-b`) as immediate subdirectories of a genuinely
    non-repository base dir — exercised by all 4 test methods.

AC-2 (exits with a failure status, creates no worktree under any candidate) covered:
  implementation: `templates/scripts/setup_ticket_worktree.py` lines 266-282
    (`len(candidates) != 1` branch raises `subprocess.SubprocessError`) plus
    `main()` lines 1568-1585 (`except subprocess.SubprocessError: ... sys.exit(1)`).
  test: `test_two_candidate_repositories_refuse_and_create_nothing`
    (`unit_tests/ac_driven_dev/test_acd_2100a_2_i.py:201-234`) — asserts
    non-zero exit and identical `git worktree list --porcelain` output for
    both candidates before/after.

AC-3 (failure names every candidate and states it will not choose) covered:
  implementation: staged diff hunk in `templates/scripts/setup_ticket_worktree.py`
    (`git diff --cached`) — the ambiguous-branch message changed from
    `"...(need exactly 1): {candidates}"` to
    `"...(need exactly 1) and will not choose between them: {candidates}"`.
  test: `test_refusal_names_every_candidate_it_found`
    (`unit_tests/ac_driven_dev/test_acd_2100a_2_i.py:241-281`) — asserts both
    candidate paths and the substring `"not choose"` appear in combined
    stdout+stderr. This was test-writer's flagged RED baseline and is now
    green (verified independently below).

AC-4 (no branch and no directory left behind in any candidate) covered:
  implementation: the same lines 266-282 — the raise occurs inside
    `_resolve_repository_with_search_fallback()`, which is called (line 1265)
    before `cmd_create_only()` performs any branch/worktree creation, so the
    refusal is atomic with respect to the filesystem.
  test: `test_ambiguous_refusal_leaves_no_branch_in_any_candidate`
    (`unit_tests/ac_driven_dev/test_acd_2100a_2_i.py:288-339`) — asserts
    identical `git branch --list -a` output and identical top-level directory
    entries for both candidates before/after.

Independently re-ran `AC_ENFORCE_STRICT=1 python3 -m pytest
unit_tests/ac_driven_dev/test_acd_2100a_2_i.py -q`: 4 passed. Cross-file
contract trace (Expects From → ACD-2100a-2): read
`templates/scripts/setup_ticket_worktree.py` end-to-end from the resolver
(line 196) through `main()` (line 1568) and confirmed the full candidate set
the resolver returns is the same set the ambiguous-branch raise enumerates —
no contract gap. Ran
`python3 scripts/commit_guardian/check_v2_ac_store_alignment.py --ticket
<this ticket>`: exit 0, no store-alignment violations. Flipped AC-1..AC-4
checkboxes to `[x]` and set `ac_coverage: 4/4` in frontmatter; no
`## AC Coverage` table exists in this ticket body (confirmed by
test-writer/python-coder/test-runner above), so that sub-step is
inapplicable. `submit_feedback.py` rejected the write
(`ac-validator` is absent from `allowed_writers` for every category in
`config/feedback_categories.yaml` — a pre-existing gap, not something this
phase can fix) — recorded `feedback-id: (submit-failed)` per protocol and
continued. All ACs covered; commit phase may proceed.

### 2026-08-31 16:45 — commit (status: ok)
feedback-id: fb_2026-08-31_ff661daa
completion_manifest:
  pre_commit_hooks_pass: true
  commit_message_valid: true
  ticket_staged: true
Auto-authorized commit gate (supervised path, `ticket_path` provided; commit-phase
serialization lock held by ticket-supervisor plus upstream ac-validator /
ac-fulfillment-gate / pr-reviewer sign-offs already establish authorization —
no interactive confirmation issued per protocol). Ran the BO-1700d-3 pre-commit
probe (`scripts/commit_guardian/verify_precommit_active.py --json`) with cwd set
to the worktree root: `binary/config/git_hook/canary` all `true`, `failing_checks: []`
(the first invocation with the harness's default cwd falsely reported `git_hook:
false` because the probe resolves hooks from `Path.cwd()` rather than the target
worktree — a probe artifact, not a real hook gap; the common `.git/hooks/pre-commit`
in `leafcutter-ai/.git` is present and fresh). Staged files (all previously staged
by upstream phases, none added by this phase):
`docs/acceptance-criteria/ac-driven-dev/ACD-2100-entry-point-unblocked/ACD-2100a-2-i.yaml`,
`templates/scripts/setup_ticket_worktree.py`,
`tickets/00_inbox/epics/EPIC-StartingNewWorkTheProperWayAlways/03_TICKET-20260826-ACD-2100a-2-i.md`,
`unit_tests/ac_driven_dev/test_acd_2100a_2_i.py`. Verified the diff against the
commit message before committing: the source diff is the one-line refusal-message
wording addition (`and will not choose between them`) in
`_resolve_repository_with_search_fallback()`'s ambiguous branch, exactly as
python-coder's sign-off describes. Committed via `COMMIT_AGENT_MODE=1 git commit`
(subject: `fix(ac-driven-dev): name every candidate and refuse to choose between
them`). `submit_feedback.py` accepted the write this time (`commit` is an
allowed_writer for the `complete` category) — `feedback-id: fb_2026-08-31_ff661daa`.
Commit phase complete; ticket handed to `pull-request`.

### 2026-08-31 16:10 — ac-fulfillment-gate (status: ok)
feedback-id: (submit-failed)
completion_manifest:
  ac_resolved: true
  work_status_verified: true
  implemented_by_verified: true
  covered_by_verified: true
Resolved AC `ACD-2100a-2-i` via the shared coverage resolver (block form `{id, path}`,
one AC in the working list, `level: L3`). The resolver's raw JSON flagged both
`work_status` and `covered_by` as failed fields, but the resolver applies the same
non-empty check to `covered_by` regardless of level; per this gate's own Step 2f,
`covered_by` is exempt for L3 ACs (empty is permitted), so that flag is not a
blocker. `work_status` was genuinely `todo` and `implemented_by` listed only the
ticket file itself with no intersection against `files_touched` (`templates/scripts`,
`templates/scripts/setup_ticket_worktree.py`) ∩ branch diff (`git diff
main...HEAD --name-only`, which includes `templates/scripts/setup_ticket_worktree.py`
as a staged change). Auto-fixed both, append-only: set `work_status: done`, and
appended `templates/scripts/setup_ticket_worktree.py` to `implemented_by` (existing
entry left untouched). Ran `scripts/commit_guardian/check_ac_schema.py` on the edited
AC YAML afterward: exit 0 (fell back to manual field validation with a benign warning
that `config/ac_store_schema.json` was not found at this workspace root -- unrelated
to the edit). Re-verified post-fix: `work_status == done`, `implemented_by` now
intersects the diff, `covered_by` remains `[]` and is permitted for L3. All checks
green; `resolved_acs` had exactly 1 entry, satisfying the load-bearing ok
precondition. `submit_feedback.py` exited non-zero with no sidecar fallback path
found; recorded `feedback-id: (submit-failed)` per protocol and continued. Commit
phase may proceed.
