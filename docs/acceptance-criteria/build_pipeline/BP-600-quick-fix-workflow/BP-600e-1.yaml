id: BP-600e-1
components:
  - build_pipeline
title: "Quick-fix workflow warns when the fix modifies more than the target file"
component: build-pipeline
level: L2
status: active
readiness: reviewed
priority: medium
work_status: done
criteria: |
  Given the python-coder has been dispatched to apply the fix,
  When the coder's changes touch 2 or more source files (excluding
    the test file and AC YAML),
  Then the workflow pauses before proceeding to the green-phase test,
  And it displays a warning: "This fix modified N files (expected 1).
    Files changed: [list]. Continue with quick-fix or escalate to
    /build-feature?",
  And it waits for user confirmation before proceeding.
depends_on:
  - BP-600e
  - BP-600d-2
doc_links:
  - path: docs/architecture/agent_delivery_workflows.md
    relationship: describes
    status: exists
  - path: docs/architecture/adrs/ADR-006-flatten-supervisor-chain.md
    relationship: describes
    status: exists
assigned_agent: workflow-architect
estimated_complexity: M
it_requirements:
  - "Must operate at depth 0 per ADR-006 — phase agents dispatched at depth 1 via the Agent tool"
  - "Requires Claude Code >= 2.1.154 for JS workflow script support"
  - "Must handle errors gracefully per the project error handling policy"
  - "File-count threshold must exclude test files and AC YAML files from the count"
  - "Must use git diff --name-only to detect files modified after the coder runs, not coder self-report"
delivers_to: null
expects_from:
  ac_id: BP-600d-2
  contract: "Fix-application result: { target_file: string, files_modified: string[] } — files_modified length > 1 triggers the scope warning"
origin_agent: business-analyst-v3
created: 2026-06-05
amended_by: []
superseded_by: null
covered_by:
  - "unit_tests/workflows/test_quick_fix_workflow.py"
  - BP-600e-1-i
implemented_by:
- tickets/00_inbox/TICKET-20260608-BP-600e-1.md
change_target: pipeline
risk_surface: internal
