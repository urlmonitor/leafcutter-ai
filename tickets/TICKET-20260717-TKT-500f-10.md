---
ac_traceability:
  id: TKT-500f-10
  path: docs/acceptance-criteria/ticket-creation/TKT-500-single-source/TKT-500f-10.yaml
ac_coverage: 5/5
advances_current_outcome: true
agents:
  ac-fulfillment-gate: signed_off
  ac-validator: signed_off
  commit: needed
  pr-reviewer: signed_off
  pull-request: needed
  python-coder: signed_off
  test-runner: signed_off
  test-writer: signed_off
change_target: pipeline
complexity: medium
components:
- ticket_creation_pipeline
created: '2026-07-17'
depends_on:
- TKT-500f
files_touched:
- docs/reference/ac-schema.md
- scripts/ac_store/generate_ticket_from_ac.py
priority: high
requires_adr: false
requires_diagram: false
risk_surface: internal
roadmap_phase: phase_1
source_ac: TKT-500f-10
status: in_progress
title: Generated ticket renders the AC's delivers_to and expects_from contracts
---

# Generated ticket renders the AC's delivers_to and expects_from contracts

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `TKT-500f-10` — Generated ticket renders the AC's delivers_to and expects_from contracts — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `TKT-500f-10`. Component: `ticket-creation`. Assigned agent: `python-coder`. Estimated complexity: `M`. Complexity: `medium`.

## Acceptance Criteria

```gherkin
Given a leaf AC that carries a delivers_to contract
  (agent: "python-coder", contract: "a de-duplicated files_touched list")
  and an expects_from contract
  (ac_id: "TKT-500f-8", contract: "the union edit surface"),
When a ticket is generated from that AC by generate_ticket_from_ac.py,
Then the generated ticket renders the delivers_to contract, naming the
  downstream counterpart agent "python-coder" and the contract text,
And the generated ticket renders the expects_from contract, naming the
  upstream counterpart AC id "TKT-500f-8" and the contract text,
And both contracts appear in the ticket (in the body and/or frontmatter) so
  the implementing agent receives the producer to consumer hand-off instead
  of re-deriving it from the source AC.

Given a leaf AC whose delivers_to is null and whose expects_from is null,
When a ticket is generated from that AC,
Then no delivers_to or expects_from contract content is rendered into the
  ticket, and generation completes without error.
```

- [x] AC-1: an expects_from contract
- [x] AC-2: the generated ticket renders the delivers_to contract, naming the
- [x] AC-3: the generated ticket renders the expects_from contract, naming the
- [x] AC-4: both contracts appear in the ticket (in the body and/or frontmatter) so
- [x] AC-5: no delivers_to or expects_from contract content is rendered into the

## Test Requirements

```yaml
tests:
- name: test_delivers_to_rendered
  file: unit_tests/ac_store/test_tkt_500f_10.py
  covers:
  - TKT-500f-10
  asserts: AC with delivers_to {agent, contract}; assert the generated ticket renders
    the downstream agent name and the contract text.
  framework: unittest
  type: unit
- name: test_expects_from_rendered
  file: unit_tests/ac_store/test_tkt_500f_10.py
  covers:
  - TKT-500f-10
  asserts: AC with expects_from {ac_id, contract}; assert the generated ticket renders
    the upstream AC id and the contract text.
  framework: unittest
  type: unit
- name: test_null_contracts_no_render
  file: unit_tests/ac_store/test_tkt_500f_10.py
  covers:
  - TKT-500f-10
  asserts: AC with delivers_to null and expects_from null; assert no contract block
    is rendered and generation completes without error.
  framework: unittest
  type: unit
```

## Implementation Notes

```yaml
config_schema_fragment: null
n_location_rule: 1 (the ticket body/frontmatter contract-rendering point in _build_ticket_body,
  near line 923+)
notes:
- When the source AC carries delivers_to, the ticket must render the downstream agent
  and contract text; when it carries expects_from, render the upstream AC id and contract
  text.
- Both contracts must reach the implementing agent (body and/or frontmatter) so the
  producer-to-consumer hand-off is explicit rather than re-derived from the source
  AC.
- When both delivers_to and expects_from are null, no contract content is rendered
  and generation completes without error.
post_write_commands:
- python scripts/build.py --target-dir .
reference_file_path: scripts/ac_store/generate_ticket_from_ac.py
required_skills:
- python-coder
```

## Sign-offs

- [x] test-writer — 2026-07-17 14:30
- [x] python-coder — 2026-07-17 15:10
- [x] test-runner — 2026-07-17 10:37
- [x] pr-reviewer — 2026-07-17 16:45
- [x] ac-validator — 2026-07-17 17:00
- [x] ac-fulfillment-gate — 2026-07-17 17:30
- [ ] commit
- [ ] pull-request

## Comments

### 2026-07-17 14:30 — test-writer (status: ok)
feedback-id: fb_2026-07-17_1039c323
completion_manifest:
  test_stubs_written: true
  tests_red: true
  null_case_guard_present: true
Wrote 3 test stubs for TKT-500f-10 in unit_tests/ac_store/test_tkt_500f_10.py.
test_delivers_to_rendered and test_expects_from_rendered are RED (AssertionError: contract
text absent from generated ticket body — _build_ticket_body does not yet render
delivers_to/expects_from). test_null_contracts_no_render passes immediately (null case is
trivially correct before implementation) and is flagged in red_baseline as under-specified.
Suite exit code: 1 (non-zero — correct RED state). Verification run used AC_ENFORCE_STRICT=1
to bypass the pytest_ac_enforcement xfail masking.
red_baseline:
  - test_name: TestContractRendering::test_delivers_to_rendered
    file: unit_tests/ac_store/test_tkt_500f_10.py
    error: "AssertionError: 'a de-duplicated files_touched list' not found in <generated ticket output>"
  - test_name: TestContractRendering::test_expects_from_rendered
    file: unit_tests/ac_store/test_tkt_500f_10.py
    error: "AssertionError: 'the union edit surface' not found in <generated ticket output>"
  - test_name: TestContractRendering::test_null_contracts_no_render
    file: unit_tests/ac_store/test_tkt_500f_10.py
    error: "(no failure — test passes immediately)"
    note: "passes immediately — may be under-specified; included as regression guard for null-handling path"

### 2026-07-17 15:10 — python-coder (status: ok)
feedback-id: fb_2026-07-17_eced4001
completion_manifest:
  delivers_to_rendered: true
  expects_from_rendered: true
  null_contracts_no_render: true
  tests_green: true
  ruff_clean: true
  behavioral_spotcheck: true
context_capsule:
  agent_id: python-coder
  intent: "Render delivers_to/expects_from AC contract fields into generated ticket body so implementing agents receive the producer-consumer hand-off explicitly."
  files_touched_rationale: |
    scripts/ac_store/generate_ticket_from_ac.py: added _build_agent_contracts_section() helper and wired it into _build_ticket_body()
  consumers_checked: |
    _build_ticket_body is called at lines 1574 and 1609 — both pass agents_map pre-computed; no call-site signature change required
  red_baseline: |
    test_delivers_to_rendered: green
    test_expects_from_rendered: green
    test_null_contracts_no_render: was already passing (regression guard)
  design_constraints: |
    Section header "## Agent Contracts" only emitted when at least one contract field is non-null; null guard uses `or None` to treat falsy values as absent
Added _build_agent_contracts_section() in generate_ticket_from_ac.py (48 lines). When delivers_to or expects_from is non-null, a "## Agent Contracts" section with ### Delivers To / ### Expects From sub-sections is rendered. When both are None the function returns "" and no section appears. All 3 red_baseline tests are now green (AC_ENFORCE_STRICT=1 run: 3 passed in 0.14s). Behavioral spot-check against real on-disk AC YAML artifacts confirmed correct rendering for both contract and null-contract cases.

### 2026-07-17 10:37 — test-runner (status: ok)
feedback-id: fb_2026-07-17_b6ae7aba
completion_manifest:
  test_suite_executed: true
  all_tests_passing: true
  failure_report_structured: true
3 tests in unit_tests/ac_store/test_tkt_500f_10.py all PASSED (delivers_to_rendered, expects_from_rendered, null_contracts_no_render). Full ac_store regression suite: 339 passed, 1 skipped, no failures. Ruff clean on generate_ticket_from_ac.py.

### 2026-07-17 16:45 — pr-reviewer (status: ok)
feedback-id: fb_2026-07-17_738a41ea
completion_manifest:
  diff_reviewed: true
  implementation_correct: true
  null_guard_verified: true
  behavioral_spot_check_passed: true
  ac_coverage_confirmed: true
No high-confidence defects. _build_agent_contracts_section() correctly renders delivers_to and expects_from into the ticket body; the null guard (both fields None → empty string) is confirmed by behavioral spot-check against real fixtures. ACs 1-5 all satisfied. Observation: the ticket's pre-existing depends_on: [TKT-500f] references an AC ID rather than a ticket file path, causing ticket_frontmatter_guard to fire on every edit — this is a pre-existing generator limitation, not introduced by this diff, and will need resolution at commit time if the hook runs as a pre-commit gate.

### 2026-07-17 17:00 — ac-validator (status: ok)
feedback-id: (submit-failed)
completion_manifest:
  AC-1_covered: true
  AC-2_covered: true
  AC-3_covered: true
  AC-4_covered: true
  AC-5_covered: true
  tests_green: true
  store_alignment_check: true
All 5 ACs covered (5/5). Implementation evidence: `_build_agent_contracts_section()` added to `scripts/ac_store/generate_ticket_from_ac.py` (lines 884–930) renders `delivers_to` as `### Delivers To` with `**Agent:**` + `**Contract:**` fields, `expects_from` as `### Expects From` with `**AC:**` + `**Contract:**` fields, and returns `""` when both are None. Wired into `_build_ticket_body()`. Test evidence: `unit_tests/ac_store/test_tkt_500f_10.py` — `test_delivers_to_rendered` (AC-1, AC-2, AC-4), `test_expects_from_rendered` (AC-1, AC-3, AC-4; asserts "TKT-500f-8" in output), `test_null_contracts_no_render` (AC-5) — all 3 PASSED (AC_ENFORCE_STRICT=1, 0.07s). AC store alignment script absent (pre-store layout) — skipped silently. Note: `depends_on: [TKT-500f]` triggers `ticket_frontmatter_guard` on every edit — pre-existing generator limitation flagged by pr-reviewer; commit agent must resolve.

### 2026-07-17 17:30 — ac-fulfillment-gate (status: ok)
feedback-id: (submit-failed)
completion_manifest:
  work_status_verified: true
  implemented_by_verified: true
  covered_by_verified: true
  ac_yaml_staged: true
AC YAML store fields for TKT-500f-10 verified and auto-fixed (append-only). `work_status` updated from `todo` to `done`; `covered_by` populated with `unit_tests/ac_store/test_tkt_500f_10.py` (staged new file, 3 tests all green per test-runner); `implemented_by` retained pre-existing entry `tickets/TICKET-20260717-TKT-500f-10.md`. Implementation evidence: `scripts/ac_store/generate_ticket_from_ac.py` present in branch diff and `files_touched`. Schema validation script absent — skipped silently. The pre-existing `depends_on: [TKT-500f]` frontmatter guard violation fires on every edit to this ticket; this is a known generator limitation flagged by pr-reviewer and ac-validator — not introduced by this gate. Commit phase may proceed.
