---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  pr-reviewer: signed_off
  pull-request: needed
  python-coder: signed_off
  sql-coder: not_needed
  test-runner: signed_off
  test-writer: signed_off
components:
- build_pipeline
created: '2026-06-11'
depends_on:
- BP-900a
files_touched:
- scripts/build.py
- scripts/build_phases.py
- docs/architecture/components/template-compiler.md
priority: critical
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: BP-900a-2
status: in_progress
title: build.py deploys standalone scripts goal_to_epic.py and build_ac_mode_detection.py
---

# build.py deploys standalone scripts goal_to_epic.py and build_ac_mode_detection.py

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BP-900a-2` — build.py deploys standalone scripts goal_to_epic.py and build_ac_mode_detection.py — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BP-900a-2`. Component: `build_pipeline`. Assigned agent: `python-coder`. Estimated complexity: `S`.

## Acceptance Criteria

```gherkin
Given build.py is invoked with --target-dir pointing to a consumer project,
When the build completes successfully,
Then the file {target}/.leafcutter/scripts/goal_to_epic.py exists
And the file {target}/.leafcutter/scripts/build_ac_mode_detection.py exists
And each deployed file is byte-identical to its source in templates/scripts/
And a shim at {target}/scripts/goal_to_epic.py resolves to the deployed copy
And a shim at {target}/scripts/build_ac_mode_detection.py resolves to the deployed copy
```

## Sign-offs

- [x] python-coder — 2026-06-16 10:15
- [x] test-writer — 2026-06-16 00:00
- [x] test-runner — 2026-06-16 12:00
- [x] pr-reviewer — 2026-06-16 13:00
- [ ] commit
- [ ] pull-request

## Comments

### 2026-06-16 00:00 — ticket-supervisor (status: ok)
test_requirements empty — test-writer phase skipped (docs-only or config-only ticket)

### 2026-06-16 10:15 — python-coder (status: ok)
feedback-id: fb_2026-06-16_88db50a7

Implemented AC BP-900a-2. All four changes applied:

1. templates/scripts/goal_to_epic.py — byte-identical copy from scripts/goal_to_epic.py
2. templates/scripts/build_ac_mode_detection.py — byte-identical copy from scripts/build_ac_mode_detection.py
3. scripts/build_phases.py — added build_standalone_scripts() phase function that deploys both files from TEMPLATES_DIR/scripts/ to output_root/scripts/ using SHA-256 compare-before-copy guard and shutil.copy2
4. scripts/build.py — imported build_standalone_scripts and registered ("Standalone scripts", build_standalone_scripts) in internal_phases after ("AC store scripts", build_ac_store_scripts)
5. scripts/build_helpers.py — added two file_shims entries for scripts/goal_to_epic.py and scripts/build_ac_mode_detection.py so canonical shims at {target}/scripts/ resolve to .leafcutter/scripts/ deployed copies

completion_manifest:
  files_written:
    - templates/scripts/goal_to_epic.py (new, byte-identical to scripts/goal_to_epic.py)
    - templates/scripts/build_ac_mode_detection.py (new, byte-identical to scripts/build_ac_mode_detection.py)
    - scripts/build_phases.py (build_standalone_scripts() function added)
    - scripts/build.py (import + phase registration added)
    - scripts/build_helpers.py (two file_shims entries added)

### 2026-06-16 13:00 — pr-reviewer (status: ok)
feedback-id: fb_2026-06-16_0c421dff

Review Report

**Base:** origin/main (branch is 5 commits ahead)
**Diff size:** staged: 2003 lines across 3 files (templates + ticket); unstaged implementation: ~100 lines across 3 files (build.py, build_phases.py, build_helpers.py)

#### High-Confidence Findings

No high-confidence findings.

#### Medium-Confidence Findings

No medium-confidence findings.

#### Suppression Tally

Suppressed: 0 low-confidence nits, 0 medium findings dropped by Opus.

## Escalation

Branch: none
Reason: not escalated — no medium or high findings (threshold > 3 medium).

AC Verification:
- `{target}/.leafcutter/scripts/goal_to_epic.py` — deployed by `build_standalone_scripts()` which writes to `output_root / "scripts"` when called from `internal_phases` (receives `output_root` as its `target_root` argument). Correct.
- `{target}/.leafcutter/scripts/build_ac_mode_detection.py` — same. Correct.
- Byte-identity — SHA-256 confirmed both template files are byte-identical to the originals: `77bc087f...` and `357357ad...`. Correct.
- Shim `{target}/scripts/goal_to_epic.py` — created by `("scripts/goal_to_epic.py", "scripts/goal_to_epic.py")` entry in `file_shims`; `canonical_path = target_root / "scripts/goal_to_epic.py"`, `source_path = output_root / "scripts/goal_to_epic.py"`. Correct.
- Shim `{target}/scripts/build_ac_mode_detection.py` — same pattern. Correct.

Pattern conformance: `build_standalone_scripts()` follows `build_ac_store_scripts()` pattern exactly — same function signature, same `_should_overwrite` / `_files_content_identical` guards, same `_uptodate_count` global, same `shutil.copy2` + `OSError` handling.

completion_manifest:
  files_reviewed:
    - templates/scripts/goal_to_epic.py (staged, byte-identical to scripts/goal_to_epic.py)
    - templates/scripts/build_ac_mode_detection.py (staged, byte-identical to scripts/build_ac_mode_detection.py)
    - scripts/build_phases.py (build_standalone_scripts() correct, pattern-conformant)
    - scripts/build.py (import + internal_phases registration correct)
    - scripts/build_helpers.py (file_shims entries correctly wired)
  all_acs_satisfied: true

### 2026-06-16 12:00 — test-runner (status: ok)
feedback-id: fb_2026-06-16_e0c773da
completion_manifest:
  test_suite_executed: true
  all_tests_passing: true
  failure_report_structured: true

Ran full unit test suite (python -m pytest unit_tests/ --tb=short). Baseline comparison confirmed all 25 failures are pre-existing (identical failure list before and after ticket changes). This ticket introduced zero new test failures. 878 tests passed, 19 skipped, 25 pre-existing failures (ac_store/test_tree_traversal, commit_guardian/test_check_ac_limits, test_build_workflow_output_paths, test_check_description_field, test_visualise_knowledge_graph). Three additional tests (test_build_changelog_placeholder, test_build_package_version, test_build_version_wiring) fail on import due to pre-existing ModuleNotFoundError: release.compute_next_version.
