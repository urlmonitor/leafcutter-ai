id: GE-118
components:
  - commit_guardian
title: "Commit-guardian hooks resolve their dependencies from the deployed layout in git worktrees"
component: guardrail-engine
level: L0
status: active
req_status: draft
work_status: todo
readiness: draft
priority: medium
roadmap_phase: phase_1
criteria: |
  Commit-guardian hooks must run correctly from inside a git worktree, not only the
  main checkout. A hook that imports a helper from a deployed skills directory must
  resolve that directory from the layout build.py actually deploys (.leafcutter),
  not a hardcoded path (.claude), so the hook does not crash with ModuleNotFoundError
  when a worktree lacks the hardcoded directory.

  Surfaced during the BO-2600 build: committing in a fresh worktree failed because
  check_secrets.py read security_scanner.scripts_dir = ".claude/skills/..." from
  commit_guardian.json, while build.py deploys under .leafcutter/, so scan_secrets
  could not be imported.
depends_on: []
doc_links:
  - path: templates/scripts/commit_guardian/check_secrets.py
    relationship: modifies
    status: exists
  - path: docs/architecture/components/commit-guardian.md
    relationship: related
    status: exists
assigned_agent: null
estimated_complexity: null
delivers_to: null
expects_from: null
origin_agent: BrainCandy
created: 2026-08-11
amended_by:
  - reason: adopt-renumbered-child
    date: '2026-08-18'
    by: business-analyst
    entry: 'covered_by gained GE-118c ("A guard enforces the document types the
      project declared, not a narrower list it kept to itself"), taking this L0
      from 2 of 7 children to 3 of 7. GE-118c is not new work — it landed in PR
      #466 as a loose record at the guardrail-engine namespace root claiming
      the id GE-120, which the goal-level tree GE-120-green-means-checked/ was
      already holding. It was renumbered into this tree because its primary
      root cause is the same one GE-118b records: a hand-counted parents[2]
      plus a hardcoded "leafcutter" segment resolving to a path that never
      existed in any layout, so the gate silently no-ops. NOTE FOR ANYONE
      READING THE STANDING RULE: the guardrail-engine PROJECT_CONTEXT and
      GE-120''s L0 notes both say GE-118 is "deliberately NOT amended or
      superseded" and stands as the record of the point fixes that landed.
      That rule was written to stop the cross-cutting green-means-checked
      POLICY being grafted onto this tree, which would put the general rule
      beneath a specific instance. Adding a third point fix of the same shape
      does not invert that containment. GE-118c''s own notes carry the full
      reasoning, including why GE-120 — the semantically closer parent — is
      mechanically forbidden.'
superseded_by: null
covered_by: [GE-118a, GE-118b, GE-118c]
implemented_by: []
change_target: code
risk_surface: internal
