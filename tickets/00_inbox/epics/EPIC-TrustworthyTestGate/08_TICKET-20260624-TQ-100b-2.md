---
advances_current_outcome: true
agents:
  commit: signed_off
  documentation-expert: not_needed
  pr-reviewer: signed_off
  pull-request: needed
  python-coder: signed_off
  sql-coder: not_needed
  test-runner: signed_off
  test-writer: signed_off
components:
- testing-quality
created: '2026-06-24'
depends_on: []
files_touched:
- scripts/ac_store/test_enforcement.py
- conftest.py
- tests/testing_quality/test_ac_linkage.py
priority: high
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: TQ-100b-2
status: in_progress
title: A test linked to a done AC is enforced and its failure fails the run
---

# A test linked to a done AC is enforced and its failure fails the run

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `TQ-100b-2` — A test linked to a done AC is enforced and its failure fails the run — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `TQ-100b-2`. Component: `testing-quality`. Assigned agent: `python-coder`. Estimated complexity: `M`.

## Acceptance Criteria

```gherkin
Given a test function tagged "# covers: <AC-ID>" where that AC's work_status in
  the AC store is done,
When the suite is run and that test fails,
Then the test's failure counts as a real failure
And the overall run outcome is non-passing
And the test is enforced purely on the strength of its covering AC being done,
  with no additional manual marking on the test required.
```

## Test Requirements

- `test_done_ac_test_is_enforced`: A failing test tagged with a done AC is enforced and its failure drives the overall run non-passing.

## Sign-offs

- [x] python-coder — 2026-06-24 15:00
- [x] test-writer — 2026-06-24 14:00
- [x] test-runner — 2026-06-24 16:00
- [x] pr-reviewer — 2026-07-08 09:41
- [x] commit — 2026-07-08 09:45
- [ ] pull-request

## Comments

### 2026-06-24 14:00 — test-writer (status: ok)
feedback-id: (submit-failed)
completion_manifest:
  test_written: true
  test_verified_red_or_green_with_existing_impl: true
Added class TestDoneAcTestIsEnforced with test_done_ac_test_is_enforced to tests/testing_quality/test_ac_linkage.py. The test creates a temp AC store with a done AC (DONE-001, work_status: done), runs a failing test tagged with that AC via subprocess pytest, and asserts the exit code is non-zero and no xfail markers appear. The test passes immediately (green) because the enforcement plugin already classifies done ACs as enforced and does not silence their failures — this matches the expected outcome stated in the task brief.

## Test Writer — Completion Report

### Tests Written
| File | Directory | Framework | Status |
|---|---|---|---|
| test_ac_linkage.py (appended class) | tests/testing_quality/ | unittest | written |

### Verification Run
- Command: python -m pytest tests/testing_quality/test_ac_linkage.py::TestDoneAcTestIsEnforced -v --tb=short --no-header
- Result: green (1 passed — expected; enforcement logic already implemented per ticket context)

### Notes
The test passes immediately because `classify_by_work_status` already returns "enforced" for done ACs and the pytest plugin already skips the xfail conversion for enforced tests. This is the documented expected outcome in the task brief. The test serves as a regression guard for AC TQ-100b-2.

red_baseline:
  - test_name: test_done_ac_test_is_enforced
    file: tests/testing_quality/test_ac_linkage.py
    error: "1 passed — passes immediately (implementation already exists)"
    note: "passes immediately — enforcement logic pre-exists; test is a regression guard per task brief"

### 2026-06-24 15:00 — python-coder (status: ok)
feedback-id: (submit-failed)
completion_manifest:
  implementation_verified: true
  tests_green: true
  no_regressions: true
Verified that the implementation for AC TQ-100b-2 is already complete. The `classify_by_work_status` function in `scripts/ac_store/test_enforcement.py` correctly returns "enforced" for ACs with `work_status == "done"`, and the pytest plugin in `scripts/ac_store/pytest_ac_enforcement.py` only converts failing tests to xfail when `classification == "informational"` — leaving enforced (done-AC) failures as real run failures. The `TestDoneAcTestIsEnforced::test_done_ac_test_is_enforced` test passes (1 passed in 0.59s), and all 13 tests in `tests/testing_quality/` pass with no regressions.
red_baseline_results:
  - test_name: test_done_ac_test_is_enforced
    result: green

### 2026-07-08 09:41 — pr-reviewer (status: ok)
feedback-id: (submit-failed)
completion_manifest:
  ac_criteria_satisfied: true
  no_high_confidence_findings: true
  no_medium_confidence_findings: true
Reviewed the working diff (164-line addition to tests/testing_quality/test_ac_linkage.py; 52-line ticket frontmatter update). The new TestDoneAcTestIsEnforced class correctly exercises all three "Then" clauses of AC TQ-100b-2: (1) returncode != 0 asserts the overall run is non-passing, (2) absence of xfailed/XFAIL markers confirms the failure is not silenced, and (3) presence of FAILED/AssertionError confirms it is a real enforced failure. LEAFCUTTER_AC_STORE_ROOT env injection is wired correctly to the enforcement plugin. All module-level names used by the new class (sys, subprocess, tempfile, textwrap, Path, _REPO_ROOT) are confirmed present earlier in the file. No high- or medium-confidence findings. One style nit suppressed (inline __import__("os") instead of top-level import os — no correctness impact).

### 2026-07-08 09:45 — commit (status: ok)
feedback-id: (submit-failed)
Auto-authorized commit gate: subject "feat(testing-quality): TQ-100b-2 — enforce done-AC-tagged tests as real failures"; staged files: tests/testing_quality/test_ac_linkage.py, tickets/00_inbox/epics/EPIC-TrustworthyTestGate/08_TICKET-20260624-TQ-100b-2.md.

### 2026-07-08 09:45 — commit (status: ok)
feedback-id: (submit-failed)
completion_manifest:
  pre_commit_hooks_pass: true
  commit_message_valid: true
  ticket_staged: true
Committed SHA 0f0642dc: TestDoneAcTestIsEnforced regression guard + pr-reviewer sign-off staged; all pre-commit hooks passed. Ticket frontmatter updated commit → signed_off.
