---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  pr-reviewer: needed
  pull-request: needed
  python-coder: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
components:
- build_pipeline
created: '2026-06-22'
depends_on:
- BP-900e
- BP-900e-1
files_touched:
- docs/acceptance-criteria/build_pipeline/BP-900-deployment-completeness/BP-900e.yaml
- scripts/commit_guardian/commit_guardian.json
- docs/acceptance-criteria/build_pipeline/BP-1000-source-template-parity/BP-1000a-1.yaml
- docs/architecture/adrs/ADR-001-self-hosting-boundary.md
- docs/architecture/adrs/ADR-013-portable-skill-script-deployment-boundary.md
priority: high
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: BP-900e-2
status: todo
title: A registered hook that does have a template copy passes without a false alarm
---

# A registered hook that does have a template copy passes without a false alarm

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BP-900e-2` — A registered hook that does have a template copy passes without a false alarm — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BP-900e-2`. Component: `build-pipeline`. Assigned agent: `python-coder`. Estimated complexity: `S`.

## Acceptance Criteria

```gherkin
Given the commit-guardian hook registry registers a hook whose entry resolves
  to scripts/commit_guardian/check_ac_schema.py
And a deploy-source copy exists at
  templates/scripts/commit_guardian/check_ac_schema.py
And every other registered hook also has a deploy-source copy under
  templates/scripts/
And no deployable template references a script that lacks a template copy
When the registry-completeness check runs at the finalize merge gate
Then the check reports no registry-completeness violations
And check_ac_schema.py does not appear in any promised-but-undeployed list
And the merge gate is not blocked by this check
And the check does NOT inspect whether the existing template copy is
  byte-identical to its source (that comparison is owned by BP-1000a-1, not
  by this check)
```

## Sign-offs

- [ ] python-coder
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit
- [ ] pull-request

## Comments
