id: BP-100i-1
components:
  - build_pipeline
readiness: approved
priority: high
title: "Script parity: every .py file in the runtime commit_guardian directory has a counterpart in the canonical template directory"
component: build-pipeline
level: L2
status: active
req_status: active
work_status: done
created_by: "docs/acceptance-criteria/build_pipeline/BP-100-reliable-builds/BP-100i.yaml"
criteria: |
  Given the runtime source directory contains 5 hook scripts:
    - check_alpha.py
    - check_beta.py
    - check_gamma.py
    - check_delta.py
    - run_hook.py
  And the canonical template directory contains only 4 of those scripts:
    - check_alpha.py
    - check_beta.py
    - check_gamma.py
    - run_hook.py
  (check_delta.py is absent from the canonical template directory)
  When the parity check runs at pre-commit time
  Then the commit is blocked with exit code 1
  And the error message names "check_delta.py" as missing from the canonical template directory
  And the error message states the expected location where the file should exist
depends_on:
  - BP-100i
doc_links:
  - path: "docs/acceptance-criteria/build_pipeline/BP-100-reliable-builds/BP-100i.yaml"
    relationship: parent
    status: exists
  - path: "docs/build-drift-hook.md"
    relationship: describes
    status: exists
  - path: "docs/pre-commit-hooks.md"
    relationship: describes
    status: exists
  - path: "docs/how-to/managing-pre-commit-hooks.md"
    relationship: describes
    status: exists
  - path: "docs/build-pipeline.md"
    relationship: describes
    status: exists
assigned_agent: python-coder
estimated_complexity: M
it_requirements:
  - "Must exit 0 (fail-open) on unexpected errors such as unreadable directories or malformed JSON; must exit 1 only on detected parity violations"
  - "Must complete within 2 seconds for repositories with up to 50 hook scripts per directory"
  - "Must be idempotent -- repeated runs with the same directory state produce the same result"
  - "Must read directory paths from commit_guardian.json configuration, not hardcode paths"
delivers_to: null
expects_from: null
origin_agent: business-analyst-v3
created: 2026-06-05
amended_by:
  - it-po-v3
superseded_by: null
covered_by:
  - "unit_tests/commit_guardian/test_check_hook_parity.py"
  - BP-100i-1-i
  - BP-100i-1-ii
implemented_by:
  - templates/scripts/commit_guardian/check_hook_parity.py
change_target: code
risk_surface: internal
