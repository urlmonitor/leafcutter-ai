---
ac_traceability:
  id: TKT-500f-15
  path: docs/acceptance-criteria/ticket-creation/TKT-500-single-source/TKT-500f-15.yaml
advances_current_outcome: true
agents:
  ac-fulfillment-gate: needed
  ac-validator: needed
  commit: needed
  pr-reviewer: needed
  pull-request: needed
  python-coder: needed
  test-runner: needed
  test-writer: needed
change_target: pipeline
complexity: low
components:
- ticket_creation_pipeline
created: '2026-07-21'
depends_on:
- TKT-500f
- TKT-500f-13
files_touched:
- docs/acceptance-criteria/index.yaml
- docs/components.json
- docs/reference/ac-schema.md
- scripts/ac_store/generate_ticket_from_ac.py
priority: high
requires_adr: false
requires_diagram: false
risk_surface: internal
roadmap_phase: phase_1
source_ac: TKT-500f-15
status: todo
title: A scalar-string components field becomes a single-element graph-id list, never
  a per-character shatter
---

# A scalar-string components field becomes a single-element graph-id list, never a per-character shatter

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `TKT-500f-15` — A scalar-string components field becomes a single-element graph-id list, never a per-character shatter — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `TKT-500f-15`. Component: `ticket-creation`. Assigned agent: `python-coder`. Estimated complexity: `S`. Complexity: `low`.

## Acceptance Criteria

```gherkin
Given a leaf AC whose components field is a SCALAR string rather than a YAML
  list (for example components: ticket-creation written as a bare string, or a
  scalar underscore graph id components: ticket_creation_pipeline),
When a ticket is generated from that AC by generate_ticket_from_ac.py
  (_build_components_list),
Then the generated ticket's components LIST is a single-element list holding that
  one component's underscore components.json graph id (for example
  ["ticket_creation_pipeline"]),
And the LIST is never a per-character shatter of the scalar string — it never
  contains one single-character element per letter of the string (for example it
  is never ["t", "i", "c", "k", "e", "t", ...]),
And the resulting single element is the components.json graph id, not the raw
  scalar left verbatim when the scalar was an index.yaml kebab namespace key.
```

- [ ] AC-1: the generated ticket's components LIST is a single-element list holding that
- [ ] AC-2: the LIST is never a per-character shatter of the scalar string — it never
- [ ] AC-3: the resulting single element is the components.json graph id, not the raw

## Test Requirements

```yaml
tests:
- name: test_scalar_string_components_becomes_single_element_graph_id_list
  file: unit_tests/ac_store/test_tkt_500f_15.py
  covers:
  - TKT-500f-15
  asserts: 'A fixture AC with scalar `components: build_orchestration` yields a generated
    components LIST == [''build_orchestration''] (exactly one element).'
  framework: unittest
  type: unit
- name: test_scalar_components_never_per_character_shatter
  file: unit_tests/ac_store/test_tkt_500f_15.py
  covers:
  - TKT-500f-15
  asserts: A scalar-string components value never produces a LIST of single-character
    elements (never ['t','i','c','k','e','t',...]).
  framework: unittest
  type: unit
- name: test_scalar_kebab_namespace_key_resolves_to_graph_id
  file: unit_tests/ac_store/test_tkt_500f_15.py
  covers:
  - TKT-500f-15
  asserts: A scalar index.yaml kebab namespace key is emitted as its components.json
    underscore graph id, not left verbatim.
  framework: unittest
  type: unit
```

## Implementation Notes

```yaml
config_schema_fragment: null
n_location_rule: '1'
notes:
- The components-list builder (_build_components_list, ~line 1112) must treat a scalar-string
  components field as a single logical value, not an iterable of characters — the
  emitted LIST must never be a per-character shatter.
- A scalar that is an index.yaml kebab namespace key must be resolved to its components.json
  underscore graph id before emission; the raw kebab must not pass through verbatim.
- Graph-id resolution must be driven by docs/components.json (the ~42-entry source
  of truth), not by a hard-coded kebab-to-graph-id map.
post_write_commands:
- python scripts/build.py --target-dir .
reference_file_path: scripts/ac_store/generate_ticket_from_ac.py
required_skills:
- python-coder
```

## Sign-offs

- [ ] test-writer
- [ ] python-coder
- [ ] test-runner
- [ ] pr-reviewer
- [ ] ac-validator
- [ ] ac-fulfillment-gate
- [ ] commit
- [ ] pull-request

## Comments
