id: KM-ADM-005
readiness: reviewed
priority: high
components:
  - knowledge_management
status: active
component: knowledge-management
level: L2
title: "The test-covers edge states its diff scope, and the untagged-done backlog cannot grow"
parent: KM-ADM-100a
depends_on:
  - KM-ADM-100a
doc_links:
  - path: docs/acceptance-criteria/ac-store/ACS-1100-honest-coverage-answers/ACS-1100.yaml
    relationship: context
    status: exists
    relevance: "The GENERAL contract this AC is one concrete instance of: an answer that saw a fraction of the store must not look like one that saw all of it. ACS-1100 is standing and unbuilt; this AC satisfies its spirit for a single surface, in hand-written prose. Inherited, not duplicated — a future build of ACS-1100 must not re-implement or contradict what shipped here."
  - path: docs/acceptance-criteria/testing-quality/TQ-400-durable-done-proof/TQ-400d.yaml
    relationship: context
    status: exists
    relevance: "OWNS retiring the untagged-done backlog this AC only ratchets. TQ-400d ('the pile of unproven finished work gets worked through, not written off') is the follow-on work; this AC is the floor beneath it."
  - path: unit_tests/docs/test_artifact_graph_covers_scope.py
    relationship: verifies
    status: exists
    relevance: "Carries HIGH_WATER_MARK = 244. Lowering it as the pile is retired is TQ-400d's job; raising it is never correct."
origin_agent: BrainCandy
criteria: |
  Given the artifact data map rates test-covers (Test -> AC via "# covers: <AC-ID>") as
    enforcement "enforced" on the strength of check-done-proof running as a pre-commit
    hook and a required CI gate
  When a reader inverts that edge to ask the question the graph exists to answer —
    "which test proves this AC is really implemented?"
  Then the edge's note states that check-done-proof is DIFF-SCOPED: it evaluates only
    ACs changed in the current commit or PR, and never re-evaluates a done AC that
    predates the gate
  And the note reports that a substantial share of already-done ACs carry no covering
    test tag at all, so "enforced" describes new work only and the edge must not be read
    as store-wide coverage
  And the untagged-done backlog is ratcheted: a test measures how many work_status done
    ACs have no test tagging them and fails if that count rises above the recorded
    high-water mark, so marking further ACs done without a covering test is blocked
  And the bug symptom ("test-covers reads as a store-wide guarantee, so an auditor
    concludes every done AC is test-proven when roughly forty percent are not") must
    not occur
notes: |
  Authored by /quick-fix. Root cause: the enforcement axis records WHETHER a hook blocks,
  not WHAT it blocks over. check-done-proof genuinely blocks, so "enforced" is the correct
  rating and the KM-ADM-001 registry test agrees — but the scope of that enforcement is
  invisible on the map, and scope is exactly what a reader needs before trusting an
  inverted traversal.
  Measured on this branch (607 work_status done ACs across the store): 363 are tagged by
  at least one `# covers:` reference in unit_tests/, tests/ or leafcutter-web/, and 244
  are not. The BO-2500b-3 design makes this explicit — its own test module states
  "Pre-existing done ACs NOT in changed_yaml_paths are never evaluated, even if they are
  done and lack coverage."
  The ratchet is deliberately a floor, not a fix: it stops the backlog growing while
  leaving the 244 to be retired separately. It also gives the store the first mechanical
  answer to the user's original question, "is the implementation covered".

  ==========================================================================
  RECONCILIATION 2026-08-18 (product-owner) — READ BEFORE ACTING ON THE 244.
  ==========================================================================
  1. "TO BE RETIRED SEPARATELY" NOW HAS A NAMED OWNER. The sentence above, and
     KI-KM-002 in docs/known-issues/knowledge-management.md, both read as though the
     retirement is unowned. It is not. TQ-400d ("the pile of unproven finished work
     gets worked through, not written off", authored 2026-08-17, one day BEFORE the
     known-issue entry) owns exactly this pile, with five L2 children covering the
     worked list, per-item decisions, progress measurement, and a triage how-to. Do
     NOT author a new AC for retiring the 244 — it would duplicate TQ-400d.
  2. THE DIVISION OF LABOUR IS CLEAN, KEEP IT THAT WAY. This AC is the FLOOR (the
     count cannot grow). TQ-400d is the DRAWDOWN (the count goes down, with a
     recorded decision per record). TQ-400a is the store-wide sweep that produces the
     inventory both depend on. HIGH_WATER_MARK should be lowered by TQ-400d as the
     pile is retired; nothing should ever raise it.
  3. TWO MEASUREMENTS OF THE SAME THING DISAGREE — AND THAT IS THE ACS-1100 DEFECT
     LIVE IN THIS VERY FILE. This AC reports 244 of 607 done ACs untagged (measured
     2026-08-13, whole store, quote-stripped ids). TQ-400 reports 240 of 641
     (measured 2026-08-17, done L2/L3 records only). Different denominators, different
     dates, different inclusion rules, neither stating the other's existence — so a
     reader cannot tell whether the pile grew, shrank, or was simply counted twice.
     Both figures are defensible; what is missing is precisely what ACS-1100b
     requires, a declared scope and inclusion rule alongside the number. Whichever AC
     builds first must state its method, and the other must be reconciled to it.
  4. THE STANDING CONTRACT IS ACS-1100, AND IT STAYS IN ac-store. This AC is not
     re-homed under it. See the doc_links above and the KM-ADM-100 notes.
change_target: docs
risk_surface: internal
