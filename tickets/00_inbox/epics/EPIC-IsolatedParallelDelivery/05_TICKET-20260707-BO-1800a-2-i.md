---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  pr-reviewer: needed
  pull-request: needed
  python-coder: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
components:
- build_orchestration
created: '2026-07-07'
depends_on:
- BO-1800a-2
files_touched:
- docs/architecture/adrs/ADR-018-agent-isolation-topology.md
priority: medium
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: BO-1800a-2-i
status: todo
title: Reflink unsupported falls back cleanly to the next creation strategy
---

# Reflink unsupported falls back cleanly to the next creation strategy

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BO-1800a-2-i` — Reflink unsupported falls back cleanly to the next creation strategy — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BO-1800a-2-i`. Component: `build-orchestration`. Assigned agent: `python-coder`. Estimated complexity: `S`.

## Acceptance Criteria

```gherkin
Given the filesystem hosting the drive copies does not support copy-on-write
  reflinks (so `cp --reflink=auto` cannot make a reflinked copy),
When the isolation layer creates a new drive copy,
Then it does not fail and does not fall back to a plain byte copy of the reflink
  step; instead it moves to the next strategy in order
  (`git clone --reference <cache> --dissociate`, then plain `git clone`),
And the drive copy that results still has a fully independent, dissociated object
  store identical in isolation guarantees to the reflink path.
```

## Sign-offs

- [ ] python-coder
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit
- [ ] pull-request

## Comments
