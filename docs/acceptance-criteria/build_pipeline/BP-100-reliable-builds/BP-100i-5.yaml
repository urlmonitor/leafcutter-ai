id: BP-100i-5
readiness: approved
priority: high
title: "No violations when all directories are in sync — silent pass"
component: build-pipeline
level: L2
status: active
req_status: active
work_status: todo
created_by: "docs/acceptance-criteria/build_pipeline/BP-100-reliable-builds/BP-100i.yaml"
criteria: |
  Given the runtime source directory contains 4 hook scripts:
    - check_alpha.py
    - check_beta.py
    - check_gamma.py
    - run_hook.py
  And the canonical template directory contains the same 4 scripts
  And the legacy template directory contains the same 4 scripts
  And the deployed output directory contains the same 4 scripts
  And the canonical manifest registers hooks:
    - check-alpha
    - check-beta
    - check-gamma
  And the legacy manifest registers the same 3 hooks
  When the parity check runs at pre-commit time
  Then no violations are reported
  And the hook exits with code 0
  And no output is emitted to stderr or stdout
depends_on:
  - BP-100i
  - BP-100i-1
  - BP-100i-2
  - BP-100i-3
doc_links:
  - path: "docs/acceptance-criteria/build_pipeline/BP-100-reliable-builds/BP-100i.yaml"
    relationship: parent
    status: exists
  - path: "docs/build-drift-hook.md"
    relationship: describes
    status: exists
  - path: "docs/pre-commit-hooks.md"
    relationship: describes
    status: exists
  - path: "docs/how-to/managing-pre-commit-hooks.md"
    relationship: describes
    status: exists
  - path: "docs/build-pipeline.md"
    relationship: describes
    status: exists
assigned_agent: python-coder
estimated_complexity: S
it_requirements:
  - "Must produce zero output on stdout and stderr when no violations are detected"
  - "Must complete within 2 seconds for repositories with up to 50 hook scripts per directory"
  - "Must be idempotent -- repeated runs with the same directory state produce the same result"
delivers_to: null
expects_from:
  ac_id: BP-100i-1
  contract: "The parity-checking entry-point function -- this AC verifies its zero-violation code path returns exit code 0 with no output"
origin_agent: business-analyst-v3
created: 2026-06-05
amended_by:
  - it-po-v3
superseded_by: null
covered_by: []
implemented_by: []
change_target: code
risk_surface: internal
