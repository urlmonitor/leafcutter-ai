---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  pr-reviewer: needed
  pull-request: needed
  python-coder: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
components:
- knowledge_management
created: '2026-06-22'
depends_on:
- KM-KGS-100c
files_touched:
- config/paths.json
- scripts/knowledge_query.py
priority: high
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: KM-KGS-100c-1
status: todo
title: Every surface declared in the config is ingested, however many there are
---

# Every surface declared in the config is ingested, however many there are

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `KM-KGS-100c-1` — Every surface declared in the config is ingested, however many there are — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `KM-KGS-100c-1`. Component: `knowledge-management`. Assigned agent: `python-coder`. Estimated complexity: `M`.

## Acceptance Criteria

```gherkin
Given a surfaces configuration that declares some set of surface entries, each
  with a path and edge_fields
When the knowledge map is built
Then every declared surface whose path exists contributes its nodes to the map
And the set of surfaces that contributed is exactly the set declared in the
  configuration (minus any declared surface marked optional whose path is absent)
And no surface is read that was not declared in the configuration
And the build asserts this against the declared set itself, not against any
  fixed expected number of surfaces
```

## Sign-offs

- [ ] python-coder
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit
- [ ] pull-request

## Comments
