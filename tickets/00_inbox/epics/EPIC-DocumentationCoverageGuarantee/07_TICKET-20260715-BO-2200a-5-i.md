---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  pr-reviewer: needed
  pull-request: needed
  python-coder: signed_off
  sql-coder: not_needed
  test-runner: needed
  test-writer: signed_off
change_target: code
complexity: low
components:
- build_orchestration
created: '2026-07-15'
depends_on:
- 06_TICKET-20260715-BO-2200a-5.md
files_touched:
- scripts/ac_store/validate_ac_schema.py
priority: medium
requires_adr: false
requires_diagram: false
risk_surface: internal
roadmap_phase: phase_1
source_ac: BO-2200a-5-i
status: todo
title: A valid documentation_triggers enum value on a non-L1 AC is still rejected
  for being off-level
---

# A valid documentation_triggers enum value on a non-L1 AC is still rejected for being off-level

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BO-2200a-5-i` — A valid documentation_triggers enum value on a non-L1 AC is still rejected for being off-level — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BO-2200a-5-i`. Component: `build-orchestration`. Assigned agent: `python-coder`. Estimated complexity: `S`. Complexity: `low`.

## Acceptance Criteria

```gherkin
Given the agent-side AC schema validator with both the enum-value check and the new
  L1-only check active,
When it validates an L2 AC whose `documentation_triggers` contains only values that
  are themselves valid enum members (for example `reference-doc`),
Then validation still fails, because the field is present on a non-L1 AC — a valid
  enum value does not exempt an off-level AC from the L1-only rule.
And when it validates an L1 AC whose `documentation_triggers` contains an invalid
  value (for example `tutorial`), validation fails on the enum check even though the
  level is permitted.
And the two rules are independent, so neither masks the other.
```

## Test Requirements

```yaml
tests:
- name: test_valid_enum_on_l2_still_fails_off_level
  file: unit_tests/ac_store/test_bo_2200a_5_i.py
  covers:
  - BO-2200a-5-i
  asserts: An L2 AC whose documentation_triggers is a valid enum value (reference-doc)
    still fails on the L1-only rule.
  framework: unittest
  type: unit
- name: test_invalid_enum_on_l1_fails_enum_check
  file: unit_tests/ac_store/test_bo_2200a_5_i.py
  covers:
  - BO-2200a-5-i
  asserts: An L1 AC whose documentation_triggers contains an invalid value (tutorial)
    fails on the enum check even though the level is permitted.
  framework: unittest
  type: unit
```

## Implementation Notes

```yaml
config_schema_fragment:
  independence_rule:
    description: The level check and the enum check for documentation_triggers must
      both run and report independently.
    type: object
constraints:
- The level check and the enum check must both run and report independently — an AC
  can fail one, the other, or both.
- Do not short-circuit the enum check when the level check passes, and do not let
  a valid enum value suppress the level check.
n_location_rule: '1'
post_write_commands: []
reference_file_path: scripts/ac_store/validate_ac_schema.py
required_skills:
- python-coder
```

## Sign-offs

- [x] python-coder — 2026-07-17 15:00
- [x] test-writer — 2026-07-17 14:22
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit
- [ ] pull-request

## Comments

### 2026-07-17 14:22 — test-writer (status: ok)
feedback-id: fb_2026-07-17_fa2bd557
completion_manifest:
  tests_written: true
  target_dir_exists: true
  no_syntax_errors: true
  tests_are_red:
    result: false
    reason: "All 3 tests passed immediately (exit 0). The independence property was
      already correctly implemented by the parent ticket BO-2200a-5 python-coder,
      who placed both the enum check and the L1-only level check as independent `if`
      branches in _validate_file(). No valid red assertion could be written without
      asserting incorrect behavior outside the AC scope."
    remediation: "Tests are well-specified regression guards for the independence
      property — they would fail if short-circuit logic were ever introduced. No
      remediation needed; python-coder for this ticket should verify the independence
      rule is still in place and consider adding an independence helper or constant
      if refactoring."

3 tests written to unit_tests/ac_store/test_bo_2200a_5_i.py covering AC BO-2200a-5-i.
Tests pass immediately because the parent ticket BO-2200a-5 (signed off 2026-07-16)
already implemented the independence property correctly — both the enum-value check
and the L1-only level check run as independent `if` branches in _validate_file(), with
no short-circuit between them. The tests serve as regression guards that would fail
if independence were broken by a future refactor. Pre-existing frontmatter violations
(missing change_target, risk_surface, invalid depends_on) were repaired as part of
this sign-off to unblock the hook.

## Test Writer — Completion Report

### Tests Written
| File | Directory | Framework | Status |
|---|---|---|---|
| test_bo_2200a_5_i.py | unit_tests/ac_store/ | unittest | written |

### Verification Run
- Command: python3 -m pytest unit_tests/ac_store/test_bo_2200a_5_i.py -v
- Result: green (3 passed — implementation pre-exists from parent ticket BO-2200a-5)

### Notes
- All 3 tests import from existing `validate_ac_schema._validate_file`; no ImportError.
- Implementation already satisfies all 3 test assertions: parent ticket's python-coder
  placed the two checks as independent `if` branches with no short-circuit.
- Tests are regression guards: they would catch any future introduction of short-circuit
  logic (e.g. `elif invalid: ... level_check` instead of independent `if` blocks).
- `test_valid_enum_on_l2_still_fails_off_level`: additionally asserts NO enum error
  appears for a valid enum value (independence in the "valid enum → level still fires"
  direction).
- `test_invalid_enum_on_l1_fails_enum_check`: additionally asserts NO level error
  appears for an L1 AC (independence in the "L1 level → enum still fires" direction).
- `test_both_checks_fire_when_both_violated`: asserts both errors appear as separate
  list entries when an AC violates both rules simultaneously.
- Frontmatter gaps repaired: added change_target (code), risk_surface (internal),
  fixed depends_on to reference actual file 06_TICKET-20260715-BO-2200a-5.md.

### 2026-07-17 15:00 — python-coder (status: ok)
feedback-id: fb_2026-07-17_c3253c23
completion_manifest:
  independence_property_verified: true
  tests_green: true
  doc_enforcer_applied: true
  decision_history_added: true
Verified that the `documentation_triggers` enum-value check and L1-only level check in
`_validate_file` run as two independent `if` branches with no short-circuit between them
— exactly as required by AC BO-2200a-5-i. All 3 regression tests pass (3/3 green). Added
MODULE/GOAL/BUSINESS CONTEXT/ARCHITECTURE docstring header fields and a DECISION HISTORY
block to `validate_ac_schema.py` to satisfy doc-enforcer requirements.
