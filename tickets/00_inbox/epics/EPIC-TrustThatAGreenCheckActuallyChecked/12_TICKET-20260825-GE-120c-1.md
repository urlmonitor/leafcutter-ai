---
ac_coverage: 5/5
ac_traceability:
  id: GE-120c-1
  path: docs/acceptance-criteria/guardrail-engine/GE-120-green-means-checked/GE-120c-1.yaml
advances_current_outcome: true
agents:
  ac-fulfillment-gate: signed_off
  ac-validator: signed_off
  commit: needed
  pull-request: not_needed
  test-runner: signed_off
  test-writer: signed_off
change_target: code
complexity: high
components:
- testing_quality
- commit_guardian
created: '2026-08-25'
depends_on: []
files_touched:
- unit_tests/portability/_deployed_check_harness.py
- unit_tests/portability/test_ge_120c_1.py
priority: high
requires_adr: false
requires_diagram: false
risk_surface: internal
roadmap_phase: phase_1
source_ac: GE-120c-1
status: todo
title: A harness executes the deployed checks out of process from a real separate
  working copy
---

# A harness executes the deployed checks out of process from a real separate working copy

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `GE-120c-1` — A harness executes the deployed checks out of process from a real separate working copy — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `GE-120c-1`. Component: `guardrail-engine`. Assigned agent: `test-writer`. Estimated complexity: `L`. Complexity: `high`.

## Acceptance Criteria

```gherkin
Given a verification harness for the guardrail engine's checks,
When it exercises a check,
Then it creates a real second working copy of the repository, stages real files in it, and
  invokes the check as a separate process in the way the commit path invokes it,
And it asserts on what that process observably produced — its result status and the text it
  wrote for the reader — and on nothing imported from the source tree,
And the source tree is not on the import path of the process under test, so a check that can
  only find its prerequisites via the source tree fails the harness rather than passing in
  it,
And the harness reports, per check exercised, the result status and output text from each of
  the two working copies, so a disagreement can be read directly rather than inferred from a
  failed assertion,
And a harness case that only searches a check's source text for a string is not accepted as
  coverage for any criterion in this tree.

Coverage note: the third clause is the one that distinguishes this harness from the tests
that already exist. Demonstrate it by pointing the harness at a check that resolves its
prerequisites only via the source tree and showing the harness fails — a harness that cannot
be made to fail is not evidence (CLAUDE.md, "Gate / Workflow ACs — Verify Behaviorally, Not
by Grep").
```

- [x] AC-1: it creates a real second working copy of the repository, stages real files in it, and
- [x] AC-2: it asserts on what that process observably produced — its result status and the text it
- [x] AC-3: the source tree is not on the import path of the process under test, so a check that can
- [x] AC-4: the harness reports, per check exercised, the result status and output text from each of
- [x] AC-5: a harness case that only searches a check's source text for a string is not accepted as

## Implementation Notes

```yaml
- 'BINDING CONSTRAINTS (these are the requirement; the mechanism below is a recommendation).
  (1) The process under test must not be able to import from the source tree: scrub
  PYTHONPATH, run the interpreter in isolated mode, and do not set cwd to the source
  tree. (2) The deployed layout must be what the check sees: .leafcutter/scripts/commit_guardian/
  inside the second copy, produced by running ''python scripts/build.py --target-dir
  <copy>'' there. (3) The invocation must match the real commit path in shape — ''python
  .leafcutter/scripts/commit_guardian/run_hook.py .leafcutter/scripts/commit_guardian/<check>.py
  <staged files>'' as .pre-commit-config.yaml declares it (line 294 for check-ac-parent-covered-by)
  — plus the direct-script shape used by precommit-canary.'
- 'OPEN QUESTION ANSWERED — recommended mechanism: ''git worktree add'' into a temporary
  directory at the same commit, then build into it. It matches how people actually
  work, which is the L1''s stated standard, and it gives real staged files via a real
  git index. CAVEAT TO HANDLE: a worktree SHARES the git object store, so the harness
  must not run concurrently with another drive in the same repository, and must clean
  up with ''git worktree remove'' even on failure. A copied tree is acceptable if
  staging can still be exercised; a clone is safest under concurrency and slowest.
  Record which was chosen and why — the behavioural requirement is only that the source
  tree is not importable and the deployed layout is what the check sees.'
- 'THE HARNESS ASSERTS ONLY ON THE SUBPROCESS''S OBSERVABLE OUTPUT: exit status, the
  result value carried in its output, and the text it wrote. It must not import any
  check module, any commit_guardian helper, or _resolve_root.py. Importing anything
  from the source tree reintroduces the exact blindness this harness exists to remove.'
- Per check exercised, record and print result status plus output text FROM BOTH copies,
  side by side, so a disagreement is readable directly rather than inferred from a
  failed assertion. This report is the artifact GE-120b-1, GE-120b-3 and GE-120b-4
  consume.
- A grep or source-text harness case is NOT acceptable coverage for any criterion
  in this tree. Encode that as a rule the harness enforces on its OWN case definitions
  — a review convention will not survive the fourth author.
- 'SELF-DEMONSTRATION IS A DELIVERABLE, not a nice-to-have: point the harness at a
  check that resolves its prerequisites only via the source tree (any of the 19 un-migrated
  files listed in GE-120b-2, before that AC migrates it) and show the harness FAILS.
  A harness that cannot be made to fail is not evidence.'
- 'HOME: unit_tests/portability/, which already holds test_build_deployment.py, the
  existing deployed-layout test. Do NOT place it in unit_tests/commit_guardian/, whose
  tests import from the source tree — that directory is the layout the defect hides
  in, and a harness living there will be tempted into the same import.'
- 'RUNTIME BUDGET: the sweep builds a second copy and spawns one process per manifest
  check (54 today, 108 across both copies). Build each copy ONCE per sweep, never
  per check, and mark the sweep slow rather than letting it quietly narrow its subject
  list to stay fast.'
- 'Error-handling policy applies to the harness''s own subprocess and filesystem calls:
  specific exceptions, logged at WARNING, never swallowed — and per GE-120c-1-i a
  setup failure is a reported failure, not a skipped case.'
```

## Test Requirements

```yaml
tests:
- name: test_ge120c1_harness_creates_real_second_copy_and_stages_files
  file: unit_tests/portability/test_ge_120c_1.py
  covers:
  - GE-120c-1
  asserts: The harness stands up a real second working copy of the repository and
    stages real files in it, with a real git index rather than a simulated one.
  framework: unittest
  type: behavioral
- name: test_ge120c1_check_invoked_as_separate_process_in_commit_path_form
  file: unit_tests/portability/test_ge_120c_1.py
  covers:
  - GE-120c-1
  asserts: 'The check is invoked as a separate process using the entry shape
    .pre-commit-config.yaml declares — run_hook.py <check>.py <staged files> — and
    also the direct-script form used by precommit-canary.'
  framework: unittest
  type: behavioral
- name: test_ge120c1_source_tree_absent_from_import_path_of_process_under_test
  file: unit_tests/portability/test_ge_120c_1.py
  covers:
  - GE-120c-1
  asserts: 'The subprocess cannot import from the source tree: PYTHONPATH is scrubbed,
    the interpreter runs isolated, and cwd is not the source tree. Asserted by
    inspecting the real subprocess environment, not by reading harness source.'
  framework: unittest
  type: behavioral
- name: test_ge120c1_source_tree_only_check_fails_the_harness
  file: unit_tests/portability/test_ge_120c_1.py
  covers:
  - GE-120c-1
  asserts: Pointed at a check that resolves its prerequisites only via the source
    tree, the harness FAILS. This is the self-demonstration deliverable — a harness
    that cannot be made to fail is not evidence.
  framework: unittest
  type: behavioral
- name: test_ge120c1_reports_status_and_output_from_both_copies_per_check
  file: unit_tests/portability/test_ge_120c_1.py
  covers:
  - GE-120c-1
  asserts: Per check exercised, the harness reports result status and output text
    from each of the two working copies, side by side, so a disagreement is readable
    directly rather than inferred from a failed assertion.
  framework: unittest
  type: behavioral
```

## Agent Contracts

### Delivers To

- **Agent:** python-coder
- **Contract:** Out-of-process execution of a deployed check from a second working copy, returning its result status and output text for comparison.

## Sign-offs

- [x] test-writer — 2026-08-31 13:00
- [x] test-runner — 2026-08-31 13:30
- [x] ac-validator — 2026-08-31 14:15
- [x] ac-fulfillment-gate — 2026-08-31 15:10
- [ ] commit

## Comments

### 2026-08-31 13:00 — test-writer (status: ok)
feedback-id: fb_2026-08-31_d0f350a6
completion_manifest:
  harness_implemented: true
  own_tests_written_and_green: true
  self_demonstration_case_present: true
  grep_only_case_self_rule_enforced: true
  sibling_ge120c1i_test_verified_green_against_real_harness: true
  cross_layer_seam_answer:
    result: covered
    producing_side: "unit_tests/portability/_deployed_check_harness.py::DeployedCheckHarness.run_sweep (the new real producer)"
    consuming_side: "unit_tests/portability/test_ge120c1i_setup_failure_reporting.py::TestSetupFailureReportedNotVacuousSuccess (a pre-existing, independently-authored consumer test that previously self-failed via its _HARNESS_MISSING_MSG guard — re-ran it against the real module and confirmed all 4 of its tests now pass green, unmodified)"
DEVIATION FROM STANDARD TDD RED-HANDOFF, DELIBERATE AND AC-SANCTIONED: this
AC's own YAML (GE-120c-1.yaml) sets `assigned_agent: test-writer`, and this
ticket's frontmatter carries no python-coder/sql-coder/frontend-coder in its
`agents:` map — there is no next-phase coder to hand a red baseline to. The
AC's own IT-PO note explains why: "the deliverable is verification apparatus,
and the failure mode being guarded against is an implementer building a
harness shaped to pass against their own fix." Per that explicit design,
test-writer authored BOTH `unit_tests/portability/_deployed_check_harness.py`
(the real DeployedCheckHarness implementation — a real second working copy
via `git init` + a real `scripts/build.py --target-dir` run, real subprocess
invocation of both entry shapes read from the real deployed manifest,
PYTHONPATH-scrubbed + `-I`-isolated child processes, and a per-check
first-copy/second-copy report) AND `unit_tests/portability/test_ge_120c_1.py`
(7 tests covering AC-1 through AC-5, including a genuine self-demonstration
that the harness FAILS on a check whose only prerequisite lives in the source
tree — verified via a real ModuleNotFoundError in the deployed-only copy
versus a real success from inside the actual source tree — and a
self-applied AST rule rejecting grep-only test cases). All 7 tests pass
(`python -m unittest discover -s unit_tests/portability -t . -p
test_ge_120c_1.py`, 7 passed in ~9.7s). No `red_baseline` block is included
in this comment because none of the newly written tests are red — that is
the intended, AC-sanctioned outcome here, not an under-specification. As
independent confirmation this is a real (not self-serving) contract, the
PRE-EXISTING sibling test file `test_ge120c1i_setup_failure_reporting.py`
(authored by a prior, separate ticket, GE-120c-1-i, before this harness
existed) was re-run unmodified against the new module and its 4 tests — which
had previously been failing via a "module not found" guard — now pass green.
A full `unit_tests/portability/` regression run shows 14 pre-existing failures
in test_ge_120b_2_i.py / test_ge_120e_2_i.py / test_ge_120e_4_i.py — these are
unrelated, expected red baselines for their own separate, not-yet-implemented
tickets and were not touched by this change.

### 2026-08-31 13:30 — test-runner (status: ok)
feedback-id: fb_2026-08-31_743aa69c
completion_manifest:
  test_suite_executed: true
  all_tests_passing: true
  failure_report_structured: true
Independently re-ran `python -m unittest discover -s unit_tests/portability -t .
-p "test_ge_120c_1.py"` (7 tests, all green) and the pre-existing sibling
`test_ge120c1i_setup_failure_reporting.py` (4 tests, all green, confirming
test-writer's claim that this real consumer test now passes against the real
harness unmodified). Also ran a full `unittest discover` over
`unit_tests/portability/` (54 tests): the 14 failures are confined to
`test_ge_120b_2_i.py`, `test_ge_120e_2_i.py`, and `test_ge_120e_4_i.py` —
none in `test_ge_120c_1.py` or `test_ge120c1i_setup_failure_reporting.py` —
matching the pre-existing, unrelated baseline test-writer reported. No
routing table in this repo maps to `unit_tests/portability`; ran the
ticket-specified suite directly per Test Requirements.

### 2026-08-31 14:15 — ac-validator (status: ok)
feedback-id: (submit-failed)
completion_manifest:
  all_acs_have_implementation_evidence: true
  all_acs_have_test_evidence: true
  ac_store_alignment_check_clean: true
  cross_file_contract_traced: true
`submit_feedback.py` rejected phase `ac-validator` as an unregistered writer
for every category in `config/feedback_categories.yaml` (not listed in
`Allowed writers` for `complete` or any other category) — recorded as
submit-failed per signoff SKILL.md §2a fallback; not treated as a phase
failure.

All 5 ACs covered. Re-ran `python -m unittest discover -s unit_tests/portability
-t . -p "test_ge_120c_1.py"` myself (7/7 green, ~18s) before writing this
verdict.

AC-1 covered: implementation `unit_tests/portability/_deployed_check_harness.py`
— `DeployedCheckHarness.create_second_copy` (git init + real `scripts/build.py
--target-dir` run, lines 140-172) and `.stage_files` (real `git add` against a
real working-copy index, lines 174-197). test:
`test_ge120c1_harness_creates_real_second_copy_and_stages_files`
(`unit_tests/portability/test_ge_120c_1.py:174`) — asserts against real `git
diff --cached --name-only` output, not a Python-side simulation.

AC-2 covered: implementation `DeployedCheckHarness.invoke_check` (lines
238-277) and `CopyCheckOutcome` dataclass (lines 92-97) — returns only
`status`/`exit_code`/`output`, the subprocess's own observable output. test:
`test_ge120c1_check_invoked_as_separate_process_in_commit_path_form`
(`test_ge_120c_1.py:208`) — exercises both the `run_hook.py`-wrapped shape
(read from the real deployed manifest) and the direct-script
`precommit-canary` shape, asserting on real subprocess exit code + stdout.

AC-3 covered: implementation `DeployedCheckHarness.build_argv` (lines
226-236, forces `-I` isolated interpreter) and `invoke_check`'s environment
construction (line 251, `PYTHONPATH` stripped from the child env). test:
`test_ge120c1_source_tree_absent_from_import_path_of_process_under_test`
(`test_ge_120c_1.py:254`) — deliberately sets `PYTHONPATH` on the parent
process to the real source `scripts/` dir, then asserts the child's own
reported `PYTHONPATH_ENV:` line is empty and its `cwd` is the working copy,
never the source tree; also asserts a source-tree-only fixture check gets a
real `ModuleNotFoundError` against the deployed-only copy. Self-demonstration
deliverable independently verified by
`test_ge120c1_source_tree_only_check_fails_the_harness`
(`test_ge_120c_1.py:314`), which runs the identical fixture script from
inside a real source-tree location (success) versus the deployed-only copy
(failure) and asserts the two copies' statuses disagree.

AC-4 covered: implementation `DeployedCheckHarness.run_sweep`
(`_deployed_check_harness.py:280-363`) and `CheckSweepEntry`
(lines 100-114) — builds a `first`/`second` `CopyCheckOutcome` pair per check
and a side-by-side `message` line (`f"{check_id}: first_copy=... second_copy=..."`,
lines 348-353). test:
`test_ge120c1_reports_status_and_output_from_both_copies_per_check`
(`test_ge_120c_1.py:366`) — asserts both `entry.first`/`entry.second` are
populated per check and that `first_copy=`/`second_copy=` labels appear in
the returned message text.

AC-5 covered: implementation `enforce_no_grep_only_test_cases`
(`_deployed_check_harness.py:378-407`) — AST-walks a test file's `test_*`
functions and flags any that read source text (`.read_text(`) plus a
containment check (`" in "`) without ever invoking `subprocess`,
`invoke_check`, or `run_sweep`. test:
`test_ge120c1_grep_only_case_flagged_by_harness_self_check` (flags a
synthetic grep-only case) and
`test_ge120c1_own_test_file_has_no_grep_only_cases` (self-applies the rule
to `test_ge_120c_1.py` itself, both at `test_ge_120c_1.py:413` and `:434`).

Cross-file contract (this AC's `delivers_to` python-coder / consumed by the
independently-authored, pre-existing sibling ticket GE-120c-1-i): re-read
`unit_tests/portability/test_ge120c1i_setup_failure_reporting.py` and
confirmed every field it reads on `HarnessResult`
(`success`, `checks_exercised`, `manifest_check_count`, `message`,
`failed_setup_step`) is populated exactly as that consumer expects by
`run_sweep`'s early-return setup-failure branch
(`_deployed_check_harness.py:298-323`); re-ran its 4 tests myself
(`python -m unittest discover -s unit_tests/portability -t
<worktree-root> -p "test_ge120c1i_setup_failure_reporting.py"`, all green) —
no contract gap.

AC-store alignment: `python scripts/commit_guardian/check_v2_ac_store_alignment.py
--ticket <this ticket>` exited 0 (clean, no ERROR lines).

No `## AC Coverage` table exists on this ticket and no `### ac-validator`
heading exists under `## Agent Contracts`, so per signoff SKILL.md
§2c.1/§2c.2 the AC-coverage-table fill step is correctly skipped — I flipped
the 5 checkboxes in the ticket body's own `## Acceptance Criteria` section
instead (Step 4a) and set `ac_coverage: 5/5` in frontmatter (Step 4c).
Commit phase may proceed.

### 2026-08-31 15:10 — ac-fulfillment-gate (status: ok)
feedback-id: (submit-failed)
completion_manifest:
  ac_coverage_resolver_ran: true
  work_status_auto_fixed: true
  implemented_by_auto_fixed: true
  covered_by_verified_non_empty: true
  schema_validation_passed: true
`submit_feedback.py` rejected phase `ac-fulfillment-gate` as an unregistered
writer for category `complete` (same gap ac-validator hit at 14:15) —
recorded as submit-failed per SKILL.md §2a fallback, not a phase failure.

Resolved AC via `ac_coverage_resolver.py`: 1 AC (`GE-120c-1`, L2) from the
two-key traceability block. Initial resolver run flagged `work_status`
(`todo`) as the only failing field. Auto-fixed, append-only, both diff-backed:
`work_status: todo` -> `done` (both new files are staged and present in
`git diff HEAD --name-only`, matching `files_touched`), and appended
`unit_tests/portability/_deployed_check_harness.py` and
`unit_tests/portability/test_ge_120c_1.py` to `implemented_by` (previously
only listed the ticket file itself, which is not in `files_touched`).
`covered_by` was already non-empty (`GE-120c-1-i`, the store's convention for
an L2 pointing at its `-i` child rather than a literal test path) — left
untouched, no auto-fix needed. Ran `check_ac_schema.py` on the edited YAML
after each edit (exit 0, manual-fallback mode since
`config/ac_store_schema.json` is absent in this workspace layout). Re-ran the
resolver: `ok: true`, zero failures. Commit phase may proceed.

### 2026-08-31 15:45 — commit (status: blocker)
feedback-id: (submit-failed)
completion_manifest:
  pre_commit_hooks_pass:
    result: false
    reason: "check-build-drift (a required pre-commit hook) fails independently of this ticket's staged files."
    remediation: "Re-run scripts/build.py --force to resync templates/ with deployed output, review the resulting diff, and commit that separately from this ticket's change."
  commit_message_valid: true
  ticket_staged: true
Attempted `COMMIT_AGENT_MODE=1 git commit` with the 4 staged files
(`docs/acceptance-criteria/guardrail-engine/GE-120-green-means-checked/GE-120c-1.yaml`,
this ticket file, `unit_tests/portability/_deployed_check_harness.py`,
`unit_tests/portability/test_ge_120c_1.py`) and subject "feat(testing-quality):
out-of-process harness for deployed commit-guardian checks (GE-120c-1)". The
`check-build-drift` pre-commit hook BLOCKED with 8 drifted templates
(`templates/agents/ac-fulfillment-gate.md`, `templates/agents/documentation-verifier.md`,
`templates/agents/test-writer.md`, `templates/scripts/commit_guardian/_ac_schema_validators.py`,
`templates/scripts/commit_guardian/_drift_exemptions.py`,
`templates/scripts/commit_guardian/check_adr_collision.py`,
`templates/scripts/commit_guardian/check_build_drift.py`,
`templates/scripts/commit_guardian/check_output_drift.py`) plus 10 UNCOMPARABLE gaps
in `templates/scripts/commit_guardian/`, none of which appear in this ticket's
`files_touched` or staged set.

Verified this is pre-existing and unrelated to this ticket, per the "When Tests
Fail at Pre-Commit" determination step: `git stash`'d the 4 staged files, ran
`python3 .leafcutter/scripts/commit_guardian/check_build_drift.py` directly
against the clean HEAD (`d83eecf97`, nothing staged) — it failed identically
(same 8 drifted files, same 10 gaps, `RESULT verified=161 uncomparable=10
gaps=10 drifted=8`). `git stash pop`'d immediately after and re-staged the 4
files (confirmed via `git diff --cached --stat`, same 4-file/1089-insertion
diff as before the test). None of the 8 drifted / 10 gap files are modified in
the working tree either — this is baked into the current branch tip, not
something this ticket's diff touches or introduces.

Per the commit agent's `--no-verify` policy and the "genuinely pre-dates this
change" rule: NOT bypassing the gate. Running `scripts/build.py --force` to
fix the drift is out of scope for this commit — it would pull unrelated
template/build-output changes into a commit whose message and AC/store edits
are scoped to GE-120c-1, and per this repo's own CLAUDE.md caution about
`build.py`'s `docs_root` hazard it should not be run casually. The 4 files
this ticket touches remain staged and untouched; the commit has NOT been made.
Escalating to the epic supervisor / user: someone needs to run `scripts/build.py
--force` (or hand-fix the 8 templates + 10 gap registrations) in a dedicated
commit before this ticket's commit phase can proceed, since `check-build-drift`
is a required hook that blocks unconditionally regardless of what is staged.
