---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  pr-reviewer: needed
  pull-request: needed
  python-coder: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
components:
- commit_guardian
created: '2026-06-16'
depends_on:
- GE-100
files_touched:
- docs/acceptance-criteria/guardrail-engine/GE-100-code-quality-hooks/GE-100.yaml
- docs/pre-commit-hooks.md
- docs/how-to/managing-pre-commit-hooks.md
priority: high
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: GE-100d
status: todo
title: diff-cover hook exits cleanly when the diff-cover tool or coverage artifact
  is absent
---

# diff-cover hook exits cleanly when the diff-cover tool or coverage artifact is absent

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `GE-100d` — diff-cover hook exits cleanly when the diff-cover tool or coverage artifact is absent — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `GE-100d`. Component: `guardrail-engine`. Assigned agent: `python-coder`. Estimated complexity: `S`.

## Acceptance Criteria

```gherkin
Given the commit guardian configuration has the diff_coverage section with:
  enabled: true
  strict: false
  min_coverage_percent: 80
  coverage_xml_path: "coverage.xml"
  max_age_seconds: 3600
And the diff-cover binary is not present on the system PATH
When the diff-cover hook runs at pre-commit time
Then the hook exits with code 0 (fail-open)
And it emits an advisory message to stderr stating that diff-cover was not found
And the advisory includes installation guidance

---

Given the diff-cover binary IS present on the system PATH
And the configured coverage.xml file does not exist at the expected path
When the diff-cover hook runs at pre-commit time
Then the hook exits with code 0 (fail-open)
And it emits an advisory message stating that coverage.xml was not found
And the advisory includes guidance on generating the coverage artifact
```

## Sign-offs

- [ ] python-coder
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit
- [ ] pull-request

## Comments
