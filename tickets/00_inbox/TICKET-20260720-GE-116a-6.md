---
ac_traceability:
  id: GE-116a-6
  path: docs/acceptance-criteria/guardrail-engine/GE-116-consistent-agent-config/GE-116a-6.yaml
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: needed
  pull-request: needed
change_target: docs
complexity: low
components:
- commit_guardian
created: '2026-07-20'
depends_on: []
files_touched:
- docs/how-to/managing-pre-commit-hooks.md
- docs/pre-commit-hooks.md
priority: medium
requires_adr: false
requires_diagram: false
risk_surface: internal
roadmap_phase: phase_1
source_ac: GE-116a-6
status: todo
title: Reference documentation covers the agent-definition consistency guard
---

# Reference documentation covers the agent-definition consistency guard

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `GE-116a-6` — Reference documentation covers the agent-definition consistency guard — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `GE-116a-6`. Component: `guardrail-engine`. Assigned agent: `documentation-expert`. Estimated complexity: `S`. Complexity: `low`.

## Acceptance Criteria

```gherkin
Given the agent-definition consistency guard exists and blocks
  verification-required read-only agents at commit time,
When a contributor reads the pre-commit hook reference documentation,
Then the documentation describes the guard's rule (an agent that requires
  verification must be able to edit or create files),
And it states what triggers a block and the two valid fixes,
And it records that this contradiction was previously caught only after merge
  by the registry self-description validation.
```

- [ ] AC-1: the documentation describes the guard's rule (an agent that requires
- [ ] AC-2: it states what triggers a block and the two valid fixes
- [ ] AC-3: it records that this contradiction was previously caught only after merge

## Implementation Notes

```yaml
- 'Document in docs/pre-commit-hooks.md (the commit_guardian reference doc): the guard
  rule (requires_verification: true requires Edit or Write), what triggers a block,
  and the two valid fixes (add Edit/Write to tools, or set requires_verification:
  false).'
- Record that this contradiction was previously caught only post-merge by build.py's
  registry self-description validation, and is now caught fail-closed at commit time.
- Keep docs/how-to/managing-pre-commit-hooks.md consistent if it enumerates hooks
  (add the new guard where hooks are listed).
```

## Agent Contracts

### Expects From

- **AC:** GE-116a-1
- **Contract:** The implemented guard's rule (requires_verification: true requires Edit or Write), its block triggers, the two valid fixes, and the block message, plus the historical registry self-description validation context to document.

## Sign-offs

- [ ] documentation-expert
- [ ] commit
- [ ] pull-request

## Comments
