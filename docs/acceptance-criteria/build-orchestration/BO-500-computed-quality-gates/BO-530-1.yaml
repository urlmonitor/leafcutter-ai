id: BO-530-1
readiness: reviewed
priority: medium
components:
  - build-orchestration
title: "test-writer is injected before any coder-class agent in the computed chain"
component: build-orchestration
level: L2
status: active
req_status: active
work_status: todo
criteria: |
  Given a ticket has change_type: flow_change
    and the assigned work agent is python-coder (produces: production_code),
  When ticket-supervisor computes the agent chain,
  Then test-writer appears in the chain with a lower sequence number
    than python-coder
  And test-writer's status is set to "needed"
  And test-writer is dispatched and must sign off before python-coder is spawned

  Given a ticket has change_type: data_change
    and the assigned work agent is sql-coder (produces: production_code),
  When ticket-supervisor computes the agent chain,
  Then test-writer appears before sql-coder in the sequence
  And sql-test-writer (if applicable) also appears before sql-coder

  Given a ticket has change_type: doc_change
    and the assigned work agent is documentation-expert (produces: documentation),
  When ticket-supervisor computes the agent chain,
  Then test-writer does NOT appear in the chain
  And the chain does not include any TDD-related agents
depends_on: [BO-530, BO-510-5]
doc_links:
  - path: templates/agents/ticket-supervisor.md
    relationship: describes
    status: exists
  - path: templates/skills/building-epics/SKILL.md
    relationship: describes
    status: exists
  - path: config/agent_registry.json
    relationship: describes
    status: exists
  - path: docs/architecture/components/build-ticket-workflow-dispatch.md
    relationship: describes
    status: exists
assigned_agent: llm-expert
estimated_complexity: M
it_requirements:
  - "Template changes must preserve existing dispatch behavior for non-affected change types"
  - "TDD injection must be driven by the produces trait, not agent ID string matching"
delivers_to: null
expects_from:
  ac_id: BO-510-5
  contract: "Ticket-supervisor reads the produces trait to determine guardrails"
origin_agent: business-analyst-v3
created: 2026-06-05
amended_by: [it-po-v3]
superseded_by: null
covered_by: [BO-530-1-i]
implemented_by: []
change_target: prompt
risk_surface: internal
product_truth:
  - flow: leafcutter/how-acs-are-built
    node: test
    node_kind: step
    flow_kind: data
    screen: null
    mock_data: null
    entities:
      - Ac
      - Test
      - Ticket
    source: real
    asof: '2026-07-14'
