---
title: "Changelog PR #450 — Sequence the AC-Driven Build v2 migration and close its five gaps"
date: "2026-08-17"
time: "14:58"
type: manual
components:
  - ac_driven_dev
  - roadmap
summary: "Made the move to requirement-as-unit-of-work actually plannable: the build order is now recorded where the machine reads it, the big cutover is split into a half that ships on its own and a half that can wait, and the five jobs the ticket does today that nothing else was going to pick up now have owners."
description: "1 commit, PR #450 — feat(ac-driven-dev): sequence the ticketless migration (ADR-026) and close its five gaps. 92 files, 36 added. ORDERING: every migration L1 previously listed only its parent L0 in depends_on, so a supervisor batching by depends_on saw twelve independent leaves; the real order existed only as prose in notes and in ADR-026, which itself warns the AC numbering disagrees with the plan. Adds cross-tree depends_on edges with per-file amended_by rationale, after which the foundation topologically sorts into six parallel-safe waves. The most load-bearing edge is ACD-1600a to ACD-1900b-5 — the latter's title says it exists so a thin ticket is always verified, and neither file referenced the other; thinning ahead of it fails quietly, since ac-fulfillment-gate signs off status ok having read no YAML and ac-validator finds an empty coverage table and reports zero unmet criteria. One genuine contradiction resolved rather than papered over: ACD-1900c's notes said it depends on ACD-1900b while fresh analysis concluded the reverse, and encoding both is a cycle — declaring the flag is inert and precedes dual-read, demonstrating the kill-switch needs dual-read to exist, so the edge runs 1900c to 1900b with amended_by recording why not to add the reverse. ROADMAP: phase_acbuild_2_cutover split into phase_acbuild_2a_unit_of_work (requirement gains the deliverable checklist, per-deliverable sign-offs and the five gap fields, ticket untouched, independently shippable) and phase_acbuild_2b_ticket_demotion (thin the ticket, move sign-offs off it), mirroring the two flag stages ADR-026 already defines; as one phase there was no state you could stop at and ship. Every AC referencing the old id retagged — nothing validates roadmap_phase against roadmap.json, so a stale id fails silently rather than blocking. Phase 1's third exit criterion, 'all phase agents and gates dual-read', was unverifiable as written and covered two LLM agents whose dual-read is prose in a template; replaced with the three gates that actually degrade, named, plus a behavioural-evidence requirement. GAPS: ACD-1600g scope boundary (out_of_scope has no AC-side existence), ACD-1800f observable-behaviour proof, new L0 ACD-2000 for the adjudication trail and parallel-safety claim, and ACD-1800a-4 for product-truth artifacts as a declarable deliverable — decomposed into 30 L2/L3 leaves and technically enriched, every parent at or under its child cap. Two design traps are specified rather than left to be discovered: exclusions compose by difference not union, since one member's out_of_scope is routinely another member's files_touched; and the product-truth status cycle has two entry points, because flow.schema.json's expands_to gives impl_status a second derivation that takes precedence over implements. CORRECTIONS: ADR-026 listed done_proof as a phantom-green hazard, but verify_done_eligible takes (ac_id, ac_root, test_root) and the module has no ticket reference — it is the reference design for AC-anchored evidence, so the claim is struck with evidence and check_ticket_signoff_parity named as the real third case, which fails loud rather than silent. ADR-026 also called additionalProperties false a dormant tripwire nothing enforces; the ACS-200e fix wired jsonschema.Draft7Validator into both the validator and the commit hook, upgrading ACD-1900a to a hard ordering gate on every write-side item. New PROJECT_CONTEXT.md for ac-driven-dev records five fields named by an agent template or runbook as driving behaviour that have zero occurrences in BOTH the AC schema and the generator — declares_side_effect, user_facing_surface, live_surface_test, test_failure_rework_cap and schema_version — the first making the mandatory non-overridable user-surface-smoker gate unreachable from the AC path entirely, the last being the data signal ADR-026's own kill-switch reads to decide whether enforce may be turned on. Also fixes ACD-1800b, which declared child_limit_override 6 with 7 children, already a violation under check_ac_limits' child_count greater-than-limit test and dormant only because the hook checks staged files. Verified: all 164 touched AC files valid against config/ac_store_schema.json via explicit globs; store-wide 2922 ACs with zero duplicate ids, zero dependency cycles and zero dangling roadmap_phase references; roadmap.json passes check_roadmap_schema and roadmap.md regenerated by its hook; full pre-commit suite green including AC governance, parent covered_by back-links, circular depends_on, tree-depth limits and done-proof. No production code changed. Everything remains readiness draft."
pr: 450
commits:
  - 06a482b33
breaking: false
---

## Entry
