---
advances_current_outcome: true
agents:
  commit: signed_off
  documentation-expert: not_needed
  llm-expert: signed_off
  pr-reviewer: signed_off
  pull-request: signed_off
  sql-coder: not_needed
  test-runner: signed_off
  test-writer: signed_off
complexity: low
components:
- build_orchestration
created: '2026-07-15'
change_target: prompt
depends_on:
- 09_TICKET-20260715-BO-2200b-2.md
files_touched: []
priority: medium
risk_surface: internal
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: BO-2200b-2-i
status: done
title: Partial documentation coverage still fails, naming the specific missing doc
---

# Partial documentation coverage still fails, naming the specific missing doc

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BO-2200b-2-i` — Partial documentation coverage still fails, naming the specific missing doc — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BO-2200b-2-i`. Component: `build-orchestration`. Assigned agent: `llm-expert`. Estimated complexity: `S`. Complexity: `low`.

## Acceptance Criteria

```gherkin
Given a ticket whose documentation brief requires two documentation files,
When the git diff contains a real change to the first required file but no change to
  the second,
Then the verifier reports `status: blocker` rather than passing on partial coverage.
And the blocker message names the second file as the one still missing a real change,
  and does not name the first file (which was satisfied).
```

## Implementation Notes

```yaml
'Extend the documentation-verifier template instructions so the agent evaluates EACH

  required doc independently and fails if ANY is missing, reporting precisely which
  ones.

  One satisfied doc must not mask an unsatisfied sibling. This is template-prompt

  hardening of the diff-presence behavior authored in BO-2200b-2.

  '
```

## Sign-offs

- [x] llm-expert — 2026-07-20 16:15
- [x] test-writer — 2026-07-20 14:30
- [x] test-runner — 2026-07-20 17:00
- [x] pr-reviewer — 2026-07-20 18:00
- [x] commit — 2026-07-20 17:15
- [x] pull-request — 2026-07-20 17:35

## Comments

### 2026-07-20 14:30 — test-writer (status: ok)
feedback-id: fb_2026-07-20_1b8fd54f
completion_manifest:
  tests_written: true
No production-code agent in this ticket — no tests required (non-code ticket). AC BO-2200b-2-i has `test_required: false` in the AC YAML; the assigned agent is `llm-expert` (prompt hardening only). Also corrected three pre-existing frontmatter violations generated by build-ac: `depends_on` now uses the full filename, and `change_target: prompt` and `risk_surface: internal` were added.

### 2026-07-20 16:15 — llm-expert (status: ok)
feedback-id: fb_2026-07-20_608d7831
completion_manifest:
  template_written: true
  prompt_quality_checklist_passed: true
  convention_violations_resolved: true
Hardened `templates/agents/documentation-verifier.md` Step 5 per AC BO-2200b-2-i. Added step 4 to the per-doc loop ("If it DOES appear → satisfied; do NOT add to `missing_docs`"), an explicit "Independent evaluation guarantee" block stating the full list is iterated without early exit and satisfied docs must not appear in the blocker, a "List ONLY the paths in `missing_docs`" instruction before the blocker message, a clarifying parenthetical inside the blocker message template, and updated the Signoff Comment Schema failure path `reason` hint to say "MISSING doc paths only". All six Prompt-Quality Checklist items pass (no compound bash, tools match, no extras, no sub-agents, signoff section present, stop-and-ask present).

### 2026-07-20 17:00 — test-runner (status: ok)
feedback-id: fb_2026-07-20_548a0ade
completion_manifest:
  test_suite_executed:
    result: false
    reason: "AC BO-2200b-2-i is prompt-hardening only (test_required: false, change_target: prompt); git diff shows only template/ticket changes — routing is no-op per routing table."
    remediation: "No remediation needed; correct behavior for a non-code ticket."
  all_tests_passing: true
  failure_report_structured: true
No test suite executed: prompt-hardening only ticket (change_target: prompt, test_required: false). Git diff shows templates/agents/documentation-verifier.md and ticket files only — no Python source files changed. Routing: no-op. Feedback submitted successfully.

### 2026-07-20 17:15 — commit (status: ok)
Auto-authorized commit gate: subject "chore(ticket): commit phase sign-off for BO-2200b-2-i"; staged files: tickets/00_inbox/epics/EPIC-DocumentationCoverageGuarantee/10_TICKET-20260715-BO-2200b-2-i.md.
feedback-id: fb_2026-07-20_c346e8bd
completion_manifest:
  pre_commit_hooks_pass: true
  commit_message_valid: true
  ticket_staged: true
Implementation for BO-2200b-2-i (independent evaluation guarantee in documentation-verifier.md Step 5) was already committed in a8b10f0b as part of BO-2200b-3-i. Ticket sign-off committed separately.

### 2026-07-20 17:35 — pull-request (status: ok)
feedback-id: fb_2026-07-20_a5e5a5df
completion_manifest:
  branch_pushed: true
  pr_created: true
  pr_body_complete: true
Pushed 1 pending commit (b8f4f7d4 commit phase sign-off) to existing PR #337 (feat(config): add documentation coverage guarantee (BO-2200)) at https://github.com/urlmonitor/leafcutter-ai/pull/337. Merge state: MERGEABLE / BLOCKED (CI and pr-reviewer pending). pr-reviewer remains needed; status: todo unchanged.

### 2026-07-20 18:00 — pr-reviewer (status: ok)
feedback-id: (submit-failed)
completion_manifest:
  ac_implementation_verified: true
  high_confidence_findings: true
  medium_confidence_findings: true
  working_tree_clean:
    result: false
    reason: "git diff HEAD shows 149 lines of uncommitted changes across 3 files unrelated to this ticket: (1) templates/workflows-js/build-epic.js and build-ticket.js each receive a duplicate parseAgentJson function definition — committed HEAD already has one at line 171; the uncommitted diff inserts a second copy 56 lines earlier, producing two function declarations in each file. (2) tickets/.../05_TICKET-BO-2200a-4.md has uncommitted test-writer sign-off and red_baseline content."
    remediation: "Before any future commit from this worktree, discard the duplicate-function changes (git checkout -- templates/workflows-js/build-epic.js templates/workflows-js/build-ticket.js) and commit the ticket-05 changes under that ticket's own commit. The duplicate function definitions are NOT in PR #337 — this is a worktree-state advisory only."

**Review Report**

**Base:** origin/main
**Diff size:** 149 lines changed across 3 files (uncommitted working tree); 6029 insertions across 54 files (committed branch vs origin/main)

**AC Implementation (committed):** PASS. templates/agents/documentation-verifier.md Step 5 correctly implements AC BO-2200b-2-i — the independent evaluation guarantee block is present, the full required_docs list is iterated without early exit, satisfied docs are explicitly excluded from missing_docs and from the blocker message, and the Signoff Comment Schema failure path reason field carries the "MISSING doc paths only" constraint. Decision history entry 2026-07-20 confirms the specific additions.

**High-confidence finding (uncommitted, advisory):**

[H-1] templates/workflows-js/build-epic.js + build-ticket.js — duplicate parseAgentJson function definition in uncommitted working tree.
The committed HEAD defines parseAgentJson once at line 171 in each file. The uncommitted diff (git diff HEAD) inserts a second, identical copy starting at line 168 in each file. If these changes were committed, each file would contain two function parseAgentJson declarations in the same scope. In non-strict JS the latter declaration wins, but the double-definition is a real defect and a code review failure. These changes are NOT part of PR #337. Discard them (git checkout -- templates/workflows-js/build-epic.js build-ticket.js) before any future commit from this worktree.

**Medium-confidence finding (uncommitted, advisory):**

[M-1] tickets/.../05_TICKET-20260715-BO-2200a-4.md — uncommitted test-writer sign-off and red_baseline left in working tree.
The test-writer phase for ticket 05 (BO-2200a-4) left its sign-off edits uncommitted. These must be committed under that ticket's own commit flow, not mixed into a future commit for this ticket.

**Suppression Tally:** 2 low-confidence nits suppressed (traceability: BO-2200b-2-i implementation bundled in BO-2200b-3-i commit; no test_bo_2200b_2.py — expected for test_required: false). 0 medium findings dropped by Opus.

**Escalation**

Branch: none
Reason: not escalated — medium count was 1 (threshold > 3). PR content (committed changes) correctly implements AC BO-2200b-2-i; H-1 is advisory (uncommitted, not in PR).
