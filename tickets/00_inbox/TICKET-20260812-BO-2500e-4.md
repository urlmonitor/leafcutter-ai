---
ac_traceability:
  id: BO-2500e-4
  path: docs/acceptance-criteria/build-orchestration/BO-2500-mechanical-done-proof/BO-2500e-4.yaml
advances_current_outcome: true
agents:
  ac-fulfillment-gate: needed
  ac-validator: needed
  commit: needed
  documentation-expert: needed
  documentation-verifier: needed
  pr-reviewer: needed
  pull-request: needed
  python-coder: needed
  test-runner: needed
  test-writer: needed
change_target: pipeline
complexity: medium
components:
- build_orchestration
- testing_quality
created: '2026-08-12'
depends_on:
- BO-2500e
documentation_required: true
files_touched:
- scripts/ac_store/mark_ac_done.py
priority: medium
requires_adr: false
requires_diagram: false
risk_surface: internal
roadmap_phase: phase_1
source_ac: BO-2500e-4
status: todo
title: A JS-covered AC stuck in progress can be mechanically marked done
---

# A JS-covered AC stuck in progress can be mechanically marked done

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BO-2500e-4` — A JS-covered AC stuck in progress can be mechanically marked done — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BO-2500e-4`. Component: `build-orchestration`. Assigned agent: `python-coder`. Estimated complexity: `M`. Complexity: `medium`.

## Acceptance Criteria

```gherkin
Given an active AC currently stuck at work_status in_progress solely because its only tests are
  passing "// covers:<id>" vitest tests (e.g. one of the 12 UXP-596 / UXP-591 ACs),
When the done-proof oracle evaluates it and the mark-done step runs,
Then the AC is mechanically marked done through the same mark-done path used for Python-covered
  ACs — it is no longer blocked for being non-Python,
So front-end features backed by real, passing tests can be proven done end to end.
```

- [ ] AC-1: the AC is mechanically marked done through the same mark-done path used for Python-covered

## Test Requirements

```yaml
tests:
- name: test_js_covered_ac_marked_done_via_shared_path
  file: unit_tests/ac_store/test_bo_2500e_4.py
  covers:
  - BO-2500e-4
  asserts: A JS-covered AC stuck in_progress with a passing vitest test is mechanically
    marked done through the same mark-done path used for Python-covered ACs.
  framework: unittest
  type: integration
- name: test_js_mark_done_gated_on_eligibility
  file: unit_tests/ac_store/test_bo_2500e_4.py
  covers:
  - BO-2500e-4
  asserts: A JS-covered AC is transitioned to done only when the BO-2500e-2 eligibility
    verdict holds, not on tag presence alone.
  framework: unittest
  type: integration
```

## Implementation Notes

```yaml
config_schema_fragment:
  done_proof_js_mark_done_end_to_end:
    description: 'End-to-end mark-done for a JS-covered AC: an AC stuck in_progress
      solely because its only tests are passing ''// covers:<id>'' vitest tests flows
      through the SAME mark-done path used for Python-covered ACs and is transitioned
      to done — no longer blocked for being non-Python.'
    type: object
constraints:
- The JS-covered AC must flow through the SAME mark_ac_done path used for Python-covered
  ACs (no separate JS-only transition), so front-end work is marked done end to end
  via one mechanism.
- The mark-done must be gated on the JS eligibility verdict from BO-2500e-2 (genuine
  passing vitest), never on the mere presence of a '// covers:' tag.
- The transition must be idempotent — re-running on an already-done AC is a no-op
  — and must only touch the target AC's work_status field.
- Must unblock the currently stuck UXP-596 / UXP-591 set that sits at in_progress
  solely for being non-Python.
n_location_rule: '1'
post_write_commands: []
reference_file_path: scripts/ac_store/mark_ac_done.py
required_skills:
- python-coder
```

## Agent Contracts

### documentation-expert

Existing docs to update / cross-link:

- docs/architecture/components/build-orchestration.md (relationship: describes | status: exists)
- docs/architecture/adrs/ADR-004-tdd-workflow-enforcement.md (relationship: describes | status: exists)
- docs/architecture/adrs/ADR-003-test-source-of-truth-discipline.md (relationship: describes | status: exists)

- [ ] AC-1: [how-to] docs/architecture/components/build-orchestration.md — the AC is mechanically marked done through the same mark-done path used for Python-covered
- [ ] AC-2: [sequence-diagram] docs/architecture/components/build-orchestration.md — the AC is mechanically marked done through the same mark-done path used for Python-covered

### Expects From

- **AC:** BO-2500e-2
- **Contract:** Per-AC JS eligibility verdict { ac_id, can_be_done, reason } that gates whether mark_ac_done transitions the AC to done.

## Sign-offs

- [ ] test-writer
- [ ] python-coder
- [ ] test-runner
- [ ] documentation-expert
- [ ] pr-reviewer
- [ ] ac-validator
- [ ] ac-fulfillment-gate
- [ ] documentation-verifier
- [ ] commit
- [ ] pull-request

## Comments
