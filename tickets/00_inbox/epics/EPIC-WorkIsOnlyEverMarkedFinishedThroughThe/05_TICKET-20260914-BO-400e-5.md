---
ac_traceability:
  id: BO-400e-5
  path: docs/acceptance-criteria/build-orchestration/BO-400-ticket-status-source-of-truth/BO-400e-5.yaml
advances_current_outcome: true
agents:
  ac-fulfillment-gate: not_needed
  ac-validator: not_needed
  architect-review: not_needed
  architecture-diagram-author: needed
  commit: needed
  documentation-expert: needed
  documentation-verifier: needed
  frontend-coder: not_needed
  pr-reviewer: not_needed
  pull-request: not_needed
  python-coder: not_needed
  sql-coder: not_needed
  test-runner: not_needed
  test-writer: not_needed
  user-surface-smoker: not_needed
change_target: docs
complexity: medium
components:
- build_orchestration
- supervisor_system
created: '2026-09-14'
depends_on:
- 01_TICKET-20260914-BO-400e-1.md
- 02_TICKET-20260914-BO-400e-2.md
- 03_TICKET-20260914-BO-400e-3.md
- 04_TICKET-20260914-BO-400e-4.md
documentation_required: true
files_touched:
- docs/architecture/agent_delivery_workflows.md
- docs/architecture/components/build-orchestration.md
priority: high
requires_adr: false
requires_diagram: false
risk_surface: internal
roadmap_phase: phase_1
source_ac: BO-400e-5
status: todo
title: A sequence diagram shows every route to the finished state, and that exactly
  one of them passes the guard
---

# A sequence diagram shows every route to the finished state, and that exactly one of them passes the guard

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BO-400e-5` — A sequence diagram shows every route to the finished state, and that exactly one of them passes the guard — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BO-400e-5`. Component: `build-orchestration`. Assigned agent: `architecture-diagram-author`. Estimated complexity: `M`. Complexity: `medium`.

## Acceptance Criteria

```gherkin
Given a reader who needs to know how a ticket comes to be recorded finished, and who has
  not read the drivers,
When they open the architecture documentation for the close of a ticket,
Then a sequence diagram is present that shows the close from the moment a drive decides a
  ticket is finished to the moment the recorded state changes,
  And every participant that can write the ticket's recorded state appears in it, so that
    a route which is not drawn is a route which does not exist,
  And the checking mechanism is shown reading the ticket's own record and deciding from
    what it read, not from what the caller passed it,
  And both drivers are shown -- the one that carries a whole epic and the one that carries
    a single ticket -- and both are shown reaching that same mechanism, so a reader can
    see whether the two have diverged,
  And the refusal is drawn as its own path, ending with the recorded state unchanged and
    the outstanding step named, rather than the diagram showing only the successful close,
  And the blanket override is drawn as a path that exists and is marked as absent from the
    ordinary close,
  And exactly one path in the diagram ends in the finished state being written, and that
    path passes through the checking mechanism,
So the diagram answers the question this feature exists to answer -- how many doors are
  there -- rather than only illustrating the happy path through one of them.
```

- [ ] AC-1: a sequence diagram is present that shows the close from the moment a drive decides a
- [ ] AC-2: every participant that can write the ticket's recorded state appears in it, so that
- [ ] AC-3: the checking mechanism is shown reading the ticket's own record and deciding from
- [ ] AC-4: both drivers are shown -- the one that carries a whole epic and the one that carries
- [ ] AC-5: the refusal is drawn as its own path, ending with the recorded state unchanged and
- [ ] AC-6: the blanket override is drawn as a path that exists and is marked as absent from the
- [ ] AC-7: exactly one path in the diagram ends in the finished state being written, and that

## Implementation Notes

```yaml
config_schema_fragment: null
constraints:
- THE DIAGRAM IS DERIVED FROM THE RECORDS, NOT FROM THE SOURCE. Draw the close path
  BO-400e-1 through BO-400e-4 establish. If the diagram and those records ever disagree,
  the records are right and the diagram is stale -- this record must never be read
  as amending them.
- 'AUTHOR IT AFTER THE BEHAVIOUR LANDS. depends_on already orders this last for a
  reason: a diagram of the single door drawn before the single door exists documents
  a shape the repository does not have, and is indistinguishable from documentation
  of the shape it does.'
- THE DIAGRAM MUST BE FALSIFIABLE AGAINST THE DRIVERS. Its stated purpose is that
  a route which is not drawn is a route which does not exist. That only holds if EVERY
  participant able to write the ticket's recorded state is named, and if 'exactly
  one path ends in the finished state' is stated on the diagram itself rather than
  left for the reader to count. A diagram a later second door could be added under
  without contradicting it has failed.
- NO HAPPY-PATH-ONLY DIAGRAM. The refusal path is drawn as its own path with its own
  terminal state -- recorded state unchanged, outstanding step named -- and the blanket
  override is drawn as a path that exists and is marked as absent from the ordinary
  close. Omitting either documents the half that was never in doubt.
- BOTH DRIVERS OR IT HIDES THE THING IT IS FOR. The epic driver and the single-ticket
  driver must both appear and must both be shown reaching the same mechanism, so a
  reader can see whether the twins have diverged. A diagram of one of them cannot
  show a divergence between them.
- FOLLOW THE HOUSE FORMAT. docs/architecture/agent_delivery_workflows.md already carries
  the worktree-lifecycle, approval-to-PR and files-touched-reconciliation sequences,
  each with a dated changelog trailer. Add the close-path sequence there, in that
  form. Do not start a new document.
- DOCUMENTATION ONLY -- test_required is false deliberately. This record produces
  no executable behaviour, so test-writer skips it with a recorded reason rather than
  the code being silently left untested. Every behavioural claim the diagram depicts
  is already owned and tested by BO-400e-1 through BO-400e-4.
n_location_rule: 2 -- the new close-path sequence added to docs/architecture/agent_delivery_workflows.md
  alongside its existing sequences, plus one link to it from docs/architecture/components/build-orchestration.md
  so a reader arriving at the component finds the close path.
post_write_commands: []
reference_file_path: docs/architecture/agent_delivery_workflows.md
required_skills:
- architecture-diagram-author
- write-c4-diagram
```

## Agent Contracts

### documentation-expert

Existing docs to update / cross-link:

- docs/architecture/agent_delivery_workflows.md (relationship: modifies | status: exists | relevance: The canonical home for supervisor/driver sequence diagrams in this repository -- it already carries the worktree-lifecycle, approval-to-PR and files-touched-reconciliation sequences, each with a dated changelog trailer. Add the close-path sequence here rather than starting a new document.)
- docs/architecture/components/build-orchestration.md (relationship: modifies | status: exists | relevance: The component document the diagram belongs to; link the new sequence from it so a reader arriving at the component finds the close path. Relationship set to modifies by it-po: the record's own relevance note describes an edit to this file, so it is an edit surface and must appear in the generated ticket's files_touched.)
- templates/workflows-js/build-feature.js (relationship: describes | status: exists | relevance: The epic driver -- one of the two participants that must appear. Read its completion-write step for the routes the diagram has to depict. TWIN of build-ticket.js.)
- templates/workflows-js/build-ticket.js (relationship: describes | status: exists | relevance: The single-ticket driver -- the other participant. A diagram showing only one of the two would hide precisely the divergence BO-400a-2-ii's standing constraint exists to prevent.)
- scripts/set_ticket_status.py (relationship: describes | status: exists | relevance: The checking mechanism at the centre of the diagram: it reads the record's agents map, refuses while any entry is unaccounted for, and carries the blanket override that must be drawn as off the ordinary path.)
- templates/agents/status-checker.md (relationship: describes | status: exists | relevance: The party that performs the write, and the participant through which the second door was reachable. Its closing protocol is the rule the diagram should make visible.)
- docs/known-issues/build-orchestration.md (relationship: describes | status: exists | relevance: KI-BO-20260831-1932 -- the split outcome the diagram must make impossible to misread. The diagram is worth drawing because the defect was a routing question that nobody could see without reading two files.)
- docs/architecture/adrs/ADR-006-flatten-supervisor-chain.md (relationship: describes | status: exists | relevance: Why the two workflow drivers, and not ticket-supervisor, are the participants the diagram must draw. A close-path sequence that still shows a supervisor would depict a topology that no longer exists.)
- docs/reference/architecture-docs-layout.md (relationship: describes | status: exists | relevance: House layout and conventions for architecture documents -- follow it rather than inventing a placement or trailer format for the new sequence.)

- [ ] AC-1: sequence-diagram | docs/architecture/agent_delivery_workflows.md | a sequence diagram is present that shows the close from the moment a drive decides a

### Expects From

- **AC:** BO-400e-3
- **Contract:** The single close route -- the checking mechanism as the only writer of the finished state, reached identically by both drivers -- which is the shape the diagram depicts

## Sign-offs

- [ ] documentation-expert
- [ ] documentation-verifier
- [ ] architecture-diagram-author
- [ ] commit

## Comments
