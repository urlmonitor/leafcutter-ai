---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  pr-reviewer: signed_off
  pull-request: not_needed
  sql-coder: not_needed
  test-runner: signed_off
  test-writer: signed_off
  llm-expert: signed_off
components:
- build-pipeline
created: '2026-06-08'
depends_on:
- BP-600b
- BP-600b-1
files_touched:
- docs/reference/ac-schema.md
- docs/acceptance-criteria/index.yaml
- docs/architecture/adrs/ADR-006-flatten-supervisor-chain.md
priority: medium
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: BP-600b-2
status: in_progress
title: Quick-fix AC uses the correct component prefix and sequential ID
---

# Quick-fix AC uses the correct component prefix and sequential ID

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BP-600b-2` — Quick-fix AC uses the correct component prefix and sequential ID — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BP-600b-2`. Component: `build-pipeline`. Assigned agent: `workflow-architect`. Estimated complexity: `S`.

## Acceptance Criteria

```gherkin
Given the AC store for component "build-pipeline" already contains ACs
  with IDs BP-001 through BP-006,
When the quick-fix workflow creates a new AC for a bug in the
  build-pipeline component,
Then the new AC has the prefix "BP" matching the component's prefix
  in index.yaml,
And its numeric suffix is the next available sequential integer
  (never reusing a retired or existing ID).
```

## Sign-offs

- [x] llm-expert — 2026-06-08 00:10
- [x] test-writer — 2026-06-08 00:00
- [x] test-runner — 2026-06-08 00:15
- [x] pr-reviewer — 2026-06-08 00:20
- [ ] commit

## Comments

### 2026-06-08 00:01 — ticket-supervisor (status: ok)
test_requirements empty — test-writer phase skipped (docs-only or config-only ticket)

### 2026-06-08 00:02 — ticket-supervisor (status: ok)
pull-request phase marked not_needed — PRs are handled at the epic level, not per-ticket (caller instruction).

### 2026-06-08 00:10 — llm-expert (status: ok)
feedback-id: (submit-failed)
completion_manifest:
  template_written: true
  prompt_quality_checklist_passed: true
  convention_violations_resolved: true
Added /quick-fix ID assignment algorithm section to docs/reference/ac-schema.md (prefix lookup from index.yaml, sequential scan, atomicity lock); added directory_patterns field to index.yaml build-pipeline entry; added AC BP-600b-2 addendum to ADR-006; marked BP-600b-2.yaml work_status done with implemented_by links.

### 2026-06-08 00:15 — ticket-supervisor (status: ok)
test-runner phase signed off by supervisor: git diff --name-only HEAD shows only docs/YAML/config changes (no Python or SQL files). Per test-runner No-op Rule, no test suite is applicable. Marking test-runner signed_off without spawning the agent to avoid blocking on an interactive "Run anyway?" prompt.

### 2026-06-08 00:20 — pr-reviewer (status: ok)
feedback-id: fb_2026-06-08_4e418ae4
completion_manifest:
  diff_reviewed: true
  high_confidence_findings: true
  escalation_decision: true
Docs-only diff reviewed (5 files, 117 insertions, 11 deletions). No high-confidence findings. One medium: the lock file path `docs/acceptance-criteria/<component-id>/.quick-fix-lock` is under the tracked docs/ tree and may accidentally get committed; a system-temp or gitignored path would be safer. Medium count is 1 (threshold > 3) — no Opus escalation. Suppressed: 0 low-confidence nits. Escalation: none — medium count was 1 (threshold > 3).
