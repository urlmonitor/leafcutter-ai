---
ac_traceability:
  id: ACD-400b-6
  path: docs/acceptance-criteria/ac-driven-dev/ACD-400b-6.yaml
advances_current_outcome: true
agents:
  ac-fulfillment-gate: needed
  ac-validator: needed
  architect-review: needed
  commit: needed
  documentation-expert: needed
  documentation-verifier: needed
  pr-reviewer: needed
  pull-request: needed
  python-coder: needed
  test-runner: needed
  test-writer: signed_off
change_target: code
complexity: low
components:
- ac_driven_dev
created: '2026-08-13'
depends_on: []
documentation_required: true
files_touched:
- scripts/ac_store/generate_ticket_from_ac.py
priority: high
requires_adr: false
requires_diagram: false
risk_surface: contract_boundary
roadmap_phase: phase_1
source_ac: ACD-400b-6
status: todo
title: Generated files_touched includes every implementation source the AC points
  at, never dropping the behavior's home file
---

# Generated files_touched includes every implementation source the AC points at, never dropping the behavior's home file

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `ACD-400b-6` — Generated files_touched includes every implementation source the AC points at, never dropping the behavior's home file — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `ACD-400b-6`. Component: `ac-driven-dev`. Assigned agent: `python-coder`. Estimated complexity: `S`. Complexity: `low`.

## Acceptance Criteria

```gherkin
Given an AC whose doc_links reference more than one local source file
  (for example a primary file where the behavior lives plus a second file
  that only delegates to it),
When the generator is invoked with that AC's identifier and writes the ticket,
Then the generated ticket's files_touched lists every implementation-relevant
  local source path referenced by the AC — not a subset,
And the file where the AC's behavior actually lives (the primary target)
  is always present in files_touched,
And no referenced implementation source path is silently omitted.
```

- [ ] AC-1: the generated ticket's files_touched lists every implementation-relevant
- [ ] AC-2: the file where the AC's behavior actually lives (the primary target)
- [ ] AC-3: no referenced implementation source path is silently omitted.

## Test Requirements

```yaml
tests:
- name: test_files_touched_includes_every_impl_source
  file: unit_tests/ac_store/test_acd_400b_6.py
  covers:
  - ACD-400b-6
  asserts: Generating a ticket from an AC whose doc_links reference two local source
    files yields a files_touched containing both paths (not a subset).
  framework: pytest
  type: behavioral
- name: test_files_touched_always_contains_primary_home_file
  file: unit_tests/ac_store/test_acd_400b_6.py
  covers:
  - ACD-400b-6
  asserts: The file where the AC's behavior lives (primary target) is present in the
    generated files_touched regardless of doc_links ordering.
  framework: pytest
  type: behavioral
```

## Implementation Notes

```yaml
- 'File of record: the fix lives in scripts/ac_store/generate_ticket_from_ac.py, in
  the logic that derives the ticket''s files_touched from the source AC''s doc_links.'
- files_touched derivation must be union-preserving over the AC's implementation-relevant
  local source doc_links — never a first-wins or single-element reduction that silently
  drops a referenced path.
- Non-source doc_links (e.g. docs/**.md reference pages, architecture docs) may be
  excluded from files_touched, but no local implementation source path may be dropped.
- Must accept a doc_links entry in either supported shape (plain path string or {path,
  relationship, status} object) without raising, per the AC-store doc_links schema.
- 'Must follow the repo error-handling policy: wrap file/YAML I/O in try/except with
  a specific exception type and log at WARNING or higher (or re-raise); no bare or
  silent excepts.'
- Verify behaviorally — generate a ticket from a crafted AC and assert the resulting
  frontmatter's files_touched, not by grepping the generator source.
```

## Agent Contracts

### documentation-expert

Existing docs to update / cross-link:

- docs/reference/ac-schema.md

- [ ] AC-1: [(unspecified genre)] docs/(unspecified genre)/acd-400b-6.md — the generated ticket's files_touched lists every implementation-relevant

### Delivers To

- **Agent:** python-coder
- **Contract:** A ticket whose files_touched sends the coder to the real implementation target(s), not a partial set

### Expects From

- **AC:** ACD-400b-1
- **Contract:** Ticket generation logic that derives files_touched from the AC's doc_links

## Sign-offs

- [ ] architect-review
- [x] test-writer — 2026-08-13 08:35
- [ ] python-coder
- [ ] test-runner
- [ ] documentation-expert
- [ ] pr-reviewer
- [ ] ac-validator
- [ ] ac-fulfillment-gate
- [ ] documentation-verifier
- [ ] commit
- [ ] pull-request

## Comments

### 2026-08-13 08:35 — test-writer (status: ok)
feedback-id: (submit-failed)
completion_manifest:
  test_files_touched_includes_the_acs_implementation_source_written: true
  test_files_touched_does_not_drop_home_file_when_helper_also_linked_written: true
  red_baseline_confirmed: true
red_baseline:
  - test_name: test_files_touched_includes_the_acs_implementation_source
    file: unit_tests/ac_store/test_generator_frontmatter_gaps.py
    error: "AssertionError: files_touched must contain the AC's real implementation source 'scripts/ac_store/scan_ac_store.py' ... Got files_touched=[]. assert 'scripts/ac_store/scan_ac_store.py' in []"
  - test_name: test_files_touched_does_not_drop_home_file_when_helper_also_linked
    file: unit_tests/ac_store/test_generator_frontmatter_gaps.py
    error: "AssertionError: the AC's behavior home file 'scripts/ac_store/scan_ac_store.py' must never be dropped from files_touched ... Got files_touched=['scripts/ac_store/ac_prioritizer.py']. assert 'scripts/ac_store/scan_ac_store.py' in ['scripts/ac_store/ac_prioritizer.py']"
  - test_name: test_files_touched_preserves_leading_dot_paths
    file: unit_tests/ac_store/test_generator_frontmatter_gaps.py
    error: "AssertionError: a dotfile-prefixed path named in prose ('.github/workflows/ci.yml') must survive ... Got files_touched=['github/workflows/ci.yml']. assert '.github/workflows/ci.yml' in ['github/workflows/ci.yml']"
Wrote unit_tests/ac_store/test_generator_frontmatter_gaps.py covering ACD-400b-6 (+ ACD-400b-6-i) and the sibling ticket ACD-400b-7 (+ ACD-400b-7-i) in one shared file, per explicit dispatch instructions. All 6 behavioral tests confirmed RED via `AC_ENFORCE_STRICT=1 python -m pytest ... -v` (6 failed / 0 passed); ruff clean. Root cause for this AC: `_extract_local_paths()` requires `isinstance(link, dict)` and silently `continue`s past plain-string `doc_links` entries — exactly reproducing the verified ACD-400a-3 bug. A third, bonus defect was found live in the same code path (`_PROSE_PATH_TOKEN_RE` drops a leading `.` from dotfile-prefixed prose paths) and is covered by `test_files_touched_preserves_leading_dot_paths` (`# covers: UNKNOWN` — no dedicated AC exists yet for it). No `## AC Coverage` table is present in this ticket body (only a flat `## Acceptance Criteria` checklist and a `### documentation-expert` block under `## Agent Contracts`), so the §2c AC-coverage-table fill step was not applicable; AC-1/2/3 of the global checklist are addressed by the two tests above.
