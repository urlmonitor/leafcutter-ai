---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  pr-reviewer: needed
  pull-request: needed
  python-coder: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
complexity: low
components:
- build_orchestration
created: '2026-07-15'
depends_on:
- BO-2200c-4
files_touched:
- scripts/ac_store/generate_ticket_from_ac.py
priority: medium
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: BO-2200c-4-i
status: done
title: A doc_link that is a bare path or is missing optional fields is surfaced gracefully
---

# A doc_link that is a bare path or is missing optional fields is surfaced gracefully

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BO-2200c-4-i` — A doc_link that is a bare path or is missing optional fields is surfaced gracefully — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BO-2200c-4-i`. Component: `build-orchestration`. Assigned agent: `python-coder`. Estimated complexity: `S`. Complexity: `low`.

## Acceptance Criteria

```gherkin
Given a documented AC whose `doc_links` mixes entry shapes -- some are plain path
  strings, some are objects missing `relevance` or `status`,
When the generator renders the 'existing docs to update / cross-link' list,
Then a plain-path entry is surfaced with just its path (no fabricated metadata) and
  does not crash the generator.
And an object entry missing an optional field is surfaced with the fields it does have,
  omitting the absent ones rather than emitting empty or `None` values.
```

## Test Requirements

```yaml
tests:
- name: test_bare_path_doc_link_surfaced_without_crash
  file: unit_tests/test_bo_2200c_4_i.py
  covers:
  - BO-2200c-4-i
  asserts: A plain-string doc_links entry is surfaced with just its path (no fabricated
    metadata) and does not crash the generator.
  framework: unittest
  type: behavioral
- name: test_object_missing_optional_field_omits_absent
  file: unit_tests/test_bo_2200c_4_i.py
  covers:
  - BO-2200c-4-i
  asserts: An object doc_links entry missing relevance/status is surfaced with only
    the fields it has, omitting absent ones (no None/empty values).
  framework: unittest
  type: behavioral
```

## Implementation Notes

```yaml
config_schema_fragment:
  heterogeneous_doc_links:
    description: doc_links entries may be plain path strings OR objects with optional
      relationship/status/relevance; normalise both, omit absent optional fields,
      never emit None or crash.
    type: object
constraints:
- doc_links entries are heterogeneous per the schema (plain strings OR objects with
  optional relationship/status/relevance). Normalise both shapes.
- Omit absent optional fields rather than rendering None; never crash on a bare-string
  entry.
n_location_rule: '1'
post_write_commands: []
reference_file_path: scripts/ac_store/generate_ticket_from_ac.py
required_skills:
- python-coder
```

## Sign-offs

- [ ] python-coder
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit
- [ ] pull-request

## Comments
