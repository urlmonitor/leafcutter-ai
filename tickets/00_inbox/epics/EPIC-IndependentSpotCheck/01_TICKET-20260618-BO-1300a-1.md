---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  llm-expert: needed
  pr-reviewer: needed
  pull-request: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
components:
- build-orchestration
created: '2026-06-18'
depends_on:
- BO-1300a
files_touched:
- docs/acceptance-criteria/build-orchestration/BO-1300-independent-spot-check/BO-1300a.yaml
- docs/architecture/components/build-ticket-workflow-dispatch.md
- docs/architecture/adrs/ADR-006-flatten-supervisor-chain.md
priority: high
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: BO-1300a-1
status: todo
title: On-demand spot-check command targets a named finished feature
---

# On-demand spot-check command targets a named finished feature

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BO-1300a-1` — On-demand spot-check command targets a named finished feature — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BO-1300a-1`. Component: `build-orchestration`. Assigned agent: `llm-expert`. Estimated complexity: `M`.

## Acceptance Criteria

```gherkin
Given a feature has just finished building (its tickets are implemented and merged)
When the user invokes the standalone spot-check command and names that feature as the
  target scope (for example, the epic folder, the feature branch, or the changed files
  of the just-built feature)
Then the command resolves the target into a concrete review scope describing what was
  built (the feature's behavior and the files it changed)
And the review proceeds against that scope without requiring a full build cycle to run
And the command does not ask the user to author any new tests
```

## Sign-offs

- [ ] llm-expert
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit
- [ ] pull-request

## Comments
