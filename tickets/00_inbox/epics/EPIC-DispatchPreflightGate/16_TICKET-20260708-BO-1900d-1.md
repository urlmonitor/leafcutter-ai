---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  pr-reviewer: needed
  pull-request: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
  workflow-architect: needed
complexity: medium
components:
- build_orchestration
created: '2026-07-08'
depends_on:
- BO-1900d
files_touched:
- docs/architecture/agent_delivery_workflows.md
- templates/skills/building-epics/SKILL.md
- docs/architecture/components/build-ticket-workflow-dispatch.md
priority: high
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: BO-1900d-1
status: todo
title: A payload of only allowlisted pointers is accepted
---

# A payload of only allowlisted pointers is accepted

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BO-1900d-1` — A payload of only allowlisted pointers is accepted — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BO-1900d-1`. Component: `build-orchestration`. Assigned agent: `workflow-architect`. Estimated complexity: `M`. Complexity: `medium`.

## Acceptance Criteria

```gherkin
Given a dispatch payload whose keys are exactly the allowlisted pointers
  (ticket_path, worktree_path, branch, files_touched) with well-formed values,
When the supervisor constructs the payload and preflight validates it before spawn,
Then the payload is accepted,
And the phase agent is spawned with only those pointer values,
And no free-composed prose prompt is attached to the dispatch.
```

## Sign-offs

- [ ] workflow-architect
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit
- [ ] pull-request

## Comments
