{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "$id": "config/ac_store_schema.json",
    "title": "AC Store — Acceptance Criterion YAML Schema",
    "description": "JSON Schema (draft-07) for acceptance criterion files stored under docs/acceptance-criteria/. See ADR-007 for rationale and ID format specification.",
    "type": "object",
    "required": [
        "id",
        "title",
        "component",
        "status",
        "created_by",
        "criteria",
        "readiness",
        "priority"
    ],
    "additionalProperties": false,
    "properties": {
        "id": {
            "type": "string",
            "description": "Stable AC identifier in the format COMPONENT_PREFIX-NNN (e.g. FIN-001). Assigned at creation time; never changes.",
            "pattern": "^[A-Z]{2,6}-[0-9]{3}$"
        },
        "title": {
            "type": "string",
            "description": "One-line human-readable description of the criterion.",
            "minLength": 1
        },
        "component": {
            "type": "string",
            "description": "Component name matching a key in docs/components.json.",
            "minLength": 1
        },
        "status": {
            "type": "string",
            "description": "Lifecycle status of the criterion.",
            "enum": [
                "active",
                "deprecated",
                "superseded_by"
            ]
        },
        "created_by": {
            "type": "string",
            "description": "Path to the ticket (relative to repo root) that first introduced this criterion.",
            "minLength": 1
        },
        "criteria": {
            "type": "string",
            "description": "Multi-line Gherkin scenario body (Given/When/Then/And).",
            "minLength": 1
        },
        "superseded_by": {
            "oneOf": [
                {
                    "type": "string",
                    "pattern": "^[A-Z]{2,6}-[0-9]{3}$",
                    "description": "AC ID of the replacement criterion (set when status is superseded_by)."
                },
                {
                    "type": "null"
                }
            ]
        },
        "amended_by": {
            "type": "array",
            "description": "Ticket paths that subsequently amended this criterion.",
            "items": {
                "type": "string"
            },
            "uniqueItems": true,
            "default": []
        },
        "covered_by": {
            "type": "array",
            "description": "Test file paths (optionally with ::test_function) that verify this criterion.",
            "items": {
                "type": "string"
            },
            "uniqueItems": true,
            "default": []
        },
        "implemented_by": {
            "type": "array",
            "description": "Source file paths (optionally with #anchor) that implement this criterion.",
            "items": {
                "type": "string"
            },
            "uniqueItems": true,
            "default": []
        },
        "origin_agent": {
            "type": "string",
            "description": "Identity of the agent or workflow that created this AC file. Enum: business-analyst | debug | human | ticket-wiring. Free-form string to allow future agents.",
            "minLength": 1
        },
        "readiness": {
            "type": "string",
            "description": "Lifecycle readiness state of this AC. draft = written but not reviewed (scanner ignores); reviewed = IT PO v3 has enriched and approved (scanner ignores); approved = user has signed off (scanner may pick up).",
            "enum": ["draft", "reviewed", "approved"]
        },
        "priority": {
            "type": "string",
            "description": "Implementation priority of this AC. Set by the user (or PO v3) at approval time. Used by the scanner for ranking.",
            "enum": ["critical", "high", "medium", "low"]
        },
        "documentation_triggers": {
            "description": "List of documentation types that must be produced for this feature. Valid on L1 ACs only. Set by product-owner-v3 based on the nature of the feature. Empty array means no docs needed (provide documentation_rationale in that case).",
            "oneOf": [
                {
                    "type": "array",
                    "items": {
                        "type": "string",
                        "enum": ["how-to", "sequence-diagram", "state-diagram", "component-diagram", "reference-doc"]
                    },
                    "uniqueItems": true
                },
                {
                    "type": "null"
                }
            ]
        },
        "documentation_rationale": {
            "description": "Required when documentation_triggers is empty on an L1 AC. Explains why no documentation is needed.",
            "oneOf": [
                {
                    "type": "string",
                    "minLength": 1
                },
                {
                    "type": "null"
                }
            ]
        },
        "work_status": {
            "oneOf": [
                {
                    "type": "string",
                    "description": "Implementation lifecycle status for this AC. todo = not yet implemented; in_progress = implementation underway; done = implemented and verified.",
                    "enum": [
                        "todo",
                        "in_progress",
                        "done"
                    ]
                },
                {
                    "type": "null"
                }
            ]
        },
        "level": {
            "oneOf": [
                {
                    "type": "string",
                    "description": "Hierarchy level of this AC. L0 = top-level composite (portfolio/program); L1 = composite (epic/feature); L2 = implementable leaf (story/task); L3 = fine-grained implementable leaf (sub-task). L0/L1 ACs are composite — fulfillment is derived from children, not directly verified.",
                    "enum": [
                        "L0",
                        "L1",
                        "L2",
                        "L3"
                    ]
                },
                {
                    "type": "null"
                }
            ]
        },
        "pattern_slots": {
            "oneOf": [
                {
                    "type": "array",
                    "description": "Named placeholder slots (in curly-brace notation, e.g. '{columns}', '{default_sort}') that consuming ACs must fill via pattern_bindings. Present only on ACs that act as shared-behavior patterns. Empty array or absent means this AC is not a pattern.",
                    "items": {
                        "type": "string",
                        "pattern": "^\\{[a-zA-Z_][a-zA-Z0-9_]*\\}$"
                    },
                    "uniqueItems": true
                },
                {
                    "type": "null"
                }
            ]
        },
        "implements_pattern": {
            "oneOf": [
                {
                    "type": "string",
                    "description": "AC ID of the pattern AC that this AC instantiates. Must reference a valid AC in the store whose pattern_slots is non-empty. Consuming ACs set this field; pattern ACs leave it absent or null.",
                    "pattern": "^[A-Z]{2,6}-[0-9]{3}([a-z]+(-[0-9]+(-[a-z0-9]+)*)?)?$"
                },
                {
                    "type": "null"
                }
            ]
        },
        "pattern_bindings": {
            "oneOf": [
                {
                    "type": "object",
                    "description": "Mapping of slot names (without curly braces) to their concrete values for this consuming AC. Every slot declared in the referenced pattern's pattern_slots must be bound here. Only valid when implements_pattern is set.",
                    "additionalProperties": {
                        "type": "string"
                    }
                },
                {
                    "type": "null"
                }
            ]
        }
    }
}
