---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  pr-reviewer: needed
  pull-request: needed
  python-coder: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
components:
- testing_quality
created: '2026-06-24'
depends_on:
- TQ-100e-1
files_touched:
- scripts/ac_store/test_enforcement.py
- tests/testing_quality/test_enforcement_mode.py
priority: high
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: TQ-100e-1-iii
status: todo
title: Switching enforcement modes changes behavior with no edits to any test
---

# Switching enforcement modes changes behavior with no edits to any test

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `TQ-100e-1-iii` — Switching enforcement modes changes behavior with no edits to any test — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `TQ-100e-1-iii`. Component: `testing-quality`. Assigned agent: `test-runner`. Estimated complexity: `S`.

## Acceptance Criteria

```gherkin
Given a fixed suite and a fixed AC store, run once with the enforcement mode
  configured to report-only and the run observed to be non-blocking,
When only the configuration value is changed to blocking and the suite is run
  again, with no edit made to any test file,
Then the second run's enforcement behavior changes accordingly (a failing
  enforced test now blocks the outcome)
And the change in behavior is produced entirely by the configuration change,
  not by any code or test edit
And the same is true when switching among report-only, exclude-unbuilt, and
  blocking in any order.
```

## Test Requirements

- `test_switching_mode_changes_behavior_without_test_edits`: Changing only the enforcement-mode config/env value changes gating behavior with no edits to any test.

## Sign-offs

- [ ] test-runner
- [ ] test-writer
- [ ] pr-reviewer
- [ ] commit
- [ ] pull-request

## Comments
