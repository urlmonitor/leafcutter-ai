---
title: "Changelog 98411822a..463b9eb35 — 2026-08-18"
date: "2026-08-18"
time: "11:10"
type: manual
components: 
  - commit_guardian
  - doc_compliance
  - ac_store
  - testing_quality
summary: "Fixed a long-standing bug that silently blocked agent cards and the how_to doc type from ever being committed, and approved a 37-leaf acceptance-criteria tree specifying (but not yet building) ongoing proof that finished work stays proven over time."
description: "2 commits. (1) 4406a1ef9 fix(commit-guardian): config/doc_types.json declares 10 document types but was never actually read — a doubled path segment (parents[2] resolving to .leafcutter/leafcutter/config/doc_types.json) meant _load_doc_types() always fell through to a hardcoded 7-type fallback before any exception could fire, so nothing was ever logged. Effect: type: card (every generated agent card) was rejected outright, and the canonical how_to spelling was rejected while its _deprecated how-to alias was the only one accepted; build.py also regenerates two agent templates from this file, so agents were being told about doc types the validator would then reject. Fixed via a __file__-anchored ancestor walk mirroring the existing diagram-types resolver, made an unreadable declaring file raise instead of silently substituting a narrower list, and derived the card components-exemption from the declaring file itself rather than a hardcoded special-case; the enum was deliberately not widened by hand. Four new tests exercise the DEPLOYED copy, including a must_block negative control. unit_tests/commit_guardian: 1140 passed under AC_ENFORCE_STRICT=1, one more than the pre-fix baseline, no regressions. The regenerated agent cards ship in the same commit as end-to-end proof — they are the artifacts the guard was rejecting. (2) 463b9eb35 chore(ac): specification-only, not behaviour — promotes all 37 TQ-400 leaves (29 L2, 8 L3) to readiness: approved, making the durable-done-proof tree scanner-eligible; the L0 and its five L1 composites stay draft. Also settles TQ-400c-2's previously-unnamed publication surface as the pull-request check list (non-blocking, reporting the last completed scheduled sweep) and closes a test-contract gap on its new lapsed-cadence clause. No sweep, demotion path, cadence job, or reader change has been implemented — TQ-400 remains entirely unbuilt specification; approval only means /build-ac can now surface it."
commits: 
  - 4406a1ef9
  - 463b9eb35
breaking: false
---

## Entry
