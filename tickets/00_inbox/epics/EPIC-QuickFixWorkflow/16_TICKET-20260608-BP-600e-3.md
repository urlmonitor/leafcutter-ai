---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  pr-reviewer: needed
  pull-request: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
  workflow-architect: needed
components:
- build-pipeline
created: '2026-06-08'
depends_on:
- BP-600e
- BP-600e-1
- BP-600e-2
files_touched:
- docs/architecture/agent_delivery_workflows.md
- docs/architecture/adrs/ADR-006-flatten-supervisor-chain.md
- templates/skills/build-single-ticket/SKILL.md
priority: medium
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: BP-600e-3
status: todo
title: Quick-fix workflow preserves progress when escalating to full build pipeline
---

# Quick-fix workflow preserves progress when escalating to full build pipeline

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BP-600e-3` — Quick-fix workflow preserves progress when escalating to full build pipeline — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BP-600e-3`. Component: `build-pipeline`. Assigned agent: `workflow-architect`. Estimated complexity: `M`.

## Acceptance Criteria

```gherkin
Given the workflow has paused with a scope-exceeded warning
  (from BP-600e-1 or BP-600e-2),
When the user chooses to escalate to the full build pipeline,
Then the AC YAML file already created is preserved in the AC store,
And the test file already written is preserved in the test directory,
And the workflow outputs a summary of what was completed and what
  remains (AC ID, test file path, diagnosed file, root cause),
And it provides the AC ID so the user can reference it in
  /create-ac or /build-feature.
```

## Sign-offs

- [ ] workflow-architect
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit
- [ ] pull-request

## Comments
