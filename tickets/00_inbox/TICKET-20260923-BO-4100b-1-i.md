---
ac_traceability:
  id: BO-4100b-1-i
  path: docs/acceptance-criteria/build-orchestration/BO-4100-sound-workspace-by-construction/BO-4100b-1-i.yaml
advances_current_outcome: true
agents:
  ac-fulfillment-gate: needed
  ac-validator: needed
  architect-review: needed
  commit: needed
  documentation-expert: needed
  documentation-verifier: needed
  frontend-coder: not_needed
  pr-reviewer: needed
  pull-request: needed
  python-coder: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
  user-surface-smoker: not_needed
change_target: code
complexity: low
components:
- build_orchestration
- worktree_manager
created: '2026-09-23'
depends_on: []
documentation_required: true
files_touched:
- scripts/commit_guardian/check_build_drift.py
- scripts/commit_guardian/check_output_drift.py
- templates/scripts/commit_guardian/check_build_drift.py
- templates/scripts/commit_guardian/check_output_drift.py
priority: high
requires_adr: false
requires_diagram: false
risk_surface: contract_boundary
roadmap_phase: phase_1
source_ac: BO-4100b-1-i
status: todo
title: Real drift in material the commit did change is still refused
---

# Real drift in material the commit did change is still refused

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BO-4100b-1-i` — Real drift in material the commit did change is still refused — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BO-4100b-1-i`. Component: `build-orchestration`. Assigned agent: `python-coder`. Estimated complexity: `S`. Complexity: `low`.

## Acceptance Criteria

```gherkin
Given a commit whose changed-file set includes a file belonging to the
  supporting-material tree,
And that file's content differs from the content the check expects for it,
When the checks that compare supporting material run,
Then the commit is refused,
And the refusal names that file,
And the refusal names no file the commit did not change.
```

- [ ] AC-1: that file's content differs from the content the check expects for it
- [ ] AC-2: the commit is refused
- [ ] AC-3: the refusal names that file
- [ ] AC-4: the refusal names no file the commit did not change.

## Test Requirements

```yaml
tests:
- name: test_a_drifted_file_the_commit_did_change_is_still_refused
  file: unit_tests/commit_guardian/test_bo_4100b_1_i.py
  covers:
  - BO-4100b-1-i
  asserts: A supporting-material file is both included in the commit's changed-file
    set and drifted from its expected content; the gate must exit non-zero at the
    severity it uses today.
  framework: unittest
  type: behavioral
  angle: failure
  implementation_files:
  - scripts/commit_guardian/check_build_drift.py
  - scripts/commit_guardian/check_output_drift.py
  - templates/scripts/commit_guardian/check_build_drift.py
  - templates/scripts/commit_guardian/check_output_drift.py
- name: test_the_refusal_names_the_drifted_file_the_commit_changed
  file: unit_tests/commit_guardian/test_bo_4100b_1_i.py
  covers:
  - BO-4100b-1-i
  asserts: The refusal output contains that file's path.
  framework: unittest
  type: behavioral
  angle: criterion
  implementation_files:
  - scripts/commit_guardian/check_build_drift.py
  - scripts/commit_guardian/check_output_drift.py
  - templates/scripts/commit_guardian/check_build_drift.py
  - templates/scripts/commit_guardian/check_output_drift.py
- name: test_the_refusal_names_no_file_the_commit_did_not_change
  file: unit_tests/commit_guardian/test_bo_4100b_1_i.py
  covers:
  - BO-4100b-1-i
  asserts: The fixture carries ONE in-scope drift and several out-of-scope drifts
    at once; the refusal must name exactly the one. A mixed fixture is required --
    with only in-scope drift present, an unscoped gate passes this test unchanged.
  framework: unittest
  type: behavioral
  angle: boundary
  implementation_files:
  - scripts/commit_guardian/check_build_drift.py
  - scripts/commit_guardian/check_output_drift.py
  - templates/scripts/commit_guardian/check_build_drift.py
  - templates/scripts/commit_guardian/check_output_drift.py
- name: test_bo_4100b_1_i_reachable_from_entry_point
  file: unit_tests/commit_guardian/test_bo_4100b_1_i.py
  covers:
  - BO-4100b-1-i
  angle: reachability
  asserts: 'REQUIRED — invoke the production entry point (CLI, hook, slash command,
    workflow dispatch, or main()) as a subprocess/dispatch and assert the new behaviour
    actually occurs. Do NOT satisfy this by importing the function directly. The AC
    authored no test_spec, so the entry point is not declared: resolve it before writing
    this test, and do not delete this entry.'
  implementation_files:
  - scripts/commit_guardian/check_build_drift.py
  - scripts/commit_guardian/check_output_drift.py
  - templates/scripts/commit_guardian/check_build_drift.py
  - templates/scripts/commit_guardian/check_output_drift.py
```

## Implementation Notes

```yaml
- The true-positive path must keep the exit code and the severity it has today. Narrowing
  what a gate may refuse over must not change what it does when it finds a genuine
  mismatch inside the scope that remains -- that would convert a false-positive fix
  into the silent-check failure BO-1700 exists to prevent.
- This record and BO-4100b-1 must be covered by tests that run in the same suite against
  the same fixture repository, so no change can widen one and break the other without
  turning something red in the same run.
- The refusal must name the in-scope file at path granularity, not as a count or a
  category, because BO-4100c-2 and the operator both act on the name.
- Applies to both drift gates; a true positive suppressed in one of the two is still
  a suppressed true positive.
- Template and deployed copies change together and are verified in parity via the
  build.py round-trip (ADR-001).
```

## Agent Contracts

### documentation-expert

Existing docs to update / cross-link:

- docs/architecture/components/commit-guardian.md (relationship: describes | status: exists)
- templates/scripts/commit_guardian/check_build_drift.py (relationship: modifies | status: exists | relevance: Canonical source. The true-positive path must keep today's exit code and severity, and must name the in-scope drifted file at path granularity.)
- scripts/commit_guardian/check_build_drift.py (relationship: modifies | status: exists | relevance: Build output of the template above; the hook runs from this deployed copy. Must change in parity (ADR-001).)
- templates/scripts/commit_guardian/check_output_drift.py (relationship: modifies | status: exists | relevance: Canonical source. A true positive suppressed in one of the two gates is still a suppressed true positive, so both carry this guarantee.)
- scripts/commit_guardian/check_output_drift.py (relationship: modifies | status: exists | relevance: Build output of the template above. Must change in parity (ADR-001).)

- [ ] AC-1: (unspecified genre) | docs/architecture/components/commit-guardian.md | the commit is refused

## Sign-offs

- [ ] architect-review
- [ ] test-writer
- [ ] python-coder
- [ ] test-runner
- [ ] documentation-expert
- [ ] pr-reviewer
- [ ] ac-validator
- [ ] ac-fulfillment-gate
- [ ] documentation-verifier
- [ ] commit
- [ ] pull-request

## Comments
