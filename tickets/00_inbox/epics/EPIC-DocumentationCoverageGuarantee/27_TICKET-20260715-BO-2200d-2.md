---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  pr-reviewer: signed_off
  pull-request: needed
  python-coder: signed_off
  sql-coder: not_needed
  test-runner: needed
  test-writer: signed_off
complexity: low
components:
- build_orchestration
change_target: code
created: '2026-07-15'
risk_surface: internal
depends_on:
- 08_TICKET-20260715-BO-2200b-1.md
- 25_TICKET-20260715-BO-2200d-1.md
files_touched:
- scripts/ac_store/generate_ticket_from_ac.py
priority: medium
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: BO-2200d-2
status: todo
title: On a doc-required ticket, the writer runs after code and tests and the verifier
  runs last before commit
---

# On a doc-required ticket, the writer runs after code and tests and the verifier runs last before commit

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BO-2200d-2` — On a doc-required ticket, the writer runs after code and tests and the verifier runs last before commit — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BO-2200d-2`. Component: `build-orchestration`. Assigned agent: `python-coder`. Estimated complexity: `S`. Complexity: `low`.

## Acceptance Criteria

```gherkin
Given a documentation-required ticket whose resolved phase order includes
  `python-coder`, `test-runner`, `documentation-expert`, and `documentation-verifier`,
When the phase order is evaluated,
Then the index of `documentation-expert` is greater than the index of `python-coder`
  and greater than the index of `test-runner` (docs are written after the code exists
  and after tests run).
And `documentation-verifier` is the last phase before the commit phase.
```

## Test Requirements

```yaml
tests:
- name: test_doc_expert_ordered_after_coder_and_test_runner
  file: unit_tests/test_bo_2200d_2.py
  covers:
  - BO-2200d-2
  asserts: On a resolved doc-required ticket, index(documentation-expert) > index(python-coder)
    and > index(test-runner).
  framework: unittest
  type: behavioral
- name: test_verifier_is_last_phase_before_commit
  file: unit_tests/test_bo_2200d_2.py
  covers:
  - BO-2200d-2
  asserts: documentation-verifier is the last phase immediately before the commit
    phase.
  framework: unittest
  type: behavioral
```

## Implementation Notes

```yaml
config_schema_fragment:
  resolved_phase_order:
    description: 'On a real doc-required ticket: index(documentation-expert) > index(python-coder)
      and > index(test-runner); documentation-verifier immediately precedes commit.'
    type: object
constraints:
- 'Assert the resolved relative order on a real doc-required ticket: index(documentation-expert)
  > index(python-coder) and > index(test-runner); documentation-verifier immediately
  precedes commit.'
- This depends on both the post-coder placement (BO-2200d-1) and the verifier's 11.9
  priority (BO-2200b-1).
n_location_rule: '1'
post_write_commands: []
reference_file_path: scripts/ac_store/generate_ticket_from_ac.py
required_skills:
- python-coder
```

## Sign-offs

- [x] python-coder — 2026-07-21 11:30
- [x] test-writer — 2026-07-21 10:15
- [ ] test-runner
- [x] pr-reviewer — 2026-07-21 13:15
- [ ] commit
- [ ] pull-request

## Comments

### 2026-07-21 11:30 — python-coder (status: ok)
feedback-id: fb_2026-07-21_2c10ff2a
completion_manifest:
  canonical_phase_order_reordered: true
  flow_change_phase_order_reordered: true
  red_baseline_test_green: true
  ruff_clean: true
  spot_check_passed: true
Moved `documentation-verifier` from index 6 (after `documentation-expert`) to index 9 (immediately before `commit`) in `_CANONICAL_PHASE_ORDER`, and made the same change in `_FLOW_CHANGE_PHASE_ORDER` for consistency. Both red_baseline tests are now green (test_verifier_is_last_phase_before_commit gap = 1); 81 generate_ticket + 355 ac_store tests all pass; ruff reports no violations; real-artifact spot-check confirms the needed phase order is `[..., ac-fulfillment-gate, documentation-verifier, commit, ...]`.

red_baseline_results:
  - test_name: test_verifier_is_last_phase_before_commit
    result: green
  - test_name: test_doc_expert_ordered_after_coder_and_test_runner
    result: green

### 2026-07-21 13:15 — pr-reviewer (status: ok)
feedback-id: (submit-failed)
completion_manifest:
  ac_conditions_satisfied: true
  no_high_confidence_findings: true
  medium_findings_below_escalation_threshold: true
Reviewed the reordering of `documentation-verifier` in `_CANONICAL_PHASE_ORDER` (moved from index 6 to index 9, immediately before `commit`). The change satisfies all three AC conditions: `documentation-expert` remains after both `python-coder` and `test-runner`; `documentation-verifier` is now adjacent to `commit` with gap = 1. One medium-confidence finding noted: the parallel change to `_FLOW_CHANGE_PHASE_ORDER` is dead code (that list is never read by `_build_agents_map` since BO-2200d-1 hardwired to `_CANONICAL_PHASE_ORDER`), but it is harmless and kept for documentation consistency. Medium count = 1, below Opus escalation threshold of 3.

### 2026-07-21 10:15 — test-writer (status: ok)
feedback-id: fb_2026-07-21_c0bf9e53
completion_manifest:
  tests_written: true
  tests_red: true
  frontmatter_fixed: true

## Test Writer — Completion Report

### Tests Written
| File | Directory | Framework | Status |
|---|---|---|---|
| test_bo_2200d_2.py | unit_tests/ | unittest | written |

### Verification Run
- Command: `python -m unittest /home/henzeh/projects/leafcutter/worktrees/BO-2200-doc-coverage/unit_tests/test_bo_2200d_2.py -v`
- Result: red (1 failure — expected; implementation not yet written)
- test_doc_expert_ordered_after_coder_and_test_runner: PASS (passes immediately — regression guard)
- test_verifier_is_last_phase_before_commit: FAIL (AssertionError: 4 != 1)

### Notes
Pre-existing ticket frontmatter defects fixed (known build-ac generator quirks): `depends_on` AC-ID references replaced with valid sibling filenames; `BO-2200d` parent AC removed (no corresponding ticket); `change_target: code` and `risk_surface: internal` added.

red_baseline:
  - test_name: test_verifier_is_last_phase_before_commit
    file: unit_tests/test_bo_2200d_2.py
    error: "AssertionError: 4 != 1 : BO-2200d-2: documentation-verifier must be the LAST needed phase immediately before commit (commit_idx - dv_idx must equal 1). Current needed phase order: ['architect-review', 'test-writer', 'python-coder', 'test-runner', 'documentation-expert', 'documentation-verifier', 'pr-reviewer', 'ac-validator', 'ac-fulfillment-gate', 'commit', 'pull-request']. Phases between documentation-verifier and commit: ['pr-reviewer', 'ac-validator', 'ac-fulfillment-gate']"
  - test_name: test_doc_expert_ordered_after_coder_and_test_runner
    file: unit_tests/test_bo_2200d_2.py
    error: "(none — test passes immediately)"
    note: "passes immediately — may be under-specified; retained as regression guard against BO-2200d-1 regression"
