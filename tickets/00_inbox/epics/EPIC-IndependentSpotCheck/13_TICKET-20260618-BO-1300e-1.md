---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  pr-reviewer: needed
  pull-request: not_needed
  python-coder: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
components:
- build_orchestration
created: '2026-06-18'
depends_on:
- BO-1300e
- BO-1300b-2
files_touched:
- docs/acceptance-criteria/build-orchestration/BO-1300-independent-spot-check/BO-1300e.yaml
- docs/architecture/components/ticket-lifecycle.md
priority: high
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: BO-1300e-1
status: todo
title: Each spot-check finding is written as a ticket into the inbox for the fix flow
---

# Each spot-check finding is written as a ticket into the inbox for the fix flow

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BO-1300e-1` — Each spot-check finding is written as a ticket into the inbox for the fix flow — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BO-1300e-1`. Component: `build-orchestration`. Assigned agent: `python-coder`. Estimated complexity: `M`.

## Acceptance Criteria

```gherkin
Given the consolidated spot-check result contains a distinct finding describing a problem
  with the feature
When the spot-check pass records that finding
Then it writes the finding as its own ticket into tickets/00_inbox/
And the ticket carries valid ticket frontmatter following the project's ticket-authoring
  conventions, so the normal fix flow can pick it up
And the ticket body describes the finding: what was probed, the reviewer angle that found it,
  and the observed problem
And the finding is recorded as a tracked ticket rather than left only inside a review report
```

## Sign-offs

- [ ] python-coder
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit

## Comments
