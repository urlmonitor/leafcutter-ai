---
ac_traceability:
  id: ACD-2100b-1
  path: docs/acceptance-criteria/ac-driven-dev/ACD-2100-entry-point-unblocked/ACD-2100b-1.yaml
advances_current_outcome: true
agents:
  ac-fulfillment-gate: needed
  ac-validator: needed
  commit: needed
  documentation-expert: needed
  documentation-verifier: needed
  pr-reviewer: needed
  pull-request: not_needed
  python-coder: needed
  test-runner: needed
  test-writer: needed
change_target: pipeline
complexity: medium
components:
- ac_driven_dev
- agent_registry
created: '2026-08-26'
depends_on: []
documentation_required: true
files_touched:
- templates/workflows-js/plan-feature.js
priority: medium
requires_adr: false
requires_diagram: false
risk_surface: internal
roadmap_phase: phase_1
source_ac: ACD-2100b-1
status: todo
title: A registry the startup check cannot read is reported as unreadable and names
  what it tried to read
---

# A registry the startup check cannot read is reported as unreadable and names what it tried to read

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `ACD-2100b-1` — A registry the startup check cannot read is reported as unreadable and names what it tried to read — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `ACD-2100b-1`. Component: `ac-driven-dev`. Assigned agent: `python-coder`. Estimated complexity: `M`. Complexity: `medium`.

## Acceptance Criteria

```gherkin
Given a run in which the agent registry the startup check resolves to cannot be
  read -- the location holds no file, or the process is refused permission to
  open it,
When the startup check runs,
Then the run stops and reports that the registry could not be read,
And the report states the location that was tried and the reason the read
  failed,
And the report contains no statement about whether any agent is permitted to run
  repository commands,
And the report does not direct the reader at a permission setting.
```

- [ ] AC-1: the run stops and reports that the registry could not be read
- [ ] AC-2: the report states the location that was tried and the reason the read
- [ ] AC-3: the report contains no statement about whether any agent is permitted to run
- [ ] AC-4: the report does not direct the reader at a permission setting.

## Test Requirements

```yaml
tests:
- name: test_absent_registry_is_reported_as_unreadable_with_the_location_tried
  file: unit_tests/workflows/test_acd_2100b_1.py
  covers:
  - ACD-2100b-1
  asserts: No file at the resolved registry location; the run stops and its report
    contains that location and states the read failed because nothing is there.
  framework: unittest
  type: integration
  angle: criterion
- name: test_unreadable_registry_report_contains_no_permission_verdict
  file: unit_tests/workflows/test_acd_2100b_1.py
  covers:
  - ACD-2100b-1
  asserts: The same report contains no statement about any agent being permitted or
    not permitted to run repository commands and does not name a permission setting
    -- asserted as an absence, because the buggy code also prints a message.
  framework: unittest
  type: integration
  angle: criterion
- name: test_permission_refused_registry_reports_a_different_reason_than_absent
  file: unit_tests/workflows/test_acd_2100b_1.py
  covers:
  - ACD-2100b-1
  asserts: A registry file present but with read permission withheld from the process
    produces a report naming the location and a reason distinct from the nothing-is-there
    reason.
  framework: unittest
  type: integration
  angle: boundary
- name: test_unreadable_registry_halts_the_run_at_the_check
  file: unit_tests/workflows/test_acd_2100b_1.py
  covers:
  - ACD-2100b-1
  asserts: Driving the real workflow with the registry unreadable stops the run at
    the check -- no step after the check runs -- and the halt is what the caller observes.
  framework: unittest
  type: integration
  angle: reachability
```

## Implementation Notes

```yaml
- The change must land under templates/. The deployed workflow is build output and
  today carries no charter gate at all (see ACD-2100d-3).
- 'Fail closed: the run still stops. The objection is to the diagnosis, not to the
  caution -- relaxing this outcome to a warning reintroduces the hazard the BO-1500f-1
  gate was built for, and that AC is work_status: done.'
- The reported location must be the location actually attempted, taken from the resolution
  that ran -- not a constant reproduced in the message. A hard-coded path in the message
  is wrong the moment the resolution changes and is exactly what made the current
  message misleading.
- The reported reason must distinguish 'no file at that location' from 'permission
  refused'; these have different remedies and collapsing them recreates the defect
  one level down.
- The report must not name a permission setting, and must not assert anything about
  any agent's permission. This is a negative constraint on observable output and must
  survive as one -- a test that asserts only that some message was printed restates
  the bug.
- 'Must satisfy the repository error-handling policy: the failed read is external
  I/O and must be logged at WARNING or higher before the halt.'
- Must not emit the registry's contents or the process environment in the report --
  the location and the failure reason only.
```

## Agent Contracts

### documentation-expert

Existing docs to update / cross-link:

- docs/reference/plan-feature-layout-and-startup-checks.md (relationship: describes | status: planned | relevance: The reference page authored by ACD-2100d-4 must name each startup check and say what each of its reports means -- this is one of the four.)
- docs/known-issues/ac-driven-dev.md (relationship: describes | status: exists | relevance: KI-ACD-009 -- the false verdict, and the first of its three fix directions ('distinguish the four outcomes').)
- templates/workflows-js/plan-feature.js (relationship: implements | status: exists | relevance: Canonical source. The permitsShell computation and its single message are at lines 1745-1791.)
- docs/reference/false-green-mechanisms.md (relationship: describes | status: exists | relevance: Mechanism M8 inverted -- a check reporting a specific failure cause it did not establish.)
- docs/architecture/components/agent-registry.md (relationship: describes | status: exists | relevance: Component doc for the registry whose unreadability this outcome reports.)

- [ ] AC-1: [reference-doc] docs/reference/plan-feature-layout-and-startup-checks.md — the run stops and reports that the registry could not be read

### Delivers To

- **Agent:** python-coder
- **Contract:** One of the four distinguishable startup-check outcomes, each of which the fail-closed guard must observe halting the run: the unreadable-registry outcome, carrying the attempted location and a read-failure reason and carrying no permission verdict.

## Sign-offs

- [ ] test-writer
- [ ] python-coder
- [ ] test-runner
- [ ] documentation-expert
- [ ] pr-reviewer
- [ ] ac-validator
- [ ] ac-fulfillment-gate
- [ ] documentation-verifier
- [ ] commit

## Comments
