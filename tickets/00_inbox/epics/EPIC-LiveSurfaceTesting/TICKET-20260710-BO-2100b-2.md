---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  llm-expert: needed
  pr-reviewer: needed
  pull-request: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
complexity: low
components:
- build-orchestration
created: '2026-07-10'
depends_on:
- BO-2100b
files_touched:
- templates/skills/ticket-authoring/SKILL.md
- docs/architecture/adrs/ADR-020-live-surface-tester.md
priority: medium
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: BO-2100b-2
status: todo
title: Ticket not opted in means the check is skipped, even when the project enables
  it
---

# Ticket not opted in means the check is skipped, even when the project enables it

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BO-2100b-2` — Ticket not opted in means the check is skipped, even when the project enables it — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BO-2100b-2`. Component: `build-orchestration`. Assigned agent: `llm-expert`. Estimated complexity: `S`. Complexity: `low`.

## Acceptance Criteria

```gherkin
Given a project `skills_config.json` whose `live_surface_testing.enabled` is `true`
  And a ticket whose frontmatter sets `live_surface_test: false` or omits the field
When the ticket is authored and driven
Then no `live-surface-tester` phase is added to the ticket's `agents:` map
  And the live-app check is skipped for that ticket.
```

## Implementation Notes

```yaml
- A feature frontmatter toggle that is false OR absent must yield no agents-map entry
  even when the project toggle is true; absence must be treated identically to explicit
  false.
```

## Sign-offs

- [ ] llm-expert
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit
- [ ] pull-request

## Comments
