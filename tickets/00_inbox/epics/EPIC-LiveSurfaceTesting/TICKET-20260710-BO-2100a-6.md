---
advances_current_outcome: true
agents:
  architecture-diagram-author: needed
  commit: needed
  documentation-expert: not_needed
  pr-reviewer: needed
  pull-request: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
complexity: low
components:
- build-orchestration
created: '2026-07-10'
depends_on:
- BO-2100a
- BO-2100a-2
files_touched:
- docs/architecture/agent_delivery_workflows.md
- docs/architecture/adrs/ADR-020-live-surface-tester.md
priority: medium
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: BO-2100a-6
status: todo
title: Component diagram of the live-surface-testing pieces and their links
---

# Component diagram of the live-surface-testing pieces and their links

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BO-2100a-6` — Component diagram of the live-surface-testing pieces and their links — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BO-2100a-6`. Component: `build-orchestration`. Assigned agent: `architecture-diagram-author`. Estimated complexity: `S`. Complexity: `low`.

## Acceptance Criteria

```gherkin
Given the components that together deliver live-app testing
  (ticket-authoring skill, ticket-supervisor Spawn Allowlist, build-ticket.js
  phase ordering, live-surface-tester agent, live_surface_startup.py,
  port_registry.py, and skills_config.json)
When a maintainer needs to see how these parts connect
Then a component diagram exists showing each part and the edges between them:
  which component reads the config toggles, which dispatches the agent, and which
  manages the server process and port registry.
```

## Implementation Notes

```yaml
- The diagram must name every real component in the wiring (ticket-authoring skill,
  ticket-supervisor allowlist, build-ticket.js phaseOrder, live-surface-tester agent,
  live_surface_startup.py, port_registry.py, skills_config.json) and show the toggle-read,
  dispatch, and process/port-management edges between them.
- Component names and file paths shown must match the actual repository paths so the
  diagram stays verifiable against the code.
```

## Sign-offs

- [ ] architecture-diagram-author
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit
- [ ] pull-request

## Comments
