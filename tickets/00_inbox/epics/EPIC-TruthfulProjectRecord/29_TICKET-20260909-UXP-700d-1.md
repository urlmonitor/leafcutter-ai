---
ac_traceability:
  id: UXP-700d-1
  path: docs/acceptance-criteria/ux-prototyping/UXP-700-truthful-project-record/UXP-700d-1.yaml
advances_current_outcome: true
agents:
  ac-fulfillment-gate: needed
  ac-validator: needed
  architect-review: signed_off
  commit: needed
  documentation-expert: signed_off
  documentation-verifier: needed
  frontend-coder: not_needed
  pr-reviewer: needed
  pull-request: not_needed
  python-coder: signed_off
  sql-coder: not_needed
  test-runner: signed_off
  test-writer: signed_off
  user-surface-smoker: not_needed
change_target:
- code
- docs
complexity: high
components:
- ux_prototyping
created: '2026-09-09'
depends_on: []
documentation_required: true
files_touched: []
priority: medium
requires_adr: false
requires_diagram: false
risk_surface: contract_boundary
roadmap_phase: phase_1
source_ac: UXP-700d-1
status: todo
title: Example artifacts live under their own product root and never appear in a project's
  own record
---

# Example artifacts live under their own product root and never appear in a project's own record

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `UXP-700d-1` — Example artifacts live under their own product root and never appear in a project's own record — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `UXP-700d-1`. Component: `ux-prototyping`. Assigned agent: `python-coder`. Estimated complexity: `L`. Complexity: `high`.

## Acceptance Criteria

```gherkin
Given the example product's journeys, example datasets and screens,
When the artifacts belonging to the project's own record are enumerated,
Then none of the example product's artifacts is returned,
And the example product's artifacts are still returned when the example product is asked for
  by name,
And which product an artifact belongs to is decided by the product root it lives under rather
  than by anything in its content, so an artifact cannot belong to both and cannot be moved
  between them by an edit.
```

- [x] AC-1: none of the example product's artifacts is returned
- [x] AC-2: the example product's artifacts are still returned when the example product is asked for
- [x] AC-3: which product an artifact belongs to is decided by the product root it lives under rather

## Test Requirements

```yaml
tests:
- name: test_projects_own_record_excludes_every_example_artifact
  file: unit_tests/product_truth/test_uxp_700d_1.py
  covers:
  - UXP-700d-1
  asserts: Enumerating the project's own record returns no artifact under the example
    product root.
  framework: unittest
  type: integration
  angle: criterion
- name: test_example_product_remains_enumerable_by_name
  file: unit_tests/product_truth/test_uxp_700d_1.py
  covers:
  - UXP-700d-1
  asserts: Asking for the example product by name returns its journeys, datasets and
    screens, so separation is not achieved by deletion.
  framework: unittest
  type: integration
  angle: criterion
- name: test_ownership_is_decided_by_product_root_not_by_content
  file: unit_tests/product_truth/test_uxp_700d_1.py
  covers:
  - UXP-700d-1
  asserts: Editing an example artifact's content does not move it into the project's
    record, and editing a project artifact's content does not move it out.
  framework: unittest
  type: unit
  angle: seam
- name: test_uxp_700d_1_reachable_from_entry_point
  file: unit_tests/product_truth/test_uxp_700d_1.py
  covers:
  - UXP-700d-1
  angle: reachability
  asserts: 'REQUIRED — invoke the production entry point (CLI, hook, slash command,
    workflow dispatch, or main()) as a subprocess/dispatch and assert the new behaviour
    actually occurs. Do NOT satisfy this by importing the function directly. The AC
    authored no test_spec, so the entry point is not declared: resolve it before writing
    this test, and do not delete this entry.'
```

## Agent Contracts

### documentation-expert

Existing docs to update / cross-link:

- docs/architecture/adrs/ADR-022-mockups-are-the-real-app-in-mock-mode.md (relationship: context | status: exists | relevance: Hard constraint. ADR-022 depends on the example content continuing to exist and remaining referenceable. Separation, never deletion.)
- docs/acceptance-criteria/ux-prototyping/UXP-550-atlas-mock-mode/UXP-554.yaml (relationship: context | status: exists | relevance: Mock mode also depends on the example content existing and remaining resolvable by name.)

- [x] AC-1: reference-doc | docs/architecture/adrs/ADR-022-mockups-are-the-real-app-in-mock-mode.md | none of the example product's artifacts is returned <!-- signed: documentation-expert -->

### Delivers To

- **Agent:** python-coder
- **Contract:** A product-root-derived ownership predicate that UXP-700d-2 reuses for the work store and UXP-700d-4 reuses for per-type population counts.

## Sign-offs

- [x] architect-review — 2026-09-09 17:18
- [x] test-writer — 2026-09-09 18:05
- [x] python-coder — 2026-09-09 17:40
- [x] test-runner — 2026-09-09 18:20
- [x] documentation-expert — 2026-09-09 19:05
- [ ] pr-reviewer
- [ ] ac-validator
- [ ] ac-fulfillment-gate
- [ ] documentation-verifier
- [ ] commit

## Comments

### 2026-09-09 17:18 — architect-review (status: ok)
feedback-id: (submit-failed)
completion_manifest:
  blast_radius_analysis: true
  impact_classification: true
  cross_layer_seam_answer:
    result: not_applicable
    reason: "architect-review performs classification and documentation only; it introduces no code of its own and therefore has no producer/consumer seam to test."
    remediation: "n/a — not_applicable is a first-class outcome, not a defect."
Classified small: no always-large trigger fires (no Alembic migration, no TimescaleDB/hypertable change, no FastAPI public-API surface change — none apply to this tooling repo — and no ADR or ADR-contract file is modified; ADR-022 and UXP-554 are cited read-only for context). Threshold rules: frontmatter `components:` lists a single component (`ux_prototyping`); the concrete deliverable (a product-root ownership predicate plus its unit test file `unit_tests/product_truth/test_uxp_700d_1.py`) is well under the 5-file ceiling and stays inside one top-level package — the stated reuse by UXP-700d-2 and UXP-700d-4 happens in those tickets' own change sets, not this one's. `requires_adr: false` and `requires_diagram: false` in the existing frontmatter are confirmed correct; no diagram suggested (pure predicate addition within one component, no new service/container/API/state-machine boundary). Design note: this predicate is the load-bearing invariant for the rest of the epic (ownership decided by product-root path, never by content) — implement it as a single pure function (path → product id) so the two downstream tickets that commit to reusing it verbatim have exactly one place to import from, not a re-derivation. Anomaly: the ticket_path/worktree_path arguments this phase was dispatched with pointed at an unrelated worktree (`EPIC-TruthfulProjectRecord`, branch tracking `origin/main`) that does not contain this ticket; the real ticket was resolved instead in the `leafcutter-ai` repo on branch `epic/uxp-700-product-truth`. Also noted: AC-3's bullet text is truncated relative to its full gherkin clause, and sibling ticket UXP-700d-2 is already `status: done` while listing this still-`todo` ticket as a dependency (sequencing inversion) — neither blocks this classification but both are worth a look by the epic owner.

### 2026-09-09 18:05 — test-writer (status: ok)
feedback-id: (submit-failed)
completion_manifest:
  tests_written: true
  red_baseline_confirmed: true
  cross_layer_seam_answer:
    result: covered
    producing_side: "docs/product-truth/index.json (the real, checked-in product-truth generator output — flow/mockup/mock_data artifact records keyed on <product>/<name> ids)"
    consuming_side: "the new docs/product-truth/scripts/product_ownership.py predicate (own_record_artifacts / artifacts_for_product / is_example_artifact_id) that python-coder must add — test_ownership_is_decided_by_product_root_not_by_content pipes real index.json artifact records, mutated only in content, into the real predicate"
  reachability_entry_point_answer:
    result: resolved
    entry_point: "python docs/product-truth/scripts/product_ownership.py (no args = own record; --product <name> = by-name lookup) — new CLI, main(argv) guarded by if __name__ == '__main__', invoked via subprocess (CLI via subprocess)"
red_baseline:
  - test_name: test_projects_own_record_excludes_every_example_artifact
    file: unit_tests/product_truth/test_uxp_700d_1.py
    error: "ModuleNotFoundError: No module named 'product_ownership'"
  - test_name: test_example_product_remains_enumerable_by_name
    file: unit_tests/product_truth/test_uxp_700d_1.py
    error: "ModuleNotFoundError: No module named 'product_ownership'"
  - test_name: test_ownership_is_decided_by_product_root_not_by_content
    file: unit_tests/product_truth/test_uxp_700d_1.py
    error: "ModuleNotFoundError: No module named 'product_ownership'"
  - test_name: test_uxp_700d_1_reachable_from_entry_point
    file: unit_tests/product_truth/test_uxp_700d_1.py
    error: "ModuleNotFoundError: No module named 'product_ownership'"

Wrote unit_tests/product_truth/test_uxp_700d_1.py (4 tests: criterion x2 for AC-1/AC-2, seam for AC-3, reachability). No production module exists yet at docs/product-truth/scripts/product_ownership.py, so the whole file fails at collection with `ModuleNotFoundError: No module named 'product_ownership'` (confirmed via `python -m pytest unit_tests/product_truth/test_uxp_700d_1.py -v`, exit code 1, 1 collection error) — the correct red baseline. Tests are written against the REAL, checked-in docs/product-truth/index.json (11 leafcutter + 3 fern-and-fig flows, 1 fern-and-fig mock_data, 9 fern-and-fig mockups), never a hand-typed fixture. The reachability test resolves a NEW CLI entry point on the module python-coder must add (no pre-existing script's behaviour is gated on this predicate; wiring it into generate_product_truth.py or validate_product_truth.py instead would either duplicate UXP-700d-4's own scope or add an untracked side-effect to a script this ticket does not own — see the test file's REACHABILITY ENTRY-POINT RESOLUTION docstring note for the full checked-in-order rationale). python-coder must implement PROJECT_PRODUCT, product_of_artifact_id, is_example_artifact_id, own_record_artifacts, artifacts_for_product, and a main(argv) CLI in docs/product-truth/scripts/product_ownership.py to turn this file green.

### 2026-09-09 17:40 — python-coder (status: ok)
feedback-id: (submit-failed)
red_baseline_results:
  - test_name: test_projects_own_record_excludes_every_example_artifact
    result: green
  - test_name: test_example_product_remains_enumerable_by_name
    result: green
  - test_name: test_ownership_is_decided_by_product_root_not_by_content
    result: green
  - test_name: test_uxp_700d_1_reachable_from_entry_point
    result: green

Created docs/product-truth/scripts/product_ownership.py (199 lines, well under the 400-line cap): `PROJECT_PRODUCT = "leafcutter"`, pure `product_of_artifact_id`/`is_example_artifact_id`, `own_record_artifacts`/`artifacts_for_product` filters, and `main(argv)` — a small argparse CLI (no args = own record, `--product NAME` = by-name lookup) that reads `docs/product-truth/index.json` once via a typed try/except OSError/json.JSONDecodeError I/O boundary (log + re-raise, matching generate_product_truth.py's existing pattern). This is a standalone module with no dependency on generate_product_truth.py/validate_product_truth.py — deliberately, since neither existing script's behaviour is gated on this predicate (see the test file's REACHABILITY ENTRY-POINT RESOLUTION note). Note for UXP-700d-2/UXP-700d-4 (this AC's delivers_to contract): UXP-700d-2 is marked `status: done` already and its own `_PROJECT_PRODUCT`/`_is_example_content` in scripts/ac_store/scan_ac_store.py were written independently rather than importing this module (the sequencing inversion architect-review flagged) — that pre-existing duplication is out of this ticket's scope to reconcile, but UXP-700d-4 can and should import `product_ownership.py` verbatim.

All 4 red_baseline tests are green. Ran the full `unit_tests/product_truth/` suite (17 passed, 8 xfailed — the 8 are pre-existing AC-not-done xfails on UXP-700a-1 / UXP-700a-1-i, unrelated to this change) — no regressions. `ruff check` on the new file passed with zero findings; `scripts/commit_guardian/check_complexity.py` passed (10 files, including the new one); `.claude/hooks/check_exception_handling_hook.py` passed (exit 0). doc-enforcer requirements satisfied directly: module docstring carries MODULE/GOAL/BUSINESS CONTEXT/ARCHITECTURE, every function has a docstring, and a DECISION HISTORY block with a tail-tag was appended. No `.sql` files touched; no cross-file research needed beyond reading scan_ac_store.py and generate_product_truth.py directly for convention (research-agent not invoked). Flipped AC-1/AC-2/AC-3 checkboxes in `## Acceptance Criteria` to `[x]` — all three are directly exercised by the four tests above. No test-writer handoff needed: no new bugs found, no additional test coverage required beyond what test-writer already wrote.

### 2026-09-09 18:20 — test-runner (status: ok)
feedback-id: fb_2026-09-09_37dbd6cf
completion_manifest:
  test_suite_executed: true
  all_tests_passing: true
  failure_report_structured: true
Anomaly first: the dispatched `ticket_path`/`worktree_path` were an unrelated `EPIC-TruthfulProjectRecord` worktree at `C:\Users\Hendrik\Code\leafcutter\EPIC-TruthfulProjectRecord` (tracking `origin/main`) that does not contain this ticket file at all — confirms architect-review's earlier anomaly note. Resolved and ran the real work instead in `C:\Users\Hendrik\Code\leafcutter\leafcutter-ai` on branch `epic/uxp-700-product-truth`, where `unit_tests/product_truth/test_uxp_700d_1.py` and `docs/product-truth/scripts/product_ownership.py` actually live. Ran `python -m pytest unit_tests/product_truth/test_uxp_700d_1.py -v`: 4/4 tests PASSED (criterion x2, seam x1, reachability x1), total 0.37s — well under the 5s ceiling. Ran the broader `python -m pytest unit_tests/product_truth/ -v`: 17 passed, 8 xfailed (pre-existing AC-not-done masks on UXP-700a-1 / UXP-700a-1-i, unrelated to this change) — matches python-coder's reported baseline exactly, no regressions. No poetry/pyproject.toml present in this repo root, so ran via plain `python -m pytest` per the repo's own pytest.ini convention rather than the generic routing table's `poetry run` commands (those target a different, unrelated codebase).

### 2026-09-09 19:05 — documentation-expert (status: ok)
feedback-id: fb_2026-09-09_53bae4ba
completion_manifest:
  doc_written: true
  cross_links_added: true
  diataxis_genre_correct: true
Anomaly first, matching architect-review/test-runner's notes: the dispatched `ticket_path`/`worktree_path` pointed at an unrelated `EPIC-TruthfulProjectRecord` worktree at `C:\Users\Hendrik\Code\leafcutter\EPIC-TruthfulProjectRecord` (tracking `origin/main`) that does not contain this ticket; resolved and did the real work in `C:\Users\Hendrik\Code\leafcutter\leafcutter-ai` on branch `epic/uxp-700-product-truth`, where this ticket, ADR-022, and the new `product_ownership.py` module actually live. Read AC-1's contract line under `### documentation-expert` ("reference-doc | ADR-022... | none of the example product's artifacts is returned") as: author reference-genre documentation for the new `product_ownership.py` predicate/CLI and cross-link it to ADR-022 (context: the "separation, never deletion" hard constraint the predicate implements) and to UXP-554 (context: mock mode's own dependency on example content staying resolvable by name) — not an instruction to edit ADR-022 itself, consistent with `adr-author`'s documented constraint that it never edits existing ADRs. Added a new "Ownership predicate — `product_ownership.py`" reference section (definition, symbol table for `PROJECT_PRODUCT`/`product_of_artifact_id`/`is_example_artifact_id`/`own_record_artifacts`/`artifacts_for_product`, CLI usage) plus updated See Also and frontmatter `related_docs`/`related_code` cross-links to `docs/how-to/product-truth-schema-reference.md` — the project's established home for this exact kind of product-truth script documentation (mirrors the pre-existing `validate_product_truth.py` "Validator run outcome" section in the same file), and added a one-line entry for `product_ownership.py` to `docs/product-truth/README.md`'s Layout listing. No new file was created under `docs/reference/`: the topical-folder rule in `reference-author`'s own contract (`docs/agents/documentation/reference-author.md §3`) prefers "topical folder closest to the subsystem" over the generic default, and the subsystem already has a canonical reference doc for exactly this material. `check_doc_frontmatter.py` and `check_doc_links.py` both pass clean on the edited file. Attempted `scripts/generate_doc_index.py` per the mandatory INDEX.md regeneration instruction, but reverted the result: on this Windows environment the generator writes backslash path separators into every link (`docs\how-to\...` instead of the checked-in `docs/how-to/...`), which would have corrupted ~170 unrelated links repo-wide — flagging this as a tooling issue rather than committing a platform-specific regression. No `## AC Coverage` table exists in this v2 ticket's body (older template variant, predates that section's introduction) — flipped AC-1's checkbox under `### documentation-expert` in `## Agent Contracts` per §2c.2, but §2c.3's table-fill step has no table to fill; not a coverage gap, just an absent section. ADR-022 itself, UXP-554.yaml, and the AC-1/AC-2/AC-3 checkboxes in `## Acceptance Criteria` (already flipped by python-coder) were left untouched — this phase only added/extended reference documentation and cross-links.
