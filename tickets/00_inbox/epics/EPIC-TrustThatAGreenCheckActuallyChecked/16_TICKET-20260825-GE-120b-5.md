---
ac_traceability:
  id: GE-120b-5
  path: docs/acceptance-criteria/guardrail-engine/GE-120-green-means-checked/GE-120b-5.yaml
advances_current_outcome: true
agents:
  ac-fulfillment-gate: needed
  ac-validator: needed
  commit: needed
  documentation-expert: needed
  documentation-verifier: needed
  pull-request: not_needed
change_target: docs
complexity: low
components:
- documentation_system
- precommit_hooks
created: '2026-08-25'
depends_on:
- 15_TICKET-20260825-GE-120b-4.md
documentation_required: true
files_touched:
- CLAUDE.md
- docs/how-to/drive-epic-manually.md
- docs/how-to/managing-pre-commit-hooks.md
- docs/how-to/verify-precommit-active.md
- templates/scripts/commit_guardian/verify_precommit_active.py
priority: high
requires_adr: false
requires_diagram: false
risk_surface: internal
roadmap_phase: phase_1
source_ac: GE-120b-5
status: todo
title: The manual link-the-layout workaround is deleted, not left standing beside
  the fix
---

# The manual link-the-layout workaround is deleted, not left standing beside the fix

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `GE-120b-5` — The manual link-the-layout workaround is deleted, not left standing beside the fix — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `GE-120b-5`. Component: `guardrail-engine`. Assigned agent: `documentation-expert`. Estimated complexity: `S`. Complexity: `low`.

## Acceptance Criteria

```gherkin
Given the project's pre-drive instructions currently direct the reader to link the deployed
  layout into a fresh working copy by hand, and warn that all package checks are otherwise
  silently skipped for the entire drive,
When the parity behaviour of GE-120b-1 through GE-120b-4 is in place,
Then that manual instruction is removed from the pre-drive instructions rather than left in
  place alongside the fix,
And the how-to states instead what a reader should now expect — that the checks reach the
  same verdict from any working copy — and gives the one command that confirms it for the
  copy in front of them,
And no remaining instruction anywhere in the documentation set tells a reader to create the
  layout link by hand as a prerequisite for the checks to run,
And the related latent-hazard note about a suppression file resolving through the layout
  link is either removed with it or rewritten to describe the behaviour that now holds,
And the removal is verified by searching the documentation set for the instruction and
  finding no surviving occurrence.
```

- [ ] AC-1: that manual instruction is removed from the pre-drive instructions rather than left in
- [ ] AC-2: the how-to states instead what a reader should now expect — that the checks reach the
- [ ] AC-3: no remaining instruction anywhere in the documentation set tells a reader to create the
- [ ] AC-4: the related latent-hazard note about a suppression file resolving through the layout
- [ ] AC-5: the removal is verified by searching the documentation set for the instruction and

## Implementation Notes

```yaml
- THE INSTRUCTION TO DELETE is CLAUDE.md -> 'Pre-Drive Checklist' -> 'Worktree pre-commit
  config (MANDATORY for worktree-based drives)', including its Option A symlink recipe
  and its Option B copy recipe for NTFS/WSL2. Delete the section, do not soften it.
- The LATENT HAZARD paragraph in that same section — .security-allowlist resolving
  via the .leafcutter symlink target rather than the worktree root, with the dual-update
  rule — must be removed with it or rewritten to describe the behaviour that now holds.
  Leaving it orphaned re-teaches the workaround by implication, because a reader cannot
  follow a suppression rule about a link they were never told to create.
- 'SWEEP THE WHOLE DOCUMENTATION SET, not just the two named files: docs/how-to/managing-pre-commit-hooks.md,
  docs/how-to/verify-precommit-active.md, docs/how-to/drive-epic-manually.md, BOOTSTRAP.md,
  SETUP.md, and any templates/ copy of these. The criterion is NO SURVIVING OCCURRENCE
  anywhere. Verify by an explicit repo-wide search and record the command and its
  empty result in the sign-off.'
- The replacement text states the guarantee (the checks reach the same verdict from
  any working copy) and gives ONE confirm command. Use the SAME command GE-120d-5
  documents — docs/how-to/verify-precommit-active.md and templates/scripts/commit_guardian/verify_precommit_active.py
  already exist and are the natural home — so the two how-to edits agree rather than
  offering a reader two different commands.
- 'ORDERING — HARD CONSTRAINT: this AC lands only after GE-120b-4 demonstrates family-wide
  parity AND after GE-120d-4 demonstrates it end-to-end on a working copy nobody repaired
  by hand. Deleting the workaround while the defect is still live removes the only
  thing protecting drives from it.'
- CLAUDE.md and the how-to files are tracked documents subject to check_doc_frontmatter,
  check_doc_length and check_doc_links; edit in place and keep them passing in the
  same commit.
```

## Agent Contracts

### documentation-expert

Existing docs to update / cross-link:

- CLAUDE.md (relationship: modifies | status: exists | relevance: Pre-Drive Checklist -> 'Worktree pre-commit config (MANDATORY)' is the manual workaround to delete.)
- docs/how-to/managing-pre-commit-hooks.md (relationship: modifies | status: exists)
- docs/architecture/components/worktree-manager.md (relationship: related | status: exists)
- docs/how-to/verify-precommit-active.md (relationship: related | status: exists | relevance: Existing home for the confirm command the replacement text points at; keep it consistent with GE-120d-5.)

- [ ] AC-1: how-to | docs/how-to/managing-pre-commit-hooks.md | that manual instruction is removed from the pre-drive instructions rather than left in

### Expects From

- **AC:** GE-120b-4
- **Contract:** Demonstrated family-wide parity — the precondition that makes the manual instruction obsolete rather than merely inconvenient.

## Sign-offs

- [ ] documentation-expert
- [ ] ac-validator
- [ ] ac-fulfillment-gate
- [ ] documentation-verifier
- [ ] commit

## Comments
