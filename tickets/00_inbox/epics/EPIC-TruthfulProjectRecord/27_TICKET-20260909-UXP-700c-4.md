---
ac_traceability:
  id: UXP-700c-4
  path: docs/acceptance-criteria/ux-prototyping/UXP-700-truthful-project-record/UXP-700c-4.yaml
advances_current_outcome: true
agents:
  ac-fulfillment-gate: not_needed
  ac-validator: not_needed
  architect-review: not_needed
  commit: needed
  documentation-expert: signed_off
  documentation-verifier: needed
  frontend-coder: not_needed
  pr-reviewer: not_needed
  pull-request: not_needed
  python-coder: not_needed
  sql-coder: not_needed
  test-runner: not_needed
  test-writer: not_needed
  user-surface-smoker: not_needed
change_target: docs
complexity: low
components:
- ux_prototyping
- documentation_system
created: '2026-09-09'
depends_on:
- 21_TICKET-20260909-UXP-700c-2.md
- 24_TICKET-20260909-UXP-700c-3.md
documentation_required: true
files_touched: []
priority: medium
requires_adr: false
requires_diagram: false
risk_surface: internal
roadmap_phase: phase_1
source_ac: UXP-700c-4
status: todo
title: A how-to for reading a drift report and reconciling the record
---

# A how-to for reading a drift report and reconciling the record

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `UXP-700c-4` — A how-to for reading a drift report and reconciling the record — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `UXP-700c-4`. Component: `ux-prototyping`. Assigned agent: `documentation-expert`. Estimated complexity: `S`. Complexity: `low`.

## Acceptance Criteria

```gherkin
Given a person who has just been told by the automatic checks that part of their record is
  behind,
When they open the how-to for reconciling the record,
Then it states, for each kind of finding the checker can report - a pointer that does not
  resolve, a pointer it could not classify, a journey behind, a journey never confirmed -
  what the finding means and what to open first,
And it walks through reconciling one journey end to end, including how to record that the
  journey has been confirmed against the current code,
And it states what to do when the code was right and the record was wrong, and separately
  what to do when the record was right and the code was wrong,
And it states that a separator-only difference between platforms is not drift and must not be
  reconciled as though it were.
```

- [ ] AC-1: it states, for each kind of finding the checker can report - a pointer that does not
- [ ] AC-2: it walks through reconciling one journey end to end, including how to record that the
- [ ] AC-3: it states what to do when the code was right and the record was wrong, and separately
- [ ] AC-4: it states that a separator-only difference between platforms is not drift and must not be

## Agent Contracts

### documentation-expert

Existing docs to update / cross-link:

- docs/how-to/authoring-product-truth-artifacts.md (relationship: related | status: exists)
- docs/product-truth/README.md (relationship: related | status: exists)

- [x] AC-1: how-to | docs/how-to/reading-a-drift-report-and-reconciling-the-record.md | it states, for each kind of finding the checker can report - a pointer that does not resolve, a pointer it could not classify, a journey behind, a journey never confirmed <!-- signed: documentation-expert -->
- [x] AC-2: how-to | docs/how-to/reading-a-drift-report-and-reconciling-the-record.md | it walks through reconciling one journey end to end, including how to record that the journey has been confirmed against the current code <!-- signed: documentation-expert -->

### Expects From

- **AC:** UXP-700c-2
- **Contract:** The finding vocabulary the how-to explains.

## Sign-offs

- [x] documentation-expert — 2026-09-25 11:23
- [ ] documentation-verifier
- [ ] commit

## Comments

### 2026-09-25 11:23 — documentation-expert (status: ok)
feedback-id: fb_2026-09-25_f479342b
completion_manifest:
  doc_written: true
  cross_links_added: true
  diataxis_genre_correct: true

Authored a new how-to,
`docs/how-to/reading-a-drift-report-and-reconciling-the-record.md`, covering all
four AC clauses: Part 1 is a lookup table of the four findings the checker can
report (`[pointer]`, `[pointer-unresolvable]`, `[freshness]`,
`[freshness-never-confirmed]`, plus a note on the related
`[freshness-unresolvable]` case), what each means, and what to open first —
sourced directly from `product_truth_checks.py`/`validate_product_truth.py`'s
own message prefixes and docstrings (AC-1). Part 2 walks one `[freshness]`
finding end to end, including how to re-confirm a journey against current code
per the `confirmed.against`/`state` contract from UXP-700c-2 (AC-2). Part 3
states the code-right/record-wrong vs. record-right/code-wrong split, and for
the latter case points at this repository's own ticket mandate
(`CLAUDE.md` — no untracked code changes, every code change needs a ticket)
rather than "just fixing" the code inline (AC-3). Part 4 states the
separator-only-is-not-drift rule verbatim from `UXP-700c.yaml`'s and
`UXP-700c-3-i.yaml`'s notes, including why the checker's own path-separator
normalisation (UXP-700c-3-i, done) does not fully retire the risk of a human
misreading a raw cross-platform diff (AC-4).

**Anomaly / ticket-authoring fix**, matching the identical defect
architect-review flagged and documentation-expert corrected on sibling tickets
19 and 21 in this same epic: this ticket's own `### documentation-expert`
Agent Contracts block had both AC-1 (how-to) and AC-2 (sequence-diagram) rows
naming the same `target_path`
(`docs/how-to/authoring-product-truth-artifacts.md`) and, this time, the same
copy-pasted AC-1 description text for both rows too. `UXP-700c-4`'s own
criteria is a single how-to document covering four distinct clauses, not two
separate artifacts, and `requires_diagram: false` rules out a real
sequence-diagram for AC-2. Corrected both rows in place to the actual new file
produced, `docs/how-to/reading-a-drift-report-and-reconciling-the-record.md`,
substituting AC-2's genre `sequence-diagram → how-to` (same precedent ticket
19 set for its own AC-2), and gave AC-2 its own real description (the
walkthrough clause) instead of AC-1's.

Cross-linked from both docs named as `related` in this ticket's doc_links —
`docs/how-to/authoring-product-truth-artifacts.md` (`related_docs` entry,
inline pointer after Part 6, and a `See Also` entry) and
`docs/product-truth/README.md` (`related_docs` entry and a Validation-section
pointer) — plus, going slightly beyond the ticket's named targets because it
is the natural reciprocal link, `docs/reference/product-truth-checker-outcomes.md`
(the run-level outcome vocabulary this page's per-finding vocabulary rolls up
into). Regenerated `docs/INDEX.md` via `scripts/generate_doc_index.py`. Ran
`check_doc_frontmatter.py`, `check_doc_links.py`, and `check_documentation.py`
against the new file and all three edited files — all pass (exit 0).

**Glossary coverage:** ran `.leafcutter/scripts/glossary_detector.py` against
the new file. One novel candidate surfaced, `validate_product_truth`
(the script name, stripped of `.py`, appearing twice as an inline code span) —
absent from both `docs/glossary.md` and `docs/glossary_blacklist.md`. No
`Agent`/`Skill` tool was available in this session to dispatch
`glossary-triage` (the same toolset anomaly several sibling tickets in this
epic recorded independently), so the candidate was left untriaged rather than
hand-added to either file, per this project's "do not hand-edit the glossary"
rule. Flagging here for a future run with the `Agent` tool available to triage
it; it is very likely a false positive (a filename, not a concept).

Top-level `## Acceptance Criteria` AC-1..AC-4 checkboxes were left unchecked:
`ac-validator` is `not_needed` on this ticket's frontmatter, and per the
signoff skill's §2c.2 protocol, flipping those is that gate's job, not
documentation-expert's own `### documentation-expert` Agent Contracts block
(which has been flipped above). No `## AC Coverage` table exists in this
ticket's body, so §2c.3 is a no-op, matching the same gap noted on sibling
tickets 21/23/24 in this epic.
