id: INF-400e-3
title: "Second-run IT PO assigns agents correctly based on previously-learned component-agent mappings"
component: infrastructure
level: L2
status: active
req_status: active
work_status: todo
priority: high
readiness: reviewed
criteria: |
  Given the it-po agent ran once on the infrastructure component and discovered
    that infrastructure ACs are typically assigned to "llm-expert" for template work
    and "python-coder" for script work (not "frontend-coder" or "sql-coder"),
  And that mapping was captured in a component-scoped context file,
  When the it-po agent runs a second time on new infrastructure L2 ACs,
  Then the agent assigns "llm-expert" or "python-coder" to the new ACs
    without needing to re-read the full agent registry to discover this pattern,
  And the agent does not assign agents that have never been relevant to this
    component (e.g., does not assign "sql-coder" to infrastructure template ACs).
depends_on: [INF-400e, INF-400a, INF-400d]
doc_links:
  - path: docs/architecture/agent_knowledge_plane.md
    relationship: describes
    status: exists
  - path: templates/agents/it-po.md
    relationship: constrains
    status: exists
assigned_agent: llm-expert
estimated_complexity: M
it_requirements:
  - "Must demonstrate measurable improvement over baseline (first-run) output"
  - "Must not skip agent registry validation — learned mappings are heuristics, not overrides"
  - "Must be verifiable by comparing first-run and second-run agent assignments for the same component"
  - "Must still assign the correct agent even if the learned mapping is stale (agent registry is authoritative)"
delivers_to: null
expects_from: null
origin_agent: business-analyst
created: 2026-06-05
amended_by:
  - "python-coder: updated v3 agent name references to canonical names, 2026-06-29"
superseded_by: null
covered_by: []
implemented_by: []
