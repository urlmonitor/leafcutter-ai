id: ACS-300g-4
title: "Write tooling script and skill for adding component entries to the registry"
component: ac-store
level: L2
status: superseded_by
readiness: reviewed
priority: medium
req_status: active
work_status: todo
criteria: |
  Given a caller provides a component definition as structured input
    (id, name, type, description, primary_code, detail_ref, status),
  When the write tooling is invoked with that definition,
  Then a new entry is appended to docs/components.json under the components object,
  And the resulting JSON is valid (parseable, no duplicate keys),
  And the output preserves existing entries unchanged,
  And the script exits with a non-zero code if the component ID already exists
    (preventing silent overwrites).

  The tooling consists of two layers:
    1. A standalone Python script that reads the current components.json,
       validates the new entry against the schema, appends it, and writes
       the file back with consistent formatting (2-space indent, sorted keys).
    2. A skill that wraps the script, providing the agent-facing interface
       so that agents can invoke it during workflows without knowing the
       script path or argument format.
depends_on: [ACS-300g-1]
doc_links:
  - path: docs/components.json
    relationship: governs
    status: exists
assigned_agent: python-coder
estimated_complexity: M
delivers_to:
  agent: workflow-architect
  contract: "A working Python script that the skill wraps as its execution engine"
expects_from: null
origin_agent: business-analyst-v3
created: 2026-06-05
amended_by: [it-po-v3]
superseded_by: [ACS-300g-4a, ACS-300g-4b]
covered_by: []
implemented_by: []
