---
ac_traceability:
  id: BP-1100g-3
  path: docs/acceptance-criteria/build_pipeline/BP-1100-phantom-done-prevention/BP-1100g-3.yaml
advances_current_outcome: true
agents:
  ac-fulfillment-gate: needed
  ac-validator: needed
  commit: needed
  llm-expert: needed
  pull-request: needed
  python-coder: needed
  test-runner: needed
  test-writer: needed
change_target:
- prompt
- code
complexity: medium
components:
- build_pipeline
- testing_quality
created: '2026-08-25'
depends_on:
- TICKET-20260825-BP-1100g-1.md
files_touched:
- scripts/ac_store/done_proof.py
- templates/agents/test-writer.md
- unit_tests/ac_store/test_bp_1100g_3.py
- unit_tests/commit_guardian/test_bp_1100g_3_deployed.py
out_of_scope:
- .claude/agents/
- .leafcutter/
- scripts/commit_guardian/check_test_ac_tags.py
priority: medium
requires_adr: false
requires_diagram: false
risk_surface: internal
roadmap_phase: phase_1
source_ac: BP-1100g-3
status: todo
title: Each test states which kind of proof it was written to give, on the same records
  the existing coverage tag uses
---

# Each test states which kind of proof it was written to give, on the same records the existing coverage tag uses

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BP-1100g-3` — Each test states which kind of proof it was written to give, on the same records the existing coverage tag uses — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BP-1100g-3`. Component: `build-pipeline`. Assigned agent: `python-coder`. Estimated complexity: `M`. Complexity: `medium`.

## Acceptance Criteria

```gherkin
Given a test written to answer a specific requested kind of proof for a specific
    piece of work,
  And that test already carries the existing tag naming the piece of work it
    covers,
When the test is written and the suite is read,
Then the test also carries a tag naming the kind of proof it was written to give,
  And both tags are collected for the same test by one reader in one pass, so a
    test cannot be present on one axis and absent from the other by accident,
  And a test carrying a kind that is not one of the taught kinds is reported,
    naming the test and the unrecognised kind,
  And the new tag changes nothing about how any failing test is treated: it is a
    statement of what the test was written for, and it feeds no pass, done or
    eligibility decision anywhere.
```

- [ ] AC-1: that test already carries the existing tag naming the piece of work it
- [ ] AC-2: the test also carries a tag naming the kind of proof it was written to give
- [ ] AC-3: both tags are collected for the same test by one reader in one pass, so a
- [ ] AC-4: a test carrying a kind that is not one of the taught kinds is reported
- [ ] AC-5: the new tag changes nothing about how any failing test is treated: it is a

## Test Requirements

```yaml
tests:
- name: test_bp_1100g_3_both_tag_axes_collected_for_the_same_test_in_one_pass
  file: unit_tests/ac_store/test_bp_1100g_3.py
  covers:
  - BP-1100g-3
  asserts: One scan of a test tree returns, per test function, both the covers ids
    and the declared kinds; a test carrying only one of the two axes appears with
    the other empty rather than being omitted from the results.
  framework: unittest
  type: unit
  angle: criterion
- name: test_bp_1100g_3_tags_are_collected_from_real_on_disk_test_files_in_every_accepted_position
  file: unit_tests/ac_store/test_bp_1100g_3.py
  covers:
  - BP-1100g-3
  asserts: 'Scan actual .py files written to disk exactly as the project''s own convention
    produces them — tag on the line above the def, on the first body line, and inside
    the docstring — rather than hand-typed source strings. The PhantomDoneFilesTouched
    lesson: a hand-typed fixture reproduces the bias that hides the bug.'
  framework: unittest
  type: integration
  angle: real_artifact
- name: test_bp_1100g_3_unrecognised_kind_is_reported_naming_test_and_value
  file: unit_tests/ac_store/test_bp_1100g_3.py
  covers:
  - BP-1100g-3
  asserts: A test tagged with a kind outside the permitted set produces a report naming
    that test and that value, and the scan completes without raising and without dropping
    the record.
  framework: unittest
  type: unit
  angle: failure
- name: test_bp_1100g_3_second_axis_is_collected_through_the_deployed_done_proof_hook
  file: unit_tests/commit_guardian/test_bp_1100g_3.py
  covers:
  - BP-1100g-3
  asserts: 'PRODUCTION ENTRY POINT: run `python .leafcutter/scripts/commit_guardian/run_hook.py
    .leafcutter/scripts/commit_guardian/check_done_proof.py <staged AC yaml>` as a
    subprocess against a temp test tree carrying both axes, and assert the deployed
    hook path collects them — importing done_proof from the source tree is structurally
    blind to the deploy-manifest gap that broke this exact module on 2026-07-22.'
  framework: unittest
  type: integration
  angle: reachability
```

## Implementation Notes

```yaml
config_schema_fragment:
  description: One record per test function, collected in a single pass, carrying
    BOTH tag axes. A test present on one axis and absent from the other is representable
    and preserved, never dropped and never defaulted.
  properties:
    angles:
      items:
        type: string
      type: array
    covers:
      items:
        type: string
      type: array
    file:
      type: string
    function:
      type: string
    lineno:
      type: integer
  required:
  - file
  - function
  - covers
  - angles
  type: object
constraints:
- ONE SCANNER, TWO AXES. Extend _scan_test_root_for_covers_tags / _scan_single_test_file
  in the reference file so both tags are collected for the same test function in the
  same pass — that is the single location n_location_rule refers to. A parallel scanner
  over the same files WILL drift, and drift between two readers of one source is the
  EPIC-ComputedQualityGates layer-3 failure again. Extending also makes the one-reader-one-pass
  clause true by construction rather than by assertion.
- 'NAME EVERY READER OF THIS TAG SURFACE. There are two live ones: done_proof.py''s
  scanner (library, used by the check_done_proof hook and CI gate) and scripts/commit_guardian/check_test_ac_tags.py
  (pre-commit, its own COVERS_REGEX, currently warn mode). Any change to tag placement
  or format must keep both in agreement, and the new axis must not change check_test_ac_tags''
  enforcement mode or its verdict.'
- The new axis must not enter _classify_outcomes, verify_done_eligible, or any eligibility
  computation. BP-1100g-3-i makes that falsifiable; an implementation that consumes
  the kind tag in a verdict has converted a planning declaration into a verdict, which
  the parent L1 forbids.
- An unrecognised kind must be REPORTED naming the test and the value, and must not
  be dropped silently and must not raise. The permitted set is the one BP-1100g-1
  makes single — read it, do not restate it.
- Tag placement and format must match the existing convention exactly (line above
  the def, first line of the body, or the docstring — the three positions check_test_ac_tags
  already accepts), so the writer learns one convention rather than two.
- 'DEPLOY-MANIFEST OBLIGATION: done_proof.py backs a deployed pre-commit hook and
  a CI gate. Any new module it imports must be added to build_ac_store''s deploy_map
  in scripts/build_phases.py, or the deployed hook raises ModuleNotFoundError and
  blocks every merge once the gate is required — the 2026-07-22 done_proof.py incident,
  verbatim.'
- Scanning must stay linear in the number of test files and must not add a second
  full walk of the test tree; the scanner runs on the pre-commit path.
- 'COMPANION SURFACE THIS AGENT CANNOT WRITE: the first Then clause (''the test also
  carries a tag'') is an authoring convention that lives in templates/agents/test-writer.md
  §2i, which is llm-expert surface, not python-coder surface. It is named here so
  it is not lost; see the caveat in the enrichment note for the recommended follow-up.'
n_location_rule: '1'
post_write_commands:
- python scripts/build.py --target-dir .
- python -m pytest unit_tests/ac_store/ unit_tests/commit_guardian/ -q
reference_file_path: scripts/ac_store/done_proof.py
required_skills:
- python-coder
```

### Scope correction — TWO SURFACES, AND THE PROMPT ONE IS NOT OPTIONAL

The generated `files_touched` named only `scripts/ac_store/done_proof.py`, and the
readiness report passed it on a path count (KI-ACS-002). That is half the work.

This ticket has **two owners and two surfaces**, which is why `change_target` is
`[prompt, code]` and why both `llm-expert` and `python-coder` are in the agents map:

| clause | surface | owner |
|---|---|---|
| 1 — the test *carries* a kind tag | `templates/agents/test-writer.md` | **llm-expert** |
| 2-4 — one scanner collects both axes | `scripts/ac_store/done_proof.py` | **python-coder** |

**Build only the reader and you have built nothing.** The AC's own it-po note says it
outright: *"Until one of those happens, python-coder builds a reader for a tag nothing
is instructed to emit."* The it-po recommended splitting clause 1 into an L3, but
rejected it because `BP-1100g` is at `child_limit_override: 7`. It stays in this ticket,
so it must be built in this ticket. `BP-1100g-1` (merged today, `c042c967`) already
added the `<!-- TAUGHT-TEST-ANGLES:START/END -->` anchor to that same template — the
tag-authoring convention belongs alongside it, in the same file, as one convention.

**Edit `templates/agents/test-writer.md`, never `.claude/agents/test-writer.md`.** The
latter is a symlink into `.leafcutter/` that `build.py` regenerates; an edit there is
lost on the next build and is listed in `out_of_scope` as a hard violation.

`scripts/commit_guardian/check_test_ac_tags.py` is ALSO in `out_of_scope`, but for a
different reason: it is the **second live reader** of this tag surface, with its own
`COVERS_REGEX`, currently in warn mode. The AC requires the two readers to stay in
agreement — that is a constraint to respect, not a file to edit here. If the work
genuinely cannot be done without changing it, stop and say so rather than editing it
quietly.

Test files added to the surface because all four `test_spec` entries need somewhere to
land: `unit_tests/ac_store/` (three) and `unit_tests/commit_guardian/` (the deployed-hook
reachability one).

## Agent Contracts

### Delivers To

- **Agent:** python-coder
- **Contract:** For each test function, one record collected in a single pass carrying both axes: {file, lineno, function, covers: list[ac_id], angles: list[str]}. A test present on one axis and absent from the other is representable and preserved — never dropped, never defaulted. This is the CLAIM side that BP-1100g-4's promise-versus-claim check reads.

### Expects From

- **AC:** BP-1100g-1
- **Contract:** The single set of permitted kinds, resolvable from one source, against which an unrecognised tag value is decided.

## Sign-offs

- [ ] test-writer
- [ ] python-coder
- [ ] test-runner
- [ ] ac-validator
- [ ] ac-fulfillment-gate
- [ ] llm-expert
- [ ] commit
- [ ] pull-request

## Comments
