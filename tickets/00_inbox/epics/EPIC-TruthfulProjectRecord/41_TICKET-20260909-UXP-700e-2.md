---
ac_traceability:
  id: UXP-700e-2
  path: docs/acceptance-criteria/ux-prototyping/UXP-700-truthful-project-record/UXP-700e-2.yaml
advances_current_outcome: true
agents:
  ac-fulfillment-gate: needed
  ac-validator: needed
  architect-review: signed_off
  commit: needed
  documentation-expert: needed
  documentation-verifier: needed
  frontend-coder: not_needed
  pr-reviewer: not_needed
  pull-request: not_needed
  python-coder: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: signed_off
  user-surface-smoker: not_needed
change_target:
- code
- schema
complexity: medium
components:
- ux_prototyping
created: '2026-09-09'
depends_on: []
documentation_required: true
files_touched: []
priority: medium
requires_adr: false
requires_diagram: false
risk_surface: internal
roadmap_phase: phase_1
source_ac: UXP-700e-2
status: todo
title: Each thing is described once; a shorter form is derived, never separately authored
---

# Each thing is described once; a shorter form is derived, never separately authored

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `UXP-700e-2` — Each thing is described once; a shorter form is derived, never separately authored — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `UXP-700e-2`. Component: `ux-prototyping`. Assigned agent: `python-coder`. Estimated complexity: `M`. Complexity: `medium`.

## Acceptance Criteria

```gherkin
Given a journey whose authoritative description lives in the journey itself,
When a shorter form of that description is needed for a place that cannot show the whole of
  it,
Then the shorter form is produced from the authoritative one at the time it is needed,
And no second, separately authored description of the same journey exists anywhere in the
  record,
And changing the authoritative description changes every place the shorter form appears, with
  no other edit,
And a second authored description introduced anywhere in the record is reported, naming the
  journey and both places its description is held.
```

- [ ] AC-1: the shorter form is produced from the authoritative one at the time it is needed
- [ ] AC-2: no second, separately authored description of the same journey exists anywhere in the
- [ ] AC-3: changing the authoritative description changes every place the shorter form appears, with
- [ ] AC-4: a second authored description introduced anywhere in the record is reported, naming the

## Test Requirements

```yaml
tests:
- name: test_shorter_form_is_produced_from_the_authoritative_description
  file: unit_tests/product_truth/test_uxp_700e_2.py
  covers:
  - UXP-700e-2
  asserts: The short form shown in the index is computed from the journey's own description.
  framework: unittest
  type: integration
  angle: criterion
- name: test_changing_the_authoritative_description_changes_every_shorter_form
  file: unit_tests/product_truth/test_uxp_700e_2.py
  covers:
  - UXP-700e-2
  asserts: Editing a journey's description and regenerating changes the index short
    form, with no other file edited.
  framework: unittest
  type: integration
  angle: seam
- name: test_a_second_authored_description_is_reported
  file: unit_tests/product_truth/test_uxp_700e_2.py
  covers:
  - UXP-700e-2
  asserts: A hand-authored second description for the same journey is reported naming
    the journey and both locations.
  framework: unittest
  type: integration
  angle: failure
- name: test_uxp_700e_2_reachable_from_entry_point
  file: unit_tests/product_truth/test_uxp_700e_2.py
  covers:
  - UXP-700e-2
  angle: reachability
  asserts: 'REQUIRED — invoke the production entry point (CLI, hook, slash command,
    workflow dispatch, or main()) as a subprocess/dispatch and assert the new behaviour
    actually occurs. Do NOT satisfy this by importing the function directly. The AC
    authored no test_spec, so the entry point is not declared: resolve it before writing
    this test, and do not delete this entry.'
```

## Agent Contracts

### documentation-expert

Existing docs to update / cross-link:

- docs/acceptance-criteria/ux-prototyping/UXP-300-product-truth-store/UXP-300.yaml (relationship: context | status: exists | relevance: Sibling L2 and this L1's existing child: one canonical dataset per entity per component, extend never duplicate. This AC applies the same rule to descriptions.)
- docs/acceptance-criteria/ux-prototyping/UXP-500-product-truth-generation/UXP-514.yaml (relationship: context | status: exists | relevance: Complementary, not duplicate. UXP-514-6 guards the duplicate copy while it exists; this AC removes the duplication.)

- [ ] AC-1: reference-doc | docs/acceptance-criteria/ux-prototyping/UXP-300-product-truth-store/UXP-300.yaml | the shorter form is produced from the authoritative one at the time it is needed

### Expects From

- **AC:** UXP-700e-1
- **Contract:** The declared bound for the place the shorter form is shown.

## Sign-offs

- [x] architect-review — 2026-09-09 17:18
- [x] test-writer — 2026-09-09 17:35
- [ ] python-coder
- [ ] test-runner
- [ ] documentation-expert
- [ ] ac-validator
- [ ] ac-fulfillment-gate
- [ ] documentation-verifier
- [ ] commit

## Comments

### 2026-09-09 17:18 — architect-review (status: ok)
feedback-id: fb_2026-09-09_881e707f
completion_manifest:
  blast_radius_analysis_performed: true
  impact_classification: true
  requires_adr_confirmed: true
Small-case classification. Blast radius confined to docs/product-truth/scripts/generate_product_truth.py, docs/product-truth/scripts/validate_product_truth.py, docs/product-truth/index.json (regenerated data), and the new unit_tests/product_truth/test_uxp_700e_2.py — all within the single `ux_prototyping` component, well under the 5-file threshold, no cross-module boundary crossed. Confirmed no always-large trigger fires: no Alembic migration, no hypertable change, no FastAPI/public-API surface, no ADR-contract file touched (this project's docs/architecture/adrs is untouched; the AC's own docs/acceptance-criteria tree is a different surface). `requires_adr: false` and `requires_diagram: false` (already set in ticket frontmatter) are confirmed correct — impact is not HIGH and only one component is touched. Full architectural note and acceptance-criteria adjustments are returned in this phase's structured payload; no architect-review-deep escalation needed.

### 2026-09-09 17:35 — test-writer (status: ok)
feedback-id: fb_2026-09-09_5b43c87c
completion_manifest:
  tests_written: true
  red_baseline_confirmed: true
  reachability_entry_point_answer:
    result: resolved
    entry_point: "python docs/product-truth/scripts/generate_product_truth.py --check --quiet (CLI via subprocess, main() guarded by if __name__ == '__main__':). This AC's own Test Requirements entry carried the BO-2900g-4 unresolved sentinel (no surface_invoked declared); resolved per Step 1 category 1 (CLI script with main() + argparse) — no further steps needed."
  cross_layer_seam_answer:
    result: covered
    producing_side: "generate_product_truth.py's generate()/write_index(), run for real (not mocked) against a from-scratch fixture store, writing index.json's derived artifacts[].summary for a flow-type artifact from the flow's own on-disk summary."
    consuming_side: "a fresh read of the written index.json — the real consumer of the store's derived data — asserting its own observable content changes when, and only when, the authoritative flow summary it derives from changes on disk, with no other flow file's bytes touched."
Wrote unit_tests/product_truth/test_uxp_700e_2.py with 4 tests (one per test_spec entry: criterion, seam, failure, reachability), each tagged `# covers: UXP-700e-2` and its sourced `# angle:`. Confirmed via `AC_ENFORCE_STRICT=1 python3 -m pytest unit_tests/product_truth/test_uxp_700e_2.py -v` that all 4 are red against the current (pre-fix) generate_product_truth.py: the criterion/seam tests fail because the stored, independently-hand-typed `artifacts[].summary` survives regeneration untouched (nothing in the generator today derives it from the flow's own `summary`); the failure test fails because no warning is logged for the divergence; the reachability test fails because `--check` exits 0 on a store that was first normalized by one real (pre-fix) `generate()` run so only the summary-derivation gap remains stale. See `red_baseline` below for exact captured errors.
red_baseline:
  - test_name: test_shorter_form_is_produced_from_the_authoritative_description
    file: unit_tests/product_truth/test_uxp_700e_2.py
    error: "AssertionError: 'A short, independently hand-typed blurb about the ticket pipeline.' == 'A short, independently hand-typed blurb about the ticket pipeline.' : the independently hand-typed stale summary must be overwritten by a value computed from the flow's own summary, not preserved as a second authored text"
  - test_name: test_changing_the_authoritative_description_changes_every_shorter_form
    file: unit_tests/product_truth/test_uxp_700e_2.py
    error: "AssertionError: 'A short, independently hand-typed blurb about the ticket pipeline.' == 'A short, independently hand-typed blurb about the ticket pipeline.' : editing journey A's authoritative description must change EVERY place the shorter form appears — the index short form for A did not change"
  - test_name: test_a_second_authored_description_is_reported
    file: unit_tests/product_truth/test_uxp_700e_2.py
    error: "AssertionError: no logs of level WARNING or higher triggered on root"
  - test_name: test_uxp_700e_2_reachable_from_entry_point
    file: unit_tests/product_truth/test_uxp_700e_2.py
    error: "AssertionError: 0 != 1 : a store whose index artifact summary was hand-typed independently of the flow's own (938-character-class) summary must be reported stale by the real CLI's --check mode once the derivation is wired in; stdout='' stderr=''"

### 2026-09-09 18:20 — python-coder (status: blocker)
feedback-id: fb_2026-09-09_459cfa33
completion_manifest:
  code_implemented: true
  three_of_four_red_baseline_tests_green: true
  reachability_test_green:
    result: false
    reason: "TestReachability.test_uxp_700e_2_reachable_from_entry_point cannot pass against any correct, idempotent implementation of the derivation this AC requires — see classification below."
    remediation: "Respawn test-writer to fix the fixture in unit_tests/product_truth/test_uxp_700e_2.py's _build_minimal_cli_store (see prose)."
(classification: test_drift)

NOTE ON WORKING LOCATION: this ticket's `ticket_path` resolved only under
leafcutter-ai's tickets/ tree, but the sign-off trail already on this ticket
(architect-review, test-writer) was produced against code and tests that
physically exist only in the sibling `EPIC-TruthfulProjectRecord` worktree
(e.g. unit_tests/product_truth/test_uxp_700e_2.py exists there, not in
leafcutter-ai; leafcutter-ai's own docs/product-truth/scripts/generate_product_truth.py
is on a different, further-ahead branch state entirely). Following the same
precedent, this phase implemented and ran everything in the
EPIC-TruthfulProjectRecord worktree and is recording this sign-off, per the
"this ticket's own record" instruction, on this file.

Implemented `derive_artifact_summary()` and `_sync_artifact_summaries()` in
`docs/product-truth/scripts/generate_product_truth.py` (worktree path:
EPIC-TruthfulProjectRecord/docs/product-truth/scripts/generate_product_truth.py):
for every flow-type artifact, `write_index()` now overwrites
`artifacts[].summary` with a value derived from the flow's own authoritative
`summary` (a word-boundary truncation plus a short content fingerprint of
the FULL text, so ANY edit to the authoritative description — even past the
visible truncation point — changes the derived form), and logs a WARNING
naming the flow and showing both the stale and the freshly-derived text when
a divergence (a second, separately authored description) is found. This
turned 3 of the 4 red_baseline tests green:
  - test_shorter_form_is_produced_from_the_authoritative_description: green
  - test_changing_the_authoritative_description_changes_every_shorter_form: green
  - test_a_second_authored_description_is_reported: green

The 4th, `test_uxp_700e_2_reachable_from_entry_point`, stays red under ANY
correct implementation, confirmed empirically (ran the fixture's own
`_build_minimal_cli_store` helper in-process, then called
`gpt.generate(check=True, ...)` immediately after: `changed=False` — nothing
is stale). The helper builds a store with a stale, hand-typed artifact
summary, then calls `_run_generate(..., check=False, ...)` ONCE to
"normalize" every OTHER derived field before asserting on `--check`. That
pre-run uses the same imported, already-fixed `gpt` module the CLI subprocess
also runs — so the very act of normalizing the fixture also derives and
writes the corrected `artifacts[].summary`, exactly the field this AC
targets. By the time the CLI's own `--check` subprocess runs, the store is
already fully consistent (this module's documented IDEMPOTENCY invariant:
"running the generator twice in a row produces no diff"), so `--check` finds
nothing stale and exits 0, never 1. This is not implementation-formula
dependent — I verified it holds for both a plain prefix-truncation and a
hash-fingerprinted truncation, since idempotency is what breaks it, not the
derivation's shape. Classification per ADR-003 / the Contract-Shrinkage
Guard: production is correct (the derivation IS idempotent, which is this
module's own stated architectural invariant); the test's fixture assumption
("today's script never touches artifacts[].summary... surviving the
normalization run untouched") was true only against the PRE-fix code and
necessarily stops holding once the fix is wired in — `test_drift`, not
`production_drift`. Per the TDD Red-Baseline Success Gate, I have NOT
modified, skipped, or weakened this test, and I am halting rather than
signing off. Suggested remediation: respawn test-writer to change
`_build_minimal_cli_store`'s pre-run to `check=True` (so the on-disk store
is NOT written during normalization, leaving the summary genuinely stale for
the CLI's own `--check` to find), or otherwise decouple the reachability
assertion from a fixture step that runs the exact same producer being
tested. `python-coder`'s own frontmatter/Sign-offs entries are left
unchanged (`needed`) pending that fix.
