id: ACD-1100a-2
components:
  - ac-driven-dev
readiness: approved
priority: high
title: "Legacy pipeline agent registry entries are removed"
component: ac-driven-dev
level: L2
status: active
req_status: approved
work_status: todo
criteria: |
  Given the agent registry (config/agent_registry.json) currently contains
    entries with ids: "create-ticket", "create-ticket-v2", "business-analyst",
    "business-analyst-v2", "create-epic", "refinement", and "it-po" (the
    entry with spawned_by including "create-ticket" or "create-ticket-v2"),
  When the v2.0 migration removes legacy agents,
  Then config/agent_registry.json contains zero entries whose "id" field
    matches any of those seven values,
  And the total agent count in the registry decreases by exactly 7
    (accounting for the it-po entry being replaced by the promoted it-po-v3).
depends_on:
  - ACD-1100a
doc_links:
  - path: docs/reference/agent-template-frontmatter.md
    relationship: describes
    status: exists
  - path: docs/architecture/agent_knowledge_system.md
    relationship: describes
    status: exists
assigned_agent: python-coder
estimated_complexity: S
it_requirements:
  - "Registry file must remain valid JSON after edits"
  - "Must preserve all non-removed entries exactly as-is (no reordering or reformatting of unrelated entries)"
  - "Must be idempotent — running against a registry where entries are already absent produces no error"
delivers_to:
  agent: python-coder
  contract: "config/agent_registry.json with exactly 7 fewer entries in the agents array; valid JSON; zero entries with ids matching the removed set"
expects_from: null
origin_agent: BrainCandy
created: 2026-06-05
amended_by: []
superseded_by: null
covered_by: []
implemented_by: []
change_target: config
risk_surface: contract_boundary
