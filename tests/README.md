# Leafcutter Unit Tests

This directory contains the unit tests for the Leafcutter-AI package.

## Running Tests

You can run the tests with:
```bash
python -m unittest discover -s leafcutter-ai/tests -p "test_*.py"
```
