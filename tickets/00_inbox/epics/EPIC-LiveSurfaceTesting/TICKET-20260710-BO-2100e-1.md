---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  pr-reviewer: needed
  pull-request: not_needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
complexity: medium
components:
- build_orchestration
created: '2026-07-10'
depends_on:
- BO-2100e
- BO-2100a-1
- BO-2100a-2
- BO-2100a-3
- BO-2100a-4
- BO-2100c-1
- BO-2100c-2
- BO-2100c-3
- BO-2100d-1
- BO-2100d-2
- BO-2100d-3
files_touched:
- templates/workflows-js/build-ticket.js
- templates/agents/ticket-supervisor.md
- docs/architecture/adrs/ADR-020-live-surface-tester.md
priority: critical
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: BO-2100e-1
status: todo
title: An opted-in ticket driven through ticket-supervisor demonstrably spawns live-surface-tester
---

# An opted-in ticket driven through ticket-supervisor demonstrably spawns live-surface-tester

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BO-2100e-1` — An opted-in ticket driven through ticket-supervisor demonstrably spawns live-surface-tester — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BO-2100e-1`. Component: `build-orchestration`. Assigned agent: `test-runner`. Estimated complexity: `M`. Complexity: `medium`.

## Acceptance Criteria

```gherkin
Given a real ticket whose frontmatter sets `live_surface_test: true` and a project
  whose `live_surface_testing.enabled` is `true`
When that ticket is driven through ticket-supervisor / build-ticket.js in a real
  process (not a synthetic-fixture unit test)
Then the run demonstrably dispatches the `live-surface-tester` phase — observed
  from the real dispatch path, e.g. the phase appearing as executed in the drive
  output with the recorded dispatch occurring after user-surface-smoker and before
  commit
  And this observation is the closure gate for the epic: the epic is not done until
  a real run has been seen to fire the phase.
```

## Implementation Notes

```yaml
- 'Must execute a REAL ticket whose frontmatter sets live_surface_test: true, against
  a project whose skills_config live_surface_testing.enabled is true, driven through
  ticket-supervisor / build-ticket.js in a fresh real process — NOT a synthetic-fixture
  unit test.'
- Must assert the live-surface-tester phase is actually dispatched/spawned, observed
  from the real drive output, with the dispatch occurring after user-surface-smoker
  and before commit.
- 'This observation is the epic closure gate: the epic MUST NOT be marked done until
  a real run has been seen to fire the phase (anti-phantom-done).'
- Must run only after the dispatch wiring (BO-2100a-1..a-4) and the startup/teardown
  fixes (BO-2100c-1..c-3, BO-2100d-1..d-3) are in place, so a failure to spawn reflects
  a real wiring defect and not a missing prerequisite.
```

## Sign-offs

- [ ] test-runner
- [ ] test-writer
- [ ] pr-reviewer
- [ ] commit

## Comments
