id: BO-2100b-5
components:
  - build_orchestration
title: "Reference documentation for the live_surface_testing config block"
component: build-orchestration
level: L2
status: active
req_status: draft
work_status: todo
readiness: approved
priority: medium
roadmap_phase: phase_1
criteria: |
  Given the `live_surface_testing` block in skills_config.json
  When a maintainer needs the authoritative field list
  Then reference documentation exists that names each field
    (`enabled`, `startup_command`, `health_check_path`, `startup_timeout_seconds`,
    `port_range_start`, `port_range_end`), its type, its default, and its meaning,
    And it states that an absent block means the capability is disabled.
depends_on: [BO-2100b, BO-2100b-1]
doc_links:
  - path: scripts/config_loader.py
    relationship: describes
    status: exists
  - path: docs/reference/skills-config-fields.md
    relationship: describes
    status: exists
  - path: docs/architecture/adrs/ADR-020-live-surface-tester.md
    relationship: describes
    status: exists
assigned_agent: documentation-expert
estimated_complexity: S
it_requirements:
  - "Must document each field (`enabled`, `startup_command`, `health_check_path`, `startup_timeout_seconds`, `port_range_start`, `port_range_end`) with its type, default, and meaning."
  - "Must state that an absent live_surface_testing block means the capability is disabled (matching the BO-2100b-1-i safe-default behavior)."
  - "The field list, types, and defaults must match config_loader.py validation so the reference cannot drift from the enforced schema."
delivers_to: null
expects_from: null
origin_agent: business-analyst
created: 2026-07-10
amended_by: []
superseded_by: null
covered_by: []
implemented_by:
- tickets/00_inbox/epics/EPIC-LiveSurfaceTesting/TICKET-20260710-BO-2100b-5.md
