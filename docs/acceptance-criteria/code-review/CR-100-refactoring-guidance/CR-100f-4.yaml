id: CR-100f-4
title: "Leaf reviewers are read-only and return their findings"
component: code-review
components:
  - review_system
level: L2
status: active
req_status: active
work_status: done
readiness: reviewed
priority: high
roadmap_phase: phase_1
criteria: |
  Given the two leaf reviewer agents find-structural-smells and find-design-smells,
  When each agent's configuration is inspected,
  Then each may run Bash, read files, and invoke skills but has no file-writing
    capability,
  And each is not marked as requiring verification,
  And each returns its findings to the caller rather than writing them to a file.
depends_on:
  - CR-100f
  - CR-100f-2
doc_links:
  - path: docs/acceptance-criteria/code-review/PROJECT_CONTEXT.md
    relationship: describes
    status: exists
  - path: templates/agents/find-structural-smells.md
    relationship: implements
    status: exists
  - path: templates/agents/find-design-smells.md
    relationship: implements
    status: exists
assigned_agent: python-coder
estimated_complexity: S
it_requirements:
  - "Assert from the deployed agent-template frontmatter that each leaf agent's tools are exactly Bash, Read, Skill — no Write and no Edit (no file-writing capability)."
  - "Assert requires_verification is false for both leaf agents in BOTH the template frontmatter and config/agent_registry.json, and that each declares produces: review_verdict (returns findings to the caller rather than writing a file)."
test_required: true
test_spec:
  - name: test_leaf_agents_have_no_write_tool
    target_dir: unit_tests/
    framework: pytest
    type: unit
    description: "In unit_tests/test_code_smell_review_wiring.py, parse both agent templates and assert tools == {Bash, Read, Skill} with no Write or Edit."
    covers:
      - CR-100f-4
  - name: test_leaf_agents_not_requires_verification
    target_dir: unit_tests/
    framework: pytest
    type: unit
    description: "Parse both agent templates and agent_registry.json and assert requires_verification is false in both sources and produces == review_verdict (findings returned, not written to a file)."
    covers:
      - CR-100f-4
delivers_to: null
expects_from: null
origin_agent: business-analyst
created: 2026-08-11
amended_by: []
superseded_by: null
covered_by:
  - "unit_tests/test_code_smell_review_wiring.py"
implemented_by:
  - "templates/agents/find-structural-smells.md"
  - "templates/agents/find-design-smells.md"
  - "config/agent_registry.json"
