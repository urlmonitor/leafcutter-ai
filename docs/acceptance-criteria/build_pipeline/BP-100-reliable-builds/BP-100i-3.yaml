id: BP-100i-3
readiness: approved
priority: high
title: "Deployed output parity: every hook in the canonical template also appears in the build output directory"
component: build-pipeline
level: L2
status: active
req_status: active
work_status: todo
created_by: "docs/acceptance-criteria/build_pipeline/BP-100-reliable-builds/BP-100i.yaml"
criteria: |
  Given the canonical template directory contains 5 hook scripts:
    - check_alpha.py
    - check_beta.py
    - check_gamma.py
    - check_delta.py
    - run_hook.py
  And the deployed output directory (where build.py writes commit guardian
    files for the consumer project) contains only 3 of those scripts:
    - check_alpha.py
    - check_beta.py
    - run_hook.py
  (check_gamma.py and check_delta.py were not deployed)
  When the parity check runs at pre-commit time
  Then the commit is blocked with exit code 1
  And the error message lists "check_gamma.py" and "check_delta.py" as scripts
    present in the canonical template but absent from the deployed output
  And the error message names the deployed output directory that was checked
depends_on:
  - BP-100i
doc_links:
  - path: "docs/acceptance-criteria/build_pipeline/BP-100-reliable-builds/BP-100i.yaml"
    relationship: parent
    status: exists
  - path: "docs/build-drift-hook.md"
    relationship: describes
    status: exists
  - path: "docs/pre-commit-hooks.md"
    relationship: describes
    status: exists
  - path: "docs/how-to/managing-pre-commit-hooks.md"
    relationship: describes
    status: exists
  - path: "docs/build-pipeline.md"
    relationship: describes
    status: exists
assigned_agent: python-coder
estimated_complexity: M
it_requirements:
  - "Must exit 0 (fail-open) on unexpected errors such as unreadable directories; must exit 1 only on detected parity violations"
  - "Must complete within 2 seconds for directories with up to 50 hook scripts"
  - "Must be idempotent -- repeated runs with the same directory state produce the same result"
  - "Must determine the deployed output directory path from commit_guardian.json or skills_config.json, not hardcode it"
delivers_to: null
expects_from:
  ac_id: BP-100i-1
  contract: "Reusable directory-scanning function that returns a set[str] of .py filenames found in a given directory, applying the same filename filter (hook script naming pattern) used for runtime-vs-canonical comparison"
origin_agent: business-analyst-v3
created: 2026-06-05
amended_by:
  - it-po-v3
superseded_by: null
covered_by: [BP-100i-3-i]
implemented_by: []
change_target: code
risk_surface: internal
