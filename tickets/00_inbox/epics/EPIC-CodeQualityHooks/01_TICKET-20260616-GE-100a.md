---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  pr-reviewer: needed
  pull-request: needed
  python-coder: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
components:
- guardrail-engine
created: '2026-06-16'
depends_on:
- GE-100
files_touched:
- docs/acceptance-criteria/guardrail-engine/GE-100-code-quality-hooks/GE-100.yaml
- docs/pre-commit-hooks.md
- docs/how-to/managing-pre-commit-hooks.md
priority: high
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: GE-100a
status: todo
title: jscpd hook exits cleanly when the jscpd binary is not installed
---

# jscpd hook exits cleanly when the jscpd binary is not installed

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `GE-100a` — jscpd hook exits cleanly when the jscpd binary is not installed — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `GE-100a`. Component: `guardrail-engine`. Assigned agent: `python-coder`. Estimated complexity: `S`.

## Acceptance Criteria

```gherkin
Given the commit guardian configuration has the duplicate_code section with:
  enabled: true
  strict: false
And the jscpd binary is not present on the system PATH
When the duplicate code hook runs at pre-commit time
Then the hook exits with code 0 (fail-open)
And it emits an advisory message to stderr stating that jscpd was not found
And the advisory message includes guidance on how to install jscpd
And no staged files are blocked from committing
```

## Sign-offs

- [ ] python-coder
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit
- [ ] pull-request

## Comments
