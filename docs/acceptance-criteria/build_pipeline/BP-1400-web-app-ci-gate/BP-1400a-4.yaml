id: BP-1400a-4
components:
  - build_pipeline
title: "Every web-app check reports its result on the pull request and gates the merge"
component: build-pipeline
level: L2
status: active
req_status: draft
work_status: todo
readiness: draft
priority: medium
roadmap_phase: phase_1
criteria: |
  Given the web-app build/type-check, style, and route-render checks defined for
    the repository,
  When a pull request that changes the web app is evaluated,
  Then each web-app check reports its pass/fail result visibly on the pull
    request,
  And a pull request for which any web-app check reports a failing result cannot
    be merged,
  And a pull request merges only once every applicable web-app check has reported
    a passing result.
depends_on:
  - BP-1400a
  - BP-1400a-1
  - BP-1400b-1
  - BP-1400c-1
doc_links:
  - path: docs/acceptance-criteria/build_pipeline/PROJECT_CONTEXT.md
    relationship: describes
    status: exists
  - path: .github/workflows/ci.yml
    relationship: implements
    status: exists
assigned_agent: python-coder
estimated_complexity: S
delivers_to: null
expects_from:
  - ac_id: BP-1400a-1
    contract: "The named, blocking web-app build/type-check result whose visibility and merge-gating this AC asserts"
  - ac_id: BP-1400b-1
    contract: "The named, blocking web-app style result whose visibility and merge-gating this AC asserts"
  - ac_id: BP-1400c-1
    contract: "The named, blocking web-app route-render result whose visibility and merge-gating this AC asserts"
origin_agent: business-analyst
created: 2026-07-21
amended_by: []
superseded_by: null
covered_by: []
implemented_by: []
notes: |
  Cross-cutting enforcement outcome shared by all three web-app checks: the L0
  promise that a change breaking the web app is "caught and stopped before it
  merges, not after". Framed outcome-first — the result is visible on the pull
  request and a failing web-app check gates (blocks) the merge. Whether each
  check is wired as a required (blocking) status check vs. an informational one,
  and the exact branch-protection required-status-check list (mirroring BP-1200c-1
  for the Python gate), is the it-po's technical decision at enrichment; this AC
  fixes the OUTCOME, not the wiring mechanism.
change_target: pipeline
risk_surface: safety
