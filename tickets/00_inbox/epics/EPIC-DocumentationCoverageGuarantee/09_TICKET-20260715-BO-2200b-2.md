---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  llm-expert: needed
  pr-reviewer: needed
  pull-request: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
complexity: medium
components:
- build-orchestration
created: '2026-07-15'
depends_on:
- BO-2200b
- BO-2200b-1
files_touched: []
priority: medium
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: BO-2200b-2
status: todo
title: The verifier fails the ticket when a required documentation file was not changed
---

# The verifier fails the ticket when a required documentation file was not changed

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BO-2200b-2` — The verifier fails the ticket when a required documentation file was not changed — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BO-2200b-2`. Component: `build-orchestration`. Assigned agent: `llm-expert`. Estimated complexity: `M`. Complexity: `medium`.

## Acceptance Criteria

```gherkin
Given a ticket that requires documentation and the documentation-verifier phase runs,
When the ticket's git diff contains NO change to any of the required documentation
  files named in its documentation brief,
Then the verifier reports a failure with `status: blocker`.
And the blocker message names each required documentation file that is missing a
  real change.
And the ticket cannot advance to commit or be reported as complete while the blocker
  stands.
```

## Implementation Notes

```yaml
'Author the documentation-verifier agent template

  (templates/agents/documentation-verifier.md) by cloning the user-surface-smoker.md

  skeleton: tools Bash + Read (no Write/Edit), signoff: true, produces: test_artifact,

  priority 11.9 matching the BO-2200b-1 registry entry. The template instructs the
  agent

  to read the `## Agent Contracts` -> `### documentation-expert` block (BO-2200c)
  for the

  list of required docs, run `git diff` via Bash, and assert a real doc-file change
  exists

  for each required doc. A missing required doc must yield status: blocker (not a
  warning),

  and the blocker message must name each missing file. Fail-closed: an exception or

  ambiguous parse must NOT emit status: ok. The required-docs list comes solely from
  the

  Agent Contracts brief (single source of truth with BO-2200c) — do not maintain a
  second

  list. Mirror the BP-1100 phantom-done posture; where deterministic detection helps,

  reuse scripts/build_placeholder_detection.py rather than re-implementing scanning.

  '
```

## Sign-offs

- [ ] llm-expert
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit
- [ ] pull-request

## Comments
