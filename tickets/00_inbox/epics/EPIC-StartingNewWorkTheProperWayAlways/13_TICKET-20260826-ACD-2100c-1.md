---
ac_coverage: 3/3
ac_traceability:
  id: ACD-2100c-1
  path: docs/acceptance-criteria/ac-driven-dev/ACD-2100-entry-point-unblocked/ACD-2100c-1.yaml
advances_current_outcome: true
agents:
  ac-fulfillment-gate: signed_off
  ac-validator: signed_off
  architect-review: signed_off
  commit: signed_off
  documentation-expert: not_needed
  documentation-verifier: not_needed
  pr-reviewer: signed_off
  pull-request: not_needed
  python-coder: signed_off
  test-runner: signed_off
  test-writer: signed_off
change_target: pipeline
complexity: high
components:
- ac_driven_dev
created: '2026-08-26'
depends_on: []
documentation_required: false
files_touched:
- templates/workflows-js/plan-feature.js
priority: medium
requires_adr: false
requires_diagram: false
risk_surface: safety
roadmap_phase: phase_1
source_ac: ACD-2100c-1
status: todo
title: Every decision the route records as the user's is put to the user and to no
  agent
---

# Every decision the route records as the user's is put to the user and to no agent

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `ACD-2100c-1` — Every decision the route records as the user's is put to the user and to no agent — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `ACD-2100c-1`. Component: `ac-driven-dev`. Assigned agent: `python-coder`. Estimated complexity: `L`. Complexity: `high`.

## Acceptance Criteria

```gherkin
Given a run driven far enough to reach each of the five points at which the route
  asks for a decision -- approving a drafted product-truth artifact; approving
  the mid-pipeline authoring stage; giving final approval; choosing how to
  proceed when the request looks already covered; and deciding what to do with
  stranded drafts from an earlier run,
When each of those points is reached,
Then the question is put on the channel that reaches the person running the
  route,
And the run's own record of every dispatch it made contains no dispatch whose
  purpose was to obtain an answer to any of the five,
And the run does not move past any of the five until an answer has arrived on
  that channel.
```

- [x] AC-1: the question is put on the channel that reaches the person running the
- [x] AC-2: the run's own record of every dispatch it made contains no dispatch whose
- [x] AC-3: the run does not move past any of the five until an answer has arrived on

## Test Requirements

```yaml
tests:
- name: test_each_of_the_five_decision_points_is_put_on_the_user_channel
  file: unit_tests/workflows/test_acd_2100c_1.py
  covers:
  - ACD-2100c-1
  asserts: The run is driven to each of the five decision points in turn and each
    question appears on the channel that reaches the person running the route.
  framework: unittest
  type: integration
  angle: criterion
- name: test_no_dispatch_is_made_to_obtain_an_answer_to_any_of_the_five
  file: unit_tests/workflows/test_acd_2100c_1.py
  covers:
  - ACD-2100c-1
  asserts: Across all five, the run's record of every dispatch it made contains no
    dispatch whose purpose was to obtain a gate answer -- read from the record the
    run produced, not from the workflow source.
  framework: unittest
  type: integration
  angle: reachability
- name: test_run_does_not_advance_past_a_decision_point_before_an_answer_arrives
  file: unit_tests/workflows/test_acd_2100c_1.py
  covers:
  - ACD-2100c-1
  asserts: With the channel open but no answer supplied, no step after the decision
    point executes for the duration of the run.
  framework: unittest
  type: integration
  angle: criterion
- name: test_a_newly_added_human_decision_point_inherits_the_rule
  file: unit_tests/workflows/test_acd_2100c_1.py
  covers:
  - ACD-2100c-1
  asserts: A sixth human-decision point introduced into the run is put on the user
    channel and produces no dispatch, without any per-site registration -- the guard
    is on the class, not on a list of five.
  framework: unittest
  type: integration
  angle: boundary
```

## Implementation Notes

```yaml
- The change must land under templates/. The deployed workflow is build output.
- The class must be closed by one mechanism covering every decision whose answer is
  a human choice -- not by five per-site edits. Two of the five sites appear in no
  known issue, and one of those can delete drafts; per-site patching is how those
  two get missed.
- A new decision point added later must inherit the rule by virtue of being a human-decision
  gate, not by someone remembering to route it. A mechanism that must be opted into
  at each site is the same defect deferred.
- 'The property must be observable from outside the run: the run''s own record of
  what it dispatched is the evidence. A test that asserts a particular agent name
  is absent from the source passes as soon as the dispatch target is built from a
  variable.'
- The route must not proceed past a decision point on a timeout or a default. What
  happens when the channel reaches nobody is ACD-2100c-2's subject and must be reached,
  not bypassed.
- Must satisfy the repository error-handling policy on the channel interaction, which
  is external I/O.
- Must not change what any of the five decisions offers or means -- only who is asked.
  The covered-route and orphan-resolution decisions keep their existing choices.
```

## Implementation Tasks

### test-writer

Landing ACD-2100c-1 removes `resolveGate()`'s live-dispatch attempt entirely
(it now goes straight to `pauseAtGate()` for every gate, on every call,
whenever no valid `args.resume_answer` is present — see
`templates/workflows-js/plan-feature.js`'s `resolveGate()`) and adds a
`question` object to `pauseAtGate()`'s returned/persisted payload. Three
SIBLING tests were written against the exact mechanism this AC closes — a
live, direct answer to the gate-answer dispatch (never via
`args.resume_answer`) being honoured as a real decision — and now fail as a
direct, foreseeable consequence. This is not a defect in this
implementation: it is the exact behaviour ACD-2100c-1's own red_baseline
(test-writer's 2026-09-07 15:10 sign-off comment, this ticket) requires —
"the run does not move past any of the five until an answer has arrived on
[the resume] channel", which these three tests' own scenarios directly
contradict by construction. Confirmed by direct execution: all three were
green before this ticket's changes (verified via `git stash` on
`templates/workflows-js/plan-feature.js` + rerun) and are red after, with no
other test in either file affected (21 passed / 2 failed in the combined
`test_bo_2300a_2_cancel_status_distinct.py` + `test_bo_2300_pause_resume.py`
run; 2 passed / 1 failed in `test_bo_2300a_1_refusal_not_decision.py`).

Classification (Source-of-Truth Discipline Rule 1): **test_drift** for all
three — production is correct per ACD-2100c-1's own signed-off AC and
red_baseline; the tests assert the pre-ACD-2100c-1 contract, which this AC
explicitly and intentionally supersedes.

- [ ] `unit_tests/workflows/test_bo_2300a_1_refusal_not_decision.py::test_ac1_genuine_approve_answer_still_accepted`
      (1 failing) — asserts a genuine, non-refusal `{"action": "approve"}`
      answered DIRECTLY at the `final-gate` label dispatches
      `apply-approval`. Under ACD-2100c-1 no direct label answer is ever
      consulted, genuine or not (the whole point: the run cannot tell a
      human's direct reply from an agent's), so the run now always pauses
      instead. Rewrite to drive the decision through `args={"resume_answer":
      {"gate_id": "final-gate", "action": "approve"}}` (the only channel that
      can still resolve a gate) and assert `apply-approval` is dispatched via
      THAT path, or repoint this test to assert the new pause behaviour
      (mirroring `unit_tests/workflows/test_acd_2100c_1.py`'s own
      `test_run_does_not_advance_past_a_decision_point_before_an_answer_arrives`
      scenario for `final_gate`).
- [ ] `unit_tests/workflows/test_bo_2300a_2_cancel_status_distinct.py::test_ac2_pt_gate_cancel_status_is_distinct_from_ok`
      (1 failing) — answers `pt-gate-mockdata` directly with `{"action":
      "cancel"}` and expects terminal `status: "cancelled"`. Under
      ACD-2100c-1 the direct answer is never consulted, so the run pauses
      instead (observed status: `pause_persist_failed`, because this older
      test's harness call does not stub `pause-persist-verify` the way
      `unit_tests/workflows/test_acd_2100c_1.py`'s `_PERSIST_VERIFY_OK`
      fixture does — add that same override). Rewrite to drive the cancel
      decision via `args={"resume_answer": {"gate_id": "pt-gate-mockdata",
      "action": "cancel"}}` plus the `_PERSIST_VERIFY_OK`-style stub, then
      re-assert `status == "cancelled"` on that resumed run — the
      distinct-status behaviour BO-2300a-2 covers is unaffected by
      ACD-2100c-1 and should still hold once reached via the resume channel.
- [ ] `unit_tests/workflows/test_bo_2300_pause_resume.py::test_paused_state_distinct_from_cancelled`
      (1 failing) — feeds `_EXPLICIT_CANCEL_RESPONSES` (direct label
      answers) and asserts the run does NOT dispatch `pause-persist`. Under
      ACD-2100c-1 every run without a valid `args.resume_answer` dispatches
      `pause-persist` at whichever gate it reaches — including one "answered"
      directly — so this assertion is now categorically false. Rewrite the
      "cancelled" half of this test to supply a resume-channel answer (e.g.
      `args={"resume_answer": {"gate_id": "final-gate", "action":
      "cancel"}}`) and assert NO pause-persist dispatch occurs on THAT run,
      preserving the paused-vs-cancelled distinction this test exists to
      cover.

None of the above are within this ticket's `files_touched`, and `python-coder`
is prohibited from editing test files (Test Delegation policy) — hence the
handoff.

## Agent Contracts

### documentation-expert

Existing docs to update / cross-link:

- docs/architecture/diagrams/c3-006-plan-feature-decision-gate-sequence.md (relationship: describes | status: planned | relevance: The sequence diagram authored by ACD-2100e-1 -- the five decision points and the channel each is put on are what it depicts.)
- docs/known-issues/ac-driven-dev.md (relationship: describes | status: exists | relevance: KI-ACD-005 -- the observed run in which a status-checker agent answered a user gate and the pipeline discarded the work.)
- templates/workflows-js/plan-feature.js (relationship: implements | status: exists | relevance: Canonical source. The five decision points are at lines 2067 (product-truth gate), 2361 (mid-pipeline gate), 2458 (final gate), 1913 (covered-route gate) and 841 (orphan resolution); all five dispatch to status-checker today.)
- docs/architecture/adrs/ADR-024-interactive-pause-resume.md (relationship: describes | status: exists | relevance: The pause and resume contract that gives a gate somewhere to go when no person is reachable.)
- docs/architecture/components/interactive-pause-resume-substrate.md (relationship: describes | status: exists | relevance: Component doc for the substrate a gate falls back to when the channel reaches nobody.)
- docs/architecture/components/ac-driven-dev.md (relationship: describes | status: exists | relevance: Component doc for the route these five decision points belong to.)

This record owns none of the documents above -- every entry is a cross-link to a
document another record produces. The two contract lines that stood here both
demanded `docs/architecture/diagrams/c3-006-plan-feature-decision-gate-sequence.md`,
which ACD-2100e-1 creates; this ticket could never have satisfied them. (Both
lines named the same path with different genres, which is its own generator
defect -- the target is computed once from the first doc_link and reused for
every genre.) Removed per BO-2200c-5-i and -iii; the list above is retained as
reading for the implementer.

Note the `### Delivers To` entry below addressed to architecture-diagram-author:
this record SUPPLIES the decision points that diagram depicts. That is a
contract with a downstream record, not a demand that this ticket draw it.

### Delivers To

- **Agent:** python-coder
- **Contract:** A decision-point mechanism in which an answer carries the channel it arrived on, so that provenance is a property of the answer the acceptance test can read -- rather than something to be inferred from the answer's content.
- **Agent:** architecture-diagram-author
- **Contract:** The final set of decision points and the channel each is put on, which the sequence diagram depicts rather than re-derives from the source.

## Sign-offs

- [x] architect-review — 2026-09-07 14:32
- [x] test-writer — 2026-09-07 15:10
- [x] python-coder — 2026-09-07 20:31
- [x] test-runner — 2026-09-08 09:25
- [x] pr-reviewer — 2026-09-08 09:30
- [x] ac-validator — 2026-09-08 10:05
- [x] ac-fulfillment-gate — 2026-09-08 09:39
- [x] commit — 2026-09-08 10:30

## Comments

### 2026-09-07 14:32 — architect-review (status: ok)
feedback-id: fb_2026-09-07_165d21be
completion_manifest:
  blast_radius_reviewed: true
  rubric_applied: true
  escalation_decision_recorded: true
Rubric applied directly (research-agent's Agent-tool spawn path was unavailable in
this invocation, so I ran the equivalent grep-based blast-radius check myself via
Bash/Read on `templates/workflows-js/plan-feature.js`): 1 file touched, 1 component
(`ac_driven_dev`), no cross-module boundary crossed, and none of the four
always-large triggers fire (no Alembic migration, no hypertable change, no FastAPI
surface change, no ADR-* file edited — ADR-024 is read/composed with, not modified).
Classified **small**; escalation: none. `requires_adr` confirmed `false` per the
mandatory field rule (no HIGH-impact trigger, only 1 component touched), consistent
with the ticket's existing frontmatter. Full architectural note, acceptance
adjustments, and escalation rationale recorded in this sign-off's structured output
to the supervisor.

### 2026-09-07 15:10 — test-writer (status: ok)
feedback-id: (submit-failed)
completion_manifest:
  tests_written: true
  red_baseline_verified: true
  cross_layer_seam_answer:
    result: not_applicable
    reason: "This ticket's scope is confined to plan-feature.js's OWN five gate-answering call sites and the shared resolveGate()/pauseAtGate() mechanism they call -- a single script's internal control flow, not a boundary between two independently-deployed layers. Every test drives the REAL, on-disk plan-feature.js through the real workflow-runner entry point (run_workflow_under_e2), which is itself the producer-to-consumer path for this AC's own dispatch-record and terminal-payload evidence; there is no separate, already-existing consumer module outside this ticket's own files_touched to pipe real output into. The consuming skill/CLI side that would read the terminal payload's new `question` field is out of scope for this ticket (files_touched: templates/workflows-js/plan-feature.js only) and is not touched by this change."
    remediation: "n/a -- not_applicable is a first-class outcome, not a defect."
  reachability_entry_point_answer:
    result: resolved
    entry_point: "the plan-feature workflow driven through the workflow runner to each decision point, with the run's own dispatch record read back afterwards"
Wrote unit_tests/workflows/test_acd_2100c_1.py with 4 test functions (one per
`## Test Requirements` entry, tagged `# covers: ACD-2100c-1` and their declared
`# angle:`), covering the five named decision points (resolve-orphans-choice,
covered-route-gate, pt-gate-mockdata, gate-po, final-gate) via
`run_workflow_under_e2()` driving the real, on-disk plan-feature.js, plus a
fourth boundary test that extracts the real `resolveGate()`/`pauseAtGate()`
machinery verbatim (brace-counted from the on-disk source) and drives it
directly with a synthetic sixth gate id to prove the guard must live in the
shared mechanism, not in five per-site edits. Verification run: `AC_ENFORCE_STRICT=1
python3 -m pytest unit_tests/workflows/test_acd_2100c_1.py -q` — 16 failed
(all subTests + the boundary test), 3 passed (the outer subTest-container test
functions, per pytest-subtests' own reporting convention; every individual
subTest assertion is captured in the red_baseline below). Without
`AC_ENFORCE_STRICT=1` this repo's `pytest_ac_enforcement` plugin downgrades
these same failures to xfail (AC `ACD-2100c-1` `work_status: todo`) and the
suite exits 0 — this is the documented, intentional masking behaviour for
not-yet-done ACs, not a false green; `AC_ENFORCE_STRICT=1` is what shows the
true red state python-coder must turn green.
red_baseline:
  - test_name: test_each_of_the_five_decision_points_is_put_on_the_user_channel (scenario=orphan_resolution)
    file: unit_tests/workflows/test_acd_2100c_1.py
    error: "AssertionError: None != 'resolve-orphans-choice' : [orphan_resolution] Terminal payload does not name the decision point it is awaiting an answer for. terminal={'status': 'error', 'message': 'Uncommitted AC files must be resolved first. Re-run /plan-feature after committing or discarding them.'}"
  - test_name: test_each_of_the_five_decision_points_is_put_on_the_user_channel (scenario=covered_route)
    file: unit_tests/workflows/test_acd_2100c_1.py
    error: "AssertionError: None is not an instance of <class 'dict'> : [covered_route] Terminal payload for 'covered-route-gate' carries no 'question' object. terminal={'status': 'paused_awaiting_input', 'run_id': 'default-run', 'gate_id': 'covered-route-gate'}"
  - test_name: test_each_of_the_five_decision_points_is_put_on_the_user_channel (scenario=product_truth_gate)
    file: unit_tests/workflows/test_acd_2100c_1.py
    error: "AssertionError: None is not an instance of <class 'dict'> : [product_truth_gate] Terminal payload for 'pt-gate-mockdata' carries no 'question' object. terminal={'status': 'paused_awaiting_input', 'run_id': 'default-run', 'gate_id': 'pt-gate-mockdata'}"
  - test_name: test_each_of_the_five_decision_points_is_put_on_the_user_channel (scenario=mid_pipeline_gate)
    file: unit_tests/workflows/test_acd_2100c_1.py
    error: "AssertionError: None is not an instance of <class 'dict'> : [mid_pipeline_gate] Terminal payload for 'gate-po' carries no 'question' object. terminal={'status': 'paused_awaiting_input', 'run_id': 'default-run', 'gate_id': 'gate-po'}"
  - test_name: test_each_of_the_five_decision_points_is_put_on_the_user_channel (scenario=final_gate)
    file: unit_tests/workflows/test_acd_2100c_1.py
    error: "AssertionError: None is not an instance of <class 'dict'> : [final_gate] Terminal payload for 'final-gate' carries no 'question' object. terminal={'status': 'paused_awaiting_input', 'run_id': 'default-run', 'gate_id': 'final-gate'}"
  - test_name: test_no_dispatch_is_made_to_obtain_an_answer_to_any_of_the_five (scenario=orphan_resolution)
    file: unit_tests/workflows/test_acd_2100c_1.py
    error: "AssertionError: 'resolve-orphans-choice' unexpectedly found in [...dispatched labels including 'resolve-orphans-choice']"
  - test_name: test_no_dispatch_is_made_to_obtain_an_answer_to_any_of_the_five (scenario=covered_route)
    file: unit_tests/workflows/test_acd_2100c_1.py
    error: "AssertionError: 'covered-route-gate' unexpectedly found in [...dispatched labels including 'covered-route-gate']"
  - test_name: test_no_dispatch_is_made_to_obtain_an_answer_to_any_of_the_five (scenario=product_truth_gate)
    file: unit_tests/workflows/test_acd_2100c_1.py
    error: "AssertionError: 'pt-gate-mockdata' unexpectedly found in [...dispatched labels including 'pt-gate-mockdata']"
  - test_name: test_no_dispatch_is_made_to_obtain_an_answer_to_any_of_the_five (scenario=mid_pipeline_gate)
    file: unit_tests/workflows/test_acd_2100c_1.py
    error: "AssertionError: 'gate-po' unexpectedly found in [...dispatched labels including 'gate-po']"
  - test_name: test_no_dispatch_is_made_to_obtain_an_answer_to_any_of_the_five (scenario=final_gate)
    file: unit_tests/workflows/test_acd_2100c_1.py
    error: "AssertionError: 'final-gate' unexpectedly found in [...dispatched labels including 'final-gate']"
  - test_name: test_run_does_not_advance_past_a_decision_point_before_an_answer_arrives (scenario=orphan_resolution)
    file: unit_tests/workflows/test_acd_2100c_1.py
    error: "AssertionError: 'final-gate' unexpectedly found in [...] : [orphan_resolution] The run advanced past the 'resolve-orphans-choice' decision point (dispatched 'final-gate') even though no answer ever arrived via args.resume_answer"
  - test_name: test_run_does_not_advance_past_a_decision_point_before_an_answer_arrives (scenario=covered_route)
    file: unit_tests/workflows/test_acd_2100c_1.py
    error: "AssertionError: 'ok' == 'ok' : [covered_route] The 'covered-route-gate' decision resolved as though a real decision had arrived on the channel, but no args.resume_answer was ever supplied. terminal={'status': 'ok', 'message': 'Request is already covered by: FOO-1. No new ACs created.', 'covered_by': ['FOO-1']}"
  - test_name: test_run_does_not_advance_past_a_decision_point_before_an_answer_arrives (scenario=product_truth_gate)
    file: unit_tests/workflows/test_acd_2100c_1.py
    error: "AssertionError: 'branch-check' unexpectedly found in [...] : [product_truth_gate] The run advanced past the 'pt-gate-mockdata' decision point (dispatched 'branch-check')"
  - test_name: test_run_does_not_advance_past_a_decision_point_before_an_answer_arrives (scenario=mid_pipeline_gate)
    file: unit_tests/workflows/test_acd_2100c_1.py
    error: "AssertionError: 'branch-check' unexpectedly found in [...] : [mid_pipeline_gate] The run advanced past the 'gate-po' decision point (dispatched 'branch-check')"
  - test_name: test_run_does_not_advance_past_a_decision_point_before_an_answer_arrives (scenario=final_gate)
    file: unit_tests/workflows/test_acd_2100c_1.py
    error: "AssertionError: 'apply-approval' unexpectedly found in [...] : [final_gate] The run advanced past the 'final-gate' decision point (dispatched 'apply-approval')"
  - test_name: test_a_newly_added_human_decision_point_inherits_the_rule
    file: unit_tests/workflows/test_acd_2100c_1.py
    error: "AssertionError: Lists differ: [{'prompt': 'Ask the user something new...', 'opts': {'agentType': 'status-checker', 'label': 'a-newly-added-decision-gate'}}] != [] : resolveGate() dispatched an agent() call whose purpose was to obtain an answer for a decision point it has never seen before ('a-newly-added-decision-gate')"

### 2026-09-07 20:31 — python-coder (status: handoff)
feedback-id: fb_2026-09-07_10e795ad
completion_manifest:
  red_baseline_all_green: true
  research_agent_consumers_check: true
  sibling_regressions_classified: true
  contract_shrinkage_handoff_recorded: true
`resolveGate()` (templates/workflows-js/plan-feature.js) no longer calls
`liveGateFn` at all — the parameter is still accepted (every one of the five
existing call sites, plus any future one, still builds and passes a
liveGateFn closure, so the guard lives on this ONE function rather than on a
per-site opt-out) but it is never invoked; on a headless/direct-answer path
resolveGate() now goes straight to `pauseAtGate()`. `pauseAtGate()`'s
returned/persisted payload now carries the `question` object (`type`,
`options`, `prompt`) it already built internally, so the run's own terminal
return value — the only channel back to the caller in this sandboxed E2 body
— actually names what is being asked. The one site with no pre-existing
resolveGate()/pauseAtGate() wrapping (`resolveOrphanedDrafts`, site 1) was
restructured: the caller now resolves the "resolve-orphans-choice" decision
through `resolveGate()` before calling `resolveOrphanedDrafts()`, which was
changed to take an already-obtained `userChoice` parameter instead of
dispatching its own agent() call to ask. All 4 of this ticket's own tests in
unit_tests/workflows/test_acd_2100c_1.py pass (16 subtests + the boundary
test, all transitioned red-to-green per the red_baseline above; verified
under `AC_ENFORCE_STRICT=1`).

Ran the full `unit_tests/workflows/` suite before and after (via `git stash`
on the one touched file + rerun, mirroring ACD-2100b-5's own precedent) to
check for consumer regressions per the Contract-Shrinkage Guard. Found and
classified 3 (Source-of-Truth Discipline Rule 1, **classification:
test_drift** — production is correct per this AC's own signed-off
red_baseline; the tests assert the pre-ACD-2100c-1 contract this AC
intentionally supersedes): `test_bo_2300a_1_refusal_not_decision.py
::test_ac1_genuine_approve_answer_still_accepted`,
`test_bo_2300a_2_cancel_status_distinct.py
::test_ac2_pt_gate_cancel_status_is_distinct_from_ok`, and
`test_bo_2300_pause_resume.py::test_paused_state_distinct_from_cancelled` —
all three assert that a DIRECT (non-resume-channel) live-gate answer is
honoured as a real decision, which is exactly the loophole ACD-2100c-1 closes
(no live-gate answer, genuine or refused, may ever resolve a gate again —
only `args.resume_answer` can). No other test in either file is affected (21
passed / 2 failed combined in the first two files; 2 passed / 1 failed in the
third). Per Test Delegation policy, `python-coder` cannot edit test files;
see the new `### test-writer` section under `## Implementation Tasks` for the
full per-test remediation. Handing off to `test-writer`.

### 2026-09-08 09:25 — test-runner (status: ok)
feedback-id: fb_2026-09-08_ce01b581
completion_manifest:
  test_suite_executed: true
  all_tests_passing: true
  failure_report_structured: true
By the time this phase ran, the `### test-writer` handoff task list above was
already resolved (committed in `e3aa88c2a`, alongside the `plan-feature.js`
change) — `test_bo_2300_pause_resume.py`, `test_bo_2300a_1_refusal_not_decision.py`,
and `test_bo_2300a_2_cancel_status_distinct.py` all now drive their decisions
via `args.resume_answer` per the remediation notes. Ran (all under
`AC_ENFORCE_STRICT=1` to unmask true red/green per this repo's
`pytest_ac_enforcement` convention): (1) this ticket's own
`unit_tests/workflows/test_acd_2100c_1.py` — 4 passed, 15 subtests passed;
(2) the three rewritten sibling files together — 26 passed; (3) the full
`unit_tests/workflows/` suite as a regression check — 22 failed, 592 passed,
40 subtests passed, an EXACT match to the pre-existing baseline python-coder's
own before/after `git stash` comparison recorded in its 20:31 sign-off comment
(ACD-2100a-{1,3,4,5}, one ACD-2100b-4 subtest set, and BO-1500f-1* — all
unrelated to ACD-2100c-1 and out of this ticket's scope); (4) the sibling
`unit_tests/ac_driven_dev/` suite — 43 passed, 4 subtests passed, matching its
own reported baseline. No new regressions introduced by this ticket's change.

### 2026-09-08 09:30 — pr-reviewer (status: ok)
feedback-id: fb_2026-09-08_de47b704
completion_manifest:
  diff_reviewed: true
  behavioral_spotcheck_run: true
  contract_delivers_to_checked: true
  high_confidence_blockers: false
Reviewed `templates/workflows-js/plan-feature.js`'s actual diff (commit
`e3aa88c2a` vs its parent) rather than trusting prior sign-offs alone.
Confirmed all five decision points (`resolve-orphans-choice`,
`covered-route-gate`, `pt-gate-mockdata`, `gate-po`, `final-gate`) now route
through the single `resolveGate()`/`pauseAtGate()` mechanism, which never
invokes `liveGateFn` and always falls through to a pause carrying a
`question` object in the terminal payload — satisfying AC-1..AC-3 and the
"closed by one mechanism, not five per-site edits" implementation note. Spot-
ran `unit_tests/workflows/test_acd_2100c_1.py` (4 passed / 15 subtests) and
the three rewritten sibling files (26 passed) directly rather than trusting
the prior sign-offs' reported numbers — both match. One medium-confidence
finding, not a blocker: `isAgentRefusal()` and `AGENT_REFUSAL_MARKERS`
(plan-feature.js, formerly the BO-2300a-1/KI-ACD-005 refusal-detection
mechanism) are no longer called by any production code path after this
change — the only remaining reference is the new test's own
`_assemble_gate_machinery_source()` helper, which extracts them into a
standalone Node driver but never exercises their branch. This is dead code,
not a correctness bug (resolveGate's new unconditional pause-on-no-
resume-answer behavior makes the refusal/genuine distinction moot), and
there is no JS lint gate under `templates/workflows-js/` that would catch it
mechanically, so it will not surface again until someone reads the function
body. Recommend a follow-up ticket (or a note in KI-ACD-005/KI-ACD-019) to
either delete the now-unreachable function+constant or explicitly document
why they are being kept. Checked the `### Delivers To` contract lines
(python-coder: "an answer carries the channel it arrived on"; architecture-
diagram-author: decision points + channel) — the python-coder contract is
satisfied trivially by design (only one channel, `args.resume_answer`, can
now resolve any gate, so there is no provenance ambiguity left to encode);
the architecture-diagram-author contract is out of scope for this ticket
(downstream, ACD-2100e-1). No high-confidence findings; no ruff/type
concerns (JS file, no Python touched by this ticket). Signing off.

### 2026-09-08 10:05 — ac-validator (status: ok)
feedback-id: (submit-failed)
completion_manifest:
  acs_parsed: true
  implementation_evidence_found: true
  test_evidence_found: true
  ac_store_alignment_checked: true
`submit_feedback.py` exited 1: "Phase 'ac-validator' is not an allowed writer
for category 'complete'" (config/feedback_categories.yaml has no
`ac-validator` entry in any category's `allowed_writers`) — recording
`(submit-failed)` per §2a and continuing, not a phase failure.

Parsed 3 ACs from `## Acceptance Criteria` (AC-1..AC-3). Evidence gathered
from the already-committed diff at `e3aa88c2a` (`git show e3aa88c2a --stat`
lists `templates/workflows-js/plan-feature.js` and
`unit_tests/workflows/test_acd_2100c_1.py` together) since both implementation
and tests for this ticket landed in that commit before this phase ran; no
separate staged diff exists on top of it (only this ticket file's own
sign-off edits are currently staged).

AC-1 (question put on the user's channel) covered:
  implementation: `templates/workflows-js/plan-feature.js` — `pauseAtGate()`
    now returns `{ status: "paused_awaiting_input", run_id, gate_id, question }`
    (diff hunk adding `question: question` to the return statement, e3aa88c2a).
  test: `test_each_of_the_five_decision_points_is_put_on_the_user_channel`
    (`unit_tests/workflows/test_acd_2100c_1.py:245`, `# covers: ACD-2100c-1`,
    `# angle: criterion`) — asserts a `question` dict is present in the
    terminal payload for all five gate ids. Verified green: `AC_ENFORCE_STRICT=1
    python3 -m pytest unit_tests/workflows/test_acd_2100c_1.py -q` → `4 passed,
    15 subtests passed`.

AC-2 (run's dispatch record contains no dispatch to obtain a gate answer)
covered:
  implementation: `templates/workflows-js/plan-feature.js` — `resolveGate()`'s
    `void liveGateFn;` line (the closure built by every one of the five call
    sites is deliberately never invoked; diff hunk replacing the old
    `if (typeof liveGateFn === "function") { gateAnswer = await liveGateFn(); }`
    block, e3aa88c2a).
  test: `test_no_dispatch_is_made_to_obtain_an_answer_to_any_of_the_five`
    (`unit_tests/workflows/test_acd_2100c_1.py:323`, `# angle: reachability`)
    — asserts each gate id is absent from the run's own dispatched-label
    record. Same pytest run as above: passing.

AC-3 (run does not move past a decision point until an answer arrives)
covered:
  implementation: same `resolveGate()` change as AC-2 — with `liveGateFn`
    unread, the only path back is `return pauseAtGate(...)`, so no gate
    resolves without a matching `args.resume_answer`.
  test: `test_run_does_not_advance_past_a_decision_point_before_an_answer_arrives`
    (`unit_tests/workflows/test_acd_2100c_1.py:361`, `# angle: criterion`) —
    drives each of the five gates with the channel open but no
    `resume_answer` and asserts no post-gate dispatch occurred. Same pytest
    run as above: passing.

A fourth test, `test_a_newly_added_human_decision_point_inherits_the_rule`
(`unit_tests/workflows/test_acd_2100c_1.py:591`, `# angle: boundary`),
additionally proves the guard lives on the shared `resolveGate()` mechanism
rather than on a per-site list of five, per the ticket's Implementation Notes
— also green in the same run.

AC store alignment: ran
`python3 scripts/commit_guardian/check_v2_ac_store_alignment.py --ticket
<this ticket>` — exit 0, no violations.

Cross-file contract trace (Step 2d): the ticket's `### Delivers To` section
names two downstream contracts (python-coder: "an answer carries the channel
it arrived on"; architecture-diagram-author: decision points + channel) but
neither is an AC-level `delivers_to`/`expects_from` field on AC-1..AC-3
themselves, and pr-reviewer's 2026-09-08 09:30 sign-off already traced both:
the python-coder contract is satisfied trivially (only `args.resume_answer`
can resolve any gate now, so there is no provenance ambiguity left to
encode) and the architecture-diagram-author contract is explicitly out of
scope for this ticket (downstream, ACD-2100e-1, which does not yet exist as
a file to trace against). No contract gap found.

All 3 ACs classified **covered** (implementation + test evidence both
found for each). Flipped AC-1..AC-3 checkboxes to `[x]` in
`## Acceptance Criteria` and set `ac_coverage: 3/3` in frontmatter. Commit
phase may proceed.

### 2026-09-08 09:39 — ac-fulfillment-gate (status: ok)
feedback-id: (submit-failed)
completion_manifest:
  ac_yaml_loaded: true
  work_status_verified: true
  implemented_by_verified: true
  covered_by_verified: true
  schema_validated_after_autofix: true
`ac_traceability` resolved via the shared resolver
(`.leafcutter/scripts/ac_store/ac_coverage_resolver.py`, two-key `{id, path}`
form) to a single AC, `ACD-2100c-1` (level L2), at
`docs/acceptance-criteria/ac-driven-dev/ACD-2100-entry-point-unblocked/ACD-2100c-1.yaml`.
Initial resolver run reported `ok: false` with two failures: `work_status`
was `todo` despite the ticket's own commit `e3aa88c2a` already landing
`templates/workflows-js/plan-feature.js` (present in both `git diff
main...HEAD --name-only` and the ticket's `files_touched`), and `covered_by`
was `[]`. Auto-fixed per BO-202 (append-only): set `work_status: done`
(files_touched ∩ diff non-empty); appended
`templates/workflows-js/plan-feature.js` to `implemented_by` (it was absent —
the list previously held only the ticket's own path, which does not
intersect `files_touched`); searched `unit_tests/` for `# covers:
ACD-2100c-1` and found 4 matching tags in
`unit_tests/workflows/test_acd_2100c_1.py` (lines 246, 324, 362, 592),
appended that file path to `covered_by`. Ran
`scripts/commit_guardian/check_ac_schema.py` on the edited YAML — exit 0
(fell back to manual field validation; `config/ac_store_schema.json` not
found, a pre-install-worktree condition the instructions say to tolerate).
Re-ran the resolver: `ok: true`, `failures: []`. `resolved_acs` is non-empty
and the sole AC is now `passed` — sign-off is `ok` per the "All passed, and
at least one AC was resolved" rule. `submit_feedback.py` exited 1
("`ac-fulfillment-gate` is not an allowed writer for category `complete`" —
same gap ac-validator hit above); recording `(submit-failed)` and continuing
per §2a.

### 2026-09-08 10:30 — commit (status: ok)
feedback-id: fb_2026-09-08_be56b7e5
Auto-authorized commit gate: subject "chore(ac-driven-dev): close out ACD-2100c-1 sign-offs and AC store links"; staged files: docs/acceptance-criteria/ac-driven-dev/ACD-2100-entry-point-unblocked/ACD-2100c-1.yaml, tickets/00_inbox/epics/EPIC-StartingNewWorkTheProperWayAlways/13_TICKET-20260826-ACD-2100c-1.md. Pre-commit hook probe (`scripts/commit_guardian/verify_precommit_active.py --json`, run with cwd at the worktree root) returned `failing_checks: []`; the underlying `templates/workflows-js/plan-feature.js` implementation and its tests were already committed at `e3aa88c2a` by an earlier phase in this drive, so this commit carries only the ticket/AC-store sign-off bookkeeping (test-runner, pr-reviewer, ac-validator, and ac-fulfillment-gate sign-offs; `work_status: done`; `covered_by`/`implemented_by` backfill) that those phases produced.
