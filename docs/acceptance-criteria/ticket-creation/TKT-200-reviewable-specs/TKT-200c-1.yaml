id: TKT-200c-1
components:
  - ticket_creation_pipeline
title: "Review package assembles specs, diagrams, and docs into a single location"
component: ticket-creation
level: L2
status: active
req_status: active
work_status: todo
criteria: |
  Given a feature folder contains:
    - 3 L2 AC YAML files (the specs),
    - 1 component-relationship diagram (generated by TKT-200a),
    - 2 boundary documents (generated by TKT-200b),
  When the review package assembly step runs,
  Then all 6 artifacts are listed in a single manifest
    that a reviewer can open to see the complete set,
  And the manifest includes a hyperlink or path to each artifact,
  And no artifact referenced in the manifest is missing from the file system.
depends_on: [TKT-200c, TKT-200a, TKT-200b]
doc_links:
  - path: docs/architecture/components/ticket-creation.md
    relationship: describes
    status: planned
assigned_agent: llm-expert
estimated_complexity: M
it_requirements:
  - "Must validate that every artifact referenced in the manifest exists on disk before finalizing"
  - "Must use relative paths from the feature folder root for portability"
  - "Must produce a deterministic manifest (same inputs produce identical output)"
delivers_to:
  agent: llm-expert
  contract: "Review manifest file listing all spec, diagram, and boundary-doc artifacts with relative paths"
expects_from:
  ac_id: TKT-200a-1
  contract: "Generated architecture diagram file(s) in the feature folder"
origin_agent: business-analyst-v3
created: 2026-06-05
amended_by: []
superseded_by: null
covered_by: []
implemented_by: []
change_target: prompt
risk_surface: contract_boundary
