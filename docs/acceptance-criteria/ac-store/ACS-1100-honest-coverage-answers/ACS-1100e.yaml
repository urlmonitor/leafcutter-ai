id: ACS-1100e
components:
  - ac_store
title: "The coverage contract is written down in one place a tool author will find"
component: ac-store
level: L1
status: active
req_status: draft
work_status: todo
readiness: draft
priority: medium
roadmap_phase: phase_1
criteria: |
  One reference section explains the whole contract, not four scattered fragments.

  Everything a reader needs in order to produce or consume an honest answer — what an
  answer carries, how its total was arrived at, what may be left out and on what
  grounds, what happens when the numbers do not add up, and what a downstream verdict
  may claim — is written in a single place in the store's reference documentation, so
  a tool written a year from now inherits the whole contract rather than rediscovering
  a fraction of it.
depends_on:
  - ACS-1100
doc_links:
  - path: docs/reference/ac-schema.md
    relationship: specifies
    status: exists
    relevance: "The single existing store reference document that all four coverage sections belong in."
  - path: docs/acceptance-criteria/knowledge-management/KM-ADM-005.yaml
    relationship: related
    status: exists
    relevance: "The worked example the reference section must cite: a real, shipped, self-describing answer produced BEFORE the contract was written down. Its vocabulary (scope / measurement date / enforcing test / explicit do-not-read-as) is the starting point, not a competitor."
assigned_agent: null
estimated_complexity: null
delivers_to: null
expects_from: null
origin_agent: BrainCandy
created: 2026-08-13
amended_by: []
superseded_by: null
covered_by:
  - ACS-1100e-1
implemented_by: []
documentation_triggers:
  - reference-doc
change_target: docs
risk_surface: internal
notes: |
  WHY THIS L1 EXISTS (added 2026-08-13 in response to expert review).

  The first authoring pass put a reference-doc AC under each of ACS-1100a, b, c and d
  (ACS-1100a-4, ACS-1100b-3, ACS-1100c-3, ACS-1100d-3). All four wrote sections of the
  SAME file, docs/reference/ac-schema.md. The build's file-touch parallelism gate would
  have serialised them into four sequential tickets editing one file — a guaranteed
  conflict — and they accounted for 5 of 15 leaves (33%) of the whole tree. They are now
  one AC, ACS-1100e-1.

  WHY A SIBLING L1 RATHER THAN A CHILD OF a/b/c/d. The consolidated section documents
  contracts owned by all four behavioural L1s. Hanging it under any one of them would
  (i) give a child prerequisites living outside its own parent's subtree — its
  depends_on would reach into three sibling L1s' children — and (ii) gate one L1's
  completion on documentation of behaviour that L1 does not own. A sibling L1 whose
  single outcome is "the contract is written down" is the honest home. This takes the
  L0 to five L1s (cap 7) and leaves every behavioural L1 at three children or fewer
  (cap 5).

  ACS-1100a, b and c consequently carry documentation_triggers: [] plus a
  documentation_rationale naming this L1. ACS-1100d keeps [component-diagram], which is
  a different artifact by a different agent (ACS-1100d-4) and is deliberately NOT
  folded in here.

  THE CONTRACT NOW HAS A PRIOR INSTANCE TO ACCOUNT FOR (2026-08-18, PO). This L1 is
  the one the KM-ADM-005 reconciliation makes MORE work, not less. Two additions for
  ACS-1100e-1's scope, neither of them optional:
    1. CITE THE WORKED EXAMPLE. A shipped, self-describing answer already exists
       (the artifact map's test-covers edge note). The reference section must present
       it as an instance of the contract's vocabulary, so the next author copies a
       real example rather than inventing a fifth phrasing. If the contract's chosen
       vocabulary cannot express that note, the vocabulary is wrong.
    2. STATE WHO INHERITS AND WHO OWNS. ACS-1100 is scope: standing — other components
       inherit it and do not re-specify it. Three trees already relate to it and a
       reader currently has to reconstruct how: KM-200 (inherits it for artifact
       census figures), KM-ADM-100 (inherits it for map trust ratings, and shipped the
       first instance), TQ-400 (adjacent — proof over time, not answer coverage; its
       TQ-400d owns retiring the untagged-done pile). The reference section is the
       one place that can say this once instead of five AC notes saying it partially.
