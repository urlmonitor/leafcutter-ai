---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  pr-reviewer: needed
  pull-request: needed
  python-coder: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
components:
- testing_quality
created: '2026-06-24'
depends_on:
- TQ-100e-1
files_touched:
- scripts/ac_store/test_enforcement.py
- tests/testing_quality/test_enforcement_mode.py
priority: high
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: TQ-100e-1-i
status: todo
title: Report-only mode surfaces results but never fails the run
---

# Report-only mode surfaces results but never fails the run

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `TQ-100e-1-i` — Report-only mode surfaces results but never fails the run — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `TQ-100e-1-i`. Component: `testing-quality`. Assigned agent: `python-coder`. Estimated complexity: `S`.

## Acceptance Criteria

```gherkin
Given the enforcement mode is configured to report-only and the suite contains
  at least one failing test that would otherwise block (for example a failing
  test covering a done AC),
When the suite is run,
Then every test result (including the failures) is surfaced in the run report
And the overall run outcome is non-failing regardless of those failures
And report-only never drives the run to a blocking outcome.
```

## Test Requirements

- `test_report_only_mode_never_fails_run`: In report-only mode, linkage results are surfaced but never fail the run.

## Sign-offs

- [ ] python-coder
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit
- [ ] pull-request

## Comments
