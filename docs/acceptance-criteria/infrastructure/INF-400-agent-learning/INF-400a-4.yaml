id: INF-400a-4
title: "First run on a component with no prior context produces normal-quality output"
component: infrastructure
level: L2
status: active
req_status: active
work_status: todo
priority: high
readiness: reviewed
criteria: |
  Given a component folder has no PROJECT_CONTEXT.md files,
  And no memory files exist for that component's domain,
  And no per-folder README.md exists in the component's AC directory,
  When a v3 agent (PO, BA, or IT PO) is spawned for the first time in that component,
  Then the agent completes its task without error,
  And the agent does not emit warnings about missing context files,
  And the output quality matches the baseline established by the agent template alone
    (i.e., the pre-flight step skips missing files silently and proceeds).
depends_on: [INF-400a, INF-400a-1]
doc_links:
  - path: docs/architecture/agent_knowledge_plane.md
    relationship: describes
    status: exists
  - path: templates/agents/product-owner.md
    relationship: constrains
    status: exists
  - path: templates/agents/business-analyst.md
    relationship: constrains
    status: exists
  - path: templates/agents/it-po.md
    relationship: constrains
    status: exists
assigned_agent: llm-expert
estimated_complexity: S
it_requirements:
  - "Must not emit user-visible warnings or log messages about absent context files"
  - "Must not degrade output quality below the template's baseline when no context exists"
delivers_to: null
expects_from:
  ac_id: INF-400a-1
  contract: "Pre-flight step specifies graceful skip (no error, no warning) for absent context files"
origin_agent: business-analyst
created: 2026-06-05
amended_by: []
superseded_by: null
covered_by: [INF-400a-4-i]
implemented_by: []
