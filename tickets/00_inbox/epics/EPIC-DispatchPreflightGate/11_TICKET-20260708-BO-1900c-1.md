---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  pr-reviewer: needed
  pull-request: not_needed
  python-coder: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
complexity: medium
components:
- build_orchestration
created: '2026-07-08'
depends_on:
- BO-1900c
files_touched:
- config/agent_registry.json
- docs/architecture/agent_delivery_workflows.md
- docs/architecture/components/agent-registry.md
priority: high
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: BO-1900c-1
status: todo
title: A run-and-report task assigned to test-writer is caught as a mis-assignment
---

# A run-and-report task assigned to test-writer is caught as a mis-assignment

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BO-1900c-1` — A run-and-report task assigned to test-writer is caught as a mis-assignment — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BO-1900c-1`. Component: `build-orchestration`. Assigned agent: `python-coder`. Estimated complexity: `M`. Complexity: `medium`.

## Acceptance Criteria

```gherkin
Given a ticket whose task asks to "run the existing suite and report the results"
  and whose assigned phase agent is test-writer,
  and the agent registry describes test-writer's charter as authoring new tests and
  test-runner's charter as executing an existing suite and reporting,
When the preflight charter check runs before spawn,
Then it compares the task verb ("run and report") against the assigned agent's charter,
And it returns an unfit verdict because test-writer's charter does not cover run-and-report,
And the held-back reason names the assigned agent, the task verb, and the agent whose
  charter does fit (test-runner),
And no phase agent is spawned.
```

## Test Requirements

```yaml
tests: []
```

## Sign-offs

- [ ] python-coder
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit

## Comments
