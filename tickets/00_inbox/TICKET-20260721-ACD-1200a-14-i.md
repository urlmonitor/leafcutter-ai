---
ac_traceability:
  id: ACD-1200a-14-i
  path: docs/acceptance-criteria/ac-driven-dev/ACD-1200-goal-to-epic/ACD-1200a-14-i.yaml
advances_current_outcome: true
agents:
  ac-fulfillment-gate: needed
  ac-validator: needed
  commit: needed
  pr-reviewer: needed
  pull-request: needed
  python-coder: needed
  test-runner: needed
  test-writer: needed
change_target: pipeline
complexity: low
components:
- ac_driven_dev
created: '2026-07-21'
depends_on:
- ACD-1200a-14
files_touched:
- docs/reference/ac-schema.md
- scripts/ac_store/generate_ticket_from_ac.py
priority: high
requires_adr: false
requires_diagram: false
risk_surface: internal
roadmap_phase: phase_1
source_ac: ACD-1200a-14-i
status: todo
title: When the repo root cannot be derived from git, the back-reference write degrades
  to a documented fallback
---

# When the repo root cannot be derived from git, the back-reference write degrades to a documented fallback

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `ACD-1200a-14-i` — When the repo root cannot be derived from git, the back-reference write degrades to a documented fallback — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `ACD-1200a-14-i`. Component: `ac-driven-dev`. Assigned agent: `python-coder`. Estimated complexity: `S`. Complexity: `low`.

## Acceptance Criteria

```gherkin
This edge case bounds ACD-1200a-14 for the case where canonical git-based
repo-root derivation is unavailable.

Given the generator runs where git rev-parse --show-toplevel cannot resolve a
  repo root — for example the tickets tree is not inside a git repository or the
  git command is unavailable,
When the back-reference is written into the AC's implemented_by list by
  _write_implemented_by,
Then the write does not crash: it falls back to a documented repo-root derivation
  (the existing repo-root helper's non-git path) and emits a WARNING that the git
  top-level could not be resolved,
And the recorded value is still made repo-relative against that fallback root — it
  never records a raw absolute filesystem path with a leading "/",
And when git rev-parse does resolve a repo root the fallback is not used (no
  regression of ACD-1200a-14).
```

- [ ] AC-1: the write does not crash: it falls back to a documented repo-root derivation
- [ ] AC-2: the recorded value is still made repo-relative against that fallback root — it
- [ ] AC-3: when git rev-parse does resolve a repo root the fallback is not used (no

## Test Requirements

```yaml
tests:
- name: test_git_rev_parse_failure_falls_back_with_warning
  file: unit_tests/ac_driven_dev/test_acd_1200a_14_i.py
  covers:
  - ACD-1200a-14-i
  asserts: Simulate git rev-parse failing (not a git repo); the write uses the documented
    fallback root and emits a WARNING, never a raw absolute path.
  framework: unittest
  type: unit
- name: test_fallback_value_still_repo_relative_no_leading_slash
  file: unit_tests/ac_driven_dev/test_acd_1200a_14_i.py
  covers:
  - ACD-1200a-14-i
  asserts: The value recorded via the fallback root is repo-relative with no leading
    '/' and no absolute prefix.
  framework: unittest
  type: unit
- name: test_no_fallback_when_git_resolves_repo_root
  file: unit_tests/ac_driven_dev/test_acd_1200a_14_i.py
  covers:
  - ACD-1200a-14-i
  asserts: When git rev-parse resolves a repo root, the fallback path is not taken
    (no regression of ACD-1200a-14).
  framework: unittest
  type: unit
```

## Implementation Notes

```yaml
config_schema_fragment: null
n_location_rule: '1'
notes:
- 'When git rev-parse --show-toplevel cannot resolve a repo root (not a git repo,
  or git unavailable), the write must not crash: fall back to the existing repo-root
  helper''s documented non-git path and emit a WARNING that the git top-level could
  not be resolved.'
- The recorded value must still be made repo-relative against the fallback root —
  it must never record a raw absolute filesystem path with a leading '/'.
- The fallback must be reached only when git cannot resolve the top-level — when git
  resolves a repo root the fallback is not used (no regression of ACD-1200a-14).
- 'Error handling must follow the project policy: catch the specific failure, log
  at WARNING or higher, no bare/blind except.'
post_write_commands:
- python scripts/build.py --target-dir .
reference_file_path: scripts/ac_store/generate_ticket_from_ac.py
required_skills:
- python-coder
```

## Agent Contracts

### Expects From

- **AC:** ACD-1200a-14
- **Contract:** The git-derived repo-root relativisation in _write_implemented_by (generate_ticket_from_ac.py); this edge case bounds it so a missing git top-level degrades to a logged fallback root instead of crashing or recording an absolute path.

## Sign-offs

- [ ] test-writer
- [ ] python-coder
- [ ] test-runner
- [ ] pr-reviewer
- [ ] ac-validator
- [ ] ac-fulfillment-gate
- [ ] commit
- [ ] pull-request

## Comments
