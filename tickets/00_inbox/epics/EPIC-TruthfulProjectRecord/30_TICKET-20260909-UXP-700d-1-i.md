---
ac_traceability:
  id: UXP-700d-1-i
  path: docs/acceptance-criteria/ux-prototyping/UXP-700-truthful-project-record/UXP-700d-1-i.yaml
advances_current_outcome: true
agents:
  ac-fulfillment-gate: needed
  ac-validator: needed
  architect-review: not_needed
  commit: needed
  documentation-expert: not_needed
  documentation-verifier: not_needed
  frontend-coder: not_needed
  pr-reviewer: not_needed
  pull-request: not_needed
  python-coder: failed
  sql-coder: not_needed
  test-runner: needed
  test-writer: signed_off
  user-surface-smoker: not_needed
change_target: code
complexity: low
components:
- ux_prototyping
created: '2026-09-09'
depends_on: []
files_touched: []
priority: medium
requires_adr: false
requires_diagram: false
risk_surface: internal
roadmap_phase: phase_1
source_ac: UXP-700d-1-i
status: todo
title: A newly added example artifact is excluded by where it lives, not by a list
  of names
---

# A newly added example artifact is excluded by where it lives, not by a list of names

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `UXP-700d-1-i` — A newly added example artifact is excluded by where it lives, not by a list of names — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `UXP-700d-1-i`. Component: `ux-prototyping`. Assigned agent: `python-coder`. Estimated complexity: `S`. Complexity: `low`.

## Acceptance Criteria

```gherkin
Given a new example journey added under the example product root after the separation rule
  was built,
When the project's own record is enumerated,
Then the new journey is absent from it, and no list of artifact names was edited to make that
  so,
And the new journey is returned when the example product is asked for by name,
And the stated count of artifacts set aside as example content is one greater than before the
  journey was added.
```

- [ ] AC-1: the new journey is absent from it, and no list of artifact names was edited to make that
- [ ] AC-2: the new journey is returned when the example product is asked for by name
- [ ] AC-3: the stated count of artifacts set aside as example content is one greater than before the

## Test Requirements

```yaml
tests:
- name: test_newly_added_example_artifact_is_excluded_without_a_list_edit
  file: unit_tests/product_truth/test_uxp_700d_1_i.py
  covers:
  - UXP-700d-1-i
  asserts: An example journey created inside the test is absent from the project's
    record with no configuration change.
  framework: unittest
  type: integration
  angle: seam
- name: test_set_aside_count_rises_by_one_when_an_example_artifact_is_added
  file: unit_tests/product_truth/test_uxp_700d_1_i.py
  covers:
  - UXP-700d-1-i
  asserts: The stated count of artifacts set aside as example content rises from N
    to N+1.
  framework: unittest
  type: integration
  angle: seam
- name: test_uxp_700d_1_i_reachable_from_entry_point
  file: unit_tests/product_truth/test_uxp_700d_1_i.py
  covers:
  - UXP-700d-1-i
  angle: reachability
  asserts: 'REQUIRED — invoke the production entry point (CLI, hook, slash command,
    workflow dispatch, or main()) as a subprocess/dispatch and assert the new behaviour
    actually occurs. Do NOT satisfy this by importing the function directly. The AC
    authored no test_spec, so the entry point is not declared: resolve it before writing
    this test, and do not delete this entry.'
```

## Agent Contracts

### Expects From

- **AC:** UXP-700d-1
- **Contract:** The ownership predicate whose derivation this record proves.

## Sign-offs

- [x] test-writer — 2026-09-09 17:00
- [ ] python-coder — failed 2026-09-09 17:27
- [ ] test-runner
- [ ] ac-validator
- [ ] ac-fulfillment-gate
- [ ] commit

## Comments

### 2026-09-09 17:00 — test-writer (status: ok)
feedback-id: fb_2026-09-09_4b5482ee
completion_manifest:
  tests_written: true
  red_baseline_confirmed: true
  reachability_entry_point_answer:
    result: resolved
    entry_point: "python docs/product-truth/scripts/product_ownership.py --store <dir> [--product NAME] (CLI via subprocess, main() guarded by if __name__ == '__main__':). This is a NEW CLI this ticket's own contract requires python-coder to build — its prerequisite UXP-700d-1 (still work_status: todo) has not yet shipped any caller, so there is no pre-existing hook/command/workflow step to attach to; this mirrors the precedent already set in this same directory by test_uxp_700b_3_i.py for universal_rule_check.py."
  cross_layer_seam_answer:
    result: covered
    producing_side: "generate_product_truth.load_flows() reading real *.flow.json files from disk (the on-disk producer side of the product-truth flows seam)."
    consuming_side: "product_ownership.own_record_flows() / flows_by_product() / set_aside_count() (the new predicate consumer this ticket adds, invented per the not-yet-built UXP-700d-1 contract)."
Wrote unit_tests/product_truth/test_uxp_700d_1_i.py: two seam tests (own_record_flows/flows_by_product/set_aside_count fed the real generate_product_truth.load_flows() output from an on-disk fixture store, proving a newly-added example flow is excluded without any list edit and that the set-aside count rises by exactly one) and one reachability test (subprocess against a new, not-yet-existing docs/product-truth/scripts/product_ownership.py CLI, whose contract is documented in the test file's own docstring since its prerequisite UXP-700d-1 is still todo — same precedent as test_uxp_700b_3_i.py). Ran `python -m pytest unit_tests/product_truth/test_uxp_700d_1_i.py -v`: 2 failed (ModuleNotFoundError: product_ownership) + 1 xfailed (AC-enforcement masking on the reachability test since UXP-700d-1-i work_status != done), overall exit code 1 — confirmed red baseline non-implementation-agnostic. AC-1/AC-2/AC-3 are each tagged via `# covers:`/inline AC-N comments in the test bodies. No production code was written by this phase.

### 2026-09-09 17:27 — python-coder (status: blocker)
feedback-id: fb_2026-09-09_dab28193
completion_manifest:
  upstream_dependency_verified: false
  code_implemented: false
This ticket's `## Agent Contracts` → `Expects From` names AC `UXP-700d-1` with contract "The ownership predicate whose derivation this record proves" — i.e. the red_baseline tests in `unit_tests/product_truth/test_uxp_700d_1_i.py` (`own_record_flows`, `flows_by_product`, `set_aside_count`, and a `docs/product-truth/scripts/product_ownership.py` CLI entry point) require the product-root-derived ownership predicate that upstream ticket `29_TICKET-20260909-UXP-700d-1.md` is contracted to deliver ("Delivers To" → python-coder: "A product-root-derived ownership predicate that UXP-700d-2 reuses ... and UXP-700d-4 reuses ..."). I verified that dependency is unmet: ticket 29 (`UXP-700d-1`) is still `status: todo` with `python-coder: needed` (not signed off, architect-review only), and a repo-wide search found no `product_ownership.py` or any equivalent ownership-predicate module under `docs/product-truth/scripts/` in either the `EPIC-TruthfulProjectRecord` worktree or the `leafcutter-ai` source tree (only `generate_product_truth.py`, `validate_product_truth.py`, `apply_flow_backlinks.py`, `universal_rule_check.py` exist there). Implementing this ticket now would mean inventing the ownership predicate myself inside the wrong ticket's scope, duplicating/pre-empting UXP-700d-1's contracted deliverable and risking a shape mismatch with what UXP-700d-2/UXP-700d-4 are already committed to reusing verbatim. Per this agent's Contract-Aware Mode Step 1 (verify `Depends on` / `Expects From` upstream deliverables before implementing), I am halting without touching any file. Suggested remediation: dispatch `python-coder` against ticket `29_TICKET-20260909-UXP-700d-1.md` first (its `test-writer` and `python-coder` are both still `needed`), then re-dispatch `python-coder` on this ticket (30) once UXP-700d-1 signs off and the ownership-predicate module exists on disk. Separately noting the same ticket_path/worktree_path mismatch architect-review flagged on ticket 29: the `ticket_path` and `worktree_path` arguments this phase was dispatched with point at `EPIC-TruthfulProjectRecord` (tracking `origin/main`), which does not contain this ticket file at all (nor at the given inbox path, nor anywhere in its tickets tree); the ticket was resolved instead by name-search in the `leafcutter-ai` repo on branch `epic/uxp-700-product-truth`, where this sign-off was recorded.
