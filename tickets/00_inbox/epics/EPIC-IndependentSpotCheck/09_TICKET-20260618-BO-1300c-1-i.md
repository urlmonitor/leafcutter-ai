---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  llm-expert: needed
  pr-reviewer: needed
  pull-request: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
components:
- build-orchestration
created: '2026-06-18'
depends_on:
- BO-1300c-1
files_touched:
- docs/acceptance-criteria/build-orchestration/BO-1300-independent-spot-check/BO-1300c.yaml
- docs/architecture/adrs/ADR-003-test-source-of-truth-discipline.md
priority: high
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: BO-1300c-1-i
status: todo
title: Reviewer tempted to fall back to running tests must refuse and exercise the
  feature instead
---

# Reviewer tempted to fall back to running tests must refuse and exercise the feature instead

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BO-1300c-1-i` — Reviewer tempted to fall back to running tests must refuse and exercise the feature instead — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BO-1300c-1-i`. Component: `build-orchestration`. Assigned agent: `llm-expert`. Estimated complexity: `S`.

## Acceptance Criteria

```gherkin
Given a spot-check reviewer is unsure how to probe a feature and the easiest path would be
  to run the existing unit test suite and report its result
When the reviewer decides what to do next
Then the reviewer does not run the unit test suite as a substitute for exercising the feature
And the reviewer instead invokes the feature directly to probe the uncovered gap from its
  assigned angle
And if the reviewer genuinely cannot exercise the feature for its angle, it reports that it
  could not exercise the feature (and why) rather than reporting unit-test output as its result
```

## Sign-offs

- [ ] llm-expert
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit
- [ ] pull-request

## Comments
