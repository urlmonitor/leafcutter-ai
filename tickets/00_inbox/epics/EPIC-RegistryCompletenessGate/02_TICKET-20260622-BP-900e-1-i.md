---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  pr-reviewer: needed
  pull-request: needed
  python-coder: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
components:
- build_pipeline
created: '2026-06-22'
depends_on:
- BP-900e-1
files_touched:
- docs/acceptance-criteria/build_pipeline/BP-900-deployment-completeness/BP-900e-1.yaml
- docs/acceptance-criteria/build_pipeline/BP-900-deployment-completeness/BP-900b.yaml
- scripts/build_propagation_audit.py
- docs/architecture/adrs/ADR-001-self-hosting-boundary.md
- docs/architecture/adrs/ADR-013-portable-skill-script-deployment-boundary.md
priority: high
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: BP-900e-1-i
status: todo
title: A template-referenced script with no template copy is also flagged, coordinated
  with the BP-900b preflight
---

# A template-referenced script with no template copy is also flagged, coordinated with the BP-900b preflight

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BP-900e-1-i` — A template-referenced script with no template copy is also flagged, coordinated with the BP-900b preflight — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BP-900e-1-i`. Component: `build-pipeline`. Assigned agent: `python-coder`. Estimated complexity: `S`.

## Acceptance Criteria

```gherkin
Given a deployable agent template references a runtime script by path
  (for example "python scripts/some_capability.py")
And no deploy-source copy exists at the derived
  templates/scripts/some_capability.py
And that script is NOT in the external-dependency allowlist
When the registry-completeness check runs at the finalize merge gate
Then the template-referenced-but-undeployed script is flagged as a
  completeness failure
And the failure entry records that it was promised by a template reference and
  names the referencing template path (per BP-900e-4)
And this finding is the MERGE-GATE complement of BP-900b's build.py PREFLIGHT
  script-reference guard: BP-900b catches the same missing-template-copy class
  earlier (at preflight, against the deployable manifest), and this check
  re-asserts it at the merge gate alongside the registry axis — the two are
  coordinated, not contradictory, and a script caught by both is reported once
  per surface with its origin stated
```

## Sign-offs

- [ ] python-coder
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit
- [ ] pull-request

## Comments
