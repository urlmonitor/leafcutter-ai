---
ac_traceability:
  id: GE-120b-3
  path: docs/acceptance-criteria/guardrail-engine/GE-120-green-means-checked/GE-120b-3.yaml
advances_current_outcome: true
agents:
  ac-fulfillment-gate: needed
  ac-validator: needed
  architect-review: needed
  commit: needed
  documentation-expert: needed
  documentation-verifier: needed
  pr-reviewer: needed
  pull-request: not_needed
  python-coder: needed
  test-runner: needed
  test-writer: needed
change_target: code
complexity: medium
components:
- commit_guardian
- ac_store
created: '2026-08-25'
depends_on:
- 09_TICKET-20260825-GE-120b-2.md
documentation_required: true
files_touched:
- config/ac_store_schema.json
- scripts/build.py
- scripts/commit_guardian
- templates/scripts/commit_guardian
- templates/scripts/commit_guardian/_resolve_root.py
- templates/scripts/commit_guardian/check_ac_schema.py
- templates/scripts/commit_guardian/commit_guardian.json
priority: high
requires_adr: false
requires_diagram: false
risk_surface: contract_boundary
roadmap_phase: phase_1
source_ac: GE-120b-3
status: todo
title: Configuration and data prerequisites resolve from a separate working copy,
  not only importable helpers
---

# Configuration and data prerequisites resolve from a separate working copy, not only importable helpers

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `GE-120b-3` — Configuration and data prerequisites resolve from a separate working copy, not only importable helpers — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `GE-120b-3`. Component: `guardrail-engine`. Assigned agent: `python-coder`. Estimated complexity: `M`. Complexity: `medium`.

## Acceptance Criteria

```gherkin
Given the AC-schema check, whose unresolvable prerequisite is a schema document rather than
  an importable helper module,
And a fixture of staged AC YAML files of which a known number violate the authoritative
  schema,
When the check is executed against those identical staged files from the main checkout and
  again from a separate working copy,
Then both runs load the authoritative schema document,
And both report the identical set of violating files,
And neither run substitutes the reduced manual field validation,
And the degradation to manual field validation observed today when running from a separate
  working copy no longer occurs,
And the same holds for at least one further prerequisite kind beyond a schema document — a
  configuration document read at runtime — so the resolution facility is shown to handle
  data files generally and not one path special-cased.

Coverage note: covered by executing the check from a real second working copy through the
GE-120c-1 harness and comparing the two reported violation sets. A test that asserts the
schema path resolves to a non-empty string does not cover it — the observed defect produced
a path that was wrong, not a path that was empty.
```

- [ ] AC-1: a fixture of staged AC YAML files of which a known number violate the authoritative
- [ ] AC-2: both runs load the authoritative schema document
- [ ] AC-3: both report the identical set of violating files
- [ ] AC-4: neither run substitutes the reduced manual field validation
- [ ] AC-5: the degradation to manual field validation observed today when running from a separate
- [ ] AC-6: the same holds for at least one further prerequisite kind beyond a schema document — a

## Test Requirements

```yaml
tests:
- name: test_ge120b3_authoritative_schema_loaded_from_both_working_copies
  file: unit_tests/portability/test_ge_120b_3.py
  covers:
  - GE-120b-3
  asserts: Main checkout and a separate working copy both load config/ac_store_schema.json
    — the schema_validated path is taken in both runs.
  framework: unittest
  type: behavioral
- name: test_ge120b3_identical_violating_file_sets_from_both_copies
  file: unit_tests/portability/test_ge_120b_3.py
  covers:
  - GE-120b-3
  asserts: Against a fixture with a known number of schema-violating staged AC files,
    both runs report the identical set of violating files.
  framework: unittest
  type: behavioral
- name: test_ge120b3_manual_field_validation_not_substituted_in_either_run
  file: unit_tests/portability/test_ge_120b_3.py
  covers:
  - GE-120b-3
  asserts: Neither run falls back to the reduced manual field validation; the degradation
    observed today no longer occurs.
  framework: unittest
  type: behavioral
- name: test_ge120b3_second_config_prerequisite_also_resolves_from_both_copies
  file: unit_tests/portability/test_ge_120b_3.py
  covers:
  - GE-120b-3
  asserts: A second, different prerequisite kind — a runtime configuration document
    — resolves identically from both copies, showing the facility handles data files
    generally.
  framework: unittest
  type: behavioral
- name: test_ge_120b_3_reachable_from_entry_point
  file: unit_tests/portability/test_ge_120b_3.py
  covers:
  - GE-120b-3
  angle: reachability
  asserts: 'REQUIRED — invoke the production entry point (CLI, hook, slash command,
    workflow dispatch, or main()) as a subprocess/dispatch and assert the new behaviour
    actually occurs. Do NOT satisfy this by importing the function directly. The AC
    authored no test_spec, so the entry point is not declared: resolve it before writing
    this test, and do not delete this entry.'
```

## Implementation Notes

```yaml
- 'Target: templates/scripts/commit_guardian/check_ac_schema.py; the prerequisite
  is a DATA FILE, config/ac_store_schema.json, not an importable module. VERIFIED
  2026-08-14: this check carries a private root walk and does not import the shared
  _resolve_root.py, which is why the lookup fails from a separate working copy.'
- 'Consume GE-120b-2''s facility EXTENDED for data and configuration prerequisites.
  Do not special-case the one schema path: the criteria explicitly require a second,
  different prerequisite kind — a configuration document read at runtime. templates/scripts/commit_guardian/commit_guardian.json,
  read by nearly every check through config.py, is the natural second case and exercises
  the same resolution path from a different consumer.'
- The success condition is that BOTH runs LOAD the authoritative schema and report
  the IDENTICAL violating-file set — not merely that both exit the same way. Compare
  the sets, file by file. Two runs that both exit 0 for different reasons satisfy
  an exit-code assertion and fail this AC.
- 'A resolved-to-non-empty-string assertion does NOT satisfy this: the observed defect
  produced a WRONG path, not an empty one. That distinction is the criterion''s own
  coverage note.'
- The manual field-validation fallback must not be substituted in either run. Assert
  on the GE-112 schema_validated flag (see check_ac_schema.py DECISION HISTORY ~lines
  29-37 and the fallback at ~line 623) so 'authoritative validation actually ran'
  is observable rather than inferred.
- 'COMPLEMENTARY WITH GE-120a-3, NOT REDUNDANT: this AC makes the schema REACHABLE;
  GE-120a-3 makes the check ANNOUNCE when it is not. An implementation that makes
  the path resolve and removes the announce path fails GE-120a-3. Neither closes the
  other, because no resolution fix covers every future environment.'
- 'ADR-001 deployed/template parity: edit templates/scripts/commit_guardian/ then
  run ''python scripts/build.py --target-dir .'' so scripts/commit_guardian/ and .leafcutter/scripts/commit_guardian/
  match (check_hook_parity enforces all four locations). The data file resolution
  must work from the DEPLOYED layout, which is the layout .pre-commit-config.yaml
  invokes — resolving correctly only from the source tree is the defect, not the fix.'
```

## Agent Contracts

### documentation-expert

Existing docs to update / cross-link:

- templates/scripts/commit_guardian/check_ac_schema.py (relationship: modifies | status: exists)
- config/ac_store_schema.json (relationship: related | status: exists)
- docs/reference/ac-schema.md (relationship: context | status: exists | relevance: Warns that the manual fallback is what actually gates in some environments.)
- templates/scripts/commit_guardian/_resolve_root.py (relationship: modifies | status: exists | relevance: The shared facility to extend for data and configuration prerequisites, rather than adding a sibling module.)
- templates/scripts/commit_guardian/commit_guardian.json (relationship: related | status: exists | relevance: Candidate second prerequisite kind: the runtime configuration document nearly every check reads via config.py.)

- [ ] AC-1: how-to | templates/scripts/commit_guardian/check_ac_schema.py | both runs load the authoritative schema document

### Expects From

- **AC:** GE-120b-2
- **Contract:** The shared resolution facility, covering data and configuration prerequisites as well as helper modules.

## Sign-offs

- [ ] architect-review
- [ ] test-writer
- [ ] python-coder
- [ ] test-runner
- [ ] documentation-expert
- [ ] pr-reviewer
- [ ] ac-validator
- [ ] ac-fulfillment-gate
- [ ] documentation-verifier
- [ ] commit

## Comments
