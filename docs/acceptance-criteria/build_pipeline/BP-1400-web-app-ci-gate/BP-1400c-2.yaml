id: BP-1400c-2
components:
  - build_pipeline
title: "Sequence diagram documents the web-app route-render check flow"
component: build-pipeline
level: L2
status: active
req_status: draft
work_status: todo
readiness: approved
priority: high
roadmap_phase: phase_1
criteria: |
  Given the documentation set for the build pipeline,
  When a reader opens the web-app route-render check documentation,
  Then a sequence diagram shows the flow from a pull request that changes the web
    app, through the check headlessly loading each route and observing the HTTP
    status and any console or render output, to the pass/fail result being
    reported on the pull request and gating the merge decision,
  And the diagram identifies the participating actors: the pull request, the CI
    system, the headless browser loading each route, and the merge gate.
depends_on:
  - BP-1400c
  - BP-1400c-1
doc_links:
  - path: docs/acceptance-criteria/build_pipeline/PROJECT_CONTEXT.md
    relationship: describes
    status: exists
  - path: docs/architecture/components/web-app-ci-gate.md
    relationship: documents
    status: planned
assigned_agent: architecture-diagram-author
estimated_complexity: S
it_requirements:
  - "Author a Mermaid sequenceDiagram in the planned web-app CI gate documentation (docs/architecture/components/web-app-ci-gate.md, or a diagram file it references) using the write-c4-diagram skill conventions."
  - "The diagram MUST depict the route-render-specific interaction: PR changes leafcutter-web/ -> CI starts the route-render check -> the headless browser loads each route -> the check observes HTTP status and console/render output per route -> pass/fail reported on the PR -> merge gate decision. Name the actors: the pull request, the CI system, the headless browser loading each route, and the merge gate."
  - "Check/actor naming MUST match BP-1400c-1: the route-render check's label must be the same stable ci.yml job name that BP-1400a-4 wires into the required-status-checks list — no divergent labels."
  - "Diagram is documentation only — it must not assert behavior beyond BP-1400c-1 (no invented retry/notification steps). Distinct from BP-1400a-3, which depicts the shared build/type-check PR flow; this diagram depicts the per-route headless-load interaction (status + console/render observation)."
test_required: false
delivers_to: null
expects_from:
  ac_id: BP-1400c-1
  contract: "The web-app route-render check whose headless-load-per-route flow this diagram depicts"
origin_agent: business-analyst
created: 2026-07-21
amended_by:
  - reason: "it-po-technical-enrichment"
    entry: "Confirmed assigned_agent architecture-diagram-author (correct docs/diagram surface, kept per direction). Added it_requirements (Mermaid sequenceDiagram via write-c4-diagram; route-render-specific per-route headless-load flow with the four named actors incl the headless browser; check-name/actor parity with BP-1400c-1 and the BP-1400a-4 required-check name; docs-only, no invented steps; distinct from BP-1400a-3). Set test_required: false (documentation AC, change_target docs — no executable behavior). Criteria, contracts, doc_links, readiness, and components unchanged."
    by: it-po
superseded_by: null
covered_by: []
implemented_by:
- /home/henzeh/projects/leafcutter/leafcutter-ai/tickets/00_inbox/epics/EPIC-ChangesToTheWebAppCantReachUsersBroken/11_TICKET-20260721-BP-1400c-2.md
notes: |
  Satisfies the documentation_triggers [sequence-diagram] declared on BP-1400c.
  Distinct from BP-1400a-3 (which depicts the shared build/type-check pull-request
  flow): this diagram depicts the route-render-specific interaction — the headless
  browser loading each route and the check observing status plus console/render
  output.
change_target: docs
risk_surface: internal
