---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  llm-expert: needed
  pr-reviewer: needed
  pull-request: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
components:
- build-orchestration
created: '2026-07-07'
depends_on:
- BO-1800c-1
files_touched:
- docs/architecture/adrs/ADR-018-agent-isolation-topology.md
priority: medium
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: BO-1800c-1-i
status: todo
title: An inherently sequential task is not force-parallelized
---

# An inherently sequential task is not force-parallelized

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BO-1800c-1-i` — An inherently sequential task is not force-parallelized — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BO-1800c-1-i`. Component: `build-orchestration`. Assigned agent: `llm-expert`. Estimated complexity: `S`.

## Acceptance Criteria

```gherkin
Given a task whose steps are inherently sequential (each step depends on the
  previous one),
When work on that task is scheduled,
Then it is not split across multiple agents to force artificial parallelism,
And it runs with a single collaborator (or the minimum the dependencies allow)
  rather than being padded up toward the per-feature cap just because the cap
  permits it.
```

## Sign-offs

- [ ] llm-expert
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit
- [ ] pull-request

## Comments
