---
ac_traceability:
  id: BP-1400c-2
  path: docs/acceptance-criteria/build_pipeline/BP-1400-web-app-ci-gate/BP-1400c-2.yaml
advances_current_outcome: true
agents:
  architecture-diagram-author: needed
  commit: needed
  documentation-expert: needed
  pull-request: not_needed
change_target: docs
complexity: low
components:
- build_pipeline
created: '2026-07-21'
depends_on:
- 08_TICKET-20260721-BP-1400c-1.md
files_touched:
- docs/architecture/components/web-app-ci-gate.md
priority: high
requires_adr: false
requires_diagram: false
risk_surface: internal
roadmap_phase: phase_1
source_ac: BP-1400c-2
status: todo
title: Sequence diagram documents the web-app route-render check flow
---

# Sequence diagram documents the web-app route-render check flow

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BP-1400c-2` — Sequence diagram documents the web-app route-render check flow — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BP-1400c-2`. Component: `build-pipeline`. Assigned agent: `architecture-diagram-author`. Estimated complexity: `S`. Complexity: `low`.

## Acceptance Criteria

```gherkin
Given the documentation set for the build pipeline,
When a reader opens the web-app route-render check documentation,
Then a sequence diagram shows the flow from a pull request that changes the web
  app, through the check headlessly loading each route and observing the HTTP
  status and any console or render output, to the pass/fail result being
  reported on the pull request and gating the merge decision,
And the diagram identifies the participating actors: the pull request, the CI
  system, the headless browser loading each route, and the merge gate.
```

- [ ] AC-1: a sequence diagram shows the flow from a pull request that changes the web
- [ ] AC-2: the diagram identifies the participating actors: the pull request, the CI

## Implementation Notes

```yaml
- Author a Mermaid sequenceDiagram in the planned web-app CI gate documentation (docs/architecture/components/web-app-ci-gate.md,
  or a diagram file it references) using the write-c4-diagram skill conventions.
- 'The diagram MUST depict the route-render-specific interaction: PR changes leafcutter-web/
  -> CI starts the route-render check -> the headless browser loads each route ->
  the check observes HTTP status and console/render output per route -> pass/fail
  reported on the PR -> merge gate decision. Name the actors: the pull request, the
  CI system, the headless browser loading each route, and the merge gate.'
- 'Check/actor naming MUST match BP-1400c-1: the route-render check''s label must
  be the same stable ci.yml job name that BP-1400a-4 wires into the required-status-checks
  list — no divergent labels.'
- Diagram is documentation only — it must not assert behavior beyond BP-1400c-1 (no
  invented retry/notification steps). Distinct from BP-1400a-3, which depicts the
  shared build/type-check PR flow; this diagram depicts the per-route headless-load
  interaction (status + console/render observation).
```

## Agent Contracts

### Expects From

- **AC:** BP-1400c-1
- **Contract:** The web-app route-render check whose headless-load-per-route flow this diagram depicts

## Sign-offs

- [ ] documentation-expert
- [ ] architecture-diagram-author
- [ ] commit

## Comments
