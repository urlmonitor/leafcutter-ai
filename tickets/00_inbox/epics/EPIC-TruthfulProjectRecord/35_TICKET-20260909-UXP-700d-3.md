---
ac_traceability:
  id: UXP-700d-3
  path: docs/acceptance-criteria/ux-prototyping/UXP-700-truthful-project-record/UXP-700d-3.yaml
advances_current_outcome: true
agents:
  ac-fulfillment-gate: needed
  ac-validator: needed
  architect-review: needed
  commit: needed
  documentation-expert: needed
  documentation-verifier: needed
  frontend-coder: not_needed
  pr-reviewer: needed
  pull-request: not_needed
  python-coder: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
  user-surface-smoker: not_needed
change_target:
- code
- schema
- docs
complexity: medium
components:
- ux_prototyping
- ac_store
created: '2026-09-09'
depends_on:
- TICKET-20260909-UXP-700d-1.md
documentation_required: true
files_touched:
- docs/product-truth/schemas/flow.schema.json
- docs/reference/ac-schema.md
priority: medium
requires_adr: false
requires_diagram: false
risk_surface: contract_boundary
roadmap_phase: phase_1
source_ac: UXP-700d-3
status: todo
title: Every piece of example content says it is an example, read on its own
---

# Every piece of example content says it is an example, read on its own

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `UXP-700d-3` — Every piece of example content says it is an example, read on its own — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `UXP-700d-3`. Component: `ux-prototyping`. Assigned agent: `python-coder`. Estimated complexity: `M`. Complexity: `medium`.

## Acceptance Criteria

```gherkin
Given any example artifact or any example acceptance criterion,
When it is read on its own, by a person or by an agent that knows nothing about where it came
  from,
Then it states the product it belongs to and that that product is an example,
And that statement is held in the artifact itself rather than inferred from its location,
And an artifact of the project's own record read the same way carries no such statement,
And an artifact whose stated product disagrees with the product root it lives under is
  reported, naming both.
```

- [ ] AC-1: it states the product it belongs to and that that product is an example
- [ ] AC-2: that statement is held in the artifact itself rather than inferred from its location
- [ ] AC-3: an artifact of the project's own record read the same way carries no such statement
- [ ] AC-4: an artifact whose stated product disagrees with the product root it lives under is

## Test Requirements

```yaml
tests:
- name: test_example_artifact_declares_its_example_product_in_its_own_content
  file: unit_tests/product_truth/test_uxp_700d_3.py
  covers:
  - UXP-700d-3
  asserts: Loading an example artifact's file content alone reveals the product it
    belongs to and that the product is an example.
  framework: unittest
  type: unit
  angle: real_artifact
- name: test_project_owned_artifact_carries_no_example_declaration
  file: unit_tests/product_truth/test_uxp_700d_3.py
  covers:
  - UXP-700d-3
  asserts: A project-owned artifact read the same way carries no example declaration.
  framework: unittest
  type: unit
  angle: boundary
- name: test_declaration_disagreeing_with_the_product_root_is_reported
  file: unit_tests/product_truth/test_uxp_700d_3.py
  covers:
  - UXP-700d-3
  asserts: An artifact declaring the example product while living under the project's
    root is reported naming both values.
  framework: unittest
  type: integration
  angle: failure
- name: test_uxp_700d_3_reachable_from_entry_point
  file: unit_tests/product_truth/test_uxp_700d_3.py
  covers:
  - UXP-700d-3
  angle: reachability
  asserts: 'REQUIRED — invoke the production entry point (CLI, hook, slash command,
    workflow dispatch, or main()) as a subprocess/dispatch and assert the new behaviour
    actually occurs. Do NOT satisfy this by importing the function directly. The AC
    authored no test_spec, so the entry point is not declared: resolve it before writing
    this test, and do not delete this entry.'
```

## Agent Contracts

### documentation-expert

Existing docs to update / cross-link:

- docs/product-truth/schemas/flow.schema.json (relationship: specifies | status: exists | relevance: The artifact shape that gains the declaration; a source field already exists with values real and mock and may be the right axis to build on.)
- docs/reference/ac-schema.md (relationship: specifies | status: exists)

- [ ] AC-1: reference-doc | docs/product-truth/schemas/flow.schema.json | it states the product it belongs to and that that product is an example

### Delivers To

- **Agent:** frontend-coder
- **Contract:** A self-declared example marker on every example artifact and criterion, so any reading surface can label it without consulting a registry.

### Expects From

- **AC:** UXP-700d-1
- **Contract:** The product-root ownership predicate this declaration is cross-checked against.

## Sign-offs

- [ ] architect-review
- [ ] test-writer
- [ ] python-coder
- [ ] test-runner
- [ ] documentation-expert
- [ ] pr-reviewer
- [ ] ac-validator
- [ ] ac-fulfillment-gate
- [ ] documentation-verifier
- [ ] commit

## Comments
