---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  pr-reviewer: signed_off
  pull-request: needed
  python-coder: signed_off
  sql-coder: not_needed
  test-runner: signed_off
  test-writer: signed_off
components:
- ac-store
created: '2026-06-17'
depends_on:
- ACS-500f-3
files_touched:
- config/ac_store_schema.json
priority: medium
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: ACS-500f-3-i
status: in_progress
title: Widened schema still rejects malformed ids and unknown fields
---

# Widened schema still rejects malformed ids and unknown fields

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `ACS-500f-3-i` — Widened schema still rejects malformed ids and unknown fields — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `ACS-500f-3-i`. Component: `ac-store`. Assigned agent: `python-coder`. Estimated complexity: `S`.

## Acceptance Criteria

```gherkin
Given an AC record whose id does not match any valid AC id form
  (e.g. "acs-500" lowercase prefix, "ACS_500" underscore separator, or
  "ACS-500a-" with a trailing empty leaf segment),
When it is validated against the widened schema,
Then the record is rejected with a validation error naming the id field.

Given an AC record that includes a field name that is not part of the AC
  schema (e.g. a misspelled "critera" or an invented "foo_bar" key),
When it is validated against the widened schema,
Then the record is rejected,
And the widening to accept pattern_slots does NOT loosen the schema into
  accepting arbitrary unknown fields.

Given an AC record missing a required field (e.g. no criteria),
When it is validated against the widened schema,
Then the record is still rejected for the missing required field.
```

## Sign-offs

- [x] python-coder — 2026-06-18 10:15
- [x] test-writer — 2026-06-18 10:00
- [x] test-runner — 2026-06-18 10:30
- [x] pr-reviewer — 2026-06-18 10:45
- [ ] commit
- [ ] pull-request

## Comments

### 2026-06-18 10:00 — ticket-supervisor (status: ok)
feedback-id: no-feedback
test_requirements empty — test-writer phase skipped (docs-only or config-only ticket)

### 2026-06-18 10:15 — python-coder (status: ok)
feedback-id: fb_2026-06-17_d2f2b426

completion_manifest:
- unit_tests/commit_guardian/test_check_ac_schema.py — added 4 new test classes (7 new test methods) covering all 3 ACs in ACS-500f-3-i

Added edge-case guard tests verifying that the widened AC schema (which now admits `pattern_slots`) still correctly rejects: malformed IDs (lowercase prefix `acs-500`, underscore separator `ACS_500`, trailing empty leaf `ACS-500a-`); unknown/misspelled fields (`critera`, `foo_bar`) — confirming `additionalProperties: false` is intact; and AC records missing the required `criteria` field. A positive guard test confirms `pattern_slots` itself is admitted. All 36 tests pass (7 new, 29 pre-existing).

### 2026-06-18 10:30 — test-runner (status: ok)
feedback-id: fb_2026-06-17_77b96492
completion_manifest:
  tests_green: true
  new_tests_count: 7
All 36 tests pass (7 new edge-case guard tests + 29 pre-existing).

### 2026-06-18 10:45 — pr-reviewer (status: ok)

feedback-id: fb_2026-06-17_72e9cfaf

completion_manifest:
- ac1_malformed_ids_rejected: true
- ac2_unknown_fields_rejected: true
- ac2_pattern_slots_admitted: true
- ac3_missing_required_field_rejected: true
- all_36_tests_green: true
- no_regressions: true

PR review passed. The diff adds 4 new test classes (7 test methods) to `unit_tests/commit_guardian/test_check_ac_schema.py` with no production code changes. AC coverage is complete: AC1 is satisfied by 3 methods in `TestMalformedIdRejectedAfterWidening` (lowercase prefix `acs-500`, underscore separator `ACS_500`, trailing empty leaf `ACS-500a-`); AC2 is satisfied by 2 rejection methods in `TestUnknownFieldRejectedAfterWidening` (`critera`, `foo_bar`) plus a positive guard in `TestPatternSlotsAcceptedAfterWidening` confirming `pattern_slots` is admitted; AC3 is satisfied by `TestMissingRequiredFieldAfterWidening.test_missing_criteria_rejected_after_widening` which confirms `criteria` remains required even when `pattern_slots` is present. All 36 tests pass with no regressions.
