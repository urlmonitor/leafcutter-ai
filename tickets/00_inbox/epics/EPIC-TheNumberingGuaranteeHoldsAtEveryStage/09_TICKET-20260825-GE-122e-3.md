---
ac_traceability:
  id: GE-122e-3
  path: docs/acceptance-criteria/guardrail-engine/GE-122-numbers-mean-one-thing/GE-122e-3.yaml
advances_current_outcome: true
agents:
  ac-fulfillment-gate: needed
  ac-validator: needed
  commit: needed
  documentation-expert: needed
  documentation-verifier: needed
  pull-request: needed
  python-coder: needed
  test-runner: needed
  test-writer: needed
change_target: docs
complexity: low
components:
- commit_guardian
- ac_store
created: '2026-08-25'
depends_on:
- 08_TICKET-20260825-GE-122e-2.md
- ../../../99_done/TICKET-20260817-GE-122e-1.md
documentation_required: true
files_touched:
- unit_tests/commit_guardian
priority: high
requires_adr: false
requires_diagram: false
risk_surface: internal
roadmap_phase: phase_1
source_ac: GE-122e-3
status: todo
title: The repaired collection passes the guard itself, with nothing excused
---

# The repaired collection passes the guard itself, with nothing excused

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `GE-122e-3` — The repaired collection passes the guard itself, with nothing excused — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `GE-122e-3`. Component: `guardrail-engine`. Assigned agent: `python-coder`. Estimated complexity: `S`. Complexity: `low`.

## Acceptance Criteria

```gherkin
Given the contested requirement identifier and the five twice-held work items have been
  repaired,
When the whole-collection uniqueness pass is run over the repaired collection with no
  exemption list, no allowlist, and no known-bad entries configured for any namespace,
Then it terminates with a passing result for all four namespaces,
And it states, per namespace, the number of artifacts it inspected, and each count equals the
  number of artifacts actually present in that namespace,
And no configuration exists anywhere that would cause a contested number to be reported as
  acceptable,

And the artifacts that carry no number at all are unchanged in name and in location — they are
  not ambiguous, they break no reference, and they are deliberately outside the repair,

And when a contested number is then reintroduced into any one of the four namespaces, the same
  pass over the same collection terminates with a failing result naming it — so the passing
  result above is evidence that the collection is clean, not evidence that the pass is inert.

Coverage note: the final clause is the load-bearing one and must be executed, not reasoned
about. A pass that reports success over the repaired collection proves nothing on its own,
because a pass that inspects nothing reports the same thing. Only the reintroduce-and-re-run
pair distinguishes a clean collection from a dead check.
```

- [ ] AC-1: it terminates with a passing result for all four namespaces
- [ ] AC-2: it states, per namespace, the number of artifacts it inspected, and each count equals the
- [ ] AC-3: no configuration exists anywhere that would cause a contested number to be reported as
- [ ] AC-4: the artifacts that carry no number at all are unchanged in name and in location — they are
- [ ] AC-5: when a contested number is then reintroduced into any one of the four namespaces, the same

## Test Requirements

```yaml
tests:
- name: test_repaired_collection_passes_all_four_namespaces
  file: unit_tests/commit_guardian/test_ge_122e_3.py
  covers:
  - GE-122e-3
  asserts: The real gate, invoked through its production entry point over the repaired
    collection with no exemption arguments, returns a passing outcome for all four
    namespaces.
  framework: unittest
  type: integration
- name: test_inspected_counts_equal_actual_artifact_counts
  file: unit_tests/commit_guardian/test_ge_122e_3.py
  covers:
  - GE-122e-3
  asserts: For each namespace the reported inspected count equals an independently-computed
    count of the artifacts present — asserted as equality, since a non-zero assertion
    passes on a pass that read one file.
  framework: unittest
  type: integration
- name: test_reintroduced_collision_fails_in_every_namespace
  file: unit_tests/commit_guardian/test_ge_122e_3.py
  covers:
  - GE-122e-3
  asserts: A contested number reintroduced into each of the four namespaces in turn
    makes the same pass over the same collection fail and name it; each reintroduction
    is removed afterwards. This is the mutation check the coverage note calls load-bearing.
  framework: unittest
  type: integration
- name: test_no_exemption_configuration_exists_on_any_surface
  file: unit_tests/commit_guardian/test_ge_122e_3.py
  covers:
  - GE-122e-3
  asserts: No allowlist, known-bad list, skip argument or enabled:false manifest flag
    exists on any of the enumerated configuration surfaces that would cause a contested
    number to be reported as acceptable.
  framework: unittest
  type: behavioral
- name: test_unnumbered_artifacts_unchanged_by_name_and_location
  file: unit_tests/commit_guardian/test_ge_122e_3.py
  covers:
  - GE-122e-3
  asserts: All eleven unnumbered artifacts are present at their original names and
    paths — asserted by name and location, not by count, so renaming one to tidy it
    up fails rather than passes.
  framework: unittest
  type: behavioral
- name: test_ge_122e_3_reachable_from_entry_point
  file: unit_tests/commit_guardian/test_ge_122e_3.py
  covers:
  - GE-122e-3
  angle: reachability
  asserts: 'REQUIRED — invoke the production entry point (CLI, hook, slash command,
    workflow dispatch, or main()) as a subprocess/dispatch and assert the new behaviour
    actually occurs. Do NOT satisfy this by importing the function directly. The AC
    authored no test_spec, so the entry point is not declared: resolve it before writing
    this test, and do not delete this entry.'
```

## Implementation Notes

```yaml
- Run the REAL gate, in the deployed layout, by the same entry point the commit-time
  and build-time stages use. A bespoke audit script written for this verification
  would prove that a script can count correctly, not that the guard that will hold
  the line afterwards works — and the guard is the artifact under test.
- The reintroduce-and-re-run pair must be executed as one test, in all four namespaces,
  with the reintroduced collision REMOVED afterwards. It is the only clause here that
  a broken implementation cannot satisfy, and it is the mutation check for the whole
  tree. Running it in one namespace leaves the other three unproven, which is the
  same gap as a pass that inspects nothing.
- The inspected counts must be asserted as EQUAL to independently-computed artifact
  counts for each namespace, not merely as non-zero. A non-zero assertion passes on
  a pass that reads one file per namespace, and the whole point of the count is to
  detect a partial walk.
- '''No configuration exists anywhere that would excuse a contested number'' must
  be checked against the actual configuration surfaces, enumerated: the enrolment/identifier
  rule data GE-122b-1 introduces, the `hooks_manifest.hooks` entry (an `enabled: false`
  flag there silently removes a hook from `.pre-commit-config.yaml` at build time),
  the ci.yml job''s own conditions, and any skip or exemption argument on the gate''s
  command line. An `enabled: false` manifest entry is the most plausible way this
  clause is violated without anyone writing an allowlist.'
- The unchanged-artifacts clause must be asserted by NAME AND LOCATION over the eleven
  unnumbered diagrams, not by count. A count is preserved by renaming one of them,
  which is exactly the tidy-up the clause exists to fail.
- This AC is the tree's exit gate and must run AFTER GE-122e-1 and GE-122e-2, and
  BEFORE GE-122d-2 makes the CI gate required — that ordering is what stops a required
  gate from failing every merge on pre-existing drift.
- The verification must be re-run at merge time, not only when it is written. The
  collection moves continuously; a pass recorded three weeks before the merge is evidence
  about a different collection.
```

## Agent Contracts

### documentation-expert

Existing docs to update / cross-link:

- docs/acceptance-criteria/guardrail-engine/GE-122-numbers-mean-one-thing/GE-122a-1.yaml (relationship: context | status: exists | relevance: The pass this criterion runs. Using the real guard rather than a bespoke audit script is the point — the repair is verified by the thing that will hold the line afterwards.)
- docs/acceptance-criteria/guardrail-engine/GE-122-numbers-mean-one-thing/GE-122a-2.yaml (relationship: context | status: exists | relevance: The work-item half of the same pass.)
- docs/architecture/components/commit-guardian.md (relationship: describes | status: exists)
- docs/acceptance-criteria/guardrail-engine/GE-120-green-means-checked/GE-120a-1.yaml (relationship: context | status: exists | relevance: The sibling policy the per-namespace inspected count serves: a check that could not inspect must not look like one that inspected and found nothing. This criterion turns that policy into an equality assertion against the real artifact counts.)

- [ ] AC-1: [(unspecified genre)] docs/acceptance-criteria/guardrail-engine/GE-122-numbers-mean-one-thing/GE-122a-1.yaml — it terminates with a passing result for all four namespaces

### Expects From

- **AC:** GE-122a-1
- **Contract:** The whole-collection uniqueness pass, run unmodified and unconfigured, as the acceptance instrument for the repair.

## Sign-offs

- [ ] test-writer
- [ ] python-coder
- [ ] test-runner
- [ ] documentation-expert
- [ ] ac-validator
- [ ] ac-fulfillment-gate
- [ ] documentation-verifier
- [ ] commit
- [ ] pull-request

## Comments
