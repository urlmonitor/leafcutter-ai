---
advances_current_outcome: true
agents:
  architecture-diagram-author: needed
  commit: needed
  documentation-expert: not_needed
  pr-reviewer: needed
  pull-request: not_needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
complexity: low
components:
- build_orchestration
created: '2026-07-10'
depends_on:
- BO-2100c
- BO-2100c-2
files_touched:
- docs/architecture/agent_delivery_workflows.md
- docs/architecture/adrs/ADR-020-live-surface-tester.md
priority: medium
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: BO-2100c-4
status: todo
title: Sequence diagram of server startup, PID recording, and teardown
---

# Sequence diagram of server startup, PID recording, and teardown

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BO-2100c-4` — Sequence diagram of server startup, PID recording, and teardown — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BO-2100c-4`. Component: `build-orchestration`. Assigned agent: `architecture-diagram-author`. Estimated complexity: `S`. Complexity: `low`.

## Acceptance Criteria

```gherkin
Given the lifecycle of a test server from start through teardown
When a maintainer needs to understand how ports and PIDs are managed
Then a sequence diagram exists showing: port allocation, server spawn in its own
  session, PID recorded to the registry, health probe, and teardown that kills the
  process group and releases the port at both stop and finalize.
```

## Implementation Notes

```yaml
- 'The diagram must depict the full lifecycle: port allocation, session-isolated server
  spawn, PID recording to the registry, health probe, and teardown (process-group
  kill + port release) at BOTH stop and finalize.'
- The depicted behavior must match the implemented lifecycle in live_surface_startup.py
  and port_registry.py (BO-2100c-1, c-2, c-3), not an idealized version.
```

## Sign-offs

- [ ] architecture-diagram-author
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit

## Comments
