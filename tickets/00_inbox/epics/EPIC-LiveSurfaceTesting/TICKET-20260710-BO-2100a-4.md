---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  llm-expert: needed
  pr-reviewer: needed
  pull-request: not_needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
complexity: low
components:
- build_orchestration
created: '2026-07-10'
depends_on:
- BO-2100a
files_touched:
- templates/skills/building-epics/SKILL.md
- templates/workflows-js/build-ticket.js
- docs/architecture/adrs/ADR-020-live-surface-tester.md
priority: medium
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: BO-2100a-4
status: todo
title: building-epics canonical dispatch order documents live-surface-tester at 11.8
---

# building-epics canonical dispatch order documents live-surface-tester at 11.8

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BO-2100a-4` — building-epics canonical dispatch order documents live-surface-tester at 11.8 — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BO-2100a-4`. Component: `build-orchestration`. Assigned agent: `llm-expert`. Estimated complexity: `S`. Complexity: `low`.

## Acceptance Criteria

```gherkin
Given the canonical phase-ordering description in
  templates/skills/building-epics/SKILL.md, which lists `user-surface-smoker`
  at priority 11.5 and `commit` at priority 12
When a reader consults that ordering to learn where live-app testing runs
Then `live-surface-tester` appears in both the natural-language dispatch-order
  prose and the priority table at priority 11.8
  And it is placed after `user-surface-smoker` (11.5) and before `commit` (12)
  consistently with the build-ticket.js `phaseOrder` array.
```

## Implementation Notes

```yaml
- The documented priority (11.8) and placement must match the build-ticket.js phaseOrder
  array (BO-2100a-3); the prose and the priority table must agree with each other
  and with the code.
- Must update both surfaces in the skill (natural-language dispatch-order prose AND
  the priority table), not just one, so the documentation cannot drift internally.
```

## Sign-offs

- [ ] llm-expert
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit

## Comments
