---
ac_traceability:
  id: GE-120d-5
  path: docs/acceptance-criteria/guardrail-engine/GE-120-green-means-checked/GE-120d-5.yaml
advances_current_outcome: true
agents:
  ac-fulfillment-gate: needed
  ac-validator: needed
  commit: needed
  documentation-expert: needed
  documentation-verifier: needed
  pull-request: needed
change_target: docs
complexity: low
components:
- documentation_system
- worktree_manager
created: '2026-08-25'
depends_on:
- 23_TICKET-20260825-GE-120d-2.md
- 16_TICKET-20260825-GE-120b-5.md
documentation_required: true
files_touched:
- docs/how-to/managing-pre-commit-hooks.md
- docs/how-to/verify-precommit-active.md
- templates/scripts/commit_guardian/verify_precommit_active.py
priority: high
requires_adr: false
requires_diagram: false
risk_surface: internal
roadmap_phase: phase_1
source_ac: GE-120d-5
status: todo
title: The how-to states what a prepared working copy guarantees and how to confirm
  it
---

# The how-to states what a prepared working copy guarantees and how to confirm it

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `GE-120d-5` — The how-to states what a prepared working copy guarantees and how to confirm it — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `GE-120d-5`. Component: `guardrail-engine`. Assigned agent: `documentation-expert`. Estimated complexity: `S`. Complexity: `low`.

## Acceptance Criteria

```gherkin
Given a reader who has just had a working copy prepared for them,
When they consult the how-to for managing commit-time checks,
Then it states what set-up guarantees — that the checks are active and reach the same
  verdicts as the main checkout,
And it states what set-up does when it cannot establish that: it names the missing part and
  refuses, rather than handing over an unprotected copy,
And it gives the one command the reader runs to confirm protection is active in the copy in
  front of them, and what its output looks like in both the healthy and the unhealthy case,
And it does not instruct the reader to establish any part of the protection by hand,
And it does not reintroduce the manual layout-link step that GE-120b-5 removes.
```

- [ ] AC-1: it states what set-up guarantees — that the checks are active and reach the same
- [ ] AC-2: it states what set-up does when it cannot establish that: it names the missing part and
- [ ] AC-3: it gives the one command the reader runs to confirm protection is active in the copy in
- [ ] AC-4: it does not instruct the reader to establish any part of the protection by hand
- [ ] AC-5: it does not reintroduce the manual layout-link step that GE-120b-5 removes.

## Implementation Notes

```yaml
- 'DOCUMENT: docs/how-to/managing-pre-commit-hooks.md, edited AFTER GE-120b-5''s removal
  has landed on the same file. RE-READ the file as GE-120b-5 left it before editing
  — do not work from a copy taken before that edit. Two agents editing one document
  in sequence from separate ACs is the named hazard here.'
- 'THE CONFIRM COMMAND IS THE SUBSTANTIVE ADDITION and it already exists: docs/how-to/verify-precommit-active.md
  and templates/scripts/commit_guardian/verify_precommit_active.py are both present
  (verified 2026-08-14). Use that command rather than inventing a second one, and
  cross-link the two documents so a reader is not offered two ways to check the same
  thing.'
- Show the command's output in BOTH the healthy and the unhealthy case, quoting the
  message GE-120d-2's refusal path actually emits. Do not paraphrase it — a paraphrased
  error message is one a reader cannot match against what they see, and it silently
  rots when the real message changes.
- State the set-up guarantee and the refusal behaviour AS GE-120d-2 IMPLEMENTS THEM.
  Sequence this AC after GE-120d-2 lands so the documented behaviour is the one that
  exists rather than one the author expected.
- 'ANTI-REINTRODUCTION: the file must not regain the manual layout-link step GE-120b-5
  removed, in ANY wording — including the ''if you are on WSL/NTFS, copy instead of
  symlink'' variant and any ''if the checks seem to be skipped, link .leafcutter''
  troubleshooting note. Run GE-120b-5''s absence guard before sign-off; a reviewer
  reading a diff will not reliably catch a re-added variant phrased differently from
  the deleted one.'
- No instruction anywhere in this document may tell the reader to establish any part
  of the protection by hand. Removing the layout-link step while leaving a different
  manual step satisfies the letter and not the criterion.
- The file is a tracked how-to subject to check_doc_frontmatter, check_doc_length
  and check_doc_links; edit in place and keep it passing in the same commit.
```

## Agent Contracts

### documentation-expert

Existing docs to update / cross-link:

- docs/how-to/managing-pre-commit-hooks.md (relationship: modifies | status: exists)
- docs/architecture/components/worktree-manager.md (relationship: related | status: exists)
- docs/how-to/verify-precommit-active.md (relationship: related | status: exists | relevance: The existing home of the confirm command; use it rather than inventing a second command, and keep it consistent with GE-120b-5's replacement text.)
- templates/scripts/commit_guardian/verify_precommit_active.py (relationship: related | status: exists | relevance: The script behind the confirm command, verified present in the package.)

- [ ] AC-1: how-to | docs/how-to/managing-pre-commit-hooks.md | it states what set-up guarantees — that the checks are active and reach the same

### Expects From

- **AC:** GE-120d-2
- **Contract:** The set-up refusal behaviour and its message, to document as it actually is.

## Sign-offs

- [ ] documentation-expert
- [ ] ac-validator
- [ ] ac-fulfillment-gate
- [ ] documentation-verifier
- [ ] commit
- [ ] pull-request

## Comments
