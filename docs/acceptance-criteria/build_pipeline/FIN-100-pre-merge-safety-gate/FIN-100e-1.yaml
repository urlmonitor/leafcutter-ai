id: FIN-100e-1
readiness: reviewed
priority: medium
components:
  - finalize
title: "One tracking ticket is created per pre-existing or flaky failure"
level: L2
component: finalize
status: deprecated
parent: FIN-100e
criteria: |
  Given the triage report contains entries with category "pre_existing" or "flaky"
  And the PR merge to main succeeded (Step 4 completed)
  When Step 6a iterates over the pre-existing and flaky triage entries
  Then it dispatches create-ticket for each entry
  And each ticket includes the test ID, error output, baseline SHA, and triage category
  And flaky tickets additionally note the intermittent nature and need for investigation
  And the ticket path returned by create-ticket is recorded in created_tracking_tickets
origin_agent: "business-analyst-v3"
created_by_ticket: "tickets/99_done/TICKET-20260604-FinalizeFeatureStepReorder.md"
assigned_agent: "create-ticket"
estimated_complexity: M
it_requirements: |
  - Must iterate over triageReport entries filtering for category "pre_existing" or "flaky"
  - Each create-ticket dispatch must include: test_id, baseline_sha, triage_category, baseline_run_at
  - Flaky tickets must additionally note intermittent nature and investigation need
  - Must record each returned ticket_path in created_tracking_tickets array
  - Tickets land in tickets/00_inbox/ per the create-ticket agent's default behavior
delivers_to: []
expects_from:
  - agent: "test-failure-triage"
    contract: "provides triage_report with pre_existing/flaky entries containing test_id and category"
  - agent: "finalize-feature-workflow"
    contract: "provides triageReport state variable and baseline_sha/baseline_run_at context"
doc_links:
  - path: "templates/workflows-js/finalize-feature.js"
    relevance: "Step 6a — auto-ticketing loop for pre_existing/flaky entries (lines 662-736)"
  - path: "docs/how-to/finalize-feature.md"
    relevance: "Pre-existing failures and tracking tickets section"
  - path: "templates/workflows/finalize-feature.md"
    relevance: "Step 6 — create_pre_existing_tickets_and_close"
depends_on: []
superseded_by: null
amended_by:
  - reason: "python-coder-reconciliation (EPIC-BuildPipelinePhantomRemediation/04_fin100e_autoticketing_decision.md): RETIRED, no replacement AC authored. This AC described a live create-ticket dispatch loop over pre_existing/flaky triage entries in finalize-feature.js Step 6a. That loop was deliberately DISABLED under EPIC-FinalizeFeatureHardening (create-ticket is a workflow, not a dispatchable agent -- see finalize-feature.js Step 6a comment) and replaced with an untrackedFailures[] structured report (see unit_tests/test_finalize_feature_step6a.py::TestStep6aNoFalseTicketClaim / TestWithFailuresPath, which lock in the replacement behaviour). Per this ticket's default (option b, endorsed by architect-review as lower-risk than re-enabling), the AC is superseded with no successor id: the replacement behaviour is intentionally NOT re-specified as a new AC because it is a simple reporting substitution already fully covered by the existing lock-in test, not a new product requirement. Operators create tracking tickets manually via /create-ticket per the untracked_failures report."
    by: python-coder
  - reason: "ac-store-reconciliation (EPIC-BuildPipelinePhantomRemediation): status field corrected from 'superseded_by' to 'deprecated'. Per docs/reference/ac-schema.md the 'superseded_by' lifecycle state requires the superseded_by field to name the replacement AC; this record was retired with NO successor (see the entry above), so 'superseded_by' with 'superseded_by: null' was internally inconsistent and diverged from every sibling retirement in the store, all of which carry a real successor id. 'deprecated' is the schema's state for 'retired; retained for audit', which is exactly what happened here. Enforcement behaviour is unchanged: check_ac_coverage.py excludes both states identically. unit_tests/test_finalize_feature_step6a.py::TestStep6aContractMatchesDecision::test_ac_status_matches_actual_js_dispatch_behaviour explicitly accepts either state plus an amended_by rationale, and stays green. Also recorded the lock-in test that covers the replacement behaviour in covered_by."
    by: general-purpose
covered_by:
  - unit_tests/test_finalize_feature_step6a.py::TestStep6aContractMatchesDecision
implemented_by: []
change_target: pipeline
risk_surface: internal
