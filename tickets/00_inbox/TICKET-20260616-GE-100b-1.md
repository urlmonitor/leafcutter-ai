---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  pr-reviewer: needed
  pull-request: needed
  python-coder: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
components:
- commit_guardian
created: '2026-06-16'
depends_on:
- GE-100b
files_touched:
- docs/acceptance-criteria/guardrail-engine/GE-100-code-quality-hooks/GE-100b.yaml
- docs/pre-commit-hooks.md
priority: high
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: GE-100b-1
status: todo
title: jscpd hook filters results to only report clones involving staged files
---

# jscpd hook filters results to only report clones involving staged files

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `GE-100b-1` — jscpd hook filters results to only report clones involving staged files — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `GE-100b-1`. Component: `guardrail-engine`. Assigned agent: `python-coder`. Estimated complexity: `S`.

## Acceptance Criteria

```gherkin
Given the commit guardian configuration has the duplicate_code section with:
  enabled: true
  strict: true
  min_lines: 5
  threshold_percent: 5
And the jscpd binary is installed (version 3.x)
And the working tree contains a duplicate code pair between:
  - file_a.py (NOT staged) lines 10-25
  - file_b.py (NOT staged) lines 30-45
And the developer has staged only file_c.py which contains no duplicates
When the duplicate code hook runs at pre-commit time
Then the hook does NOT report the file_a.py / file_b.py duplicate pair
And the hook exits with code 0
And no warnings are emitted about unstaged-file duplicates
```

## Sign-offs

- [ ] python-coder
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit
- [ ] pull-request

## Comments
