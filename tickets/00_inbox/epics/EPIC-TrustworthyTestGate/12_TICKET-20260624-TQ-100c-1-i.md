---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  pr-reviewer: needed
  pull-request: not_needed
  python-coder: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
components:
- testing_quality
created: '2026-06-24'
depends_on:
- TQ-100c-1
files_touched:
- scripts/ac_store/test_enforcement.py
- tests/testing_quality/test_ac_linkage.py
priority: high
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: TQ-100c-1-i
status: todo
title: Removing a covers tag makes a test unlinked-and-enforced, not informational
---

# Removing a covers tag makes a test unlinked-and-enforced, not informational

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `TQ-100c-1-i` — Removing a covers tag makes a test unlinked-and-enforced, not informational — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `TQ-100c-1-i`. Component: `testing-quality`. Assigned agent: `python-coder`. Estimated complexity: `S`.

## Acceptance Criteria

```gherkin
Given a failing test that was tagged "# covers: <AC-ID>" linking it to a
  not-done AC, observed in a first run to be reported informationally,
When the "# covers:" tag is removed from the test and the suite is run again,
Then the now-untagged test is enforced (its failure counts as a real failure
  and drives the overall outcome non-passing)
And removing the tag does not make the test informational
And there is no edit to the test that converts an enforced test into an
  informational one by dropping its link.
```

## Test Requirements

- `test_removing_covers_tag_enforces_test`: Removing a test's '# covers:' tag makes it unlinked-and-enforced, never informational.

## Sign-offs

- [ ] python-coder
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit

## Comments
