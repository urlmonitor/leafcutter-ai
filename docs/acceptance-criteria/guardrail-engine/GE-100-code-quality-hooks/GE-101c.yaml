id: GE-101c
components:
  - guardrail-engine
readiness: approved
priority: high
title: "diff-cover hook warns on stale coverage artifact and degrades gracefully in shallow clones"
component: guardrail-engine
level: L2
status: active
req_status: active
work_status: done
created_by: "docs/acceptance-criteria/guardrail-engine/GE-100-code-quality-hooks/GE-100.yaml"
criteria: |
  Given the commit guardian configuration has the diff_coverage section with:
    enabled: true
    strict: true
    min_coverage_percent: 80
    coverage_xml_path: "coverage.xml"
    max_age_seconds: 3600
  And the diff-cover binary is installed
  And coverage.xml exists but was last modified 7200 seconds ago (exceeds max_age_seconds)
  When the diff-cover hook runs at pre-commit time
  Then the hook emits a warning to stderr stating that coverage.xml is stale
    (age: 7200s, max allowed: 3600s)
  And the hook exits with code 0 (fail-open despite strict mode)
  And the warning includes guidance to regenerate the coverage artifact

  ---

  Given the repository is a shallow clone (depth-limited fetch)
  And the diff-cover binary is installed
  And coverage.xml exists and is fresh
  When the diff-cover hook runs at pre-commit time
  Then the hook detects the shallow clone condition
  And the hook exits with code 0 (fail-open)
  And it emits an advisory message explaining that diff-cover requires
    full history and suggesting fetch-depth: 0
depends_on:
  - GE-101
  - GE-101b
doc_links:
  - path: "docs/acceptance-criteria/guardrail-engine/GE-100-code-quality-hooks/GE-101.yaml"
    relationship: parent
    status: exists
  - path: "docs/pre-commit-hooks.md"
    relationship: describes
    status: exists
  - path: "docs/how-to/managing-pre-commit-hooks.md"
    relationship: describes
    status: exists
assigned_agent: python-coder
estimated_complexity: M
delivers_to: null
expects_from:
  ac_id: GE-101a
  contract: "The artifact freshness check function that reads coverage.xml mtime and compares against max_age_seconds"
origin_agent: BrainCandy
created: 2026-06-11
amended_by:
  - reason: "split: GE-100 -> GE-100 + GE-101, moved to GE-101 (intermediate Pattern C), 2026-06-16"
  - reason: "rename: GE-100f -> GE-101c, structural parent corrected to GE-101, 2026-06-18"
superseded_by: null
covered_by:
  - GE-101c-1
implemented_by:
- tickets/00_inbox/epics/EPIC-CodeQualityHooks/12_TICKET-20260616-GE-100f.md
change_target: code
risk_surface: internal
