id: BO-1600d-1
readiness: draft
priority: medium
title: "Recovery is human-invoked and confirmation-gated — it never runs inside the automatic drive loop"
component: build-orchestration
level: L2
status: active
req_status: draft
work_status: todo
created_by: "docs/acceptance-criteria/build-orchestration/BO-1600-safe-concurrent-dispatch/BO-1600d.yaml"
criteria: |
  Given the corruption-detection halt from BO-1600c has stopped a parallel drive
    and reported that the shared repository is in a damaged state
  When a parallel drive loop is running
  Then the drive loop never invokes the guided recovery on its own — recovery is
    only ever started by an explicit human action (a person running the recovery
    entry point), never by the orchestrator continuing automatically
  And when the recovery entry point is started, it takes no action that changes the
    repository until the human confirms; absent a confirmation it makes no git write
  And the halt path from BO-1600c only reports the damage and points the human at the
    recovery entry point — it does not chain into running recovery itself
depends_on:
  - BO-1600d
doc_links:
  - path: "docs/acceptance-criteria/build-orchestration/BO-1600-safe-concurrent-dispatch/BO-1600d.yaml"
    relationship: parent
    status: exists
  - path: "docs/acceptance-criteria/build-orchestration/BO-1600-safe-concurrent-dispatch/BO-1600c.yaml"
    relationship: relates
    status: exists
  - path: "docs/acceptance-criteria/build-orchestration/PROJECT_CONTEXT.md"
    relationship: describes
    status: exists
  - path: "docs/architecture/agent_delivery_workflows.md"
    relationship: describes
    status: exists
assigned_agent: python-coder
estimated_complexity: M
it_requirements:
  - "Recovery must be a SEPARATE human-invoked entry point (a distinct command/script surface), NOT a step wired into the automatic drive loop or the BO-1600c halt path; the halt path must only report and reference recovery, never call it."
  - "On start, the entry point must be inert with respect to the repository: it makes no git write of any kind until the human has explicitly confirmed (this is the gate; the plan-then-confirm mechanics are BO-1600d-2)."
  - "Must handle errors gracefully per the project error handling policy (CLAUDE.md Rules 1-3): wrap git/filesystem probes in try/except with a specific exception type and log at WARNING or higher; an errored probe must not be treated as permission to proceed."
  - "PORTABLE per ADR-001: the entry point ships to consumer projects via build.py (see BO-1600d-4), so the human-invoked-only + gate guarantee holds in any installing project — it must NOT depend on the leafcutter repo's own root CLAUDE.md."
delivers_to:
  agent: python-coder
  contract: "A human-invoked recovery entry point that (a) is never called by the automatic drive loop or the BO-1600c halt path, and (b) performs no repository write before explicit human confirmation. This entry point is the shell that the dry-run-first plan/confirm behavior (BO-1600d-2) and the recovery-step engine (BO-1600d-3) are wired into."
expects_from: null
origin_agent: business-analyst-v3
created: 2026-07-01
amended_by: []
superseded_by: null
covered_by: []
implemented_by: []
notes: |
  Origin: EPIC-FinalizeFeatureHardening incident (parallel supervisors produced a
  0-byte loose object corrupting the shared worktree index). BO-1600c owns
  detect-and-halt; BO-1600d is recovery-only. Per BO PROJECT_CONTEXT, recovery is
  a separate explicitly-requested capability and must stay OUT of the automatic
  drive loop — this AC pins the human-invoked-only + never-auto-run boundary.
