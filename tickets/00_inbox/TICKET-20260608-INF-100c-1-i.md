---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  pr-reviewer: needed
  pull-request: needed
  python-coder: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
components:
- infrastructure
created: '2026-06-08'
depends_on:
- INF-100c-1
files_touched:
- docs/architecture/feedback-lifecycle.md
priority: medium
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: INF-100c-1-i
status: todo
title: Config resolution in a worktree of a deployed project
---

# Config resolution in a worktree of a deployed project

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `INF-100c-1-i` — Config resolution in a worktree of a deployed project — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `INF-100c-1-i`. Component: `infrastructure`. Assigned agent: `python-coder`. Estimated complexity: `S`.

## Acceptance Criteria

```gherkin
Given a git worktree is created from a deployed project that has .leafcutter/,
When submit_feedback.py runs from the worktree's .leafcutter/scripts/feedback/,
Then config resolution does not walk past the .leafcutter/ directory into the worktree's parent,
And the config is found at the worktree's own .leafcutter/config/feedback_categories.yaml.
```

## Sign-offs

- [ ] python-coder
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit
- [ ] pull-request

## Comments
