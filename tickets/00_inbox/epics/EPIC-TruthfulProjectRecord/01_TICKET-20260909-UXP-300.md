---
ac_traceability:
  id: UXP-300
  path: docs/acceptance-criteria/ux-prototyping/UXP-300-product-truth-store/UXP-300.yaml
advances_current_outcome: true
agents:
  ac-fulfillment-gate: needed
  ac-validator: needed
  architect-review: not_needed
  commit: needed
  documentation-expert: not_needed
  documentation-verifier: not_needed
  frontend-coder: not_needed
  pr-reviewer: not_needed
  pull-request: not_needed
  python-coder: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: signed_off
  user-surface-smoker: not_needed
change_target: config
complexity: medium
components:
- ux_prototyping
created: '2026-09-09'
depends_on: []
files_touched:
- docs/product-truth/README.md
priority: medium
requires_adr: false
requires_diagram: false
risk_surface: internal
roadmap_phase: phase_1
source_ac: UXP-300
status: todo
title: Product-truth artifacts live in docs/product-truth with one canonical dataset
  per entity per component
---

# Product-truth artifacts live in docs/product-truth with one canonical dataset per entity per component

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `UXP-300` — Product-truth artifacts live in docs/product-truth with one canonical dataset per entity per component — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `UXP-300`. Component: `ux-prototyping`. Assigned agent: `python-coder`. Estimated complexity: `M`. Complexity: `medium`.

## Acceptance Criteria

```gherkin
Given the product-truth store,
When a flow, mock-data, or mockup artifact is authored,
Then it lives under docs/product-truth/ (flows/, mock-data/, mockups/),
And each entity has exactly one canonical mock-data artifact per component (extend, never duplicate),
And flows link to acceptance criteria via step.implements (flow -> AC), while AC -> flow is derived,
And scripts/validate_product_truth.py passes for the store.
```

- [ ] AC-1: it lives under docs/product-truth/ (flows/, mock-data/, mockups/)
- [ ] AC-2: each entity has exactly one canonical mock-data artifact per component (extend, never duplicate)
- [ ] AC-3: flows link to acceptance criteria via step.implements (flow -> AC), while AC -> flow is derived
- [ ] AC-4: scripts/validate_product_truth.py passes for the store.

## Test Requirements

```yaml
tests:
- name: test_every_registered_artifact_resolves_under_a_declared_store_directory
  file: unit_tests/product_truth/test_uxp_300.py
  covers:
  - UXP-300
  asserts: Every artifact the index registers resolves to a path under flows/, mock-data/
    or mockups/; a registration pointing outside those three is reported, not ignored.
  framework: unittest
  type: integration
  angle: real_artifact
- name: test_one_canonical_dataset_per_entity_per_component
  file: unit_tests/product_truth/test_uxp_300.py
  covers:
  - UXP-300
  asserts: No entity is carried by two mock-data artifacts within the same component;
    a second dataset for an entity already covered is rejected rather than merged,
    so the store grows by extension and never by duplication.
  framework: unittest
  type: integration
  angle: real_artifact
- name: test_flow_to_ac_is_authored_and_the_reverse_edge_is_derived
  file: unit_tests/product_truth/test_uxp_300.py
  covers:
  - UXP-300
  asserts: 'step.implements is the only authored link direction: an AC''s product_truth
    back-reference is recomputed from the flow steps that name it, and a hand-written
    back-reference no flow supports is reported.'
  framework: unittest
  type: integration
  angle: seam
- name: test_the_store_passes_its_own_validator
  file: unit_tests/product_truth/test_uxp_300.py
  covers:
  - UXP-300
  asserts: validate_product_truth.py exits zero against the committed store, and the
    run states how many artifacts it examined so a pass over an empty store is distinguishable
    from a real one.
  framework: unittest
  type: integration
  angle: deployed
- name: test_uxp_300_reachable_from_entry_point
  file: unit_tests/product_truth/test_uxp_300.py
  covers:
  - UXP-300
  angle: reachability
  asserts: 'REQUIRED — invoke the production entry point (CLI, hook, slash command,
    workflow dispatch, or main()) as a subprocess/dispatch and assert the new behaviour
    actually occurs. Do NOT satisfy this by importing the function directly. The AC
    authored no test_spec, so the entry point is not declared: resolve it before writing
    this test, and do not delete this entry.'
```

## Implementation Notes

```yaml
'The store location and the flow->AC linking direction are conventions enforced by

  the validator and consumed by the Leafcutter Atlas /flows view. Mock data is the

  single source of truth that both populates mockups and seeds test fixtures.

  '
```

## Sign-offs

- [x] test-writer — 2026-09-09 15:59
- [ ] python-coder
- [ ] test-runner
- [ ] ac-validator
- [ ] ac-fulfillment-gate
- [ ] commit

## Comments

### 2026-09-09 15:59 — test-writer (status: ok)
feedback-id: fb_2026-09-09_164246c7
completion_manifest:
  tests_written: true
  red_baseline_captured: true
  cross_layer_seam_answer:
    result: covered
    producing_side: "docs/product-truth/scripts/generate_product_truth.py (build_by_entity / write_ac_product_truth — the single writer of index.json's by_entity and by_ac derived maps)"
    consuming_side: "docs/product-truth/scripts/validate_product_truth.py (main() / _check_index / _check_product_truth — reads the real, freshly-written index.json back)"
  reachability_entry_point_answer:
    result: resolved
    entry_point: "python docs/product-truth/scripts/validate_product_truth.py (CLI via subprocess — main() is guarded by `if __name__ == \"__main__\": sys.exit(main())` with its own argparse parser; the same script is also wired as the \"Product-Truth Store Validation\" pre-commit hook entry in .pre-commit-config.yaml via run_hook.py, but Step 1 of the resolution procedure resolves the CLI-script leg first since it applies)"
red_baseline:
  - test_name: test_every_registered_artifact_resolves_under_a_declared_store_directory
    file: unit_tests/product_truth/test_uxp_300.py
    error: "AssertionError: validator must report an artifact whose registered path escapes the declared store directories; expected substring \"[index] artifact 'fern-and-fig/customer-buys-a-plant' path 'not-a-declared-directory/bogus.json' is outside the declared store directories (flows/, mock-data/, mockups/)\" not found in output"
  - test_name: test_one_canonical_dataset_per_entity_per_component
    file: unit_tests/product_truth/test_uxp_300.py
    error: "AssertionError: validator must report the duplicate canonical mock-data artifact for the same entity+component; expected substring \"entity 'Plant' already has a canonical mock-data artifact for component 'ux-prototyping': 'fern-and-fig/catalog'; 'fern-and-fig/catalog-dup' is a duplicate\" not found in output (validator exited 0)"
  - test_name: test_flow_to_ac_is_authored_and_the_reverse_edge_is_derived
    file: unit_tests/product_truth/test_uxp_300.py
    error: "AssertionError: the real validator, reading the real generator's freshly-written index.json, must surface the duplicate canonical dataset; same expected substring as above not found after generate_product_truth.py + validate_product_truth.py both exited 0"
  - test_name: test_deployed_validator_degrades_informatively_on_an_empty_store
    file: unit_tests/product_truth/test_uxp_300.py
    error: "AssertionError: 'Traceback (most recent call last):' unexpectedly found in output — FileNotFoundError from generate_product_truth.py:_load_json(STORE / \"index.json\") propagates uncaught out of validate_product_truth.py main() when the deployed store has no data"
  - test_name: test_uxp_300_reachable_from_entry_point
    file: unit_tests/product_truth/test_uxp_300.py
    error: "AssertionError: 0 == 0 : the real CLI entry point must exit non-zero when the store carries a duplicated canonical mock-data artifact; returncode was 0"
Wrote unit_tests/product_truth/test_uxp_300.py (6 tests, unittest, real-artifact subprocess integration style — no mocks of the producer/consumer scripts themselves). Empirically confirmed against the current tree: (1) no check exists yet for an artifact's registered `path` resolving under a declared store directory (flows/, mock-data/, mockups/); (2) generate_product_truth.py's build_by_entity() silently lets a second mock-data artifact in the same component overwrite the canonical entry for an entity instead of rejecting the duplicate, and validate_product_truth.py has no check that flags it either directly or after a real generator round trip; (3) the DEPLOYED copy of validate_product_truth.py (via build_phases.build_product_truth()) raises an uncaught FileNotFoundError traceback rather than degrading informatively when the store has no data yet authored; (4) the same duplicate-dataset defect is reachable via the real CLI entry point's exit code. One test (`test_the_store_passes_its_own_validator`, angle: deployed) is a locked-down regression that already passes today — the scripts/schemas deploy-glob mechanism itself has no gap; it is kept as the positive-count leg alongside its red sibling per README §8 ("varying the population" / "a check that examined nothing must not look like a check that found nothing"). Full verification run: `AC_ENFORCE_STRICT=1 python -m pytest unit_tests/product_truth/test_uxp_300.py -v` — plain (non-strict) runs are masked to xfail by this repo's `pytest_ac_enforcement` plugin because UXP-300's work_status is not yet `done` (see unit_tests/README.md §9); AC_ENFORCE_STRICT=1 surfaces the real 5 failed / 1 passed result the coder phase must turn fully green (the 1 passing test excluded from the red_baseline below per the "do not include passing tests" rule). No production code beyond the new test file and its `__init__.py` was touched.
