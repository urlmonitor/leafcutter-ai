id: GE-120e-1
title: "The one contested requirement number is resolved without breaking what cites it"
component: guardrail-engine
components:
  - commit_guardian
  - ac_store
level: L2
status: active
req_status: draft
work_status: todo
readiness: approved
priority: high
roadmap_phase: phase_1
criteria: |
  Given the requirement collection contains two records claiming the identifier "GE-119": a
    goal-level record owning a folder of 33 files, and a single parentless detail-level record at
    the namespace root whose subject is the contract-shrinking guard,
  And nine test functions carry a coverage tag naming "GE-119", all of them written for the
    parentless record,
  And a guard source file and two release notes cite "GE-119" — one release note meaning the
    parentless record, the other meaning the goal record,
  When the repair is complete,
  Then exactly one record in the collection claims "GE-119", and it is the goal record,
  And every one of the 33 files in that goal record's folder is unchanged,
  And the parentless record claims an identifier in the same namespace that no other artifact
    claims, and that was not previously claimed by any retired artifact,
  And it declares a parent that exists, and that parent lists it among its children,
  And all nine coverage tags name the identifier the moved record now claims, so each tag
    resolves to exactly one record,
  And the guard source citation names the moved record's new identifier,
  And each remaining citation of "GE-119" in a dated historical record either genuinely refers to
    the goal record, or states the identifier the moved record now carries, so no reader is left
    holding a citation that resolves to the wrong work,
  And no citation of either identifier anywhere in the repository resolves to zero records or to
    more than one.

  Coverage note: covered by executing the whole-collection uniqueness pass and a repository-wide
  citation resolution over the repaired tree, asserting one claimant for the old identifier, one
  for the new, and no unresolved or multiply-resolved citation of either. Asserting only that the
  file was renamed does not cover it — the references are where a renumbering does its damage.
depends_on:
  - GE-120e
doc_links:
  - path: docs/acceptance-criteria/guardrail-engine/GE-119.yaml
    relationship: modifies
    status: exists
    relevance: "The parentless detail-level claimant — the artifact that moves. Single file, no children, authored by /quick-fix from a different working copy."
  - path: docs/acceptance-criteria/guardrail-engine/GE-119-green-means-checked/GE-119.yaml
    relationship: context
    status: exists
    relevance: "The goal-level claimant — the artifact that keeps the identifier. Its 33-file tree must not be touched."
  - path: scripts/adr_refs.py
    relationship: context
    status: exists
    relevance: "A worked precedent for the reference half of a renumbering: it rewrites slug-qualified citations mechanically, refuses to rewrite bare ambiguous ones, and deliberately excludes dated historical records. The same three-way treatment applies here."
  - path: docs/conventions/adr-numbering.md
    relationship: context
    status: exists
    relevance: "The only written account in this repository of what a renumbering actually costs — the 2026-08-13 decision-number repair: six renames, roughly 430 citation rewrites across about 200 files, one gap filled, two records written retroactively. Read it before estimating this one."
  - path: docs/architecture/adrs/ADR-029-adr-number-collision-prevention.md
    relationship: context
    status: exists
    relevance: "Carries the operative free-number rule this repair must obey: a number is free only when it has NEITHER a file NOR a citation. GE-121 must be checked against both."
  - path: docs/reference/ac-schema.md
    relationship: context
    status: exists
    relevance: "The identifier format, the allocation algorithm and the never-reuse-a-retired-slot rule for this namespace, plus the parent back-link requirement the moved record must satisfy."
assigned_agent: python-coder
estimated_complexity: M
it_requirements:
  - "The nine coverage tags are the mechanically load-bearing part and they are in ONE file: `unit_tests/commit_guardian/test_ge_119_contract_shrinking_rename_aware.py`, as nine `# covers: GE-119` comments. The done-proof oracle resolves these tags, so leaving them pointing at the old identifier after the move silently re-points nine passing tests at the goal record — turning a repair into a false coverage claim for a tree the tests do not exercise."
  - "The identifier chosen must satisfy ADR-029's operative rule as applied to this namespace: free means NEITHER a file NOR a citation, not merely 'no file exists'. Reusing a number that text still cites converts a dangling reference into a confidently wrong one, which is worse than the ambiguity being repaired."
  - "WHEN LISTING TAKEN IDENTIFIERS, scan the loose files as well as the folders. Verified 2026-08-17: `GE-114-1.yaml` through `GE-114-4.yaml`, `GE-115.yaml` and the `GE-119.yaml` being moved all sit at the namespace root alongside the feature folders, so a folder-only listing under-reports what is taken and is a plausible way to mint a second collision while repairing the first. GE-121 is free by both tests as of 2026-08-17."
  - "The AC-store gates will police the move and must be satisfied, not bypassed: `check-ac-schema`, `check-ac-parent-covered-by` (the new parent must list the moved record in `covered_by`), `check-ac-tree-limits` (the chosen parent must have room under the child cap), `check-ac-circular-deps` and `check-done-proof` all run on `docs/acceptance-criteria/**/*.yaml` at commit time and again in the `AC store valid` CI job. Choosing a parent that is already at its child cap turns a one-file move into a tree restructure."
  - "Rewrite citations with the three-way treatment `scripts/adr_refs.py` already implements rather than a blanket search-and-replace: qualified citations are rewritten mechanically, bare ambiguous ones are examined individually because a bare 'GE-119' today returns citations meaning BOTH records, and dated historical records are deliberately left alone because repointing them would make the record claim something that did not happen. A blanket replace across the repository would rewrite the release note that legitimately means the goal record."
  - "Changelog entries and release notes are dated historical records. The one meaning the moved record gets a clarifying note stating the new identifier rather than a rewritten citation; the one meaning the goal record is untouched."
  - "Idempotency: re-running any scripted portion of this repair over the already-repaired tree must make no further change and must not re-rewrite an already-rewritten citation. Verify by running twice and diffing."
  - "Do not delete either claimant. Both describe real, wanted work — the goal record is an active five-child tree, the parentless record documents a live defect covered by nine passing tests."
  - "This AC's ONLY prerequisite is GE-120e; it does not depend on the pass being built. Doing it first makes GE-120a-1's own store clean, and it supplies GE-120c-3 with a real repair to write its procedure from."
test_spec:
  - name: test_old_identifier_has_exactly_one_claimant_after_repair
    target_dir: unit_tests/commit_guardian/
    framework: unittest
    type: behavioral
    description: "Over the repaired tree, exactly one record claims GE-119 and it is the goal-level record; the whole-collection uniqueness pass reports no finding for that identifier."
  - name: test_moved_record_claims_a_free_identifier_with_a_real_parent
    target_dir: unit_tests/commit_guardian/
    framework: unittest
    type: behavioral
    description: "The moved record claims an identifier no other artifact claims and that no citation refers to, declares a parent that exists, and appears in that parent's covered_by list."
  - name: test_goal_records_thirty_three_files_are_unchanged
    target_dir: unit_tests/commit_guardian/
    framework: unittest
    type: behavioral
    description: "Every file in the goal record's folder is byte-identical to its pre-repair content — the cheap direction was actually taken."
  - name: test_all_nine_coverage_tags_resolve_to_exactly_one_record
    target_dir: unit_tests/commit_guardian/
    framework: unittest
    type: behavioral
    description: "Each of the nine covers tags in test_ge_119_contract_shrinking_rename_aware.py names the moved record's new identifier, and each resolves to exactly one record in the store."
  - name: test_no_citation_of_either_identifier_resolves_to_zero_or_many
    target_dir: unit_tests/commit_guardian/
    framework: unittest
    type: behavioral
    description: "A repository-wide citation resolution over both identifiers finds no citation resolving to zero records and none resolving to more than one — the assertion the coverage note says a rename alone does not satisfy."
  - name: test_dated_historical_records_are_not_repointed
    target_dir: unit_tests/commit_guardian/
    framework: unittest
    type: behavioral
    description: "The release note that legitimately means the goal record is unchanged, and the one meaning the moved record carries a clarifying statement rather than a rewritten citation — so a blanket search-and-replace fails this test."
test_rationale: |
  The coverage note is explicit that asserting the rename does not cover this AC, because the
  references are where a renumbering does its damage — so five of the six descriptors assert on
  citations rather than on files. The nine-coverage-tags descriptor is the highest-value one:
  those tags are machine-resolved by the done-proof oracle, so leaving them behind does not merely
  mislead a reader, it silently re-points nine passing tests at a tree they do not exercise,
  manufacturing exactly the false coverage claim this repository's guardrails exist to prevent.
  The dated-historical-records descriptor is written to FAIL on a blanket search-and-replace,
  which is the fast implementation and the one that would rewrite the release note that
  legitimately means the goal record.
delivers_to: null
expects_from: null
origin_agent: business-analyst
created: 2026-08-17
amended_by:
- reason: it-po-technical-enrichment
  entry: 'Added ac_store to components. Added it_requirements naming the concrete
    surfaces: the nine covers tags are in one file (unit_tests/commit_guardian/test_ge_119_contract_shrinking_rename_aware.py)
    and are machine-resolved by the done-proof oracle, so leaving them behind re-points
    nine passing tests at the wrong tree; the free-identifier test is ADR-029''s neither-a-file-nor-a-citation
    rule; the taken-identifier listing must include the loose namespace-root files
    (GE-114-1..4, GE-115 and the record being moved), with GE-121 verified free by
    both tests as of 2026-08-17; the AC-store gates that will police the move, including
    the child cap on whichever parent is chosen; the three-way citation treatment
    from adr_refs.py, which a blanket search-and-replace fails; run-twice idempotency.
    Added test_spec, test_rationale and three doc_links. criteria, title, agent and
    complexity unchanged.'
  by: it-po
- action: approved
  agent: approve_acs
  date: '2026-08-17'
superseded_by: null
covered_by: []
implemented_by: []
change_target: docs
risk_surface: internal
notes: |
  EXPECTED RESOLUTION, stated as guidance and consistent with the criterion above: move the
  parentless record to GE-121 — verified free at authoring time, since GE-120 was taken by this
  tree — and give it a parent. That is the cheap direction: one file with no children moves,
  while the 33-file tree stays put. The criterion is deliberately written against "an identifier
  no other artifact claims" rather than against the literal GE-121, so that a later mint of
  GE-121 by another session does not invalidate the acceptance test.

  WHEN LISTING TAKEN IDENTIFIERS IN THIS NAMESPACE, scan the loose files as well as the folders.
  GE-114-*, GE-115 and the GE-119 record being moved all sit at the namespace root, so a
  folder-only listing under-reports what is taken. That listing error is a plausible way to
  produce a second collision while repairing the first.

  REPAIR IS NOT FREE, AND THIS CASE PROVES IT. The parentless record looks isolated, but it is
  cited from nine coverage tags in one test module, from a guard source file, and from a release
  note — and the same bare identifier is cited by 33 requirement files and a second release note
  meaning the OTHER record. So a bare "GE-119" citation is genuinely ambiguous today, which is
  the goal's own thesis demonstrated on its own worked example. Any tool resolving a coverage tag
  named GE-119 currently gets two answers.

  DELETING EITHER CLAIMANT IS NOT THE ANSWER. Both describe real, wanted work: the goal record is
  an active five-child tree, and the parentless record documents a live defect in the
  contract-shrinking guard that is covered by nine passing tests.

  THE MOVED RECORD IS PARENTLESS TODAY and would be reported by an orphan scan regardless. Giving
  it a parent is required by the criterion because a record with a new identifier and no parent
  simply relocates the problem, and because the store's own back-link gate would reject it.

  IT PO CONFIRMATION (2026-08-17): all of the BA's measured facts verified independently. The nine
  covers tags are in unit_tests/commit_guardian/test_ge_119_contract_shrinking_rename_aware.py;
  the loose namespace-root files are GE-114-1..4, GE-115 and GE-119; GE-121 owns no file and is
  cited by nothing. This AC depends only on GE-120e and is the cheapest first move in the tree.
