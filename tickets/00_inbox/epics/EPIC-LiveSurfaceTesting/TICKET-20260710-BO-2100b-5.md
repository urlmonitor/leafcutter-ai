---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: needed
  pr-reviewer: needed
  pull-request: not_needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
complexity: low
components:
- build_orchestration
created: '2026-07-10'
depends_on:
- BO-2100b
- BO-2100b-1
files_touched:
- scripts/config_loader.py
- docs/reference/skills-config-fields.md
- docs/architecture/adrs/ADR-020-live-surface-tester.md
priority: medium
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: BO-2100b-5
status: todo
title: Reference documentation for the live_surface_testing config block
---

# Reference documentation for the live_surface_testing config block

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BO-2100b-5` — Reference documentation for the live_surface_testing config block — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BO-2100b-5`. Component: `build-orchestration`. Assigned agent: `documentation-expert`. Estimated complexity: `S`. Complexity: `low`.

## Acceptance Criteria

```gherkin
Given the `live_surface_testing` block in skills_config.json
When a maintainer needs the authoritative field list
Then reference documentation exists that names each field
  (`enabled`, `startup_command`, `health_check_path`, `startup_timeout_seconds`,
  `port_range_start`, `port_range_end`), its type, its default, and its meaning,
  And it states that an absent block means the capability is disabled.
```

## Implementation Notes

```yaml
- Must document each field (`enabled`, `startup_command`, `health_check_path`, `startup_timeout_seconds`,
  `port_range_start`, `port_range_end`) with its type, default, and meaning.
- Must state that an absent live_surface_testing block means the capability is disabled
  (matching the BO-2100b-1-i safe-default behavior).
- The field list, types, and defaults must match config_loader.py validation so the
  reference cannot drift from the enforced schema.
```

## Sign-offs

- [ ] documentation-expert
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit

## Comments
