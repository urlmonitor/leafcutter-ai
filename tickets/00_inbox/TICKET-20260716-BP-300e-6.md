---
ac_traceability:
  id: BP-300e-6
  path: docs/acceptance-criteria/build_pipeline/BP-300-workflow-orchestration/BP-300e-6.yaml
advances_current_outcome: true
agents:
  architect-review: signed_off
  commit: failed
  llm-expert: needed
  pr-reviewer: failed
  pull-request: failed
change_target: prompt
complexity: medium
components:
- build_pipeline
created: '2026-07-16'
depends_on: []
files_touched:
- templates/agents/status-checker.md
- templates/agents/ac-triage.md
- templates/agents/pt-classifier.md
- templates/agents/worktree-agent.md
- unit_tests/test_bp_300e_6_agent_json_only_contract.py
priority: high
requires_adr: false
requires_diagram: false
risk_surface: contract_boundary
roadmap_phase: phase_1
source_ac: BP-300e-6
status: todo
title: Agents dispatched for a machine-parsed result return only the structured payload,
  with any anomaly carried inside it
---

# Agents dispatched for a machine-parsed result return only the structured payload, with any anomaly carried inside it

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BP-300e-6` — Agents dispatched for a machine-parsed result return only the structured payload, with any anomaly carried inside it — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BP-300e-6`. Component: `build-pipeline`. Assigned agent: `llm-expert`. Estimated complexity: `M`. Complexity: `medium`.

## Acceptance Criteria

```gherkin
Given a delivery workflow (/plan-feature, /build-feature, /finalize-feature,
  or the epic and ticket workflows behind them) dispatches an internal helper
  agent whose reply the workflow will machine-parse as a structured result
  (a single JSON object or array) — for example the status-checker agent that
  reports ticket state,
When that agent completes its task and returns its reply,
Then the agent's reply is exactly one structured JSON value (a single object or
  array) and nothing else — no leading or trailing prose, no markdown headings
  (such as a "## Anomalies" section), no explanatory notes, and no commentary
  before or after the payload,
And any anomaly, warning, or caveat the agent needs to report is carried INSIDE
  the structured payload (for example in an "anomalies" or "warnings" field of
  the returned object) rather than appended as free text outside it,
And this contract applies to every agent used as a machine-parsed producer by
  these workflows — it is a requirement on the class "agents whose reply a
  delivery workflow parses", not on any single named agent.
```

## Implementation Notes

```yaml
- 'Update templates/agents/status-checker.md so that, when dispatched for a machine-parsed
  result, its output instruction reads: return ONLY the single JSON object/array —
  no prose and no markdown headings (including the existing ''## Anomalies'' and ''##
  Verdict'' sections) before or after the payload. status-checker is the confirmed
  trigger case: it currently appends an ''## Anomalies'' markdown section after its
  structured output.'
- Anomalies/warnings/caveats must be relocated to a field INSIDE the returned JSON
  payload (e.g. an 'anomalies' or 'warnings' array) rather than emitted as a trailing
  markdown section. Do not remove the agent's ability to report anomalies — change
  only where they are carried.
- 'Identify the full class of machine-parsed producer agents by grepping the delivery
  workflow scripts (templates/workflows-js/*.js) for agent dispatches whose result
  is JSON.parse''d, and apply the same output contract to every member. Known members
  at authoring time: status-checker (primary; ~48 dispatch sites), ac-triage, pt-classifier,
  worktree-agent. Do NOT hard-code this list in the templates — re-derive it from
  the dispatch sites so newly added producers inherit the contract (data-driven, not
  by-name).'
- Edit only the canonical source templates under templates/agents/*.md; rebuild via
  build.py so the deployed .claude/agents/*.md copies stay byte-identical (compiled-output
  drift guard) — never hand-edit the deployed copy.
- Preserve each agent's human-facing / sign-off behavior (e.g. the signoff-skill path
  when a ticket_path is provided). The 'return ONLY JSON' contract applies to the
  machine-parsed dispatch path; it must not strip the sign-off or completion-manifest
  behavior these agents also perform.
- 'This AC is defense-in-depth and independent of the parseAgentJson tolerant reader
  (BP-300e-1): it hardens the producer side while BP-300e-1 hardens the consumer side
  of the same contract. The template change must not assume, require, or be blocked
  by that helper, and must remain correct whether or not BP-300e-1 has shipped.'
```

## Test Requirements

```yaml
tests:
- name: test_status_checker_instructs_json_only
  file: unit_tests/test_bp_300e_6_agent_json_only_contract.py
  covers:
  - BP-300e-6
  asserts: status-checker.md's machine-parsed dispatch path instructs "return ONLY
    the JSON object/array, no prose/headings before or after"; no trailing '## Anomalies'
    markdown section remains in its output contract.
  framework: unittest
  type: unit
- name: test_status_checker_carries_anomalies_inside_payload
  file: unit_tests/test_bp_300e_6_agent_json_only_contract.py
  covers:
  - BP-300e-6
  asserts: status-checker.md's documented output carries anomalies/warnings as a field
    INSIDE the JSON payload rather than as a trailing prose section.
  framework: unittest
  type: unit
- name: test_all_machine_parsed_producers_carry_json_only_contract
  file: unit_tests/test_bp_300e_6_agent_json_only_contract.py
  covers:
  - BP-300e-6
  asserts: 'Class-level check derived from the JSON.parse''d dispatch sites in templates/workflows-js/*.js
    (status-checker, ac-triage, pt-classifier, worktree-agent): every machine-parsed
    producer template carries the return-only-JSON contract (not a hard-coded list).'
  framework: unittest
  type: unit
```

## Sign-offs

- [x] architect-review — 2026-07-17 10:00
- [ ] pr-reviewer — failed 2026-07-17 10:30
- [ ] llm-expert
- [ ] commit — failed 2026-07-17 11:00
- [ ] pull-request — failed 2026-07-17 11:30

## Comments

### 2026-07-17 11:00 — commit (status: blocker)
feedback-id: fb_2026-07-17_c2259089
completion_manifest:
  staged_files_match_files_touched:
    result: false
    reason: "None of the 5 files in files_touched (templates/agents/status-checker.md, ac-triage.md, pt-classifier.md, worktree-agent.md, unit_tests/test_bp_300e_6_agent_json_only_contract.py) are staged or present; the branch diff contains only docs/agents/conventions.md and ticket files."
    remediation: "Respawn llm-expert to implement the JSON-only output contract on all four agent templates and author the three required unit tests, then re-run pr-reviewer, then re-run commit."
  pre_commit_hooks_pass:
    result: false
    reason: "No implementation commit was attempted — there is nothing to commit for the BP-300e-6 implementation."
    remediation: "Once llm-expert implements the contract, commit will proceed normally."
  commit_message_valid: true
  ticket_staged: true
Committed only the ticket status updates (architect-review sign-offs for BP-300e-5 and BP-300e-6, plus the pr-reviewer failure update). The BP-300e-6 implementation — JSON-only output contract for status-checker, ac-triage, pt-classifier, worktree-agent, and the unit test — was never authored. Respawn llm-expert to implement the contract, then re-run pr-reviewer, then re-run commit.

### 2026-07-17 10:30 — pr-reviewer (status: blocker)
feedback-id: fb_2026-07-17_13c88fd6
completion_manifest:
  diff_reviewed: true
  implementation_verified:
    result: false
    reason: "The branch diff (origin/main...HEAD) touches only docs/agents/conventions.md; none of the 5 files in files_touched (templates/agents/status-checker.md, ac-triage.md, pt-classifier.md, worktree-agent.md, unit_tests/test_bp_300e_6_agent_json_only_contract.py) appear in the diff."
    remediation: "Respawn llm-expert to implement the JSON-only output contract on all four agent templates and add the unit test before re-running pr-reviewer."
  test_coverage_verified:
    result: false
    reason: "unit_tests/test_bp_300e_6_agent_json_only_contract.py does not exist; confirmed via ls."
    remediation: "Respawn llm-expert to author the three required unit tests."
The branch diff contains only a docs/agents/conventions.md change (section 3.2.1 on gated-agent spawning). None of the BP-300e-6 implementation — JSON-only output contract for status-checker, ac-triage, pt-classifier, worktree-agent, and the unit test — is present. The status-checker template still carries its ## Anomalies trailing markdown section (line 226) and a ## Verdict markdown output format (line 99), both of which the AC requires to be replaced with in-payload JSON fields. Respawn llm-expert to implement the contract before re-running pr-reviewer.

### 2026-07-17 11:30 — pull-request (status: blocker)
feedback-id: fb_2026-07-17_eccbdb33
completion_manifest:
  branch_pushed: false
  pr_created:
    result: false
    reason: "The BP-300e-6 implementation (JSON-only output contract for templates/agents/status-checker.md, ac-triage.md, pt-classifier.md, worktree-agent.md, and unit_tests/test_bp_300e_6_agent_json_only_contract.py) is absent from the branch diff (origin/main...HEAD); none of the 5 files in files_touched appear in the diff."
    remediation: "Respawn llm-expert to implement the JSON-only output contract on all four agent templates and author the three required unit tests, then re-run pr-reviewer, then re-run commit, then re-run pull-request."
  pr_body_complete:
    result: false
    reason: "No PR was drafted because the implementation commit is absent."
    remediation: "Same as pr_created — re-run the full llm-expert → pr-reviewer → commit → pull-request chain once the implementation exists."
Attempted to create PR for branch docs/conventions-no-spawn-gated-agent. Branch diff (origin/main...HEAD) contains only docs/agents/conventions.md and ticket housekeeping commits; none of the 5 files_touched for BP-300e-6 appear in the diff. Pushing and creating the PR would produce a phantom-done state. Respawn llm-expert to author the JSON-only output contract implementation, then re-run the full phase chain.

### 2026-07-17 10:00 — architect-review (status: ok)
feedback-id: fb_2026-07-17_27a3d277
completion_manifest:
  impact_classification_done: true
  rubric_triggers_evaluated: true
  requires_adr_assessed: true
  suggested_diagrams_assessed: true
Ticket classified **small**: 5 files at the ≤5 threshold, single component (build_pipeline), no always-large triggers (no Alembic migration, no hypertable change, no FastAPI change, no ADR contract change). The change is a prompt-engineering hardening adding a return-only-JSON output contract to 4 machine-parsed producer agent templates. Key design concern: the anomaly-relocation from trailing markdown to JSON fields is semantically breaking for consumers that parse trailing prose — verify all downstream JSON.parse call sites discard trailing text (or rely on BP-300e-1 tolerant reader). The test's dynamic producer-list derivation from workflows-js dispatch sites is the correct data-driven approach. No acceptance-criteria adjustments needed. ADR-024 (machine-parsed-agent-output-contract) is suggested but not required (single-component, non-HIGH impact). escalation: none.
