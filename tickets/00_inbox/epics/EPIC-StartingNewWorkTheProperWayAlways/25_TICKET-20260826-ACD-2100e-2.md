---
ac_traceability:
  id: ACD-2100e-2
  path: docs/acceptance-criteria/ac-driven-dev/ACD-2100-entry-point-unblocked/ACD-2100e-2.yaml
advances_current_outcome: true
agents:
  commit: signed_off
  documentation-expert: signed_off
  documentation-verifier: signed_off
  pull-request: not_needed
change_target: docs
complexity: medium
components:
- ac_driven_dev
- documentation_system
created: '2026-08-26'
depends_on:
- tickets/00_inbox/epics/EPIC-StartingNewWorkTheProperWayAlways/15_TICKET-20260826-ACD-2100c-3.md
- tickets/00_inbox/epics/EPIC-StartingNewWorkTheProperWayAlways/16_TICKET-20260826-ACD-2100c-3-i.md
- tickets/00_inbox/epics/EPIC-StartingNewWorkTheProperWayAlways/05_TICKET-20260826-ACD-2100a-4.md
- tickets/00_inbox/epics/EPIC-StartingNewWorkTheProperWayAlways/24_TICKET-20260826-ACD-2100e-1.md
documentation_required: true
files_touched:
- docs/INDEX.md
- docs/components.json
- docs/how-to/resume-a-paused-plan-feature-run.md
- docs/how-to/approval-gate.md
priority: medium
requires_adr: false
requires_diagram: false
risk_surface: internal
roadmap_phase: phase_1
source_ac: ACD-2100e-2
status: done
title: A how-to guide takes an operator from a waiting run back to a running one
---

# A how-to guide takes an operator from a waiting run back to a running one

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `ACD-2100e-2` — A how-to guide takes an operator from a waiting run back to a running one — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `ACD-2100e-2`. Component: `ac-driven-dev`. Assigned agent: `documentation-expert`. Estimated complexity: `M`. Complexity: `medium`.

## Acceptance Criteria

```gherkin
Given an operator who has found a run that stopped and reported that it is
  waiting for an answer,
When they open the project's how-to guides,
Then a page tells them how to list the runs that are waiting and how to see
  which decision each one is waiting on,
And it gives the invocation that starts the route again supplying the person's
  answer for that decision -- the invocation the route itself accepts rather
  than an edit of the stored record by hand,
And it states what the operator observes in each of the three ways that
  invocation ends: the run continuing from the decision it was waiting on
  without repeating the steps before it and no longer listed as waiting; the
  run reporting there is nothing to resume when no waiting record exists; and
  the run staying waiting and naming the decision that is outstanding when the
  answer supplied names a different decision,
And for each of the two endings that do not resume it states that the drafted
  work is unchanged and the waiting record is still there to be answered,
And each step and each outcome it gives names the criterion whose delivered
  behaviour it describes,
And the page is reachable from the documentation index and declares the same
  component as this criterion.
```

- [ ] AC-1: a page tells them how to list the runs that are waiting and how to see
- [ ] AC-2: it gives the invocation that starts the route again supplying the person's
- [ ] AC-3: it states what the operator observes in each of the three ways that
- [ ] AC-4: for each of the two endings that do not resume it states that the drafted
- [ ] AC-5: each step and each outcome it gives names the criterion whose delivered
- [ ] AC-6: the page is reachable from the documentation index and declares the same

## Implementation Notes

```yaml
- TARGET FILE -- a NEW how-to at exactly docs/how-to/resume-a-paused-plan-feature-run.md.
  ACD-2100c-3 and ACD-2100c-3-i both link that exact path as planned, and ACD-2100c-3
  carries a delivers_to contract naming this id and documentation-expert.
- NOTHING IN THIS REPOSITORY WILL CATCH A WRONG PATH -- check-doc-links IS NOT THE
  NET THE STORE NOTES ASSUME. check_doc_links validates DOC_LINKS declared in Python
  and SQL files only, and its own docstring states every failure is a warning and
  it always exits 0. No hook validates a markdown-to-markdown link at all. So the
  two inbound planned links and every outbound link this page carries are unguarded
  in both directions. Publishing at the exact path above is the entire defence.
- 'THE OUTBOUND LINK TO c3-006 MAY POINT AT A FILE THAT DOES NOT EXIST YET. ACD-2100e-1
  authors docs/architecture/diagrams/c3-008-plan-feature-decision-gate-sequence.md
  and is NOT in this record''s depends_on, so the sequencer may build this page first
  (KI-ACD-015: expects_from orders nothing). Combined with the previous requirement
  -- no hook validates a markdown link -- publishing first would ship a live page
  pointing at nothing, silently. Before publishing, either confirm the ordering edge
  has been added (recommended in the notes) or confirm the diagram exists; do not
  hard-link a file that is not there.'
- 'TITLE AND OPENING MUST DISTINGUISH THIS PAGE FROM docs/how-to/approval-gate.md.
  That page already exists, is about the goal-to-epic readiness gate and its yes /
  review-all / cancel choices, is not resumable and is none of the five decision points
  -- and it declares components: [ac_driven_dev, build_orchestration], so it lands
  in the same component view and the same how-to index as this page. An operator searching
  for "gate" reaches it first. Name the route and the waiting-run situation in the
  title and in the first sentence, and link approval-gate.md once as explicitly not
  this gate. Neither page absorbs the other.'
- 'FRONTMATTER components: MUST BE THE UNDERSCORE GRAPH ID ac_driven_dev -- NOT the
  kebab ac-driven-dev. check_doc_frontmatter validates every components entry against
  the keys of docs/components.json, which are the underscore ids. That declaration
  is the mechanically checkable half of the last Then clause. ACD-2100d-4''s equivalent
  requirement states the kebab form; that wording is wrong and must not be copied.
  Follow the sibling how-tos for the rest of the block: type: how-to with a matching
  category: how-to, as docs/how-to/approval-gate.md does.'
- docs/INDEX.md IS GENERATED. Register the page by running the build and committing
  the regenerated index -- do not hand-edit it. A hand-edited index is reverted by
  the next build.
- QUOTE THE INVOCATION AND EACH OF THE THREE OUTCOME REPORTS VERBATIM, never a paraphrase.
  The operator is matching a message on their screen against this page; a paraphrase
  they cannot match sends them away believing the page does not cover their case,
  which is the moment they escalate or re-run the route. Take every quoted string
  from the delivered behaviour of ACD-2100c-3 and ACD-2100c-3-i, not from an expectation.
- THE LISTING STEP AND THE RESUME STEP MUST BOTH WORK FROM THE PROJECT ROOT AND FROM
  A WORKTREE. ACD-2100a-4's contract is a waiting record whose location is a function
  of the project rather than of the caller's working directory. Do not publish a path
  or an invocation that only resolves from one of them -- a working-directory-dependent
  path is the exact defect family this L1 sits in, and a page that reproduces it would
  send an operator to look for a record that is findable only from where the run happened
  to start.
- DO NOT WRITE ANY OF THIS FROM KI-ACD-005's FIX DIRECTION. That known issue's stated
  root cause and stated fix are both out of date -- resolveGate already pauses on
  an unparseable answer and the observed loss came from a well-formed one -- so a
  procedure written from it would be wrong on the day it was published.
- LENGTH IS A REAL CONSTRAINT -- check_doc_length caps a non-ADR document at 300 lines
  and 25 sections. Link out to ADR-024 and to c3-006 for how a run came to be waiting
  rather than absorbing either; this page is a procedure with an outcome, not an explanation
  of the gate flow.
```

## Agent Contracts

### documentation-expert

Existing docs to update / cross-link:

- docs/how-to/resume-a-paused-plan-feature-run.md (relationship: creates | status: planned | relevance: THE TARGET FILE, AND THIS RECORD'S SOLE DELIVERABLE. ACD-2100c-3 and ACD-2100c-3-i already link this exact path as planned; publishing under any other name leaves both dangling and check-doc-links cannot catch a target that never appears.)
- docs/architecture/adrs/ADR-024-interactive-pause-resume.md (relationship: describes | status: exists | relevance: The pause and resume contract -- including re-entry with a supplied answer, which is the invocation the second Then clause requires the page to give.)
- docs/architecture/diagrams/c3-008-plan-feature-decision-gate-sequence.md (relationship: related | status: planned | relevance: The sequence diagram authored by ACD-2100e-1. An operator who wants to know how their run came to be waiting is sent there; this page does not restate the decision points. ORDERING GAP -- this page's depends_on does not name ACD-2100e-1 and no hook validates a markdown link, so see the requirements before hard-linking it.)
- docs/architecture/components/interactive-pause-resume-substrate.md (relationship: describes | status: exists | relevance: Component doc for the store the waiting record lives in and the record lifecycle a successful resume completes.)
- docs/how-to/approval-gate.md (relationship: related | status: exists | relevance: ADJACENT HOW-TO WITH A DIFFERENT SUBJECT -- the goal-to-epic readiness gate and its yes / review-all / cancel choices. Not this route's decision points and not resumable. Do not conflate the two gates in the reader's mind. It also declares components: [ac_driven_dev, build_orchestration], so it sits in the same component view as this page.)
- docs/INDEX.md (relationship: describes | status: exists | relevance: The index the last clause requires the page to be reachable from. It is generated.)

- [x] AC-1: sequence-diagram | docs/how-to/resume-a-paused-plan-feature-run.md | a page tells them how to list the runs that are waiting and how to see <!-- signed: documentation-expert -->
- [x] AC-2: how-to | docs/how-to/resume-a-paused-plan-feature-run.md | a page tells them how to list the runs that are waiting and how to see <!-- signed: documentation-expert -->

### Expects From

- **AC:** ACD-2100c-3
- **Contract:** The resume invocation and its observable outcomes -- resumed, nothing to resume, mismatched answer -- which the how-to walks an operator through.
- **AC:** ACD-2100c-3-i
- **Contract:** The mismatched-answer outcome in detail: the answer is not applied, the run remains paused with its record on disk, it reports which decision it is waiting on, and nothing is discarded even when the supplied answer names a destructive choice on another decision.
- **AC:** ACD-2100a-4
- **Contract:** A waiting record whose location is a function of the project rather than of the caller's working directory -- so the listing step and the resume step in this page work from the project root and from a worktree alike, and a run that reported itself waiting is actually findable.
- **AC:** ACD-2100e-1
- **Contract:** ORDERING GAP -- SEE NOTES. The published sequence diagram at docs/architecture/diagrams/c3-008-plan-feature-decision-gate-sequence.md, which this page links and sends an operator to for how their run came to be waiting -- so that this page can stay a procedure instead of restating the five decision points. depends_on does not carry this edge and no hook validates the markdown link, so building this record first ships a live page pointing at a file that does not exist.

## Sign-offs

- [x] documentation-expert — 2026-09-14 13:10
- [x] documentation-verifier — 2026-09-14 13:15
- [x] commit — 2026-09-14 13:30

## Comments

### 2026-09-14 13:10 — documentation-expert (status: ok)

feedback-id: fb_2026-09-14_9cc3545f
completion_manifest:
  doc_written: true
  cross_links_added: true
  diataxis_genre_correct: true
Published the target file at exactly `docs/how-to/resume-a-paused-plan-feature-run.md`
(no path adjudication needed here -- c3-008 already exists at its adjudicated path from
ACD-2100e-1). Content is sourced strictly from delivered behaviour, not expectation:
step 1 (listing) and step 2 (reading which decision) use the real, worktree-safe
`git rev-parse --git-common-dir` resolution `ACD-2100a-4` implemented (verified against
`scripts/pause_store.py` and `_buildRepoRootResolutionSnippet()` in
`templates/workflows-js/plan-feature.js` directly, not from the ticket's own description);
`pause_store.py` has no `list` subcommand, so listing is a directory listing plus a
per-run `read`, stated as such rather than inventing a capability that doesn't exist.
Step 3's `resume_answer` object is quoted verbatim from `templates/skills/plan-feature/SKILL.md`
§RS.2 (authored earlier in this same epic, dated 2026-09-14, as the canonical statement of
this exact contract), paired with ADR-024's own verbatim resume-invocation sentence. The
three outcome payloads in step 4 are quoted verbatim from `resolveGate()`/`pauseAtGate()`'s
actual return statements in `templates/workflows-js/plan-feature.js` (`paused_awaiting_input`
with `question`, `nothing_to_resume`, and the post-clear `pause_store.py read` response
`{"exists": false, "stale": false, "record": null}`) and from `ACD-2100c-3-i`'s delivered
mismatch behaviour (answer's `gate_id` not matching the actually-paused gate falls through
`resolveGate()`'s bottom branch, which re-pauses naming the real gate, not the one the
answer named) -- traced by reading the current code, not copied from the Gherkin's own
wording. Title and opening paragraph name the route and the waiting-run situation and link
`approval-gate.md` once as explicitly not this gate, per the notes' disambiguation
requirement -- did not edit `approval-gate.md` itself (its Agent Contracts relationship is
`related`, not a target). Regenerated `docs/INDEX.md` (`scripts/generate_doc_index.py`): the
how-to category scans `docs/how-to/` recursively (unlike the architecture-diagrams gap
`ACD-2100e-1` found), so the new page is now listed there under `ac_driven_dev`'s reachable
surface -- one line inserted, confirmed via `git diff --stat`. Added the inbound half of
every cross-link this ticket's Agent Contracts assign to this phase: `ADR-024`'s
`related_docs` + `## References`, `interactive-pause-resume-substrate.md`'s `related_docs` +
`## Cross-References`, and `c3-008-plan-feature-decision-gate-sequence.md`'s `related_docs` +
`## Cross-References`. Did not edit `docs/components.json` -- `ac_driven_dev` already exists
there and the frontmatter `components: [ac_driven_dev]` I declared matches it exactly (same
zero-diff pattern `ACD-2100e-1`'s own `ac-fulfillment-gate` pass confirmed for this same
file); verified no diff via `git diff --stat -- docs/components.json`. Ran
`check_doc_frontmatter.py`, `check_doc_length.py` (185 lines, well under the 300-line /
25-section cap for a non-ADR doc), `check_doc_links.py` (advisory, exit 0), and
`env --chdir=<worktree> check_identifier_uniqueness.py` (diagrams: OK, 27 inspected,
unchanged -- no new diagram file was added in this phase) on every touched file: all green.
Flipped both `## Agent Contracts` AC checkboxes for this agent. The two
`ticket_frontmatter_guard.py` PostToolUse warnings on this section's own edits, claiming
`depends_on` targets 15/16/05/24 are missing, are the same known cwd-resolution
false-positive `ACD-2100e-1`'s own commit sign-off documented (verified all four files exist
on disk in this same epic folder via `ls`), not a real dependency gap.

### 2026-09-14 13:15 — documentation-verifier (status: ok)

feedback-id: fb_2026-09-14_9845d8c7
completion_manifest:
  required_docs_list_parsed: true
  all_required_docs_present_in_diff: true
  no_placeholder_content_in_changed_docs: true
Documentation coverage verified: the sole required doc (AC-1/AC-2 both target
`docs/how-to/resume-a-paused-plan-feature-run.md`) is present in `git diff HEAD --name-only`
(uncommitted working-tree change; absent from the `origin/main...HEAD` branch-range diff,
which is expected since this ticket's work has not yet been committed). Candidate 1
(`@{upstream}` = `origin/EPIC-StartingNewWorkTheProperWayAlways`) was rejected as a
self-tracking mirror of the current branch name; Candidate 2 (`origin/HEAD` =
`origin/main`) resolved successfully. Placeholder check on the 186-line file: helper
script scan (TODO/PLACEHOLDER/FIXME/Replace-with/QUESTION) returned zero hits; `\bTBD\b`
grep zero hits; unfilled single-identifier `{token}` grep zero hits; content is dense
prose, numbered steps, fenced code/JSON examples, and a bulleted field-reference list —
not an empty or heading-only stub. All four sub-checks pass; no placeholder content
detected.

### 2026-09-14 13:30 — commit (status: ok)

feedback-id: fb_2026-09-14_05e6294d
completion_manifest:
  pre_commit_hooks_pass: true
  commit_message_valid: true
  ticket_staged: true
Auto-authorized commit gate (supervised path, ticket_path provided by /build-feature —
no interactive confirmation issued per commit-agent §Step 3 supervised path): subject
"docs(ac-driven-dev): add how-to for resuming a paused plan-feature run"; staged files:
docs/INDEX.md, docs/architecture/adrs/ADR-024-interactive-pause-resume.md,
docs/architecture/components/interactive-pause-resume-substrate.md,
docs/architecture/diagrams/c3-008-plan-feature-decision-gate-sequence.md,
docs/how-to/resume-a-paused-plan-feature-run.md. Pre-commit probe
(`verify_precommit_active.py --json`) initially reported `git_hook: false` when run
from the Bash tool's default cwd (`/home/henzeh/projects/leafcutter`, not a git repo);
re-run with cwd set to the worktree root via `env --chdir=<worktree>` returned all four
checks passing (`{"binary": true, "config": true, "git_hook": true, "canary": true,
"failing_checks": []}`) — confirmed the hook file is installed and fresh at
`leafcutter-ai/.git/hooks/pre-commit` (shared commondir for this worktree), so the
initial failing_checks was a cwd-resolution artifact of the invoking shell, not an
inactive gate. Ran `classify_staged_files()` / `detect_mixed_set()` from
`commit_classifier.py`: `specific_pattern_matched: true`, `is_mixed: false` — no
pattern-learner delegation needed. Verified the staged diff matches every factual claim
in documentation-expert's and documentation-verifier's sign-off comments above (new
how-to page, single INDEX.md line, cross-links in ADR-024/substrate/c3-008, zero diff on
`docs/components.json` and `docs/how-to/approval-gate.md` as those comments state).
Committed the docs changes, then staged and committed this ticket's own sign-off edit
as a separate `chore(ticket): commit phase sign-off` commit per repo convention (see
`git log` — this pattern already appears twice in this epic's history). Verified `HEAD`
moved after each commit via `git log -1`.
