---
ac_traceability:
  id: TKT-500f-18
  path: docs/acceptance-criteria/ticket-creation/TKT-500-single-source/TKT-500f-18.yaml
advances_current_outcome: true
agents:
  ac-fulfillment-gate: needed
  ac-validator: needed
  commit: needed
  pr-reviewer: needed
  pull-request: needed
  python-coder: needed
  test-runner: needed
  test-writer: needed
change_target: pipeline
complexity: medium
components:
- ticket_creation_pipeline
created: '2026-07-21'
depends_on:
- TKT-500f
files_touched:
- docs/components.json
- docs/reference/ac-schema.md
- scripts/ac_store/generate_ticket_from_ac.py
priority: high
requires_adr: false
requires_diagram: false
risk_surface: internal
roadmap_phase: phase_1
source_ac: TKT-500f-18
status: todo
title: The kebab-to-graph-id mapping is obtained from a side-effect-free source that
  does not reconfigure global logging on import
---

# The kebab-to-graph-id mapping is obtained from a side-effect-free source that does not reconfigure global logging on import

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `TKT-500f-18` — The kebab-to-graph-id mapping is obtained from a side-effect-free source that does not reconfigure global logging on import — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `TKT-500f-18`. Component: `ticket-creation`. Assigned agent: `python-coder`. Estimated complexity: `M`. Complexity: `medium`.

## Acceptance Criteria

```gherkin
Given the generator module scripts/ac_store/generate_ticket_from_ac.py is
  imported,
When the kebab-to-graph-id mapping used to normalise the components LIST is
  obtained,
Then the mapping is read from a side-effect-free source — parsing
  docs/components.json or importing a plain data module — rather than from a
  module whose import runs configuration side effects,
And importing the generator does NOT reconfigure global logging: no
  logging.basicConfig call (or equivalent root-logger reconfiguration) runs as a
  side effect of importing the generator or the mapping source,
And a program that imports the generator keeps whatever root logging
  configuration it had before the import (handlers and level are unchanged by the
  import).
```

- [ ] AC-1: the mapping is read from a side-effect-free source — parsing
- [ ] AC-2: importing the generator does NOT reconfigure global logging: no
- [ ] AC-3: a program that imports the generator keeps whatever root logging

## Test Requirements

```yaml
tests:
- name: test_import_does_not_call_logging_basicconfig
  file: unit_tests/ac_store/test_tkt_500f_18.py
  covers:
  - TKT-500f-18
  asserts: Importing generate_ticket_from_ac does not call logging.basicConfig / does
    not mutate the root logger's handlers or level.
  framework: unittest
  type: unit
- name: test_import_preserves_preexisting_root_logging_config
  file: unit_tests/ac_store/test_tkt_500f_18.py
  covers:
  - TKT-500f-18
  asserts: A root logger configured before the import keeps its handlers and level
    after importing the generator.
  framework: unittest
  type: unit
- name: test_mapping_read_from_side_effect_free_source
  file: unit_tests/ac_store/test_tkt_500f_18.py
  covers:
  - TKT-500f-18
  asserts: The kebab-to-graph-id mapping is obtained by parsing components.json /
    a plain data module, not by exec-ing the sibling migrate_component_vocab.py script.
  framework: unittest
  type: unit
```

## Implementation Notes

```yaml
config_schema_fragment: null
n_location_rule: '1'
notes:
- The kebab-to-graph-id mapping must be obtained from a side-effect-free source (parse
  docs/components.json or import a plain data module) — the migration-map loader (_load_migration_map,
  ~line 194) must stop exec-ing the sibling scripts/migrate_component_vocab.py at
  import time.
- 'Importing the generator (or its mapping source) must NOT reconfigure global logging:
  no logging.basicConfig or equivalent root-logger reconfiguration may run as an import
  side effect.'
- A program importing the generator must retain its pre-existing root logging configuration
  (handlers and level unchanged by the import).
- Import of the generator must not raise on a sibling-script error (no exec-at-import
  coupling to a script that can fail).
post_write_commands:
- python scripts/build.py --target-dir .
reference_file_path: scripts/ac_store/generate_ticket_from_ac.py
required_skills:
- python-coder
```

## Sign-offs

- [ ] test-writer
- [ ] python-coder
- [ ] test-runner
- [ ] pr-reviewer
- [ ] ac-validator
- [ ] ac-fulfillment-gate
- [ ] commit
- [ ] pull-request

## Comments
