---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  pr-reviewer: needed
  pull-request: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
  workflow-architect: needed
components:
- build-pipeline
created: '2026-06-08'
depends_on:
- BP-600c
- BP-600c-2
- BP-600d-2
files_touched:
- docs/architecture/agent_delivery_workflows.md
- docs/architecture/adrs/ADR-006-flatten-supervisor-chain.md
priority: medium
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: BP-600c-3
status: todo
title: Quick-fix workflow runs the test after the fix and confirms it passes (green
  phase)
---

# Quick-fix workflow runs the test after the fix and confirms it passes (green phase)

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BP-600c-3` — Quick-fix workflow runs the test after the fix and confirms it passes (green phase) — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BP-600c-3`. Component: `build-pipeline`. Assigned agent: `workflow-architect`. Estimated complexity: `M`.

## Acceptance Criteria

```gherkin
Given the python-coder has applied the fix to the diagnosed file,
When the workflow reaches the green-phase verification step,
Then it dispatches the test-runner agent targeting the same test file
  from the red phase,
And the test-runner reports all tests PASSED,
And if the test still fails the workflow halts with a warning:
  "The fix did not resolve the failing test -- the root cause may be
  different than diagnosed."
```

## Sign-offs

- [ ] workflow-architect
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit
- [ ] pull-request

## Comments
