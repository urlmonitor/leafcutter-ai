---
ac_traceability:
  id: BP-1100g-1
  path: docs/acceptance-criteria/build_pipeline/BP-1100-phantom-done-prevention/BP-1100g-1.yaml
advances_current_outcome: true
agents:
  architect-review: signed_off
  commit: signed_off
  documentation-expert: signed_off
  documentation-verifier: signed_off
  llm-expert: signed_off
  pr-reviewer: signed_off
  pull-request: needed
  test-runner: signed_off
  test-writer: signed_off
change_target: prompt
complexity: medium
components:
- build_pipeline
- testing_quality
- llm_authoring
created: '2026-08-25'
depends_on: []
documentation_required: true
files_touched:
- docs/testing/test-angles.md
- templates/agents/test-writer.md
- unit_tests/prompt_assembly/test_bp_1100g_1.py
out_of_scope:
- .claude/agents/
- .leafcutter/
priority: medium
requires_adr: false
requires_diagram: false
risk_surface: contract_boundary
roadmap_phase: phase_1
source_ac: BP-1100g-1
status: todo
title: Every kind of proof the plan can ask for is a kind the test writer has been
  taught
---

# Every kind of proof the plan can ask for is a kind the test writer has been taught

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BP-1100g-1` — Every kind of proof the plan can ask for is a kind the test writer has been taught — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BP-1100g-1`. Component: `build-pipeline`. Assigned agent: `llm-expert`. Estimated complexity: `M`. Complexity: `medium`.

## Acceptance Criteria

```gherkin
Given the planning side can request a proof by any name drawn from its own set of
    kinds,
  And the instructions the test-writing side works from define some set of names,
When the two sets are compared against each other,
Then they are the same set — no name the planning side can emit is undefined for
  the test-writing side, and no name the test-writing side is taught is one the
  planning side can never emit,
  And for each name, the instructions state what distinguishes a proof of that
    kind from a proof of the behaviour alone, in terms the writer can act on
    without further interpretation,
  And when a name is added to either side while the other is left alone, the
    mismatch is reported naming that specific name and the side that lacks it,
  And the comparison reads both sets from where they really live, so a copy of
    either set made for the comparison's own convenience cannot make the two
    agree while the real sources disagree.
```

- [ ] AC-1: the instructions the test-writing side works from define some set of names
- [ ] AC-2: they are the same set — no name the planning side can emit is undefined for
- [ ] AC-3: for each name, the instructions state what distinguishes a proof of that
- [ ] AC-4: when a name is added to either side while the other is left alone, the
- [ ] AC-5: the comparison reads both sets from where they really live, so a copy of

## Test Requirements

```yaml
tests:
- name: test_bp_1100g_1_taught_set_equals_the_emittable_set
  file: unit_tests/prompt_assembly/test_bp_1100g_1.py
  covers:
  - BP-1100g-1
  asserts: 'Set-equality between the kinds the planning side may emit and the kinds
    templates/agents/test-writer.md teaches: no emittable name is undefined for the
    writer, and no taught name is unemittable.'
  framework: unittest
  type: unit
  angle: criterion
- name: test_bp_1100g_1_both_sets_are_read_from_their_real_sources
  file: unit_tests/prompt_assembly/test_bp_1100g_1.py
  covers:
  - BP-1100g-1
  asserts: Both sets are parsed from the actual on-disk files (the single definition
    file and templates/agents/test-writer.md) in a fresh subprocess — no fixture copy
    of either side, and the test fails if either source path is missing rather than
    falling back to a literal.
  framework: unittest
  type: integration
  angle: real_artifact
- name: test_bp_1100g_1_one_sided_name_addition_is_reported_by_name_and_side
  file: unit_tests/prompt_assembly/test_bp_1100g_1.py
  covers:
  - BP-1100g-1
  asserts: Add a name to a temp copy of one source only, run the comparison, and assert
    the report names that specific name and states which side lacks it — then repeat
    with the sides swapped.
  framework: unittest
  type: unit
  angle: failure
- name: test_bp_1100g_1_deployed_test_writer_template_carries_the_taught_set
  file: unit_tests/prompt_assembly/test_bp_1100g_1.py
  covers:
  - BP-1100g-1
  asserts: 'PRODUCTION ENTRY POINT: run `python scripts/build.py --target-dir <tmp>`
    as a subprocess, then read the DEPLOYED .claude/agents/test-writer.md that the
    agent runtime actually loads and assert its taught set equals the definition.
    Source-tree reads are structurally blind to deploy-manifest gaps.'
  framework: unittest
  type: integration
  angle: reachability
```

## Implementation Notes

```yaml
- THE TAUGHT SET MUST BE MACHINE-EXTRACTABLE. Write it into templates/agents/test-writer.md
  behind one stable anchor (a single fenced block or a table under a fixed heading)
  so the comparison can read the real source. Prose that merely mentions the names
  is not a set and cannot be compared; a taught set the checker cannot find is the
  same as no taught set.
- The comparison must read BOTH sides from where they really live — the single definition
  established by BO-2900g-3, and templates/agents/test-writer.md — never a fixture
  copy of either. A comparison against a convenience copy reproduces exactly the EPIC-ComputedQualityGates
  FP-1 layer-3 defect it exists to catch (a hook's allow-list and the config it mirrored
  were disjoint, each tested against its own copy).
- A reported mismatch must name the specific missing name AND the side that lacks
  it. 'The sets differ' is unactionable.
- 'The teaching must be a set-cover checklist over decidable predicates, never a ratio
  model. docs/testing/test-angles.md ''Prompt-design note'' is binding here: a pyramid/trophy
  framing plus a cheapest-green optimiser collapses reliably to zero high-level tests.'
- The template ships through build.py. The taught set must be present in the DEPLOYED
  copy (.claude/agents/test-writer.md), not only in templates/ — a template edit that
  does not survive deployment teaches nobody.
- 'NOT MACHINE-CHECKABLE, DELIBERATELY UNCOVERED: the second Then clause (''for each
  name, the instructions state what distinguishes a proof of that kind from a proof
  of the behaviour alone, in terms the writer can act on'') is a judgement about prose
  quality. It is NOT to be tested by grepping the template for keywords — that asserts
  presence, not actionability, and is the Neverfail Test smell. It is assigned to
  human/pr-reviewer review against the llm-expert Prompt-Quality Checklist. See caveat
  in the enrichment note.'
- Nothing here reads a test. A reported mismatch is a mismatch between two declarations
  of a vocabulary, never a verdict about any piece of work.
```

### Scope correction — READ BEFORE EDITING ANY TEMPLATE

The generated `files_touched` named `.claude/agents/test-writer.md` as an edit
surface. It was wrong, and it has been removed. `.claude/agents` is a **symlink to
`.leafcutter/agents`** — a build output that `build.py` regenerates from
`templates/`. The path is not one of the AC's five `doc_links`; the generator's prose
fallback scraped it out of the it_requirement above that *warns* the deployed copy
must also carry the taught set (KI-ACS-002).

**Edit `templates/agents/test-writer.md`. Never the deployed copy.**

Getting this backwards produces a green ticket that ships nothing, and it is worth
understanding exactly how, because this AC exists to prevent that failure:

1. `test_spec` entry 4 is the **reachability** test. It runs `build.py` and then reads
   the DEPLOYED `.claude/agents/test-writer.md` — deliberately, because the agent
   runtime loads the built copy, and a source-tree read is structurally blind to a
   deploy-manifest gap.
2. Edit the deployed copy directly and that test passes **immediately**.
3. `templates/` is untouched, so the next `build.py` run overwrites the deployed copy
   and the taught set disappears.
4. The ticket has closed green. Nothing shipped.

The deployed copy is the **assertion target**, not the edit target. It earns the
taught set by being rebuilt from `templates/`, which is precisely what entry 4 is
checking.

`out_of_scope` now lists `.claude/agents/` and `.leafcutter/`, so `change-scope-reviewer`
treats a diff there as a hard violation rather than a judgement call.

Also added to `files_touched`: `unit_tests/prompt_assembly/test_bp_1100g_1.py`. All four
`test_spec` entries target that directory and it was absent from the declared surface,
which would have made the actual deliverable read as unexpected scope.

## Agent Contracts

### documentation-expert

Existing docs to update / cross-link:

- templates/agents/test-writer.md (relationship: describes | status: exists | relevance: The doer, and the taught side of the comparison. Contains zero occurrences of the word 'angle' at HEAD while the generator already emits it — the taught set is currently empty and the emitted set is not. Read the working-tree version; Rule 3 was rescoped 2026-08-14 and is uncommitted.)
- config/ac_store_schema.json (relationship: describes | status: exists | relevance: Where the emittable set currently lives (the seven-value test_spec[].angle enum). After BO-2900g-3 there is one such set; before it there are two, which is why this AC depends on that reconciliation for a well-defined left-hand side.)
- docs/testing/test-angles.md (relationship: describes | status: exists | relevance: Supplies what each name means, including the one-question test ('if I deleted the single line that wires this in, would this test go red?') and the explicit prompt-design note: give the writer a set-cover checklist over decidable predicates, never a ratio model.)
- docs/acceptance-criteria/build-orchestration/BO-2900-runtime-reachability-guard/BO-2900g-3.yaml (relationship: related | status: exists | relevance: Makes 'the set of kinds the plan can ask for' single. Until it lands there are two candidate sets and this comparison has no well-defined left-hand side.)
- scripts/ac_store/generate_ticket_from_ac.py (relationship: describes | status: exists | relevance: Carries the third copy of the emittable set (_TEST_ANGLES, ~line 77) and is what actually emits a kind onto a ticket. The comparison's left-hand side must resolve to the single definition, not to this frozenset.)

- [x] AC-1: [reference-doc] templates/agents/test-writer.md — they are the same set — no name the planning side can emit is undefined for <!-- signed: documentation-expert -->
- [x] AC-2: [sequence-diagram] templates/agents/test-writer.md — they are the same set — no name the planning side can emit is undefined for <!-- signed: documentation-expert -->

### Delivers To

- **Agent:** test-writer
- **Contract:** templates/agents/test-writer.md carries the taught kind set behind one stable, documented anchor, one entry per permitted kind, each with its distinguishing rule. The anchor and the value shape are the contract the set-equality check and BP-1100g-3's tag validation both read.

### Expects From

- **AC:** BO-2900g-3
- **Contract:** One authoritative statement of the permitted kinds, resolvable from a single file path — the well-defined left-hand side of this comparison. Until it lands there are two candidate sets and no comparison is decidable.

## Sign-offs

- [x] architect-review — 2026-08-25 17:36
- [x] test-writer — 2026-08-25 17:52
- [x] test-runner — 2026-08-25 18:19
- [x] documentation-expert — 2026-08-25 19:05
- [x] pr-reviewer — 2026-08-25 18:29
- [x] documentation-verifier — 2026-08-25 18:33
- [x] llm-expert — 2026-08-25 17:50
- [x] commit — 2026-08-25 20:24
- [ ] pull-request

## Comments

### 2026-08-25 17:36 — architect-review (status: ok)
feedback-id: fb_2026-08-25_2cccc969
completion_manifest:
  blast_radius_reviewed: true
  rubric_applied: true
  escalation_decided: true
Classified **small** — 3 files touched (docs/testing/test-angles.md, templates/agents/test-writer.md, unit_tests/prompt_assembly/test_bp_1100g_1.py), no always-large trigger (no Alembic/hypertable/public-API/ADR-contract change in this repo; highest ADR on disk is ADR-034, none touched by this diff), and the `components:` list (build_pipeline, testing_quality, llm_authoring) reflects AC-store tagging granularity rather than independent runtime service boundaries — no cross-package coupling is introduced (templates/agents/test-writer.md is a static prompt file with no import relationship to the doc or the new test). Verified templates/agents/test-writer.md has zero occurrences of "angle" at HEAD (matches the ticket's stated defect) and that the comparison's left-hand side (config/ac_store_schema.json `test_spec[].angle`, the seven-value enum) is well-defined — BO-2900g-3 is `work_status: done`. Design concern for test-writer: the new comparison test must read the angle enum from `config/ac_store_schema.json` directly, not from `generate_ticket_from_ac.py`'s `_TEST_ANGLES` frozenset — even though BO-2900g-3 claims that copy is now reconciled, the schema remains the more authoritative single source and reading the frozenset would reproduce the EPIC-ComputedQualityGates FP-1 layer-3 pattern this AC exists to prevent. No acceptance-criteria adjustments. `requires_adr: false` and `requires_diagram: false` (already set in frontmatter) are confirmed correct.

### 2026-08-25 17:52 — test-writer (status: ok)
feedback-id: fb_2026-08-25_f37df786
completion_manifest:
  test_stubs_created: true
  all_tests_red: true
  red_baseline_captured: true
  ac_ids_covered: [BP-1100g-1]

Wrote `unit_tests/prompt_assembly/test_bp_1100g_1.py` (6 tests) implementing all
four `## Test Requirements` entries. No production-code agent (python-coder /
sql-coder / frontend-coder) is `needed` on this ticket — only `llm-expert` — but
the ticket's Test Requirements block is fully populated and names this exact
file in `files_touched`, so it is treated as a code-testable deliverable (same
precedent as `test_python_coder_template.py` for `python-coder.md`), not
skipped as a non-code ticket.

**Emittable-set source (per architect-review's design note):** read directly
from `config/ac_store_schema.json`'s `properties.test_spec` array-branch
`items.properties.angle.enum` — the schema, not
`generate_ticket_from_ac.py`'s `_TEST_ANGLES` frozenset copy (BO-2900g-3
already pins that copy to the schema with its own cross-source test; reading
it here would reproduce the EPIC-ComputedQualityGates FP-1 layer-3 pattern).

**Anchor contract defined for llm-expert** (documented at the top of the test
file): a fenced YAML block between `<!-- TAUGHT-TEST-ANGLES:START -->` /
`<!-- TAUGHT-TEST-ANGLES:END -->` markers in `templates/agents/test-writer.md`,
one `angle_name: distinguishing_rule` entry per taught kind. **Edit
`templates/agents/test-writer.md` only — never the deployed
`.claude/agents/test-writer.md` / `.leafcutter/agents/test-writer.md`**, per
the ticket's Scope correction; Test 4 below re-fails on the next `build.py`
run if the anchor is ever added only to the deployed copy.

**AC-1..AC-5 mapping** (ticket has no `### test-writer` subsection under
`## Agent Contracts` and no `## AC Coverage` table, so per signoff §2c.2 the
AC-coverage-table step is skipped; mapping recorded here instead for audit):
- AC-1 (taught side defines a set) — `test_bp_1100g_1_taught_set_equals_the_emittable_set`, `test_bp_1100g_1_deployed_test_writer_template_carries_the_taught_set`
- AC-2 (same set as emittable) — same two tests, plus `test_bp_1100g_1_both_sets_are_read_from_their_real_sources`
- AC-3 (per-name distinguishing rule, in actionable terms) — **(not testable: judgement about prose quality/actionability, explicitly excluded from grep-style checking in the ticket's Implementation Notes; assigned to human/pr-reviewer review against the llm-expert Prompt-Quality Checklist)**. A weak structural check (`assertTrue(rule.strip())`) is included in `test_bp_1100g_1_taught_set_equals_the_emittable_set` — this asserts only that a non-empty rule field exists per taught name, not that its content is actionable.
- AC-4 (one-sided addition named + attributed) — `test_bp_1100g_1_name_added_only_to_emittable_side_is_named_and_attributed`, `test_bp_1100g_1_name_added_only_to_taught_side_is_named_and_attributed`
- AC-5 (both sets read from real sources, no convenience copy) — `test_bp_1100g_1_both_sets_are_read_from_their_real_sources`, `test_bp_1100g_1_missing_source_path_fails_loudly_not_a_literal_fallback`

**Verification run:** `AC_ENFORCE_STRICT=1 python3 -m pytest
unit_tests/prompt_assembly/test_bp_1100g_1.py -q` → exit 1, **3 failed, 3
passed** (the repo's `pytest_ac_enforcement` plugin xfail-masks the 3 failures
by default since `work_status != done`; confirmed true red state with
`AC_ENFORCE_STRICT=1`, consistent with documented repo behaviour). The 3
passing tests (`test_bp_1100g_1_missing_source_path_fails_loudly_not_a_literal_fallback`
and both one-sided-addition tests) exercise the in-test comparator/guard logic
itself, which is already correct and does not depend on the anchor existing —
they are intentionally excluded from `red_baseline` per the "do not include
passing tests" rule.

```yaml
red_baseline:
  - test_name: test_bp_1100g_1_taught_set_equals_the_emittable_set
    file: unit_tests/prompt_assembly/test_bp_1100g_1.py
    error: "AssertionError: {} is not true : templates/agents/test-writer.md has no <!-- TAUGHT-TEST-ANGLES:START/END --> anchor (or it parsed empty) — a taught set the checker cannot find is the same as no taught set"
  - test_name: test_bp_1100g_1_both_sets_are_read_from_their_real_sources
    file: unit_tests/prompt_assembly/test_bp_1100g_1.py
    error: 'AssertionError: 1 != 0 : fresh-subprocess comparison of the two real on-disk sources must agree. stdout: {"emittable": ["boundary", "criterion", "deployed", "failure", "reachability", "real_artifact", "seam"], "taught": [], "missing_from_taught": ["boundary", "criterion", "deployed", "failure", "reachability", "real_artifact", "seam"], "unemittable_taught": []}'
  - test_name: test_bp_1100g_1_deployed_test_writer_template_carries_the_taught_set
    file: unit_tests/prompt_assembly/test_bp_1100g_1.py
    error: "AssertionError: {} is not true : deployed <tmp>/.claude/agents/test-writer.md has no taught-angle anchor (or it parsed empty) — the taught set must survive the build, not only exist in templates/"
```

### 2026-08-25 17:50 — llm-expert (status: ok)
feedback-id: fb_2026-08-25_6d3ba321
completion_manifest:
  template_written: true
  prompt_quality_checklist_passed: true
  convention_violations_resolved: true

Added the `<!-- TAUGHT-TEST-ANGLES:START/END -->` anchor to
`templates/agents/test-writer.md` (edited the template only, per the ticket's
"Scope correction" — never the deployed `.claude/agents/test-writer.md`), a
fenced YAML block with all seven `angle` names from
`config/ac_store_schema.json`'s `test_spec[].angle` enum
(`criterion, reachability, seam, real_artifact, deployed, boundary, failure`),
each paired with a one-sentence, decidable distinguishing rule stating what
separates a proof of that kind from a proof of the behaviour alone (AC-3).
Verified the block parses to a 7-key dict via the exact regex/`yaml.safe_load`
logic the comparison test uses before running the suite.

Re-ran `unit_tests/prompt_assembly/test_bp_1100g_1.py` before touching any
file and confirmed the red baseline exactly matched test-writer's captured
state (3 failed / 3 passed, `AC_ENFORCE_STRICT=1`). After the edit, re-ran the
full suite (plain and `AC_ENFORCE_STRICT=1`): **6 passed, 0 failed**, including
`test_bp_1100g_1_deployed_test_writer_template_carries_the_taught_set`, which
runs `build.py` into a temp target and confirms the taught set survives the
build into the deployed copy — proving the fix was made in `templates/`, not
the deployed layer.

Also updated `docs/testing/test-angles.md`: the "Known weakness of the floor"
callout previously stated `test-writer` "contains the word 'angle' zero
times"; that claim is now stale, so I corrected it to describe the taught-set
anchor BP-1100g-1 landed, while being explicit that the *vocabulary* gap
(test-writer now knows the seven angle names and their rules) is distinct from
the *judgement* gap the paragraph is actually about (naming the correct
concrete production entry point for a given AC) — the latter is unresolved by
this ticket and I did not claim otherwise.

Prompt-Quality Checklist: no compound bash commands used; `tools:` frontmatter
(`Bash, Read, Edit, Write, Agent`) is unchanged and still matches body usage;
no new tool references introduced; the existing `## Your Available Sub-Agents`
spawn allowlist, `## Sign-off` section, and Stop-and-Ask scope-boundary content
in `test-writer.md` were left intact and unaffected by this addition;
`requires_verification: true` remains present alongside `Edit`/`Write` in
`tools:`. AC-1, AC-2, AC-4, AC-5 are machine-verified by the passing test
suite; AC-3 ("in terms the writer can act on without further interpretation")
is a judgement call per the ticket's Implementation Notes and is flagged here
for `pr-reviewer` to assess against this checklist, as the ticket specifies.

### 2026-08-25 18:19 — test-runner (status: ok)
feedback-id: fb_2026-08-25_426ff2fa
completion_manifest:
  test_suite_executed: true
  all_tests_passing: true
  failure_report_structured: true
Ran `unit_tests/prompt_assembly/test_bp_1100g_1.py` via `AC_ENFORCE_STRICT=1 python3 -m pytest unit_tests/prompt_assembly/test_bp_1100g_1.py -v`: 6 passed, 0 failed, 0 errors in 2.10s (also verified plain, no strict flag). This includes the reachability test that runs `build.py` into a temp target and reads the deployed `.claude/agents/test-writer.md`. test-writer's captured red baseline (3 failed / 3 passed pre-fix) is now fully green after llm-expert's anchor addition to `templates/agents/test-writer.md`. No production-code regressions observed; no failures to report.

### 2026-08-25 19:05 — documentation-expert (status: ok)
feedback-id: fb_2026-08-25_4863a1f1
completion_manifest:
  doc_written: true
  cross_links_added: true
  diataxis_genre_correct: true

Reviewed the documentation surface for this AC rather than re-authoring it: both
declared doc files (`docs/testing/test-angles.md`, `templates/agents/test-writer.md`)
were already written by `llm-expert` in an earlier phase of this same ticket, and
`git diff --stat` confirms both are the only content changes beyond the new test file
(plus unrelated pre-existing working-tree drift in `docs/agents/cards/*.card.md`, out
of this ticket's `files_touched` and left untouched).

**Genre check.** `docs/testing/test-angles.md` frontmatter declares `type: reference`,
correctly — it is a lookup-oriented taxonomy (the seven-angle table, evidence catalogue,
literature grounding), not a how-to or explanation. The two-line diff in this ticket
(the "Known weakness of the floor" callout) accurately narrates the vocabulary-gap fix
BP-1100g-1 delivers, is honest about the residual judgement gap it does *not* close, and
matches the actual diff in `templates/agents/test-writer.md` (verified by reading both
files in full).

**Cross-link check.** `templates/agents/test-writer.md`'s new "Test Angles — The Taught
Set" section explicitly references `docs/testing/test-angles.md` ("See ... for the full
rationale, evidence base, and literature") and names both comparison consumers
(`unit_tests/prompt_assembly/test_bp_1100g_1.py`, BP-1100g-3). `docs/testing/test-angles.md`
already carried the reverse link (`templates/agents/test-writer.md` — Rule 3 ...) before
this ticket. The link is bidirectional and both ends resolve.

**Agent Contracts anomaly (flagged, not blocking).** The `### documentation-expert`
subsection lists AC-1 `[reference-doc]` and AC-2 `[sequence-diagram]`, both against
`templates/agents/test-writer.md` and both carrying the *identical* AC-text fragment
("they are the same set — no name the planning side can emit is undefined for"). This
reads as ticket-generator noise — a single Gherkin `Then`-clause double-scraped under two
different guessed doc-type tags — of the same class the ticket's own "Scope correction"
section already documents for `files_touched`. There is no actual sequence-diagram
deliverable in this AC's Gherkin (AC-1..AC-5), and `architect-review` already confirmed
`requires_diagram: false` is correct in frontmatter. I checked both boxes: AC-1 because
the reference-doc requirement it names (a documented, comparable taught set at
`templates/agents/test-writer.md`) is genuinely satisfied by llm-expert's anchor; AC-2
because its identical text names the same already-satisfied requirement under a doc-type
label architect-review has already ruled inapplicable, not an unmet diagram obligation.

**Not run:** full glossary-triage dispatch on `docs/testing/test-angles.md` — this
invocation has no `Agent` tool available, so `glossary-triage` could not be dispatched.
`glossary_detector.py` was run read-only against the file; findings are almost entirely
pre-existing code identifiers from before this ticket's two-line diff (e.g.
`generate_ticket_from_ac`, already blacklisted at `docs/glossary_blacklist.md`), and the
two genuinely new strings introduced by this ticket (`TAUGHT-TEST-ANGLES`,
`test_bp_1100g_1`) are code/anchor identifiers of the same already-established
blacklist-pattern, not domain jargon. Per CLAUDE.md, blacklist/glossary entries must go
through the triage flow, not a hand-edit — logging this as a non-blocking gap for the
next agent with `Agent` tool access rather than hand-editing `docs/glossary_blacklist.md`.

## Escalation

Branch: none
Reason: 3 files in one logical unit (prompt-authoring contract enforcement: templates/agents/test-writer.md + docs/testing/test-angles.md + unit_tests/prompt_assembly/test_bp_1100g_1.py); no always-large trigger fired (no Alembic/hypertable/public-API change in this repo; no docs/architecture/adrs/ADR-* file touched).

### 2026-08-25 18:29 — pr-reviewer (status: ok)
feedback-id: fb_2026-08-25_4d9c4a48
completion_manifest:
  diff_reviewed: true
  tests_executed: true
  ac3_prompt_quality_reviewed: true
  scope_correction_honored: true

Reviewed the working diff against `origin/main` (HEAD sits exactly at the
merge-base, so the diff is precisely the ticket's three declared files —
confirmed via `git diff origin/main --stat`). No high- or medium-confidence
findings.

**Verification performed:**
- Ran `AC_ENFORCE_STRICT=1 python3 -m pytest unit_tests/prompt_assembly/test_bp_1100g_1.py -v`
  — 6/6 passed. `ruff check` on the new test file — clean.
- Confirmed the emittable set in `config/ac_store_schema.json`
  (`test_spec[].angle` enum: boundary, criterion, deployed, failure,
  reachability, real_artifact, seam) exactly matches the 7 keys inside the
  `<!-- TAUGHT-TEST-ANGLES:START/END -->` anchor added to
  `templates/agents/test-writer.md`.
- Confirmed the "Scope correction" was honored: only `templates/agents/test-writer.md`
  is modified (`git status --porcelain` shows no change under `.claude/agents/`
  or `.leafcutter/agents/`); the deployed-copy reachability test (Test 4) is
  the assertion target, not an edit target, and passes by rebuilding from
  `templates/`.
- Cross-file contract trace: `Expects From: BO-2900g-3` is satisfied — the
  comparator reads `config/ac_store_schema.json` directly (never
  `generate_ticket_from_ac.py`'s `_TEST_ANGLES` frozenset), matching
  architect-review's design note and avoiding the EPIC-ComputedQualityGates
  FP-1 layer-3 recurrence the ticket warns against. `Delivers To: test-writer`
  is satisfied — the anchor is present, one entry per permitted kind, each
  with a distinguishing rule.
- AC-3 judgement review (per Implementation Notes, assigned to pr-reviewer
  against the llm-expert Prompt-Quality Checklist, not to be grepped): all
  seven distinguishing rules are decidable and actionable without further
  interpretation — each names a concrete mechanism (real subprocess vs.
  `importlib.reload()`, real producer piped into real consumer, `build.py`
  into a temp target, etc.) and a concrete negative example of what does NOT
  satisfy the angle, rather than merely restating the angle's name. This
  clears the "set-cover checklist over decidable predicates, never a ratio
  model" bar from `docs/testing/test-angles.md`'s Prompt-design note.
- `docs/testing/test-angles.md` diff is accurate and appropriately scoped: it
  corrects the now-stale "contains the word 'angle' zero times" claim and is
  explicit that the *vocabulary* gap this ticket closes is distinct from the
  *judgement* gap (naming the correct concrete production entry point) that
  remains open — no overclaiming.

**Anomaly (not a finding against this ticket):** the working tree carries
unrelated, unstaged drift in `docs/agents/cards/{architecture-diagram-author,
documentation-expert,frontend-coder,python-coder}.card.md` (ambient
regenerated-card mirror content referencing unrelated AC IDs such as
GE-124*/BO-2400f-12/BO-2400f-13, last touched by an unrelated historical
commit per `git log`). This is outside `files_touched`, not staged, and was
already identified and left alone by documentation-expert's prior sign-off.
Confirmed it predates and is unrelated to this diff; not blocking.

No production-code agent ran on this ticket (llm-expert only), so there is no
call-site/signature-extension surface to audit and no path-convention change
to grep for. `## Agent Contracts` has no `### pr-reviewer` subsection and the
ticket has no `## AC Coverage` table, so per signoff §2c.1/§2c.2 the AC
checkbox/coverage-table steps are skipped for this agent.

### 2026-08-25 18:33 — documentation-verifier (status: ok)
feedback-id: fb_2026-08-25_23437f3d
completion_manifest:
  required_docs_list_parsed: true
  all_required_docs_present_in_diff: true
  no_placeholder_content_in_changed_docs: true

Required-docs list parsed from `## Agent Contracts` → `### documentation-expert`:
AC-1 and AC-2 both name the same target, `templates/agents/test-writer.md`
(deduplicated to one required doc). **Anomaly:** both lines use the bracket
format `[genre] doc_path — constraint` (`[reference-doc] templates/agents/test-writer.md — ...`
/ `[sequence-diagram] templates/agents/test-writer.md — ...`), not the
pipe-delimited `genre | doc_path | constraint` format this agent's own Step 2
parsing spec documents — this matches the real output of
`_build_agent_contracts_section` in `scripts/ac_store/generate_ticket_from_ac.py`,
and every real generated ticket in this store uses the bracket form (confirmed
by grepping other 00_inbox tickets and by documentation-verifier's own prior
`(status: ok)` sign-off on TICKET-20260811-GE-113c-3.md, which made the same
call). Parsed the doc_path from the bracket form rather than failing closed on
a delimiter mismatch that would blocker 100% of real tickets.

Resolved `integration_target` via Candidate 1 (`@{upstream}` = `origin/main`;
not self-tracking, since the current branch is `build/bp-1100g-1`, not `main`).
The branch-range diff (`origin/main...HEAD`) is empty (no commits yet on this
branch); the working-tree diff (`git diff HEAD --name-only`) includes
`templates/agents/test-writer.md`, so the union satisfies the sole required
doc. `docs/testing/test-angles.md` was also edited but is not named by any
`### documentation-expert` AC-N line, so it is not part of the required-docs
set per the single-source-of-truth rule — its presence is a bonus, not a gap.

Placeholder check on `templates/agents/test-writer.md`: 6a's helper script
(`scan_for_placeholders`, a plain case-insensitive `\bPLACEHOLDER\b` regex with
no backtick exemption or line-position narrowing in the currently-deployed
`scripts/build_placeholder_detection.py`) flagged two hits — line 605
("use the placeholder `# covers: UNKNOWN`") and line 712 ("not a placeholder or
a guess. Copy-paste from the test output."). Confirmed via `git diff HEAD --
templates/agents/test-writer.md` (single hunk, 48 insertions, 0 deletions) that
both lines are pre-existing prose, hundreds of lines outside this ticket's
sole added section (the `<!-- TAUGHT-TEST-ANGLES:START/END -->` block), using
"placeholder" as an ordinary English word to instruct the test-writer agent
about sourcing/reporting conventions — not an unresolved template marker. Ruled
non-blocking, consistent with the same bareword-false-positive treatment this
agent's own Step 6a documentation describes as a known, accepted risk. 6b (TBD),
6c (unfilled `{token}`), and 6d (empty/heading-only stub) all passed clean; the
added section is substantive prose plus a 7-key YAML anchor, not a stub.

No blockers. Ticket proceeds to `commit`.

### 2026-08-25 20:24 — commit (status: ok)
feedback-id: fb_2026-08-25_c5de7516
completion_manifest:
  pre_commit_hooks_pass: true
  commit_message_valid: true
  ticket_staged: true

Step 0a probe (`verify_precommit_active.py`) initially reported
`git_hook: false` — traced to a sandbox artifact, not a real gap: the
harness resets Bash cwd to the workspace parent
(`/home/henzeh/projects/leafcutter`, not a git repo) between calls, and
the probe resolves `.git` from `Path.cwd()`. Re-ran with
`env --chdir=<worktree-root>` and got a clean `failing_checks: []`, so
proceeded (no user override needed).

Staged the three declared `files_touched` files
(`docs/testing/test-angles.md`, `templates/agents/test-writer.md`,
`unit_tests/prompt_assembly/test_bp_1100g_1.py`) via `commit_classifier`;
left the unrelated, pre-existing, unstaged
`docs/agents/cards/*.card.md` drift alone (out of `files_touched`, already
flagged by documentation-expert and pr-reviewer as unrelated). The
classifier's `detect_mixed_set` also flagged the ticket file itself
(tickets/tests/docs groups) — expected and normal for this repo's
per-ticket workflow, not a real mixed-unrelated-work signal, so proceeded
without a user gate per the supervised-path authorization already
established.

Committed `715a48cb` on `build/bp-1100g-1`: "feat(testing-quality):
test-writer learns the seven test-angle kinds" — pre-commit hooks passed
clean on the first attempt, no autofix needed. Verified HEAD moved
(`715a48cbf16795ee1ad13e5ee99ef2b5823b30b5`) per the background-commit
safety check. The ticket file itself is committed separately in a
follow-up "chore(ticket): commit phase sign-off" commit immediately after
this sign-off is written, per the Commit-Agent Sign-off Step.
