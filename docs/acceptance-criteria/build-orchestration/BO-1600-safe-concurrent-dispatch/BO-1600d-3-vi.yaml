id: BO-1600d-3-vi
readiness: approved
priority: high
title: "In a large object store, recovery removes only the detected corrupt objects, never every empty file"
component: build-orchestration
level: L3
status: active
req_status: draft
work_status: done
created_by: "docs/acceptance-criteria/build-orchestration/BO-1600-safe-concurrent-dispatch/BO-1600d.yaml"
criteria: |
  Given a repository with a large object store that contains both the specific
    zero-byte corrupt loose objects identified by detection and other files, and
    where scanning the whole store for every empty file would be slow and unsafe
  When the recovery step engine performs the remove-zero-byte-objects action
  Then it removes only the specific corrupt loose objects that detection (BO-1600c)
    identified and listed, referenced by their concrete object paths
  And it does NOT blindly delete every empty file it can find in the object store
  And the dry-run plan names the exact objects that will be removed so the human can
    confirm the removal set is the detected set, not a blanket sweep
depends_on:
  - BO-1600d-3
doc_links:
  - path: "docs/acceptance-criteria/build-orchestration/BO-1600-safe-concurrent-dispatch/BO-1600d.yaml"
    relationship: relates
    status: exists
  - path: "docs/acceptance-criteria/build-orchestration/BO-1600-safe-concurrent-dispatch/BO-1600c.yaml"
    relationship: relates
    status: exists
assigned_agent: python-coder
estimated_complexity: S
it_requirements:
  - "The removal set MUST be the explicit list of corrupt objects produced by the BO-1600c detection, addressed by their concrete object paths — never a store-wide 'delete all empty files' sweep."
  - "The dry-run plan must enumerate the exact objects to be removed so the human confirms the detected set, guarding against an over-broad deletion in a large store."
  - "PORTABLE SURFACE: this scoped-removal guard lives in the same standalone recovery script (templates/scripts/git_recovery.py) pinned in BO-1600d-1/BO-1600d-4; ADR-001 deployed/template parity applies."
  - "Must handle errors gracefully per the project error handling policy (CLAUDE.md Rules 1-3)."
delivers_to: null
expects_from:
  ac_id: BO-1600d-3
  contract: "The remove-zero-byte-objects step this edge case scopes to the detection-identified set."
origin_agent: business-analyst-v3
created: 2026-07-01
amended_by:
  - reason: it-po-technical-enrichment
    entry: "Pinned portable surface (templates/scripts/git_recovery.py, ADR-001 parity). readiness draft->reviewed."
    confirmed_by: it-po-v3
    confirmed_at: 2026-07-01
superseded_by: null
covered_by:
- tests/test_git_recovery.py::TestLargeObjectStoreSelectiveRemoval
implemented_by:
- tickets/00_inbox/epics/EPIC-GuidedGitRecovery/09_TICKET-20260706-BO-1600d-3-vi.md
