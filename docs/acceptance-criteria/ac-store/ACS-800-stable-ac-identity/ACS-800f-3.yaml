id: ACS-800f-3
readiness: approved
priority: high
title: "The schema reference prose describes stable UIDs and metadata hierarchy"
component: ac-store
level: L2
status: active
req_status: approved
work_status: todo
criteria: |
  Given the schema reference prose that today explains that a requirement's name
    encodes its position and that the parent is derived by stripping the last
    name segment,
  When that prose is updated for the new identity model,
  Then it explains that a requirement's id is a stable opaque UID that encodes no
    position,
  And it describes the parent-pointer, level, and order metadata fields as the
    source of hierarchy,
  And the section that previously presented name-based parent derivation as the
    canonical algorithm is replaced by the metadata-lookup description or clearly
    marked as the migration-window fallback only.
depends_on: [ACS-800f, ACS-800a-1, ACS-800b-1, ACS-800b-2, ACS-800b-3]
doc_links:
  - path: docs/reference/ac-schema.md
    relationship: specifies
    status: exists
assigned_agent: llm-expert
estimated_complexity: M
delivers_to: null
expects_from: null
origin_agent: business-analyst
created: 2026-06-22
amended_by: []
superseded_by: null
covered_by: []
implemented_by: []
notes: |
  Decomposed from ACS-800f. Target surface: docs/reference/ac-schema.md prose (the
  ID Format and Parent Derivation sections). Describes the new model rather than the
  old name-encodes-position mechanic. llm-expert work.
change_target: docs
risk_surface: internal
