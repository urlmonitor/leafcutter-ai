---
ac_traceability:
  id: GE-120a-5
  path: docs/acceptance-criteria/guardrail-engine/GE-120-green-means-checked/GE-120a-5.yaml
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: needed
  documentation-verifier: needed
  pull-request: not_needed
change_target: docs
complexity: low
components:
- documentation_system
- commit_guardian
created: '2026-08-25'
depends_on:
- 01_TICKET-20260825-GE-120a-1.md
- 04_TICKET-20260825-GE-120a-2.md
documentation_required: true
files_touched:
- docs/acceptance-criteria/guardrail-engine/PROJECT_CONTEXT.md
- docs/how-to/creating-a-claude-code-hook.md
- docs/reference/ac-schema.md
- templates/commit-guardian/commit_guardian.json
- templates/scripts/commit_guardian/commit_guardian.json
priority: high
requires_adr: false
requires_diagram: false
risk_surface: internal
roadmap_phase: phase_1
source_ac: GE-120a-5
status: todo
title: The no-silent-pass rule is written where the next check author will find it
---

# The no-silent-pass rule is written where the next check author will find it

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `GE-120a-5` — The no-silent-pass rule is written where the next check author will find it — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `GE-120a-5`. Component: `guardrail-engine`. Assigned agent: `documentation-expert`. Estimated complexity: `S`. Complexity: `low`.

## Acceptance Criteria

```gherkin
Given the reference documentation an author consults before adding a check to the
  guardrail engine,
When that author reads it,
Then it states the no-silent-pass rule in the form this tree defines it: a check that
  could not perform its inspection reports a could-not-check outcome rather than success,
And it states the boundary against the existing fail-open convention explicitly — failing
  open on one bad input while the check still ran remains correct and is not repealed —
  and cites GE-116a-1-iii as the approved example,
And it states the third state as well: a check that fell back to a reduced inspection
  labels its clean result as not authoritative,
And it states that the cannot-run disposition, block or announce, is recorded per check
  with no implicit default, and names where it is recorded,
And the existing documentation that records only the fail-open convention, without its
  limit, is corrected in place rather than contradicted by a second document,
And the AC-parent-covered-by reproduction is included as the worked example of the
  difference between "nothing wrong here" and "I never looked".
```

- [ ] AC-1: it states the no-silent-pass rule in the form this tree defines it: a check that
- [ ] AC-2: it states the boundary against the existing fail-open convention explicitly — failing
- [ ] AC-3: cites GE-116a-1-iii as the approved example
- [ ] AC-4: it states the third state as well: a check that fell back to a reduced inspection
- [ ] AC-5: it states that the cannot-run disposition, block or announce, is recorded per check
- [ ] AC-6: the existing documentation that records only the fail-open convention, without its
- [ ] AC-7: the AC-parent-covered-by reproduction is included as the worked example of the

## Implementation Notes

```yaml
- CORRECT IN PLACE, do not add a second document. The document to correct is docs/reference/ac-schema.md,
  which today records the fail-open convention (including the unlimited form, that
  any unexpected exception causes the hook to exit 0) without stating its limit. Adding
  a new document while the old one still says the opposite leaves the next author
  with two rules and no ordering between them — that is the failure the criterion
  names.
- 'Sweep the other surfaces that state the same unlimited rule and reconcile them
  in the same change: docs/acceptance-criteria/guardrail-engine/PROJECT_CONTEXT.md
  (its FAIL-OPEN CONTRADICTION section) and docs/how-to/creating-a-claude-code-hook.md,
  which is what a new check author actually reads.'
- 'Name the disposition storage location EXACTLY as GE-120a-2 implements it — a new
  key on each entry of hooks_manifest.hooks[] in templates/scripts/commit_guardian/commit_guardian.json
  (canonical; templates/commit-guardian/commit_guardian.json is the legacy mirror
  and config/ copies are build outputs). Verified: there is no pre-existing mode field
  to describe. SEQUENCE THIS AFTER GE-120a-2 LANDS so the location documented is the
  one that exists, not one the author guessed at.'
- Use the same names for all three states that the shared outcome module actually
  defines (ordinary success, degraded-but-ran / not authoritative, could-not-check).
  Documentation that invents its own vocabulary for a machine-readable enum is drift
  on day one.
- Cite GE-116a-1-iii by id as the approved fail-open example that is NOT repealed,
  and state the boundary as the unit of visibility rather than blocking, matching
  GE-120 and GE-120a notes.
- 'Use the check_ac_parent_covered_by reproduction as the worked example with its
  evidence label intact (DIRECTLY OBSERVED AND REPRODUCIBLE: same check, same repo,
  same staged files, opposite verdicts decided purely by workspace layout). Do NOT
  cite the 26-schema-violating-files figure — it is REPORTED, NOT VERIFIED until GE-120c-4
  runs.'
- 'docs/reference/ac-schema.md is an existing tracked document: edit it, do not replace
  it, and keep it passing check_doc_frontmatter, check_doc_length and check_doc_links
  in the same commit.'
```

## Agent Contracts

### documentation-expert

Existing docs to update / cross-link:

- docs/reference/ac-schema.md (relationship: modifies | status: exists | relevance: Currently documents the fail-open convention for the AC hooks without stating its limit.)
- docs/architecture/components/commit-guardian.md (relationship: related | status: exists)
- docs/acceptance-criteria/guardrail-engine/PROJECT_CONTEXT.md (relationship: context | status: exists | relevance: Records the fail-open contradiction and the line GE-120 draws.)
- docs/how-to/creating-a-claude-code-hook.md (relationship: related | status: exists | relevance: The other surface a new check author reads; must not be left stating the unlimited fail-open rule.)

- [ ] AC-1: reference-doc | docs/reference/ac-schema.md | it states the no-silent-pass rule in the form this tree defines it: a check that

### Expects From

- **AC:** GE-120a-2
- **Contract:** The recorded per-check disposition vocabulary and its storage location, to document accurately.

## Sign-offs

- [ ] documentation-expert
- [ ] documentation-verifier
- [ ] commit

## Comments
