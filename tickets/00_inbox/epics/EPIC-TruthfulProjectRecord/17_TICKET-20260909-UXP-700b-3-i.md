---
ac_coverage: 5/5
ac_traceability:
  id: UXP-700b-3-i
  path: docs/acceptance-criteria/ux-prototyping/UXP-700-truthful-project-record/UXP-700b-3-i.yaml
advances_current_outcome: true
agents:
  ac-fulfillment-gate: needed
  ac-validator: signed_off
  architect-review: not_needed
  commit: needed
  documentation-expert: not_needed
  documentation-verifier: not_needed
  frontend-coder: not_needed
  pr-reviewer: not_needed
  pull-request: not_needed
  python-coder: signed_off
  sql-coder: not_needed
  test-runner: signed_off
  test-writer: signed_off
  user-surface-smoker: not_needed
change_target: code
complexity: low
components:
- ux_prototyping
created: '2026-09-09'
depends_on: []
files_touched: []
priority: medium
requires_adr: false
requires_diagram: false
risk_surface: internal
roadmap_phase: phase_1
source_ac: UXP-700b-3-i
status: todo
title: One record is enough to exercise the rule, and a violating record still fails
  it
---

# One record is enough to exercise the rule, and a violating record still fails it

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `UXP-700b-3-i` — One record is enough to exercise the rule, and a violating record still fails it — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `UXP-700b-3-i`. Component: `ux-prototyping`. Assigned agent: `python-coder`. Estimated complexity: `S`. Complexity: `low`.

## Acceptance Criteria

```gherkin
Given a check phrased as a rule that must hold of every record of some kind,
And a project record holding exactly one record of that kind, which satisfies the rule,
When the check runs,
Then it reports that the rule holds and that it examined one record,
And it does not report that it was not exercised.
Given instead a project record holding exactly one record of that kind, which violates the
  rule,
When the check runs,
Then it reports the violation naming that record,
And it does not report that it was not exercised.
```

- [x] AC-1: a project record holding exactly one record of that kind, which satisfies the rule
- [x] AC-2: it reports that the rule holds and that it examined one record
- [x] AC-3: it does not report that it was not exercised.
- [x] AC-4: it reports the violation naming that record
- [x] AC-5: it does not report that it was not exercised.

## Test Requirements

```yaml
tests:
- name: test_single_satisfying_record_reports_the_rule_holds_and_one_examined
  file: unit_tests/product_truth/test_uxp_700b_3_i.py
  covers:
  - UXP-700b-3-i
  asserts: Population of one that satisfies the rule yields a holds verdict with an
    examined figure of one.
  framework: unittest
  type: unit
  angle: boundary
- name: test_single_violating_record_reports_the_violation_by_name
  file: unit_tests/product_truth/test_uxp_700b_3_i.py
  covers:
  - UXP-700b-3-i
  asserts: Population of one that violates the rule yields a violation naming that
    record, not a not-exercised report.
  framework: unittest
  type: unit
  angle: failure
- name: test_uxp_700b_3_i_reachable_from_entry_point
  file: unit_tests/product_truth/test_uxp_700b_3_i.py
  covers:
  - UXP-700b-3-i
  angle: reachability
  asserts: 'REQUIRED — invoke the production entry point (CLI, hook, slash command,
    workflow dispatch, or main()) as a subprocess/dispatch and assert the new behaviour
    actually occurs. Do NOT satisfy this by importing the function directly. The AC
    authored no test_spec, so the entry point is not declared: resolve it before writing
    this test, and do not delete this entry.'
```

## Agent Contracts

### Expects From

- **AC:** UXP-700b-3
- **Contract:** The not-exercised verdict whose boundary this record pins at a population of one.

## Sign-offs

- [x] test-writer — 2026-09-09 15:32
- [x] python-coder — 2026-09-09 16:20
- [x] test-runner — 2026-09-09 16:45
- [x] ac-validator — 2026-09-09 17:10
- [ ] ac-fulfillment-gate
- [ ] commit

## Comments

### 2026-09-09 15:32 — test-writer (status: ok)
feedback-id: fb_2026-09-09_7fa73500
completion_manifest:
  tests_written: true
  red_baseline_confirmed: true
  cross_layer_seam_answer:
    result: not_applicable
    reason: "This ticket's scope is a single new, self-contained utility module (universal_rule_check.py) with no consumer outside its own module in this ticket's scope. The generic every-record checker it introduces is not yet wired into validate_product_truth.py's D1-D4 checks or any other consumer — that wiring belongs to a separate, not-yet-built ticket (e.g. UXP-514-5). There is therefore no real producer/consumer pair to pin a seam test to yet."
    remediation: "n/a — not_applicable is a first-class outcome, not a defect."
  reachability_entry_point_answer:
    result: resolved
    entry_point: "python docs/product-truth/scripts/universal_rule_check.py --records <file> --rule positive --kind widget (CLI via subprocess; module has main() guarded by if __name__ == '__main__':). No hook/slash-command/workflow surface exists yet because this module's own prerequisites (UXP-700b, UXP-700b-1, UXP-700b-2, UXP-700b-3) are all still work_status: todo in this repo, so no automatic gate calls it yet; the CLI this ticket's own contract requires python-coder to build is the entry point being pinned."
red_baseline:
  - test_name: test_single_satisfying_record_reports_the_rule_holds_and_one_examined
    file: unit_tests/product_truth/test_uxp_700b_3_i.py
    error: "ModuleNotFoundError: No module named 'universal_rule_check'"
  - test_name: test_single_violating_record_reports_the_violation_by_name
    file: unit_tests/product_truth/test_uxp_700b_3_i.py
    error: "ModuleNotFoundError: No module named 'universal_rule_check'"
  - test_name: test_uxp_700b_3_i_reachable_from_entry_point
    file: unit_tests/product_truth/test_uxp_700b_3_i.py
    error: "AssertionError: 2 != 0 : a single satisfying record must exit 0; stderr=\"...can't open file '...\\\\docs\\\\product-truth\\\\scripts\\\\universal_rule_check.py': [Errno 2] No such file or directory\""

Wrote `unit_tests/product_truth/test_uxp_700b_3_i.py` (new directory, `__init__.py` added)
covering the two Test Requirements entries plus the mandatory reachability test, all tagged
`# covers: UXP-700b-3-i` and their declared `# angle:`. No prior implementation exists for
any AC in the UXP-700b family (UXP-700b/700b-1/700b-2/700b-3 are all still `work_status:
todo` in this repo, and a repo-wide search found no `examined`/`not_exercised`/outcome-
vocabulary code anywhere), so this is greenfield: the tests target a new module
`docs/product-truth/scripts/universal_rule_check.py` (not created — that is python-coder's
job) exposing `check_universal_rule(records, predicate, kind, name_of=...)` returning a
verdict with `.outcome` in `{not_exercised, holds, violated}`, `.examined`, `.violations`,
plus a CLI `main()` reading `--records/--rule/--kind` and exiting 0 for
holds/not_exercised, 1 for violated. Verified red with `AC_ENFORCE_STRICT=1 python -m
pytest unit_tests/product_truth/test_uxp_700b_3_i.py -v` (plain run without
`AC_ENFORCE_STRICT=1` xfail-masks the reachability test since the AC is not yet done — the
strict run is the true baseline): all 3 fail (2 `ModuleNotFoundError`, 1 `AssertionError`
from a genuine subprocess exit-code mismatch), overall exit code 1. No mock_data_ref, no
bug-fix regression, and no durable-artifact round-trip mandate applies (the CLI prints to
stdout; it writes nothing durable to read back).

### 2026-09-09 16:20 — python-coder (status: ok)
feedback-id: fb_2026-09-09_5bb7bf3d
completion_manifest:
  tests_green: true
  red_baseline_turned_green: true
  files_within_size_limit: true
  ruff_error_handling_clean: true
  complexity_check_clean: true
red_baseline_results:
  - test_name: test_single_satisfying_record_reports_the_rule_holds_and_one_examined
    result: green
  - test_name: test_single_violating_record_reports_the_violation_by_name
    result: green
  - test_name: test_uxp_700b_3_i_reachable_from_entry_point
    result: green

NOTE ON WORKTREE LOCATION: this ticket's own .md record exists only in
leafcutter-ai/tickets/00_inbox/epics/EPIC-TruthfulProjectRecord/, but the actual
implementation target (docs/product-truth/scripts/, unit_tests/product_truth/,
scripts/feedback/submit_feedback.py) exists only in the EPIC-TruthfulProjectRecord
worktree (a separate git checkout on branch `EPIC-TruthfulProjectRecord`, not this
ticket file's own branch `epic/uxp-700-product-truth`). All code was written and
tests were run in that worktree per the dispatch's worktree_path; this sign-off is
recorded here because it is the only location where the ticket's own record exists.

Created `docs/product-truth/scripts/universal_rule_check.py` (135 lines) in the
EPIC-TruthfulProjectRecord worktree implementing `check_universal_rule(records,
predicate, kind, name_of=None) -> RuleCheckResult` per test-writer's declared
contract: `RuleCheckResult` is a frozen dataclass with `.outcome` in
`{not_exercised, holds, violated}`, `.examined`, `.kind`, `.violations`; zero
records yields not_exercised, a populated set with no failures yields holds
(examined == len(records)), and any failure yields violated with each failing
record's id in `.violations`. Added a `main(argv)` CLI (`--records/--rule/--kind`,
guarded by `if __name__ == "__main__":`) that reads a JSON records file, applies a
canned `--rule` predicate (`positive`), prints the verdict as JSON to stdout, and
exits 0 for holds/not_exercised, 1 for violated — matching the reachability test's
subprocess contract exactly. All 3 red_baseline tests (run with
`AC_ENFORCE_STRICT=1 python -m pytest unit_tests/product_truth/test_uxp_700b_3_i.py
-v` in the EPIC-TruthfulProjectRecord worktree) now pass; no other test in that
module regressed. `python -m ruff check` (full default ruleset, and the
E722/BLE001/TRY subset the PostToolUse hook enforces) is clean on the new file
after refactoring the CLI's unknown-rule error out of an inline SystemExit message
(TRY003) into a dedicated `UnknownRuleError` class plus a `_unknown_rule_message`
helper. `scripts/commit_guardian/check_complexity.py` passes on the new file.

Ran the module-level `python scripts/commit_guardian/check_documentation.py --file
docs/product-truth/scripts/universal_rule_check.py` gate: it reports a
pre-existing "DIRECTORY README MISSING" violation for
`docs/product-truth/scripts/` — confirmed pre-existing and NOT introduced by this
change by re-running the same check against the sibling `generate_product_truth.py`,
which fails identically (missing README AND missing the MODULE/GOAL/BUSINESS
CONTEXT/ARCHITECTURE docstring fields this ticket's own new file already carries).
Per the ticket mandate (no untracked code changes outside ticket scope), I did not
create `docs/product-truth/scripts/README.md` — that is directory-wide
pre-existing documentation debt unrelated to UXP-700b-3-i and belongs to its own
ticket if the project wants it closed. Flagging here so `documentation-verifier`
and the `commit` phase are not surprised by it.

Also observed 6 pre-existing, unrelated failures in
`unit_tests/test_commit_stage_output_product_truth.py` (Windows `cp1252`
`UnicodeEncodeError` on a subprocess stdin write containing a `→` character) —
confirmed unrelated: that file does not reference `universal_rule_check` and the
failures reproduce identically without this ticket's changes present.

### 2026-09-09 16:45 — test-runner (status: ok)
feedback-id: fb_2026-09-09_a46fbcae
completion_manifest:
  test_suite_executed: true
  all_tests_passing: true
  failure_report_structured: true
Ran `AC_ENFORCE_STRICT=1 python -m pytest unit_tests/product_truth/test_uxp_700b_3_i.py -v`
in the EPIC-TruthfulProjectRecord worktree (this ticket's implementation lives only
there). All 3 tests pass: `test_single_satisfying_record_reports_the_rule_holds_and_one_examined`,
`test_single_violating_record_reports_the_violation_by_name`, and the mandatory
`test_uxp_700b_3_i_reachable_from_entry_point` — 3 passed in 0.44s, exit code 0. No
routing-table entry in the test-runner card matches this repo's directory layout
(no `live_trader`/`sql_functions` here); ran the ticket's own scoped test file per
test-writer's and python-coder's declared verification command instead.

### 2026-09-09 17:10 — ac-validator (status: ok)
feedback-id: (submit-failed)
completion_manifest:
  all_acs_covered: true
  store_alignment_checked: true
  reachability_test_verified: true
`python3 scripts/feedback/submit_feedback.py` exited 1: "Phase 'ac-validator' is not
an allowed writer for category 'complete'" (checked `config/feedback_categories.yaml`
in the EPIC-TruthfulProjectRecord worktree — `ac-validator` appears in no category's
`allowed_writers` list at all). Per skill §2a this is not a phase failure; recorded
`feedback-id: (submit-failed)` and proceeded.

All 5 sub-ACs covered with concrete implementation and test evidence, verified
against the EPIC-TruthfulProjectRecord worktree's staged diff (`git diff --cached`):

- AC-1 (a population of one, satisfying) covered: implementation —
  `docs/product-truth/scripts/universal_rule_check.py` `check_universal_rule()`
  (records=[{"id":"widget-1","value":5}] path, non-empty branch); test —
  `test_single_satisfying_record_reports_the_rule_holds_and_one_examined`
  (`unit_tests/product_truth/test_uxp_700b_3_i.py`).
- AC-2 (reports holds + examined=1) covered: implementation —
  `check_universal_rule()`'s `outcome = "violated" if violations else "holds"` and
  `examined=len(records)`; test — same test, `self.assertEqual(result.outcome,
  "holds")` / `self.assertEqual(result.examined, 1)`.
- AC-3 (does not report not_exercised, satisfying case) covered: implementation —
  the non-empty branch of `check_universal_rule()` never returns `not_exercised`;
  test — same test, `self.assertNotEqual(result.outcome, "not_exercised")`.
- AC-4 (violation naming the record) covered: implementation — `violations =
  [resolve_name(record) for record in records if not predicate(record)]` in
  `check_universal_rule()`; test —
  `test_single_violating_record_reports_the_violation_by_name`, asserts
  `result.violations == ["widget-bad-1"]`.
- AC-5 (does not report not_exercised, violating case) covered: implementation —
  same non-empty branch; test — same test,
  `self.assertNotEqual(result.outcome, "not_exercised")`.

The mandatory reachability test
(`test_uxp_700b_3_i_reachable_from_entry_point`) additionally exercises both arms
through the real CLI subprocess entry point
(`python docs/product-truth/scripts/universal_rule_check.py --records <file> --rule
positive --kind widget`), confirming exit code 0/1 and JSON payload for both the
holds and violated cases. Re-ran `AC_ENFORCE_STRICT=1 python -m pytest
unit_tests/product_truth/test_uxp_700b_3_i.py -v` in the EPIC-TruthfulProjectRecord
worktree: 3 passed in 0.40s. Ran `python
scripts/commit_guardian/check_v2_ac_store_alignment.py --ticket <ticket_path>`: exit
0, no store-alignment violations. This ticket's `## Agent Contracts` has no
`### ac-validator` heading (only `### Expects From`), so skill §2c.2/§2c.3 (per-AC
checkbox flip inside Agent Contracts, AC Coverage table fill) do not apply — flipped
AC-1..AC-5 checkboxes in the ticket's `## Acceptance Criteria` list instead (Step 4a
of my own agent contract) and set `ac_coverage: 5/5` in frontmatter. No `## AC
Coverage` table exists in this ticket body, so §2c.3/Step 4b is a no-op. No
`delivers_to`/`expects_from` cross-file contract gap applies: the ticket's own
`Expects From: UXP-700b-3` contract concerns the not-exercised boundary pinned by a
sibling ticket (UXP-700b-3), not a data path this module reads — and test-writer's
`cross_layer_seam_answer: not_applicable` (this module has no consumer yet) is
independently confirmed by grepping the diff: no other file in the staged or
unstaged diff imports `universal_rule_check`. All 3 sign-offs (test-writer,
python-coder, test-runner) are present and consistent with the diff. Commit phase
may proceed.

### 2026-09-09 15:53 — ac-fulfillment-gate (status: question)
feedback-id: (submit-failed)
completion_manifest:
  ac_yaml_located:
    result: false
    reason: "Resolved via ac_coverage_resolver.py to a real, parseable AC YAML file
      (work_status: todo, covered_by: [], implemented_by: [<ticket path
      placeholder>], level: L3), but two blocking ambiguities prevent a safe
      verdict on it — see reason below."
    remediation: "See prose; needs a human decision, not a mechanical fix."
  auto_fix_evidence_determined: false
`python3 scripts/feedback/submit_feedback.py --category convention-ambiguity` exited
1: "Phase 'ac-fulfillment-gate' is not an allowed writer for category
'convention-ambiguity'." Recorded `feedback-id: (submit-failed)` per skill §2a and
proceeded.

Stopping per the Stop-and-Ask Rule rather than emitting ok/blocker, because two
compounding ambiguities make this ticket's AC-store evidence undeterminable by the
mechanical procedure as written:

1. **`files_touched: []` is empty in this ticket's own frontmatter.** My
   instructions require `implemented_by`/`work_status` auto-fix evidence to be the
   intersection of `files_touched` and the branch diff — with `files_touched`
   empty, that intersection is unconditionally empty regardless of what the diff
   actually contains, which is exactly the Stop-and-Ask condition ("`files_touched`
   ... missing or empty and auto-fix evidence cannot be determined").
2. **The resolved AC YAML lives in a different git worktree than the one the diff
   evidence lives in, with no diff evidence of its own.** `ac_coverage_resolver.py`
   (run against this ticket's own path) resolved `ac_yaml_path` to
   `leafcutter-ai\docs\acceptance-criteria\ux-prototyping\UXP-700-truthful-project-record\UXP-700b-3-i.yaml`
   — because it walks up from the ticket file's own location, and this ticket's
   `.md` record lives only in the `leafcutter-ai` worktree (branch
   `epic/uxp-700-product-truth`). But per python-coder's and test-runner's own
   comments above, all implementation and test files
   (`docs/product-truth/scripts/universal_rule_check.py`,
   `unit_tests/product_truth/test_uxp_700b_3_i.py`) exist ONLY in the
   `EPIC-TruthfulProjectRecord` worktree (a linked worktree of the same repo, but a
   different branch, `EPIC-TruthfulProjectRecord`, at a different, unrelated
   commit) — the one I was dispatched into via `worktree_path`. Confirmed
   mechanically: `git diff main...HEAD --name-only` and `git diff HEAD --name-only`
   in `leafcutter-ai` show zero occurrences of `universal_rule_check` or
   `test_uxp_700b_3_i`; the same commands in `EPIC-TruthfulProjectRecord` show both
   (staged). A second copy of the AC YAML also exists at the equivalent path inside
   `EPIC-TruthfulProjectRecord` (same `work_status: todo`, `covered_by: []`), so
   editing "the AC YAML" is ambiguous between two real, divergent-branch files.
   Marking the `leafcutter-ai` copy `work_status: done` would assert fulfillment in
   a repo/branch whose own diff shows no corresponding change — a false claim by
   construction. Marking the `EPIC-TruthfulProjectRecord` copy instead would
   satisfy the mechanical diff check but contradicts Step 1's explicit instruction
   to use the resolver's own `ac_yaml_path` verbatim, never reconstruct one.

   For reference, independent of the above (informational only — no edit made): a
   grep for `# covers: UXP-700b-3-i` in `EPIC-TruthfulProjectRecord`'s
   `unit_tests/` does find three matching tags in
   `unit_tests/product_truth/test_uxp_700b_3_i.py`, so `covered_by` auto-fix (§3c)
   would be mechanically satisfiable there if the worktree ambiguity above were
   resolved in that worktree's favor.

Options for the user: (a) populate `files_touched` on this ticket with the two
implementation files listed above so the diff-intersection check can run, and (b)
confirm which AC YAML copy is canonical for this cross-worktree ticket layout — the
`leafcutter-ai` copy the resolver returns by walking up from the ticket path, or
the `EPIC-TruthfulProjectRecord` copy that actually has matching diff evidence — so
future `ac-fulfillment-gate` runs on this epic's tickets have an unambiguous
target. No AC YAML file was edited by this run; frontmatter `agents:` and the
`## Sign-offs` checkbox for `ac-fulfillment-gate` are left as `needed` /
unchecked per skill §1 and §3 (question halts, it does not sign off).

### 2026-09-09 18:05 — commit (status: blocker)
feedback-id: (not submitted — see below)
completion_manifest:
  pre_commit_hooks_pass:
    result: not_applicable
    reason: "No commit was attempted; blocked before Step 1 (staging inspection) per the
      analysis below."
    remediation: "Resolve the two open items in ac-fulfillment-gate's question, then
      re-dispatch commit."
  commit_message_valid:
    result: not_applicable
    reason: "No commit message was drafted — there is no unambiguous, single-ticket
      change set to describe yet."
    remediation: "Same as above."
  ticket_staged:
    result: false
    reason: "This ticket's own .md record is staged (uncommitted) in the leafcutter-ai
      worktree (branch epic/uxp-700-product-truth), but no git commit exists yet for any
      of this ticket's sign-off history."
    remediation: "Commit once the ac-fulfillment-gate question is resolved and the
      staged-file set in the implementation worktree is scoped to this ticket alone (see
      below)."

Not committing. Refusing to proceed past ac-fulfillment-gate's still-open question
(status: question, 2026-09-09 15:53 above) rather than route around it:

1. **ac-fulfillment-gate has not signed off.** Both the frontmatter
   (`agents.ac-fulfillment-gate: needed`) and the `## Sign-offs` checkbox for
   `ac-fulfillment-gate` are still unchecked. Its comment explicitly halted with two
   unresolved ambiguities requiring a human decision (empty `files_touched` blocking
   the diff-intersection auto-fix check, and a two-worktree/two-branch split between
   where this ticket's `.md` record lives — `leafcutter-ai`, branch
   `epic/uxp-700-product-truth` — and where the implementation/test files actually
   live — `EPIC-TruthfulProjectRecord`, branch `EPIC-TruthfulProjectRecord`, an
   unrelated commit). Committing now would route around that open question rather
   than resolve it.
2. **Independently confirmed the worktree split is real and current.** In
   `EPIC-TruthfulProjectRecord` (this dispatch's own `worktree_path`),
   `git diff --cached --name-status` shows exactly the files python-coder and
   test-runner described (`docs/product-truth/scripts/universal_rule_check.py`,
   `docs/product-truth/scripts/validate_product_truth.py`,
   `unit_tests/product_truth/__init__.py`,
   `unit_tests/product_truth/test_uxp_700b_3_i.py`) plus three more test files
   carrying `# covers:` tags for *other* tickets in this same epic
   (`test_uxp_700a_1_ii.py`, `test_uxp_700b_1_ii.py`, `test_uxp_700c_1.py`) — a mixed
   set spanning at least four tickets, not this ticket alone (AC BO-1100b-1
   mixed-set condition). There is no single-ticket commit to cut here without either
   (a) waiting for those sibling tickets' own commit dispatches, or (b) a human
   decision on how this epic's cross-ticket batching is meant to work.
3. **The `leafcutter-ai` worktree has zero implementation evidence for this ticket.**
   `git diff main...HEAD --name-only` and `git diff HEAD --name-only` there show no
   occurrence of `universal_rule_check` or `test_uxp_700b_3_i` — matching
   ac-fulfillment-gate's own finding. Committing the staged ticket-record edits there
   alone (without the code) would produce a commit whose message claims a feature
   landed while the diff contains only frontmatter/checkbox churn.

No git operations were performed (no `git add`, no `git commit`) in either worktree
by this run. Frontmatter `agents.commit` and the `## Sign-offs` checkbox for `commit`
are left as `needed` / unchecked, matching skill §1/§3 (a blocker halts, it does not
sign off).

Handing back to `ac-fulfillment-gate`: its open question needs a human answer on (a)
populating `files_touched` for this ticket, and (b) which worktree/branch is
canonical for this epic's AC-YAML and commit boundary, before `commit` can produce a
scoped, truthful commit. `scripts/feedback/submit_feedback.py` was not invoked for
this comment — the prior two comments' own attempts (`ac-validator`,
`ac-fulfillment-gate`) already established that neither is an allowed writer for the
categories they tried, and `commit` has no clearly-applicable category here (this is
a process blocker, not a convention-ambiguity or completion event); recording
`feedback-id: (not submitted — see below)` per skill §2a rather than guessing a
category and risking a similar rejection.
