---
ac_traceability:
  id: BO-1000c-1a
  path: docs/acceptance-criteria/build-orchestration/BO-1000-live-automation-progress/BO-1000c-1a.yaml
advances_current_outcome: true
agents:
  commit: signed_off
  pr-reviewer: signed_off
  pull-request: needed
  python-coder: signed_off
  test-runner: signed_off
  test-writer: signed_off
change_target: pipeline
complexity: medium
components:
- build_orchestration
created: '2026-07-20'
depends_on:
- BO-1000c
- BO-1000a-1
- BO-1000b-1
files_touched:
- templates/workflows-js/finalize-feature.js
priority: medium
requires_adr: false
requires_diagram: false
risk_surface: internal
roadmap_phase: phase_1
source_ac: BO-1000c-1a
status: done
title: Background finalize appends each progress line to a durable, pollable run-progress
  journal as it happens
---

# Background finalize appends each progress line to a durable, pollable run-progress journal as it happens

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BO-1000c-1a` — Background finalize appends each progress line to a durable, pollable run-progress journal as it happens — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BO-1000c-1a`. Component: `build-orchestration`. Assigned agent: `python-coder`. Estimated complexity: `M`. Complexity: `medium`.

## Acceptance Criteria

```gherkin
Given the finalize-feature workflow is running in the background while the
  user remains in the main conversation,
When the workflow emits a start-of-step or per-step-outcome progress line,
Then that same line is appended, at the moment it is emitted, to a durable
  run-progress record for the run that a caller outside the background run can
  read while the run is still in flight,
And the record preserves the emission order of the lines and is written
  incrementally over the run rather than only flushed once at the end.
```

- [ ] AC-1: that same line is appended, at the moment it is emitted, to a durable
- [ ] AC-2: the record preserves the emission order of the lines and is written

## Test Requirements

```yaml
tests:
- name: test_progress_line_appended_to_journal_at_moment_it_is_emitted
  file: unit_tests/workflows/test_bo_1000c_1a.py
  covers:
  - BO-1000c-1a
  asserts: When the workflow emits a start-of-step or per-step-outcome line, that
    same line is appended to the durable run-progress journal at the moment it is
    emitted.
  framework: unittest
  type: behavioral
- name: test_journal_readable_by_external_caller_while_run_in_flight
  file: unit_tests/workflows/test_bo_1000c_1a.py
  covers:
  - BO-1000c-1a
  asserts: A caller outside the background run can read the run-progress journal entries
    while the run is still in flight, before it completes.
  framework: unittest
  type: behavioral
- name: test_journal_preserves_emission_order
  file: unit_tests/workflows/test_bo_1000c_1a.py
  covers:
  - BO-1000c-1a
  asserts: The journal preserves the emission order of the progress lines.
  framework: unittest
  type: behavioral
- name: test_journal_written_incrementally_not_only_at_end
  file: unit_tests/workflows/test_bo_1000c_1a.py
  covers:
  - BO-1000c-1a
  asserts: The journal is written incrementally over the course of the run (append-as-you-go),
    not flushed only once at the end.
  framework: unittest
  type: behavioral
```

## Implementation Notes

```yaml
config_schema_fragment: null
n_location_rule: all
notes: "Surface: the .js workflow body (templates/workflows-js/finalize-feature.js)\
  \ —\npython-coder per repo precedent (BP-1000). This is the producer half of the\n\
  split from BO-1000c-1; the launcher/relay half is BO-1000c-1b (llm-expert).\nPolicy\
  \ constraints (state WHAT, coder picks HOW):\n- Every start-of-step (BO-1000a-1)\
  \ and per-step-outcome (BO-1000b-1) line\n  must be appended to a durable run-progress\
  \ journal at the SAME point it is\n  emitted — append-as-you-go, not a single end-of-run\
  \ flush — so an external\n  poller sees lines while the run is in flight.\n- The\
  \ journal must live at a path the launcher can locate deterministically\n  for THIS\
  \ run (e.g. keyed by worktree/run id), be append-only, and preserve\n  emission\
  \ order (line ordering is the contract BO-1000c-1b relays).\n- Journal writes must\
  \ follow the project error-handling policy for file I/O\n  and must never take down\
  \ the finalize run: a journal-write failure degrades\n  to best-effort (log at WARNING)\
  \ rather than aborting the workflow.\n- No new config key is introduced by this\
  \ AC beyond the journal path\n  convention. ADR-001 build.py round-trip parity on\
  \ the template workflow.\n"
post_write_commands:
- python scripts/build.py --target-dir .
reference_file_path: templates/workflows-js/finalize-feature.js
required_skills:
- python-coder
```

## Agent Contracts

### Delivers To

- **Agent:** llm-expert
- **Contract:** A durable append-only run-progress journal for the run at a launcher-locatable path (keyed by worktree/run id). Each entry is one human-readable progress line (a start-of-step or per-step-outcome line) in emission order, written incrementally while the run is in flight. Consumed by BO-1000c-1b (poll + relay), BO-1000c-2 (over-time delivery), and BO-1000c-2-i (halting-step line).

### Expects From

- **AC:** BO-1000b-1
- **Contract:** The start-of-step (BO-1000a-1) and per-step-outcome (BO-1000b-1) lines as they are emitted, which this AC also appends to the journal.

## Sign-offs

- [x] test-writer — 2026-07-22 10:14
- [x] python-coder — 2026-07-22 11:50
- [x] test-runner — 2026-07-22 10:46
- [x] pr-reviewer — 2026-07-22 12:00
- [x] commit — 2026-07-22 12:10
- [ ] pull-request

## Comments

### 2026-07-22 11:50 — python-coder (status: ok)
feedback-id: fb_2026-07-22_96e86aea
completion_manifest:
  ac1_journal_append_mechanism_defined: true
  ac1_journal_append_in_narrate: true
  ac1_journal_append_in_outcome: true
  ac1_journal_path_variable_defined: true
  ac1_journal_path_keyed_to_run_context: true
  ac2_append_mode_not_overwrite: true
  ac2_no_overwrite_anti_pattern: true
  ac2_both_functions_covered: true
  ac2_incremental_not_deferred: true
  ac2_stepOutcomes_preserved: true
  all_10_red_baseline_tests_green: true
context_capsule:
  agent_id: python-coder
  intent: "Add append-only run-progress journal to finalize-feature.js so each narrate/outcome line is durably written at emission time, readable by an external poller mid-run."
  files_touched_rationale: |
    templates/workflows-js/finalize-feature.js — added journalPath const keyed to WORKTREE_ROOT, appendJournal() helper using fs.appendFileSync (best-effort, catches and logs on error), and calls in narrate() and outcome().
  consumers_checked: |
    No blast-radius analysis required — appendJournal is a new local helper; narrate/outcome signatures are unchanged; stepOutcomes[] retained for downstream consumers.
  red_baseline: |
    test_ac1_journal_append_mechanism_defined_in_js, test_ac1_journal_append_called_inside_narrate_function_body, test_ac1_journal_append_called_inside_outcome_function_body, test_ac1_journal_path_variable_defined, test_ac1_journal_path_keyed_to_run_context, test_ac2_journal_uses_append_mode_not_overwrite, test_ac2_journal_append_call_count_covers_both_narrate_and_outcome, test_ac2_journal_append_not_deferred_to_end_of_workflow, test_ac2_stepOutcomes_array_is_not_the_only_journal_mechanism
  design_constraints: |
    journalPath placed after WORKTREE_ROOT is set (line ~366) so const is initialized before first narrate() call at step 0. appendJournal uses require('fs') inside try/catch so a missing fs module degrades gracefully (WARNING log, never aborts). stepOutcomes[] retained unchanged.
Implemented AC BO-1000c-1a: added journalPath (WORKTREE_ROOT/run-progress.journal.jsonl) and appendJournal() helper to finalize-feature.js; both narrate() and outcome() now call appendJournal() at emission time using fs.appendFileSync (append-only, preserves order). All 10 red_baseline tests flipped green (AC_ENFORCE_STRICT=1 verified). Pre-existing depends_on guard hook fires on every ticket edit — not introduced by this phase.
red_baseline_results:
  - test_name: test_ac1_journal_append_mechanism_defined_in_js
    result: green
  - test_name: test_ac1_journal_append_called_inside_narrate_function_body
    result: green
  - test_name: test_ac1_journal_append_called_inside_outcome_function_body
    result: green
  - test_name: test_ac1_journal_path_variable_defined
    result: green
  - test_name: test_ac1_journal_path_keyed_to_run_context
    result: green
  - test_name: test_ac2_journal_uses_append_mode_not_overwrite
    result: green
  - test_name: test_ac2_journal_append_call_count_covers_both_narrate_and_outcome
    result: green
  - test_name: test_ac2_journal_append_not_deferred_to_end_of_workflow
    result: green
  - test_name: test_ac2_stepOutcomes_array_is_not_the_only_journal_mechanism
    result: green

### 2026-07-22 10:14 — test-writer (status: ok)
feedback-id: fb_2026-07-22_da19be91
completion_manifest:
  tests_written: true
  tests_red: true
  ac1_covered: true
  ac2_covered: true
Wrote 10 unittest tests in unit_tests/workflows/test_bo_1000c_1a.py covering AC-1 (journal-append at moment of emission, file-based path, external readability) and AC-2 (append mode, emission order, incremental writes). Verification run produced 9 failures and 1 vacuous-pass negative contract test; overall exit code is non-zero (red). Pre-existing `depends_on` references in the ticket trigger the frontmatter guard hook on every edit — this is not introduced by test-writer and does not affect sign-off correctness.

Note: no `## AC Coverage` table exists in this ticket and no `### test-writer` section in `## Agent Contracts`, so §2c table-fill is skipped (nothing to fill). The two ACs covered by the tests are:
  - AC-1 (appended at moment of emission / externally readable): test_ac1_journal_append_mechanism_defined_in_js, test_ac1_journal_append_called_inside_narrate_function_body, test_ac1_journal_append_called_inside_outcome_function_body, test_ac1_journal_path_variable_defined, test_ac1_journal_path_keyed_to_run_context
  - AC-2 (emission order / incremental): test_ac2_journal_uses_append_mode_not_overwrite, test_ac2_no_overwrite_of_journal_file, test_ac2_journal_append_call_count_covers_both_narrate_and_outcome, test_ac2_journal_append_not_deferred_to_end_of_workflow, test_ac2_stepOutcomes_array_is_not_the_only_journal_mechanism

red_baseline:
  - test_name: test_ac1_journal_append_mechanism_defined_in_js
    file: unit_tests/workflows/test_bo_1000c_1a.py
    error: "AssertionError: False is not true : finalize-feature.js does not define or call a journal-append mechanism (appendJournal, writeProgressLine, fs.appendFileSync, appendFileSync)."
  - test_name: test_ac1_journal_append_called_inside_narrate_function_body
    file: unit_tests/workflows/test_bo_1000c_1a.py
    error: "AssertionError: False is not true : The narrate() function body does not contain a journal-append call (appendJournal, writeProgressLine, fs.appendFileSync, etc.)."
  - test_name: test_ac1_journal_append_called_inside_outcome_function_body
    file: unit_tests/workflows/test_bo_1000c_1a.py
    error: "AssertionError: False is not true : The outcome() function body does not contain a journal-append call (appendJournal, writeProgressLine, fs.appendFileSync, etc.)."
  - test_name: test_ac1_journal_path_variable_defined
    file: unit_tests/workflows/test_bo_1000c_1a.py
    error: "AssertionError: False is not true : finalize-feature.js does not declare a journal-path variable (journalPath, progressJournalPath, runProgressPath, etc.)."
  - test_name: test_ac1_journal_path_keyed_to_run_context
    file: unit_tests/workflows/test_bo_1000c_1a.py
    error: "AssertionError: unexpectedly None : finalize-feature.js does not declare a journal-path variable (journalPath, progressJournalPath, etc.)."
  - test_name: test_ac2_journal_uses_append_mode_not_overwrite
    file: unit_tests/workflows/test_bo_1000c_1a.py
    error: "AssertionError: False is not true : finalize-feature.js does not contain an append-mode file write (fs.appendFileSync, fs.appendFile, or open flag 'a')."
  - test_name: test_ac2_journal_append_call_count_covers_both_narrate_and_outcome
    file: unit_tests/workflows/test_bo_1000c_1a.py
    error: "AssertionError: Lists differ: ['narrate() body: no journal-append call found', 'outcome() body: no journal-append call found'] != []"
  - test_name: test_ac2_journal_append_not_deferred_to_end_of_workflow
    file: unit_tests/workflows/test_bo_1000c_1a.py
    error: "AssertionError: False is not true : Neither narrate() nor outcome() contains an inline journal-append call."
  - test_name: test_ac2_stepOutcomes_array_is_not_the_only_journal_mechanism
    file: unit_tests/workflows/test_bo_1000c_1a.py
    error: "AssertionError: False is not true : finalize-feature.js only has the stepOutcomes[] in-memory accumulation — no file-based journal-append mechanism was found."
  - test_name: test_ac2_no_overwrite_of_journal_file
    file: unit_tests/workflows/test_bo_1000c_1a.py
    error: "(passes immediately — negative contract test: asserts no writeFileSync(journal...) exists; vacuously true pre-implementation; will catch regression if coder uses wrong write mode)"
    note: "passes immediately — may be under-specified"

### 2026-07-22 12:10 — commit (status: ok)
feedback-id: fb_2026-07-22_8cd9c783
completion_manifest:
  pre_commit_hooks_pass: true
  commit_message_valid: true
  ticket_staged: true
Auto-authorized commit gate (supervised path): staged templates/workflows-js/finalize-feature.js and unit_tests/workflows/test_bo_1000c_1a.py; committed feat(finalize): add run-progress journal append (BO-1000c-1a). Also fixed pre-existing pr-reviewer Sign-offs parity gap (frontmatter showed signed_off but checkbox was unchecked).

### 2026-07-22 10:46 — test-runner (status: ok)
feedback-id: fb_2026-07-22_922d717e
completion_manifest:
  test_suite_executed: true
  all_tests_passing: true
  failure_report_structured: true
Ran 10 tests in unit_tests/workflows/test_bo_1000c_1a.py via `python3 -m pytest -v` (single-file action). All 10 passed: 0 failures, 0 errors, 0 skipped (elapsed 0.06s). Tests verify that finalize-feature.js implements the append-only run-progress journal (appendJournal helper, journalPath const, calls inside narrate() and outcome()) as required by AC BO-1000c-1a.
