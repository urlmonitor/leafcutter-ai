---
ac_traceability:
  id: UXP-700e-3
  path: docs/acceptance-criteria/ux-prototyping/UXP-700-truthful-project-record/UXP-700e-3.yaml
advances_current_outcome: true
agents:
  ac-fulfillment-gate: needed
  ac-validator: needed
  architect-review: needed
  commit: needed
  documentation-expert: needed
  documentation-verifier: needed
  frontend-coder: not_needed
  pr-reviewer: not_needed
  pull-request: not_needed
  python-coder: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
  user-surface-smoker: not_needed
change_target:
- code
- schema
complexity: medium
components:
- ux_prototyping
created: '2026-09-09'
depends_on: []
documentation_required: true
files_touched:
- docs/components.json
- docs/product-truth/schemas/flow.schema.json
priority: medium
requires_adr: false
requires_diagram: false
risk_surface: internal
roadmap_phase: phase_1
source_ac: UXP-700e-3
status: todo
title: The record's labels come from declared vocabularies, so one thing has one name
---

# The record's labels come from declared vocabularies, so one thing has one name

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `UXP-700e-3` — The record's labels come from declared vocabularies, so one thing has one name — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `UXP-700e-3`. Component: `ux-prototyping`. Assigned agent: `python-coder`. Estimated complexity: `M`. Complexity: `medium`.

## Acceptance Criteria

```gherkin
Given the labels a journey carries - the component it belongs to, and the tags it is filed
  under,
When the record is checked,
Then each component label resolves to a component registered in the project, and one that
  does not is reported naming the artifact and the label,
And each tag matches the declared shape for a tag, and one that does not is reported the same
  way,
And the check states how many labels it resolved, so a run over a record carrying no labels is
  distinguishable from a run in which every label resolved,
And a label that differs from a registered one only by case or by spacing is reported as the
  registered one having been meant, rather than as a new label.
```

- [ ] AC-1: each component label resolves to a component registered in the project, and one that
- [ ] AC-2: each tag matches the declared shape for a tag, and one that does not is reported the same
- [ ] AC-3: the check states how many labels it resolved, so a run over a record carrying no labels is
- [ ] AC-4: a label that differs from a registered one only by case or by spacing is reported as the

## Test Requirements

```yaml
tests:
- name: test_unregistered_component_label_is_reported_with_artifact_and_label
  file: unit_tests/product_truth/test_uxp_700e_3.py
  covers:
  - UXP-700e-3
  asserts: A journey labelled with a component absent from the registry is reported
    naming both.
  framework: unittest
  type: integration
  angle: criterion
- name: test_tag_not_matching_the_declared_shape_is_reported
  file: unit_tests/product_truth/test_uxp_700e_3.py
  covers:
  - UXP-700e-3
  asserts: A tag violating the declared pattern is reported naming the artifact and
    the tag.
  framework: unittest
  type: integration
  angle: criterion
- name: test_near_miss_label_is_reported_as_the_registered_one_having_been_meant
  file: unit_tests/product_truth/test_uxp_700e_3.py
  covers:
  - UXP-700e-3
  asserts: A component label differing only by case or spacing is reported as a near
    miss naming the registered label.
  framework: unittest
  type: unit
  angle: boundary
- name: test_resolved_label_count_rises_with_the_number_of_labels
  file: unit_tests/product_truth/test_uxp_700e_3.py
  covers:
  - UXP-700e-3
  asserts: Adding one labelled artifact raises the stated resolved-label count.
  framework: unittest
  type: integration
  angle: seam
- name: test_uxp_700e_3_reachable_from_entry_point
  file: unit_tests/product_truth/test_uxp_700e_3.py
  covers:
  - UXP-700e-3
  angle: reachability
  asserts: 'REQUIRED — invoke the production entry point (CLI, hook, slash command,
    workflow dispatch, or main()) as a subprocess/dispatch and assert the new behaviour
    actually occurs. Do NOT satisfy this by importing the function directly. The AC
    authored no test_spec, so the entry point is not declared: resolve it before writing
    this test, and do not delete this entry.'
```

## Agent Contracts

### documentation-expert

Existing docs to update / cross-link:

- docs/components.json (relationship: specifies | status: exists | relevance: The registry component labels must resolve against.)
- docs/product-truth/schemas/flow.schema.json (relationship: specifies | status: exists)

- [ ] AC-1: reference-doc | docs/components.json | each component label resolves to a component registered in the project, and one that

### Expects From

- **AC:** UXP-700e-1
- **Contract:** The declared-shape mechanism the tag pattern is expressed through.

## Sign-offs

- [ ] architect-review
- [ ] test-writer
- [ ] python-coder
- [ ] test-runner
- [ ] documentation-expert
- [ ] ac-validator
- [ ] ac-fulfillment-gate
- [ ] documentation-verifier
- [ ] commit

## Comments
