---
title: "Create .github/ISSUE_TEMPLATE/bug.yml on leafcutter-ai"
status: todo
components:
  - documentation_system
  - infrastructure
created: 2026-05-19
depends_on: []
priority: low
requires_diagram: false
requires_adr: false
files_touched:
  - leafcutter/.github/ISSUE_TEMPLATE/bug.yml
agents:
  architect-review: not_needed
  python-coder: not_needed
  test-writer: not_needed
  documentation-expert: needed
  pr-reviewer: needed
  commit: needed
  pull-request: needed
  sql-coder: not_needed
---

# 02: Create .github/ISSUE_TEMPLATE/bug.yml on leafcutter-ai

## Goal

In order to collect reproducible bug reports from external consumers of `leafcutter-ai`, we need a structured GitHub Issue form that captures all the information needed to reproduce and fix the bug.

## Context

Stub generated by create-epic. Harden with create-ticket (depth 3).

All changes are on the `urlmonitor/leafcutter-ai` upstream repo. This ticket can run in parallel with 01, 03, 04, 05, 06, 07, 08.

Required fields based on the CANDIDATE spec: title prefix, description, steps to reproduce, expected vs. actual behaviour, environment (OS, Python version, leafcutter version).

## Acceptance Criteria

```gherkin
Given leafcutter-ai is published on GitHub and config.yml disables blank issues
When a user selects "Bug Report" from the issue template picker
Then they see a structured form with fields: description, steps to reproduce, expected behaviour, actual behaviour, environment (OS, Python version, leafcutter version)
And the issue title is prefixed with "[Bug]"
And all required fields are marked required
```

## Sign-offs

- [ ] documentation-expert
- [ ] pr-reviewer
- [ ] commit
- [ ] pull-request

## Comments

## Implementation Tasks

### documentation-expert
- [ ] Author `.github/ISSUE_TEMPLATE/bug.yml` with:
  - `name: Bug Report`
  - `title: "[Bug] "`
  - `labels: ["bug"]`
  - Fields: description (required), steps-to-reproduce (required), expected-behaviour (required), actual-behaviour (required), environment (OS, Python version, leafcutter version — required)

## Risk & Safety
- Touches money? No.
- Touches data? No.
- Reversibility? Fully reversible — delete or revert the file; no downstream consequence.
