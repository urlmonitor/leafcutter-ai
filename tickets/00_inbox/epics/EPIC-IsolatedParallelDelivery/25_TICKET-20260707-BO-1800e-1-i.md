---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  pr-reviewer: needed
  pull-request: needed
  python-coder: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
components:
- build-orchestration
created: '2026-07-07'
depends_on:
- BO-1800e-1
files_touched:
- docs/architecture/adrs/ADR-018-agent-isolation-topology.md
- docs/architecture/components/commit-guardian.md
priority: medium
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: BO-1800e-1-i
status: todo
title: An attempt to commit onto local main during a drive is prevented or flagged
---

# An attempt to commit onto local main during a drive is prevented or flagged

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BO-1800e-1-i` — An attempt to commit onto local main during a drive is prevented or flagged — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BO-1800e-1-i`. Component: `build-orchestration`. Assigned agent: `python-coder`. Estimated complexity: `S`.

## Acceptance Criteria

```gherkin
Given a drive clone whose current branch is `main`,
When a drive step attempts `git commit` onto `main`,
Then the local pre-commit main-guard prevents or flags the commit with a message
  saying work must go on a feature branch, not `main`,
And this local guard is defense-in-depth only -- it is explicitly NOT the
  guarantee (the server-side gate in BO-1800b is), so a bypass of the local guard
  still cannot land on the hub's `main`.
```

## Sign-offs

- [ ] python-coder
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit
- [ ] pull-request

## Comments
