---
ac_traceability:
  id: ACD-400b-7
  path: docs/acceptance-criteria/ac-driven-dev/ACD-400b-7.yaml
advances_current_outcome: true
agents:
  ac-fulfillment-gate: needed
  ac-validator: needed
  architect-review: needed
  commit: needed
  documentation-expert: needed
  documentation-verifier: needed
  pr-reviewer: needed
  pull-request: needed
  python-coder: needed
  test-runner: needed
  test-writer: signed_off
change_target: code
complexity: low
components:
- ac_driven_dev
created: '2026-08-13'
depends_on: []
documentation_required: true
files_touched:
- scripts/ac_store/generate_ticket_from_ac.py
- templates/hooks/ticket_frontmatter_guard.py
priority: high
requires_adr: false
requires_diagram: false
risk_surface: contract_boundary
roadmap_phase: phase_1
source_ac: ACD-400b-7
status: todo
title: Generated standalone ticket's depends_on never leaks AC-level dependencies
  and passes the frontmatter guard
---

# Generated standalone ticket's depends_on never leaks AC-level dependencies and passes the frontmatter guard

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `ACD-400b-7` — Generated standalone ticket's depends_on never leaks AC-level dependencies and passes the frontmatter guard — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `ACD-400b-7`. Component: `ac-driven-dev`. Assigned agent: `python-coder`. Estimated complexity: `S`. Complexity: `low`.

## Acceptance Criteria

```gherkin
Given an AC whose depends_on lists other AC identifiers (such as its own
  parent AC),
When the generator produces a standalone ticket from that AC,
Then the generated ticket's depends_on does not contain any of the AC's
  depends_on identifiers,
And the generated ticket's depends_on is either empty or contains only
  references to other tickets that exist in the same location,
And running the ticket_frontmatter_guard against the generated ticket exits
  with code 0 (no depends_on validation error).
```

- [ ] AC-1: the generated ticket's depends_on does not contain any of the AC's
- [ ] AC-2: the generated ticket's depends_on is either empty or contains only
- [ ] AC-3: running the ticket_frontmatter_guard against the generated ticket exits

## Test Requirements

```yaml
tests:
- name: test_generated_ticket_depends_on_excludes_ac_ids
  file: unit_tests/ac_store/test_acd_400b_7.py
  covers:
  - ACD-400b-7
  asserts: A ticket generated from an AC whose depends_on lists AC ids has a depends_on
    that contains none of those AC ids (empty or ticket-only).
  framework: pytest
  type: behavioral
- name: test_generated_ticket_passes_frontmatter_guard
  file: unit_tests/ac_store/test_acd_400b_7.py
  covers:
  - ACD-400b-7
  asserts: Running templates/hooks/ticket_frontmatter_guard.py against the generated
    ticket exits with code 0 (no depends_on validation error).
  framework: pytest
  type: behavioral
```

## Implementation Notes

```yaml
- 'File of record: scripts/ac_store/generate_ticket_from_ac.py currently copies the
  AC''s depends_on verbatim into ticket frontmatter (the `depends_on: ac.get(''depends_on'')
  or []` path around line 1847); the ticket''s depends_on must be ticket-scoped, not
  AC-scoped.'
- The generated ticket's depends_on must not contain any AC identifier; it must be
  empty or contain only ticket references that resolve in the same location.
- The output must satisfy templates/hooks/ticket_frontmatter_guard.py — the guard
  is the load-bearing enforcement point that rejects AC ids in ticket depends_on;
  assert its exit code, not just a string diff.
- 'Must follow the repo error-handling policy: specific-exception try/except around
  any file/YAML I/O, log at WARNING or higher (or re-raise); no bare or silent excepts.'
- Verify behaviorally — generate a ticket from a crafted AC with AC-id depends_on,
  then run the frontmatter guard against the generated ticket file and assert exit
  0; do not grep the generator source.
```

## Agent Contracts

### documentation-expert

Existing docs to update / cross-link:

- docs/reference/ac-schema.md

- [ ] AC-1: [(unspecified genre)] docs/(unspecified genre)/acd-400b-7.md — the generated ticket's depends_on does not contain any of the AC's

### Delivers To

- **Agent:** python-coder
- **Contract:** A standalone ticket that the frontmatter guard accepts without manual editing of depends_on

### Expects From

- **AC:** ACD-400b-1
- **Contract:** Ticket generation logic that populates ticket frontmatter from the source AC

## Sign-offs

- [ ] architect-review
- [x] test-writer — 2026-08-13 08:35
- [ ] python-coder
- [ ] test-runner
- [ ] documentation-expert
- [ ] pr-reviewer
- [ ] ac-validator
- [ ] ac-fulfillment-gate
- [ ] documentation-verifier
- [ ] commit
- [ ] pull-request

## Comments

### 2026-08-13 08:35 — test-writer (status: ok)
feedback-id: (submit-failed)
completion_manifest:
  test_generated_standalone_ticket_depends_on_excludes_ac_ids_written: true
  test_generated_ticket_passes_frontmatter_guard_written: true
  test_ticket_from_ac_depending_on_parent_not_blocked_by_guard_written: true
  red_baseline_confirmed: true
red_baseline:
  - test_name: test_generated_standalone_ticket_depends_on_excludes_ac_ids
    file: unit_tests/ac_store/test_generator_frontmatter_gaps.py
    error: "AssertionError: generated ticket depends_on leaked the AC-level dependency 'ZZGAP-9000a': depends_on=['ZZGAP-9000a']. assert 'ZZGAP-9000a' not in ['ZZGAP-9000a']"
  - test_name: test_generated_ticket_passes_frontmatter_guard
    file: unit_tests/ac_store/test_generator_frontmatter_gaps.py
    error: "AssertionError: the real ticket_frontmatter_guard.py rejected the generated ticket: TICKET FRONTMATTER VIOLATION ... Issues: - 'depends_on' references missing file: 'ZZGAP-9000b' ... assert not True"
  - test_name: test_ticket_from_ac_depending_on_parent_not_blocked_by_guard
    file: unit_tests/ac_store/test_generator_frontmatter_gaps.py
    error: "AssertionError: depends_on must not contain the parent AC id 'ZZGAP-9000c'; got depends_on=['ZZGAP-9000c']. assert 'ZZGAP-9000c' not in ['ZZGAP-9000c']"
Wrote unit_tests/ac_store/test_generator_frontmatter_gaps.py covering ACD-400b-7 (+ ACD-400b-7-i) and the sibling ticket ACD-400b-6 (+ ACD-400b-6-i) in one shared file, per explicit dispatch instructions. All 6 behavioral tests confirmed RED via `AC_ENFORCE_STRICT=1 python -m pytest ... -v` (6 failed / 0 passed); ruff clean. IMPORTANT finding during authoring: the guard-invoking tests must create a `.git` marker directory directly above the tickets/ folder (see `_make_project_root_marker`) — without it, `ticket_frontmatter_guard.find_project_root()` cannot resolve a project root from a bare `tmp_path`, so the hook silently no-ops (prints a WARNING, emits no decision) and a naive "not blocked" assertion would be a false green. Confirmed this by manually running the guard both with and without the marker. No `## AC Coverage` table is present in this ticket body, so the §2c table-fill step was not applicable; the two `## Test Requirements`-named tests plus the ACD-400b-7-i edge case are covered above.
