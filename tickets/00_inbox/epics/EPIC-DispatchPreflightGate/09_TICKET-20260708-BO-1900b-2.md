---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  pr-reviewer: needed
  pull-request: needed
  python-coder: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
complexity: low
components:
- build_orchestration
created: '2026-07-08'
depends_on:
- BO-1900b
- TKT-200e
files_touched:
- docs/architecture/agent_delivery_workflows.md
- docs/architecture/components/build-ticket-workflow-dispatch.md
priority: high
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: BO-1900b-2
status: todo
title: A premise that still reproduces at dispatch passes the freshness re-check
---

# A premise that still reproduces at dispatch passes the freshness re-check

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BO-1900b-2` — A premise that still reproduces at dispatch passes the freshness re-check — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BO-1900b-2`. Component: `build-orchestration`. Assigned agent: `python-coder`. Estimated complexity: `S`. Complexity: `low`.

## Acceptance Criteria

```gherkin
Given a ticket whose captured claim "these tests are red" still reproduces at dispatch
  time (the captured command re-run yields the same failing result that was recorded),
When the preflight premise re-check runs before spawn,
Then the re-check confirms the premise still holds,
And it contributes a fit verdict for the premise check,
And the dispatch proceeds to spawn the phase agent (no held-back on the premise axis).
```

## Test Requirements

```yaml
tests: []
```

## Sign-offs

- [ ] python-coder
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit
- [ ] pull-request

## Comments
