id: ACD-2500e-1
components:
  - ac_driven_dev
  - agent_registry
  - llm_authoring
readiness: approved
priority: high
title: "The technical planner's profile says it changes and produces requirement records"
component: ac-driven-dev
level: L2
status: active
req_status: draft
work_status: todo
criteria: |
  Given the technical planner's published profile, and the agent card and registry entry
    generated from it,
  When they are read,
  Then the profile declares that the planner changes requirement records in the
    requirement store and names that surface, instead of declaring it changes nothing,
  And it declares that the planner produces requirement records, instead of analysis,
  And its description no longer claims the planner writes the test design or chooses
    reference files,
  And the agent card and the registry entry show the same values as the profile.
depends_on:
  - ACD-2500e
doc_links:
- path: templates/agents/it-po.md
  relationship: describes
  status: exists
- path: docs/architecture/components/agent-registry.md
  relationship: describes
  status: exists
assigned_agent: llm-expert
estimated_complexity: S
it_requirements:
- Canonical source is templates/agents/it-po.md frontmatter; the registry entry (config/agent_registry.json)
  is updated to match, and the generated card is regenerated by the build, never hand-edited.
- The produces value comes from the registry's allowed produces vocabulary (existing produces validation).
- mutates names the requirement store surface (docs/acceptance-criteria/).
- If shipped before ACD-2500e-2, the description clause (clause 3) lands with ACD-2500e-2.
delivers_to: null
expects_from: null
test_spec:
- name: test_it_po_mutates_requirement_store
  target_dir: unit_tests/agents/
  framework: pytest
  type: unit
  description: The it-po profile's mutates entry names the requirement store, not 'none'.
- name: test_it_po_produces_requirement_records
  target_dir: unit_tests/agents/
  framework: pytest
  type: unit
  description: The it-po produces value is the requirement-record value, not 'analysis'.
- name: test_it_po_registry_matches_profile
  target_dir: unit_tests/agents/
  framework: pytest
  type: unit
  description: config/agent_registry.json's it-po entry shows the same produces/mutates values as the
    template frontmatter.
  angle: seam
origin_agent: business-analyst
created: 2026-09-25
amended_by:
- reason: it-po-technical-enrichment
  date: '2026-09-25'
  by: it-po
superseded_by: null
covered_by:
  - ACD-2500e-1-i
implemented_by: []
change_target: config
risk_surface: internal
notes: |
  Today (templates/agents/it-po.md frontmatter): `produces: analysis` and
  `mutates: [{name: none, surface: none}]`, while its tools include Write and Edit and its
  body writes AC YAML. The description still lists "the test contract (test_spec /
  test_required)" as its output.
  Tactical and independent: ACD-2500e notes say this can ship first, so this AC depends
  only on its parent, not on ACD-2500a/b/d. The description clause (clause 3) should land
  with ACD-2500e-2 if shipped early.
