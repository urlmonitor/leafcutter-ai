---
ac_coverage: 1/3
ac_traceability:
  id: GE-120e-4-i
  path: docs/acceptance-criteria/guardrail-engine/GE-120-green-means-checked/GE-120e-4-i.yaml
advances_current_outcome: true
agents:
  ac-fulfillment-gate: failed
  ac-validator: failed
  commit: failed
  pull-request: not_needed
  python-coder: signed_off
  test-runner: failed
  test-writer: signed_off
change_target: code
complexity: low
components:
- commit_guardian
- git_vcs_operations
created: '2026-08-25'
depends_on: []
files_touched: []
priority: high
requires_adr: false
requires_diagram: false
risk_surface: internal
roadmap_phase: phase_1
source_ac: GE-120e-4-i
status: todo
title: Reworking a merge after the operation record is gone still attributes only
  the author's part
---

# Reworking a merge after the operation record is gone still attributes only the author's part

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `GE-120e-4-i` — Reworking a merge after the operation record is gone still attributes only the author's part — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `GE-120e-4-i`. Component: `guardrail-engine`. Assigned agent: `python-coder`. Estimated complexity: `S`. Complexity: `low`.

## Acceptance Criteria

```gherkin
Given a merge commit that has already been made, so no record of an operation in progress
  remains, and the author now revises it — restaging the same merged content plus one edit
  of their own,
When a check that works out its own change set runs on that commit,
Then the change set it inspects holds the author's edit and not the merged-in work,
And the absence of an operation record does not cause the whole staged tree to be
  attributed to the author,
And the verdict is identical to the verdict on the original merge commit plus that same
  edit made as an ordinary follow-up commit.

Coverage note: perform the revision as a real repository operation — the states the commit
is built on are still discoverable from the commit being revised even though the operation
record is gone, and an implementation that reads only the operation record fails this case
while passing every merge fixture in this L1. Execute the check as a process and assert
result status and output text in both arms.
```

- [ ] AC-1: the change set it inspects holds the author's edit and not the merged-in work
- [x] AC-2: the absence of an operation record does not cause the whole staged tree to be
- [ ] AC-3: the verdict is identical to the verdict on the original merge commit plus that same

## Test Requirements

```yaml
tests:
- name: test_ge120e4i_revised_merge_change_set_holds_only_the_authors_edit
  file: unit_tests/portability/test_ge_120e_4_i.py
  covers:
  - GE-120e-4-i
  asserts: After a real merge is revised with one additional authored edit, the check
    inspects that edit and not the merged-in work.
  framework: unittest
  type: behavioral
- name: test_ge120e4i_absent_operation_record_does_not_widen_to_the_whole_staged_tree
  file: unit_tests/portability/test_ge_120e_4_i.py
  covers:
  - GE-120e-4-i
  asserts: With no operation record present, the merged-in content is still absent
    from what the check objects to — the expired-marker trap.
  framework: unittest
  type: behavioral
- name: test_ge120e4i_verdict_matches_the_ordinary_followup_commit_arm
  file: unit_tests/portability/test_ge_120e_4_i.py
  covers:
  - GE-120e-4-i
  asserts: 'Both arms in one case: revised merge plus edit, and original merge plus
    the same edit as a follow-up commit, produce the same status and the same objected-to
    content.'
  framework: unittest
  type: behavioral
- name: test_ge_120e_4_i_reachable_from_entry_point
  file: unit_tests/portability/test_ge_120e_4_i.py
  covers:
  - GE-120e-4-i
  angle: reachability
  asserts: 'REQUIRED — invoke the production entry point (CLI, hook, slash command,
    workflow dispatch, or main()) as a subprocess/dispatch and assert the new behaviour
    actually occurs. Do NOT satisfy this by importing the function directly. The AC
    authored no test_spec, so the entry point is not declared: resolve it before writing
    this test, and do not delete this entry.'
```

## Implementation Notes

```yaml
- SIGNAL ORDER IS THE DEFECT. The states the commit is built on must be consulted
  FIRST; the operation record refines the answer. The named anti-pattern is 'if there
  is no operation record, treat this as an ordinary commit and use the whole staged
  tree' — it reads as a sane default and it fails only on this case, because every
  other fixture in this L1 has the record present.
- THE PARENTS OF THE COMMIT BEING REVISED ARE STILL DISCOVERABLE after the operation
  record has expired. Derive them from the commit under revision, not from the expired
  marker. When the commit being amended has two parents, the derivation must use both.
- 'VERDICT EQUALITY IS AN ASSERTION ABOUT TWO REAL RUNS, not an inference: the revised
  merge plus the edit must produce the same result status and the same objected-to
  content as the original merge followed by the same edit as an ordinary commit. Both
  arms in one case.'
- This case must hold in the deployed layout after build.py (ADR-001), and it must
  hold inside a git worktree — resolve git state through plumbing rather than filesystem
  assumptions.
```

## Agent Contracts

### Expects From

- **AC:** GE-120e-4
- **Contract:** The authored-change derivation that combines the states the commit is built on with the operation record.

## Sign-offs

- [x] test-writer — 2026-08-25 21:40
- [x] python-coder — 2026-09-07 16:31
- [ ] test-runner — failed 2026-09-07 15:46
- [ ] ac-validator — failed 2026-09-07 15:57
- [ ] ac-fulfillment-gate — failed 2026-09-07 16:10
- [ ] commit — failed 2026-09-07 16:22

## Comments

### 2026-08-25 21:40 — test-writer (status: ok)
feedback-id: fb_2026-08-25_6397a99e
completion_manifest:
  tests_written: true
  red_baseline_captured: true
  dependency_gap_flagged: true
Wrote unit_tests/portability/test_ge_120e_4_i.py with the three tests named
in GE-120e-4-i's AC-store test_spec plus the mandatory reachability floor
test. Before writing any assertion, I empirically ran today's actual
`check_contract_shrinking.py` (via two real scratch git fixtures — a real
`git init`/branch/merge, never a synthetic diff) against this exact
AC scenario (a completed, clean, disjoint-file merge, then one edit staged
on top as if for `git commit --amend`) and confirmed it ALREADY produces
the AC-correct exit code purely by coincidence: its MERGE_HEAD-only probe
finds no marker post-merge and falls back to the full unscoped
`git diff --cached`, which happens to already exclude the merge's content
because HEAD already contains it. A naive black-box behavioral test against
today's code would therefore be green before any implementation exists —
the exact under-specified-test trap the test-writer skill's red-baseline
rule and CLAUDE.md's "TDD Order" section both warn against. Neither of this
ticket's two real dependencies exist yet: GE-120e-1's shared
authored-change derivation (no `templates/scripts/commit_guardian/_authored_change.py`
anywhere in the repo — confirmed absent) and GE-120e-4's extension of it to
REVERT_HEAD/CHERRY_PICK_HEAD (ticket 35, still `status: todo`, no such
handling in `check_contract_shrinking.py` today). All 4 tests therefore
fail via an explicit `self.fail()` naming the missing dependency — verified
genuinely RED by running `AC_ENFORCE_STRICT=1 python3 -m pytest
unit_tests/portability/test_ge_120e_4_i.py -v` (4 failed, 0 passed; plain
`pytest` would mask these as `xfail`/exit 0 via `pytest_ac_enforcement`
since this AC's `work_status` is not yet `done` — STRICT mode is required
to see the true failures, per established convention in this epic). The
test file's docstring records the full proposed contract
(`_authored_change.get_authored_change()` and its `AuthoredChange` fields)
for GE-120e-1's and GE-120e-4's implementers to honour or update this file
to match. Each test body is fully real/behavioral once the gate clears: a
real `git init` + two-branch + clean merge fixture, the check executed as a
subprocess (never imported), and — for the reachability test — dispatched
through the real manifest-registered entry point
(`run_hook.py check_contract_shrinking.py`). Also flagging the same
ticket-authoring gap noted on ticket 31 (GE-120e-2-i): this ticket's own
`depends_on:` frontmatter is empty even though the AC store correctly
records `depends_on: [GE-120e-4]` — not fixed here, out of this phase's
scope.

red_baseline:
  - test_name: test_ge120e4i_revised_merge_change_set_holds_only_the_authors_edit
    file: unit_tests/portability/test_ge_120e_4_i.py
    error: "AssertionError: templates/scripts/commit_guardian/_authored_change.py does not exist yet (or does not define get_authored_change()). This is GE-120e-1's shared authored-change derivation, extended by GE-120e-4 to consult REVERT_HEAD/CHERRY_PICK_HEAD — neither has landed. ... This test cannot verify the REAL contract until the shared derivation exists."
  - test_name: test_ge120e4i_absent_operation_record_does_not_widen_to_the_whole_staged_tree
    file: unit_tests/portability/test_ge_120e_4_i.py
    error: "AssertionError: templates/scripts/commit_guardian/_authored_change.py does not exist yet (or does not define get_authored_change()). ... This test cannot verify the REAL contract until the shared derivation exists."
  - test_name: test_ge120e4i_verdict_matches_the_ordinary_followup_commit_arm
    file: unit_tests/portability/test_ge_120e_4_i.py
    error: "AssertionError: templates/scripts/commit_guardian/_authored_change.py does not exist yet (or does not define get_authored_change()). ... This test cannot verify the REAL contract until the shared derivation exists."
  - test_name: test_ge_120e_4_i_reachable_from_entry_point
    file: unit_tests/portability/test_ge_120e_4_i.py
    error: "AssertionError: templates/scripts/commit_guardian/_authored_change.py does not exist yet (or does not define get_authored_change()). ... This test cannot verify the REAL contract until the shared derivation exists."
    note: "Verified red via `AC_ENFORCE_STRICT=1 pytest`; plain pytest run reports these as xfail (exit 0) due to pytest_ac_enforcement masking non-done ACs — do not treat plain-run exit 0 as green."

### 2026-08-25 22:10 — python-coder (status: blocker)
feedback-id: (submit-failed — .leafcutter/feedback_categories.yaml absent in this worktree)
Re-verified test-writer's dependency-gap finding before doing any implementation
work, per Contract-Aware Mode Step 1 (`Expects From` names AC `GE-120e-4` /
"The authored-change derivation that combines the states the commit is built
on with the operation record"):

- `templates/scripts/commit_guardian/_authored_change.py` — CONFIRMED ABSENT
  (`find` over the whole worktree returns nothing). This is GE-120e-1's shared
  authored-change derivation (ticket 28, `status: todo`).
- GE-120e-4's extension of that derivation to consult
  `REVERT_HEAD`/`CHERRY_PICK_HEAD` — CONFIRMED NOT LANDED (ticket 35,
  `status: todo`; no such handling exists in `check_contract_shrinking.py`
  today).
- Re-ran `AC_ENFORCE_STRICT=1 python3 -m pytest
  unit_tests/portability/test_ge_120e_4_i.py -v`: all 4 tests still fail via
  the explicit `self.fail()` naming the missing dependency — red baseline is
  unchanged from test-writer's report.

I am not implementing GE-120e-4-i's revised-merge/expired-marker logic against
today's `check_contract_shrinking.py` directly. This ticket's own Implementation
Notes describe the correct fix in terms of "the states the commit is built on"
vs. "the operation record" as two inputs to a shared derivation that does not
exist yet — writing this AC's logic ad hoc, before GE-120e-1's
`get_authored_change()` lands and GE-120e-4 extends it, would mean re-deriving
(and likely diverging from) that shared contract in a second place, which is
exactly the kind of drift GE-120e-1/GE-120e-4 exist to prevent. It would also
leave the 4 red_baseline tests unable to import the real dependency they are
written against (they `self.fail()` on `_AUTHORED_CHANGE_OK` being False by
design), so no amount of standalone work in this ticket can turn them green.

Missing deliverables:
- `GE-120e-1` (ticket 28, `status: todo`) — shared `get_authored_change()` in
  `templates/scripts/commit_guardian/_authored_change.py`.
- `GE-120e-4` (ticket 35, `status: todo`) — REVERT_HEAD/CHERRY_PICK_HEAD
  extension of that derivation.

Suggested remediation: dispatch `python-coder` on tickets 28 (GE-120e-1) and
35 (GE-120e-4) first, in that dependency order, then re-dispatch `python-coder`
on this ticket (36 / GE-120e-4-i). No files touched in this pass.

### 2026-08-25 22:35 — test-writer (status: ok)
feedback-id: (submit-failed — .leafcutter/feedback_categories.yaml absent in this worktree)
completion_manifest:
  tests_written: true
  red_baseline_still_confirmed: true
  dependency_gap_unchanged: true
Re-dispatched after python-coder's blocker. No new test-writer work is
required: `unit_tests/portability/test_ge_120e_4_i.py` (written in this
ticket's prior test-writer pass) is unchanged and still correct against the
ticket's Test Requirements and this AC's implied contract. Re-verified the
red baseline is unchanged by re-running `AC_ENFORCE_STRICT=1 python3 -m
pytest unit_tests/portability/test_ge_120e_4_i.py -v`: 4 failed, 0 passed,
each via the same explicit `self.fail()` naming the missing shared
dependency. Re-confirmed both blocking dependencies are still absent:
`templates/scripts/commit_guardian/_authored_change.py` does not exist
anywhere in the worktree (GE-120e-1, ticket 28, `status: todo`), and
`check_contract_shrinking.py` has no REVERT_HEAD/CHERRY_PICK_HEAD handling
(GE-120e-4, ticket 35, `status: todo`). python-coder's blocker is correct
and unchanged; there is nothing for test-writer to fix here — the fix is
sequencing (tickets 28 and 35 must land first), not the test file. Leaving
this ticket's own sign-off state as-is (test-writer already `signed_off`
from the prior pass) and adding this comment purely as a re-verification
record for the supervisor.

### Diagnosis before re-drive — 2026-09-07 (session note, not a phase-agent sign-off)

`unit_tests/portability/test_ge_120e_4_i.py` was restored from `98797e669` and its state is
now **2 passed, 2 failed** — down from 4 failed. Two things changed, and neither is this
ticket's remaining work:

1. **A fixture bug is fixed.** `_init_repo()` ran `git init -q` with `cwd=repo` without
   creating `repo`, so all four tests died at setup with
   `[Errno 2] No such file or directory` regardless of implementation quality. One `mkdir`.
2. **`GE-120e-4` has landed** (`_operation_record.py`, ticket 35), so the operation-record
   derivation this file depends on now exists.

**What remains is genuinely this ticket's AC, and it is narrow.** Both surviving failures are
the same assertion: the objection text must **name the author's own weakened test file**.
`check_contract_shrinking.py` currently blocks correctly but its report names only the
violation and the production files:

```
[contract-shrinking guard] BLOCKED
Reason: Staged diff contains test-weakening changes concurrent with production code changes.
Violations detected:
  - pytest.skip added: '+    pytest.skip('
Production files modified:
  - prod/other_feature.py
```

`test_other.py` — the file the author actually weakened — appears nowhere. The test asserts
it must, and it is right to: "a check that does raise one is named, together with the content
it objected to" is the same three-things-named requirement `GE-120e-2-i` applies to its sweep.
An objection that names the production file but not the weakened test tells the author what
they changed, not what they broke.

So the fix is to the **objection text**, not to the derivation. Do not touch
`_authored_change.py` or `_operation_record.py` — both are merged-adjacent and their own ACs
are covered. `python-coder` is `failed` from the earlier pass, before either dependency
existed; re-dispatch it against this diagnosis.

### 2026-09-07 15:46 — test-runner (status: blocker)
feedback-id: fb_2026-09-07_b8765ef6
completion_manifest:
  test_suite_executed: true
  all_tests_passing:
    result: false
    reason: "2 of 4 tests in unit_tests/portability/test_ge_120e_4_i.py are red. Both
      surviving failures assert the same thing: the contract-shrinking objection text
      must name the author's own weakened test file (unit_tests/test_other.py), not just
      the production file it accompanies. Today's check_contract_shrinking.py output
      names 'other_feature.py' but never 'test_other.py'."
    remediation: "Re-dispatch python-coder against the 2026-09-07 diagnosis comment above:
      fix the objection-text formatting in check_contract_shrinking.py (or wherever it
      builds the 'Production files modified' report) to also name the weakened test
      file(s), not the shared _authored_change.py/_operation_record.py derivation — both
      of those are confirmed correct and out of scope for this fix."
  failure_report_structured: true
Ran `AC_ENFORCE_STRICT=1 python3 -m pytest unit_tests/portability/test_ge_120e_4_i.py -v`
(plain pytest would mask non-done-AC failures as xfail per established convention in this
epic). Result: 2 passed, 2 failed, confirming the 2026-09-07 diagnosis note verbatim — the
two dependency-derivation tests now pass (GE-120e-1's `_authored_change.py` and GE-120e-4's
`_operation_record.py` have both landed since the last python-coder pass), but
`test_ge120e4i_revised_merge_change_set_holds_only_the_authors_edit` and
`test_ge120e4i_verdict_matches_the_ordinary_followup_commit_arm` still fail because the
guard's objection text omits the weakened test file's name. This is a real, narrow
implementation gap, not a test-authoring defect — the tests are correctly written per this
AC's Gherkin ("a check that does raise one is named, together with the content it objected
to"). Marking test-runner `failed` (not `signed_off`) per the signoff skill's anti-pattern
rule against signing off on a red suite; recommending the supervisor re-dispatch
python-coder with this comment and the 2026-09-07 diagnosis as input.

### Failure 1: TestRevisedMergeChangeSetHoldsOnlyTheAuthorsEdit.test_ge120e4i_revised_merge_change_set_holds_only_the_authors_edit

File: unit_tests/portability/test_ge_120e_4_i.py
Failure type: assertion
Stacktrace excerpt:
  self.assertIn(
      "test_other.py", combined,
      "The author's own weakened test file must be named. "
      f"Output:\n{combined}",
  )
  AssertionError: 'test_other.py' not found in "...Production files modified:\n  - prod/other_feature.py\n..."
Rerun command:
  AC_ENFORCE_STRICT=1 python3 -m pytest unit_tests/portability/test_ge_120e_4_i.py::TestRevisedMergeChangeSetHoldsOnlyTheAuthorsEdit::test_ge120e4i_revised_merge_change_set_holds_only_the_authors_edit -v

### Failure 2: TestVerdictMatchesOrdinaryFollowupCommitArm.test_ge120e4i_verdict_matches_the_ordinary_followup_commit_arm

File: unit_tests/portability/test_ge_120e_4_i.py
Failure type: assertion
Stacktrace excerpt:
  for needle in ("other_feature.py", "test_other.py"):
      self.assertIn(needle, combined_a, f"Arm A output missing {needle!r}")
  AssertionError: 'test_other.py' not found in "...Production files modified:\n  - prod/other_feature.py\n..." : Arm A output missing 'test_other.py'
Rerun command:
  AC_ENFORCE_STRICT=1 python3 -m pytest unit_tests/portability/test_ge_120e_4_i.py::TestVerdictMatchesOrdinaryFollowupCommitArm::test_ge120e4i_verdict_matches_the_ordinary_followup_commit_arm -v

## Summary

Suite: unit_tests/portability/test_ge_120e_4_i.py   Total failures: 2 (both assertion, same
root cause: objection text omits the weakened test file's name)
Suite rerun: AC_ENFORCE_STRICT=1 python3 -m pytest unit_tests/portability/test_ge_120e_4_i.py -v

### 2026-09-07 15:57 — ac-validator (status: blocker)
feedback-id: (submit-failed — ac-validator is not a registered writer for `blocker` or
`quality-concern` in config/feedback_categories.yaml; no other category fits)
completion_manifest:
  ac_sections_located: true
  ac_coverage_evidence_gathered: true
  all_acs_covered:
    result: false
    reason: "2 of 3 ACs (AC-1, AC-3) have no implementation evidence in the diff and their
      corresponding tests are RED when actually executed."
    remediation: "Re-dispatch python-coder against the 2026-09-07 diagnosis comment above:
      make check_contract_shrinking.py's objection text also name the weakened test
      file(s), not just the production file."
Store-alignment script (scripts/commit_guardian/check_v2_ac_store_alignment.py) is absent
in this worktree — Step 2c skipped silently per protocol.

Ticket exposes three plain `## Acceptance Criteria` checkboxes (no `## Agent Contracts`
per-agent block, no `## AC Coverage` table exists to fill, so those sub-steps of §2c.3 are
skipped — nothing to fill).

Independently re-ran `AC_ENFORCE_STRICT=1 python3 -m pytest
unit_tests/portability/test_ge_120e_4_i.py -v` myself before writing this verdict (did not
rely solely on test-runner's prior report): confirmed 2 passed, 2 failed, identical to the
2026-09-07 15:46 test-runner comment above. Also confirmed via `git diff --cached
--name-only` that `templates/scripts/commit_guardian/check_contract_shrinking.py` — the file
that would carry the fix (naming the weakened test file in the objection text) — is absent
from the staged diff entirely; only `_authored_change.py`, `_operation_record.py`, and the
new/updated test files are staged.

AC-1 covered: NO.
  test: test_ge120e4i_revised_merge_change_set_holds_only_the_authors_edit
    (unit_tests/portability/test_ge_120e_4_i.py:303-375) — FAILS: asserts `"test_other.py"`
    is in the guard's combined stdout/stderr; actual output only names
    `prod/other_feature.py`.
  implementation: NONE — check_contract_shrinking.py not present in the staged diff.

AC-2 covered: YES.
  implementation: templates/scripts/commit_guardian/_operation_record.py (staged, new file)
    and templates/scripts/commit_guardian/_authored_change.py (staged, modified) — the
    shared authored-change/operation-record derivation GE-120e-1/GE-120e-4 required.
  test: test_ge120e4i_absent_operation_record_does_not_widen_to_the_whole_staged_tree
    (unit_tests/portability/test_ge_120e_4_i.py:377-424) — PASSES (confirmed in the run
    above: 2 passed, 2 failed, and this is one of the two passing tests along with the
    reachability test).
  Checkbox flipped to `- [x]` in `## Acceptance Criteria` above.

AC-3 covered: NO.
  test: test_ge120e4i_verdict_matches_the_ordinary_followup_commit_arm
    (unit_tests/portability/test_ge_120e_4_i.py:425-522) — FAILS: `for needle in
    ("other_feature.py", "test_other.py"): self.assertIn(needle, combined_a, ...)` — Arm A
    output is missing `test_other.py`.
  implementation: NONE — same missing objection-text fix as AC-1; both failures share one
    root cause per test-runner's Summary block above.

AC coverage incomplete: AC-1, AC-3 have implementation evidence absent from the diff and
their tests are genuinely RED under my own independent execution (not merely inherited from
the prior test-runner comment). AC-2 is covered by both channels and its checkbox is now
flipped. `ac_coverage: 1/3` set in frontmatter. Commit phase must NOT proceed until
python-coder lands the objection-text fix described in the 2026-09-07 diagnosis comment and
test-runner reconfirms green. Marking myself `failed` per the signoff skill's anti-pattern
rule against signing off on unresolved AC gaps; suggested remediation: respawn
`python-coder` with this comment and the 2026-09-07 diagnosis comment as input, then
re-dispatch `test-runner` and `ac-validator` in sequence.

### 2026-09-07 16:10 — ac-fulfillment-gate (status: blocker)
feedback-id: (submit-failed — ac-fulfillment-gate is not a registered writer for category
`blocker` in config/feedback_categories.yaml; no other category fits)
completion_manifest:
  ac_store_yaml_loaded: true
  work_status_verified:
    result: false
    reason: "GE-120e-4-i.yaml has work_status: todo, and this ticket's own files_touched
      is [] (empty) so there is no diff evidence available to auto-fix it — 3a's
      eligibility condition (files_touched ∩ diff non-empty) is not met."
    remediation: "Land the objection-text fix in check_contract_shrinking.py per the
      2026-09-07 diagnosis comment and test-runner's finding, populate this ticket's
      files_touched with the real changed file(s), then re-dispatch python-coder,
      test-runner, ac-validator, and this gate in sequence."
  covered_by_auto_fixed: true
Ran `.leafcutter/scripts/ac_store/ac_coverage_resolver.py --ticket` against this ticket:
resolved exactly one AC (`GE-120e-4-i`, via the two-key `{id, path}` traceability block),
so the `len(resolved_acs) >= 1` precondition for an `ok` verdict is met — the failure here
is a genuine content gap, not an empty resolution. The resolver's first run reported two
failing fields: `work_status` and `covered_by`.

`covered_by` was auto-fixed per Step 3c (no level qualifier — this AC is L3, and the
criterion runs regardless): `grep -rn "covers: GE-120e-4-i" unit_tests/ tests/` found four
genuine `# covers: GE-120e-4-i` tags inside
`unit_tests/portability/test_ge_120e_4_i.py` (lines 310, 384, 432, 534), so I appended that
path to the previously-empty `covered_by: []` list (append-only; no existing entries
touched). Ran `check_ac_schema.py` against the file both before and after the edit — exit 0
both times, confirming the edit is schema-valid. Re-running the resolver afterward confirms
`covered_by` cleared and only `work_status` remains failing.

`work_status` remains `todo` and is NOT auto-fixable per Step 3a: this ticket's frontmatter
`files_touched: []` is empty, so `files_touched ∩ diff` is empty regardless of what the
branch diff itself contains — there is no evidence set to draw from. This is also
consistent with the ticket's own record: `ac-validator`'s 2026-09-07 15:57 comment confirms
`check_contract_shrinking.py` (the file that would carry the AC-1/AC-3 fix) is absent from
the staged diff, and `test-runner`'s 15:46 comment confirms 2 of 4 tests in
`unit_tests/portability/test_ge_120e_4_i.py` are still RED under
`AC_ENFORCE_STRICT=1 pytest`. Setting `work_status: done` here would misrepresent the AC
store against both the ticket's own test evidence and the diff — declining to do so per the
"no false positives" constraint.

Per the load-bearing precondition, one resolved AC in a `blocker` state after auto-fix means
this gate's overall verdict is `blocker`, not `ok`. Commit phase must not proceed until the
objection-text fix lands, `files_touched` is populated with the real changed file, and
`work_status` can be genuinely (not mechanically) set to `done`. Suggested remediation:
re-dispatch `python-coder` against the 2026-09-07 diagnosis comment, then `test-runner`,
`ac-validator`, and this gate again in sequence.

### 2026-09-07 16:22 — commit (status: blocker)
feedback-id: fb_2026-09-07_cc2fd42d
completion_manifest:
  pre_commit_hooks_pass:
    result: false
    reason: "Never reached — commit was not attempted. The staged tree does not carry this
      ticket's deliverable."
    remediation: "Land the objection-text fix in check_contract_shrinking.py per the
      2026-09-07 diagnosis comment, then re-run test-runner/ac-validator/ac-fulfillment-gate."
  commit_message_valid:
    result: false
    reason: "No commit message was drafted — there is nothing of this ticket's own work to
      describe as done."
    remediation: "Same as above."
  ticket_staged: true
Ran the mandatory `pkill -f pytest` preamble and the pre-commit hook probe step (script
absent in this worktree — probe skipped per the documented incomplete-guardian-install
fallback). Inspected `git status --short` / `git diff --cached --stat` in this shared
worktree: the index does contain real, substantial work (`_authored_change.py`,
`_operation_record.py`, tickets 31/35/36, several new test files), but per this ticket's own
upstream record, four consecutive phase agents — `python-coder` (blocker), `test-runner`
(blocker, 2 of 4 tests red), `ac-validator` (blocker, AC-1 and AC-3 uncovered), and
`ac-fulfillment-gate` (blocker, `work_status` still `todo`) — all independently confirm that
`GE-120e-4-i`'s actual deliverable (naming the weakened test file in
`check_contract_shrinking.py`'s objection text) is neither implemented nor present in the
staged diff (`check_contract_shrinking.py` does not appear in `git diff --cached
--name-only`). Committing the currently-staged tree under this ticket's name would represent
this AC as delivered when it is not — the exact phantom-done failure mode this repo's own
commit-message-matches-diff rule and the guardrail-engine epic itself exist to prevent. No
`git commit` was run. Not proceeding past the confirmation gate: there is no user-authorized
subject to confirm here since the work is not done. Marking `commit` `failed` (not
`signed_off`) per the signoff skill's anti-pattern rule and §4 Failed Path. Suggested
remediation: re-dispatch `python-coder` against the 2026-09-07 diagnosis comment (fix
`check_contract_shrinking.py`'s objection text to name the weakened test file), then
re-dispatch `test-runner`, `ac-validator`, `ac-fulfillment-gate`, and finally `commit` again
in sequence. This ticket's own frontmatter/Sign-offs edits (marking `commit: failed`) were
made directly to the ticket file and are left unstaged for the supervisor/next commit pass
to pick up alongside the eventual real fix, per the shared-worktree convention that
in-progress tickets in this drive are staged together at the end, not individually.

### 2026-09-07 16:31 — python-coder (status: ok)
feedback-id: fb_2026-09-07_d9bd8c1d
completion_manifest:
  red_baseline_results:
  - test_name: test_ge120e4i_revised_merge_change_set_holds_only_the_authors_edit
    result: green
  - test_name: test_ge120e4i_absent_operation_record_does_not_widen_to_the_whole_staged_tree
    result: green
  - test_name: test_ge120e4i_verdict_matches_the_ordinary_followup_commit_arm
    result: green
  - test_name: test_ge_120e_4_i_reachable_from_entry_point
    result: green
  objection_text_names_weakened_file: true
  deployed_copy_parity_verified: true
Re-dispatched per the 2026-09-07 diagnosis comment. Confirmed both upstream
dependencies (`_authored_change.py` GE-120e-1, `_operation_record.py`
GE-120e-4) are present and correct before touching anything, per the
diagnosis's explicit "do not touch" instruction — neither was modified.
The remaining gap was exactly as diagnosed: `check_contract_shrinking.py`'s
BLOCKED objection named `scan.production_files` but never the file the
test-weakening pattern actually matched in. Added `_split_by_file()` (splits
the unified diff into per-file `diff --git a/... b/...` sections) and
`_find_weakened_files()` (re-applies the existing weakening-pattern /
deleted-test / deleted-test-file detectors per file chunk, scoped to the
same `weakening_diff` `_scan_diff` already judges against — so a merge's
carried-in content is excluded from file attribution exactly as it already
is from the violation list). `ScanResult` gained a `weakened_files` field,
populated only when `scan.violations` is non-empty. `main()`'s BLOCKED
branch now prints a "Test files weakened:" section, listing those paths,
ahead of the existing "Production files modified:" section — no existing
line was removed or reworded, so no test or human workflow depending on the
prior text loses anything. Neither `_authored_change.py` nor
`_operation_record.py` was touched, per the ticket's scope boundary.

Verification, in order: (1) `AC_ENFORCE_STRICT=1 pytest
unit_tests/portability/test_ge_120e_4_i.py -v` — 4 passed (was 2 passed, 2
failed); (2) `AC_ENFORCE_STRICT=1 pytest
unit_tests/portability/test_ge_120e_4.py` — 6 passed, confirming GE-120e-4's
own suite is untouched by this change; (3) the full contract-shrinking
family (`test_check_contract_shrinking.py`, `test_contract_shrinking.py`,
`test_ge_119_contract_shrinking_rename_aware.py`,
`test_ac_limits_merge_scope.py`, `test_contract_shrinking_ignores_mentions.py`,
`test_ge_122e_1.py`, `test_bp_1100b_5.py`) — 62 passed, 2 subtests passed,
confirming no regression to the existing guard behavior or its objection-text
consumers; (4) `ruff check` on both the template and deployed copies of
`check_contract_shrinking.py` — all checks passed; (5) ADR-001 parity: ran
`python scripts/build.py --target-dir <worktree-root> --force` from the
worktree root and diffed the deployed
`scripts/commit_guardian/check_contract_shrinking.py` against the template —
identical, byte-for-byte; (6) real-artifact behavioral spot-check: built a
fresh scratch git repo under `/tmp` (real `git init`, a real commit, then a
concurrent production edit + `pytest.skip` addition staged for a *second*
commit — not a hand-authored diff fixture), ran the real deployed check
script as a subprocess with `env --chdir` pointing at that repo, and
confirmed the BLOCKED output names both `unit_tests/test_thing.py` (the
file actually weakened) and `prod/thing.py` (the production file), matching
the diagnosis's target shape exactly. `git diff --stat` on the touched file:
`templates/scripts/commit_guardian/check_contract_shrinking.py | 69 +++++++`
(1 file changed, 69 insertions). Not flipping this ticket's own
`## Acceptance Criteria` AC-1/AC-3 checkboxes myself — `ac-validator`'s prior
comment on this ticket already established that flipping those on
verification is its own job (it flipped AC-2 the same way), not
python-coder's; recommending the supervisor re-dispatch `test-runner`, then
`ac-validator`, `ac-fulfillment-gate`, and `commit` in sequence so those
agents can independently re-verify against the now-green suite.

### Status — 2026-09-07 (session note, not a phase-agent sign-off)

**Suite is green: `test_ge_120e_4_i.py` runs 4/4**, up from 4 failed at the start of this
batch. The ticket nonetheless stays `todo`, because `test-runner`, `ac-validator`,
`ac-fulfillment-gate` and `commit` all still read `failed` from the red pass and none has
re-run. Green tests are not the same claim as passed gates, and only the second closes a
ticket.

**A workflow lesson worth recording, because it cost 807k tokens for nothing.** Re-driving
this ticket through `build-ticket.js` confirmed **zero** phases and wrote **no code**: 13
agents ran, `test-runner` went first, found the same two failures, blockered, and cascaded
skips through the rest. `python-coder` was never dispatched — it was `failed`, and **the
planner re-runs phases marked `needed`, not `failed`.** A `failed` phase is a dead end for
the workflow.

Dispatching `python-coder` directly instead fixed it for **177k tokens**. When a ticket's
blocking phase is already `failed`, either flip it to `needed` first or dispatch that agent
directly — do not re-run the whole workflow and expect it to pick the phase up. Ticket 28
hit the same wall earlier in this epic with `pr-reviewer`.
