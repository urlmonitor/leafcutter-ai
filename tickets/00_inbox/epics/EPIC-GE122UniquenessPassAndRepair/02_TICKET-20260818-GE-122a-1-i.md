---
ac_traceability:
  id: GE-122a-1-i
  path: docs/acceptance-criteria/guardrail-engine/GE-122-numbers-mean-one-thing/GE-122a-1-i.yaml
advances_current_outcome: true
agents:
  ac-fulfillment-gate: needed
  ac-validator: needed
  architect-review: needed
  commit: needed
  documentation-expert: needed
  documentation-verifier: needed
  pr-reviewer: signed_off
  pull-request: not_needed
  python-coder: signed_off
  test-runner: signed_off
  test-writer: signed_off
change_target: code
complexity: low
components:
- commit_guardian
created: '2026-08-18'
depends_on:
- 01_TICKET-20260818-GE-122a-1.md
documentation_required: true
files_touched:
- templates/scripts/commit_guardian/check_identifier_uniqueness.py
- unit_tests/commit_guardian/test_ge_122a_1_i.py
priority: high
requires_adr: false
requires_diagram: false
risk_surface: contract_boundary
roadmap_phase: phase_1
source_ac: GE-122a-1-i
status: in_progress
title: A collision is found even when only one claimant is in the current change set
---

# A collision is found even when only one claimant is in the current change set

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `GE-122a-1-i` — A collision is found even when only one claimant is in the current change set — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `GE-122a-1-i`. Component: `guardrail-engine`. Assigned agent: `python-coder`. Estimated complexity: `S`. Complexity: `low`.

## Acceptance Criteria

```gherkin
Given the collection contains two acceptance-criterion records that both declare the
  identifier "GE-119",
And the current change set contains ONLY ONE of the two — the other has sat unmodified on
  the shared branch for weeks and appears in no diff,
When the uniqueness pass runs,
Then it reports the contested identifier and names BOTH claimant paths, including the one
  absent from the change set.

And Given a change set that touches NEITHER claimant while the collision is still present in
  the collection,
When the pass runs,
Then it still reports the contested identifier and both claimant paths.

And Given a change set that adds a record claiming an identifier no other record claims,
When the pass runs,
Then it reports nothing for that record.

The property this criterion pins: the change set may be used to decide which findings are
ATTRIBUTED to the current author, but it must never bound WHAT IS INSPECTED. A check whose
input set is the changed files cannot observe a collision whose second claimant is not in the
diff — and in the collision actually observed in this repository, the two claimants were
authored weeks apart from different working copies, so they were never in one diff together.

Coverage note: covered only by a test that stages exactly one claimant, executes the pass, and
asserts the absent claimant's path appears in the output. A test that stages both claimants
cannot distinguish a whole-collection pass from a changed-files pass and therefore does not
cover this criterion.
```

- [ ] AC-1: the current change set contains ONLY ONE of the two — the other has sat unmodified on
- [ ] AC-2: it reports the contested identifier and names BOTH claimant paths, including the one
- [ ] AC-3: Given a change set that touches NEITHER claimant while the collision is still present in
- [ ] AC-4: it still reports the contested identifier and both claimant paths.
- [ ] AC-5: Given a change set that adds a record claiming an identifier no other record claims
- [ ] AC-6: it reports nothing for that record.

## Test Requirements

```yaml
tests:
- name: test_collision_reported_when_only_one_claimant_is_staged
  file: unit_tests/commit_guardian/test_ge_122a_1_i.py
  covers:
  - GE-122a-1-i
  asserts: Stage exactly ONE of the two claimants — the other is committed and untouched
    — execute the pass, and assert the unstaged claimant's path appears in the output.
    Staging both claimants is explicitly not an acceptable substitute.
  framework: unittest
  type: behavioral
- name: test_collision_reported_when_neither_claimant_is_staged
  file: unit_tests/commit_guardian/test_ge_122a_1_i.py
  covers:
  - GE-122a-1-i
  asserts: With a change set touching neither claimant while the collision is still
    present, the pass still reports the contested identifier and both paths.
  framework: unittest
  type: behavioral
- name: test_unattributed_collision_reports_but_does_not_block_commit
  file: unit_tests/commit_guardian/test_ge_122a_1_i.py
  covers:
  - GE-122a-1-i
  asserts: The neither-claimant-staged case yields a non-blocking commit-time disposition
    and a printed count of unattributed contested numbers, while the one-claimant-staged
    case blocks.
  framework: unittest
  type: behavioral
- name: test_new_uncontested_record_produces_no_finding
  file: unit_tests/commit_guardian/test_ge_122a_1_i.py
  covers:
  - GE-122a-1-i
  asserts: A change set adding a record whose identifier no other record claims produces
    no finding for that record.
  framework: unittest
  type: behavioral
```

## Implementation Notes

```yaml
- This is the single most important test in the tree and the one a plausible implementation
  fails by accident. The inspection input MUST be the on-disk collection, resolved
  independently of `git diff --cached` / `--name-only`; the change set may only be
  intersected with the findings AFTERWARDS. Any implementation that derives its file
  list from the diff satisfies GE-122a-1 and fails this criterion, and the reviewer
  should read the input-set construction specifically.
- 'DECIDES THE QUESTION THE BA LEFT OPEN (notes: ''whether a pre-existing collision
  in which NEITHER claimant is in the change set should BLOCK or only be REPORTED'').
  DECISION: attribution is diff-scoped, inspection is not. A contested number with
  at least one claimant in the change set BLOCKS. A contested number with no claimant
  in the change set is REPORTED with a visible count and does NOT block. This follows
  the resolution already documented in ci.yml''s ''AC store valid'' job for the 57
  pre-existing orphans, and is what lets the gate be switched on before GE-122e''s
  repair has landed rather than after.'
- The count of reported-but-unattributed collisions MUST be printed on every run,
  exactly as GE-122b-2 requires for its pre-existing unnumbered population. A silently-tolerated
  backlog is how the current drift accumulated; a visible count is what makes the
  backlog shrink.
- 'Once GE-122e has repaired the collection, no configuration change should be needed
  to make the unattributed case block — the set is expected to be empty, so the two
  dispositions converge. Do NOT implement the tolerance as an allowlist or a known-bad
  list of identifiers: GE-122e-3 asserts that no such configuration exists anywhere.'
- 'Must not add a second collection walk: reuse GE-122a-1''s single pass and filter
  its verdict. A separate diff-scoped walk would be a second definition of the rule
  and would fail GE-122d-1.'
```

## Agent Contracts

### documentation-expert

Existing docs to update / cross-link:

- templates/scripts/commit_guardian/check_ac_schema.py (relationship: context | status: exists | relevance: Reads its file set from `git diff --cached --name-only` (_get_staged_ac_paths). This is the changed-files scoping that makes the incumbent validator blind to the condition in this criterion.)
- templates/scripts/commit_guardian/check_ac_circular_deps.py (relationship: context | status: exists | relevance: Existing precedent for the required shape: it builds an adjacency list from the WHOLE on-disk store and then overlays the staged changes. The whole-collection load mechanism already exists in the hook family and should be reused rather than reinvented.)
- docs/architecture/components/commit-guardian.md (relationship: describes | status: exists)
- .github/workflows/ci.yml (relationship: describes | status: exists | relevance: The 'AC store valid' job documents the identical attribution-versus-inspection trade-off and its chosen resolution — diff-scope the ATTRIBUTION, leave the rule whole — for the 57 pre-existing orphans. It is the precedent this criterion's disposition decision follows.)

- [ ] AC-1: [(unspecified genre)] templates/scripts/commit_guardian/check_ac_schema.py — it reports the contested identifier and names BOTH claimant paths, including the one

### Expects From

- **AC:** GE-122a-1
- **Contract:** The collection-wide uniqueness pass whose inspection scope this criterion constrains.

## Sign-offs

- [ ] architect-review
- [x] test-writer — 2026-08-18 15:07
- [x] python-coder — 2026-08-18 15:07
- [x] test-runner — 2026-08-18 15:07
- [ ] documentation-expert
- [x] pr-reviewer — 2026-08-19 16:06
- [ ] ac-validator
- [ ] ac-fulfillment-gate
- [ ] documentation-verifier
- [ ] commit

## Comments

### 2026-08-18 — bookkeeping correction (status: ok)

Implementation for this ticket was driven manually from the main loop, not by
`ticket-supervisor`: `test-writer` and `python-coder` were dispatched directly
(flattened chain), and `test-runner` plus an operator-driven `git commit` were
executed from the main loop. `test-writer`, `python-coder` and `test-runner`
are marked `signed_off` because they genuinely ran; no sign-off comment was
written at the time, which is the omission this entry corrects.

Evidence: commit `78536224` on `feat/ge-122-integrity-guard`. Verified with
`AC_ENFORCE_STRICT=1` (plain pytest reports `xfailed` and exits 0 for a
not-done AC, so it cannot distinguish red from green here).

STILL UNRUN, deliberately left `needed` so `/build-feature` dispatches them:
`architect-review`, `pr-reviewer`, `ac-validator`, `ac-fulfillment-gate`,
`documentation-expert`, `documentation-verifier`, `pull-request`.

`commit` is deliberately left `needed` even though the commit above exists. The
implementation is safely in git, but the pending gates will produce new
artifacts — documentation, any fix they demand — and those need a phase to
persist them. The cost of the manual drive is that the gates now run AFTER the
implementation commit instead of gating it, so their findings land in a
follow-up commit. Recorded rather than hidden.

### 2026-08-19 16:06 — pr-reviewer (status: ok)

feedback-id: fb_2026-08-19_11e4f28f
completion_manifest:
  diff_reviewed: true
  suite_run_with_strict_flag: true
  attribution_logic_verified: true
  value_typed_contract_verified: true
Reviewed `git diff origin/main...HEAD` for this ticket's files
(`templates/scripts/commit_guardian/_commit_disposition.py`,
`check_identifier_uniqueness.py`, and the sibling modules
`_uniqueness_types.py` / `_uniqueness_scanners.py` / `_work_items_scanner.py`
that `compute_commit_disposition` and `run_uniqueness_pass` depend on) plus
`unit_tests/commit_guardian/test_ge_122a_1_i.py`, with the specific defect
class from the sibling ticket (01, "collision detector silently misses
collisions") as the standing hypothesis to disprove, not a green suite as
evidence.

**Attribution logic.** `compute_commit_disposition` resolves both sides of the
membership test with `Path(...).resolve()` — the caller-supplied `staged_paths`
in `_resolve_staged`, and each claimant path from `finding.paths` inline in the
`any(...)` check — so relative-vs-absolute and non-canonical (`./`, `..`)
forms compare correctly regardless of which form either side uses, and
symlink components resolve to the same canonical target on both sides. I
traced the concrete flow the hostile scenario in the AC's own coverage note
requires (one claimant staged, the other only committed weeks earlier and
absent from the diff): `run_uniqueness_pass` performs the whole-collection
walk unconditionally (it takes no `staged_paths` argument at all — the
property this AC exists to pin), and `main()` calls it exactly once before
consulting `_get_staged_paths()`, so inspection is provably decoupled from
the change set; only the post-hoc `compute_commit_disposition` filter is
diff-scoped. I did not find a case, including an EMPTY staged set (every
`attributed` is `False`, `blocking` is `False`, `unattributed_count` equals
the full finding count — no crash) or a staged set naming files outside the
collection (no match, no crash, no false attribution), where this silently
produces a wrong answer.

**Value-typed contract.** Confirmed real, not string-shaped: `CommitFinding`
and `CommitDisposition` are frozen dataclasses with a `bool` `.attributed` /
`.blocking` and an `int` `.unattributed_count`; `main()`'s exit-code decision
reads `disposition.blocking` directly with no substring match against printed
output anywhere in the diff. `check_identifier_uniqueness.py`'s printed lines
(`_print_finding`, `_print_unattributed_summary`) are cosmetic and are not
consumed by any control-flow branch.

**Test genuineness.** All four new tests build a real on-disk git repository
via actual `git init`/`add`/`commit` subprocess calls (never a mock of git or
of the scan) and assert on the returned dataclass's typed fields as the
primary evidence, with the subprocess exit code as a secondary, documented
check — matching the AC's own "no string matching" requirement. Test 4
(`test_new_uncontested_record_produces_no_finding`) is deliberately paired
with an unrelated unattributed collision in the same run specifically so it
cannot pass on an implementation that has not learned real attribution — I
verified this pairing is genuine and not incidental.

**Suite run.** `AC_ENFORCE_STRICT=1 python3 -m pytest unit_tests/commit_guardian/ -q`
on a clean tree: **1181 passed, 4 skipped, 1 xfailed** — matches the ticket's
stated baseline exactly. Note for the record: a first run in this session
additionally reported `1 error`, a `tearDownModule` assertion in the sibling
`test_ge_122e_3.py` (unrelated to this ticket's `files_touched`) that detected
`tickets/.../01_TICKET-20260818-GE-122a-1.md` (sibling ticket 01, not this
one) had been modified mid-run. A second full run immediately after was clean
with the exact baseline counts and exit 0, and an isolated run of
`test_ge_122e_3.py` alone produced no further mutation — consistent with a
concurrent process in this shared worktree writing to ticket 01 (a
`documentation-verifier` dispatch, per that file's own diff content) between
this session's two test-collection passes, not a defect in this ticket's code
or tests. Flagging for visibility per this repo's "no parallel supervisors in
a shared worktree" hazard; not an AC gate finding.

**Verdict: no high-confidence findings.** One inherited, pre-existing note
for the record only (not attributed to this ticket): `_get_staged_paths`
follows the exact same `git diff --cached --name-only` convention as
`check_ac_schema.py`'s `_get_staged_ac_paths` (explicitly named as this AC's
precedent in its own doc_links) without `-z`/`--no-quote-path`; git's default
`core.quotePath` would return an octal-escaped, quote-wrapped string for a
staged path containing non-ASCII characters, which would not resolve to a
matching `Path` and would silently leave that finding unattributed. This
repo's numbered-artifact filenames (`GE-*`, `ADR-*`, `c*-*`, `TICKET-*.md`)
are ASCII by convention, so this does not fire in practice today, and it is
not a defect introduced by this ticket — it is inherited verbatim from the
precedent this ticket was directed to follow. Recording it here rather than
silently passing over it.

`pr-reviewer` was present in this ticket's `agents:` map (`needed`) and its
`## Sign-offs` checklist, so it is flipped to `signed_off` here rather than
left alone. No `## AC Coverage` table and no `### pr-reviewer` heading exist
under this ticket's `## Agent Contracts` section, so signoff skill §2c.2/§2c.3
are skipped per their own no-heading-found rule; this is a v1-shaped sign-off.
