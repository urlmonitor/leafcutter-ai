---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  llm-expert: needed
  pr-reviewer: needed
  pull-request: not_needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
complexity: low
components:
- build_orchestration
created: '2026-07-10'
depends_on:
- BO-2100a
files_touched:
- templates/agents/ticket-supervisor.md
- docs/architecture/adrs/ADR-020-live-surface-tester.md
priority: medium
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: BO-2100a-2
status: todo
title: ticket-supervisor Spawn Allowlist permits live-surface-tester
---

# ticket-supervisor Spawn Allowlist permits live-surface-tester

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BO-2100a-2` — ticket-supervisor Spawn Allowlist permits live-surface-tester — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BO-2100a-2`. Component: `build-orchestration`. Assigned agent: `llm-expert`. Estimated complexity: `S`. Complexity: `low`.

## Acceptance Criteria

```gherkin
Given the ticket-supervisor agent template (templates/agents/ticket-supervisor.md)
  with its "Spawn Allowlist (is_ticket_phase agents)" section
When the supervisor reaches a phase whose agent is `live-surface-tester`
Then `live-surface-tester` appears as a listed entry in that Spawn Allowlist
  alongside `user-surface-smoker`
  And the supervisor is permitted to dispatch it rather than rejecting it as an
  unlisted agent.
```

## Implementation Notes

```yaml
- 'The allowlist change must be additive: no existing allowlisted agent may be removed
  or reordered.'
- 'The listed agent key must exactly match the key emitted by the ticket-authoring
  skill (BO-2100a-1) and the build-ticket.js phaseOrder (BO-2100a-3): `live-surface-tester`.'
- Must be consistent with the registry entry marking live-surface-tester as an is_ticket_phase
  agent.
```

## Sign-offs

- [ ] llm-expert
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit

## Comments
