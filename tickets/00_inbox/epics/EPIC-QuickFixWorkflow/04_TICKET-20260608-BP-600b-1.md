---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  pr-reviewer: needed
  pull-request: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
  llm-expert: needed
components:
- build-pipeline
created: '2026-06-08'
depends_on:
- BP-600b
- BP-600a-1
files_touched:
- docs/reference/ac-schema.md
- docs/acceptance-criteria/index.yaml
- docs/architecture/adrs/ADR-006-flatten-supervisor-chain.md
priority: medium
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: BP-600b-1
status: todo
title: Quick-fix workflow creates an AC YAML file in the AC store
---

# Quick-fix workflow creates an AC YAML file in the AC store

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BP-600b-1` — Quick-fix workflow creates an AC YAML file in the AC store — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BP-600b-1`. Component: `build-pipeline`. Assigned agent: `workflow-architect`. Estimated complexity: `M`.

## Acceptance Criteria

```gherkin
Given the user invokes /quick-fix with diagnosis "In scripts/build_helpers.py
  line 42, _resolve_precommit_cmd() returns a non-executable path because
  the executability probe is missing",
When the workflow completes the AC creation phase,
Then a new YAML file exists under docs/acceptance-criteria/ in the component
  directory matching the diagnosed file,
And the file contains the required fields: id, title, component,
  status (set to "active"), and criteria with Given/When/Then structure,
And the criteria field describes the specific bug behavior that the fix
  will prevent from recurring.
```

## Sign-offs

- [ ] llm-expert
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit
- [ ] pull-request

## Comments
