---
ac_coverage: 4/4
ac_traceability:
  id: ACD-2100b-3-i
  path: docs/acceptance-criteria/ac-driven-dev/ACD-2100-entry-point-unblocked/ACD-2100b-3-i.yaml
advances_current_outcome: true
agents:
  ac-fulfillment-gate: signed_off
  ac-validator: signed_off
  commit: signed_off
  documentation-expert: not_needed
  documentation-verifier: not_needed
  pr-reviewer: signed_off
  pull-request: not_needed
  python-coder: signed_off
  test-runner: signed_off
  test-writer: signed_off
change_target: pipeline
complexity: low
components:
- ac_driven_dev
- agent_registry
created: '2026-08-26'
depends_on:
- 09_TICKET-20260826-ACD-2100b-3.md
documentation_required: false
files_touched:
- templates/workflows-js/plan-feature.js
priority: medium
requires_adr: false
requires_diagram: false
risk_surface: internal
roadmap_phase: phase_1
source_ac: ACD-2100b-3-i
status: todo
title: A registry that holds no agent entries is reported as unusable rather than
  as a missing agent
---

# A registry that holds no agent entries is reported as unusable rather than as a missing agent

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `ACD-2100b-3-i` — A registry that holds no agent entries is reported as unusable rather than as a missing agent — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `ACD-2100b-3-i`. Component: `ac-driven-dev`. Assigned agent: `python-coder`. Estimated complexity: `S`. Complexity: `low`.

## Acceptance Criteria

```gherkin
Given a registry file that can be read and interpreted but does not carry the
  collection of agent entries the startup check expects -- for example it holds
  a single value where a list of agents belongs,
When the startup check runs,
Then the run stops with a report that the registry could not be used and states
  what was found where the agent entries were expected,
And the report does not state that any agent is absent from the registry,
And the report does not state that any agent lacks permission,
And the check itself does not end the run with an unhandled failure.
```

- [x] AC-1: the run stops with a report that the registry could not be used and states
- [x] AC-2: the report does not state that any agent is absent from the registry
- [x] AC-3: the report does not state that any agent lacks permission
- [x] AC-4: the check itself does not end the run with an unhandled failure.

## Test Requirements

```yaml
tests:
- name: test_registry_without_an_entries_collection_reports_could_not_be_used
  file: unit_tests/workflows/test_acd_2100b_3_i.py
  covers:
  - ACD-2100b-3-i
  asserts: A registry that parses cleanly but holds a single value where the agent
    entries belong; the run stops with a report that the registry could not be used
    and that names what was found in that position.
  framework: unittest
  type: integration
  angle: criterion
- name: test_no_entries_collection_report_asserts_neither_absence_nor_denial
  file: unit_tests/workflows/test_acd_2100b_3_i.py
  covers:
  - ACD-2100b-3-i
  asserts: That same report contains no statement that any agent is absent from the
    registry and no statement that any agent lacks permission -- the two false verdicts
    this outcome currently produces.
  framework: unittest
  type: integration
  angle: criterion
- name: test_no_entries_collection_does_not_raise_an_unhandled_failure
  file: unit_tests/workflows/test_acd_2100b_3_i.py
  covers:
  - ACD-2100b-3-i
  asserts: The run's recorded outcome is the report rather than an unhandled failure,
    and no unhandled-failure trace appears in the captured output.
  framework: unittest
  type: integration
  angle: failure
- name: test_outcome_is_produced_through_the_workflow_entry_point
  file: unit_tests/workflows/test_acd_2100b_3_i.py
  covers:
  - ACD-2100b-3-i
  asserts: The report is produced by driving the real workflow, and the run stops
    at the check rather than proceeding to the next step.
  framework: unittest
  type: integration
  angle: reachability
```

## Implementation Notes

```yaml
- The change must land under templates/. The deployed workflow is build output.
- 'Fail closed: the run stops.'
- A cause the check did not establish must not be asserted. When the outcome fits
  none of the four named cases the report must say the verdict could not be determined
  and say what was seen -- this is the general rule, and this record is one instance
  of it.
- The report must state what was found where the agent entries were expected, so the
  operator can tell a wrong file from a structurally changed one.
- The outcome must be a recorded report, not an unhandled failure. An unhandled failure
  also stops the run, so a fail-closed assertion alone cannot distinguish the two
  -- the report is the only observable difference.
- 'Must satisfy the repository error-handling policy: the guard must catch by type
  and log at WARNING or higher; it must not be a bare except and must not silently
  substitute an empty collection, which is precisely how this outcome becomes a false
  absence verdict.'
```

## Agent Contracts

### documentation-expert

Existing docs to update / cross-link:

- docs/reference/plan-feature-layout-and-startup-checks.md (relationship: describes | status: planned | relevance: The reference page authored by ACD-2100d-4 must include the could-not-determine outcome, not only the four named ones.)
- docs/known-issues/ac-driven-dev.md (relationship: describes | status: exists | relevance: KI-ACD-009's pattern note -- a check must not report a specific failure cause it did not establish.)
- docs/reference/false-green-mechanisms.md (relationship: describes | status: exists | relevance: M8 inverted. The same 'green means checked' distinction GE-120 draws.)
- templates/workflows-js/plan-feature.js (relationship: implements | status: exists | relevance: Canonical source. The entries.find lookup at 1745-1791 is the site where a non-collection value both raises and, once guarded, degrades into the absence branch.)
- docs/architecture/components/agent-registry.md (relationship: describes | status: exists | relevance: Component doc for the registry structure this outcome fails to find.)

This record owns none of the documents above -- every entry is a cross-link to a
document another record produces. The contract line that stood here demanded
`docs/reference/plan-feature-layout-and-startup-checks.md`, which ACD-2100d-4
creates; this ticket could never have satisfied it. Removed per BO-2200c-5-i
and -iii; the list above is retained as reading for the implementer.

### Expects From

- **AC:** ACD-2100b-3
- **Contract:** A lookup over the registry's agent entries whose result distinguishes entry-present-and-permitted, entry-present-and-denied, and entry-absent -- leaving 'the entries collection is not there' representable as a fourth state rather than folded into absence.

## Sign-offs

- [x] test-writer — 2026-09-07 09:02
- [x] python-coder — 2026-09-07 09:07
- [x] test-runner — 2026-09-07 09:12
- [x] pr-reviewer — 2026-09-07 09:20
- [x] ac-validator — 2026-09-07 09:30
- [x] ac-fulfillment-gate — 2026-09-07 09:35
- [x] commit — 2026-09-07 09:22

## Comments

### 2026-09-07 09:02 — test-writer (status: ok)
feedback-id: fb_2026-09-07_1987870b
completion_manifest:
  tests_written: true
  red_baseline_confirmed: true
  cross_layer_seam_answer:
    result: covered
    producing_side: "the real templates/workflows-js/plan-feature.js Pre-Stage-0 registry-permission gate's `_resolveWorkspaceSetupAgentEntryState()` + halt branching, driven end-to-end via a real Node child_process against a real on-disk agent_registry.json whose `agents` field holds a single malformed value (not a list)"
    consuming_side: "the run's own returned {status, message} result plus its dispatched status-checker report text, which the test asserts an operator would read to learn the registry could not be used"
Wrote unit_tests/workflows/test_acd_2100b_3_i.py (4 tests, unittest, framework/target exactly per the ticket's Test Requirements table) covering AC-1 through AC-4: (1) a registry whose `agents` field is a single distinctive number (42891, produced by the real `json.dumps` serializer) rather than a list must yield a report stating the registry "could not be used" and naming what was found there (the value or its type); (2) that same report must contain neither an absence phrase ("not found in the registry" etc., reused verbatim from ACD-2100b-3's own vocabulary) nor a denial phrase/permission-setting marker (`permits_shell`); (3) the outcome must be a recorded, returned halt with no top-level `.catch()` firing and no unhandled-failure stack-trace signature in stderr; (4) the report must be produced by driving the REAL plan-feature.js entry point via a real Node subprocess (self-contained harness copied from test_acd_2100b_3.py's convention, never a mocked agent()), with the registry-read dispatch observed to run and no downstream worktree-setup step dispatched. Ran `AC_ENFORCE_STRICT=1 python -m pytest unit_tests/workflows/test_acd_2100b_3_i.py -v` (AC_ENFORCE_STRICT=1 because the store's own pytest_ac_enforcement plugin otherwise downgrades not-done-AC failures to xfail): 2 FAILED, 2 PASSED. The 2 failures are the exact defect this AC exists to fix -- today `_resolveWorkspaceSetupAgentEntryState()` already returns a distinct "no_entries_collection" state (added by ACD-2100b-3), but the caller's halt branching only special-cases `.state === "denied"`; every other state (including "no_entries_collection") falls through to the SAME `notFoundMessage`, which literally says "was not found in the registry" and contains none of the "could not be used" / "what was found" wording this AC requires. The 2 PASSING tests (angle: failure, angle: reachability) assert invariants that already hold today and must be PRESERVED by the fix (no unhandled crash; the check is genuinely reached and its halt is consumed) -- not under-specified, per the Test Requirements table's own explicit angle assignment; excluded from red_baseline per the "do not include passing entries" schema constraint. red_baseline entries below.
red_baseline:
  - test_name: test_registry_without_an_entries_collection_reports_could_not_be_used
    file: unit_tests/workflows/test_acd_2100b_3_i.py
    error: "AssertionError: False is not true : The no-entries-collection report does not state that the registry could not be used. report='{\"status\": \"error\", \"message\": \"Workspace-setup step 'worktree-setup' is configured to dispatch to agent 'zqm7_probe_worktree_agent_44xk', which was not found in the registry. Halting before any authoring agent is dispatched. Add an entry for that agent to config/agent_registry.json, or fix the workspace_setup_agent configuration to point at an agent that is listed.\"}...'"
  - test_name: test_no_entries_collection_report_asserts_neither_absence_nor_denial
    file: unit_tests/workflows/test_acd_2100b_3_i.py
    error: "AssertionError: 'not found in the registry' unexpectedly found in '{\"status\": \"error\", \"message\": \"workspace-setup step 'worktree-setup' is configured to dispatch to agent 'zqm7_probe_worktree_agent_44xk', which was not found in the registry. halting before any authoring agent is dispatched. ...\"}...' : The no-entries-collection report wrongly asserts the agent is absent from the registry ('not found in the registry' found)."

### 2026-09-07 09:07 — python-coder (status: ok)
feedback-id: fb_2026-09-07_ee762e7b
completion_manifest:
  third_halt_branch_added: true
  no_entries_collection_names_what_was_found: true
  absence_and_denial_phrases_excluded: true
  fail_closed_preserved: true
  red_baseline_green: true
  regression_suite_confirmed: true
red_baseline_results:
  - test_name: test_registry_without_an_entries_collection_reports_could_not_be_used
    result: green
  - test_name: test_no_entries_collection_report_asserts_neither_absence_nor_denial
    result: green
Added a third halt branch to templates/workflows-js/plan-feature.js's Pre-Stage-0
`if (!permitsShell)` gate, alongside the existing `agentIsListedAndDenied` branch
and the `notFoundMessage` fallthrough. `_resolveWorkspaceSetupAgentEntryState()`
already returned the distinct `"no_entries_collection"` state (added by
ACD-2100b-3), but no caller branch consumed it, so it fell through to the same
`notFoundMessage` an "absent" entry gets. Introduced a
`registryHasNoEntriesCollection` check that renders a new, separately-labelled
`agent()` dispatch (`workspace-setup-registry-no-entries-collection`) and
returns a distinct `{status:"error", message: "...could not be used: its
'agents' field is not a list of agent entries. Found <type> <value> where the
agent entries collection was expected..."}` — captured via two new outer-scope
variables (`noEntriesCollectionFound`/`noEntriesCollectionFoundValue`) set
alongside `workspaceSetupAgentEntryState` so the halt can name the actual
resolved value (never a literal). The message states "could not be used" and
names both the value and its `typeof` (AC-1), asserts neither absence nor
denial and names no `permits_shell` (AC-2/AC-3), and fails closed identically
to the sibling branches — same early `return`, same WARNING-level `log()` call
before the halt (AC-4) — so this only adds a third diagnosis, never relaxes or
supersedes ACD-2100b-3's or BO-1500f-1's halts. Ran
`AC_ENFORCE_STRICT=1 python3 -m pytest unit_tests/workflows/test_acd_2100b_3_i.py -v`:
4/4 passed, including the two red_baseline tests above (both were RED before
this change, confirmed by stashing the plan-feature.js diff and re-running).
Also ran `unit_tests/workflows/test_acd_2100b_3.py` (the sibling ticket's own
suite, at risk of regression since both branches share the same
`if (!permitsShell)` gate): 4/4 passed — the "denied" and "absent" outcomes
are unaffected. Ran the fuller
`AC_ENFORCE_STRICT=1 python3 -m pytest unit_tests/workflows/ -q` suite: 537
passed, 4 pre-existing failures in `test_acd_2100b_5.py` (a different,
in-flight, unrelated ticket's own new red tests — confirmed pre-existing by
stashing this ticket's plan-feature.js diff and reproducing the same 4
failures with byte-identical error text before my change). This is a `.js`
file under `templates/`, so the Python doc-enforcer/complexity-reduction
skills do not apply; no `.py` production files were touched (test authoring
was already done by test-writer). `node --check` confirms valid syntax.
Handing to test-runner next.

### 2026-09-07 09:12 — test-runner (status: ok)
feedback-id: fb_2026-09-07_12c5f640
completion_manifest:
  test_suite_executed: true
  all_tests_passing: true
  failure_report_structured: true
Ran `AC_ENFORCE_STRICT=1 python3 -m pytest unit_tests/workflows/test_acd_2100b_3_i.py -v`:
4/4 passed (test_registry_without_an_entries_collection_reports_could_not_be_used,
test_no_entries_collection_report_asserts_neither_absence_nor_denial,
test_no_entries_collection_does_not_raise_an_unhandled_failure,
test_outcome_is_produced_through_the_workflow_entry_point), confirming AC-1
through AC-4 are satisfied by python-coder's third halt branch in
`templates/workflows-js/plan-feature.js`. Also re-ran the sibling regression
suite `unit_tests/workflows/test_acd_2100b_3.py` (4/4 passed, no regression
to the absent/denied branches) and `node --check` on the changed file (valid
syntax). No failures to report; ticket ready for pr-reviewer.

### 2026-09-07 09:20 — pr-reviewer (status: ok)
feedback-id: fb_2026-09-07_2b2e059d
completion_manifest:
  high_confidence_findings: true
  medium_confidence_findings: true
  cross_file_contract_traced: true
  tests_re_run_green: true
Reviewed the diff to `templates/workflows-js/plan-feature.js` (the new
`registryHasNoEntriesCollection` third halt branch and its
`noEntriesCollectionFound`/`noEntriesCollectionFoundValue` capture) against
the ticket's AC-1..AC-4 and the repo's error-handling policy: the JSON.parse
interpretation failure is still caught by type (SyntaxError) and logged at
WARNING before any halt, the new branch fails closed identically to its
"denied" and "absent" siblings (same early `return`, same WARNING `log()`
call), and the message asserts neither absence nor denial while naming the
found value and its `typeof`. Performed the cross-file contract trace named
in this ticket's "Expects From" (AC ACD-2100b-3): read
`_resolveWorkspaceSetupAgentEntryState()` at line 2001 and confirmed it
already returns the exact `{state: "no_entries_collection"}` shape this
ticket's new branch consumes — no gap. Re-ran
`unit_tests/workflows/test_acd_2100b_3_i.py` (4/4 passed) and the sibling
`test_acd_2100b_3.py` regression suite (4/4 passed); confirmed these are
real-effect tests (a real Node child_process against a real on-disk registry
file, not a mocked `agent()`), so no dispatch-topology-only finding applies.
`node --check` on the changed file is clean. No high- or medium-confidence
findings; 0 low-confidence nits suppressed.

### 2026-09-07 09:30 — ac-validator (status: ok)
feedback-id: (submit-failed)
Verified all 4 ACs against the staged diff of `templates/workflows-js/plan-feature.js`
(`git diff --cached`) and against `unit_tests/workflows/test_acd_2100b_3_i.py`.
Store-alignment check
(`scripts/commit_guardian/check_v2_ac_store_alignment.py --ticket <this ticket>`)
exited 0 — no violations. No `delivers_to`/`expects_from` cross-file contract gap:
this ticket's own "Expects From" (AC ACD-2100b-3) was already traced by
pr-reviewer against `_resolveWorkspaceSetupAgentEntryState()` and confirmed
satisfied; re-confirmed the `no_entries_collection` state shape is consumed
unchanged by the new branch.

AC-1 covered:
  implementation: templates/workflows-js/plan-feature.js — new `registryHasNoEntriesCollection`
    branch (staged diff, `if (!permitsShell)` block) builds
    `noEntriesCollectionMessage = "The agent registry (config/agent_registry.json) could not
    be used: its 'agents' field is not a list of agent entries. Found " + foundTypeText + " "
    + foundValueText + " where the agent entries collection was expected. ..."` and returns
    `{status: "error", message: noEntriesCollectionMessage}`.
  test: test_registry_without_an_entries_collection_reports_could_not_be_used
    (unit_tests/workflows/test_acd_2100b_3_i.py:415) — PASSED (verified via
    `AC_ENFORCE_STRICT=1 pytest unit_tests/workflows/test_acd_2100b_3_i.py -v`).

AC-2 covered:
  implementation: same `registryHasNoEntriesCollection` branch — a wholly separate
    message from the `notFoundMessage` absence branch; contains no absence phrase.
  test: test_no_entries_collection_report_asserts_neither_absence_nor_denial
    (unit_tests/workflows/test_acd_2100b_3_i.py:442) — PASSED.

AC-3 covered:
  implementation: same branch — the message never references `permits_shell` or any
    permission verdict (only "could not be used" / "Found <type> <value>").
  test: test_no_entries_collection_report_asserts_neither_absence_nor_denial
    (unit_tests/workflows/test_acd_2100b_3_i.py:442) — same test, asserts the
    `_PERMISSION_SETTING_MARKERS = ("permits_shell",)` marker is absent — PASSED.

AC-4 covered:
  implementation: the branch ends with an early `return { status: "error", ... }`
    (no `throw`), matching the sibling "denied"/"absent" branches' fail-closed shape.
  test: test_no_entries_collection_does_not_raise_an_unhandled_failure
    (unit_tests/workflows/test_acd_2100b_3_i.py:478) — PASSED; also
    test_outcome_is_produced_through_the_workflow_entry_point
    (unit_tests/workflows/test_acd_2100b_3_i.py:521) confirms the report is produced
    through the real workflow entry point and the run halts there — PASSED.

All 4 ACs covered (4/4). Ran
`AC_ENFORCE_STRICT=1 python3 -m pytest unit_tests/workflows/test_acd_2100b_3_i.py -v`:
4 passed. Flipped AC-1..AC-4 checkboxes and `ac_coverage: 4/4` in frontmatter.
Commit phase may proceed.

### 2026-09-07 09:35 — ac-fulfillment-gate (status: ok)
feedback-id: (submit-failed)
completion_manifest:
  resolver_ran: true
  ac_yaml_verified: true
  auto_fix_applied: true
  schema_validation_passed: true
Ran `ac_coverage_resolver.py --ticket <this ticket>`: resolved 1 AC
(`ACD-2100b-3-i`, level L3, via `traceability_block`, two-key `{id, path}`
form) with 2 failing fields on first pass: `work_status` (`todo`, but
`templates/workflows-js/plan-feature.js` is in both `files_touched` and the
branch diff `git diff HEAD --name-only`) and `covered_by` (empty). Per Step
3c, searched `unit_tests/` and `tests/` for a genuine `# covers:
ACD-2100b-3-i` tag and found four in the diff-added
`unit_tests/workflows/test_acd_2100b_3_i.py` (lines 416, 443, 479, 522) —
this is an L3 AC, so per the BO-202 fix the auto-fix eligibility check was
run regardless of level. Auto-fixed (append-only): `work_status: todo` ->
`done`, and `covered_by: []` -> `[unit_tests/workflows/test_acd_2100b_3_i.py]`.
Ran `scripts/commit_guardian/check_ac_schema.py` on the edited AC YAML:
exit 0 (schema module absent locally, script fell back to manual field
validation, still exit 0). Re-ran the resolver: `ok: true`, 0 remaining
failures, `resolved_acs` non-empty (1). Commit phase may proceed.

### 2026-09-07 09:22 — commit (status: ok)
feedback-id: fb_2026-09-07_e1b135f7
completion_manifest:
  pre_commit_hooks_pass: true
  commit_message_valid: true
  ticket_staged: true
Ran the Step 0a probe from the wrong cwd first (workspace root, not the
worktree), which reported `git_hook: false`; re-ran with cwd set to the
worktree root via `env --chdir=` and got a clean `failing_checks: []` —
confirmed this was a cwd artifact, not a real hook gap, since
`.git/hooks/pre-commit` in the shared commondir is present, executable, and
current (mtime today). Staged only this ticket's own files out of a shared
dirty worktree — `templates/workflows-js/plan-feature.js`,
`unit_tests/workflows/test_acd_2100b_3_i.py`, this ticket file, and
`docs/acceptance-criteria/.../ACD-2100b-3-i.yaml` — deliberately leaving
untouched the concurrent, unrelated modifications to `ACD-2100b-5.yaml`,
`unit_tests/workflows/test_acd_2100b_5.py`, the `docs/agents/cards/*.card.md`
files, and the other sibling tickets' `.md` files, per the shared-worktree
commit discipline. Committed via `COMMIT_AGENT_MODE=1 git commit`; pre-commit
hooks ran and passed.
