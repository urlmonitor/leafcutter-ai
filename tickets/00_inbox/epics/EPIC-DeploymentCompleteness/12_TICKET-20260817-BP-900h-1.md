---
advances_current_outcome: true
agents:
  commit: signed_off
  documentation-expert: not_needed
  pr-reviewer: signed_off
  pull-request: needed
  python-coder: signed_off
  sql-coder: not_needed
  test-runner: signed_off
  test-writer: signed_off
components:
- build_pipeline
created: '2026-08-17'
depends_on: []
files_touched:
- .github/workflows/ci.yml
- scripts/ci/check_consumer_install.py
- unit_tests/portability/test_consumer_simulation.py
priority: critical
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: BP-900h-1
status: in_progress
title: CI installs the package into an empty project and the build succeeds
change_target: pipeline
risk_surface: contract_boundary
---

# CI installs the package into an empty project and the build succeeds

## Actor / Goal

As an adopter installing leafcutter into my own repository, I want every change
to the package proven against a real clean install, so that I never receive a
release whose install is broken.

## Context

This ticket was generated from AC store entry `BP-900h-1`. Component:
`build_pipeline`. Assigned agent: `python-coder`. Estimated complexity: `M`.

Added to this epic on 2026-08-17, after the epic was scoped. BP-900a..BP-900g
verify deployment completeness *statically*, from inside the build. Nothing
verifies it *empirically*, by performing an install and looking at the result —
which is why phase_1 exit criterion 1 in [docs/roadmap.json](../../../../docs/roadmap.json)
has no automated check, and why seven consumer-install defects (BP-900g-4/-5/-6,
BP-015, BP-016, BP-017, BP-018) shipped and were hotfixed after release between
2026-08-13 and 2026-08-17.

The nearest existing coverage is not a substitute:
`unit_tests/portability/test_build_deployment.py` asserts byte-identity for the
`build_commit_guardian` phase only, and `tests/test_build_clean.py` exercises the
`--clean` flag rather than a clean install.

## Acceptance Criteria

```gherkin
Given a scratch directory containing no files other than a minimal skills_config.json,
And the package checked out into leafcutter-ai/ beneath it, simulating a consumer install,
When CI runs "python leafcutter-ai/scripts/build.py --target-dir ." from that directory,
Then the build exits 0
And the deployed output root exists and contains scripts/ and agents/
And every script path referenced by a compiled agent template or skill file
  resolves to a file that exists in the deployed tree
And the job fails with the unresolved paths named on stderr if any does not
```

## Implementation Notes

- Build the scratch project from an **empty directory**, never from a copy of
  this repository. Copying the repo reintroduces the source-tree bias that hides
  every missing-deploy defect.
- Assert against the **deployed** tree only. Do not add the package source
  directory to `sys.path` when checking that references resolve.
- The job must run on `pull_request`, so it gates before merge.
- Reuse the reference-resolution logic that BP-900b-1/-2 build inside the build
  rather than reimplementing the matching rules. The new value here is the
  empty-project execution context.

## Test Requirements

Per CLAUDE.md "Gate / Workflow ACs — Verify Behaviorally, Not by Grep", a test
that only greps `ci.yml` for the job name is not acceptable coverage — it passes
on a job that never runs.

- `test_consumer_simulation_build_succeeds_in_empty_project` — run the real
  `build.py --target-dir` into an empty `tmp_path` containing only a minimal
  `skills_config.json`; assert exit 0 and that the deployed output root contains
  `scripts/` and `agents/`.
- `test_consumer_simulation_detects_unresolved_reference` — negative case: remove
  one deployed script after the build, re-run the reference check, and assert it
  reports a non-zero result naming that path. This is the test that proves the
  gate can actually fail.

Target dir: `unit_tests/portability/`.

## Sign-offs

- [x] python-coder — 2026-08-26 08:29
- [x] test-writer — 2026-08-26 08:21
- [x] test-runner — 2026-08-26 09:40
- [x] pr-reviewer — 2026-08-26 09:04
- [x] commit — 2026-08-26 10:15
- [ ] pull-request

## Comments

### 2026-08-26 08:21 — test-writer (status: ok)
feedback-id: fb_2026-08-26_31674d96
completion_manifest:
  test_requirements_parsed: true
  tests_written_using_ticket_names_verbatim: true
  tests_confirmed_red: true
  real_artifact_behavioral_test_present: true

## Test Writer — Completion Report

### Tests Written
| File | Directory | Framework | Status |
|---|---|---|---|
| test_consumer_simulation.py | unit_tests/portability/ | pytest | written |

Both required test functions were written exactly as named in `## Test
Requirements`: `test_consumer_simulation_build_succeeds_in_empty_project` and
`test_consumer_simulation_detects_unresolved_reference`. Each invokes
`scripts/ci/check_consumer_install.py` as a real subprocess (`sys.executable`)
against a real `tmp_path` scratch directory (never a copy of this repo, per
Implementation Notes), against the CLI contract fixed by the ticket
(`--package-dir`, `--target-dir`, `--skip-build`; exit codes 0/1/2) — this
satisfies the real-artifact behavioral mandate (BP-1100f-2): no mocking of the
build subprocess, filesystem, or reference-resolution logic; test 1 reads the
deployed `.leafcutter/{agents,scripts}` tree back off disk, and test 2 deletes
a real deployed file (`.leafcutter/scripts/ac_store/ac_prioritizer.py`,
confirmed both deployed and referenced by `templates/agents/build-ac.md`) and
re-runs the real script to assert the negative case fires on real output.
Confirmed via research: `build_referential_integrity.extract_compiled_script_path_refs`
and `build_propagation_audit.build_broken_ref_report`/`BrokenRefEntry` already
exist in this worktree (landed by BP-900b-1/BP-900c-1) with the exact
signatures the ticket's Implementation Notes specify, so the test does not
reimplement or duplicate that matching logic — it only asserts against
`check_consumer_install.py`'s own CLI surface, which does not exist yet.

### Verification Run
- Command: `AC_ENFORCE_STRICT=1 python -m pytest unit_tests/portability/test_consumer_simulation.py -q`
- Result: red (2 failures — expected; `scripts/ci/check_consumer_install.py` not yet written)

### Notes
No new test directories were needed (`unit_tests/portability/` already exists
with `__init__.py` + `conftest.py`, which puts the worktree root on
`sys.path`). No fixture extraction was required — no dict/table in this file
exceeds the §2h thresholds. Not a bug-fix ticket, so the regression-test
mandate does not apply.

red_baseline:
  - test_name: test_consumer_simulation_build_succeeds_in_empty_project
    file: unit_tests/portability/test_consumer_simulation.py
    error: "AssertionError: check_consumer_install.py did not exit 0 for a real build into an empty scratch directory.\nstderr:\n/usr/bin/python: can't open file '.../scripts/ci/check_consumer_install.py': [Errno 2] No such file or directory\nassert 2 == 0"
  - test_name: test_consumer_simulation_detects_unresolved_reference
    file: unit_tests/portability/test_consumer_simulation.py
    error: "AssertionError: Precondition failed: the initial (unmodified) check_consumer_install.py run must succeed before the negative case can be exercised.\nstderr:\n/usr/bin/python: can't open file '.../scripts/ci/check_consumer_install.py': [Errno 2] No such file or directory\nassert 2 == 0"

Handing off to python-coder to implement `scripts/ci/check_consumer_install.py`
against the CLI contract in the ticket body — both tests above are the
explicit success target.

### 2026-08-26 08:29 — python-coder (status: ok)
feedback-id: fb_2026-08-26_2c9feed7
completion_manifest:
  script_implemented_per_cli_contract: true
  ci_job_wired: true
  red_baseline_green: true
  no_scope_creep: true
  doc_enforcer_requirements_met: true
  complexity_manually_reviewed: true

## Implementation

Wrote `scripts/ci/check_consumer_install.py` (346 lines) implementing the exact
CLI contract fixed by test-writer: `--package-dir`, `--target-dir`,
`--skip-build`; exit codes 0/1/2. Composed of four small, independently
testable helper functions (`_ensure_scratch_environment`, `_maybe_run_build`,
`_verify_deployed_tree`, `_check_unresolved_references`) so `main()` stays a
simple linear pipeline. The reference-resolution check reuses
`build_referential_integrity.extract_compiled_script_path_refs()` and
`build_propagation_audit.build_broken_ref_report()` unmodified (imported via
`sys.path.insert(0, str(package_dir / "scripts"))`), per the ticket's
Implementation Notes — no matching-rule logic was reimplemented. All external
I/O (subprocess, file writes, directory walks) is wrapped in
`try/except <SpecificExceptionType>` per the repo's error-handling policy,
each branch either logging+returning an error code or re-raising.

Wired a new `consumer-install-sim` job into `.github/workflows/ci.yml`:
checks the package out into `leafcutter-ai/` (`path: leafcutter-ai`), installs
`leafcutter-ai/requirements-dev.txt`, then runs
`python leafcutter-ai/scripts/ci/check_consumer_install.py --package-dir
leafcutter-ai --target-dir .` as the only logic-bearing step (arguments only,
matching the sibling BP-900h-6 constraint). Runs on both `push` and
`pull_request` (no `if:` guard needed — job logic is identical either way,
satisfying "must run on pull_request"). Added a header-comment entry
documenting the job and explicitly noting it is informational only for now
(promotion to a required/blocking check is BP-900h-3, out of scope here). Did
not touch branch-protection settings or any required-checks list. Did not
touch the 4 pre-existing unstaged `docs/agents/cards/*.card.md` modifications
(unrelated AC-store build drift, per the ticket's scope guardrails). Did not
modify `build_referential_integrity.py` or `build_propagation_audit.py`.

### Verification

```
$ AC_ENFORCE_STRICT=1 python -m pytest unit_tests/portability/test_consumer_simulation.py -v
unit_tests/portability/test_consumer_simulation.py::test_consumer_simulation_build_succeeds_in_empty_project PASSED [ 50%]
unit_tests/portability/test_consumer_simulation.py::test_consumer_simulation_detects_unresolved_reference PASSED [100%]
2 passed in 5.89s
```

```
$ AC_ENFORCE_STRICT=1 python -m pytest unit_tests/portability/ -q
................                                                         [100%]
16 passed in 4.86s
```

`ruff check scripts/ci/check_consumer_install.py` — all checks passed.
`python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"` —
YAML OK.

red_baseline_results:
  - test_name: test_consumer_simulation_build_succeeds_in_empty_project
    result: green
  - test_name: test_consumer_simulation_detects_unresolved_reference
    result: green

### Notes for downstream agents

`test-runner` should re-run the full `unit_tests/portability/` suite plus
`ruff check scripts tests unit_tests` as part of its own pass.
`pr-reviewer`/`ac-validator` should confirm the new CI job's structural
conventions (checkout/setup-python/pip install pattern) match the other seven
jobs in `.github/workflows/ci.yml`, and that no required-status-checks list
was touched (BP-900h-3 is explicitly out of scope for this ticket).
Doc-enforcer requirements (module docstring with all 4 fields, function
docstrings, DECISION HISTORY block with tail-tag `(#EPIC-DeploymentCompleteness/12)`)
were applied manually to the new script — the `doc-enforcer` and
`complexity-reduction` Skill-tool invocations named in the standard workflow
were not available as tools in this session, so their checklists were applied
by hand against `templates/skills/doc-enforcer/SKILL.md` instead. `main()` was
kept to a 5-statement linear pipeline (no nested branching) specifically to
avoid needing a complexity-reduction pass; each helper function has a single
early-return-per-error-branch shape.

### 2026-08-26 09:05 — ticket-supervisor (status: ok)
feedback-id: (submit-failed)
completion_manifest:
  files_touched_corrected: true
  negative_case_discrimination_proven: true
  config_discrepancy_documented: true

**files_touched corrected.** Frontmatter `files_touched` listed only
`.github/workflows/ci.yml`; the change actually spans three files
(`.github/workflows/ci.yml`, `scripts/ci/check_consumer_install.py`,
`unit_tests/portability/test_consumer_simulation.py`). Updated frontmatter to
the real set before staging, per the coordinator's request — an inaccurate
`files_touched` is a phantom-done vector that `check-commit-scope` compares
the staged set against.

**Negative-case discrimination independently proven (fault injection).**
`test_consumer_simulation_detects_unresolved_reference`'s only observed RED
failure prior to python-coder's implementation was a *precondition* failure
(the script did not exist) — its real assertion (`second_run.returncode !=
0` + missing-path string in stderr) had never been observed to fail. I
temporarily edited `scripts/ci/check_consumer_install.py`'s
`_check_unresolved_references` to force `broken = []` right after the real
`bpa.build_broken_ref_report(...)` call (disabling detection while leaving
everything else genuine), re-ran the single test, and confirmed it now fails
on the real assertion:
`AssertionError: check_consumer_install.py must fail (non-zero exit) ...
assert 0 != 0`. I then reverted the edit exactly (confirmed via `grep -n
"TEMP-FAULT-INJECTION"` returning no match) and re-ran the full
`unit_tests/portability/` suite, confirming 16 passed. This proves the test
detects a genuinely broken detection path, not just "script file present."

**`build.py` config-discovery discrepancy (reported, not fixed).** The AC's
scenario ("a scratch directory containing no files other than a minimal
skills_config.json") reads as though that file configures the build. It does
not, for the exact invocation this ticket's script and CI job use.
`check_consumer_install.py`'s `_ensure_scratch_environment` writes
`<target-dir>/skills_config.json` (bare root), and `_maybe_run_build` invokes
`build.py --target-dir <target-dir>` **without** a `--config` flag. When
`--config` is omitted, `build.py`'s `main()` passes `config_path=None` into
`load_config()` (`scripts/config_loader.py:33`), whose auto-detect branch
(`scripts/config_loader.py:59-68`) only checks
`target_root / <platform_dir> / "skills_config.json"` for
`platform_dir in (".claude", ".gemini", ".cursor", ".github", ".cline")` —
it never checks a bare `target_root / "skills_config.json"`. `build.py`'s own
`--config` help text confirms the intended default location:
"Defaults to `<target-dir>/.claude/skills_config.json`" (`scripts/build.py:1451`).
So the minimal config this simulation writes is inert; the simulated install
runs entirely on `config/skills_config.default.json` defaults. This does NOT
invalidate the ticket's tests — a default-config consumer install is still a
real, honest consumer install, and both tests pass against real behavior —
but the AC's literal premise (a config file that configures the build) is not
what actually happens today. The sibling AC BP-900h-6 shares the same
"minimal skills_config.json" premise and will inherit the same gap. Leaving
this as a reported finding for the coordinator to decide: amend the AC's
wording, fix the script to pass `--config <target-dir>/skills_config.json`
explicitly (a one-line, in-scope change to `check_consumer_install.py` — no
`build.py`/`config_loader.py` change required), or file as a known issue.
No change made to `build.py`, `config_loader.py`, or the AC text.

Proceeding to test-runner.

### 2026-08-26 09:40 — ticket-supervisor (status: ok)
feedback-id: (submit-failed)
completion_manifest:
  test_runner_dispatch_1_result: "tooling failure (Edit disabled), not a test failure"
  test_runner_dispatch_2_result: "tooling failure (Edit disabled), not a test failure"
  targeted_suite_independently_confirmed: true
  full_suite_independently_confirmed: true
  full_suite_failure_diagnosed_as_stale_worktree_base: true

**test-runner phase: substituted by ticket-supervisor after two consecutive
tooling failures (honest record, not a self-sign-off).** Two separate
`test-runner` dispatches for this ticket (one backgrounded, one foreground)
each ran the full requested suite successfully and reported clean results,
but neither could complete its own sign-off write: both sessions reported
"No such tool available: Edit. Edit is disabled for this session." Per
explicit operator instruction, a third `test-runner` dispatch was not
attempted. The ticket-supervisor (which does have `Edit` access) ran the
identical commands directly to independently confirm the results those two
dispatches reported, rather than taking either report on faith:

```
$ AC_ENFORCE_STRICT=1 python -m pytest unit_tests/portability/ unit_tests/test_bp_900b_1.py -q
......................                                                   [100%]
22 passed, 1 warning in 5.74s
```

```
$ AC_ENFORCE_STRICT=1 python -m pytest unit_tests/ -q --continue-on-collection-errors
FAILED unit_tests/commit_guardian/test_ge_122e_1.py::TestGE122e1::test_goal_record_claims_a_new_id_and_its_folder_matches_origin_main
1 failed, 3998 passed, 7 skipped, 6 xfailed, 14 warnings, 467 subtests passed in 605.80s
```

Three independent runs (test-runner dispatch 1, test-runner dispatch 2, and
this supervisor run) all agree on these exact numbers. The single full-suite
failure is diagnosed as pre-existing, unrelated to this ticket's diff: this
worktree's branch was created from a stale local `main` (`git merge-base HEAD
origin/main` = `498b1641`; `git rev-list --left-right --count HEAD...origin/main`
= `0  27` — zero ahead, 27 behind). AC id `GE-120c-6` was added to
`docs/acceptance-criteria/guardrail-engine/GE-120-green-means-checked/` by a
commit on `origin/main` (PR #555) that is not an ancestor of this branch
(`git merge-base --is-ancestor <PR#555 sha> HEAD` exits 1), so the AC file is
genuinely absent from this working tree and the test is correctly reporting
that absence — not a regression caused by `scripts/ci/check_consumer_install.py`,
`.github/workflows/ci.yml`, or `unit_tests/portability/test_consumer_simulation.py`
(none of which touch `docs/acceptance-criteria/guardrail-engine/`). This is
the documented `setup_ticket_worktree.py` stale-local-main trap. Resolution:
merge `origin/main` into this branch AFTER the commit phase (so in-progress
work is not put at risk by the merge), then re-run only the affected test to
confirm it turns green.

Marking `agents.test-runner: signed_off` below reflects that the test-runner
phase's actual deliverable — a verified, accurate test report distinguishing
this ticket's changes from pre-existing/unrelated failures — was produced and
independently triple-confirmed. It was NOT produced by the `test-runner`
agent's own sign-off write (both of its attempts failed on a tooling gap
unrelated to test correctness), so this comment records the substitution
honestly rather than crediting the agent falsely.

**Outstanding items from earlier in this drive, now resolved and already on
record above** (2026-08-26 09:05 comment): the negative-case fault-injection
discrimination proof, and the `build.py` config-discovery discrepancy
(`scripts/config_loader.py:59-68` auto-detect loop / `scripts/build.py:1451`
help text) — both documented there with full evidence, neither fixed, per
instruction.

Proceeding to pr-reviewer.

### 2026-08-26 09:04 — pr-reviewer (status: ok)
feedback-id: fb_2026-08-26_f4ed58e6
completion_manifest:
  toolkit_review_dispatched: true
  reuse_of_existing_matching_logic_confirmed: true
  skip_build_flag_confirmed_real: true
  error_handling_policy_compliance_confirmed: true
  ci_job_structural_convention_confirmed: true
  files_touched_matches_diff_confirmed: true
  tests_independently_reran: true

## PR Reviewer — Completion Report

Reviewed the working diff for the three real changed files
(`.github/workflows/ci.yml`, `scripts/ci/check_consumer_install.py`,
`unit_tests/portability/test_consumer_simulation.py`), excluding the 4
pre-existing unstaged `docs/agents/cards/*.card.md` files per the ticket's
scope guardrails. High-confidence findings: none.

**Reuse, not reimplementation.** `check_consumer_install.py`'s
`_check_unresolved_references` imports and calls
`build_referential_integrity.extract_compiled_script_path_refs()` and
`build_propagation_audit.build_broken_ref_report()` unmodified
(lines 194-195, 235, 245); confirmed by reading both source functions that
neither the matching rules nor the broken-ref classification logic are
duplicated in the new file. `extract_compiled_script_path_refs` is itself
documented as "never raises... fail-open," so the unwrapped call sites are
correct, not an error-handling gap.

**`--skip-build` verified real.** `_maybe_run_build` returns `0` immediately
when `skip_build=True` without touching `build.py` (lines 105-106) — exactly
what `test_consumer_simulation_detects_unresolved_reference` relies on to
keep a deleted deployed file deleted across the second run. Independently
reran both new tests
(`AC_ENFORCE_STRICT=1 python -m pytest unit_tests/portability/test_consumer_simulation.py -q`
→ 2 passed) to confirm this holds in practice, not just by inspection.

**Error-handling policy.** Every external I/O call in the new script
(`mkdir`, `write_text`, one `subprocess.run`, `read_text`/`json.loads`,
`rglob`) is wrapped in `try/except <SpecificExceptionType>`, each branch
logging to stderr and returning/raising. Confirmed no bare `except:` via
grep. `ruff check scripts/ci/check_consumer_install.py
unit_tests/portability/test_consumer_simulation.py` — all checks passed
(independently reran, not just trusted from python-coder's report).

**CI job conventions.** `consumer-install-sim` follows the identical
`actions/checkout@v4` / `actions/setup-python@v5` (3.13) /
`pip install -r requirements-dev.txt` pattern used by all 7 other jobs in
`.github/workflows/ci.yml` (path adapted for the `leafcutter-ai/` subdirectory
checkout the design requires). Confirmed via `yaml.safe_load` that the file
parses and lists exactly 8 jobs. No required-status-checks list or
branch-protection config was touched anywhere in the diff — BP-900h-3's
scope boundary is respected.

**`files_touched` accuracy.** Frontmatter's corrected 3-entry list matches
the real diff exactly.

**Noted, not re-flagged: `build.py --config` auto-detect discrepancy.**
Already discovered and fully documented by ticket-supervisor (2026-08-26
09:05 comment above) — the bare `<target-dir>/skills_config.json` this
simulation writes is not on `config_loader.py`'s auto-detect search path, so
the simulated install runs on package defaults rather than that file. This
is an AC-wording gap, not a code defect: the AC's actual pass/fail conditions
(build exits 0, deployed root contains `scripts/`/`agents/`, references
resolve, failures named on stderr) are all satisfied against real behavior
regardless. Correctly left as a reported, not-fixed finding for the
coordinator; not a blocker for this ticket.

No medium-confidence findings either — the medium-cluster escalation gate
was not triggered (0 findings, threshold > 3 not applicable).

### 2026-08-26 10:15 — commit (status: ok)
feedback-id: fb_2026-08-26_7b79f33b
Auto-authorized commit gate: subject "feat(ci): add consumer-install simulation gate (BP-900h-1)"; staged files: .github/workflows/ci.yml, scripts/ci/check_consumer_install.py, tickets/00_inbox/epics/EPIC-DeploymentCompleteness/12_TICKET-20260817-BP-900h-1.md, unit_tests/portability/test_consumer_simulation.py.
completion_manifest:
  pre_commit_hooks_pass: true
  commit_message_valid: true
  ticket_staged: true

Signing off `pr-reviewer`. Proceeding to `commit`.
