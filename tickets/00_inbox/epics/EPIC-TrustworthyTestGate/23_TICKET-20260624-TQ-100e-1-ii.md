---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  pr-reviewer: needed
  pull-request: needed
  python-coder: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
components:
- testing-quality
created: '2026-06-24'
depends_on:
- TQ-100e-1
files_touched:
- scripts/ac_store/test_enforcement.py
- tests/testing_quality/test_enforcement_mode.py
priority: high
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: TQ-100e-1-ii
status: todo
title: With no explicit config, enforcement defaults to the safe non-blocking mode
---

# With no explicit config, enforcement defaults to the safe non-blocking mode

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `TQ-100e-1-ii` — With no explicit config, enforcement defaults to the safe non-blocking mode — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `TQ-100e-1-ii`. Component: `testing-quality`. Assigned agent: `python-coder`. Estimated complexity: `S`.

## Acceptance Criteria

```gherkin
Given a project or consumer install in which no explicit enforcement-mode
  configuration is present,
When the suite is run,
Then the active enforcement mode is report-only (the safe non-blocking default)
And the run is never blocked solely because the mode was unconfigured
And no project is silently promoted to blocking by the absence of explicit
  configuration.
```

## Test Requirements

- `test_no_config_defaults_to_report_only`: With no explicit config or env value, enforcement defaults to the safe non-blocking report-only mode.

## Sign-offs

- [ ] python-coder
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit
- [ ] pull-request

## Comments
