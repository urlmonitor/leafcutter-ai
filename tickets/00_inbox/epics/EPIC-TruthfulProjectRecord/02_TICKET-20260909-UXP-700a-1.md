---
ac_traceability:
  id: UXP-700a-1
  path: docs/acceptance-criteria/ux-prototyping/UXP-700-truthful-project-record/UXP-700a-1.yaml
advances_current_outcome: true
agents:
  ac-fulfillment-gate: needed
  ac-validator: needed
  architect-review: needed
  commit: needed
  documentation-expert: needed
  documentation-verifier: needed
  frontend-coder: not_needed
  pr-reviewer: needed
  pull-request: not_needed
  python-coder: needed
  sql-coder: not_needed
  status-checker: needed
  test-runner: needed
  test-writer: needed
  user-surface-smoker: needed
change_target:
- code
- config
complexity: medium
components:
- ux_prototyping
- build_pipeline
created: '2026-09-09'
declares_side_effect: true
depends_on: []
documentation_required: true
files_touched: []
priority: medium
requires_adr: false
requires_diagram: false
risk_surface: contract_boundary
roadmap_phase: phase_1
source_ac: UXP-700a-1
status: todo
title: Installing the tooling lays down a complete, runnable, empty record
---

# Installing the tooling lays down a complete, runnable, empty record

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `UXP-700a-1` — Installing the tooling lays down a complete, runnable, empty record — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `UXP-700a-1`. Component: `ux-prototyping`. Assigned agent: `python-coder`. Estimated complexity: `M`. Complexity: `medium`.

## Acceptance Criteria

```gherkin
Given a project that has never used the tooling and holds no project record of any kind,
When the tooling is installed into that project,
Then a record is written into that project with a place for each artifact type the tooling
  supports (journeys, example data, screens), each of those places present and empty,
And a store index is written that declares zero artifacts and holds an entry for every
  derived lookup the index maintains, each of those entries empty,
And every input the checker reads before it begins checking is written into that project,
  so the checker reaches a stated verdict without an unhandled failure,
And an introduction is written into that project stating that the record is empty, naming
  each artifact type and where it lives, and naming the first thing a person should author.
```

- [ ] AC-1: a record is written into that project with a place for each artifact type the tooling
- [ ] AC-2: a store index is written that declares zero artifacts and holds an entry for every
- [ ] AC-3: every input the checker reads before it begins checking is written into that project
- [ ] AC-4: an introduction is written into that project stating that the record is empty, naming

## Test Requirements

```yaml
tests:
- name: test_fresh_install_creates_every_artifact_place_and_an_empty_index
  file: unit_tests/product_truth/test_uxp_700a_1.py
  covers:
  - UXP-700a-1
  asserts: Deploying into an empty temp project yields the three artifact directories
    and an index declaring zero artifacts with every derived lookup present and empty.
  framework: unittest
  type: integration
  angle: real_artifact
- name: test_fresh_install_record_reaches_a_verdict_from_its_own_checker
  file: unit_tests/product_truth/test_uxp_700a_1.py
  covers:
  - UXP-700a-1
  asserts: Running the record's checker against the freshly deployed record terminates
    with a stated verdict and raises no unhandled exception.
  framework: unittest
  type: integration
  angle: deployed
- name: test_every_startup_input_the_checker_reads_is_present_after_install
  file: unit_tests/product_truth/test_uxp_700a_1.py
  covers:
  - UXP-700a-1
  asserts: Each input the checker opens before checking begins resolves in the deployed
    record.
  framework: unittest
  type: integration
  angle: seam
- name: test_fresh_install_record_carries_a_written_introduction
  file: unit_tests/product_truth/test_uxp_700a_1.py
  covers:
  - UXP-700a-1
  asserts: The deployed record contains an introduction naming each artifact type,
    its location, and the first artifact to author.
  framework: unittest
  type: integration
  angle: real_artifact
- name: test_uxp_700a_1_reachable_from_entry_point
  file: unit_tests/product_truth/test_uxp_700a_1.py
  covers:
  - UXP-700a-1
  angle: reachability
  asserts: 'REQUIRED — invoke the production entry point (CLI, hook, slash command,
    workflow dispatch, or main()) as a subprocess/dispatch and assert the new behaviour
    actually occurs. Do NOT satisfy this by importing the function directly. The AC
    authored no test_spec, so the entry point is not declared: resolve it before writing
    this test, and do not delete this entry.'
```

## Agent Contracts

### documentation-expert

Existing docs to update / cross-link:

- docs/product-truth/README.md (relationship: related | status: exists)
- docs/how-to/authoring-product-truth-artifacts.md (relationship: related | status: exists)

- [ ] AC-1: how-to | docs/product-truth/README.md | a record is written into that project with a place for each artifact type the tooling

### Delivers To

- **Agent:** python-coder
- **Contract:** A deployed, empty-but-runnable record that UXP-700a-2 can regenerate and UXP-700a-4 can smoke-test.

## Sign-offs

- [ ] architect-review
- [ ] test-writer
- [ ] python-coder
- [ ] test-runner
- [ ] documentation-expert
- [ ] pr-reviewer
- [ ] user-surface-smoker
- [ ] ac-validator
- [ ] ac-fulfillment-gate
- [ ] documentation-verifier
- [ ] status-checker
- [ ] commit

## Comments
