id: BO-2200c-3
components:
  - build_orchestration
  - documentation_system
readiness: approved
priority: medium
title: "The genre is sourced from the parent L1's documentation_triggers"
component: build-orchestration
level: L2
status: active
req_status: draft
work_status: done
created_by: "docs/acceptance-criteria/build-orchestration/BO-2200-documentation-coverage-guarantee/BO-2200c.yaml"
criteria: |
  Given the generator is building the contract line for a leaf AC and the generator has
    loaded only that single leaf AC record,
  When it determines the Diataxis genre for the line,
  Then it resolves the leaf AC's parent L1 via the parent/`depends_on` link and reads the
    parent L1's `documentation_triggers` field to source the genre.
  And when the parent L1 declares multiple `documentation_triggers` (for example
    `[how-to, sequence-diagram]`), the contract reflects each declared genre.
depends_on: [BO-2200c, BO-2200c-2]
doc_links:
  - path: docs/reference/ac-schema.md
    relationship: describes
    status: exists
  - path: docs/architecture/components/build-orchestration.md
    relationship: describes
    status: exists
assigned_agent: python-coder
estimated_complexity: M
it_requirements:
  config_schema_fragment:
    parent_genre_resolution:
      type: object
      description: "Genre sourced from the parent L1's documentation_triggers (an L1-only field), resolved via derive_parent_id/depends_on; multiple triggers each reflected."
  reference_file_path: "scripts/ac_store/generate_ticket_from_ac.py"
  n_location_rule: "1"
  required_skills:
    - python-coder
  post_write_commands: []
  constraints:
    - "The generator today loads only the single leaf AC (via _find_ac_by_id / main); it must additionally resolve the parent L1 (via ac_parent_id.derive_parent_id / depends_on) and read its documentation_triggers, since documentation_triggers is an L1-only field."
    - "Handle multiple triggers declared on the parent L1 — reflect each declared genre in the contract."
delivers_to: null
expects_from:
  ac_id: BO-2200c-2
  contract: "The contract-line genre slot this behavior populates from the parent L1's documentation_triggers."
test_spec:
  - name: test_genre_resolved_from_parent_l1_documentation_triggers
    target_dir: unit_tests/
    framework: unittest
    type: behavioral
    description: "Generating a contract line for a leaf AC resolves the parent L1 and sources the genre from the parent's documentation_triggers."
  - name: test_multiple_parent_triggers_each_reflected
    target_dir: unit_tests/
    framework: unittest
    type: behavioral
    description: "When the parent L1 declares multiple documentation_triggers, the contract reflects each declared genre."
origin_agent: business-analyst
created: 2026-07-15
amended_by: []
superseded_by: null
covered_by: []
implemented_by:
- tickets/00_inbox/epics/EPIC-DocumentationCoverageGuarantee/19_TICKET-20260715-BO-2200c-3.md
