---
ac_traceability:
  id: GE-120b-2-i
  path: docs/acceptance-criteria/guardrail-engine/GE-120-green-means-checked/GE-120b-2-i.yaml
advances_current_outcome: true
agents:
  ac-fulfillment-gate: needed
  ac-validator: needed
  architect-review: needed
  commit: needed
  documentation-expert: needed
  documentation-verifier: needed
  pr-reviewer: needed
  pull-request: needed
  python-coder: needed
  test-runner: needed
  test-writer: needed
change_target: code
complexity: medium
components:
- commit_guardian
- precommit_hooks
created: '2026-08-25'
depends_on: []
documentation_required: true
files_touched: []
priority: high
requires_adr: false
requires_diagram: false
risk_surface: contract_boundary
roadmap_phase: phase_1
source_ac: GE-120b-2-i
status: todo
title: Main-checkout verdicts are unchanged by the shared resolution path
---

# Main-checkout verdicts are unchanged by the shared resolution path

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `GE-120b-2-i` — Main-checkout verdicts are unchanged by the shared resolution path — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `GE-120b-2-i`. Component: `guardrail-engine`. Assigned agent: `python-coder`. Estimated complexity: `M`. Complexity: `medium`.

## Acceptance Criteria

```gherkin
Given the main checkout, in which every check already resolves its prerequisites today,
And a recorded baseline of the verdict each check produces there against a fixed fixture of
  staged changes, captured before the shared resolution facility is adopted,
When each check is executed in the main checkout after adoption against that same fixture,
Then each check produces the same verdict as its recorded baseline,
And no check that previously located its prerequisites now fails to locate them,
And no check that previously reported a violation now reports a clean result,
And the only observable difference between before and after is confined to working copies
  in which resolution previously failed.

Coverage note: the baseline must be captured by execution before the change, not asserted
from memory or from the check sources afterwards. A comparison against a baseline recorded
after the change proves nothing.
```

- [ ] AC-1: a recorded baseline of the verdict each check produces there against a fixed fixture of
- [ ] AC-2: each check produces the same verdict as its recorded baseline
- [ ] AC-3: no check that previously located its prerequisites now fails to locate them
- [ ] AC-4: no check that previously reported a violation now reports a clean result
- [ ] AC-5: the only observable difference between before and after is confined to working copies

## Test Requirements

```yaml
tests:
- name: test_ge120b2i_main_checkout_verdicts_match_recorded_baseline
  file: unit_tests/portability/test_ge_120b_2_i.py
  covers:
  - GE-120b-2-i
  asserts: Every check executed in the main checkout after adoption produces the verdict
    recorded in the pre-adoption baseline, compared per check id.
  framework: unittest
  type: behavioral
- name: test_ge120b2i_no_check_loses_prerequisite_resolution
  file: unit_tests/portability/test_ge_120b_2_i.py
  covers:
  - GE-120b-2-i
  asserts: No check that previously located its prerequisites now fails to locate
    them.
  framework: unittest
  type: behavioral
- name: test_ge120b2i_no_check_downgrades_violation_to_clean
  file: unit_tests/portability/test_ge_120b_2_i.py
  covers:
  - GE-120b-2-i
  asserts: No check that previously reported a violation against the provoking fixture
    now reports clean — asserted per check id, not in aggregate.
  framework: unittest
  type: behavioral
- name: test_ge120b2i_differences_confined_to_previously_failing_copies
  file: unit_tests/portability/test_ge_120b_2_i.py
  covers:
  - GE-120b-2-i
  asserts: The only before/after differences observed are in working copies where
    resolution previously failed.
  framework: unittest
  type: behavioral
- name: test_ge_120b_2_i_reachable_from_entry_point
  file: unit_tests/portability/test_ge_120b_2_i.py
  covers:
  - GE-120b-2-i
  angle: reachability
  asserts: 'REQUIRED — invoke the production entry point (CLI, hook, slash command,
    workflow dispatch, or main()) as a subprocess/dispatch and assert the new behaviour
    actually occurs. Do NOT satisfy this by importing the function directly. The AC
    authored no test_spec, so the entry point is not declared: resolve it before writing
    this test, and do not delete this entry.'
```

## Implementation Notes

```yaml
- THE BASELINE IS AN ARTIFACT, captured by EXECUTION before the first line of GE-120b-2
  is written, and stored so the after-run is diffed against it mechanically. Record
  the commit SHA of the pre-sweep tree alongside it. A baseline recorded after the
  change, or reconstructed from the check sources, proves nothing — that is the criterion's
  own coverage note and it is the single way this AC is most likely to be faked.
- CAPTURE IT WITH THE GE-120c-1 HARNESS, not with an ad-hoc script written by the
  agent performing the migration. An implementer's own before/after apparatus is the
  failure mode GE-120c-2 exists to prevent, and this AC has the same exposure at smaller
  scale.
- 'THE FIXTURE MUST PROVOKE A REAL VIOLATION FOR EVERY CHECK in hooks_manifest.hooks[]
  (54 entries today, 48 judgment + 6 transform). A clean fixture makes every check
  agree trivially — including a check that has silently stopped running altogether.
  This fixture is SHARED WITH GE-120b-4: build it once and own it in one place; two
  fixtures will diverge and the family-wide claims will stop meaning the same thing.'
- 'The third clause is the load-bearing one: no check that previously reported a violation
  now reports clean. Assert PER CHECK BY ID, never in aggregate — an aggregate pass
  rate hides exactly one check going quiet, which is the consolidation bug this AC
  guards against.'
- Transform-tier hooks (6 today) mutate files. Run each baseline and each after-run
  from an identical fixture state so one check's mutation cannot change a later check's
  input; otherwise the diff reports order effects as regressions.
- 'SCOPE OF PERMITTED DIFFERENCE: only working copies in which resolution previously
  FAILED. Any main-checkout difference is a regression, not an improvement, and fails
  this AC.'
- Capture the baseline against the DEPLOYED copies (.leafcutter/scripts/commit_guardian/),
  because that is what pre-commit actually runs. A baseline taken against templates/
  measures a layout no commit uses (ADR-001).
```

## Agent Contracts

### documentation-expert

Existing docs to update / cross-link:

- docs/architecture/components/commit-guardian.md (relationship: describes | status: exists)
- templates/scripts/commit_guardian/commit_guardian.json (relationship: related | status: exists | relevance: hooks_manifest.hooks[] defines the per-check set the baseline must cover — 54 entries today.)
- docs/acceptance-criteria/guardrail-engine/GE-118-hooks-work-in-worktrees/GE-118a-1.yaml (relationship: context | status: exists | relevance: Precedent: carries the same main-checkout-unchanged clause on a change one eighteenth this size.)

- [ ] AC-1: [(unspecified genre)] docs/architecture/components/commit-guardian.md — each check produces the same verdict as its recorded baseline

### Expects From

- **AC:** GE-120b-2
- **Contract:** The shared prerequisite-resolution facility, adopted across the check family.

## Sign-offs

- [ ] architect-review
- [ ] test-writer
- [ ] python-coder
- [ ] test-runner
- [ ] documentation-expert
- [ ] pr-reviewer
- [ ] ac-validator
- [ ] ac-fulfillment-gate
- [ ] documentation-verifier
- [ ] commit
- [ ] pull-request

## Comments
