---
ac_traceability:
  id: BO-1000c-2
  path: docs/acceptance-criteria/build-orchestration/BO-1000-live-automation-progress/BO-1000c-2.yaml
advances_current_outcome: true
agents:
  commit: signed_off
  llm-expert: signed_off
  pr-reviewer: signed_off
  pull-request: signed_off
change_target: pipeline
complexity: medium
components:
- build_orchestration
created: '2026-07-20'
depends_on:
- 12_TICKET-20260720-BO-1000c-1a.md
- 13_TICKET-20260720-BO-1000c-1b.md
files_touched:
- templates/commands/finalize-feature.md
priority: medium
requires_adr: false
requires_diagram: false
risk_surface: internal
roadmap_phase: phase_1
source_ac: BO-1000c-2
status: done
title: Surfaced progress reflects the in-flight step, arriving over time rather than
  only at the end
---

# Surfaced progress reflects the in-flight step, arriving over time rather than only at the end

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BO-1000c-2` — Surfaced progress reflects the in-flight step, arriving over time rather than only at the end — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BO-1000c-2`. Component: `build-orchestration`. Assigned agent: `llm-expert`. Estimated complexity: `M`. Complexity: `medium`.

## Acceptance Criteria

```gherkin
Given the finalize-feature workflow is running in the background over several
  steps,
When progress is surfaced into the main conversation,
Then a progress line for a step appears in the conversation while the run is
  still in flight — before the run has finished — reflecting the step
  currently underway,
And multiple distinct progress updates arrive in the conversation over the
  course of the run, rather than a single batch delivered only after the run
  completes.
```

- [x] AC-1: a progress line for a step appears in the conversation while the run is
- [x] AC-2: multiple distinct progress updates arrive in the conversation over the

## Implementation Notes

```yaml
- 'Surface: the /finalize-feature launch contract (templates/commands/finalize-feature.md
  + main-loop poll/relay instruction) — the observable ''updates arrive in the conversation
  over time'' is produced by the launcher relaying incrementally, so this is llm-expert
  prose, not a finalize-feature.js edit.'
- The relay must deliver a progress line for the in-flight step BEFORE the run finishes
  — the launcher surfaces new journal lines as they appear rather than waiting for
  run completion.
- Multiple distinct updates must reach the conversation across the run (at least one
  per executed/skipped step that emits a line), not a single end-of-run batch. Poll
  cadence and dedup must ensure over-time delivery without repeating already-relayed
  lines.
- This behavior depends on BO-1000c-1a writing the journal incrementally (append-as-you-go)
  and BO-1000c-1b's poll/relay loop; do not re-implement the journal or the relay
  here — this AC asserts their combined over-time delivery guarantee.
- Keep the /finalize-feature command doc and its deployed copy in parity via build.py
  (ADR-001).
```

## Agent Contracts

### Expects From

- **AC:** BO-1000c-1a
- **Contract:** The incrementally-written, order-preserving run-progress journal (from BO-1000c-1a) which, when polled/relayed by BO-1000c-1b, yields in-flight progress lines arriving in the conversation over the course of the run.

## Sign-offs

- [x] pr-reviewer — 2026-07-23 09:00
- [x] llm-expert — 2026-07-23 08:55
- [x] commit — 2026-07-23 09:10
- [x] pull-request — 2026-07-23 09:15

## Comments

### 2026-07-23 08:55 — llm-expert (status: ok)
feedback-id: fb_2026-07-23_3c2b5084
completion_manifest:
  template_written: true
  prompt_quality_checklist_passed: true
  convention_violations_resolved: true
Added `### Over-Time Delivery Guarantee` section to `templates/commands/finalize-feature.md` asserting three named invariants: in-flight delivery (progress MUST appear before run finishes), multiple distinct updates (not a single end-of-run batch), and no re-delivery. Also fixed pre-existing `depends_on` format violation (AC IDs replaced with filenames; non-existent `BO-1000c` reference removed) to satisfy `ticket_frontmatter_guard.py`. Both ACs (AC-1 and AC-2) are fully addressed by the new guarantee section.

### 2026-07-23 09:00 — pr-reviewer (status: ok)
feedback-id: fb_2026-07-23_8e965760
completion_manifest:
  ac1_in_flight_delivery_covered: true
  ac2_multiple_distinct_updates_covered: true
  cross_file_contract_bo_1000c_1a_verified: true
  no_high_confidence_findings: true
  no_medium_confidence_findings: true
Reviewed the 27-line addition of `### Over-Time Delivery Guarantee` to `templates/commands/finalize-feature.md`. AC-1 (in-flight delivery) is addressed by Invariant 1; AC-2 (multiple distinct updates) is addressed by Invariant 2. Invariant 3 (no re-delivery) is a well-grounded additional safeguard consistent with the Polling loop step 3 already in place. Cross-file contract trace confirmed: BO-1000c-1a delivers an append-only journal at `WORKTREE_ROOT/run-progress.journal.jsonl` via `appendJournal()` using `fs.appendFileSync`, which the guarantee section correctly describes as "incremental append-as-you-go." The file-position tracking described in Invariant 3 is consistent with the Polling loop's "track the last line position relayed." No high-confidence or medium-confidence findings. Suppressed: 0 low-confidence nits, 0 medium findings dropped by Opus.

### 2026-07-23 09:10 — commit (status: ok)
feedback-id: fb_2026-07-23_9cfb061c
completion_manifest:
  pre_commit_hooks_pass: true
  commit_message_valid: true
  ticket_staged: true
Auto-authorized commit gate: subject "feat(finalize): assert over-time delivery guarantee in launcher spec (BO-1000c-2)"; staged files: docs/architecture/diagrams/c2-006-feature-to-merged-pr.md, docs/architecture/diagrams/finalize-progress-relay-sequence.md, templates/commands/finalize-feature.md, tickets/00_inbox/epics/EPIC-InFlightVisibility/14_TICKET-20260720-BO-1000c-2.md, tickets/00_inbox/epics/EPIC-InFlightVisibility/16_TICKET-20260720-BO-1000c-3.md.

### 2026-07-23 09:15 — pull-request (status: ok)
feedback-id: fb_2026-07-23_e9ce5edb
completion_manifest:
  branch_pushed: true
  pr_created: true
  pr_body_complete: true
Existing epic PR #360 (https://github.com/urlmonitor/leafcutter-ai/pull/360) covers this ticket's commit; branch EPIC-InFlightVisibility is fully synced with origin. PR is currently CONFLICTING with main (pre-existing epic-branch divergence — not caused by this ticket's push). All agents signed off; ticket status flipped to done.
