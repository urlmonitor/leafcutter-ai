id: BO-2700f-3
components:
  - build_orchestration
  - supervisor_system
title: "A skipped gate is reported apart from a passing gate and never satisfies a requirement that it pass"
component: build-orchestration
level: L2
status: active
req_status: draft
work_status: todo
readiness: draft
priority: medium
roadmap_phase: phase_1
criteria: |
  Given a work item whose recorded gate results contain gate A with outcome `passed`
    and gate B with outcome `skipped` and a reason,
    And gate B carries a requirement that it must pass before the work item is done,
  When the run's completion summary is produced and the work item is assessed for
    done-ness,
  Then the summary lists gate B under a skipped outcome, separately from the gates
    that passed, and repeats the reason it did not run,
    And gate B is not included in any count or statement that all requested gates
      passed,
    And the requirement that gate B must pass is reported as UNMET, not as satisfied,
    And the work item is assessed as NOT done, because `skipped` is not one of the
      outcomes that make a requested gate done-eligible,
    And the work item becomes done-eligible only when gate B is run and passes, or
      when gate B is explicitly re-classified as not required for this work item —
      never as a consequence of the skip itself,
    And a work item whose only non-passing gate is a skipped one is distinguishable,
      from the summary alone, from a work item on which every requested gate ran and
      passed.
depends_on:
  - BO-2700f
  - BO-2700f-1
doc_links:
  - path: templates/skills/signoff/SKILL.md
    relationship: describes
    status: exists
    relevance: "Defines done-eligibility as every agents: entry being in {not_needed, signed_off}. `skipped` is in neither set, which is the mechanical basis for the not-done clause here."
  - path: templates/skills/building-epics/SKILL.md
    relationship: describes
    status: exists
    relevance: "Where a supervisor reads phase outcomes to decide whether a work item may proceed."
assigned_agent: null
estimated_complexity: null
it_requirements: null
delivers_to: null
expects_from: null
origin_agent: BrainCandy
created: 2026-08-13
amended_by:
  - "2026-08-13 expert-review remediation: resolved the blocking question the notes had left open, which contradicted the UNMET clause and left done-ness under-determined against the signoff {not_needed, signed_off} rule."
superseded_by: null
covered_by: []
implemented_by: []
change_target: pipeline
risk_surface: internal
notes: |
  Emitting the record is necessary but not sufficient. A `skipped` entry that any
  downstream roll-up folds into "all gates green" reproduces the original defect one
  layer up — the artifact exists but still reads as a pass. This AC is the consumer
  side of BO-2700f-1's producer side, and it is the clause that makes the L1 title
  literally true.

  Testable discriminator for the implementer: two runs whose only difference is that
  one gate passed versus was skipped must produce summaries that differ observably.
  If they do not, the guarantee has not landed regardless of what the record file
  contains.

  RESOLVED 2026-08-13 — DOES A SKIPPED GATE BLOCK? YES. The earlier draft said this
  was "deliberately NOT specified", while the criteria simultaneously required the
  pass-requirement to be reported UNMET; against the signoff skill's done rule —
  done-eligible iff every entry is in `{not_needed, signed_off}` — an implementer
  could not tell whether an UNMET requirement stops the work item or merely
  annotates it. The answer is that it stops it: `skipped` is in neither member of
  the done-eligible set, so a requested gate recorded as skipped leaves the work
  item not done, and the ONLY ways out are running the gate to a pass or an
  explicit re-classification (the supervisor's `not_needed` path, which requires
  user approval). The skip can never clear itself. Per-gate policy still governs
  WHETHER a gate carries a pass-requirement at all — that stays with the gate's own
  requirement — but once it does, this AC fixes the consequence.
