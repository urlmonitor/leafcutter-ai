---
ac_traceability:
  id: BO-1000c-2-i
  path: docs/acceptance-criteria/build-orchestration/BO-1000-live-automation-progress/BO-1000c-2-i.yaml
advances_current_outcome: true
agents:
  commit: signed_off
  llm-expert: signed_off
  pr-reviewer: signed_off
  pull-request: needed
change_target: pipeline
complexity: low
components:
- build_orchestration
created: '2026-07-20'
depends_on:
- 14_TICKET-20260720-BO-1000c-2.md
files_touched:
- templates/commands/finalize-feature.md
priority: medium
requires_adr: false
requires_diagram: false
risk_surface: internal
roadmap_phase: phase_1
source_ac: BO-1000c-2-i
status: todo
title: On a mid-flight halt the last conversation line reflects the halting step,
  live
---

# On a mid-flight halt the last conversation line reflects the halting step, live

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BO-1000c-2-i` — On a mid-flight halt the last conversation line reflects the halting step, live — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BO-1000c-2-i`. Component: `build-orchestration`. Assigned agent: `llm-expert`. Estimated complexity: `S`. Complexity: `low`.

## Acceptance Criteria

```gherkin
Given the background finalize run halts partway through the sequence,
When the user reads the main conversation,
Then the most recent progress line surfaced into the conversation reflects the
  step that was underway when the run halted,
And the user learns where the run stopped from the live conversation stream,
  not only from the terminal halt result returned at the end.
```

- [ ] AC-1: the most recent progress line surfaced into the conversation reflects the
- [ ] AC-2: the user learns where the run stopped from the live conversation stream

## Implementation Notes

```yaml
- 'Surface: the /finalize-feature launch contract (templates/commands/finalize-feature.md
  + main-loop poll/relay instruction) — llm-expert, same relay surface as BO-1000c-2.'
- The relay must surface the halting step's line into the conversation from the journal
  BEFORE (or independently of) the terminal halt result, so the last conversation
  line reflects the step that was underway at the halt — the user learns where it
  stopped from the live stream, not only the returned halt payload.
- Relies on BO-1000a-1-i (the halting step's start-of-step line is emitted before
  the failure) and BO-1000c-1a (that line reaches the journal); the relay must flush
  any remaining unrelayed journal lines when it detects the run has halted, so the
  halting-step line is not dropped.
- Keep the /finalize-feature command doc and its deployed copy in parity via build.py
  (ADR-001).
```

## Agent Contracts

### Expects From

- **AC:** BO-1000c-1a
- **Contract:** The run-progress journal (from BO-1000c-1a), including the halting step's start-of-step line (guaranteed emitted before failure by BO-1000a-1-i), which the relay surfaces so the last conversation line reflects the halting step.

## Sign-offs

- [x] pr-reviewer — 2026-07-23 09:35
- [x] llm-expert — 2026-07-23 09:18
- [x] commit — 2026-07-23 10:10
- [ ] pull-request

## Comments

### 2026-07-23 09:18 — llm-expert (status: ok)
feedback-id: fb_2026-07-23_28d02440
completion_manifest:
  template_written: true
  prompt_quality_checklist_passed: true
  convention_violations_resolved: true
Added `### Halt-Flush Protocol` subsection to `templates/commands/finalize-feature.md` specifying: (1) a final journal read must be performed immediately on halt detection, before the halt payload is surfaced; (2) all remaining unrelayed journal lines must be emitted first; (3) the halt summary is presented only after the flush. Also added a 4th invariant to the Over-Time Delivery Guarantee section formalising this as the observable contract for AC BO-1000c-2-i. Note: the pre-existing `depends_on: BO-1000c-2` field in this ticket triggers the `ticket_frontmatter_guard.py` hook on every edit (PostToolUse — edits succeed); the referenced file `14_TICKET-20260720-BO-1000c-2.md` exists in the epic folder but the hook uses exact-stem matching. This is a pre-existing ticket authoring issue, not introduced by this phase.

### 2026-07-23 09:35 — pr-reviewer (status: ok)
feedback-id: fb_2026-07-23_4c82197f
completion_manifest:
  ac1_covered: true
  ac2_covered: true
  cross_file_contract_verified: true
  high_confidence_blockers: true
No high-confidence findings. Diff is documentation/spec-only (templates/commands/finalize-feature.md). Cross-file contract tracing confirmed: BO-1000a-1-i guarantees start-of-step line emitted before failure, BO-1000c-1a guarantees that line reaches the journal — both consistent with what the Halt-Flush Protocol asserts. Both ticket ACs are addressed by the new spec. Three medium findings surfaced (not escalated, count ≤ 3 threshold): [M-1] Halt-Flush Protocol header uses "halt or error payload" but invariant 4 and remainder of spec say only "halt" — terminology inconsistency may cause implementers to apply the flush on one case but not the other. [M-2] Step 3 says "most recent conversation line" is the "start-of-step" line, but if the halting step emits further journal lines after its start-of-step, the most recent line would be that later line, not the start-of-step line — wording is misleading for multi-line steps. [M-3] No explicit fallback specified when the final journal read during halt-flush itself fails, unlike the Graceful Degradation section covering polling-time absence. Pre-existing ticket issue: depends_on references BO-1000c-2 by stem without the numeric prefix; hook fires on every edit but edits succeed (noted by llm-expert).

### 2026-07-23 10:10 — commit (status: ok)
feedback-id: fb_2026-07-23_b2cc6f74
completion_manifest:
  pre_commit_hooks_pass: true
  commit_message_valid: true
  ticket_staged: true
Auto-authorized commit gate: subject "feat(finalize): add halt-flush protocol to finalize-feature launcher (BO-1000c-2-i)"; staged files: templates/commands/finalize-feature.md, tickets/00_inbox/epics/EPIC-InFlightVisibility/15_TICKET-20260720-BO-1000c-2-i.md.
