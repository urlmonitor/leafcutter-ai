---
ac_coverage: 4/4
ac_traceability:
  id: ACD-2100b-3
  path: docs/acceptance-criteria/ac-driven-dev/ACD-2100-entry-point-unblocked/ACD-2100b-3.yaml
advances_current_outcome: true
agents:
  ac-fulfillment-gate: signed_off
  ac-validator: signed_off
  commit: signed_off
  documentation-expert: not_needed
  documentation-verifier: not_needed
  pr-reviewer: signed_off
  pull-request: not_needed
  python-coder: signed_off
  test-runner: signed_off
  test-writer: signed_off
change_target: pipeline
complexity: medium
components:
- ac_driven_dev
- agent_registry
created: '2026-08-26'
depends_on: []
documentation_required: false
files_touched:
- templates/workflows-js/plan-feature.js
priority: medium
requires_adr: false
requires_diagram: false
risk_surface: internal
roadmap_phase: phase_1
source_ac: ACD-2100b-3
status: done
title: An agent missing from the registry and an agent denied permission produce different
  reports
---

# An agent missing from the registry and an agent denied permission produce different reports

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `ACD-2100b-3` — An agent missing from the registry and an agent denied permission produce different reports — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `ACD-2100b-3`. Component: `ac-driven-dev`. Assigned agent: `python-coder`. Estimated complexity: `M`. Complexity: `medium`.

## Acceptance Criteria

```gherkin
Given two runs that differ only in their agent registry -- the first registry is
  readable and interpretable and does not list the agent the route would use to
  create the authoring worktree; the second lists that same agent and withholds
  permission to run repository commands from it,
When the startup check runs in each,
Then the first run stops with a report that names the agent and states it was not
  found in the registry,
And the second run stops with a report that states the agent is listed and is not
  permitted to run repository commands,
And the two reports differ from each other,
And only the second report directs the reader at the permission setting to
  change.
```

- [x] AC-1: the first run stops with a report that names the agent and states it was not
- [x] AC-2: the second run stops with a report that states the agent is listed and is not
- [x] AC-3: the two reports differ from each other
- [x] AC-4: only the second report directs the reader at the permission setting to

## Test Requirements

```yaml
tests:
- name: test_absent_agent_and_denied_agent_produce_different_reports
  file: unit_tests/workflows/test_acd_2100b_3.py
  covers:
  - ACD-2100b-3
  asserts: Two runs differing only in their registry -- one omitting the worktree-creation
    agent, one listing it with repository-command permission withheld -- produce reports
    that differ from each other.
  framework: unittest
  type: integration
  angle: criterion
- name: test_absent_agent_report_names_the_agent_and_says_it_was_not_found
  file: unit_tests/workflows/test_acd_2100b_3.py
  covers:
  - ACD-2100b-3
  asserts: The first run's report contains the agent id the check resolved and states
    the agent was not found in the registry, and it does not name a permission setting.
  framework: unittest
  type: integration
  angle: criterion
- name: test_only_the_denied_report_names_the_permission_setting
  file: unit_tests/workflows/test_acd_2100b_3.py
  covers:
  - ACD-2100b-3
  asserts: The second run's report states the agent is listed and not permitted and
    names the permission setting; the first run's report names no permission setting
    -- asserted as a pair, because either half alone passes with one shared message.
  framework: unittest
  type: integration
  angle: criterion
- name: test_both_outcomes_are_reached_through_the_workflow_entry_point
  file: unit_tests/workflows/test_acd_2100b_3.py
  covers:
  - ACD-2100b-3
  asserts: Both reports are produced by driving the real workflow with the two registries
    and observing the recorded outcome, not by calling the lookup directly.
  framework: unittest
  type: integration
  angle: reachability
```

## Implementation Notes

```yaml
- The change must land under templates/. The deployed workflow is build output.
- 'Fail closed in both cases: both runs stop. This L1 refines the reporting of the
  gate introduced by BO-1500f-1 (work_status: done) and must not supersede it -- a
  child that relaxes either halt to a warning is the wrong fix.'
- Only the denial outcome may name a permission setting. Naming it on the absence
  outcome sends the operator to audit an agent entry that does not exist and is the
  defect this record removes.
- Both reports must name the agent the check was looking for, taken from the value
  the check actually resolved rather than a literal in the message, so a change to
  which agent creates the worktree cannot make the report lie.
- The distinction must be established from the registry contents -- 'entry present'
  versus 'entry present and permission withheld' -- and not from a default applied
  when the lookup returns nothing.
- Must satisfy the repository error-handling policy.
- The denial case does not exist in the real registry (the worktree-creation agent
  carries permission today), so it must be constructible from a registry supplied
  to the run rather than requiring the project's own file to be edited.
```

## Agent Contracts

### documentation-expert

Existing docs to update / cross-link:

- docs/reference/plan-feature-layout-and-startup-checks.md (relationship: describes | status: planned | relevance: The reference page authored by ACD-2100d-4 must state the difference between these two reports -- it is the distinction the current message destroys.)
- docs/known-issues/ac-driven-dev.md (relationship: describes | status: exists | relevance: KI-ACD-009 -- 'permitsShell is false for four different reasons ... and only the last is a permissions problem'.)
- templates/workflows-js/plan-feature.js (relationship: implements | status: exists | relevance: Canonical source. Lines 1745-1791; the entries.find lookup is where absence and denial become indistinguishable.)
- docs/reference/false-green-mechanisms.md (relationship: describes | status: exists | relevance: M8 inverted -- the only one of the four outcomes that may assert a permissions cause is the one that established it.)
- docs/architecture/components/agent-registry.md (relationship: describes | status: exists | relevance: Component doc for the registry and the permits_shell field the second report may name.)

This record owns none of the documents above -- every entry is a cross-link to a
document another record produces. The contract line that stood here demanded
`docs/reference/plan-feature-layout-and-startup-checks.md`, which ACD-2100d-4
creates; this ticket could never have satisfied it. Removed per BO-2200c-5-i
and -iii; the list above is retained as reading for the implementer.

Note the `### Delivers To` entry below addressed to documentation-expert: this
record SUPPLIES the wording distinction that ACD-2100d-4's page states. That is
a contract with a downstream record, not a demand that this ticket write
documentation.

### Delivers To

- **Agent:** python-coder
- **Contract:** A lookup over the registry's agent entries whose result distinguishes three states -- entry present and permitted, entry present and denied, entry absent -- so that a fourth state (the entries collection is not there at all) is representable rather than silently folded into 'absent'.
- **Agent:** python-coder
- **Contract:** The absence outcome and the denial outcome as two distinct recorded halts, both of which the fail-closed guard must observe stopping the run.
- **Agent:** documentation-expert
- **Contract:** The final wording distinction between the absence report and the denial report, which the reference page states so a reader can map a message they received to a remedy.

## Sign-offs

- [x] test-writer — 2026-09-01 11:50
- [x] python-coder — 2026-09-01 12:30
- [x] test-runner — 2026-09-01 12:45
- [x] pr-reviewer — 2026-09-01 13:00
- [x] ac-validator — 2026-09-01 13:15
- [x] ac-fulfillment-gate — 2026-09-01 13:30
- [x] commit — 2026-09-01 14:00

## Comments

### 2026-09-01 11:50 — test-writer (status: ok)
feedback-id: fb_2026-09-01_868cd2e6
completion_manifest:
  tests_written: true
  red_baseline_confirmed: true
  cross_layer_seam_answer:
    result: covered
    producing_side: "the real templates/workflows-js/plan-feature.js Pre-Stage-0 registry-permission gate, driven end-to-end via a real Node child_process against a real on-disk agent_registry.json"
    consuming_side: "the run's own returned {status, message} result and its dispatched status-checker report text, which the test asserts an operator would read to distinguish absent-from-denied"
Wrote unit_tests/workflows/test_acd_2100b_3.py (4 tests, unittest, framework/target per the ticket's Test Requirements table) covering AC-1 through AC-4: an absent-registry-entry run and a denied-permission run must produce different reports, the absent report must name the agent and say "not found in the registry" with no permission-setting marker, the denied report must say the agent is listed/not-permitted and name the permits_shell setting, and both outcomes must be reached and consumed through the real plan-feature.js entry point (self-contained real-Node-subprocess harness copied from test_acd_2100b_2.py's convention, never a mocked agent()). Ran the file under `AC_ENFORCE_STRICT=1 python -m pytest` (the store's own pytest_ac_enforcement plugin otherwise downgrades not-done-AC failures to xfail) and confirmed all 4 tests genuinely FAIL today: the current `entries.find(...)` lookup collapses "entry absent" and "entry present with permits_shell: false" into the identical generic "does not permit running repository-mutating shell commands ... Fix ... permits_shell field" message, which is exactly the bug this AC exists to fix. red_baseline entries below.
red_baseline:
  - test_name: test_absent_agent_and_denied_agent_produce_different_reports
    file: unit_tests/workflows/test_acd_2100b_3.py
    error: "AssertionError: the absent-agent report is identical to the denied-agent report -- 'the agent is not in the registry' and 'the agent is in the registry but denied' must produce distinguishable reports."
  - test_name: test_absent_agent_report_names_the_agent_and_says_it_was_not_found
    file: unit_tests/workflows/test_acd_2100b_3.py
    error: "AssertionError: False is not true : The absent-agent report does not state that the agent was not found in the registry."
  - test_name: test_only_the_denied_report_names_the_permission_setting
    file: unit_tests/workflows/test_acd_2100b_3.py
    error: "AssertionError: False is not true : The denied-agent report does not state that the agent is listed in the registry."
  - test_name: test_both_outcomes_are_reached_through_the_workflow_entry_point
    file: unit_tests/workflows/test_acd_2100b_3.py
    error: "AssertionError: the absent-agent and denied-agent runs' own returned results are identical (both render the shared permission-mis-assignment message)."

### 2026-09-01 12:30 — python-coder (status: ok)
feedback-id: fb_2026-09-01_e615dfc2
completion_manifest:
  three_state_lookup_implemented: true
  absent_and_denied_render_distinct_halts: true
  fail_closed_preserved_on_both_halts: true
  tests_green: true
  regression_suite_green: true
red_baseline_results:
  - test_name: test_absent_agent_and_denied_agent_produce_different_reports
    result: green
  - test_name: test_absent_agent_report_names_the_agent_and_says_it_was_not_found
    result: green
  - test_name: test_only_the_denied_report_names_the_permission_setting
    result: green
  - test_name: test_both_outcomes_are_reached_through_the_workflow_entry_point
    result: green
Replaced the single `entries.find(...)` boolean collapse in
templates/workflows-js/plan-feature.js's Pre-Stage-0 Workspace-Setup Dispatch
Permission Gate with a new `_resolveWorkspaceSetupAgentEntryState(registryJson,
agentId)` helper that returns one of four distinct states -- "permitted",
"denied" (entry present, permits_shell !== true), "absent" (entries array
present but no matching id), and "no_entries_collection" (agents is not an
array at all) -- so the fourth state is representable rather than silently
folded into "absent" per the Delivers-To contract. The `if (!permitsShell)`
halt now branches on `.state === "denied"` to render two distinct, WARNING-
logged reports: the denied report names the agent (from the resolved
`workspaceSetupAgentId`, never a literal), states it "is listed in the agent
registry" and "denies"/"is not permitted", and names the `permits_shell`
field; the absent/no-entries report states the agent "was not found in the
registry" and names no permission setting. Both halts still fail closed
identically (same early `return { status: "error", ... }`), so this only
refines BO-1500f-1's reporting, never relaxes the gate. Ran the full
unit_tests/workflows/ suite (530 passed) plus a targeted run of
test_acd_2100b_3.py, test_acd_2100b_1.py, test_acd_2100b_2.py,
test_acd_2100a_3.py, and test_bo_1500f_1.py under AC_ENFORCE_STRICT=1 -- all
green, including the pre-existing test_mis_assignment_halt_names_both_the_step_and_the_agent
and test_permitted_agent_is_resolved_from_the_registry_not_a_hardcoded_name
tests that exercise the "entry present, denied" path against the real and
mocked registries. This is a .js file under templates/, so the Python
doc-enforcer/complexity-reduction skills do not apply; no .py production
files were touched. Handing to test-runner next.

### 2026-09-01 12:45 — test-runner (status: ok)
feedback-id: fb_2026-09-01_6adb0329
completion_manifest:
  test_suite_executed: true
  all_tests_passing: true
  failure_report_structured: true
Ran `AC_ENFORCE_STRICT=1 python3 -m pytest unit_tests/workflows/test_acd_2100b_3.py -v` — all 4 tests
(test_absent_agent_and_denied_agent_produce_different_reports,
test_absent_agent_report_names_the_agent_and_says_it_was_not_found,
test_only_the_denied_report_names_the_permission_setting,
test_both_outcomes_are_reached_through_the_workflow_entry_point) PASSED. Also ran the
full `AC_ENFORCE_STRICT=1 python3 -m pytest unit_tests/workflows/ -q` regression suite:
530 passed, 1 pre-existing unrelated warning, 15 subtests passed, 0 failures — confirming
python-coder's claimed regression-suite result independently. No blockers; handing to
pr-reviewer next.

### 2026-09-01 13:00 — pr-reviewer (status: ok)
feedback-id: fb_2026-09-01_09b8d329
completion_manifest:
  contract_validation_pass: true
  tests_independently_reproduced: true
  regression_suite_independently_reproduced: true
  high_confidence_findings: 0
  medium_confidence_findings: 0
Reviewed the staged diff to templates/workflows-js/plan-feature.js (the new
`_resolveWorkspaceSetupAgentEntryState` helper plus the two-branch halt at the
`!permitsShell` gate) against ACD-2100b-3's ticket-body ACs and both
Delivers-To contracts to python-coder: the lookup now returns four distinct,
representable states (`permitted`/`denied`/`absent`/`no_entries_collection`,
never folding the fourth into `absent`), and the absent vs. denied outcomes
render as two separately-labelled `agent()` dispatches and two distinct
`{status:"error", ...}` returns, both still failing closed identically before
any authoring agent is dispatched — matching Implementation Notes ("must not
supersede" BO-1500f-1's halt). Only the denied-report branch names
`permits_shell`; the absent/no-entries branch names no permission setting,
per the "does not exist, so no permission fact was established" note.
Independently re-ran `unit_tests/workflows/test_acd_2100b_3.py` (4/4 passed)
and the full `unit_tests/workflows/` suite (530 passed, same 1 pre-existing
warning) — both reproduce test-runner's reported results exactly. No dead
code or unused variables from the new helper/state variable. No high- or
medium-confidence findings; the ticket has no `### pr-reviewer` block under
`## Agent Contracts` and no `## AC Coverage` table, so §2c.2/§2c.3 do not
apply. Handing to ac-validator next.

### 2026-09-01 13:15 — ac-validator (status: ok)
feedback-id: (submit-failed)
completion_manifest:
  all_acs_have_implementation_evidence: true
  all_acs_have_test_evidence: true
  ac_store_alignment_check_passed: true
All 4 ACs covered by both implementation and test evidence; store-alignment
check exits 0 (no violations). AC-1 covered: implementation is the
`notFoundMessage` branch in `templates/workflows-js/plan-feature.js` (new
`if (!permitsShell)` block, "not found" path) which names
`workspaceSetupAgentId` and states "was not found in the registry"; test is
`test_absent_agent_report_names_the_agent_and_says_it_was_not_found`
(unit_tests/workflows/test_acd_2100b_3.py). AC-2 covered: implementation is
the `agentIsListedAndDenied` branch's `deniedMessage`, which states the agent
"is listed in the agent registry ... but is not permitted to run
repository-mutating shell commands"; test is
`test_only_the_denied_report_names_the_permission_setting`. AC-3 covered:
implementation is the new `_resolveWorkspaceSetupAgentEntryState(registryJson,
agentId)` helper (plan-feature.js, ~line 1958) that replaces the single
`entries.find(...)` collapse with two distinct branches/messages; test is
`test_absent_agent_and_denied_agent_produce_different_reports`. AC-4 covered:
implementation reserves `permits_shell` naming to the denied branch only (the
not-found branch's return message never mentions `permits_shell`); test is
`test_only_the_denied_report_names_the_permission_setting` (asserts the pair).
Independently reran `unit_tests/workflows/test_acd_2100b_3.py` — 4/4 passed.
Ran `check_v2_ac_store_alignment.py --ticket <this ticket>` — exit 0, no
ERROR lines. Ticket has no `### ac-validator` heading under `## Agent
Contracts` and no `## AC Coverage` table, so §2c.2/§2c.3 do not apply per the
signoff skill's v1/v2 detection rule (no per-agent AC block to flip).
`submit_feedback.py` rejected `ac-validator` as an allowed writer for every
category tried (not in the `feedback_categories.yaml` allowed-writers list for
`complete`), so feedback-id is `(submit-failed)` per §2a fallback — not a
phase failure. Flipped ticket-body AC-1..AC-4 checkboxes to `[x]` and set
`ac_coverage: 4/4` in frontmatter. Handing to ac-fulfillment-gate next.

### 2026-09-01 13:30 — ac-fulfillment-gate (status: ok)
feedback-id: (submit-failed)
completion_manifest:
  ac_store_fields_verified: true
  auto_fixes_applied: true
  schema_validation_passed: true
Resolved ACD-2100b-3 via `.leafcutter/scripts/ac_store/ac_coverage_resolver.py`
(two-key `{id, path}` traceability block; 1 AC resolved). The AC YAML
(`docs/acceptance-criteria/ac-driven-dev/ACD-2100-entry-point-unblocked/ACD-2100b-3.yaml`,
level L2) had three stale fields relative to the diff: `work_status: todo`
though `templates/workflows-js/plan-feature.js` (this ticket's sole
`files_touched` entry) is present in `git diff HEAD`; `implemented_by`
contained only the ticket's own `.md` path, with no intersecting
`files_touched` file; and `covered_by` contained only the sibling AC id
`ACD-2100b-3-i` with no real test-file path, though
`unit_tests/workflows/test_acd_2100b_3.py` carries four `# covers:
ACD-2100b-3` tags (confirmed via grep across `tests/` and `unit_tests/`).
Applied three append-only auto-fixes: `work_status` -> `done`;
`implemented_by` gained `templates/workflows-js/plan-feature.js`;
`covered_by` gained `unit_tests/workflows/test_acd_2100b_3.py` — no existing
entries removed. Ran `scripts/commit_guardian/check_ac_schema.py` on the
edited file post-fix: exit 0 (fell back to manual field validation with a
warning, `config/ac_store_schema.json` absent — not a failure). Re-ran the
resolver after the edits: `{"ok": true, "verified_count": 1, "failures":
[]}`. Ticket has no `### ac-fulfillment-gate` heading under `## Agent
Contracts`, so §2c.2/§2c.3 do not apply. `submit_feedback.py` rejected
`ac-fulfillment-gate` as an allowed writer for the `complete` category (same
allowed-writers gap ac-validator hit above), so feedback-id is
`(submit-failed)` per §2a fallback — not a phase failure. AC store
fulfillment complete; commit phase may proceed.

### 2026-09-01 14:00 — commit (status: ok)
feedback-id: fb_2026-09-01_14f93d6a
completion_manifest:
  pre_commit_hooks_pass: true
  commit_message_valid: true
  ticket_staged: true
Auto-authorized commit gate (ticket_path supplied; no reply channel exists
mid-drive, per commit-agent §Step 3 supervised path): subject
"fix(ac-driven-dev): distinguish an absent registry entry from a denied one
in the halt report (ACD-2100b-3)"; staged files:
docs/acceptance-criteria/ac-driven-dev/ACD-2100-entry-point-unblocked/ACD-2100b-3.yaml,
templates/workflows-js/plan-feature.js,
tickets/00_inbox/epics/EPIC-StartingNewWorkTheProperWayAlways/09_TICKET-20260826-ACD-2100b-3.md,
unit_tests/workflows/test_acd_2100b_3.py. Pre-commit hook probe
(`verify_precommit_active.py`) initially reported `git_hook: false` because
the shell tool's default cwd (`/home/henzeh/projects/leafcutter`, no `.git`)
was outside the worktree; re-ran with `env --chdir=<worktree-root>` and got
`failing_checks: []` — a cwd artifact, not a real hook gap. The
`commit_classifier.classify_staged_files` mixed-set check flagged
implementation+test+ticket+AC-yaml as "unrelated groups", but `git log`
confirms this exact bundle is the repo's established convention for an
AC-completion commit (see 484efdfe7, ccc88542d) — proceeded as intentional,
not a defect. `submit_feedback.py --category complete` succeeded for
`commit` (feedback-id above) — unlike the `(submit-failed)` results the
upstream ac-validator/ac-fulfillment-gate phases hit for their own
categories.
