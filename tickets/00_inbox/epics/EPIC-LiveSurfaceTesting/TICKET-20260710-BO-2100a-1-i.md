---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  llm-expert: needed
  pr-reviewer: needed
  pull-request: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
complexity: low
components:
- build-orchestration
created: '2026-07-10'
depends_on:
- BO-2100a-1
files_touched:
- templates/skills/ticket-authoring/SKILL.md
- docs/architecture/adrs/ADR-020-live-surface-tester.md
priority: medium
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: BO-2100a-1-i
status: todo
title: live-surface-tester is absent from the agents map unless both toggles are on
---

# live-surface-tester is absent from the agents map unless both toggles are on

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BO-2100a-1-i` — live-surface-tester is absent from the agents map unless both toggles are on — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BO-2100a-1-i`. Component: `build-orchestration`. Assigned agent: `llm-expert`. Estimated complexity: `S`. Complexity: `low`.

## Acceptance Criteria

```gherkin
Given a ticket authored via the ticket-authoring skill in each of these states:
  (a) frontmatter `live_surface_test: true` but `skills_config.json`
      `live_surface_testing.enabled` is `false`,
  (b) frontmatter `live_surface_test` false or absent while
      `live_surface_testing.enabled` is `true`,
  (c) neither toggle set,
When the business-analyst produces the ticket's `agents:` map
Then the map contains NO `live-surface-tester` key in any of states (a), (b), or (c)
  And the key appears ONLY when the feature toggle and the project toggle are both true.
```

## Implementation Notes

```yaml
- 'The default must be fail-safe: when either toggle is false or absent, the phase
  must NOT be added.'
- Coverage must exercise all three negative states (a), (b), (c) plus the positive
  both-on case, so no single-toggle path can silently enable the phase.
```

## Sign-offs

- [ ] llm-expert
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit
- [ ] pull-request

## Comments
