# Changelog

## v0.0.1 — initial extraction from bybit-trader

First standalone release. Extracted from the bybit-trader monorepo.
Internal package name: `leafcutter`. GitHub repo: `https://github.com/urlmonitor/leafcutter-ai`.

> Note: the per-file changelog system (`scripts/changelog/emit_entry.py`) is the
> long-term source of truth for detailed change entries. This root `CHANGELOG.md`
> is a human-readable summary entry-point for the v0.0.1 release.
