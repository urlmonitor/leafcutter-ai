---
ac_traceability:
  id: GE-127e-3-i
  path: docs/acceptance-criteria/guardrail-engine/GE-127-files-stay-workable/GE-127e-3-i.yaml
advances_current_outcome: true
agents:
  ac-fulfillment-gate: needed
  ac-validator: needed
  architect-review: needed
  commit: needed
  documentation-expert: needed
  documentation-verifier: needed
  frontend-coder: not_needed
  pr-reviewer: needed
  pull-request: not_needed
  python-coder: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
  user-surface-smoker: not_needed
change_target: code
complexity: low
components:
- commit_guardian
created: '2026-09-14'
declares_side_effect: false
depends_on:
- 04_TICKET-20260914-GE-127e-3.md
documentation_required: true
files_touched:
- scripts/build.py
- templates/commit-guardian/commit_guardian.json
- templates/scripts/commit_guardian/check_file_size.py
priority: medium
requires_adr: false
requires_diagram: false
risk_surface: contract_boundary
roadmap_phase: phase_1
source_ac: GE-127e-3-i
status: todo
title: Guidance that could not be produced is said so plainly, and whether guidance
  exists never moves the commit verdict in either direction
---

# Guidance that could not be produced is said so plainly, and whether guidance exists never moves the commit verdict in either direction

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `GE-127e-3-i` — Guidance that could not be produced is said so plainly, and whether guidance exists never moves the commit verdict in either direction — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `GE-127e-3-i`. Component: `guardrail-engine`. Assigned agent: `python-coder`. Estimated complexity: `S`. Complexity: `low`.

## Acceptance Criteria

```gherkin
Given a tracked file of a kind the size standard covers stands above its
  permitted length, and no description of what is inside it can be produced —
  its content cannot be resolved into the parts the description is built from,
When the author performs an ordinary commit,
Then the commit does not complete, exactly as it would have had the description
  been produced,
And the outcome states that no description of that file's contents could be
  produced and names the reason,
And the outcome offers no parts, no division, and no substitute wording that
  reads as advice about that file,
And given a tracked file of a covered kind stands BELOW its permitted length
  and a description of what is inside it could be produced,
When the author performs an ordinary commit,
Then the commit completes and nothing about that file is reported — a file is
  never refused because guidance about it could be produced, only because of
  its length,
And given the same undescribable file is then brought below its permitted
  length while remaining undescribable,
When the author performs an ordinary commit,
Then the commit completes and no could-not-be-described statement is made about
  it — the statement is attached to a refusal, and is not a finding of its own.
```

- [ ] AC-1: the commit does not complete, exactly as it would have had the description
- [ ] AC-2: the outcome states that no description of that file's contents could be
- [ ] AC-3: the outcome offers no parts, no division, and no substitute wording that
- [ ] AC-4: given a tracked file of a covered kind stands BELOW its permitted length
- [ ] AC-5: a description of what is inside it could be produced
- [ ] AC-6: the commit completes and nothing about that file is reported — a file is
- [ ] AC-7: given the same undescribable file is then brought below its permitted
- [ ] AC-8: the commit completes and no could-not-be-described statement is made about

## Test Requirements

```yaml
tests:
- name: test_ge_127e_3_i_an_undescribable_over_limit_file_is_still_refused
  file: unit_tests/commit_guardian/test_ge_127e_3_i.py
  covers:
  - GE-127e-3-i
  asserts: 'Stage a covered file that stands above its permitted length and whose
    content is genuinely unresolvable into parts at run time, and perform an ordinary
    commit. The commit must not complete, exactly as it would have had the description
    been produced, and the exit status must be the ordinary finding status — not the
    indeterminate one. NAMED MUTATION (mandatory, run it, do not merely describe it
    — the BA''s injection 1): "the commit does not complete, exactly as it would have
    had the description been produced" — MUTATION: make the description step, on failing
    to resolve a file''s content into parts, yield an empty description and allow
    the run to continue to a clean result for that file. Under that injection the
    commit completes for a file that is over its permitted length; this clause must
    go RED, failing BY THE COMMIT OUTCOME as well as by the text, and must return
    to green on revert. This is the exact shape already recorded in this guard for
    the measurement step, and it is the reason this record exists rather than being
    trusted to the implementer.'
  framework: unittest
  type: behavioral
  angle: criterion
- name: test_ge_127e_3_i_the_outcome_states_that_no_description_could_be_produced_and_names_the_reason
  file: unit_tests/commit_guardian/test_ge_127e_3_i.py
  covers:
  - GE-127e-3-i
  asserts: The refusal carries a could-not-be-described line for that file, naming
    the specific reason — content that would not parse, a part set that could not
    account for the quoted length, or no extractor for the kind — rather than one
    shared sentence for every cause. Two runs exercising two different causes must
    produce two different reasons, which is what stops the line degrading into a second
    fixed sentence.
  framework: unittest
  type: behavioral
  angle: failure
- name: test_ge_127e_3_i_no_substitute_advice_stands_in_for_the_missing_description
  file: unit_tests/commit_guardian/test_ge_127e_3_i.py
  covers:
  - GE-127e-3-i
  asserts: 'The outcome offers no parts, no division, and no substitute wording that
    reads as advice about that file. NAMED MUTATION (mandatory, run it — the BA''s
    injection 2): "the outcome offers no parts, no division, and no substitute wording
    that reads as advice about that file" — MUTATION: when the description cannot
    be produced, fall back to a fixed sentence of general refactoring advice. Under
    that injection this clause must go RED, and must return to green on revert. Without
    it the clause is an absence and is green against any implementation that emits
    nothing. This injection is the anti-precedent in miniature: a placeholder standing
    in for a result, indistinguishable to the reader from the real thing.'
  framework: unittest
  type: behavioral
  angle: failure
- name: test_ge_127e_3_i_a_describable_file_under_its_limit_commits_and_is_not_refused_for_having_guidance
  file: unit_tests/commit_guardian/test_ge_127e_3_i.py
  covers:
  - GE-127e-3-i
  asserts: 'A covered file standing BELOW its permitted length, for which a description
    could be produced, commits cleanly with nothing about it reported. NAMED MUTATION
    (mandatory, run it — the BA''s injection 3): "a file is never refused because
    guidance about it could be produced, only because of its length" — MUTATION: refuse
    any file for which named parts could be produced, regardless of its measured length.
    Under that injection this arm must go RED, failing by the commit outcome, and
    must return to green on revert. Without it the arm is satisfied by a guard that
    refuses nothing at any commit, which is today''s implementation exactly.'
  framework: unittest
  type: behavioral
  angle: criterion
- name: test_ge_127e_3_i_an_undescribable_file_under_its_limit_produces_no_could_not_be_described_statement
  file: unit_tests/commit_guardian/test_ge_127e_3_i.py
  covers:
  - GE-127e-3-i
  asserts: THE ARM THAT STOPS THE FIX OVERSHOOTING. Take the same undescribable file,
    bring it below its permitted length while it remains undescribable, and commit.
    The commit completes and no could-not-be-described statement is made about it
    — the statement is an attachment to a refusal, not a finding of its own. Without
    this descriptor the cheap implementation reports the failure wherever it occurs
    and makes every commit touching an undescribable file noisy, breaking GE-127a-1's
    silence arm from a direction that record cannot see.
  framework: unittest
  type: behavioral
  angle: boundary
- name: test_ge_127e_3_i_the_could_not_be_described_state_does_not_take_the_indeterminate_exit
  file: unit_tests/commit_guardian/test_ge_127e_3_i.py
  covers:
  - GE-127e-3-i
  asserts: 'THE VOCABULARY DECISION MADE EXECUTABLE. Three runs, distinguished by
    exit status and by token: a covered file measured and found over but undescribable
    exits with the ordinary finding status carrying its refusal block and the could-not-be-described
    line; a covered file whose LENGTH cannot be established still takes GE-127a-1-i''s
    INDETERMINATE path at exit 2; a clean run exits 0. An author, and pre-commit,
    can tell the three apart without inspecting anything else. This is the descriptor
    that fails if someone later routes the description failure through the indeterminate
    path — which would move the verdict and collapse GE-127a-1-i''s third clause.'
  framework: unittest
  type: behavioral
  angle: seam
- name: test_ge_127e_3_i_the_could_not_be_described_line_reaches_the_registered_hook_output_and_status
  file: unit_tests/commit_guardian/test_ge_127e_3_i.py
  covers:
  - GE-127e-3-i
  asserts: PRODUCTION ENTRY POINT. Run the gate through the registered hook path with
    an undescribable over-limit file staged and assert the line and its named reason
    appear in the hook's own output stream, inside that file's refusal block, with
    the commit outcome carried by the exit status rather than an advisory note. A
    state printed by a function nothing at commit time calls is inert, and a line
    printed on a stream the hook does not surface is invisible to the author.
  framework: unittest
  type: integration
  angle: reachability
- name: test_ge_127e_3_i_the_deployed_copy_still_refuses_an_undescribable_over_limit_file_in_a_cold_process
  file: unit_tests/commit_guardian/test_ge_127e_3_i.py
  covers:
  - GE-127e-3-i
  asserts: After build.py, the DEPLOYED copy and every module it imports load and
    run in a cold process — a description module placed outside templates/scripts/commit_guardian/
    without a scripts/build_phases.py deploy-map entry surfaces here as ModuleNotFoundError
    rather than passing, and would in production be an import error at commit time
    whose handling could itself relax the verdict. With an undescribable over-limit
    file staged, the deployed run refuses the commit and emits the named line.
  framework: unittest
  type: integration
  angle: deployed
```

## Implementation Notes

```yaml
- 'NO PACKAGE SURFACE — MEASURED, NOT ASSUMED. WATCHED_REGISTRIES binds exactly two
  containers on commit_guardian.json: hooks_manifest.hooks (by entry id) and the document''s
  own top level (by mapping key). This record adds neither — it changes how one unproducible
  result is reported inside an existing gate. Both containers were read in this worktree:
  `check-file-size` is already a manifest id, `file_size` is already one of the 37
  top-level keys, and anything nested inside `file_size` is invisible to the watcher.
  Plain-list spec form, and no config_schema_fragment invented to satisfy the structured-spec
  validator — the BP-100k-4 / BP-100k-4-i fictional-key precedent is not repeated.'
- 'THE UNAVAILABILITY VOCABULARY IS REUSED, THE EXIT STATUS IS NOT — THIS IS THE DECISION
  THE BA DEFERRED AND IT IS TAKEN HERE, WITH ITS REASONING. GE-127a-1-i pinned BP-1600a-2-ii''s
  contract for a file whose LENGTH could not be established: an "INDETERMINATE: reason=<text>"
  line naming which situation occurred, at exit 2, alongside exit 0 clean and exit
  1 for a measured finding. That contract is reused HERE IN ITS LINE SHAPE AND ITS
  NAMING DISCIPLINE — a single machine-readable token, followed by `reason=` and a
  specific reason, naming the situation that actually occurred — and is NOT reused
  in its exit status. The substantive difference decides it: a failure to MEASURE
  leaves the verdict unknown, which is what exit 2 means; a failure to DESCRIBE leaves
  the verdict perfectly known — the file''s length was established and found over
  — and only the advice missing. This record positively requires the commit to be
  refused in that case, so the run must exit 1 with the ordinary refusal for that
  file, carrying the could-not-be-described line INSIDE that file''s refusal block
  on the same stream. Routing it to exit 2 would be a defect three times over: it
  would move the verdict, which the title of this record forbids; it would collapse
  the very distinction GE-127a-1-i''s third clause exists to hold, by making a known-over
  file report as indeterminate; and structurally the exit-2 paths in main() print
  to stderr and return immediately, suppressing the refusal block this line is required
  to be attached to.'
- 'THIS IS NOT A THIRD VERDICT SET, AND THE GUARD ALREADY DEMONSTRATES WHY. check_file_size
  already emits "EMPTY HISTORY: reason=<text>" — the same token-and-reason line shape
  as INDETERMINATE, deliberately named distinctly, and deliberately NOT carrying exit
  2, because an empty previous-length history is a legitimate completing result. The
  could-not-be-described state is the same construction applied to a different step:
  one shared line grammar, one naming discipline, and a status decided by the VERDICT
  rather than by the token. Choose a token in that grammar which cannot be misread
  as either of the existing two, and record it in the guard''s own module docstring
  alongside them.'
- NAME THE REASON SPECIFICALLY, AND DO NOT COLLAPSE DISTINCT CAUSES. "Could not be
  described" will have more than one cause — content that will not parse into parts,
  a part set that cannot account for half the quoted length (GE-127e-1 routes that
  state here), a kind for which no extractor exists. These are different facts about
  the world and the reason text must say which occurred, exactly as GE-127a-1-i requires
  the two measurement situations to be named separately. A single shared sentence
  for all causes costs the author the only information that tells a genuinely unparseable
  file from a gap in the gate.
- 'THE DESCRIPTION STEP MUST BE STRUCTURALLY INCAPABLE OF MOVING THE VERDICT. The
  verdict is computed from length alone and must be fixed BEFORE the description step
  runs, so no outcome of that step — success, failure, or an exception — can change
  whether the commit completes. Do not compute the verdict from anything the description
  produced, and do not let the description step raise into the classification loop.
  The recorded defect on this guard''s measurement step is the same shape one step
  earlier, and this one would be quieter: a caught exception in the description step
  turning a refusal into a completed commit silently undoes GE-127a and GE-127b for
  exactly the files hardest to describe, and no test in either of those records would
  notice, because their fixtures describe cleanly.'
- THE STATEMENT IS AN ATTACHMENT TO A REFUSAL, NEVER A FINDING OF ITS OWN. A file
  that is within its permitted length produces no could-not-be-described line even
  when it is undescribable, and no exit-status change. Gate the emission on the file
  already having been refused, not on the description step having been attempted —
  the cheap implementation reports the failure wherever it occurs and would break
  GE-127a-1's silence arm from a direction that record cannot see.
- 'Must follow the repo error-handling policy: the description step performs I/O and
  parsing, so specific-exception try/except, and log at WARNING or higher or re-raise.
  The re-raise-equivalent here is the could-not-be-described line itself. A swallowed
  error must never become an absent description that also relaxes the verdict, and
  must never become an empty parts list that reads to the author as "this file has
  no parts".'
- 'EXERCISE THE UNDESCRIBABLE STATE FOR REAL. Make the content genuinely unresolvable
  into parts at run time — a covered file whose bytes do not parse, or whose structure
  yields no locatable part — never by asserting that an error branch exists in the
  source. BP-100k-4-i''s standing constraint applies unchanged, and this record''s
  own notes say so: a source assertion stays green on precisely the swallow-and-continue
  implementation this record forbids.'
- CANONICAL VERSUS BUILD OUTPUT, AND THE BUILD IS PART OF THE CHANGE. Edit templates/scripts/commit_guardian/
  only; scripts/commit_guardian/ and the repo-root .pre-commit-config.yaml are untracked
  build outputs overwritten by build.py, and templates/commit-guardian/commit_guardian.json
  is a decoy. Re-run `python scripts/build.py --target-dir .` — the hook runs from
  the deployed layout, so an unbuilt failure path is unverified however green the
  source-tree tests are.
```

## Agent Contracts

### documentation-expert

Existing docs to update / cross-link:

- docs/acceptance-criteria/guardrail-engine/GE-127-files-stay-workable/GE-127e-3.yaml (relationship: parent | status: exists)
- docs/acceptance-criteria/guardrail-engine/GE-127-files-stay-workable/GE-127a-1-i.yaml (relationship: related | status: exists | relevance: THE ADJACENT FLOOR, AND THE ONE THIS RECORD MUST NOT DUPLICATE OR CONTRADICT. That record governs a file whose LENGTH cannot be established: it is refused, named, and never reported as within its permitted length. This record governs a file whose length WAS established and found over, but whose CONTENTS could not be described. The two are different failures of two different steps, and the second must not be allowed to reopen the first: an undescribable file whose length is known is still an ordinary length finding, and the description failure is reported alongside that finding rather than replacing it. Reuse whatever unavailability vocabulary that record's enrichment pins; do not mint a second one.)
- docs/acceptance-criteria/guardrail-engine/GE-127-files-stay-workable/GE-127e-1.yaml (relationship: related | status: exists | relevance: The description whose absence this record governs. GE-127e-1's no-division arm covers a file that was described successfully and has no seam between its parts; this covers a file that could not be described at all. Different states, and collapsing them costs the author the only signal that distinguishes a genuinely indivisible file from a step that failed.)
- templates/scripts/commit_guardian/check_file_size.py (relationship: modifies | status: exists | relevance: The host. Its recorded defect is a swallowed read error becoming a measured length of zero and a PASSED verdict. Adding a description step to this guard adds a second place with the same opportunity, and the failure would be quieter: a description step that raises and is caught could turn a refusal into a completed commit, silently undoing GE-127a and GE-127b for that file.)
- docs/known-issues/commit-guardian.md (relationship: context | status: exists | relevance: The register of this shape in this component — a check that examined inputs, concluded nothing, and exited clean. Read it before choosing how the description step reports its own failure.)
- docs/architecture/components/commit-guardian.md (relationship: describes | status: exists | relevance: IT PO. The commit-time invocation shape and the outcome stream the could-not-be-described statement must appear on.)
- docs/pre-commit-hooks.md (relationship: related | status: exists)
- CLAUDE.md (relationship: describes | status: exists | relevance: Error Handling Policy Rules 1-4. The description step performs I/O and parsing, so it must be wrapped with named exceptions and must log at WARNING or higher or re-raise; the re-raise-equivalent here is the could-not-be-described statement. A swallowed error must never become an absent description that also relaxes the verdict.)

- [ ] AC-1: (unspecified genre) | docs/acceptance-criteria/guardrail-engine/GE-127-files-stay-workable/GE-127e-3.yaml | the commit does not complete, exactly as it would have had the description

### Expects From

- **AC:** GE-127e-1
- **Contract:** The per-file description and the division built from it. This record adds the one input state for which they cannot be produced, and requires that state to be a named result carried on an unchanged refusal — never a silent omission, never a generic sentence standing in for the description, and never a change to whether the commit completes.

## Sign-offs

- [ ] architect-review
- [ ] test-writer
- [ ] python-coder
- [ ] test-runner
- [ ] documentation-expert
- [ ] pr-reviewer
- [ ] ac-validator
- [ ] ac-fulfillment-gate
- [ ] documentation-verifier
- [ ] commit

## Comments
