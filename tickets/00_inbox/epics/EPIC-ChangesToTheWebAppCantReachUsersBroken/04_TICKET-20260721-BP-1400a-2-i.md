---
ac_traceability:
  id: BP-1400a-2-i
  path: docs/acceptance-criteria/build_pipeline/BP-1400-web-app-ci-gate/BP-1400a-2-i.yaml
advances_current_outcome: true
agents:
  ac-fulfillment-gate: needed
  ac-validator: needed
  architect-review: needed
  commit: needed
  pr-reviewer: needed
  pull-request: not_needed
  python-coder: needed
  test-runner: needed
  test-writer: needed
change_target: pipeline
complexity: low
components:
- build_pipeline
created: '2026-07-21'
depends_on:
- 03_TICKET-20260721-BP-1400a-2.md
files_touched:
- .github/workflows/ci.yml
priority: high
requires_adr: false
requires_diagram: false
risk_surface: safety
roadmap_phase: phase_1
source_ac: BP-1400a-2-i
status: todo
title: A pull request that changes both a web-app file and a non-web-app file still
  requires the web-app check to pass
---

# A pull request that changes both a web-app file and a non-web-app file still requires the web-app check to pass

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BP-1400a-2-i` — A pull request that changes both a web-app file and a non-web-app file still requires the web-app check to pass — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BP-1400a-2-i`. Component: `build-pipeline`. Assigned agent: `python-coder`. Estimated complexity: `S`. Complexity: `low`.

## Acceptance Criteria

```gherkin
Given a pull request that changes at least one file under the web-app directory
  (leafcutter-web/) together with one or more files outside it,
When the checks run on that pull request,
Then the web-app build/type-check check runs and its result is required for the
  pull request to merge,
And a failing web-app check blocks the merge even though non-web-app files were
  also changed.
```

- [ ] AC-1: the web-app build/type-check check runs and its result is required for the
- [ ] AC-2: a failing web-app check blocks the merge even though non-web-app files were

## Test Requirements

```yaml
tests:
- name: test_web_check_runs_on_mixed_change
  file: unit_tests/build_pipeline/test_bp_1400a_2_i.py
  covers:
  - BP-1400a-2-i
  asserts: A changed-file set mixing a leafcutter-web/ file with a non-web-app file
    still triggers and requires the web-app check; a failing web-app check on that
    mixed set blocks the merge.
  framework: pytest
  type: behavioral
```

## Implementation Notes

```yaml
- 'Exercises the mixed-change boundary of the leafcutter-web/** paths filter from
  BP-1400a-2: a changed-file set containing at least one leafcutter-web/ file AND
  one or more non-web-app files must still TRIGGER and REQUIRE the web-app check —
  the trigger predicate is ''ANY changed path matches leafcutter-web/**'', not ''ALL
  changed paths match''.'
- A failing web-app check on such a mixed PR must block the merge; a web-app change
  must not slip through unchecked just because it is bundled with backend or docs
  changes.
- Verify against the ACTUAL paths-filter semantics chosen in BP-1400a-2 (GitHub `paths:`
  matches if any changed file matches) — do not assume; a mis-specified filter that
  requires all-paths-in-scope would silently skip mixed PRs.
```

## Agent Contracts

### Expects From

- **AC:** BP-1400a-2
- **Contract:** The path-scoped triggering rule whose mixed-change boundary this case exercises

## Sign-offs

- [ ] architect-review
- [ ] test-writer
- [ ] python-coder
- [ ] test-runner
- [ ] pr-reviewer
- [ ] ac-validator
- [ ] ac-fulfillment-gate
- [ ] commit

## Comments
