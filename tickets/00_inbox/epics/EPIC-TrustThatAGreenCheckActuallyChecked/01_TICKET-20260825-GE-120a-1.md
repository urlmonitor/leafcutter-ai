---
ac_traceability:
  id: GE-120a-1
  path: docs/acceptance-criteria/guardrail-engine/GE-120-green-means-checked/GE-120a-1.yaml
advances_current_outcome: true
agents:
  ac-fulfillment-gate: needed
  ac-validator: needed
  architect-review: signed_off
  commit: needed
  documentation-expert: needed
  documentation-verifier: needed
  pr-reviewer: needed
  pull-request: needed
  python-coder: signed_off
  test-runner: needed
  test-writer: signed_off
change_target: code
complexity: medium
components:
- commit_guardian
- precommit_hooks
created: '2026-08-25'
depends_on: []
documentation_required: true
files_touched:
- scripts/build.py
- scripts/build_phases.py
- scripts/commit_guardian
- templates/commit-guardian
- templates/scripts/commit_guardian
- templates/scripts/commit_guardian/_resolve_root.py
- templates/scripts/commit_guardian/check_ac_parent_covered_by.py
priority: high
requires_adr: false
requires_diagram: false
risk_surface: contract_boundary
roadmap_phase: phase_1
source_ac: GE-120a-1
status: todo
title: A check that could not perform its inspection reports a degraded outcome, not
  a clean pass
---

# A check that could not perform its inspection reports a degraded outcome, not a clean pass

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `GE-120a-1` — A check that could not perform its inspection reports a degraded outcome, not a clean pass — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `GE-120a-1`. Component: `guardrail-engine`. Assigned agent: `python-coder`. Estimated complexity: `M`. Complexity: `medium`.

## Acceptance Criteria

```gherkin
Given the AC-parent-covered-by pre-commit check is invoked on 6 staged AC YAML files
  that each violate the parent covered_by rule,
And the working copy it runs from does not expose the deployed layout, so the check
  cannot reach the parent-id derivation helper it needs to evaluate any of those files,
When the check finishes,
Then its output states that it did not inspect the staged files, names the prerequisite
  it could not reach, and names what was consequently left unverified — for example
  "parent covered_by links were not evaluated for 6 staged files",
And the outcome is emitted in a form a caller can distinguish from a clean run without
  reading prose: a distinct could-not-check result value, not the same success value a
  clean run produces,
And the behaviour observed today — a single stderr line reading "cannot import
  derive_parent_id ...; skipping check (fail-open)" followed by an ordinary success
  result — no longer satisfies the check's contract,
And this criterion does not by itself decide whether the commit is blocked; the
  per-check block-or-announce decision is governed by GE-120a-2.

Coverage note: this criterion is covered only by a test that EXECUTES the check under a
genuine cannot-reach condition and asserts on the observed result value and output text.
A test that searches the check's source for a warning string does not cover it — a
grep-only test passes on dead code (CLAUDE.md, "Gate / Workflow ACs — Verify
Behaviorally, Not by Grep").
```

- [ ] AC-1: the working copy it runs from does not expose the deployed layout, so the check
- [ ] AC-2: its output states that it did not inspect the staged files, names the prerequisite
- [ ] AC-3: the outcome is emitted in a form a caller can distinguish from a clean run without
- [ ] AC-4: the behaviour observed today — a single stderr line reading "cannot import
- [ ] AC-5: this criterion does not by itself decide whether the commit is blocked; the

## Test Requirements

```yaml
tests:
- name: test_ge120a1_cannot_run_emits_could_not_check_not_success
  file: unit_tests/portability/test_ge_120a_1.py
  covers:
  - GE-120a-1
  asserts: Deployed check run out of process from a working copy with no deployed
    layout, against 6 violating staged AC files, reports the could-not-check value
    and not the ordinary success value.
  framework: unittest
  type: behavioral
- name: test_ge120a1_output_names_prerequisite_and_unverified_scope
  file: unit_tests/portability/test_ge_120a_1.py
  covers:
  - GE-120a-1
  asserts: 'Same run: the output names the unreachable prerequisite AND the number
    of staged files left unevaluated.'
  framework: unittest
  type: behavioral
- name: test_ge120a1_legacy_failopen_shape_no_longer_satisfies_contract
  file: unit_tests/portability/test_ge_120a_1.py
  covers:
  - GE-120a-1
  asserts: The observed skipping-check fail-open line followed by an ordinary success
    result is no longer produced by the cannot-run path.
  framework: unittest
  type: behavioral
- name: test_ge120a1_reachable_prerequisite_still_blocks_with_six_violations
  file: unit_tests/portability/test_ge_120a_1.py
  covers:
  - GE-120a-1
  asserts: 'Main checkout, identical staged set: the blocked verdict naming the same
    6 violations is unchanged.'
  framework: unittest
  type: behavioral
- name: test_ge_120a_1_reachable_from_entry_point
  file: unit_tests/portability/test_ge_120a_1.py
  covers:
  - GE-120a-1
  angle: reachability
  asserts: 'REQUIRED — invoke the production entry point (CLI, hook, slash command,
    workflow dispatch, or main()) as a subprocess/dispatch and assert the new behaviour
    actually occurs. Do NOT satisfy this by importing the function directly. The AC
    authored no test_spec, so the entry point is not declared: resolve it before writing
    this test, and do not delete this entry.'
```

## Implementation Notes

```yaml
- 'Target is concrete: templates/scripts/commit_guardian/check_ac_parent_covered_by.py.
  The cannot-run branch to replace is its PRIVATE _get_derive_parent_id() fallback
  (~line 377), which today prints ''cannot import derive_parent_id ...; skipping check
  (fail-open)'' and returns an ordinary success. VERIFIED 2026-08-14: this check does
  NOT import the shared templates/scripts/commit_guardian/_resolve_root.py that 27
  other files in the family already use.'
- The could-not-check outcome must be machine-readable INDEPENDENTLY of the exit code.
  GE-120a-2 permits an announce disposition that still exits 0, so exit status alone
  cannot carry the distinction. Callers (pre-commit, the GE-120b-4 sweep, the GE-120c-1
  harness) must tell could-not-check from clean-pass without parsing prose.
- 'Declare the outcome vocabulary ONCE in a shared module under templates/scripts/commit_guardian/
  that every check imports. Do not re-declare the values per check: a per-check copy
  reproduces the 19-instance private-root-walk pattern that GE-120b-2 exists to remove.'
- Any new shared module a hook imports MUST be added to the build deploy manifest
  (deploy_map in scripts/build_phases.py). An undeployed hook dependency raises ModuleNotFoundError
  at hook runtime from the deployed layout and blocks every commit (CLAUDE.md, 'New
  Hook / Gate Dependencies Must Be in the Build Deploy-Manifest').
- 'ADR-001 deployed/template parity: edit templates/scripts/commit_guardian/ (canonical),
  then run ''python scripts/build.py --target-dir .'' so scripts/commit_guardian/
  and .leafcutter/scripts/commit_guardian/ match. check_hook_parity enforces all four
  locations (config section hook_parity: runtime_dir scripts/commit_guardian, canonical_template_dir
  templates/scripts/commit_guardian, legacy_template_dir templates/commit-guardian,
  deployed_output_dir .leafcutter/scripts/commit_guardian). A template-only fix is
  a NO-OP in the deployed layout, which is the exact defect class this tree closes.'
- The message must name BOTH the unreachable prerequisite AND the unverified scope
  (for example '6 staged files not evaluated'). Naming only the failed import is insufficient;
  GE-120b-1-i requires reader-actionable wording.
- 'Main-checkout behaviour must be unchanged: with the prerequisite reachable, the
  same staged set still blocks with the same 6 violations (the GE-118a-1 backward-compatibility
  precedent).'
- 'Error-handling policy: the cannot-run branch catches a SPECIFIC exception (ImportError
  / OSError), logs at WARNING, and converts to the could-not-check outcome. No bare
  except, no silent swallow (CLAUDE.md Error Handling Rules 1-3).'
```

## Agent Contracts

### documentation-expert

Existing docs to update / cross-link:

- docs/architecture/components/commit-guardian.md (relationship: describes | status: exists)
- templates/scripts/commit_guardian/check_ac_parent_covered_by.py (relationship: modifies | status: exists | relevance: The check whose observed cannot-run behaviour this criterion pins.)
- docs/architecture/adrs/ADR-001-self-hosting-boundary.md (relationship: describes | status: exists | relevance: The template-is-canonical / build.py-deploys rule. A template-only fix is a no-op in the layout the hook actually runs from.)
- docs/reference/ac-schema.md (relationship: context | status: exists | relevance: Currently documents the unlimited fail-open convention this AC bounds; corrected by GE-120a-5.)

- [ ] AC-1: [reference-doc] docs/architecture/components/commit-guardian.md — its output states that it did not inspect the staged files, names the prerequisite

### Delivers To

- **Agent:** python-coder
- **Contract:** A could-not-check outcome value that GE-120a-2 reads to decide block vs announce.

## Sign-offs

- [x] architect-review — 2026-08-25 20:46
- [x] test-writer — 2026-08-25 21:40
- [x] python-coder — 2026-08-25 21:45
- [ ] test-runner
- [ ] documentation-expert
- [ ] pr-reviewer
- [ ] ac-validator
- [ ] ac-fulfillment-gate
- [ ] documentation-verifier
- [ ] commit
- [ ] pull-request

## Comments

### 2026-08-25 20:46 — architect-review (status: ok)
feedback-id: fb_2026-08-25_0345a41b
completion_manifest:
  blast_radius_reviewed: true
  classification_decided: true
  escalation_performed: not_applicable
Classified SMALL. No always-large trigger fires (no Alembic migration, no
hypertable change, no FastAPI/api.py surface change; ADR-001 is referenced by
documentation-expert only as descriptive context, not modified). Directly
inspected `scripts/build_phases.py::build_commit_guardian` (lines ~1383-1445):
it already deploys the ENTIRE `templates/scripts/commit_guardian/` tree via
`rglob("*")` into `scripts/commit_guardian/`, and `check_hook_parity.py` only
requires script-name/content parity for `check_*.py`/`run_hook.py`/
`regenerate_*.py` files between runtime, canonical, and deployed dirs — the
legacy `templates/commit-guardian/` dir is checked only for manifest hook-ID
parity, and `check_ac_parent_covered_by.py` isn't present there today. So the
real design surface is 2 files (`check_ac_parent_covered_by.py` + a new shared
outcome-vocabulary module under `templates/scripts/commit_guardian/`), 1
primary component (`commit_guardian`; `precommit_hooks` is the same family,
well under the >=3-component large threshold), and no cross-package boundary.
`scripts/build.py`/`scripts/build_phases.py` most likely need to be RUN
(`python scripts/build.py --target-dir .`), not edited — the deploy_map
pattern the ticket's Implementation Notes cite applies to `scripts/ac_store/`,
not to `commit_guardian`, which is already whole-directory auto-deployed.
No escalation to architect-review-deep performed (small case, Sonnet-only
per protocol). Note: the `Agent` tool needed to dispatch `research-agent` was
not available in this session's toolset, so blast-radius verification above
was done directly via `Read`/`Bash` against the actual `build_phases.py` and
`check_hook_parity.py` source rather than via a spawned sub-agent — flagged
as an anomaly for the supervisor.

### 2026-08-25 21:40 — test-writer (status: ok)
feedback-id: fb_2026-08-25_6945e287
completion_manifest:
  tests_written: true
  red_baseline_captured: true
  ac_store_test_spec_followed: true
Wrote unit_tests/portability/test_ge_120a_1.py with 5 unittest tests, all
executing the real, unmodified check_ac_parent_covered_by.py as a genuine
subprocess (never imported in-process) per the AC's own coverage note and
test_rationale ("covered only by a test that EXECUTES the check under a
genuine cannot-reach condition"). No GE-120c-1 harness exists yet, so each
test builds its own isolated on-disk working copy via tempfile + shutil.copy2
of the real source files (never a hand-typed/mocked check), with PYTHONPATH
explicitly excluded from the subprocess environment so the real repo's
scripts/ package cannot leak in and mask the condition. AC YAML fixtures are
generated via yaml.safe_dump (never hand-typed), per the Fixture Authenticity
Rule.

4 of 5 tests are genuinely RED against today's unmodified production code —
confirmed with AC_ENFORCE_STRICT=1 (this repo's pytest_ac_enforcement plugin
otherwise downgrades not-done-AC failures to xfail by default, masking the
true state; see red_baseline below for the real AssertionError text captured
under AC_ENFORCE_STRICT=1). The 5th test
(test_ge120a1_reachable_prerequisite_still_blocks_with_six_violations)
legitimately passes today and must continue to pass after the fix — it pins
the AC's own explicit non-regression clause ("this criterion does not by
itself decide whether the commit is blocked... main-checkout behaviour must
be unchanged"), so it is a deliberate regression guard, not an
under-specified test, and is correctly omitted from the red_baseline list
below (per the "do not include passing tests" schema rule).
The reachability-floor test
(test_ge_120a_1_reachable_from_entry_point) additionally exercises the
OSError/IsADirectoryError sub-case named in the AC's error-handling note
(distinct from the ImportError case the other 3 red tests cover), verified
today to fall through to the generic bottom-of-file catch-all with a
differently-shaped, non-machine-readable message.
This ticket's `## Agent Contracts` section has no `### test-writer`
subsection and the ticket carries no `## AC Coverage` table, so per signoff
SKILL.md §2c.1/§2c.2 the AC-checkbox and coverage-table steps are correctly
skipped (v2 heading present, but no agent-specific block to act on).
red_baseline:
  - test_name: test_ge120a1_cannot_run_emits_could_not_check_not_success
    file: unit_tests/portability/test_ge_120a_1.py
    error: "AssertionError: 'RESULT: could_not_check' not found in '\\n[check-ac-parent-covered-by] WARNING: cannot import derive_parent_id: ac_parent_id.py not found via package import, script-relative, or project-root walk.; skipping check (fail-open)\\n'"
  - test_name: test_ge120a1_output_names_prerequisite_and_unverified_scope
    file: unit_tests/portability/test_ge_120a_1.py
    error: "AssertionError: Regex didn't match: '6\\\\s+staged\\\\s+files' not found in '\\n[check-ac-parent-covered-by] WARNING: cannot import derive_parent_id: ac_parent_id.py not found via package import, script-relative, or project-root walk.; skipping check (fail-open)\\n'"
  - test_name: test_ge120a1_legacy_failopen_shape_no_longer_satisfies_contract
    file: unit_tests/portability/test_ge_120a_1.py
    error: "AssertionError: 'skipping check (fail-open)' unexpectedly found in '\\n[check-ac-parent-covered-by] WARNING: cannot import derive_parent_id: ac_parent_id.py not found via package import, script-relative, or project-root walk.; skipping check (fail-open)\\n'"
  - test_name: test_ge_120a_1_reachable_from_entry_point
    file: unit_tests/portability/test_ge_120a_1.py
    error: "AssertionError: 'RESULT: could_not_check' not found in \"\\n[check-ac-parent-covered-by] unexpected error (fail-open): IsADirectoryError: [Errno 21] Is a directory: '/tmp/.../scripts/ac_store/ac_parent_id.py'\\n\""
    note: "Confirms the OSError/IsADirectoryError sub-case named in the AC's error-handling note falls through to the generic catch-all today, uncaught by the ImportError-only handler."

### 2026-08-25 21:45 — python-coder (status: ok)
feedback-id: fb_2026-08-25_f19faf4d
completion_manifest:
  red_baseline_all_green: true
  reachable_path_regression_free: true
  shared_outcome_module_created: true
  build_deploy_parity_verified: true
  doc_enforcer_applied: true
Created templates/scripts/commit_guardian/check_outcome.py declaring
OUTCOME_OK / OUTCOME_COULD_NOT_CHECK and emit_result() once, per the ticket's
"declare once, every check imports" note. In
templates/scripts/commit_guardian/check_ac_parent_covered_by.py, widened
main()'s prerequisite-discovery except clause from bare ImportError to
(ImportError, OSError) — the OSError arm closes the second cannot-run shape
this AC's error-handling note calls out (ac_parent_id.py present as a
directory), which previously fell through uncaught to the generic
bottom-of-file catch-all. Both arms now call a new _emit_could_not_check()
helper that names "derive_parent_id" and the exact staged-file count on
stderr ("... not evaluated for N staged files") and emits
"RESULT: could_not_check" via check_outcome.emit_result() on stdout,
independent of exit code (still 0, since GE-120a-2 owns block-vs-announce).
The legacy "skipping check (fail-open)" line is gone. check_outcome import
uses the same graceful-fallback pattern already used for _ac_store_index in
this file, so the check still degrades correctly in the isolated-copy
fixture the AC's own tests construct (which does not copy check_outcome.py
alongside it) as well as in the real deployed layout (build.py deploys the
whole templates/scripts/commit_guardian/ tree, so check_outcome.py sits
next to the check script and the primary import path is taken there).
red_baseline_results:
  - test_name: test_ge120a1_cannot_run_emits_could_not_check_not_success
    result: green
  - test_name: test_ge120a1_output_names_prerequisite_and_unverified_scope
    result: green
  - test_name: test_ge120a1_legacy_failopen_shape_no_longer_satisfies_contract
    result: green
  - test_name: test_ge_120a_1_reachable_from_entry_point
    result: green
test_ge120a1_reachable_prerequisite_still_blocks_with_six_violations
(non-regression guard, was already green) confirmed still green — same 6
violations, exit 1, no RESULT: could_not_check. Ran the existing
unit_tests/commit_guardian/test_check_ac_parent_covered_by.py (9 tests) and
the full hook-parity test slice (64 tests) — all green, no regressions.
Ran `python scripts/build.py --target-dir .` (after staging the new
check_outcome.py so the tracked-source guard passed) to sync
templates -> scripts/commit_guardian -> .leafcutter/scripts/commit_guardian
per ADR-001; verified all three copies of both files are byte-identical.
ruff check passes on both touched files. No `## Agent Contracts`
`### python-coder` heading and no `## AC Coverage` table exist on this
ticket, so per signoff SKILL.md §2c.1/§2c.2 the AC-checkbox/coverage-table
steps are correctly skipped.

### 2026-08-25 22:10 — python-coder (status: ok)
feedback-id: fb_2026-08-25_0758f1c8
completion_manifest:
  templates_fix_intact: true
  deployed_layout_reverified: true
  deploy_drift_found_and_fixed: true
  red_baseline_all_green: true
Re-invoked on this already-signed-off ticket; verified the actual on-disk
state rather than trusting the prior sign-off's claims at face value. The
templates fix (check_ac_parent_covered_by.py's (ImportError, OSError)
cannot-run branch + the new templates/scripts/commit_guardian/check_outcome.py
shared vocabulary module) was intact and unchanged. However, the DEPLOYED
copy at scripts/commit_guardian/check_ac_parent_covered_by.py (which
resolves through the .leafcutter symlink to the shared workspace-level
.leafcutter, not a copy local to this worktree) had reverted to the LEGACY
pre-fix content — missing the OSError arm, the check_outcome import, and
_emit_could_not_check() entirely — despite the prior sign-off's claim that
all three copies were verified byte-identical. This is consistent with the
documented shared-.leafcutter race: another concurrent build.py run (from a
sibling worktree/session in this same epic drive) redeployed from a
templates copy that predates this fix, silently reverting the shared
deployed layout after this ticket's own build.py run had correctly synced
it. Confirmed via direct diff (scripts/commit_guardian is a symlink to
../.leafcutter/scripts/commit_guardian, which is itself a symlink to the
main leafcutter-ai repo's .leafcutter — shared across all worktrees in this
workspace). Fixed by re-running `python scripts/build.py --target-dir
<worktree-root>` now; re-verified both check_ac_parent_covered_by.py and
check_outcome.py are byte-identical between templates and the deployed
scripts/commit_guardian across all copies. Re-ran the full
unit_tests/portability/test_ge_120a_1.py (5 tests) and
unit_tests/commit_guardian/test_check_ac_parent_covered_by.py (9 tests) —
all 14 green (the portability tests build their own isolated tempdir copy
of the templates source directly, so they were unaffected by the deployed-
layout drift either way, but confirm the templates fix itself is correct).
ruff check clean on both files; check_docstrings.py, check_documentation.py,
and check_complexity.py hooks all pass 0 against both files. No production
code changes were needed this pass — this was a re-verification + deploy-
parity repair. Flagging for the supervisor: this shared-.leafcutter build
race is a structural risk for any concurrent multi-worktree drive of this
epic — a later ticket's build.py run can silently revert an earlier
ticket's deployed fix without touching git-tracked files, so the git index
will never show it. The pull-request / commit phase for this epic should
re-run `python scripts/build.py --target-dir .` and re-verify deployed
parity for ALL touched hook files immediately before the final commit, not
rely on any single ticket's earlier verification.
