---
advances_current_outcome: true
agents:
  commit: signed_off
  documentation-expert: not_needed
  pr-reviewer: signed_off
  pull-request: needed
  python-coder: signed_off
  sql-coder: not_needed
  test-runner: signed_off
  test-writer: signed_off
components:
- build-pipeline
created: '2026-06-08'
depends_on:
- BP-700d
- BP-700d-1
files_touched:
- config/agent_registry.json
- docs/architecture/adrs/ADR-001-self-hosting-boundary.md
priority: medium
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: BP-700d-3
status: in_progress
title: skills_config.json frontend key updated to remove frontend-design reference
---

# skills_config.json frontend key updated to remove frontend-design reference

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BP-700d-3` — skills_config.json frontend key updated to remove frontend-design reference — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BP-700d-3`. Component: `build-pipeline`. Assigned agent: `python-coder`. Estimated complexity: `S`.

## Acceptance Criteria

```gherkin
Given a project's skills_config.json has a frontend section with:
  frontend.optional_skills: ["webapp-testing", "frontend-design"]
When the adopter runs build.py with the new package version
Then the frontend.optional_skills value is updated to remove "frontend-design"
And the resulting value is: ["webapp-testing"]
And the frontend.project_context_path key is preserved unchanged
And the frontend.test_command key is preserved unchanged
And if the adopter's skills_config.json did not list frontend-design,
  no change is made to the frontend section
```

## Sign-offs

- [x] python-coder — 2026-06-18 00:30
- [x] test-writer — 2026-06-18 00:00
- [x] test-runner — 2026-06-18 01:15
- [x] pr-reviewer — 2026-06-18 01:30
- [x] commit — 2026-06-18 02:15
- [ ] pull-request

## Comments

### 2026-06-18 00:01 — ticket-supervisor (status: ok)
feedback-id: (not-applicable)
test_requirements empty — test-writer phase skipped (docs-only or config-only ticket)

### 2026-06-18 00:30 — python-coder (status: ok)
feedback-id: fb_2026-06-18_80dfce68
completion_manifest:
  frontend_design_removed_from_ticket_supervisor: true
  frontend_design_removed_from_onboard: true
  migration_function_added_to_build_py: true
  skills_config_json_default_unchanged: true
  decision_history_entry_added: true
Removed `frontend-design` from `skills_invoked` arrays of both `ticket-supervisor` and `onboard` agents in `config/agent_registry.json`. Added `_migrate_skills_config()` function to `scripts/build.py` that runs as a "Config migration" phase before `_run_phases()`; it removes `"frontend-design"` from `frontend.optional_skills` in the adopter's `skills_config.json` if present, preserving all other keys unchanged. The function uses the same discovery order as `load_config` (explicit path → `.claude` → `.gemini` → `.cursor` → `.github` → `.cline`) and is a no-op when the file is absent or `frontend-design` was not listed.

### 2026-06-18 01:15 — test-runner (status: ok)
feedback-id: fb_2026-06-18_4e75adc8
completion_manifest:
  test_migration_4_tests_green: true
  test_agent_registry_9_tests_green: true
  test_skills_config_defaults_11_tests_green: true
  direct_ac_validation_3_scenarios_passed: true
  frontend_design_absent_from_ticket_supervisor_skills: true
  frontend_design_absent_from_onboard_skills: true
Ran 24 tests across test_migration, test_agent_registry, and test_skills_config_defaults — all green. Directly validated the three AC scenarios for _migrate_skills_config: (1) removes frontend-design while preserving webapp-testing and other keys, (2) no-op when frontend-design not listed, (3) no-op when skills_config.json absent. Confirmed frontend-design is absent from skills_invoked in both ticket-supervisor and onboard entries in agent_registry.json.

### 2026-06-18 01:30 — pr-reviewer (status: ok)
feedback-id: fb_2026-06-18_c8529e6c
completion_manifest:
  all_acs_satisfied: true
  error_handling_compliant: true
  no_bare_excepts: true
  no_op_when_absent_verified: true
  other_keys_preserved_verified: true
  agent_registry_changes_verified: true
All five AC scenarios satisfied: _migrate_skills_config removes "frontend-design" from frontend.optional_skills, preserves all other keys, is a no-op when the key is absent or the file is missing, and is idempotent (byte-identical guard). Error handling follows project policy — (OSError, json.JSONDecodeError) on read and OSError on write, both routed through _warn(). No bare excepts. The agent_registry.json removals (ticket-supervisor and onboard) match the python-coder manifest.

### 2026-06-18 02:15 — commit (status: ok)
feedback-id: fb_2026-06-18_384bc5b7
completion_manifest:
  staged_files_verified: true
  pre_commit_hooks_passed: true
  mechanical_fix_applied: true
  commit_succeeded: true
Committed feat(build-pipeline): remove frontend-design from skills_config and agent_registry (#BP-700d-3) — 3 files changed (config/agent_registry.json, scripts/build.py, ticket file). Pre-commit check-feedback-id hook required a mechanical fix: added `feedback-id: (not-applicable)` to the ticket-supervisor comment heading that was missing it. All other hooks passed. Commit hash: 0b6e896.
