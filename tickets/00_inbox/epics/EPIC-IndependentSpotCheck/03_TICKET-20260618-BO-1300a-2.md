---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  llm-expert: needed
  pr-reviewer: needed
  pull-request: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
components:
- build_orchestration
created: '2026-06-18'
depends_on:
- BO-1300a
- BO-1300a-1
files_touched:
- docs/acceptance-criteria/build-orchestration/BO-1300-independent-spot-check/BO-1300a.yaml
- docs/architecture/adrs/ADR-006-flatten-supervisor-chain.md
- docs/architecture/components/supervisor-spawn-topology.md
priority: high
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: BO-1300a-2
status: todo
title: On-demand command dispatches exactly three spot-check reviewers in parallel
---

# On-demand command dispatches exactly three spot-check reviewers in parallel

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BO-1300a-2` — On-demand command dispatches exactly three spot-check reviewers in parallel — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BO-1300a-2`. Component: `build-orchestration`. Assigned agent: `llm-expert`. Estimated complexity: `M`.

## Acceptance Criteria

```gherkin
Given the standalone spot-check command has resolved a review scope for a finished feature
When the command starts the review
Then it dispatches exactly three spot-check reviewer agents, no more and no fewer
And the three reviewers are all dispatched against the same resolved review scope
And the three reviewers run concurrently rather than one after another
And the three reviewers are dispatched a single level below the command (the command does
  not spawn a build supervisor or any further nested dispatch chain)
```

## Sign-offs

- [ ] llm-expert
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit
- [ ] pull-request

## Comments
