---
ac_traceability:
  id: UXP-700d-1-ii
  path: docs/acceptance-criteria/ux-prototyping/UXP-700-truthful-project-record/UXP-700d-1-ii.yaml
advances_current_outcome: true
agents:
  ac-fulfillment-gate: needed
  ac-validator: needed
  architect-review: not_needed
  commit: needed
  documentation-expert: not_needed
  documentation-verifier: not_needed
  frontend-coder: not_needed
  pr-reviewer: not_needed
  pull-request: not_needed
  python-coder: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: signed_off
  user-surface-smoker: not_needed
change_target: code
complexity: low
components:
- ux_prototyping
created: '2026-09-09'
depends_on: []
files_touched: []
priority: medium
requires_adr: false
requires_diagram: false
risk_surface: internal
roadmap_phase: phase_1
source_ac: UXP-700d-1-ii
status: todo
title: A project's own artifact is never set aside as an example
---

# A project's own artifact is never set aside as an example

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `UXP-700d-1-ii` — A project's own artifact is never set aside as an example — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `UXP-700d-1-ii`. Component: `ux-prototyping`. Assigned agent: `python-coder`. Estimated complexity: `S`. Complexity: `low`.

## Acceptance Criteria

```gherkin
Given a journey authored by the project itself, describing the project's own product, stored
  under the project's own product root,
When the project's own record is enumerated,
Then that journey is returned,
And it is absent when the example product is asked for by name,
And it is not counted among the artifacts set aside as example content.
```

- [ ] AC-1: that journey is returned
- [ ] AC-2: it is absent when the example product is asked for by name
- [ ] AC-3: it is not counted among the artifacts set aside as example content.

## Test Requirements

```yaml
tests:
- name: test_projects_own_journey_is_returned_by_the_projects_record
  file: unit_tests/product_truth/test_uxp_700d_1_ii.py
  covers:
  - UXP-700d-1-ii
  asserts: A journey under the project's own product root appears in the project's
    record.
  framework: unittest
  type: integration
  angle: criterion
- name: test_projects_own_journey_is_not_counted_as_example_content
  file: unit_tests/product_truth/test_uxp_700d_1_ii.py
  covers:
  - UXP-700d-1-ii
  asserts: The set-aside count is unchanged by adding a project-owned journey.
  framework: unittest
  type: integration
  angle: boundary
- name: test_uxp_700d_1_ii_reachable_from_entry_point
  file: unit_tests/product_truth/test_uxp_700d_1_ii.py
  covers:
  - UXP-700d-1-ii
  angle: reachability
  asserts: 'REQUIRED — invoke the production entry point (CLI, hook, slash command,
    workflow dispatch, or main()) as a subprocess/dispatch and assert the new behaviour
    actually occurs. Do NOT satisfy this by importing the function directly. The AC
    authored no test_spec, so the entry point is not declared: resolve it before writing
    this test, and do not delete this entry.'
```

## Agent Contracts

### Expects From

- **AC:** UXP-700d-1
- **Contract:** The ownership predicate whose acceptable input this record supplies.

## Sign-offs

- [x] test-writer — 2026-09-09 17:27
- [ ] python-coder
- [ ] test-runner
- [ ] ac-validator
- [ ] ac-fulfillment-gate
- [ ] commit

## Comments

### 2026-09-09 17:27 — test-writer (status: ok)
feedback-id: fb_2026-09-09_a5480e3c
completion_manifest:
  tests_written: true
  red_baseline_verified: true
  cross_layer_seam_answer:
    result: covered
    producing_side: "docs/product-truth/scripts/product_ownership.py (own_record_artifacts /
      artifacts_for_product / is_example_artifact_id -- the real module the sibling
      UXP-700d-1 ticket's own test-writer phase already pinned in
      unit_tests/product_truth/test_uxp_700d_1.py)"
    consuming_side: "unit_tests/product_truth/test_uxp_700d_1_ii.py's
      test_uxp_700d_1_ii_reachable_from_entry_point, which subprocesses the real
      product_ownership.py CLI against the real, checked-in docs/product-truth/
      index.json and asserts on its stdout"
  reachability_entry_point_answer:
    result: resolved
    entry_point: "python docs/product-truth/scripts/product_ownership.py [--product <name>]
      (CLI via subprocess; main(argv) -- Reachability Entry-Point Resolution Step 1,
      category 5). Copied through from the sibling UXP-700d-1 ticket's own
      test-writer resolution rather than re-derived, since both tickets exercise
      the same production surface."
Wrote unit_tests/product_truth/test_uxp_700d_1_ii.py (3 tests: criterion, boundary,
reachability) against UXP-700d-1-ii, the paired acceptable-input control for
UXP-700d-1. Initial draft invented a competing `by_product` index.json design;
before finalizing, discovered unit_tests/product_truth/test_uxp_700d_1.py already
staged from the sibling UXP-700d-1 ticket's own test-writer phase, pinning a
concrete, real contract instead: a new module docs/product-truth/scripts/
product_ownership.py (PROJECT_PRODUCT, own_record_artifacts(), artifacts_for_product(),
is_example_artifact_id(), main(argv) CLI). Rewrote this file to consume that same
real contract rather than invent a second, competing shape -- python-coder must
satisfy both tickets with ONE implementation, so agreeing with the sibling's
already-pinned contract was required, not optional. Fixtures load the REAL,
checked-in docs/product-truth/index.json (Fixture Authenticity Rule) rather than a
synthetic temp store, reusing the sibling file's own `_load_real_index_artifacts()`
convention; verified against that real data that "leafcutter/ac-lifecycle" exists
and 13 real fern-and-fig artifacts exist (guards the boundary test's sanity
baseline against a vacuous 0==0 pass). Verified red under `AC_ENFORCE_STRICT=1
python -m pytest unit_tests/product_truth/test_uxp_700d_1_ii.py -v`: all 3 tests
fail with the same honest `ModuleNotFoundError: No module named 'product_ownership'`
the sibling file's own tests fail with (confirmed identical failure mode by running
both files side by side) -- no SyntaxError, no vacuous passes. No non-code skip
applies -- python-coder is `needed` on this ticket.
