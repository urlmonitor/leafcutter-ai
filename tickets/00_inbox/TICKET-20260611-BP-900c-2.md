---
advances_current_outcome: true
agents:
  commit: signed_off
  documentation-expert: not_needed
  pr-reviewer: signed_off
  pull-request: signed_off
  python-coder: signed_off
  sql-coder: not_needed
  test-runner: signed_off
  test-writer: signed_off
components:
- build_pipeline
created: '2026-06-11'
depends_on:
- BP-900c
- BP-900c-1
files_touched:
- scripts/build_propagation_audit.py
- docs/architecture/components/template-compiler.md
priority: critical
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: BP-900c-2
status: todo
title: Error report is emitted to stderr in a structured, parseable format with non-zero
  exit
---

# Error report is emitted to stderr in a structured, parseable format with non-zero exit

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BP-900c-2` — Error report is emitted to stderr in a structured, parseable format with non-zero exit — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BP-900c-2`. Component: `build_pipeline`. Assigned agent: `python-coder`. Estimated complexity: `S`.

## Acceptance Criteria

```gherkin
Given the build-time guard has detected 3 broken script references,
When the guard emits the error report,
Then all error output is written to stderr (not stdout)
And the output is structured as one JSON object per line (JSONL format)
with keys "missing_path", "referencing_template", and "suggested_action"
And the process exits with a non-zero exit code
And stdout contains no error output (so piped build output is not polluted)
```

## Sign-offs

- [ ] python-coder
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit
- [ ] pull-request

## Comments
