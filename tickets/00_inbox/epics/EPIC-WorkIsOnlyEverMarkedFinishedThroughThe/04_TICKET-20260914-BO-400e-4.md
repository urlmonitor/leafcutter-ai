---
ac_traceability:
  id: BO-400e-4
  path: docs/acceptance-criteria/build-orchestration/BO-400-ticket-status-source-of-truth/BO-400e-4.yaml
advances_current_outcome: true
agents:
  ac-fulfillment-gate: signed_off
  ac-validator: signed_off
  adr-author: signed_off
  architect-review: signed_off
  commit: signed_off
  documentation-expert: signed_off
  documentation-verifier: signed_off
  frontend-coder: not_needed
  pr-reviewer: signed_off
  pull-request: not_needed
  python-coder: signed_off
  sql-coder: not_needed
  test-runner: signed_off
  test-writer: signed_off
  user-surface-smoker: not_needed
change_target: pipeline
complexity: medium
components:
- build_orchestration
- supervisor_system
created: '2026-09-14'
depends_on:
- 01_TICKET-20260914-BO-400e-1.md
- 02_TICKET-20260914-BO-400e-2.md
- 03_TICKET-20260914-BO-400e-3.md
documentation_required: true
files_touched:
- templates/workflows-js/build-feature.js
- templates/workflows-js/build-ticket.js
- unit_tests/prompt_assembly/test_bo_400e_4.py
- unit_tests/prompt_assembly/test_bo_400e_4_batching.py
- unit_tests/prompt_assembly/test_bo_400e_4_fixtures.py
- unit_tests/prompt_assembly/test_bo_400e_4_ordering.py
- unit_tests/prompt_assembly/test_bo_400e_4_reachability.py
- unit_tests/prompt_assembly/test_bo_400e_4_repeatability.py
out_of_scope:
- scripts/set_ticket_status.py
- unit_tests/prompt_assembly/_bo_400e_1_i_fixtures.py
- unit_tests/prompt_assembly/_bo_400e_1_support.py
- unit_tests/prompt_assembly/_driver_harness.py
- unit_tests/prompt_assembly/harness_build_ticket_guard.mjs
- unit_tests/prompt_assembly/test_bo_400e_1.py
- unit_tests/prompt_assembly/test_bo_400e_1_i.py
- unit_tests/prompt_assembly/test_bo_400e_1_i_handoff_resolution.py
- unit_tests/prompt_assembly/test_bo_400e_1_i_handoff_seam_and_unrelated_pass.py
- unit_tests/prompt_assembly/test_bo_400e_1_ii.py
- unit_tests/prompt_assembly/test_bo_400e_2.py
- unit_tests/prompt_assembly/test_bo_400e_2_i.py
- unit_tests/prompt_assembly/test_bo_400e_3.py
- unit_tests/prompt_assembly/test_empty_needed_phase_set_completion.py
- unit_tests/workflows/test_bo4000d_repository_reference_anchor.py
- unit_tests/workflows/test_bo4000e_clone_path_does_not_escape_the_worktree.py
priority: high
requires_adr: true
requires_diagram: false
risk_surface: contract_boundary
roadmap_phase: phase_1
source_ac: BO-400e-4
status: done
ac_coverage: 8/8
title: Several tickets in the identical state, carried by one run, all get the same
  answer -- and it is the strict one
---

# Several tickets in the identical state, carried by one run, all get the same answer -- and it is the strict one

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BO-400e-4` — Several tickets in the identical state, carried by one run, all get the same answer -- and it is the strict one — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BO-400e-4`. Component: `build-orchestration`. Assigned agent: `python-coder`. Estimated complexity: `M`. Complexity: `medium`.

## Acceptance Criteria

```gherkin
Given one run carrying four tickets,
  And the first three are in the identical state: their records name the same phases as
    needed, every one of those phases carries a sign-off, except one and the same phase
    in all three, which carries none,
  And the fourth ticket's record names those same phases as needed and carries a
    sign-off for every one of them,
When the run reaches the close of each of the four,
Then the first three all receive the same answer as each other,
  And that answer is refusal: none of the three has its recorded state changed, and each
    refusal names the same outstanding phase,
  And the fourth is written finished,
  And the answers do not vary with the order in which the run reached the tickets, nor
    with which of the four was closed first,
And given a second run over the same four tickets in the same state,
When it reaches the close of each,
Then it produces the same four answers as the first run,
So one run cannot give two answers to one question, the answer it gives is the strict
  one, and "the same answer every time" is not obtained by a run that refuses
  everything.
```

- [x] AC-1: the first three are in the identical state: their records name the same phases as
- [x] AC-2: the fourth ticket's record names those same phases as needed and carries a
- [x] AC-3: the first three all receive the same answer as each other
- [x] AC-4: that answer is refusal: none of the three has its recorded state changed, and each
- [x] AC-5: the fourth is written finished
- [x] AC-6: the answers do not vary with the order in which the run reached the tickets, nor
- [x] AC-7: given a second run over the same four tickets in the same state
- [x] AC-8: it produces the same four answers as the first run

## Test Requirements

```yaml
tests:
- name: test_four_tickets_in_one_run_receive_one_answer_three_refused_one_written
  file: unit_tests/prompt_assembly/test_bo_400e_4.py
  covers:
  - BO-400e-4
  asserts: 'Drive FOUR tickets through the real workflow in ONE run: three in the
    identical state (same phases named needed, every one signed off except the same
    single phase in all three, which has no sign-off) plus a fourth whose every needed
    phase is signed off. Assert the three receive the same answer as each other, that
    the answer is refusal -- no recorded state changed, the same outstanding phase
    named in each -- and that the fourth is written finished. FOUR SEPARATE SINGLE-TICKET
    RUNS DO NOT REPRODUCE THIS: the defect is the disagreement between tickets inside
    one run, so per-ticket assertions gathered from independent runs pass on the broken
    driver.'
  framework: unittest
  type: integration
  angle: boundary
- name: test_answers_do_not_vary_with_the_order_the_run_reaches_the_four_tickets
  file: unit_tests/prompt_assembly/test_bo_400e_4.py
  covers:
  - BO-400e-4
  asserts: 'Re-drive THE SAME four-tickets-in-one-run fixture with the tickets permuted
    -- including runs in which the control ticket is reached first and last -- and
    assert each ticket''s answer is unchanged by its position. The observed split
    correlated with position in the run (ticket 01 refused, 03 and 20 written), so
    an answer that moves with order has relocated the disagreement rather than removed
    it. Must reuse the one-run fixture: permuting four independent single-ticket runs
    proves nothing.'
  framework: unittest
  type: integration
  angle: criterion
- name: test_a_second_run_over_the_same_four_tickets_produces_the_same_four_answers
  file: unit_tests/prompt_assembly/test_bo_400e_4.py
  covers:
  - BO-400e-4
  asserts: Restore the same four tickets to the same state and drive a SECOND full
    run, asserting the four answers match the first run's four answers exactly. Covers
    the criteria's second-run clause; combined with the control ticket it also proves
    the repeatability is not the trivial repeatability of a writer that refuses everything.
  framework: unittest
  type: integration
  angle: criterion
- name: test_one_answer_per_condition_is_reachable_from_a_real_workflow_run
  file: unit_tests/prompt_assembly/test_bo_400e_4.py
  covers:
  - BO-400e-4
  asserts: 'REQUIRED -- invoke the production entry point: load and execute the real
    workflow script via unit_tests/prompt_assembly/harness_build_ticket_guard.mjs,
    carrying all four tickets in one run, and assert the per-ticket record writes
    the run actually performed. Do NOT satisfy this by importing the completion helper
    and calling it four times: that is the single-ticket shape the record''s own rationale
    rules out, and it passes on the broken driver. AUTHORED DELIBERATELY (it-po):
    because this record carries an authored test_spec, the ticket generator''s own
    reachability floor would otherwise append a generic sentinel whose text asserts
    ''the AC authored no test_spec, so the entry point is not declared'' -- false
    here, and misleading on precisely the record whose entry point and run shape are
    load-bearing.'
  framework: unittest
  type: integration
  angle: reachability
```

## Implementation Notes

```yaml
config_schema_fragment: null
constraints:
- FOUR TICKETS, ONE RUN -- CARDINALITY IS THE WHOLE POINT. A per-ticket assertion
  gathered from four independent single-ticket runs does not reproduce this defect
  and does not satisfy this record. At one ticket the broken driver and the fixed
  driver are indistinguishable, because either branch is defensible in isolation;
  the defect IS the disagreement between tickets inside a single run.
- ORDER-INDEPENDENCE IS A PROPERTY OF THE DECISION, NOT OF THE FIXTURE. No state accumulated
  during a run -- a memo, a cache, a running tally, a set of already-seen phases,
  a batch index -- may influence a later ticket's answer. The observed split correlated
  with position in the run (ticket 01 refused, tickets 03 and 20 written); an implementation
  whose answer depends on how far into the run a ticket sits has not converged, it
  has moved the disagreement somewhere harder to see.
- 'DETERMINISM MUST NOT BE OBTAINED BY REFUSING EVERYTHING. ''All four the same answer''
  is a property a completely broken writer holds trivially. The fourth, fully-signed-off
  ticket is part of the contract, not a testing nicety: only the contrast makes the
  three refusals attributable to the missing sign-off.'
- THE PER-TICKET DECISION MUST BE INDIVIDUALLY OBSERVABLE. The run must expose the
  record write it performed for EACH ticket, separately. A run that reports only an
  aggregate outcome cannot be checked for agreement between its tickets, which is
  exactly the observability gap that made the original defect look like agent caprice
  for a day.
- 'VERIFY BY EXECUTION, NEVER BY GREP. Nothing about this record is visible in the
  source: the broken driver and the fixed one both contain completion code, a phase
  list and a parity check. Only reading back the four records a single run produced
  can tell them apart.'
- BOTH TWINS IN ONE CHANGE, per BO-400a-2-ii's standing constraint.
- TEMPLATES ARE BUILD SOURCES, NOT THE RUNNING ARTEFACT. Keep the deployed copy of
  the drivers in step (check-build-drift is the gate) and confirm the four-ticket
  behaviour through the deployed layout before sign-off.
n_location_rule: 2 -- the completion-write step in build-feature.js, which is the
  only driver that can exhibit the disagreement, and the twin step in build-ticket.js,
  which cannot but must not be allowed to become the second door. Both change together.
post_write_commands:
- python -m unittest discover -s unit_tests/prompt_assembly -p test_bo_400e_4.py
- python -m unittest discover -s unit_tests/prompt_assembly -p test_ticket_done_recording.py
reference_file_path: templates/workflows-js/build-feature.js
required_skills:
- python-coder
```

## Agent Contracts

### documentation-expert

Existing docs to update / cross-link:

- docs/known-issues/build-orchestration.md (relationship: describes | status: exists | relevance: KI-BO-20260831-1932 IS this case: one drive, three tickets in exactly the same state -- every phase signed off except pull-request, with no sign-off entry for it -- and the guard refused one and flipped the other two to done. The reproduction here is that observation with a control ticket added.)
- templates/workflows-js/build-feature.js (relationship: modifies | status: exists | relevance: The epic driver, the surface on which the split outcome was observed -- it is the driver that carries several tickets in one run, so it is the only place the defect is visible. TWIN of build-ticket.js.)
- templates/workflows-js/build-ticket.js (relationship: modifies | status: exists | relevance: The single-ticket twin. It cannot exhibit the disagreement itself, which is precisely why the change must land here too: otherwise the two drivers become the two doors again.)
- unit_tests/prompt_assembly/harness_build_ticket_guard.mjs (relationship: describes | status: exists | relevance: Behavioral harness. It must be driven with FOUR tickets in ONE run and must expose the record write performed for each, individually -- a per-ticket assertion aggregated across four separate runs does not reproduce the defect.)
- docs/acceptance-criteria/build-orchestration/BO-400-ticket-status-source-of-truth/BO-400a-2-iii.yaml (relationship: related | status: exists | relevance: Already uses the three-tickets-in-one-drive shape with a control case, and is the closest precedent for how this must be tested.)
- docs/architecture/adrs/ADR-006-flatten-supervisor-chain.md (relationship: describes | status: exists | relevance: Why the epic driver's own per-ticket loop -- not a supervisor agent -- is the surface that carries several tickets in one run, and therefore the only place the disagreement can occur.)
- docs/architecture/components/build-orchestration.md (relationship: describes | status: exists | relevance: The component document for the drivers -- the boundary this change stays inside.)
- docs/architecture/agent_delivery_workflows.md (relationship: describes | status: exists | relevance: The batching and dispatch topology that determines the order tickets are reached in a run -- the dimension the order-independence clause is about.)

- [x] AC-1: sequence-diagram | docs/architecture/components/build-orchestration.md | the first three all receive the same answer as each other <!-- signed: documentation-expert -->

### Expects From

- **AC:** BO-400e-3
- **Contract:** A single close route through the checking mechanism -- the property that makes one answer per condition possible, since two routes are what produced two answers

## Sign-offs

- [x] architect-review — 2026-09-22 09:15
- [x] adr-author — 2026-09-22 05:40
- [x] test-writer — 2026-09-22 11:20
- [x] python-coder — 2026-09-22 23:03
- [x] test-runner — 2026-09-22 23:20
- [x] documentation-expert — 2026-09-22 23:45
- [x] pr-reviewer — 2026-09-22 23:59
- [x] ac-validator — 2026-09-22 23:56
- [x] ac-fulfillment-gate — 2026-09-22 23:47
- [x] documentation-verifier — 2026-09-22 23:55
- [x] commit — 2026-09-23 00:25

## Comments

### 2026-09-22 09:15 — architect-review (status: ok)
feedback-id: fb_2026-09-22_8bafd5c7
completion_manifest:
  blast_radius_confirmed: true
  requires_adr_recomputed: true
  cross_module_boundary_check: true
  adr_047_precedent_reviewed: true
  order_independence_defect_traced: true

**Classification: LARGE.** No `Agent` tool was available in this dispatch context, so
`research-agent` and `architect-review-deep` could not literally be spawned; this note
substitutes a direct Sonnet-authored deep review using Read/Bash against the worktree
source, matching the substitution already recorded on the sibling ticket
`03_TICKET-20260914-BO-400e-3.md`. Treat the substitution as an anomaly if a genuine
Opus pass is still desired downstream.

**1. Impact summary.** `files_touched` is unchanged from `BO-400e-3`'s own set restricted
to the two JS twins: `templates/workflows-js/build-feature.js` (`concludeTicket` /
`writeTicketCompletion`, ~line 1301-1520) and `templates/workflows-js/build-ticket.js`'s
mirrored single-ticket path. By file count (2, ≤5) this would sit under the small
threshold, and by itself `components: [build_orchestration, supervisor_system]` (2) is
also below the ≥3 large-component trigger. Classification is driven instead by two
always-large signals: (a) `has_adr_contract_change` — the fix amends the behavioural
guarantee of the single-writer close path that `ADR-047-single-writer-ticket-close-path.md`
(authored for the immediate sibling ticket, same two files, same two components) already
records, adding the order-independence/no-run-state invariant the AC demands; (b) the
mandatory `requires_adr` rule itself, which fires mechanically off `components:` naming
2 distinct entries — exactly the same rule and the same two components that flipped
`BO-400e-3`'s `requires_adr` to `true`. `requires_adr` here was `false` in the generated
frontmatter; corrected to `true`, and `adr-author: needed` added to both the `agents:` map
and `## Sign-offs`.

**2. Direct-inspection findings on the defect itself.** Read `concludeTicket`,
`completionVerdictFromRecord`, `demandedPhasesFromRecord` and `requiredPhasesForCompletion`
in `build-feature.js`: all four are already pure functions of the ticket's own read-back
record (per-ticket `spec.record`/`spec.demandedRecord`), with no closed-over mutable state
feeding the verdict — `BO-400e-1`'s own docstring on `concludeTicket` explicitly documents
that `spec` was stripped of a caller-supplied phase list for exactly this reason. The one
module-level mutable value in the file, `unknownPhaseAgentsReported` (a `Set` used only to
deduplicate a `console.error` diagnostic in `getPriority`), does not feed `getPriority`'s
return value — it only gates whether the same warning is printed twice — so it is not the
determinism leak. That means the per-ticket decision math this reviewer could read is
already order-independent; the constraints' "correlated with position in the run" symptom
is most consistent with state living OUTSIDE this pure core — plausibly in how the batch
loop sequences per-ticket `status-checker` dispatches (a shared LLM sub-agent conversation
across tickets in one run would explain an answer that shifts with reach-order without any
JS-level shared mutable). This is a hypothesis for `python-coder`/`test-writer` to confirm
by execution (per the ticket's own "verify by execution, never by grep" instruction) — this
review found no code-level smoking gun, only ruled out the one obvious candidate.

**3. Design guidance.** (a) Whatever the fix, it must not introduce a run-scoped memo,
cache, or index into either driver — the very shape the Implementation Notes forbid — so
prefer fixes that make the `status-checker` dispatch for each ticket demonstrably
independent (fresh context per ticket, or an explicit statelessness contract in the dispatch
prompt) over fixes that add JS-side bookkeeping to compensate for a stateful dispatch.
(b) Land both twins in the same commit per the ticket's own `n_location_rule: 2` and the
established `BOTH TWINS IN ONE CHANGE` convention from `BO-400a-2-ii`. (c) Verify against
the deployed copies (`check-build-drift`), not just source, matching the sibling ticket's own
instruction.

**4. Acceptance-criteria adjustments.**
- [added] Where the fix touches the `status-checker` dispatch mechanism rather than pure JS,
  the test harness must assert independence of the sub-agent context/session across the four
  tickets in one run, not just independence of the JS-level inputs — a fix that only
  reorders JS computation while the dispatch layer stays stateful would pass a JS-only test
  and still fail the real harness.

**5. ADR recommendation.** `ADR-047` already states "one writer" (the mechanism), but does
not state that a run carrying several tickets must produce an answer for each that does not
vary with reach-order or reuse any state across tickets — a distinct, citable invariant this
ticket's AC makes load-bearing. Recommend a **new** ADR at
`docs/architecture/adrs/ADR-048-order-independent-per-ticket-completion.md` (048 = current
high-water mark 047 + 1; ADR-045 remains an unclaimed gap per `BO-400e-3`'s own note and is
intentionally left unclaimed rather than backfilled here, to avoid colliding with any
in-flight use of that number). Cross-link to `ADR-047` (which mechanism may write) and
`ADR-046` (which phases are checked) as the third leg of the same family: how the decision
must behave when the mechanism runs across a batch. `requires_adr` corrected to `true` and
`adr-author: needed` added to `agents:` and `## Sign-offs` accordingly.

**6. Suggested documentation.** `docs/known-issues/build-orchestration.md` already names
`KI-BO-20260831-1932` as this exact defect family; the `documentation-expert` phase should
close the loop by recording the fix and its confirmation harness there, and back-link the
new ADR from `docs/architecture/components/build-orchestration.md` as `ADR-047`'s was.

**Escalation substitution note:** performed without `research-agent` / `architect-review-deep`
sub-agent tools being available to this dispatch. If sub-agent dispatch is restored for this
phase later, an independent `architect-review-deep` pass would still be valuable to confirm
or refute the status-checker-context hypothesis in finding 2 before `python-coder` commits to
an implementation approach.

```json
{
  "architectural_note": "LARGE: has_adr_contract_change (ADR-047's single-writer close-path guarantee is being extended with an order-independence/no-run-state invariant) plus the mandatory requires_adr rule firing on components:[build_orchestration, supervisor_system] (2 distinct, matching BO-400e-3's own precedent). File count (2) and component count (2) alone sit below the small/large thresholds, but the always-large ADR-contract trigger and the mandatory-ADR rule both fire independently. Direct inspection of concludeTicket/completionVerdictFromRecord/demandedPhasesFromRecord in build-feature.js found no module-level mutable state feeding the completion verdict — the one closed-over Set (unknownPhaseAgentsReported) only dedupes a diagnostic log and does not affect getPriority's return value — so the order-dependence symptom most plausibly lives outside this pure core, in how the batch loop sequences per-ticket status-checker dispatches; this must be confirmed by the required execution-based harness, not by further grep.",
  "acceptance_adjustments": [
    "Where the fix touches the status-checker dispatch mechanism (not pure JS), assert independence of the sub-agent context/session across the four tickets in one run, not just independence of JS-level inputs."
  ],
  "escalation": "opus",
  "escalation_reason": "has_adr_contract_change=true (ADR-047 single-writer close-path contract extended with an order-independence invariant) and the mandatory requires_adr rule (components: build_orchestration + supervisor_system, 2 distinct) both fire; architect-review-deep substituted by direct Sonnet review, no Agent tool available.",
  "suggested_adr": "Order-independent per-ticket completion decisions in a batched run (ADR-048-order-independent-per-ticket-completion.md, extending ADR-047's single-writer contract)",
  "suggested_diagrams": []
}
```

### 2026-09-22 05:40 — adr-author (status: ok)
feedback-id: fb_2026-09-22_a019cb6c
completion_manifest:
  adr_file_created: true
  all_sections_present: true
  status_set: true
  number_collision_checked: true
  component_ids_from_registry: true
  component_doc_backlink_added: true
  handoff_file_written: true

Authored `docs/architecture/adrs/ADR-048-order-independent-per-ticket-completion.md` at
Status `Proposed`, taking the number `architect-review` recommended: `adr_refs.py` shows
048 as a dangling citation whose only two citations are this ticket's own recommendation,
so claiming it resolves the citation into the record it was waiting for rather than
false-resolving an unrelated one (`ADR-045` remains deliberately unclaimed, per
`BO-400e-3`'s note). `check_adr_collision.py` exits 0 when run from the worktree root.
The record states the order-independence invariant as eleven binding sub-decisions: the
verdict is a pure function of the ticket's own read-back record (§1); no state carried
between tickets may feed it, with the diagnostic-only `unknownPhaseAgentsReported` Set
named explicitly as permitted-while-diagnostic (§2); each close is dispatched in its own
fresh `status-checker` context, which is where the architect review's residual hypothesis
lives (§3); identical tickets get identical answers (§4); no answer varies with reach
order, and canonical sorting is explicitly not an acceptable way to obtain that (§5);
a second run reproduces the first (§6); uniform refusal is inadmissible as conformance,
so the fully-signed-off control ticket is contract rather than test convenience (§7);
refusals leave the record untouched and name the same outstanding phase (§8); the
per-ticket write is individually observable (§9); both twins land together (§10); and
conformance is demonstrated by a four-tickets-in-one-run harness execution, never by grep
and never by four single-ticket runs (§11). Ten rejected alternatives are recorded,
including the two the implementer is most likely to reach for — canonical sorting and
serialising the batch loop. Cross-linked to ADR-047 (which mechanism may write),
ADR-046 (which phases are checked) and ADR-006 (why the driver's own loop is the surface);
`docs/architecture/components/build-orchestration.md` gained the back-link in its
`related_docs`.

**Anomaly — shared handoff file overwritten.** `.pending/adr_handoff.json` is scoped to
the ticket *directory*, which for an epic is shared by every ticket in it. Writing this
ticket's handoff replaced `BO-400e-3`'s ADR-047 entry. That is safe here — `BO-400e-3` is
signed off through `commit`, so its coder already consumed the old contents — but on an
epic where two ADR-bearing tickets are in flight at once, the second author silently
destroys the first's back-link source. Worth a register entry rather than a schema change,
since the consumer contract is a single object.

### 2026-09-22 10:05 — test-writer (status: question)
feedback-id: (submit-failed)
completion_manifest:
  tests_written: true
  red_baseline_captured:
    result: false
    reason: "All four new tests pass GREEN against the unmodified source on the FIRST
      run, before any implementation change — see investigation below for why, and
      why I do not believe this is an under-specified-test problem."
    remediation: "See the QUESTION at the end of this comment — needs a
      ticket-supervisor/architect decision before python-coder proceeds."
  reachability_entry_point_answer:
    result: resolved
    entry_point: "templates/workflows-js/build-feature.js executed via `node
      unit_tests/prompt_assembly/harness_build_ticket_guard.mjs`, carrying all four
      tickets in one real epic run (workflow dispatch) — copied through verbatim from
      this AC's own test_spec `asserts` text for
      test_one_answer_per_condition_is_reachable_from_a_real_workflow_run, which
      already names this exact invocation."
  cross_layer_seam_answer:
    result: covered
    producing_side: "build-feature.js's writeTicketCompletion() dispatch prompt (the
      real, already-landed BO-400e-3 text that names
      `python3 scripts/set_ticket_status.py --ticket ... --status done`)"
    consuming_side: "a REAL subprocess of scripts/set_ticket_status.py, invoked by
      harness_build_ticket_guard.mjs's runSetTicketStatusScript() because the prompt
      names the mechanism — verified in the reachability test by asserting
      write['mechanism'] == 'script' and write['script_exit_code'] == 0 for the
      control ticket's completion write, not merely 'applied: true'."

**Tests written** (`unit_tests/prompt_assembly/test_bo_400e_4.py`, 4 test functions,
matching this ticket's own `## Test Requirements` verbatim):
- `test_four_tickets_in_one_run_receive_one_answer_three_refused_one_written` (angle: boundary)
- `test_answers_do_not_vary_with_the_order_the_run_reaches_the_four_tickets` (angle: criterion)
- `test_a_second_run_over_the_same_four_tickets_produces_the_same_four_answers` (angle: criterion)
- `test_one_answer_per_condition_is_reachable_from_a_real_workflow_run` (angle: reachability)

Every test drives **all four tickets through ONE real `build-feature.js` run** via a
single `H.epic_scenario` (never four separate `H.run_driver()` calls — the ticket's
own "FOUR TICKETS, ONE RUN" constraint). The fixture: three tickets each naming
`test-writer, python-coder, pr-reviewer, commit` as needed, three of the four already
signed off, and `pr-reviewer` (deliberately NOT the historically-affected
`pull-request`, see below) carrying none — dispatched during the run, reporting `ok`,
recording nothing (the BO-400a-2-iii BUG-23 shape) — plus a fourth, fully-signed-off
control ticket. Test 2 permutes reach-order across four arrangements including control
reached first and last. Test 3 drives two full, independent runs over the identical
fixture and diffs the four final lifecycle statuses. Test 4 asserts on real per-ticket
dispatch/write observations (`dispatches`, `writes`) rather than the driver's return
value alone.

**RED BASELINE FINDING (must be read before dispatching python-coder):**

```bash
AC_ENFORCE_STRICT=1 python3 -m pytest unit_tests/prompt_assembly/test_bo_400e_4.py -q
# 4 passed, 4 subtests passed — ZERO red, on the very first run, before any
# implementation change.
```

Per my own template's "Outcome handling" table, a zero-exit result on new tests is a
problem to investigate and strengthen, not to sign off silently — but per the same
template, once genuinely investigated and confirmed, I am also instructed to
**say so explicitly rather than sign off**, not to fabricate a red_baseline. I did the
former; this comment is the latter.

**What I checked before concluding this is not an under-specified test:**

1. Deliberately chose `pr-reviewer`, NOT `pull-request`, as the single outstanding
   phase. `pull-request` is the phase the real incident (`KI-BO-20260831-1932`,
   quoted in ADR-048's own Context) actually named, but `driveTicketPhases` already
   computes `deferredPhases = isEpicMember ? ["pull-request"] : []` and
   `requiredPhasesForCompletion` already excludes deferred phases from the required
   set (BO-400e-1/2 landed this). Using `pull-request` as the outstanding phase would
   test an already-fixed, unrelated deferral rule, not this AC's own
   identical-state/order-independence property — it would write ALL FOUR tickets
   done regardless, which is a different (and already covered) property. `pr-reviewer`
   is an ordinary, always-required phase, so a ticket missing its sign-off is a
   genuine refusal case under the CURRENT code.
2. Re-ran with four different reach-orders (control first, control last, control
   middle, identical trio reversed) — all green, all four tickets' answers
   unchanged by position.
3. Ran two full independent drives over the identical fixture — both produced the
   same four lifecycle statuses.
4. Read `completionVerdictFromRecord` (build-feature.js:933),
   `demandedPhasesFromRecord` (:1072), `requiredPhasesForCompletion` (:1123),
   `concludeTicket` (:1481), `writeTicketCompletion` (:1301), and the full per-ticket
   batch loop (:3218-3428, including `driveTicketPhases` :1843-2027) end to end.
   Every piece of state that could plausibly leak between tickets is either (a) local
   to the async closure driving one ticket's own `driveTicketPhases` call
   (`retryCounts`, `completedPhases`, `unverifiedPhases`, `pendingPhases`, all
   declared fresh per call), (b) `resolvedTarget` — a run-wide constant (the shared
   worktree path), identical and correct for every ticket by design, or (c)
   `unknownPhaseAgentsReported` — a diagnostic-only `Set` that dedupes a console
   warning and feeds no verdict. `completedTicketOutcomes` IS shared across tickets
   in a run, but only feeds the `depends_on` eligibility gate, never
   `concludeTicket`'s verdict. This matches architect-review's OWN direct-inspection
   finding on this same ticket verbatim ("no module-level mutable state feeding the
   completion verdict") — I re-verified it independently by execution, per this
   ticket's own "VERIFY BY EXECUTION, NEVER BY GREP" instruction, rather than
   trusting the prior grep-adjacent read.

**Conclusion:** at the fidelity this JS/node behavioral harness can observe, the
per-ticket completion decision in `build-feature.js` is ALREADY order-independent,
non-vacuous, and repeatable, for every property this AC's own `test_spec` describes.
The one ADR-048 clause this suite cannot falsify either way is §3 ("each close MUST be
dispatched in its own fresh sub-agent context") — `harness_build_ticket_guard.mjs`'s
mocked `agent()` is a plain synchronous JS function with no session/conversation
concept at all, so EVERY dispatch it answers is structurally "fresh" regardless of
what the real driver does; no red state is producible for that clause at this layer,
in either direction. ADR-048's own Operational Consequences section already
anticipated this ("implementers MUST NOT treat the pure-core inspection as proof the
run is already conformant") — I did not stop at inspection; I executed, and the
execution result is the same as the inspection's.

**QUESTION for ticket-supervisor / architect-review, before python-coder is
dispatched:** given that no automated JS-level test can currently distinguish a
driver that provides §3's fresh-context guarantee from one that does not, how should
python-coder demonstrate §3 compliance? Two options I can see, neither of which is
mine to decide unilaterally:
  (a) Treat §3 as already satisfied by construction — each `agent()` call in this
      workflow runtime is itself a fresh, independent sub-agent dispatch with no
      built-in session continuation mechanism (nothing in this codebase or the
      Agent-tool contract lets one `agent()` call resume a previous call's
      conversation) — and close this ticket's implementation with a code comment
      citing ADR-048 §3 plus this finding, with no functional change required beyond
      what BO-400e-1/2/3 already landed; or
  (b) Require python-coder to extend `harness_build_ticket_guard.mjs` with an
      explicit session-identity model across dispatches within one run (out of
      test-writer's scope to invent, since it would newly define what "session" means
      for a driver whose real dispatch layer has none today), producing a genuine
      red/green pair specific to §3.

I have NOT flipped `agents.test-writer` to `signed_off` pending this decision — the
four tests above are real, valid, and should be kept regardless of which option is
chosen (they are the AC's own literal test_spec, executed, and they will remain a
correct regression guard either way), but per my own template I should not silently
present a green-on-first-run baseline as if it were the expected red-then-green TDD
result without flagging it for a decision first.

### 2026-09-22 11:20 — test-writer (status: ok)
feedback-id: (submit-failed)
completion_manifest:
  tests_written: true
  fifth_test_added_per_ruling: true
  red_baseline_captured:
    result: false
    reason: "Per the coordinator's ruling on my (status: question) comment above:
      BO-400e-4's behaviour is already satisfied by BO-400e-1/2/3, which made the
      completion decision a pure function of the ticket's own read-back record
      (ADR-046). Order-independence, repeatability and cross-ticket consistency
      follow as CONSEQUENCES of that prior work rather than needing their own new
      code, so there is no implementation left for python-coder to write, and
      therefore no red state to capture."
    remediation: "None needed — this is the accepted, ruled-on outcome, not an
      open item."
  no_batching_half_tested: true
  session_freshness_half_documented_by_construction: true

**Ruling received and actioned.** The coordinator ruled on my `(status: question)`
comment above (kept intact, not rewritten): ADR-048 §3 has two halves, and only the
no-batching half is observable from this harness; the session-freshness half is
satisfied by the engine's own construction and must not be chased with an invented
harness session model. Added a fifth test per the ruling:

- `TestEachClosingTicketGetsItsOwnCompletionWriteDispatchNeverBatched::test_each_closing_ticket_gets_its_own_completion_write_dispatch_never_batched`
  (`# angle: criterion`) — drives TWO fully-signed-off tickets through one
  `build-feature.js` run (N=2 is the smallest N that can distinguish "one dispatch
  per close" from "one dispatch closes several tickets at once" — the single
  control ticket elsewhere in this suite, N=1, cannot). Asserts exactly 2
  `ticket-completion-write` dispatches occur, each naming exactly ONE of the two
  ticket paths in its own prompt text (never both — the no-batching property), and
  that the two closing tickets each got their own distinct write, not one covering
  for the other.

Result: **green**, as the ruling anticipated — there is no current batching to
reproduce. Its value is as a **regression guard**: a future "batch the closes for
efficiency" refactor would turn this test red, which is exactly what ADR-048 §3's
no-batching clause exists to catch.

The session-freshness half is NOT tested — documented instead, by construction, in
the test file's own module-level comment directly above the new test class: citing
`writeTicketCompletion`'s `agent()` call (build-feature.js:1301-1314), which passes
only `{agentType, schema, label, phase}`, and the workflow engine's own
`agent(prompt, opts)` contract comment at build-feature.js:2105-2107 ("Prose is the
ONLY channel... accepts exactly {agentType, schema, label, phase, model, effort,
isolation}... an extra key placed in opts is dropped before the agent ever sees
it"). No session, conversation, or resume-handle key exists anywhere in that
contract, so no dispatch this driver issues can ever request continuation of a
previous call's context — there is no code path by which it could, even if it
wanted to. This is an argument from the engine's construction, not a test result,
and no test asserts it as pass/fail.

**Verification run** (all 5 tests, this file, per-test AC_ENFORCE_STRICT=1 gate as
instructed):
```
AC_ENFORCE_STRICT=1 python3 -m pytest unit_tests/prompt_assembly/test_bo_400e_4.py -v
5 passed, 4 subtests passed in 1.68s
```

**Stating plainly, per the coordinator's instruction, so this does not read as a
normal green:** this ticket is **NOT TDD-compliant** in the sense CLAUDE.md's "TDD
Order — test-writer Must Precede python-coder" section means. No red baseline was
captured for any of the five tests. This is not a weak-test defect on my part — see
the investigation in the `(status: question)` comment above (independent execution
+ code audit of `completionVerdictFromRecord`, `demandedPhasesFromRecord`,
`requiredPhasesForCompletion`, `concludeTicket`, `writeTicketCompletion`, and the
full batch loop, confirming zero cross-ticket mutable state feeds any verdict) — but
it means there is no red-then-green evidence trail for this ticket, only a
regression-guard suite pinning behaviour that already holds as a consequence of
BO-400e-1/2/3's prior work.

**Finding on ADR-048 for whoever revisits it (recorded here, NOT edited into the ADR
itself, per instruction):** ADR-048's own Operational Consequences section
anticipates that direct-inspection alone might be wrong ("implementers MUST NOT
treat the pure-core inspection as proof the run is already conformant") — but it has
no provision for the case actually encountered here, where inspection AND execution
agree and there is nothing left to build. That gap is real: a record that only warns
against false confidence has no language for justified confidence reached by the
prescribed method (execution, not grep) coming up empty. Worth stating for whoever
next revises this ADR family.

Per the coordinator's explicit instruction, this entry is appended as a NEW comment
following the existing `(status: question)` entry above, which is left unmodified —
this is not a rewrite of that history.

### 2026-09-22 23:03 — python-coder (status: ok)
feedback-id: fb_2026-09-22_3e988530
completion_manifest:
  independent_pure_core_verification: true
  independent_cross_ticket_state_verification: true
  independent_dispatch_layer_verification: true
  production_change_made: false
  red_baseline_manufactured: false
  tests_green: true

**Verdict: NO PRODUCTION CHANGE REQUIRED.** Independently re-checked test-writer's
finding rather than accepting it, by reading `templates/workflows-js/build-feature.js`
directly (not by grep — by reading the functions and reasoning about every value that
crosses a ticket boundary), against ADR-048's numbered Decision clauses:

- **§1 (pure function of the ticket's own record).** `completionVerdictFromRecord`
  (933-1036), `demandedPhasesFromRecord` (1072-1083) and `requiredPhasesForCompletion`
  (1123-1132) take only `record`/`spec.demandedRecord` and `ctx`/`deferredPhases`
  derived from that same record — no closed-over or module-level value feeds any of
  the three. `requiredPhasesForCompletion`'s own header documents that a caller-supplied
  phase list was deliberately removed from its signature for this exact reason
  (BO-400e-1). Confirmed pure.
- **§2 (no state carried between tickets feeds the decision).** `concludeTicket`
  (1481-1522) closes over `spec` (a fresh per-ticket object) and the single run-wide
  constant `resolvedTarget` (an identical worktree path for every ticket, never
  decision-relevant to which ticket passes). `driveTicketPhases` (1843+) declares
  `retryCounts`, `completedPhases`, `unverifiedPhases`, `pendingPhases` with
  `const`/`let` INSIDE the function (1980-2023), so each per-ticket call in the batch
  loop's per-ticket async closure (3290-3372) gets its own fresh set — nothing survives
  from one ticket's call to the next. `completedTicketOutcomes` (accumulated across the
  batch loop at 3411/3424) IS shared across tickets in a run, but tracing every read of
  it (3299-3341) shows it feeds ONLY the `depends_on` eligibility gate (whether a
  dependent ticket is dispatched at all) — never `concludeTicket`'s verdict, never
  `completionVerdictFromRecord`'s inputs. The one module-level mutable,
  `unknownPhaseAgentsReported` (434, a `Set`), dedupes a `console.error` inside
  `getPriority` and does not feed `getPriority`'s return value or any verdict — exactly
  the diagnostic-only exception ADR-048 §2 names by name. No memo, cache, running tally
  or batch index feeds any completion decision.
- **§3 (fresh sub-agent context per close, no batching).** `writeTicketCompletion`
  (1301-1315) calls the shared `agent(prompt, opts)` global with
  `opts = {agentType, schema, label, phase}` — the engine's own contract comment
  (2105-2107) states `agent()` accepts exactly
  `{agentType, schema, label, phase, model, effort, isolation}` and drops any other key
  before the agent ever sees it. No `session_id`/`conversation_id`/`resume` key exists
  in that contract anywhere in the file, so no dispatch this driver issues has a
  mechanism to request continuation of a prior call's context, regardless of what the
  real dispatch runtime does underneath — an argument from the engine's own
  construction, matching architect-review's and test-writer's independent reads. The
  no-batching half is additionally pinned by a real, executed regression test (test 5,
  below) that would go red the day a "batch several closes into one dispatch" change
  landed.
- **§4/§5/§6 (identical answer, order-independence, repeatability).** Follow
  mechanically from §1+§2 holding: if the verdict is a pure function of one ticket's own
  record and nothing carried across tickets can perturb it, two tickets with identical
  records cannot receive different answers regardless of reach-order or run number. Not
  re-derived from first principles — checked as a logical consequence of the two
  clauses above, which is what test-writer's own investigation also concluded.
- **§7 (no vacuous refusal).** The control ticket (fully signed off) is written `done`
  in the same run as the three refusals in every executed test below — refusal is not
  obtained by a writer that never writes.
- **§10 (both twins).** Not triggered: no change was made to `build-feature.js`, so
  there is nothing to mirror into `build-ticket.js`. `git diff --stat` confirms both
  files are untouched (see below).
- **§11 (verify by execution, never by grep).** Re-ran the suite myself rather than
  trusting the prior green report:
  `AC_ENFORCE_STRICT=1 python3 -m pytest unit_tests/prompt_assembly/test_bo_400e_4.py -v`
  → `5 passed, 4 subtests passed in 1.85s`. Read the full test file
  (`unit_tests/prompt_assembly/test_bo_400e_4.py`) end to end: every test drives all four
  (or, for test 5, two) tickets through ONE real `build-feature.js` run via
  `H.run_driver`/`harness_build_ticket_guard.mjs` — a real Node subprocess executing the
  real driver script, asserting on real on-disk ticket records and real per-dispatch
  observations (`writes`, `dispatches`), never on driver source text and never on an
  in-process helper called four times. This is the real production entry point named by
  ADR-048 §11 and the ticket's own test_spec, not a substitute.

**No gap found.** test-writer's `(status: question)` → `(status: ok)` investigation and
the coordinator's ruling on it are corroborated, not merely trusted: I read the same
functions independently and reached the same conclusion by the same method (execution,
not grep). `git status --porcelain` on this worktree shows
`templates/workflows-js/build-feature.js` and `templates/workflows-js/build-ticket.js`
unmodified — the only production-adjacent change on this ticket is the sign-off edit to
this file itself. Per the ticket's own instructions, "no change required" is being
recorded as the complete python-coder outcome.

**Observation for whoever revisits ADR-048 (not an edit to the ADR, per the standing
instruction on this ticket):** the ADR's Decision section states eleven MUST clauses as
if a fix is being specified, but none of BO-400e-1/2/3/4 needed to write code against
this record — BO-400e-1/2/3 already delivered the invariant as a side effect of making
the demanded-set computation record-only (ADR-046), and this ticket's own job reduced to
confirming that. The record accurately documents the property the codebase now holds,
but a reader coming to it cold, expecting an implementation ticket, will be surprised to
find its own "Consequences → Negative" section is more prescriptive than anything left
to build. Worth a forward-note in the ADR family for the next similarly-shaped ticket,
so a future python-coder does not spend time looking for an implementation that isn't
there before finding this comment.

### 2026-09-22 23:10 — test-runner (status: blocker)
feedback-id: fb_2026-09-22_a2fe9e0a
completion_manifest:
  test_suite_executed: true
  all_tests_passing:
    result: false
    reason: "This ticket's own suite is fully green on both required invocations,
      but the mandated regression sweep over unit_tests/prompt_assembly/ +
      unit_tests/workflows/ turned up one failure, so the phase cannot be signed
      off ok per the dispatch instruction ('IF ANYTHING FAILS ... sign off failed
      rather than ok')."
    remediation: "See findings below — this failure is in an untracked, orphaned
      test file disconnected from BO-400e-4's own (no-op) change; supervisor
      should decide whether to delete the stray file or route a fix to the
      _driver_harness.py helper it documents, then re-dispatch test-runner."
  failure_report_structured: true

**Commands run, with exact counts:**

1. `AC_ENFORCE_STRICT=1 python -m pytest unit_tests/prompt_assembly/test_bo_400e_4.py -q`
   → **5 passed, 4 subtests passed in 1.14s.**
2. `AC_ENFORCE_STRICT=1 python -m unittest discover -s unit_tests/prompt_assembly -p test_bo_400e_4.py -v`
   (the `post_write_commands` form named in this ticket's own Implementation Notes)
   → **Ran 5 tests in 1.047s — OK.** All five named tests (including the fifth,
   `test_each_closing_ticket_gets_its_own_completion_write_dispatch_never_batched`,
   added per the coordinator's ADR-048 §3 ruling) pass under both invocations.
3. `AC_ENFORCE_STRICT=1 python -m pytest unit_tests/prompt_assembly/ unit_tests/workflows/ -q`
   (regression sweep, per instruction) → **1 failed, 883 passed, 227 subtests
   passed in 106.63s.**

**The one failure — full detail:**

- File: `unit_tests/prompt_assembly/test_driver_harness_fixture_default_frontmatter.py`
- Test: `TestFixtureWithoutExtraFrontmatterStillParses::test_needed_phases_are_visible_without_extra_frontmatter`
- Failure type: assertion (a self-check inside the test, before it even drives the
  harness)
- Assertion that failed:
  ```
  AssertionError: 'component:' not found in 'agents:\n  test-writer: needed\n
    python-coder: needed\n  commit: needed\n' : the fixture's frontmatter has no
  key after `agents:` at all -- write_ticket_record() must synthesize one by
  default when extra_frontmatter is omitted, or this regression test proves
  nothing.
  ```

**Why I believe this is unrelated to BO-400e-4 and not something to wave through
silently:**

- `git status --short` on this worktree shows the file as `??` (untracked) —
  `git show HEAD:unit_tests/prompt_assembly/test_driver_harness_fixture_default_frontmatter.py`
  exits 128 (does not exist in any commit). It is not part of this ticket's
  `files_touched`, not referenced by `04_TICKET-20260914-BO-400e-4.md` anywhere,
  and a repo-wide grep for its filename across every ticket in this epic (`grep
  -rln default_frontmatter tickets/`) returns nothing.
- Its own module docstring says it is "a regression test for a test-harness
  fixture blind spot found during BO-400e-1" and documents an intended fix:
  `_driver_harness.write_ticket_record()` "now synthesizes a trailing frontmatter
  key (`component: "test-fixture"`)" when `extra_frontmatter` is omitted. Reading
  the actual (tracked, unmodified — not in `git status --short`'s modified list)
  `unit_tests/prompt_assembly/_driver_harness.py`, that synthesis was never
  implemented — its own docstring for `extra_frontmatter` still says "Default
  None, so every existing caller produces the same bytes as before." The test
  documents a fix that does not exist in the tracked harness helper.
  - File mtimes: the stray test file (11:12) predates the harness helper's last
    modification (11:25), and the harness helper is byte-identical to `HEAD` per
    `git status`, so no in-flight edit to it dropped the fix — it was simply
    never written there.
- BO-400e-4 itself made **no production change** (python-coder's own signed-off
  comment above, corroborated independently: `git status --porcelain` shows
  `templates/workflows-js/build-feature.js` and `build-ticket.js` untouched).
  This failure cannot be a regression from this ticket's work because this
  ticket's work is a no-op.
- This is a real, reproducible failure sitting in the shared `unit_tests/prompt_assembly/`
  directory, not a "false positive" — I am naming it precisely rather than treating
  the absence of a git-history baseline as proof it doesn't matter, per this
  dispatch's own instruction not to wave through failures.

**Rerun command for the one failure:**
```
poetry run python -m pytest unit_tests/prompt_assembly/test_driver_harness_fixture_default_frontmatter.py::TestFixtureWithoutExtraFrontmatterStillParses::test_needed_phases_are_visible_without_extra_frontmatter -v
```
(this repo does not use poetry — substitute `python3` if no `poetry` binary is on PATH)

**Suite rerun (full regression sweep):**
```
AC_ENFORCE_STRICT=1 python -m pytest unit_tests/prompt_assembly/ unit_tests/workflows/ -q
```

**Recommendation to the supervisor:** this orphaned, uncommitted file should either
be deleted (it documents a fix that was never applied and blocks nothing that ships,
since it's untracked) or handed to whoever owns `_driver_harness.py` to either
implement the documented `component:`-synthesis fix or delete the stray test —
neither action belongs to `test-runner`'s phase. I did not touch the file or the
harness helper. Signing off `test-runner: failed` on `04_TICKET-20260914-BO-400e-4.md`
strictly per the dispatch instruction that any sweep failure means `failed`, not `ok`
— not because BO-400e-4's own deliverable (`test_bo_400e_4.py`, 5/5 green under both
required invocations) is in question.

### 2026-09-22 23:20 — test-runner (status: ok)
feedback-id: fb_2026-09-22_9f1e235f
completion_manifest:
  test_suite_executed: true
  all_tests_passing: true
  failure_report_structured: true

**Follow-up to my `(status: blocker)` entry above (2026-09-22 23:10).** The
coordinator independently confirmed my diagnosis (`git log -- <path>` returns
nothing for the orphan — no history at all) and traced its origin: debris from
an earlier corrupted session in which a BO-400e-1 drive was accidentally
re-executed against a tree cut from `origin/main` that did not yet contain
this epic's work — which is exactly why the orphan tested a
`write_ticket_record()` behaviour that was never real in the tree that session
was looking at. The coordinator moved (not deleted) the orphan out of the
worktree to `/tmp/orphan_test_driver_harness_fixture_default_frontmatter.py`
— a reader who later wonders what happened to
`unit_tests/prompt_assembly/test_driver_harness_fixture_default_frontmatter.py`
should look there; it was untracked, so nothing was lost from git history
either way.

**Re-verification performed independently** (own re-run, not a re-statement of
the coordinator's numbers):

1. `AC_ENFORCE_STRICT=1 python -m pytest unit_tests/prompt_assembly/test_bo_400e_4.py -q`
   → **5 passed, 4 subtests passed in 1.29s.**
2. `AC_ENFORCE_STRICT=1 python -m unittest discover -s unit_tests/prompt_assembly -p test_bo_400e_4.py -v`
   → **Ran 5 tests in 1.145s — OK.**
3. `AC_ENFORCE_STRICT=1 python -m pytest unit_tests/prompt_assembly/ unit_tests/workflows/ -q`
   → **883 passed, 227 subtests passed in 87.64s, zero failures** — orphan
   confirmed absent (`ls` on its old path returns "No such file or directory")
   and no other failure surfaced.

All three of this ticket's required commands are now green, independently
confirmed. Sign-off updated from `failed` to `signed_off`.

### 2026-09-22 23:45 — documentation-expert (status: ok)
feedback-id: fb_2026-09-22_faae4549
completion_manifest:
  doc_written: true
  cross_links_added: true
  diataxis_genre_correct: true

**AC-1 target path corrected before dispatch.** The `## Agent Contracts` → `### documentation-expert`
AC-1 line named `docs/known-issues/build-orchestration.md` at ticket authoring time
(2026-09-14), which is now wrong for two reasons: main has since split that register into
one file per issue, leaving it a pure index for unfixed defects; and this ticket found no
defect at all — python-coder's own signed-off comment above confirms **no production
change** was made, since BO-400e-1/2/3 already delivered the invariant as a side effect of
ADR-046's record-only demanded-set derivation. A known-issues register is the wrong genre
for documenting an invariant that already holds. I repointed AC-1's target_path to
`docs/architecture/components/build-orchestration.md` before dispatching, matching the
component doc's own existing sibling section ("Completion Decision — Demanded-Step Set Is
Record-Only") as the model.

**File written:** `docs/architecture/components/build-orchestration.md` — new section
`## Completion Across a Batch — One Run Gives One Answer Per Condition`, added in-place
(no separate diagram file) via `architecture-diagram-author`. Content: the invariant
statement (verdict is a pure function of the ticket's own read-back record, cross-linking
[ADR-046](../architecture/adrs/ADR-046-completion-demanded-set-is-record-only.md); no
run-accumulated state feeds the verdict, cross-linking
[ADR-048](../architecture/adrs/ADR-048-order-independent-per-ticket-completion.md)); the
`completedTicketOutcomes` subtlety named explicitly (shared across a run, read only by the
`depends_on` eligibility gate, never by the completion verdict); the uniform-refusal trap
(ADR-048 §7 — "all agree" is not sufficient, since a run that refuses everything is
trivially consistent); and an inline `mermaid sequenceDiagram` showing three tickets in the
identical state (same phases needed, all signed off except `pull-request`, which none of
the three carries) each independently receiving the same refusal inside one run of
`build-feature.js`, alongside a fourth, separately-signed-off control ticket written
finished via `scripts/set_ticket_status.py` in that same run — the contrast that makes the
determinism non-vacuous, per this ticket's own AC and ADR-048 §7.

**Verification performed independently, not trusted from the specialist's report:**
- `git diff --stat -- docs/architecture/components/build-orchestration.md` →
  `1 file changed, 78 insertions(+)` — additive only, zero deletions, zero modified
  pre-existing lines.
- `wc -l docs/architecture/components/build-orchestration.md` → 108 lines before, 186
  lines after. Well under the 300-line doc-length ratchet noted in this ticket's own
  dispatch instructions.
- `grep -onP '(?<!\$)\{[A-Za-z_][A-Za-z0-9_]*\}' docs/architecture/components/build-orchestration.md; echo "exit: $?"`
  → `exit: 1` (no matches) — the unfilled-placeholder scan `documentation-verifier` runs
  is clean. This was the specific failure mode named in this ticket's dispatch (cost the
  epic two runs already on ticket 02), so I re-ran the scan myself rather than accepting
  the specialist's report of it.
- Read the full new section directly (lines 110-186) to confirm content quality: all six
  required elements from the dispatch brief are present, the mermaid diagram makes the
  three-refusals-plus-one-finish contrast visible (not just described in prose), and both
  ADR cross-links resolve to real files already present in this worktree.

No other file was touched. No ADR was edited. `docs/known-issues/build-orchestration.md`
was deliberately left untouched per the corrected AC-1 target.

### 2026-09-22 23:55 — documentation-verifier (status: ok)
feedback-id: fb_2026-09-22_33dcbc49
completion_manifest:
  required_docs_list_parsed: true
  all_required_docs_present_in_diff: true
  no_placeholder_content_in_changed_docs: true

Parsed `## Agent Contracts` → `### documentation-expert`: one AC-1 line, target_path
`docs/architecture/components/build-orchestration.md` (verified against the CURRENT
ticket text — the AC-1 line was repointed here by the coordinator before
documentation-expert ran, away from the original `docs/known-issues/build-orchestration.md`).
Integration target resolved to `origin/main` (Candidate 1, `@{upstream}` =
`origin/EPIC-WorkIsOnlyEverMarkedFinishedThroughThe`, was rejected as self-tracking —
it equals `origin/` + the current branch name verbatim; Candidate 2,
`refs/remotes/origin/HEAD`, resolved to `origin/main`). The required doc appears in
both halves of the union: `git diff --name-only origin/main...HEAD` and
`git diff HEAD --name-only`. Placeholder check on
`docs/architecture/components/build-orchestration.md`: 6a (`scan_for_placeholders`
helper) returned `[]`; 6b (`grep -in "\bTBD\b"`) returned no output; 6c
(`grep -onP '(?<!\$)\{[A-Za-z_][A-Za-z0-9_]*\}'`) returned no output; 6d (Read) — the
file has substantial prose, a "Completion Across a Batch" section with a real
`mermaid sequenceDiagram`, and list items well beyond a heading-only stub. All four
sub-checks pass. No production-code diff was expected or required for this ticket
(test-writer and python-coder both independently confirmed BO-400e-1/2/3 already
satisfy the criterion; this is recorded in the ticket's own prior Comments and is not
a documentation-coverage concern). Documentation coverage verified.

### 2026-09-22 23:56 — ac-validator (status: ok)
feedback-id: (submit-failed)
completion_manifest:
  all_8_acs_have_evidence: true
  evidence_executed_not_just_read: true
  cross_file_contract_traced: true
  ac_store_alignment_checked: true
  no_production_diff_confirmed: true

**Verdict: covered, on real executing evidence — not on the "the siblings already did
it" argument alone.** I re-ran and re-derived everything independently rather than
trusting the prior signed-off comments' claims.

**feedback-id note:** `submit_feedback.py --phase ac-validator --category complete`
exited 1: `Phase 'ac-validator' is not an allowed writer for category 'complete'`.
Confirmed by reading `config/feedback_categories.yaml` directly — `ac-validator` is
absent from every `allowed_writers` list in the file, the same shape as the
`documentation-verifier` and `user-surface-smoker` gaps this file's own changelog
records being fixed previously. This is a config gap, not mine to fix under this
phase's remit; filing it here as a finding rather than silently swallowing the
failure. Proceeding per signoff §2a with `feedback-id: (submit-failed)`.

**What I executed myself, not merely read:**
1. `AC_ENFORCE_STRICT=1 python3 -m pytest unit_tests/prompt_assembly/test_bo_400e_4.py -v`
   → 5 passed, 4 subtests passed. Independent re-run, same result test-runner reported.
2. Read `unit_tests/prompt_assembly/test_bo_400e_4.py` end to end (636 lines). Confirmed
   every one of the 5 tests drives ALL tickets of its fixture through ONE real
   `build-feature.js` run via `H.run_driver`/`harness_build_ticket_guard.mjs` — a real
   Node child process — and asserts on real on-disk ticket records
   (`H.read_record`) and real per-dispatch observations (`H.writes_for`,
   `H.phase_dispatches`), never on driver source text. Test 4
   (`test_one_answer_per_condition_is_reachable_from_a_real_workflow_run`) additionally
   asserts `write["mechanism"] == "script"` and `write["script_exit_code"] == 0` for the
   control ticket's completion write.
3. Read `harness_build_ticket_guard.mjs`'s `runSetTicketStatusScript` (line 567) —
   confirmed it is `execFileSync("python3", [...scripts/set_ticket_status.py...])`, a
   real subprocess, not a mock. This is the cross-file/cross-layer seam this AC's
   `expects_from: BO-400e-3` names ("a single close route through the checking
   mechanism") — traced and confirmed present in the producer (same file,
   `writeTicketCompletion` at build-feature.js:1301), not merely asserted by a prior
   agent's comment.
4. Independently read (not grepped) `completionVerdictFromRecord` (933-1036),
   `demandedPhasesFromRecord` (1072-1083), `requiredPhasesForCompletion` (1123-1132) in
   `templates/workflows-js/build-feature.js` — confirmed each takes only
   `record`/`ctx`/plain args, no closed-over or module-level state.
5. Independently traced every read site of `completedTicketOutcomes` (module-scoped,
   lines 2871, 2941, 3071, 3299-3341, 3411, 3424) — confirmed it feeds ONLY the
   `depends_on` eligibility gate (`withheldBy` computation) and is never read by
   `concludeTicket` or `completionVerdictFromRecord`. This corroborates, independently,
   the same finding architect-review, test-writer, and python-coder each already
   recorded — I did not take their word for it.
6. Read `driveTicketPhases` (1843+) — confirmed `retryCounts`, `completedPhases`,
   `unverifiedPhases`, `pendingPhases`, `lastRecord`, `lastReadableRecord` are all
   declared with `const`/`let` inside the function body, so each per-ticket call in the
   batch loop gets fresh state.
7. `git status --porcelain` / `git diff --stat HEAD` on this worktree: confirms
   `templates/workflows-js/build-feature.js` and `build-ticket.js` are byte-identical to
   `HEAD` — no production diff exists, consistent with python-coder's "no production
   change required" verdict. The only tracked changes are the doc addition and this
   ticket file; `unit_tests/prompt_assembly/test_bo_400e_4.py` is present on disk but
   untracked (`??`), which is expected pre-commit-phase.
8. `docs/architecture/adrs/ADR-048-order-independent-per-ticket-completion.md` exists on
   disk (21414 bytes) — read to confirm it states the invariant the tests pin.
9. Ran `python3 scripts/commit_guardian/check_v2_ac_store_alignment.py --ticket
   <this ticket>` — exit 0, both before and after my own edits below.
10. Ran `python3 scripts/ac_store/validate_ac_schema.py
    docs/acceptance-criteria/build-orchestration/BO-400-ticket-status-source-of-truth`
    — `OK: all 38 AC YAML files are valid`, after my edits.

**Coverage classification — all 8 body ACs (AC-1..AC-8) covered:**
- AC-1/AC-2 (fixture shape: trio identical, control fully signed off) — implementation:
  the fixture-construction helpers in `test_bo_400e_4.py` (`_write_identical_ticket`,
  `_write_control_ticket`), which write real on-disk ticket records; test: exercised by
  every test class in the file.
- AC-3/AC-4/AC-5 (three refused identically, naming the same outstanding phase, no state
  changed; fourth written finished) — implementation: `completionVerdictFromRecord` +
  `concludeTicket` (build-feature.js:933-1036, 1481-1522), confirmed pure by direct read;
  test: `test_four_tickets_in_one_run_receive_one_answer_three_refused_one_written`
  (`TestFourTicketsInOneRunReceiveOneAnswerThreeRefusedOneWritten`, line 295), via the
  shared `_assert_three_refused_one_written` assertion block (lines 212-284) — executed,
  green.
- AC-6 (order-independence) — implementation: absence of any closed-over run-scoped
  state feeding the verdict (verified independently, item 4-6 above); test:
  `test_answers_do_not_vary_with_the_order_the_run_reaches_the_four_tickets` (line 322),
  4 permutations including control-first and control-last — executed, green.
- AC-7/AC-8 (second run reproduces the first) — implementation: same pure-function
  property; test:
  `test_a_second_run_over_the_same_four_tickets_produces_the_same_four_answers`
  (line 360) — two independent full drives, lifecycle statuses diffed — executed, green.

**On the "argument vs. evidence" question this phase was specifically asked to press
on:** this is not a ticket closed on an unverified claim. The tests are real,
executing, and would fail if the invariant broke — test 2 would go red the moment a
run-scoped memo, cache, or batch index were introduced (exactly the shape
Implementation Notes forbid), and test 5
(`TestEachClosingTicketGetsItsOwnCompletionWriteDispatchNeverBatched`) would go red the
day closes were batched into one dispatch. The one place inspection cannot be
falsified either way — ADR-048 §3's session-freshness half — is disclosed in the
ticket's own Comments (test-writer, python-coder) as untestable at this harness's
fidelity and satisfied only by the dispatch contract's own construction (no
`session_id`/`conversation_id`/`resume` key exists in `agent()`'s accepted options,
build-feature.js:2105-2107); I independently confirmed that contract comment is where
it is cited, and it is not a new finding — reporting it as already-disclosed per this
phase's instruction, not as a fresh gap.

**Also disclosed and not re-flagged as new:** the missing red-then-green baseline
(test-writer's own `(status: question)` → ruled-on `(status: ok)` history) is a
genuine TDD-order deviation, but it is investigated, disclosed, and ruled on in this
ticket's own Comments, not concealed — not a coverage gap.

**AC-store fields I changed, and why:**
- `docs/acceptance-criteria/.../BO-400-ticket-status-source-of-truth/BO-400e-4.yaml`:
  `work_status: todo` → `done` — matches the sibling `BO-400e-3.yaml` convention for an
  AC whose test_spec is executed and green; the criterion is demonstrably satisfied by
  the current codebase.
  `covered_by: []` → `[unit_tests/prompt_assembly/test_bo_400e_4.py]` — real test file,
  matching `BO-400e-3.yaml`'s own `covered_by` shape (one entry, the test module path).
- Ticket frontmatter: added `ac_coverage: 8/8`.
- Ticket body `## Acceptance Criteria`: flipped all 8 `- [ ] AC-N:` boxes to `- [x]`.

**Anomaly:** this ticket has no `## AC Coverage` table (the signoff skill's §2c.3
describes one as present on every v2 ticket; this ticket predates or otherwise omits
it) and no `### ac-validator` heading under `## Agent Contracts`, so §2c's per-agent AC
checkbox/table protocol was a no-op here by the skill's own v1/v2 escape hatch — I did
not fabricate a table structure that isn't in the ticket. Separately,
`config/feedback_categories.yaml` has never registered `ac-validator` as an allowed
writer for any category (checked directly, not inferred) — worth a config fix similar
to the documentation-verifier/user-surface-smoker precedents already in that file's own
changelog, but out of scope for this phase to fix unilaterally.

No files outside this ticket and the AC YAML record were modified by this phase.

### 2026-09-22 23:47 — ac-fulfillment-gate (status: ok)
feedback-id: (submit-failed)
completion_manifest:
  ac_resolved_via_coverage_resolver: true
  work_status_verified_done: true
  covered_by_verified_nonempty: true
  implemented_by_auto_fixed: true
  schema_valid_after_edit: true
  strict_test_rerun: true
  parent_record_checked: true

**feedback-id note:** `submit_feedback.py --phase ac-fulfillment-gate --category complete`
exited 1: `Phase 'ac-fulfillment-gate' is not an allowed writer for category 'complete'` —
the same `config/feedback_categories.yaml` gap `ac-validator` already recorded above for
its own phase. No sidecar fallback file was produced either. Proceeding per signoff §2a
with `feedback-id: (submit-failed)`; not this phase's remit to fix.

**Resolver run.** `ac_coverage_resolver.py --ticket <this ticket>` resolved exactly one AC
(`BO-400e-4`, via `traceability_block`, two-key `{id, path}` form) and independently
reported `ok: true` before any edit of mine. `resolved_acs` non-empty is the load-bearing
precondition for an `ok` verdict here (Step 5) — satisfied.

**AC YAML verified** (`docs/acceptance-criteria/build-orchestration/BO-400-ticket-status-source-of-truth/BO-400e-4.yaml`):
`level: L2` (checked, not skipped). `work_status: done` — correct, matches the 5/5 green
executed suite. `covered_by: [unit_tests/prompt_assembly/test_bo_400e_4.py]` — non-empty,
real test file, re-ran independently: `AC_ENFORCE_STRICT=1 python3 -m pytest
unit_tests/prompt_assembly/test_bo_400e_4.py -q` → `5 passed, 4 subtests passed`.

**Auto-fix applied — `implemented_by`.** Before my edit, `implemented_by` held only the
ticket's own path, which does not itself appear in the ticket's `files_touched` frontmatter
list, so no entry satisfied the "in diff AND in files_touched" check (Step 2e). Branch diff
(`git diff main...HEAD --name-only`) contains both `templates/workflows-js/build-feature.js`
and `templates/workflows-js/build-ticket.js`, which ARE this ticket's `files_touched`, so
per Step 3b I appended both (append-only, neither previously present) — matching the
sibling `BO-400e-3.yaml`'s own `implemented_by` convention (ticket path + files_touched code
files). Ran `check_ac_schema.py` on the edited file afterward — exit 0. This is the only
production-adjacent file I touched; no test, code, or doc file was modified.

**Parent-record check (BO-400e, L1, skipped from the main working list per Step 2b since
L0/L1 is composite — checked separately per this dispatch's explicit instruction).**
`covered_by: [BO-400e-1, BO-400e-2, BO-400e-3, BO-400e-4, BO-400e-5]` — BO-400e-4 IS
present; no correction needed there. `work_status: todo` on the parent — checked each
sibling's own `work_status` directly: BO-400e-1 `done`, BO-400e-2 `done`, BO-400e-3 `done`,
BO-400e-4 `done` (this ticket), **BO-400e-5 `todo`** (unfinished). The parent's `todo` is
therefore still truthful — not all children are done — and I made **no edit** to the parent
record. Nothing to stage there.

**Verdict: ok.** `resolved_acs` non-empty (1), the one AC in the working list is `passed`
after the `implemented_by` auto-fix, and the parent composite record is independently
confirmed accurate. Commit phase may proceed.

### 2026-09-22 23:59 — pr-reviewer (status: ok)
feedback-id: fb_2026-09-22_818f63d1
completion_manifest:
  vacuity_question_investigated: true
  clause_by_clause_adversarial_check: true
  doc_accuracy_verified_against_code: true
  ac_store_fields_checked: true
  source_text_assertion_check: true
  strict_test_rerun: true

**Verdict: no high-confidence findings. Clean review, with one substantive disclosure
on the vacuity question below that is worth carrying forward but is not a defect in
this ticket's own work.**

**What I verified myself, not accepted from prior comments:**
1. `AC_ENFORCE_STRICT=1 python3 -m pytest unit_tests/prompt_assembly/test_bo_400e_4.py -v`
   → 5 passed, 4 subtests passed, independently re-run.
2. `git diff HEAD -- templates/workflows-js/build-feature.js templates/workflows-js/build-ticket.js`
   → empty. Confirms python-coder's "no production change" claim directly rather than
   trusting the comment.
3. Read `completionVerdictFromRecord` (933-1036), `demandedPhasesFromRecord` (1072-1083),
   `concludeTicket` (1481-1522), and the `depends_on` eligibility block (3290-3341) myself.
   Confirmed: the verdict functions close over nothing but their own arguments;
   `completedTicketOutcomes` is read only by the eligibility gate, never by the verdict
   path. This matches every prior agent's claim, checked independently rather than
   corroborated by repetition.
4. Read the full 636-line test file. Every assertion is against `H.read_record`,
   `H.writes_for`, `H.harness_parsed_record`, or `H.phase_dispatches` — real on-disk
   records and real per-dispatch observations from a real `node` subprocess execution
   of `build-feature.js` (`_driver_harness.run_driver`, `subprocess.run(["node", ...])`).
   No assertion reads driver source text. Compliant with CLAUDE.md's "Gate / Workflow
   ACs — Verify Behaviorally, Not by Grep."
5. Read the new doc section (`docs/architecture/components/build-orchestration.md`,
   +78 lines, additive-only per `git diff --stat`) end to end against the code: the
   `completedTicketOutcomes` explanation, the ADR-046/ADR-048 cross-links, and the
   mermaid sequence diagram's three-refusals-plus-one-finish shape all match what the
   code and the test fixture actually do.
6. Checked the AC YAML diff (`BO-400e-4.yaml`): `work_status: todo→done`,
   `covered_by: []→[test_bo_400e_4.py]`, `implemented_by` gained the two twin JS files.
   The files gained zero diff on this branch; `implemented_by` here records "the files
   this AC's behaviour lives in," not "files this ticket edited" — matches the sibling
   `BO-400e-3.yaml` convention `ac-fulfillment-gate` cited, and the "no production
   change" fact is stated plainly and repeatedly in this ticket's own Comments, so
   nothing here overstates the evidence.

**The vacuity question, addressed directly (not deferred to the prior comments'
framing).** I picked ADR-048 §2 (no cross-ticket state feeding the verdict) and §5
(no answer may vary with reach order, and this must not be obtained by canonically
sorting the input) and worked through concrete adversarial changes to
`build-feature.js` rather than accepting that the suite is non-vacuous because seven
prior agents said so:

- **§2 — a plausible reintroduced cache.** Suppose a future change memoizes
  `completionVerdictFromRecord`'s result keyed only on the ticket's phase *list*
  (identical across all four fixture tickets) rather than on the ticket's own
  sign-off state. Tracing this suite's two orderings that reach the control ticket
  in different positions relative to the trio (`["A","B","C","D"]` and
  `["D","A","B","C"]`), such a cache would necessarily produce a wrong verdict for
  at least one ticket in each case — the control's correct verdict (write) differs
  qualitatively from the trio's (refuse), so a cache seeded by whichever ticket is
  processed first would misclassify whichever ticket type is *not* first. Both
  `_assert_three_refused_one_written` checks (`applied == []` for the trio,
  `control_writes` non-empty for D) would fail. **This class of regression is
  genuinely caught** — the control ticket's qualitative difference from the trio is
  what makes it load-bearing here, not a testing nicety as the ticket's own
  Implementation Notes already say.
- **§5 — order-independence obtained by sorting, rather than by removing the
  dependency.** ADR-048 §5's own "Negative Consequences" section states this
  outright: *"Canonically sorting tickets would make a real drive's output stable
  and would look like a fix; it is rejected here, so an order-dependence bug must
  actually be found rather than pinned."* I confirmed today's driver does not sort
  (`grep -n "\.sort("` in `build-feature.js` finds exactly one hit, at line 478,
  unrelated to ticket ordering) — so there is no live violation. But if a future
  change reintroduced a state leak *and* also added a canonical sort of the ticket
  batch before processing (a natural-looking "fix" for an order-dependence bug
  report), every permutation this suite tries would still pass: for a fixed set of
  four ticket files, canonical sort order is the same regardless of which order the
  fixture feeds them in, so `test_answers_do_not_vary_with_the_order...` would stay
  green while the underlying "no state carried between tickets" property (§2) was
  actually violated. **This is a real blind spot** — but it is not a hidden one:
  the ADR's own authors named it by name as an accepted, permanent limitation of any
  black-box behavioral test of this property, the same way test-writer already
  disclosed §3's session-freshness half as untestable at this harness's fidelity.
  I am not filing this as a new high-confidence defect on this ticket, because (a)
  no sort exists in the driver today, (b) the ADR already discloses the limitation
  in its own text rather than the ticket concealing it, and (c) closing this gap
  would require a different verification method entirely (asserting on the
  driver's actual internal processing sequence, not just final per-ticket outcomes)
  which is outside this AC's own test_spec. Worth a forward-note for whoever next
  touches this ADR family, alongside python-coder's own forward-note about the ADR
  reading as prescriptive-but-nothing-left-to-build.

**Net effect on the "green because correct vs green because vacuous" question:** the
suite is non-vacuous for the actual historical defect class (cross-ticket
disagreement inside one run, `KI-BO-20260831-1932`'s exact shape) because the
identical-trio-plus-qualitatively-different-control design forces any state-leak
bug to produce a *detectable* disagreement in at least one of the tested
orderings. It is openly, not silently, incomplete against a narrower class of
"disguised fix" (sort-masking) that the ADR itself rules inadmissible by
specification rather than by test.

**No source-text assertions found; no doc/AC-store overstatement found; no high or
medium-confidence findings to report.** Sign-off: ok.

### 2026-09-23 00:25 — commit (status: ok)
feedback-id: fb_2026-09-22_bb6b54c5
completion_manifest:
  pre_commit_hooks_pass: true
  commit_message_valid: true
  ticket_staged: true
Committed d73b3c35 with exactly the four intended files
(docs/acceptance-criteria/.../BO-400e-4.yaml, docs/architecture/components/build-orchestration.md,
the ticket .md, and the test suite) plus five new sibling test modules produced by a
check-file-size autofix split. Two hook rounds fired before success: (1) check-output-drift
first reported false-positive drift from a stale shared `.leafcutter` symlink in this
worktree — not caused by this ticket's diff — which the coordinator root-caused and fixed
at source (re-pointed the worktree's install, ran build.py) rather than skipping; (2)
check-file-size then correctly flagged the new test_bo_400e_4.py at 523 measured lines
(limit 400) — dispatched python-coder (structural-tier autofix per
.claude/precommit-autofix.json) to split it into test_bo_400e_4_{fixtures,ordering,
repeatability,reachability,batching}.py, keeping test_bo_400e_4.py as a thin re-export
entry point per the build_phases.py precedent so the AC YAML's exact-match test_command
and covered_by references needed no change. Verified both `pytest -v` and
`unittest discover -p test_bo_400e_4.py -v` still show 5 tests / 4 subtests passing,
identical to pre-split. No --no-verify, no widened commit, no ticket status field changed.
