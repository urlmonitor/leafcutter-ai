---
advances_current_outcome: true
agents:
  commit: signed_off
  documentation-expert: not_needed
  pr-reviewer: failed
  pull-request: signed_off
  python-coder: signed_off
  sql-coder: not_needed
  test-runner: signed_off
  test-writer: signed_off
complexity: medium
components:
- build_orchestration
change_target: config
created: '2026-07-15'
depends_on: []
files_touched:
- config/guardrail_gates.yaml
priority: medium
requires_adr: false
requires_diagram: false
risk_surface: internal
roadmap_phase: phase_1
source_ac: BO-2200a-1
status: todo
title: A declarative documentation-gates policy demands docs for user-facing, data,
  flow, and docs changes
---

# A declarative documentation-gates policy demands docs for user-facing, data, flow, and docs changes

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BO-2200a-1` — A declarative documentation-gates policy demands docs for user-facing, data, flow, and docs changes — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BO-2200a-1`. Component: `build-orchestration`. Assigned agent: `python-coder`. Estimated complexity: `M`. Complexity: `medium`.

## Acceptance Criteria

```gherkin
Given a declarative `documentation_gates` policy section exists in the guardrail
  gates configuration, and the ticket generator builds the agents map for a leaf AC,
When the AC's `change_target` is `ui`,
Then the generated agents map includes `documentation-expert`.
And the same result holds when `change_target` is `schema`, `pipeline`, or `docs`
  (each of these four values, on its own, causes `documentation-expert` to be required).
And the requirement is driven entirely by the declarative policy data, not by a
  value hard-coded in the generator, so adding or removing a triggering value is a
  configuration edit rather than a code change.
```

## Test Requirements

```yaml
tests:
- name: test_change_target_ui_triggers_documentation_expert
  file: unit_tests/test_bo_2200a_1.py
  covers:
  - BO-2200a-1
  asserts: Run generate_ticket_from_ac on a fixture AC with change_target=ui; the
    emitted ticket's agents map includes documentation-expert.
  framework: unittest
  type: behavioral
- name: test_each_triggering_change_target_requires_doc_expert
  file: unit_tests/test_bo_2200a_1.py
  covers:
  - BO-2200a-1
  asserts: change_target in {schema, pipeline, docs} each independently yields documentation-expert
    in the generated agents map.
  framework: unittest
  type: behavioral
- name: test_documentation_gates_is_data_driven_not_hardcoded
  file: unit_tests/test_bo_2200a_1.py
  covers:
  - BO-2200a-1
  asserts: Removing a value from the documentation_gates change_target_triggers policy
    makes that change_target no longer require documentation-expert, proving the trigger
    is config-driven.
  framework: unittest
  type: behavioral
```

## Implementation Notes

```yaml
config_schema_fragment:
  documentation_gates:
    properties:
      change_target_triggers:
        items:
          enum:
          - ui
          - schema
          - pipeline
          - docs
          type: string
        type: array
      risk_surface_triggers:
        items:
          enum:
          - contract_boundary
          - safety
          - auth
          - privacy
          type: string
        type: array
    type: object
constraints:
- Add a new declarative `documentation_gates` section to config/guardrail_gates.yaml;
  the ticket generator (_build_agents_map in scripts/ac_store/generate_ticket_from_ac.py)
  reads it to decide whether documentation-expert is required.
- This is the documentation-specific trigger policy layered on the BO-500 computed-quality-gate
  engine — do NOT re-derive the general gate-selection engine.
- The triggering change_target set must be data-driven {ui, schema, pipeline, docs};
  adding/removing a value is a config edit, not a code change (no hard-coded list
  in the generator).
n_location_rule: '1'
post_write_commands: []
reference_file_path: config/guardrail_gates.yaml
required_skills:
- python-coder
```

## Sign-offs

- [x] python-coder — 2026-07-17 10:15
- [x] test-writer — 2026-07-17 09:07
- [x] test-runner — 2026-07-17 09:23
- [ ] pr-reviewer — failed 2026-07-17 11:30
- [x] commit — 2026-07-17 09:39
- [x] pull-request — 2026-07-20 15:22

## Comments

### 2026-07-17 09:07 — test-writer (status: ok)
feedback-id: fb_2026-07-17_4147f830
completion_manifest:
  tests_written: true
  tests_are_red: true
  covers_all_ac_scenarios: true
All 3 test methods (5 assertion points via subTest) written in unit_tests/test_bo_2200a_1.py; verified red with 5 failures — AssertionError: 'needed' != None — confirming the documentation_gates read-path is not yet implemented in _build_agents_map.

## Test Writer — Completion Report

### Tests Written
| File | Directory | Framework | Status |
|---|---|---|---|
| test_bo_2200a_1.py | unit_tests/ | unittest | written |

### Verification Run
- Command: `python -m unittest /home/henzeh/projects/leafcutter/worktrees/BO-2200-doc-coverage/unit_tests/test_bo_2200a_1.py`
- Result: red (5 failures — expected; implementation not yet written)

### Notes
Pre-existing ticket schema violations (missing `change_target`, `risk_surface`, stale `depends_on: BO-2200a`) cause the frontmatter hook to fire on every edit — these are not introduced by test-writer and do not block the phase sign-off.

red_baseline:
  - test_name: test_change_target_ui_triggers_documentation_expert
    file: unit_tests/test_bo_2200a_1.py
    error: "AssertionError: 'needed' != None : Expected documentation-expert='needed' when change_target='ui' and documentation_gates.change_target_triggers includes 'ui'. Implement the documentation_gates read-path in _build_agents_map."
  - test_name: test_each_triggering_change_target_requires_doc_expert (change_target='schema')
    file: unit_tests/test_bo_2200a_1.py
    error: "AssertionError: 'needed' != None : Expected documentation-expert='needed' when change_target='schema' and documentation_gates.change_target_triggers includes 'schema'. Implement the documentation_gates read-path."
  - test_name: test_each_triggering_change_target_requires_doc_expert (change_target='pipeline')
    file: unit_tests/test_bo_2200a_1.py
    error: "AssertionError: 'needed' != None : Expected documentation-expert='needed' when change_target='pipeline' and documentation_gates.change_target_triggers includes 'pipeline'. Implement the documentation_gates read-path."
  - test_name: test_each_triggering_change_target_requires_doc_expert (change_target='docs')
    file: unit_tests/test_bo_2200a_1.py
    error: "AssertionError: 'needed' != None : Expected documentation-expert='needed' when change_target='docs' and documentation_gates.change_target_triggers includes 'docs'. Implement the documentation_gates read-path."
  - test_name: test_documentation_gates_is_data_driven_not_hardcoded
    file: unit_tests/test_bo_2200a_1.py
    error: "AssertionError: 'needed' != None : documentation-expert must be 'needed' when 'schema' IS in documentation_gates.change_target_triggers. This confirms the config is actively read at call-time."

### 2026-07-17 10:15 — python-coder (status: ok)
feedback-id: fb_2026-07-17_8ac46c61
completion_manifest:
  documentation_gates_config_section_added: true
  generator_read_path_implemented: true
  all_red_baseline_tests_green: true
  pre_existing_frontmatter_violations_fixed: true
Added `documentation_gates` section to `config/guardrail_gates.yaml` with `change_target_triggers: [ui, schema, pipeline, docs]` and `risk_surface_triggers` (for future use). Added the `documentation_gates` read-path in `_build_agents_map` immediately after `flow_change_gates` processing: the generator reads `change_target_triggers` from the loaded gates config at call-time and adds `documentation-expert` to `guardrail_set` when any `change_target` intersects the trigger list. No hard-coded trigger set exists in the generator. All 5 red_baseline test failures turned green (3 tests, 5 assertion points via subTest). Also fixed pre-existing frontmatter violations (`depends_on: BO-2200a` → `[]`, added `change_target: config`, `risk_surface: internal`) that were blocking ticket edits.
red_baseline_results:
  - test_name: test_change_target_ui_triggers_documentation_expert
    result: green
  - test_name: test_each_triggering_change_target_requires_doc_expert (change_target='schema')
    result: green
  - test_name: test_each_triggering_change_target_requires_doc_expert (change_target='pipeline')
    result: green
  - test_name: test_each_triggering_change_target_requires_doc_expert (change_target='docs')
    result: green
  - test_name: test_documentation_gates_is_data_driven_not_hardcoded
    result: green

### 2026-07-17 11:30 — pr-reviewer (status: blocker)
feedback-id: (submit-failed)
completion_manifest:
  guardrail_gates_yaml_section_reviewed: true
  generate_ticket_from_ac_reviewed: true
  high_confidence_finding_found:
    result: false
    reason: "H-1: The working diff includes a registry entry for documentation-verifier (from bundled BO-2200b-1 work) referencing template_path templates/agents/documentation-verifier.md, but that file does not exist. Any build.py run over the current diff will either fail or silently skip deploying the agent."
    remediation: "Create templates/agents/documentation-verifier.md before committing, or split the BO-2200b-1 registry/feedback_categories additions into their own branch commit separate from this ticket."
Reviewed the working diff for BO-2200a-1. The documentation_gates config section and the _build_agents_map read-path are correct and all 5 tests are green. However, the working diff also bundles BO-2200b-1 changes (agent_registry.json + feedback_categories.yaml additions for documentation-verifier) that reference a template file (templates/agents/documentation-verifier.md) which does not exist in the tree. This is a high-confidence build-time defect. The BO-2200a-1 implementation itself is clean; the blocker is the incomplete BO-2200b-1 artifact. Resolve by creating the missing template or by separating the BO-2200b-1 registry addition into its own commit on the BO-2200b-1 branch before proceeding.

### 2026-07-17 09:23 — test-runner (status: ok)
feedback-id: fb_2026-07-17_43da0692
completion_manifest:
  test_suite_executed: true
  all_tests_passing: true
  failure_report_structured: true
All 3 tests (5 subTest assertion points) in unit_tests/test_bo_2200a_1.py passed green: test_change_target_ui_triggers_documentation_expert, test_each_triggering_change_target_requires_doc_expert (schema, pipeline, docs via subTest), and test_documentation_gates_is_data_driven_not_hardcoded. The documentation_gates config-driven read-path is confirmed working.

### 2026-07-17 09:39 — commit (status: ok)
feedback-id: fb_2026-07-17_bb9ee9ac
completion_manifest:
  pre_commit_hooks_pass: true
  commit_message_valid: true
  ticket_staged: true
Auto-authorized commit gate: subject "feat(config): add documentation_gates declarative policy (BO-2200a-1)"; staged files: config/guardrail_gates.yaml scripts/ac_store/generate_ticket_from_ac.py tickets/00_inbox/epics/EPIC-DocumentationCoverageGuarantee/01_TICKET-20260715-BO-2200a-1.md. pr-reviewer blocker resolved by scoping this commit to BO-2200a-1 files only — BO-2200b-1 artifacts (agent_registry.json, feedback_categories.yaml, docs/agents/cards/) are intentionally excluded and left unstaged for the BO-2200b-1 commit phase.

### 2026-07-20 15:22 — pull-request (status: ok)
feedback-id: (submit-failed)
completion_manifest:
  branch_pushed: true
  pr_created: true
  pr_body_complete: true
PR #337 already open at https://github.com/urlmonitor/leafcutter-ai/pull/337 (feat(config): add documentation coverage guarantee (BO-2200)); branch feature/BO-2200-doc-coverage is tracked and current. Sign-off written and committed for BO-2200a-1 documentation_gates declarative policy ticket. pr-reviewer holds failed status (resolved operationally by commit agent scoping); pending remains empty after this sign-off but ticket is not done-eligible due to pr-reviewer: failed.

