---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  pr-reviewer: needed
  pull-request: needed
  python-coder: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
components:
- ac_driven_dev
created: '2026-06-10'
depends_on:
- ACD-1100e
files_touched:
- docs/reference/skill-frontmatter.md
priority: high
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: ACD-1100e-1
status: todo
title: Version file exists with semver 2.0.0
---

# Version file exists with semver 2.0.0

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `ACD-1100e-1` — Version file exists with semver 2.0.0 — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `ACD-1100e-1`. Component: `ac-driven-dev`. Assigned agent: `python-coder`. Estimated complexity: `S`.

## Acceptance Criteria

```gherkin
Given the package previously had no version marker file,
When the v2.0 migration adds a version marker,
Then config/version.json exists and is valid JSON,
And it contains a top-level "version" field with the string value "2.0.0",
And the file contains no other required fields beyond "version"
  (additional metadata fields are permitted but not required).
```

## Sign-offs

- [ ] python-coder
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit
- [ ] pull-request

## Comments
