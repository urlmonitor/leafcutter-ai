---
description: >-
  Multi-angle debugging: spawns three investigative agents (database, backend,
  frontend/docs) in parallel to diagnose an issue, synthesizes findings, creates
  a fix ticket, and builds it. Asks the user when investigators are uncertain.
---

# /debug — Multi-Angle Issue Investigation & Fix

Read and follow the instructions in the `debug` skill (`.claude/skills/debug/SKILL.md`).

Issue to investigate: **$ARGUMENTS**
