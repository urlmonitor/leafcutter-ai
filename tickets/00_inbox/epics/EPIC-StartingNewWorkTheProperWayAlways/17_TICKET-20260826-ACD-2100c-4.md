---
ac_coverage: 5/5
ac_traceability:
  id: ACD-2100c-4
  path: docs/acceptance-criteria/ac-driven-dev/ACD-2100-entry-point-unblocked/ACD-2100c-4.yaml
advances_current_outcome: true
agents:
  ac-fulfillment-gate: signed_off
  ac-validator: signed_off
  architect-review: signed_off
  commit: signed_off
  documentation-expert: not_needed
  documentation-verifier: not_needed
  llm-expert: signed_off
  pr-reviewer: signed_off
  pull-request: not_needed
  python-coder: signed_off
  test-runner: signed_off
  test-writer: signed_off
change_target: pipeline
complexity: medium
components:
- ac_driven_dev
created: '2026-08-26'
depends_on:
- tickets/00_inbox/epics/EPIC-StartingNewWorkTheProperWayAlways/13_TICKET-20260826-ACD-2100c-1.md
documentation_required: false
files_touched:
- templates/workflows-js/plan-feature.js
- templates/skills/plan-feature/SKILL.md
priority: medium
requires_adr: false
requires_diagram: false
risk_surface: safety
roadmap_phase: phase_1
source_ac: ACD-2100c-4
status: todo
title: An answer that did not come from the person is refused even when it is well
  formed
---

# An answer that did not come from the person is refused even when it is well formed

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `ACD-2100c-4` — An answer that did not come from the person is refused even when it is well formed — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `ACD-2100c-4`. Component: `ac-driven-dev`. Assigned agent: `python-coder`. Estimated complexity: `M`. Complexity: `medium`.

## Acceptance Criteria

```gherkin
Given a run waiting at a decision point with drafted work on disk,
And an answer that is well formed and names one of the choices the decision
  offers but that did not originate from the person running the route,
When the run evaluates that answer,
Then it is not treated as the person's decision and the choice it names is not
  carried out,
And the run stays at the decision point and stays waiting with its record on
  disk,
And the reason it records is that the answer did not come from the person -- not
  that the answer could not be understood,
And an answer identical in every respect except that it did come from the person
  is accepted and carried out.
```

- [x] AC-1: an answer that is well formed and names one of the choices the decision
- [x] AC-2: it is not treated as the person's decision and the choice it names is not
- [x] AC-3: the run stays at the decision point and stays waiting with its record on
- [x] AC-4: the reason it records is that the answer did not come from the person -- not
- [x] AC-5: an answer identical in every respect except that it did come from the person

## Test Requirements

```yaml
tests:
- name: test_well_formed_answer_not_from_the_person_is_refused
  file: unit_tests/workflows/test_acd_2100c_4.py
  covers:
  - ACD-2100c-4
  asserts: An answer that is well formed and names a valid choice for the waiting
    decision but did not originate from the person is not carried out -- no step after
    the decision point executes and the named choice takes no effect on disk.
  framework: unittest
  type: integration
  angle: criterion
- name: test_identical_answers_differing_only_in_origin_produce_opposite_outcomes
  file: unit_tests/workflows/test_acd_2100c_4.py
  covers:
  - ACD-2100c-4
  asserts: Two answers byte-identical in content, one arriving from the person's channel
    and one not; the first is carried out and the second is refused. The only variable
    is origin.
  framework: unittest
  type: integration
  angle: seam
- name: test_refusal_records_provenance_as_the_reason_not_unparseable
  file: unit_tests/workflows/test_acd_2100c_4.py
  covers:
  - ACD-2100c-4
  asserts: The recorded reason for the refusal states that the answer did not come
    from the person, and does not state that the answer could not be understood.
  framework: unittest
  type: integration
  angle: criterion
- name: test_refused_answer_leaves_the_run_waiting_with_its_record_on_disk
  file: unit_tests/workflows/test_acd_2100c_4.py
  covers:
  - ACD-2100c-4
  asserts: After the refusal the run is still at the decision point, its record is
    still present, and the drafted work is byte-identical.
  framework: unittest
  type: integration
  angle: failure
- name: test_acd_2100c_4_reachable_from_entry_point
  file: unit_tests/workflows/test_acd_2100c_4.py
  covers:
  - ACD-2100c-4
  angle: reachability
  asserts: 'REQUIRED — invoke the production entry point (CLI, hook, slash command,
    workflow dispatch, or main()) as a subprocess/dispatch and assert the new behaviour
    actually occurs. Do NOT satisfy this by importing the function directly. The AC
    authored no test_spec, so the entry point is not declared: resolve it before writing
    this test, and do not delete this entry.'
```

## Implementation Notes

```yaml
- The change must land under templates/. The deployed workflow is build output.
- 'Provenance -- where the answer came from -- must be the discriminator. The answer''s
  content must not be inspected to decide whether to accept it. Any content-based
  test is re-implementing the defect with a longer regex: the agent gets better at
  sounding like a user and the bug returns.'
- Hardening the answer parser does not satisfy this record and must not be offered
  as satisfying it. resolveGate already pauses on a null or unparseable answer; the
  observed loss came from a well-formed answer that passed every shape check.
- A refused answer must leave the run waiting, not failed, and must leave the record
  on disk. The person is still owed the question.
- The recorded reason must name provenance. Recording 'could not understand the answer'
  for an answer that was perfectly well formed is how this defect stayed invisible
  for a whole run -- the journal showed a clean decision.
- The accept-and-refuse pair must differ only in origin. If accepting an answer requires
  it to carry anything the refused answer could not also carry, the pair is not a
  controlled comparison and the record is not proved.
- Must satisfy the repository error-handling policy on the channel interaction.
```

## Implementation Tasks

### test-writer

python-coder's implementation (resolveGate() in
`templates/workflows-js/plan-feature.js`) makes this ticket's own
`red_baseline` fully green (verified: `AC_ENFORCE_STRICT=1 python3 -m pytest
unit_tests/workflows/test_acd_2100c_4.py -v` -- 5 passed), but the same
change surfaces a genuine, previously-invisible **contract-shrinkage /
test-drift** regression in **16 pre-existing tests across 5 sibling test
files that belong to OTHER, already-signed-off tickets** (BO-2300a-1,
BO-2300a-2, BO-2300 pause-resume, ACD-2100c-3, ACD-2100c-3-i). Confirmed by
stashing the production edit and re-running each affected file: all 16 pass
on the pre-change baseline and fail only once the provenance (`channel`)
check is added -- these are not flaky or pre-existing failures.

**Root cause:** `resolveGate()` now refuses any `args.resume_answer` whose
`channel` is not exactly `"person"` (absence included), per this ticket's
own AC and its test file's DESIGN DECISION section. Every one of the 16
failing tests constructs a well-formed, ACCEPT-path `resume_answer` (an
approve or cancel decision meant to be genuinely honoured) without a
`channel` field, because that concept did not exist before this ticket. Per
Source-of-Truth Discipline: production is correct (it satisfies
ACD-2100c-4's signed-off red_baseline); these 16 tests are the stale side
and need an assertion-only fix, never a production rollback.

**Mechanical fix per file** -- add `"channel": "person"` to the specific
`resume_answer` fixture object each failing test uses to simulate a
genuine, accepted, person-originated decision. Do NOT add `channel` to any
fixture that is deliberately testing a REJECTION path unrelated to
provenance (wrong shape, invalid enum, mismatched `gate_id`, headless/no
`resume_answer`) -- those already pass unchanged and must stay that way.

Failing tests to fix (verified red only after this ticket's production
change; all pass on the pre-change baseline):

- `unit_tests/workflows/test_bo_2300a_1_refusal_not_decision.py`
  - `test_ac1_genuine_approve_answer_still_accepted` -- add `"channel":
    "person"` to `_GENUINE_APPROVE_ANSWER` (module-level constant, ~line 99)
    or to the composed `approve_answer` dict (~line 187-191).
- `unit_tests/workflows/test_bo_2300a_2_cancel_status_distinct.py`
  - `test_ac2_midgate_cancel_status_is_distinct_from_ok` -- inline
    `resume_answer` dict at ~line 112 (`{"gate_id": "gate-ba", "type":
    "single_choice", "action": "cancel"}`).
  - `test_ac2_pt_gate_cancel_status_is_distinct_from_ok` -- `cancel_answer`
    fixture used at ~line 196 (defined ~line 164-190).
- `unit_tests/workflows/test_bo_2300_pause_resume.py`
  - `test_paused_state_distinct_from_cancelled`
  - `test_valid_answer_applied_by_type_and_resumes_from_pause`
  - `test_resume_preserves_committed_earlier_stages`
  - `test_paused_state_durable_across_process_exit`
  - `test_resume_with_no_pending_pause_is_noop`
  - `test_edit_answer_preserves_feedback_through_resume`
  - (each constructs its own accept-path `approve_answer`/`resume_answer`
    dict around lines 465-1035 -- grep `resume_answer =` in this file to
    find each one; do NOT touch the wrong-shape/malformed/invalid-enum
    fixtures near lines 465/517/572, which already pass and must not
    change).
- `unit_tests/workflows/test_acd_2100c_3_i.py`
  - `test_run_remains_paused_with_its_record_intact_after_a_mismatch` -- the
    `correct_answer` fixture used at ~line 624 (the accept-path half of this
    file's mismatch/correct pair; the `mismatched_answer` fixtures must NOT
    get `channel` -- they test gate_id mismatch, not provenance).
- `unit_tests/workflows/test_acd_2100c_3.py`
  - `test_resume_in_a_new_process_continues_from_the_decision_point`
  - `test_supplied_answer_is_the_one_applied_to_that_decision`
  - `test_no_waiting_record_remains_after_the_resumed_run_passes_the_gate`
  - `test_acd_2100c_3_reachable_from_entry_point`
  - `test_h2_duplicate_ac_ids_when_multistage_route_resumes_at_final_gate`
  - `test_h1_itpo_author_skipped_when_resume_answer_targets_an_earlier_gate`
  - (each builds its own `resume_answer = {"gate_id": ..., "type":
    "single_choice", "action": "approve"}` around lines 519-1039 -- grep
    `resume_answer = {` in this file).

**Verification command** (must show 0 failures across all 6 files,
including this ticket's own new test file, before signing off):

```
AC_ENFORCE_STRICT=1 python3 -m pytest \
  unit_tests/workflows/test_bo_2300a_1_refusal_not_decision.py \
  unit_tests/workflows/test_bo_2300a_2_cancel_status_distinct.py \
  unit_tests/workflows/test_bo_2300_pause_resume.py \
  unit_tests/workflows/test_acd_2100c_3_i.py \
  unit_tests/workflows/test_acd_2100c_3.py \
  unit_tests/workflows/test_acd_2100c_4.py -v
```

Note: `unit_tests/workflows/test_acd_2100a_4.py` has 4 pre-existing failures
(`test_pause_record_written_from_a_worktree_lands_in_the_project_store`,
`test_pause_record_is_found_by_a_second_process_started_elsewhere`,
`test_reported_write_failure_matches_what_is_on_disk`,
`test_pause_write_is_reached_from_the_workflow_entry_point`) that are
UNRELATED to this ticket -- confirmed identical on the pre-change baseline
(they never reach an interactive gate at all; a `git branch --show-current`
call is the only dispatch made). Do not attempt to fix those here; they are
a separate, pre-existing defect outside this ticket's scope.

Given this regression spans 5 test files across other, already-`signed_off`
tickets, the supervisor may also want architect-review's awareness before
committing -- the original architect-review pass classified this ticket
"small" (1 production file) without visibility into this test-fixture blast
radius.

## Agent Contracts

### documentation-expert

Existing docs to update / cross-link:

- docs/architecture/diagrams/c3-006-plan-feature-decision-gate-sequence.md (relationship: describes | status: planned | relevance: The sequence diagram authored by ACD-2100e-1 -- the refused-answer branch is one of the paths it depicts.)
- docs/known-issues/ac-driven-dev.md (relationship: describes | status: exists | relevance: KI-ACD-005 -- the verbatim refusal payload. Note the KI's stated root cause and fix direction are out of date; see this record's notes.)
- templates/workflows-js/plan-feature.js (relationship: implements | status: exists | relevance: Canonical source. resolveGate's 'valid explicit decision' test at 1577-1579 and the gate parse at 2075 accept any object carrying a string action.)
- docs/reference/false-green-mechanisms.md (relationship: describes | status: exists | relevance: A recorded reason that names the wrong cause is the same family of defect: a claim the run did not establish.)
- docs/architecture/components/ac-driven-dev.md (relationship: describes | status: exists | relevance: Component doc for the route whose gate evaluation this record changes.)

This record owns none of the documents above -- every entry is a cross-link to a
document another record produces. The two contract lines that stood here both
demanded `docs/architecture/diagrams/c3-006-plan-feature-decision-gate-sequence.md`,
which ACD-2100e-1 creates; this ticket could never have satisfied them. Removed
per BO-2200c-5-i and -iii; the list above is retained as reading for the
implementer.

### Delivers To

- **Agent:** python-coder
- **Contract:** An unattributable answer resolves to a refusal that leaves the run waiting with its drafted work intact -- never to a stop that the run records as the person's choice.

### Expects From

- **AC:** ACD-2100c-1
- **Contract:** A decision-point mechanism in which an answer carries the channel it arrived on, so provenance is a readable property of the answer at the point of evaluation rather than something inferred from its content.

## Sign-offs

- [x] architect-review — 2026-09-09 19:07
- [x] test-writer — 2026-09-14 08:19
- [x] python-coder — 2026-09-09 19:49
- [x] llm-expert — 2026-09-14 08:12
- [x] test-runner — 2026-09-14 08:31
- [x] pr-reviewer — 2026-09-14 08:36
- [x] ac-validator — 2026-09-14 09:00
- [x] ac-fulfillment-gate — 2026-09-14 08:45
- [x] commit — 2026-09-14 08:55

## Comments

### 2026-09-09 19:07 — architect-review (status: ok)
feedback-id: fb_2026-09-09_a299a1fe
completion_manifest:
  blast_radius_reviewed: true
  rubric_applied: true
  escalation_decision_recorded: true
Rubric applied directly (the `research-agent` Agent-tool spawn path was not
available in this invocation's toolset, so the equivalent blast-radius check
was run by hand via Bash/Read on `templates/workflows-js/plan-feature.js` and
its sibling ticket 13/ACD-2100c-1): 1 file touched
(`templates/workflows-js/plan-feature.js`), 1 component (`ac_driven_dev`), no
cross-module boundary crossed, and none of the four always-large triggers
fire (no Alembic migration, no hypertable change, no FastAPI/public-API
surface change, no ADR-* file edited — ADR-024 is read/composed with, not
modified). Classified **small**; escalation: none. `requires_adr` confirmed
`false` per the mandatory-field rule (no HIGH-impact trigger, only 1
component touched), consistent with the ticket's existing frontmatter.

Design concern (non-blocking, for `python-coder`/`test-writer`): this
ticket's `### Expects From` contract assumes ACD-2100c-1 delivered "a
decision-point mechanism in which an answer carries the channel it arrived
on, so provenance is a readable property of the answer." Reading ticket 13's
own pr-reviewer sign-off (2026-09-08 09:30), that contract was "satisfied
trivially by design" — ACD-2100c-1 closed the loophole by removing the
second (live-agent) channel entirely rather than by tagging `args
.resume_answer` with an explicit provenance/channel field. Grepping the
current `plan-feature.js`, no `.channel` property exists anywhere on the
resume-answer shape. So the literal "answer carries the channel it arrived
on" substrate this ticket's Implementation Notes lean on does not yet exist
on disk — `python-coder` will need to either (a) establish where a
provenance marker is actually set on `args.resume_answer` upstream of
`resolveGate()` (the skill/CLI layer that constructs `args` for a real user
reply, vs. any code path that could construct the same shape
programmatically), or (b) treat this as new work rather than an extension of
an already-present field. This does not change the small/large
classification (still 1 file, 1 component, no always-large trigger) but is
worth surfacing before implementation starts so the "accept/refuse pair
differs only in origin" acceptance criterion is built on a real, not
assumed, discriminator.

Acceptance-criteria adjustments: none — the Gherkin and Test Requirements as
written are implementable once the provenance discriminator above is
resolved; no rewording needed.

### 2026-09-09 19:45 — test-writer (status: ok)
feedback-id: fb_2026-09-09_c56cd624
completion_manifest:
  tests_written: true
  red_baseline_verified: true
  cross_layer_seam_answer:
    result: not_applicable
    reason: "This ticket's scope, like its sibling ACD-2100c-1, is confined to plan-feature.js's own resolveGate()/pauseAtGate() mechanism -- a single script's internal control flow, not a boundary between two independently-deployed layers. There is no separate, already-existing producer module (no CLI/skill yet constructs a resume_answer.channel value) to pipe real output from into resolveGate() as a consumer; the 'producing side' this AC's provenance discriminator would eventually read from does not exist on disk yet -- inventing one here would be new production code, which is python-coder's job, not test-writer's. Every test below drives the REAL, on-disk plan-feature.js through the real workflow-runner entry point (run_workflow_under_e2), which is itself this ticket's only real producer-to-consumer path."
    remediation: "n/a -- not_applicable is a first-class outcome, not a defect. If a later ticket adds a real CLI/skill-side channel-tagging producer, that ticket's own test-writer pass should add the seam test pairing that real producer's output with resolveGate()."
  reachability_entry_point_answer:
    result: resolved
    entry_point: "the plan-feature workflow (templates/workflows-js/plan-feature.js) driven through the real Node-subprocess workflow-runner (unit_tests/_workflow_engine_harness.py's run_workflow_under_e2()), matching the /plan-feature slash command's own real dispatch surface (Workflow(\"plan-feature\", { userInput: $ARGUMENTS }) per templates/commands/plan-feature.md) -- the same resolution unit_tests/workflows/test_acd_2100c_1.py's own red-baseline sign-off (2026-09-07 15:10) already made for this identical file, since the Workflow tool itself has no Python-invocable surface and plan-feature.js has no CLI wrapper, is not a registered pre-commit hook, and exposes no main(argv)."
Wrote `unit_tests/workflows/test_acd_2100c_4.py` with 5 test functions (one
per `## Test Requirements` entry, each tagged `# covers: ACD-2100c-4` and its
declared `# angle:`). No `test_spec`/entry-point was authored on the AC
itself (confirmed: the reachability entry in `## Test Requirements` says so
explicitly), so per BP-1100g-2 I resolved the entry point myself (Step 1):
no CLI wrapper, no pre-commit-hook registration, no slash-command dispatch
point outside the Workflow tool itself, and no `main(argv)` exist for this
script -- the closest real invocation surface is the workflow-dispatch path
`/plan-feature` itself uses, which this repo's own test suite already
operationalises via `run_workflow_under_e2()` (see
`unit_tests/workflows/test_acd_2100c_1.py`'s identical resolution for this
same file). Recorded in `reachability_entry_point_answer` above.

Design decision (no test_spec authored a discriminator name): per the
Implementation Notes ("provenance ... must be the discriminator... the
answer's content must not be inspected"), the tests require
`args.resume_answer` to carry a `channel` field, honoured ONLY when
`channel === "person"`; any other value or its absence must refuse the
answer while leaving `status: "paused_awaiting_input"` and a `reason` string
naming provenance (not a shape/parse failure). This is the test contract
python-coder must satisfy (Source-of-Truth Discipline Rule 5 -- python-coder
may rename the field only by also updating these tests). Every
accepted/refused fixture pair is byte-identical except for `channel`, per
the Implementation Notes' controlled-comparison requirement.

Verification run: `AC_ENFORCE_STRICT=1 python3 -m pytest
unit_tests/workflows/test_acd_2100c_4.py -v` -- **5 failed, 0 passed**. All
five are genuine AssertionErrors from driving the REAL, on-disk
`plan-feature.js` (no harness/import errors): today's `resolveGate()` has no
provenance check at all, so a well-formed-but-unattributable answer is
applied identically to a genuine one (`apply-approval` dispatched in every
refused-answer scenario; no `reason` field exists anywhere in the terminal
payload). Full red_baseline below.
red_baseline:
  - test_name: test_well_formed_answer_not_from_the_person_is_refused
    file: unit_tests/workflows/test_acd_2100c_4.py
    error: "AssertionError: 'apply-approval' unexpectedly found in ['detect-current-branch', 'resolve-worktree-setup-script-path', 'worktree-setup', 'scan-orphans-git-status', 'scan-committed-stages', 'stage-0-triage', 'pt-classify', 'peek-pause-record', 'stage-itpo-author', 'read-pause-record', 'clear-pause-record', 'apply-approval', 'branch-check'] : A well-formed answer naming 'approve' for 'final-gate' was carried out (dispatched 'apply-approval') even though it carried no 'channel' marking it as having come from the person running the route."
  - test_name: test_identical_answers_differing_only_in_origin_produce_opposite_outcomes
    file: unit_tests/workflows/test_acd_2100c_4.py
    error: "AssertionError: 'apply-approval' unexpectedly found in [...] : An answer identical in content but whose channel is NOT 'person' must not be carried out -- content alone must never be the discriminator."
  - test_name: test_refusal_records_provenance_as_the_reason_not_unparseable
    file: unit_tests/workflows/test_acd_2100c_4.py
    error: "AssertionError: 'error' != 'paused_awaiting_input' : terminal={'status': 'error', 'message': 'Commit of it-po AC output failed. A pre-commit hook rejected the staged files....', 'acs_updated': [], 'priority': 'high'}"
  - test_name: test_refused_answer_leaves_the_run_waiting_with_its_record_on_disk
    file: unit_tests/workflows/test_acd_2100c_4.py
    error: "AssertionError: None != 'final-gate' : The run must still be at 'final-gate' after a refused answer. terminal={'status': 'error', 'message': 'Commit of it-po AC output failed. A pre-commit hook rejected the staged files....', 'acs_updated': [], 'priority': 'high'}"
  - test_name: test_acd_2100c_4_reachable_from_entry_point
    file: unit_tests/workflows/test_acd_2100c_4.py
    error: "AssertionError: 'apply-approval' unexpectedly found in [...] : A refused (channel!=person) decision's rejection must be consumed by suppressing the real downstream dispatch."

No `## Agent Contracts` `### test-writer` subsection and no `## AC Coverage`
table exist on this ticket (only `### documentation-expert` / `Delivers To` /
`Expects From` are present under `## Agent Contracts`), so per signoff
skill §2c.2/§2c.3 there is no AC-checkbox flip or coverage-table cell to
fill for this agent on this ticket -- skipped, not omitted.

### 2026-09-09 19:49 — python-coder (status: handoff)
feedback-id: fb_2026-09-09_6cd939df
completion_manifest:
  tests_green:
    result: false
    reason: "This ticket's own 5 red_baseline tests (unit_tests/workflows/test_acd_2100c_4.py) are all green. But implementing the change correctly turns 16 PREVIOUSLY-PASSING tests across 5 sibling files (owned by other, already-signed-off tickets: BO-2300a-1, BO-2300a-2, BO-2300 pause-resume, ACD-2100c-3, ACD-2100c-3-i) red. Verified by stashing the production edit and re-running each file: all 16 pass on the pre-change baseline (confirming this is a genuine regression, not pre-existing flake) and fail only with the provenance check in place."
    remediation: "Classified test_drift (Source-of-Truth Discipline): production is correct per this ticket's signed-off red_baseline; the 16 tests are stale because they predate the provenance/channel concept and construct accept-path resume_answer fixtures with no channel field. See the ### test-writer section under ## Implementation Tasks (added by this sign-off) for the exact file/test list and the mechanical fix (add \"channel\": \"person\" to each accept-path fixture)."
  red_baseline_results:
    - test_name: test_well_formed_answer_not_from_the_person_is_refused
      result: green
    - test_name: test_identical_answers_differing_only_in_origin_produce_opposite_outcomes
      result: green
    - test_name: test_refusal_records_provenance_as_the_reason_not_unparseable
      result: green
    - test_name: test_refused_answer_leaves_the_run_waiting_with_its_record_on_disk
      result: green
    - test_name: test_acd_2100c_4_reachable_from_entry_point
      result: green
  cross_ticket_blast_radius_disclosed: true
Implemented the provenance gate in `resolveGate()`
(`templates/workflows-js/plan-feature.js`, ~lines 1573-1601): after
`validateAnswerShape()` passes, a well-formed `args.resume_answer` is now
honoured ONLY when `answer.channel === "person"`; any other value or its
absence returns `{status: "paused_awaiting_input", run_id, gate_id, reason:
"This answer did not come from the person running the route..."}` --
identical shape to the pre-existing unattended-pause path, plus the
provenance-worded `reason` this AC requires, and it deliberately never
reaches the durable-record read/clear dispatches (so `clear-pause-record`
and `apply-approval` never fire for a refused answer -- satisfying "the run
stays waiting with its record on disk" for free, by construction). All 5 of
this ticket's own red_baseline tests are green
(`AC_ENFORCE_STRICT=1 python3 -m pytest unit_tests/workflows/test_acd_2100c_4.py -v`
-- 5 passed).

However, this correct change has real fallout: 16 tests in 5 OTHER,
already-`signed_off` tickets' test files construct genuine accept-path
`resume_answer` fixtures with no `channel` field, and my change now refuses
those (they were relying on the pre-ticket behaviour that any well-formed
answer on the resume channel was honoured). I confirmed this is caused by
my change specifically, not a pre-existing flake, by `git stash`-ing
`templates/workflows-js/plan-feature.js` alone and re-running: all 16 pass
on the unmodified file. (Separately, `test_acd_2100a_4.py`'s 4 failures are
confirmed PRE-EXISTING and unrelated -- identical failures with or without
my change; not touched.) Per the Contract-Shrinkage Guard's test_drift
classification, I am NOT rolling back or weakening the production fix (it
is required by this ticket's own signed-off AC and red_baseline). I've
added a `### test-writer` section under a new `## Implementation Tasks`
heading on this ticket with the exact 5 files, 16 test names, the
mechanical per-fixture fix, and the verification command. Handing off to
**test-writer** to apply that assertion-only fix across the 5 affected
files before this ticket (and by extension the epic) can safely commit.
Given the blast radius crosses ticket boundaries into already-closed work,
the supervisor may also want to loop in architect-review, whose original
"small, 1-file" classification of this ticket did not have visibility into
this test-fixture-only fallout.

### 2026-09-14 08:12 — llm-expert (status: ok)
feedback-id: fb_2026-09-14_96b405e2
completion_manifest:
  template_written: true
  prompt_quality_checklist_passed: true
  convention_violations_resolved: true
Authored the skill-side (producer) half of the resume contract that `resolveGate()`
consumes: added `## §RS — Resuming a Paused Run` to
`templates/skills/plan-feature/SKILL.md` (§RS.1 what `paused_awaiting_input` /
`run_id` + `gate_id` / the `reason` field mean and that `reason` must be
surfaced verbatim, not swallowed; §RS.2 how to construct `args.resume_answer`
with `gate_id`/`type`/`action`/`channel`; §RS.3 the provenance obligation —
`channel: "person"` is set ONLY when relaying the actual human's own decision,
never a guess/default/retry/agent-composed answer, and doing so anyway defeats
the gate and is prohibited). Added a `Related` cross-link to
`ADR-024-interactive-pause-resume.md` and a `DECISION HISTORY` entry. Matched
the file's existing `## §<TAG> — Title` heading depth and prose voice (see
§WSP/§PRR/§MP for precedent). Did NOT modify `plan-feature.js` or any test
file, per this ticket's explicit instruction. `templates/commands/plan-feature.md`
was intentionally left untouched — it is outside my agent's declared scope
boundary (`templates/agents/`, `templates/skills/`, and the ticket file only);
flagging for a documentation-expert/llm-expert follow-up if the command's own
short user-facing surface needs a resume-contract pointer too. `llm-expert` was
not present in this ticket's `agents:` map (the ticket's own phase pipeline
does not include it); added the entry per this dispatch's explicit sign-off
instruction, alphabetically ordered alongside the existing agents.

### 2026-09-14 08:19 — test-writer (status: ok)
feedback-id: fb_2026-09-14_5adc6f0a
completion_manifest:
  test_drift_fixtures_repaired: true
  guard_test_added: true
  regression_verification_run: true
  cross_layer_seam_answer:
    result: not_applicable
    reason: "This dispatch is a Source-of-Truth Discipline Rule 1 test-drift repair (16 pre-existing accept-path resume_answer fixtures updated to add channel: person) plus one documentation-presence guard test, not new production-code coverage of a fresh layer boundary. The repaired fixtures already exercise the real seam (run_workflow_under_e2 driving the real, on-disk plan-feature.js) -- they are the SAME seam-covered tests the sibling tickets' own test-writer passes already established; this dispatch only restores their fixture shape to match resolveGate()'s new, signed-off contract. The one new test I added (TestSkillDocumentsChannelPersonObligation) is explicitly a grep-based doc-contract presence check, not a producer/consumer pairing -- it deliberately does not invoke resolveGate() at all."
    remediation: "n/a -- not_applicable is a first-class outcome, not a defect."
Classified **test_drift** (Source-of-Truth Discipline Rule 1): production
(`resolveGate()`'s `channel === "person"` gate, landed by python-coder per
this ticket's own signed-off red_baseline) is correct; the 16 tests were
stale because they predate the provenance/channel concept and constructed
accept-path `resume_answer` fixtures with no `channel` field. Per Rule 4,
no production code was touched in this pass -- assertion-only fixture
repair, exactly as python-coder's handoff comment specified.

**Task 1 (fixture repair)** — added `"channel": "person"` to the accept-path
`resume_answer`/`approve_answer`/`cancel_answer`/`correct_answer` fixture in
each of the 16 named tests across the 5 listed files, verified line-by-line
against the ticket's own file/test enumeration:
- `test_bo_2300a_1_refusal_not_decision.py`: `_GENUINE_APPROVE_ANSWER` (1).
- `test_bo_2300a_2_cancel_status_distinct.py`: the inline mid-gate
  `resume_answer` and the `cancel_answer` fixture (2).
- `test_bo_2300_pause_resume.py`: the `cancel_answer`/`approve_answer`/
  `resume_answer` fixtures inside all 6 named tests (6).
- `test_acd_2100c_3_i.py`: `correct_answer` inside
  `test_run_remains_paused_with_its_record_intact_after_a_mismatch` (1) --
  confirmed the sibling `mismatched_answer` fixtures in the same file were
  left untouched (they test `gate_id` mismatch, not provenance).
- `test_acd_2100c_3.py`: the `resume_answer`/`approve_answer`/`cancel_answer`
  fixtures inside all 6 named tests (6).
No wrong-shape/malformed/invalid-enum/headless fixture was touched anywhere
(spot-checked `test_bo_2300_pause_resume.py` lines 465/517/572 and
`test_acd_2100c_3_i.py`'s `mismatched_answer` fixtures remain unchanged);
confirmed by the full-suite diff below showing no unexpected deletions.

**Task 2 (guard test)** — added
`TestSkillDocumentsChannelPersonObligation::test_skill_documents_channel_person_resume_obligation`
to `unit_tests/workflows/test_acd_2100c_4.py`, tagged `# covers: ACD-2100c-4`
/ `# angle: reachability`, with an explicit docstring stating it is a
documentation-presence guard (greps `templates/skills/plan-feature/SKILL.md`
for `resume_answer`, `channel`, `person`, and the literal `"person"`), not a
behavioural test — its sole job is to stop the producer-side contract from
being silently deleted while resolveGate()'s consumer-side gate stays green.
Per the task instructions this test was expected to possibly still be RED at
authoring time; in fact by the time I wrote it the concurrent `llm-expert`
dispatch (see its 2026-09-14 08:12 comment above) had already landed
`## §RS — Resuming a Paused Run` in that SKILL.md, so the test is GREEN as
authored — reported honestly here rather than weakened or removed.

**Verification (both commands run exactly as specified):**

6-file command (`AC_ENFORCE_STRICT=1 python3 -m pytest
unit_tests/workflows/test_bo_2300a_1_refusal_not_decision.py
unit_tests/workflows/test_bo_2300a_2_cancel_status_distinct.py
unit_tests/workflows/test_bo_2300_pause_resume.py
unit_tests/workflows/test_acd_2100c_3_i.py unit_tests/workflows/test_acd_2100c_3.py
unit_tests/workflows/test_acd_2100c_4.py -v`): **44 passed, 0 failed** (up
from the pre-fix state of 16 failed / 28 passed across the same 6 files).

Full `unit_tests/workflows/` directory (`AC_ENFORCE_STRICT=1 python3 -m
pytest unit_tests/workflows/ -q`): **22 failed, 614 passed, 40 subtests
passed** — failed COUNT matches the ticket's stated baseline (22) exactly,
and by NAME every one of the 22 is a pre-existing, unrelated failure already
on record: `test_acd_2100a_1.py` (3), `test_acd_2100a_3.py` (3),
`test_acd_2100a_4.py` (4 — the exact 4 names this ticket's own
`### test-writer` task section already lists as confirmed-unrelated),
`test_acd_2100a_5.py` (3), `test_acd_2100b_4.py` (4 SUBFAILED conditions,
same test), `test_bo_1500f_1.py` (4), and
`test_bo_1500f_1_real_registry_read.py` (1) — none of these files reference
`resume_answer` or `channel` in any test this pass touched (grepped all 7 to
confirm; the one `resume_answer` hit in `test_acd_2100a_4.py` is inside
`test_pause_record_is_found_by_a_second_process_started_elsewhere`, one of
the 4 already-named pre-existing failures, whose own tests never reach
`resolveGate()`'s gate check per the ticket's own note). Zero delta by name
against the baseline: no new failure, no previously-failing test still red.
The passed count (614) is 6 higher than the ticket's stated baseline of 608;
I did not attempt to reconcile that discrepancy against an unknown prior
collection run and report it here rather than silently asserting an exact
match — the load-bearing fact (identical 22-failure set, by name) is
verified above.

`git diff --stat` on the 6 touched test files: 8 files changed total (the 6
plus `templates/workflows-js/plan-feature.js` and one sibling ticket's
`status:` flip, both pre-existing from other agents' concurrent work in this
same worktree, not touched by this dispatch), 190 insertions(+), 17
deletions(-) confined to the fixture dicts and the one new test class.

No `## Agent Contracts` `### test-writer` subsection and no `## AC Coverage`
table exist on this ticket (unchanged since the prior test-writer entry), so
per signoff skill §2c.2/§2c.3 there is no AC-checkbox flip or coverage-table
cell to fill for this agent on this ticket.

### 2026-09-14 08:31 — test-runner (status: ok)
feedback-id: fb_2026-09-14_46db4c46
completion_manifest:
  test_suite_executed: true
  all_tests_passing: true
  failure_report_structured: true
Ran the ticket's own specified 6-file verification command
(`AC_ENFORCE_STRICT=1 python3 -m pytest unit_tests/workflows/test_bo_2300a_1_refusal_not_decision.py
unit_tests/workflows/test_bo_2300a_2_cancel_status_distinct.py
unit_tests/workflows/test_bo_2300_pause_resume.py unit_tests/workflows/test_acd_2100c_3_i.py
unit_tests/workflows/test_acd_2100c_3.py unit_tests/workflows/test_acd_2100c_4.py -v`): **44
passed, 0 failed** — matches test-writer's prior verification exactly, including this
ticket's own 5 red_baseline tests (now green) plus the 1 new documentation-presence guard
test. Also ran the full `unit_tests/workflows/` directory
(`AC_ENFORCE_STRICT=1 python3 -m pytest unit_tests/workflows/ -q`) as an independent
regression check: **22 failed, 614 passed, 40 subtests passed** — the 22 failures match, by
exact name and count, the pre-existing/unrelated baseline documented in python-coder's and
test-writer's prior comments (`test_acd_2100a_1` x3, `test_acd_2100a_3` x3, `test_acd_2100a_4`
x4, `test_acd_2100a_5` x3, `test_acd_2100b_4` 4 subfailed, `test_bo_1500f_1` x4,
`test_bo_1500f_1_real_registry_read` x1) — zero new failures, zero previously-failing test
still red among this ticket's own scope. No `## Agent Contracts` `### test-runner`
subsection exists on this ticket, so per signoff skill §2c.2/§2c.3 there is no AC-checkbox
flip or coverage-table cell to fill for this agent.

### 2026-09-14 08:36 — pr-reviewer (status: ok)
feedback-id: fb_2026-09-14_8ca23887
completion_manifest:
  diff_reviewed: true
  tests_verified_green: true
  contract_trace_performed: true
  high_confidence_finding_disclosed: true
Reviewed the working diff for this ticket's two `files_touched`
(`templates/workflows-js/plan-feature.js`, `templates/skills/plan-feature/SKILL.md`)
plus the six test files the `### test-writer` task section required (5
sibling-fixture repairs + this ticket's own new
`unit_tests/workflows/test_acd_2100c_4.py`). Re-ran both verification commands
myself rather than trusting the prior sign-offs' reported numbers:
`AC_ENFORCE_STRICT=1 python3 -m pytest unit_tests/workflows/test_acd_2100c_4.py -v`
— 6 passed (5 red_baseline + the doc-presence guard test), and the ticket's own
6-file command — 44 passed, 0 failed, matching test-writer's and test-runner's
reported counts exactly.

`resolveGate()`'s new `channel !== "person"` branch
(`templates/workflows-js/plan-feature.js` ~1573-1601) is a correct, minimal,
well-commented implementation of the AC: it refuses on any value other than
the literal string `"person"` (including absence), returns the SAME
`paused_awaiting_input` status/gate_id shape as an unanswered gate, adds a
`reason` string that names provenance and contains none of the
shape/parse-failure phrasing the AC's Then-clause forbids, and — by never
reaching the `read-pause-record`/`clear-pause-record`/apply dispatches on the
refused path — leaves the durable record and drafted work untouched by
construction. The accept/refuse fixture pairs in the new test file are
byte-identical except for `channel`, satisfying the controlled-comparison
requirement in the Implementation Notes. The `SKILL.md` §RS addition
correctly documents the producer-side obligation (`channel: "person"` set
only when relaying the actual person's own decision) and is verified present
on disk by the new `TestSkillDocumentsChannelPersonObligation` guard test.
The 5 sibling-file fixture repairs (`test_bo_2300a_1_refusal_not_decision.py`,
`test_bo_2300a_2_cancel_status_distinct.py`, `test_bo_2300_pause_resume.py`,
`test_acd_2100c_3_i.py`, `test_acd_2100c_3.py`) add `"channel": "person"` only
to accept-path fixtures, per spot-check diff review — no wrong-shape/mismatch
fixture was touched.

**Non-blocking high-confidence finding (cross-file, out of this ticket's
scope):** `templates/workflows-js/finalize-feature.js` carries its own inlined
copy of `resolveGate()`/`validateAnswerShape()`, with a comment reading "The
same helper is inlined in plan-feature.js — keep them in sync." That copy was
NOT given the `channel` provenance check this ticket adds — grepped the file
directly and confirmed its `resolveGate()` still applies any well-formed
`args.resume_answer` on the resume channel with no provenance check at all.
This means the exact defect ACD-2100c-4 exists to close (a well-formed but
unattributable answer is treated as the person's own decision) is still live
and reachable via `/finalize-feature`'s own resume path today. This is outside
this ticket's `files_touched` (confirmed no sibling ticket in this epic
touches `finalize-feature.js` for this purpose — `05_TICKET-20260826-ACD-2100a-4.md`
explicitly notes `finalize-feature.js` as out of its own scope for an unrelated
fix), so I am not blocking this ticket's sign-off on it, but recommend a
follow-up AC/ticket to add the identical `channel === "person"` gate to
`finalize-feature.js`'s `resolveGate()` before treating the KI-ACD-005 defect
class as closed repo-wide.

No `## Agent Contracts` `### pr-reviewer` subsection exists on this ticket (only
`### documentation-expert` / `Delivers To` / `Expects From`), so per signoff
skill §2c.2 there is no AC-checkbox flip or `## AC Coverage` table cell for
this agent on this ticket — skipped, not omitted.

### 2026-09-14 09:00 — ac-validator (status: ok)
feedback-id: (submit-failed)
completion_manifest:
  all_acs_evidenced: true
  store_alignment_checked: true
  cross_file_contract_traced: true
Verified all 5 body ACs (`## Acceptance Criteria`) have both implementation and
test evidence in the current working diff (`git diff HEAD`) and flipped their
checkboxes. `submit_feedback.py` rejected `ac-validator` as an allowed writer
for every category tried (`ac-validator` is absent from
`config/feedback_categories.yaml`'s writer lists entirely) — recorded
`feedback-id: (submit-failed)` per signoff skill §2a step 4 and proceeded, per
"Do NOT abort signoff."

AC-1 covered:
  implementation: `templates/workflows-js/plan-feature.js` — `resolveGate()`,
    `if (args.resume_answer.channel !== "person")` guard added at line 1590
    (diff hunk `@@ -1573,7 +1573,32 @@`).
  test: `test_well_formed_answer_not_from_the_person_is_refused`
    (`TestWellFormedAnswerNotFromThePersonIsRefused`,
    `unit_tests/workflows/test_acd_2100c_4.py:144`).

AC-2 covered:
  implementation: same guard (line 1590) returns `paused_awaiting_input`
    before the durable-record dispatch, so `apply-approval` is never reached
    on the refused path.
  test: `test_well_formed_answer_not_from_the_person_is_refused`
    (line 144) and
    `test_identical_answers_differing_only_in_origin_produce_opposite_outcomes`
    (`TestIdenticalAnswersDifferingOnlyInOriginProduceOppositeOutcomes`,
    line 207) both assert `'apply-approval'` is absent from the dispatched
    step list for a non-person `channel`.

AC-3 covered:
  implementation: the refusal branch returns before
    `read-pause-record`/`clear-pause-record` dispatches, leaving the durable
    record untouched by construction (python-coder's 2026-09-09 19:49
    comment).
  test: `test_refused_answer_leaves_the_run_waiting_with_its_record_on_disk`
    (`TestRefusedAnswerLeavesTheRunWaitingWithItsRecordOnDisk`,
    `unit_tests/workflows/test_acd_2100c_4.py:352`).

AC-4 covered:
  implementation: the `reason` string literal added at lines 1596-1599 of
    `plan-feature.js` ("This answer did not come from the person running the
    route...") names provenance and contains no shape/parse language.
  test: `test_refusal_records_provenance_as_the_reason_not_unparseable`
    (`TestRefusalRecordsProvenanceAsTheReasonNotUnparseable`,
    `unit_tests/workflows/test_acd_2100c_4.py:278`).

AC-5 covered:
  implementation: the same guard's else-path (`channel === "person"`) falls
    through unchanged to the pre-existing "Shape valid and person-attributed"
    durable-record consult, so an identical answer differing only in
    `channel` is carried out.
  test: `test_identical_answers_differing_only_in_origin_produce_opposite_outcomes`
    (`unit_tests/workflows/test_acd_2100c_4.py:207`).

Additional evidence run live, not just cited: `AC_ENFORCE_STRICT=1 python3 -m
pytest unit_tests/workflows/test_acd_2100c_4.py -q` — 6 passed (the 5
red_baseline tests plus the SKILL.md documentation-presence guard test). The
ticket's own 6-file regression command
(`test_bo_2300a_1_refusal_not_decision.py`,
`test_bo_2300a_2_cancel_status_distinct.py`, `test_bo_2300_pause_resume.py`,
`test_acd_2100c_3_i.py`, `test_acd_2100c_3.py`, `test_acd_2100c_4.py`) — 44
passed, 0 failed, matching test-writer/test-runner/pr-reviewer's reported
counts exactly.

`python3 scripts/commit_guardian/check_v2_ac_store_alignment.py --ticket
<this ticket>` exited 0 with no ERROR output — no store-alignment violations.

Cross-file contract trace: this ticket's `## Agent Contracts` carries an
`Expects From` entry naming ACD-2100c-1 ("a decision-point mechanism in which
an answer carries the channel it arrived on"). architect-review's 2026-09-09
comment already flagged that ACD-2100c-1 did not literally establish a
`.channel` field (it closed its own loophole a different way), so this
ticket's own `python-coder` pass established the `channel` field itself
inside `resolveGate()` rather than reading one from an external producer —
confirmed by re-reading the diff: the guard and the field are defined and
consumed in the same function, in the same commit. This is not a contract gap
in the Step 2d sense (a consumer expecting a field an external producer
failed to populate): there is no cross-file boundary being crossed for this
particular field, because this ticket is the producer. The `Delivers To`
entry (python-coder, same ticket) is satisfied by the same code. Separately,
`SKILL.md`'s new §RS section (the true producer-side contract for
`args.resume_answer.channel`, consumed by `resolveGate()`) is verified
present on disk by
`TestSkillDocumentsChannelPersonObligation::test_skill_documents_channel_person_resume_obligation`
(`unit_tests/workflows/test_acd_2100c_4.py:513`), which greps the actual
`templates/skills/plan-feature/SKILL.md` file for `resume_answer`, `channel`,
and `"person"` — confirmed those strings are present in the diff I read
directly. No consumer/producer mismatch found.

No `### ac-validator` heading exists under this ticket's `## Agent Contracts`
section (only `### documentation-expert` / `Delivers To` / `Expects From`),
so per signoff skill §2c.2 there is no AC-checkbox flip inside `## Agent
Contracts` for this agent, and no `## AC Coverage` table exists on this
ticket to fill (confirmed by the prior test-writer and pr-reviewer comments
noting the same absence) — both skipped per §2c.1/§2c.2, not omitted. The
body-level `## Acceptance Criteria` checkboxes (AC-1 through AC-5) and the
`ac_coverage: 5/5` frontmatter key were updated per this agent's own Step
4a/4c procedure, which is separate from and not superseded by the §2c
Agent-Contracts protocol.

All 5 ACs covered. Commit phase may proceed.

### 2026-09-14 08:45 — ac-fulfillment-gate (status: ok)
feedback-id: (submit-failed)
completion_manifest:
  ac_yaml_resolved: true
  ac_fields_verified_or_autofixed: true
  schema_validation_passed: true
`submit_feedback.py` rejected `ac-fulfillment-gate` as an allowed writer for
every category tried (`complete`, `success-pattern`) — same gap ac-validator
hit above; `ac-fulfillment-gate` is absent from
`config/feedback_categories.yaml`'s writer lists entirely. Recorded
`feedback-id: (submit-failed)` per signoff skill §2a step 4 and proceeded.

Ran `.leafcutter/scripts/ac_store/ac_coverage_resolver.py --ticket <this
ticket>`: resolved exactly one AC (`ACD-2100c-4`, two-key `{id, path}`
traceability block, `resolved_via: traceability_block`), non-empty, so the
load-bearing ok-precondition (`len(resolved_acs) >= 1`) is satisfied. Initial
run reported `ok: false` with two failures on `ACD-2100c-4`: `work_status`
(`todo`, not `done`) and `covered_by` (`[]`). `implemented_by` held only this
ticket's own path (`tickets/.../17_TICKET-...md`), which is not itself in
`files_touched`, so it also had no diff intersection though the resolver
did not separately flag it.

Auto-fix (append-only, Step 3): confirmed `files_touched`
(`templates/workflows-js/plan-feature.js`,
`templates/skills/plan-feature/SKILL.md`) both appear in `git diff HEAD
--name-only` (one committed on `main...HEAD`, both present via `git diff
HEAD` including the currently-uncommitted/staged working-tree edits ahead of
the commit phase) — set `work_status: done` and appended both paths to
`implemented_by` (ticket-path entry retained, untouched). For `covered_by`,
grepped `unit_tests/` (per BO-202, not `tests/` alone) for `# covers:
ACD-2100c-4` and found six matching tags in
`unit_tests/workflows/test_acd_2100c_4.py` (lines 147, 210, 281, 355, 428,
533) — appended that single test-file path to `covered_by`. Ran
`scripts/commit_guardian/check_ac_schema.py` against the edited AC YAML:
exit 0, no errors. Re-ran the resolver: `ok: true`, `failures: []`, "All 1
AC(s) verified."

AC store fields for ACD-2100c-4 are accurate and current. Commit phase may
proceed.

### 2026-09-14 08:50 — commit (status: ok)
feedback-id: fb_2026-09-14_946342be
Auto-authorized commit gate: subject "fix(ac-driven-dev): refuse resume
answers that did not come from the person"; staged files:
docs/acceptance-criteria/ac-driven-dev/ACD-2100-entry-point-unblocked/ACD-2100c-4.yaml,
templates/skills/plan-feature/SKILL.md, templates/workflows-js/plan-feature.js,
tickets/00_inbox/epics/EPIC-StartingNewWorkTheProperWayAlways/17_TICKET-20260826-ACD-2100c-4.md,
unit_tests/workflows/test_acd_2100c_3.py, unit_tests/workflows/test_acd_2100c_3_i.py,
unit_tests/workflows/test_acd_2100c_4.py, unit_tests/workflows/test_bo_2300_pause_resume.py,
unit_tests/workflows/test_bo_2300a_1_refusal_not_decision.py,
unit_tests/workflows/test_bo_2300a_2_cancel_status_distinct.py. Authorization
established by upstream gates (pr-reviewer, ac-validator, ac-fulfillment-gate,
all signed off above) plus the ticket-supervisor's commit-phase dispatch;
no interactive confirmation is issued per the supervised-path branch of the
commit agent's Step 3.

### 2026-09-14 08:55 — commit (status: ok)
feedback-id: fb_2026-09-14_a60b5747
completion_manifest:
  pre_commit_hooks_pass: true
  commit_message_valid: true
  ticket_staged: true
Pre-commit hook probe (`verify_precommit_active.py`) confirmed active before
staging (`failing_checks: []`). Staged the 9 files already prepared by
upstream agents plus `templates/workflows-js/plan-feature.js` itself, which
carried python-coder's `channel === "person"` production change but had
never been staged by any prior phase. Ran `COMMIT_AGENT_MODE=1 git commit`;
the first attempt was blocked by `check-feedback-id` on this ticket's own
08:50 audit-gate comment (missing `feedback-id:` line) — fixed by calling
`submit_feedback.py` and inserting the returned id, then retried
successfully as commit `dfead4260` ("fix(ac-driven-dev): refuse resume
answers that did not come from the person"), 10 files changed, verified via
`git log -1` (HEAD moved) and `git show --stat HEAD` after the commit
returned in the background past the 120s foreground timeout. `pull-request`
is `not_needed` on this ticket's frontmatter, so no PR phase follows;
this commit is the ticket's terminal artifact on the `EPIC-StartingNewWorkTheProperWayAlways`
branch.

## Escalation

Branch: none
Reason: 1 file (`templates/workflows-js/plan-feature.js`) in one component
(`ac_driven_dev`); no always-large trigger fired.
