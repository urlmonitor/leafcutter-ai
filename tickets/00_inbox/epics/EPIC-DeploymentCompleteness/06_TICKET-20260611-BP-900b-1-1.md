---
advances_current_outcome: true
agents:
  commit: signed_off
  documentation-expert: not_needed
  pr-reviewer: signed_off
  pull-request: needed
  python-coder: signed_off
  sql-coder: not_needed
  test-runner: needed
  test-writer: signed_off
components:
- build_pipeline
created: '2026-06-11'
depends_on:
- BP-900b-1
files_touched:
- scripts/build_propagation_audit.py
- docs/architecture/components/template-compiler.md
priority: critical
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
change_target: pipeline
risk_surface: internal
source_ac: BP-900b-1-1
status: todo
title: Allowlisted external scripts do not trigger broken-reference failures
---

# Allowlisted external scripts do not trigger broken-reference failures

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BP-900b-1-1` — Allowlisted external scripts do not trigger broken-reference failures — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BP-900b-1-1`. Component: `build_pipeline`. Assigned agent: `python-coder`. Estimated complexity: `XS`.

## Acceptance Criteria

```gherkin
Given an agent template references a script path "scripts/external_tool.py"
And that path is listed in the external-dependency allowlist
(a configuration file or constant that the guard reads),
When the guard cross-checks references against deployable scripts,
Then the allowlisted reference is treated as resolved
And it does NOT appear in the broken-reference list
And the build exits zero (assuming no other broken references exist)
```

## Sign-offs

- [x] python-coder — 2026-08-18 10:15
- [x] test-writer — 2026-08-18 09:00
- [ ] test-runner
- [x] pr-reviewer — 2026-08-18 11:30
- [x] commit — 2026-08-18 12:00
- [ ] pull-request

## Comments

### 2026-08-18 09:00 — test-writer (status: ok)
feedback-id: (submit-failed)
completion_manifest:
  tests_written: true
  red_baseline_confirmed: true
  ac_yaml_gap_investigated: true
No `## Test Requirements` block was present in this ticket body, so per the
test-writer skip-rule (`python-coder` is `needed`, so no silent skip) I
derived the test contract directly from the AC store per `source_ac:
BP-900b-1-1`.

**Investigation finding (worth flagging to python-coder/pr-reviewer):** the
behavioral mechanism this AC describes —
`EXTERNAL_DEPENDENCY_ALLOWLIST` / `check_broken_references` /
`build_broken_ref_report` in `scripts/build_propagation_audit.py`, wired
end-to-end into `build._check_script_reference_guard` (called from
`_run_phases()`) — was already authored under a prior epic
(EPIC-BuildGuardFalsePositive/03, per the module's own DECISION HISTORY) and
is already exercised indirectly, at the real-package level, by
`unit_tests/test_build_guard_real_package.py::test_guard_exits_0_on_clean_package`.
No prior test names `check_broken_references` directly or tags this AC by
id. This AC's own YAML (`BP-900b-1-1.yaml`) is marked `work_status: done`
already. Classification (ADR-003 Rule 1): **test_drift** — production is
correct and complete for the Gherkin as literally written (the Gherkin's own
parenthetical "a configuration file **or constant** that the guard reads"
explicitly sanctions the existing frozenset-constant design; the
`it_requirements` notes about a "missing or malformed allowlist **file**"
do not apply to a constant and are boilerplate mismatch, not an unmet
requirement) — the gap was a missing direct/dedicated test, not missing
production code.

Wrote `unit_tests/build_guards/test_bp900b1_1_external_dependency_allowlist.py`
with 5 tests:
- `TestBP900b11AllowlistedReferenceResolved` (2 tests): direct
  `check_broken_references()` coverage of the AC's literal example path
  (`scripts/external_tool.py`) — allowlisted-and-unresolved is NOT reported
  broken; a sibling non-allowlisted-and-unresolved path IS still reported
  broken (control case, so the allowlist can't be shown to swallow
  everything).
- `TestBP900b11BuildExitsZero` (2 tests): `build_broken_ref_report()` returns
  an empty list (the exact condition `_check_script_reference_guard` checks
  before returning exit 0) when only an allowlisted reference is present,
  both with an explicit allowlist and with the real default module-level
  allowlist (using the real `scripts/build.py` self-reference entry).
- `TestBP900b11ArchitectureDocDescribesAllowlist` (1 test, **RED**):
  `docs/architecture/components/template-compiler.md` — this ticket's own
  `files_touched` — has no section naming the `EXTERNAL_DEPENDENCY_ALLOWLIST`
  constant, citing AC BP-900b-1-1 by id, or stating the
  resolved/not-broken/exit-zero semantics; it only mentions the lowercase
  phrase "external-dependency allowlist" once, in passing, inside the
  unrelated BP-900c-1 section. Confirmed absent via direct grep before
  authoring the test (matches the precedent set by the sibling
  `unit_tests/build_guards/test_bp900c1_broken_ref_entry_fields.py` doc-gap
  test for BP-900c-1). This is the genuine, ticket-scoped red baseline for
  python-coder to close (add a dedicated "External-Dependency Allowlist (AC
  BP-900b-1-1)" section to the architecture doc).

Verification run: `python -m pytest unit_tests/build_guards/test_bp900b1_1_external_dependency_allowlist.py -v`
→ 4 passed (behavioral, against the already-correct existing implementation),
1 failed (doc gap), exit 1 — genuine mixed red/green baseline, no
syntax/import errors.
red_baseline:
  - test_name: test_ac1_doc_names_allowlist_constant_and_ac_id
    file: unit_tests/build_guards/test_bp900b1_1_external_dependency_allowlist.py
    error: "AssertionError: 'EXTERNAL_DEPENDENCY_ALLOWLIST' not found in '...template-compiler.md contents...' : template-compiler.md must name the EXTERNAL_DEPENDENCY_ALLOWLIST constant (scripts/build_propagation_audit.py) explicitly, not just paraphrase it as 'external-dependency allowlist'."

Note for python-coder: `TestBP900b11AllowlistedReferenceResolved` and
`TestBP900b11BuildExitsZero` (4 tests) pass immediately against the existing
production code — this is expected and documented above (test_drift
classification), not an under-specified assertion; do not treat their
green state as a sign the ticket needs no code changes, only that
`scripts/build_propagation_audit.py` needs no *behavioral* change. The one
red test targets this ticket's `docs/architecture/components/template-compiler.md`
files_touched entry specifically.

Pre-existing, epic-wide ticket-frontmatter schema gap (dangling `depends_on:
BP-900b-1` — the guard reports it as a missing file even though
`05_TICKET-20260611-BP-900b-1.md` exists in this same epic folder with
`source_ac: BP-900b-1`) affects this ticket; same pattern already flagged by
`python-coder` on sibling ticket `05_TICKET-20260611-BP-900b-1.md`. I added
the two missing `change_target`/`risk_surface` fields (mechanical, isolated
to this ticket, values sourced from the AC YAML) but left `depends_on`
untouched — unlike the sibling case, the dependency ticket file genuinely
exists here, so blanking it would be incorrect; this looks like a guard
resolution bug, not a missing dependency, and fixing the guard itself is
outside this ticket's scope. `scripts/feedback/submit_feedback.py` is not
deployed in this worktree (only under `templates/scripts/feedback/`), so
`feedback-id` is `(submit-failed)` per the documented fallback.

### 2026-08-18 10:15 — python-coder (status: ok)
feedback-id: (submit-failed)
completion_manifest:
  red_baseline_closed: true
  no_behavioral_change_needed: true
  files_touched_both_edited: true
red_baseline_results:
  - test_name: test_ac1_doc_names_allowlist_constant_and_ac_id
    result: green
Confirmed test-writer's `test_drift` classification (ADR-003 Rule 1) by
re-reading `check_broken_references`/`build_broken_ref_report`/
`EXTERNAL_DEPENDENCY_ALLOWLIST` in `scripts/build_propagation_audit.py`:
the AC's Gherkin is already fully satisfied by the existing implementation,
so no production behavior changed. Closed the one genuine red test by
adding a new "External-Dependency Allowlist (AC BP-900b-1-1)" section to
`docs/architecture/components/template-compiler.md` that names the
`EXTERNAL_DEPENDENCY_ALLOWLIST` constant, cites AC BP-900b-1-1, and states
the resolved/not-broken/exit-zero semantics verbatim enough to satisfy the
test's three assertions. Also added a `DECISION HISTORY` entry to
`scripts/build_propagation_audit.py` (comment-only, no functional change)
recording the test_drift finding and pointing at the new test file and doc
section, matching this file's existing precedent for doc-only AC entries.

Verification: `python -m pytest
unit_tests/build_guards/test_bp900b1_1_external_dependency_allowlist.py -v`
→ 5 passed (was 4 passed / 1 failed before this change). Also reran the full
`unit_tests/build_guards/` directory plus
`unit_tests/test_build_guard_real_package.py` (99 passed, 4 subtests) and
`unit_tests/test_bp_900a_3.py` (2 passed, 1 pre-existing unrelated masked
xfail for AC BP-900a-3, not in this ticket's scope) — no regressions. Note:
a concurrent sibling ticket (BP-900a-3) landed its own new section in
`docs/architecture/components/template-compiler.md` in this shared worktree
while I was working; I re-verified after that landed that my section and
both files' diffs are still intact and all tests still green.

No `## Agent Contracts` section is present in this ticket (v1 format), so
the §2c AC Coverage Sign-Off recipe was skipped per §2c.1. No
`## Implementation Tasks` section exists either, so §1.5 step 1 was
skipped per its own instruction (no tasks section → skip to step 4).
`scripts/feedback/submit_feedback.py` is not deployed in this worktree
(confirmed via direct invocation, `No such file or directory`), so
`feedback-id` is `(submit-failed)` per the documented fallback — this
matches test-writer's prior finding, not a new gap.

doc-enforcer and complexity-reduction skills were not directly invocable
(no `Skill` tool in this agent's toolset and no deployed skill directories
under `.claude/` in this worktree); manual review found no new/modified
Python functions (only a comment-only DECISION HISTORY addition) and no
docstring-bearing constructs requiring header fields, so no violations
apply to this change.

### 2026-08-18 11:30 — pr-reviewer (status: ok)
feedback-id: (submit-failed)
completion_manifest:
  diff_scoped_to_files_touched: true
  doc_claims_verified_against_code: true
  tests_pass: true
  no_regressions: true
Reviewed the ticket-scoped diff (`git diff HEAD -- scripts/build_propagation_audit.py
docs/architecture/components/template-compiler.md`): 138 insertions, 0 deletions —
purely additive, matching the test_drift/doc-only classification python-coder
recorded. Independently re-read `check_broken_references()` and
`build_broken_ref_report()` in `scripts/build_propagation_audit.py` line-by-line
against the new "External-Dependency Allowlist (AC BP-900b-1-1)" doc section: every
technical claim checks out, including the `_RESOLVED_ALLOWLIST` (EXTERNAL_DEPENDENCY_ALLOWLIST
merged with the empty KNOWN_UNDEPLOYED_ALLOWLIST) default-resolution nuance the doc
calls out explicitly. Ran the ticket's dedicated test file directly (5 passed) plus
the full `unit_tests/build_guards/` + `unit_tests/test_build_guard_real_package.py`
suites (99 passed, 4 subtests) — no regressions. Spot-checked the two functions
(`get_deployable_script_manifest`, `cross_check_refs_against_manifest`) that a
concurrent sibling ticket's section of the same shared doc file cites in
`scripts/build_phases.py` and confirmed both exist, so the shared doc file is not
referencing phantom code. `TestBP900b11ArchitectureDocDescribesAllowlist` exercises
real production functions in its sibling behavioral test classes (not grep-only),
satisfying the "verify behaviorally" convention for the mechanism itself; the doc
test itself is an appropriate string-presence check for a documentation AC. No
high- or medium-confidence findings in this ticket's scope. Note (not a finding
against this ticket): the `ticket_frontmatter_guard.py` hook fired a
`depends_on: BP-900b-1 references missing file` warning on my sign-off edits —
this is the pre-existing epic-wide guard-resolution bug both test-writer and
python-coder already flagged (the referenced sibling ticket file genuinely exists
in this same epic folder); fixing the guard is outside this ticket's scope.
`scripts/feedback/submit_feedback.py` confirmed not deployed in this worktree
(`No such file or directory`), so `feedback-id` is `(submit-failed)` per the
documented fallback, matching prior agents' finding.

### 2026-08-18 12:00 — commit (status: ok)
feedback-id: (submit-failed)
completion_manifest:
  pre_commit_hooks_pass: true
  commit_message_valid: true
  ticket_staged: true
Committed the ticket-scoped diff as `19a4399c5` ("docs(build-pipeline):
document external-dependency allowlist (BP-900b-1-1)"): `scripts/build_propagation_audit.py`
(10-line comment-only DECISION HISTORY entry), the new "External-Dependency
Allowlist (AC BP-900b-1-1)" section in `docs/architecture/components/template-compiler.md`
(29 lines), and test-writer's dedicated test file
`unit_tests/build_guards/test_bp900b1_1_external_dependency_allowlist.py` (5
tests, all green post-commit). This shared worktree has two OTHER tickets
(BP-900a-1-1, BP-900b-2) concurrently editing the same `template-compiler.md`
file and staging their own unrelated changes to `scripts/build.py` /
`scripts/build_phases.py` / their own ticket files while this commit was in
flight; per the "commit into a dirty shared tree" convention I did not use
`git add -A`. I isolated this ticket's exact hunk with a hand-built patch
applied via a scratch git index (`GIT_INDEX_FILE` + `read-tree`/`apply
--cached`) and `git update-index --cacheinfo` to force the real index entry
for `template-compiler.md` to precisely this ticket's content, then unstaged
(via `git reset`, not `checkout`/`clean`) the concurrently-staged
unrelated paths so they weren't swept into this commit. Verified after
commit that none of the sibling tickets' in-flight work was lost — their
sections/files are intact on disk, just unstaged (not lost) pending their
own commit phase. Pre-commit hooks ran clean (all "Passed" or
"no files to check"). The `ticket_frontmatter_guard.py` PostToolUse hook
fired the same pre-existing `depends_on: BP-900b-1 references missing file`
warning already flagged by test-writer/python-coder/pr-reviewer on this
ticket (guard-resolution bug, not a real dependency gap; out of scope here).
`scripts/feedback/submit_feedback.py` is not deployed in this worktree
(only under `templates/scripts/feedback/`), so `feedback-id` is
`(submit-failed)` per the documented fallback.
