---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: needed
  pr-reviewer: needed
  pull-request: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
components:
- build-orchestration
created: '2026-07-07'
depends_on:
- BO-1800b
- BO-1800b-1
files_touched:
- docs/architecture/adrs/ADR-018-agent-isolation-topology.md
- docs/architecture/agent_delivery_workflows.md
priority: medium
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: BO-1800b-3
status: todo
title: No privileged bypass exists, and agents authenticate as a least-privilege identity
  with no direct-push right
---

# No privileged bypass exists, and agents authenticate as a least-privilege identity with no direct-push right

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BO-1800b-3` — No privileged bypass exists, and agents authenticate as a least-privilege identity with no direct-push right — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BO-1800b-3`. Component: `build-orchestration`. Assigned agent: `documentation-expert`. Estimated complexity: `M`.

## Acceptance Criteria

```gherkin
Given the hub `main` branch protection is configured,
When a client with elevated privileges (an admin, an owner, or a privileged token)
  attempts to land on `main` outside the gate,
Then the change is still rejected -- branch protection allows no bypass, so even an
  admin merge or a privileged token cannot land on `main` outside required
  checks + the merge queue,
And the machine identity that autonomous agents authenticate as is least-privilege:
  it holds no bypass permission and no direct-push right to `main`, so the
  capability to mutate `main` directly does not exist for agents.
```

## Sign-offs

- [ ] documentation-expert
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit
- [ ] pull-request

## Comments
