---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  pr-reviewer: needed
  pull-request: needed
  python-coder: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
components:
- build-orchestration
created: '2026-07-07'
depends_on:
- BO-1800d
- BO-1800a-1
files_touched:
- docs/architecture/adrs/ADR-018-agent-isolation-topology.md
- docs/architecture/components/worktree-manager.md
priority: medium
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: BO-1800d-1
status: todo
title: Automatic garbage collection and maintenance are disabled inside a drive clone
  during a drive
---

# Automatic garbage collection and maintenance are disabled inside a drive clone during a drive

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BO-1800d-1` — Automatic garbage collection and maintenance are disabled inside a drive clone during a drive — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BO-1800d-1`. Component: `build-orchestration`. Assigned agent: `python-coder`. Estimated complexity: `S`.

## Acceptance Criteria

```gherkin
Given a drive clone has been created and a drive is active in it,
When the clone's git configuration is inspected during the drive,
Then `gc.auto` is set to 0 and `maintenance.auto` is set to false in that clone,
And these settings are established as part of drive creation (before any work
  commits), so no automatic repack or maintenance can start while the drive runs.
```

## Sign-offs

- [ ] python-coder
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit
- [ ] pull-request

## Comments
