---
advances_current_outcome: true
agents:
  commit: needed
  documentation-expert: not_needed
  llm-expert: needed
  pr-reviewer: needed
  pull-request: needed
  sql-coder: not_needed
  test-runner: needed
  test-writer: needed
components:
- build_orchestration
created: '2026-06-18'
depends_on:
- BO-1300c
files_touched:
- docs/acceptance-criteria/build-orchestration/BO-1300-independent-spot-check/BO-1300c.yaml
- docs/architecture/adrs/ADR-003-test-source-of-truth-discipline.md
priority: high
requires_adr: false
requires_diagram: false
roadmap_phase: phase_1
source_ac: BO-1300c-1
status: todo
title: Reviewers exercise the feature for uncovered gaps and never run the unit test
  suite
---

# Reviewers exercise the feature for uncovered gaps and never run the unit test suite

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `BO-1300c-1` — Reviewers exercise the feature for uncovered gaps and never run the unit test suite — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `BO-1300c-1`. Component: `build-orchestration`. Assigned agent: `llm-expert`. Estimated complexity: `M`.

## Acceptance Criteria

```gherkin
Given a spot-check reviewer is probing a finished feature from its assigned angle
When the reviewer carries out its review
Then the reviewer actually exercises the feature (invokes it, feeds it inputs, walks its
  seams) to look for problems the existing tests and acceptance criteria were never
  written to catch
And the reviewer does NOT run the project's unit test suite
And the reviewer does NOT write or add any new tests
And every finding the reviewer reports describes a gap discovered by using the feature,
  not a failing or passing unit test
```

## Sign-offs

- [ ] llm-expert
- [ ] test-writer
- [ ] test-runner
- [ ] pr-reviewer
- [ ] commit
- [ ] pull-request

## Comments
