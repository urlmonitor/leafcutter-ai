---
ac_coverage: 3/3
ac_traceability:
  id: UXP-700a-2-i
  path: docs/acceptance-criteria/ux-prototyping/UXP-700-truthful-project-record/UXP-700a-2-i.yaml
advances_current_outcome: true
agents:
  ac-fulfillment-gate: failed
  ac-validator: signed_off
  architect-review: not_needed
  commit: needed
  documentation-expert: not_needed
  documentation-verifier: not_needed
  frontend-coder: not_needed
  pr-reviewer: not_needed
  pull-request: not_needed
  python-coder: signed_off
  sql-coder: not_needed
  test-runner: signed_off
  test-writer: signed_off
  user-surface-smoker: not_needed
change_target: code
complexity: high
components:
- ux_prototyping
created: '2026-09-09'
depends_on: []
files_touched: []
priority: medium
requires_adr: false
requires_diagram: false
risk_surface: internal
roadmap_phase: phase_1
source_ac: UXP-700a-2-i
status: todo
title: Zero-artifact derived lookups are present and empty, never absent
---

# Zero-artifact derived lookups are present and empty, never absent

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `UXP-700a-2-i` — Zero-artifact derived lookups are present and empty, never absent — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `UXP-700a-2-i`. Component: `ux-prototyping`. Assigned agent: `python-coder`. Estimated complexity: `XS`. Complexity: `high`.

## Acceptance Criteria

```gherkin
Given the store index produced for a record holding zero artifacts,
When a reader asks that index for the artifacts belonging to a component, to an entity, to a
  journey, and to an acceptance criterion,
Then each of those four lookups returns an empty result,
And no lookup fails on account of its entry being missing from the index,
And the state "this lookup was never written" does not occur, so an empty result is the only
  thing a reader has to interpret.
```

- [x] AC-1: each of those four lookups returns an empty result
- [x] AC-2: no lookup fails on account of its entry being missing from the index
- [x] AC-3: the state "this lookup was never written" does not occur, so an empty result is the only

## Test Requirements

```yaml
tests:
- name: test_zero_artifact_index_has_every_derived_lookup_present_and_empty
  file: unit_tests/product_truth/test_uxp_700a_2_i.py
  covers:
  - UXP-700a-2-i
  asserts: Each of the four derived lookups is a present key whose value is an empty
    collection.
  framework: unittest
  type: unit
  angle: boundary
- name: test_uxp_700a_2_i_reachable_from_entry_point
  file: unit_tests/product_truth/test_uxp_700a_2_i.py
  covers:
  - UXP-700a-2-i
  angle: reachability
  asserts: 'REQUIRED — invoke the production entry point (CLI, hook, slash command,
    workflow dispatch, or main()) as a subprocess/dispatch and assert the new behaviour
    actually occurs. Do NOT satisfy this by importing the function directly. The AC
    authored no test_spec, so the entry point is not declared: resolve it before writing
    this test, and do not delete this entry.'
```

## Agent Contracts

### Expects From

- **AC:** UXP-700a-2
- **Contract:** The zero-artifact index whose shape this constrains.

## Sign-offs

- [x] test-writer — 2026-09-09 15:35
- [x] python-coder — 2026-09-09 16:10
- [x] test-runner — 2026-09-09 15:43
- [x] ac-validator — 2026-09-09 16:15
- [ ] ac-fulfillment-gate — failed 2026-09-09 16:35
- [ ] commit

## Comments

### 2026-09-09 15:35 — test-writer (status: ok)
feedback-id: fb_2026-09-09_3ab7e432
completion_manifest:
  tests_written: true
  red_baseline_or_mutation_proof:
    result: mutation_proof
    reason: "Both new tests (test_zero_artifact_index_has_every_derived_lookup_present_and_empty,
      test_uxp_700a_2_i_reachable_from_entry_point) pass on the unmutated repo -- this is a
      negative-control assertion (present-and-empty, never absent) that generate_product_truth.py
      already satisfies unconditionally in write_index() (by_component/by_entity/by_flow/by_ac are
      assigned with no truthiness guard). Per unit_tests/README.md #1 ('A passing test is not
      evidence until you know it can fail'), a red baseline is structurally unavailable for this
      test shape, so a mutation proof was performed instead: write_index() was temporarily patched
      to `if _by_component: index[\"by_component\"] = _by_component` (the exact 'never written'
      leak AC UXP-700a-2-i forbids), both tests went RED (AssertionError: 'by_component' not found
      in index -- verified with AC_ENFORCE_STRICT=1 to bypass the not-done-AC xfail mask), the
      mutation was reverted (git checkout -- confirmed via git diff, zero lines), and both tests
      returned to green. The mutation was applied to the one real copy of the script
      (docs/product-truth/scripts/generate_product_truth.py) that BOTH tests load -- the boundary
      test imports it directly and the reachability test copies its exact bytes into the fixture
      it subprocesses, so the mutation is guaranteed to land in the copy under test (unit_tests/README.md
      section 1a's copy-identification concern)."
    remediation: "No action needed from python-coder unless a future change reintroduces a
      truthiness guard around by_component/by_entity/by_flow/by_ac in write_index() -- these
      two tests exist specifically to catch that regression, as proven by the mutation above."
  cross_layer_seam_answer:
    result: not_applicable
    reason: "This AC scopes only the SHAPE of the generator's own output (index.json's four
      derived-lookup keys being present-and-empty for a zero-artifact store) -- it is a boundary
      condition on one producer, not a producer/consumer pair. The downstream consumer side (the
      record's checker reaching a verdict against this index) is explicitly owned by sibling ACs
      (UXP-700a-2's own third criterion and UXP-700b-1), which is where a real producer-to-real-consumer
      seam test belongs; duplicating that here would test the same consumer twice under two
      different ACs' names."
    remediation: "n/a -- not_applicable is a first-class outcome, not a defect."
  reachability_entry_point_answer:
    result: resolved
    entry_point: "python docs/product-truth/scripts/generate_product_truth.py --now <date> --quiet
      (CLI via subprocess) -- resolved via Reachability Entry-Point Resolution step 1 (CLI script):
      the module has main() guarded by `if __name__ == \"__main__\":` with argparse, invoked in
      production exactly this way; test_uxp_700a_2_i_reachable_from_entry_point copies the real
      script's verbatim bytes into a fixture store and runs it with subprocess.run + sys.executable."
Wrote unit_tests/product_truth/test_uxp_700a_2_i.py (new directory, __init__.py added) with two
tests per the ticket's Test Requirements: a boundary test asserting by_component/by_entity/by_flow/by_ac
are present-and-empty for a zero-artifact store, and a reachability test invoking the real CLI script
as a subprocess. Both pass on arrival against the current, unmutated generate_product_truth.py --
this is expected for this AC's negative-control shape (see unit_tests/README.md #1); a mutation
proof (documented above) confirms both tests actually constrain the behaviour the AC forbids. No
production code was changed by this phase (mutation was injected and fully reverted; `git diff`
on generate_product_truth.py is empty).

### 2026-09-09 16:10 — python-coder (status: ok)
feedback-id: fb_2026-09-09_0f5db97c
completion_manifest:
  upstream_dependency_verified: true
  production_code_satisfies_ac: true
  tests_green: true
  no_production_changes_required: true
Verified `docs/product-truth/scripts/generate_product_truth.py`'s `write_index()`
(lines 570-573) assigns `index["by_component"]`, `index["by_entity"]`, `index["by_flow"]`,
and `index["by_ac"]` unconditionally, with no truthiness guard, exactly matching
test-writer's mutation-proof claim -- this is the "never written" leak AC UXP-700a-2-i
forbids, and it is structurally impossible in the current code. Ran both tests in
unit_tests/product_truth/test_uxp_700a_2_i.py directly
(`python -m unittest unit_tests.product_truth.test_uxp_700a_2_i -v`): 2 passed, 0
failed, 0.24s. AC-1 (each of the four lookups returns an empty result), AC-2 (no lookup
fails on account of its entry being missing from the index), and AC-3 (the "never
written" state does not occur) are all satisfied by the existing implementation. No
files were changed by this phase -- this ticket's `python-coder` role is verification
of an already-correct production entry point, per test-writer's own sign-off note
("No action needed from python-coder unless a future change reintroduces a truthiness
guard"). doc-enforcer and complexity-reduction were not invoked because no `.py` file
was touched.

### 2026-09-09 15:43 — test-runner (status: ok)
feedback-id: (submit-failed)
completion_manifest:
  test_suite_executed: true
  all_tests_passing: true
  failure_report_structured: true
Ran `pytest unit_tests/product_truth/test_uxp_700a_2_i.py -v` (single-file action -- this repo's
routing table doesn't cover `unit_tests/product_truth/**`, so I ran the exact test file the
ticket's Test Requirements name): 2 passed, 0 failed, 0.28s
(test_uxp_700a_2_i_reachable_from_entry_point, test_zero_artifact_index_has_every_derived_lookup_present_and_empty).
Also ran the wider `unit_tests/product_truth/` directory for context: 9 failures surfaced there,
all in test_uxp_700a_1.py, test_uxp_700b_1_i.py, and test_uxp_700b_2_i.py -- files belonging to
sibling tickets whose AC work_status is not yet `done` (their own AC-enforcement xfail masking
confirms this), not to this ticket's scope, so they are out of scope for this sign-off and are
flagged here as an anomaly for the ticket-supervisor rather than blocking this ticket.
`scripts/feedback/submit_feedback.py` is absent (only `templates/scripts/feedback/submit_feedback.py`
exists in this repo state), so feedback-id falls back to `(submit-failed)` per §2a.

### 2026-09-09 16:15 — ac-validator (status: ok)
feedback-id: (submit-failed)
completion_manifest:
  acs_parsed: true
  implementation_evidence_found: true
  test_evidence_found: true
  store_alignment_checked: true
  cross_file_contract_traced: true
All 3 ACs covered; ticket checkboxes flipped and ac_coverage: 3/3 recorded.

AC-1 (each of those four lookups returns an empty result) covered:
  implementation: docs/product-truth/scripts/generate_product_truth.py:570-573 (write_index()) --
    `index["by_component"] = build_by_component(...)`, `index["by_entity"] = build_by_entity(...)`,
    `index["by_flow"] = by_flow`, `index["by_ac"] = new_by_ac` -- unconditional assignment, no
    truthiness guard. Confirmed by direct Read of the current committed file (this file is not in
    the diff -- files_touched: [] is correct; the ticket is verification-only per python-coder's
    2026-09-09 16:10 comment).
  test: test_zero_artifact_index_has_every_derived_lookup_present_and_empty
    (unit_tests/product_truth/test_uxp_700a_2_i.py:96-118) asserts `index[key] == {}` for all four
    keys against a zero-artifact fixture store. Ran locally:
    `python -m unittest unit_tests.product_truth.test_uxp_700a_2_i -v` -> 2 passed, 0 failed, 0.194s.

AC-2 (no lookup fails on account of its entry being missing from the index) covered:
  implementation: same lines as AC-1 -- the unconditional assignment means no KeyError/lookup
    failure is structurally possible for a zero-artifact store.
  test: same boundary test plus test_uxp_700a_2_i_reachable_from_entry_point
    (unit_tests/product_truth/test_uxp_700a_2_i.py:139-166), which subprocesses the real CLI
    (`generate_product_truth.py --now <date> --quiet`) and asserts returncode 0 and `assertIn(key,
    index)` for all four keys -- no crash, no missing-key failure via the real entry point.

AC-3 (the "never written" state does not occur) covered:
  implementation: same lines as AC-1 (unconditional assignment guarantees the key is always
    written).
  test: both tests above assert `assertIn(key, index)` before checking emptiness, directly pinning
    "present, never absent." test-writer's sign-off comment (2026-09-09 15:35) additionally
    documents a mutation proof: write_index() was temporarily patched to
    `if by_component: index["by_component"] = by_component` (the exact leak this AC forbids), both
    tests went RED, and the mutation was reverted (git diff empty) -- this substitutes for a red
    baseline per unit_tests/README.md #1, since this AC's negative-control shape has no natural red
    phase.

Store-alignment check: `python scripts/commit_guardian/check_v2_ac_store_alignment.py --ticket
tickets/00_inbox/epics/EPIC-TruthfulProjectRecord/06_TICKET-20260909-UXP-700a-2-i.md` exited 0, no
violations.

Cross-file contract trace (Step 2d): this ticket's `## Agent Contracts` names one `Expects From`
entry -- AC UXP-700a-2 ("the zero-artifact index whose shape this constrains"), sibling ticket
`05_TICKET-20260909-UXP-700a-2.md` (status: todo, all agents still `needed`). The producer surface
is `generate_product_truth.py`'s `generate()`/`write_index()` -- the same file both this ticket's
tests exercise directly (import) and via the real CLI subprocess. Read confirms the producer
function already implements the contract (unconditional by_component/by_entity/by_flow/by_ac
assignment) and both of this ticket's tests invoke that real code path end-to-end, so there is no
contract gap for this AC. Anomaly: sibling ticket UXP-700a-2 itself is still `status: todo` with no
agent signed off, even though the production code it is meant to deliver already exists and passes
this ticket's tests -- flagging for ticket-supervisor/epic-supervisor awareness, consistent with
test-runner's 2026-09-09 15:43 comment about the same sibling-ticket family being out of sync.

`scripts/feedback/submit_feedback.py` is absent (only `templates/scripts/feedback/submit_feedback.py`
exists in this repo state), so feedback-id falls back to `(submit-failed)` per §2a.

### 2026-09-09 16:35 — ac-fulfillment-gate (status: blocker)
feedback-id: (submit-failed)
Ran `scripts/ac_store/ac_coverage_resolver.py --ticket <this ticket>`: resolved 1 AC
(UXP-700a-2-i, via traceability_block, two-key id/path form), level L3, initial failures
[work_status, covered_by]. Auto-fixed `covered_by` (Step 3c): grepped `tests/` and
`unit_tests/` for `# covers: UXP-700a-2-i` and found two genuine tags in
`unit_tests/product_truth/test_uxp_700a_2_i.py` (lines 97, 134); appended that path to
`covered_by: []` -> `covered_by: [unit_tests/product_truth/test_uxp_700a_2_i.py]`
(append-only edit). Ran `scripts/commit_guardian/check_ac_schema.py` on the edited YAML
afterward -- exit 0, schema valid. Re-ran the resolver: `covered_by` now passes;
`work_status` (still `todo`) remains the sole failure.

`work_status` could NOT be auto-fixed (Step 3a): its criterion requires
`files_touched ∩ diff` to be non-empty, and this ticket's own frontmatter declares
`files_touched: []`. That intersection is empty against any diff by construction, so
there is no diff-based evidence this gate is permitted to act on for this field --
per the gate's own Constraints ("No false positives... do not infer fulfillment from
descriptions or commit messages"), the rich verification evidence documented in
python-coder's and ac-validator's comments (pointing at pre-existing, already-committed
lines 570-573 of `docs/product-truth/scripts/generate_product_truth.py`) cannot substitute
for diff evidence tied to `files_touched`, however credible. This is a determined
"zero evidence" outcome, not an ambiguous one, so it is reported as a blocker rather than
a stop-and-ask.

AC store fulfillment incomplete:
UXP-700a-2-i: work_status is `todo`, not `done`, and Step 3a's auto-fix precondition
(files_touched ∩ diff non-empty) cannot be satisfied because files_touched is `[]`.
Auto-fix was not possible: no file is declared touched by this ticket for the gate to
correlate against the branch diff.
Suggested remediation: either (a) have a maintainer or the commit phase manually set
`work_status: done` in
`docs/acceptance-criteria/ux-prototyping/UXP-700-truthful-project-record/UXP-700a-2-i.yaml`
once the verification-only closure documented by python-coder/ac-validator is accepted as
sufficient grounds, or (b) record the verified production file
(`docs/product-truth/scripts/generate_product_truth.py`) in this ticket's `files_touched`
so a future run of this gate can auto-fix `work_status` from real diff evidence.
