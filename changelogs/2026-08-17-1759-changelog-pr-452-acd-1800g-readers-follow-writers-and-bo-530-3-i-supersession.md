---
title: "Changelog PR #452 — ACD-1800g (readers follow the writers) and the BO-530-3-i supersession"
date: "2026-08-17"
time: "17:59"
type: manual
components:
  - ac_driven_dev
  - build_orchestration
summary: "Closed the last hole in the migration plan — when a requirement starts carrying its own proof, the specialists that act on it now have an owner making sure they actually read it — and retired an older requirement whose foundation the migration removes, recording exactly what happens to its test."
description: "1 commit, PR #452 — two follow-ups from the AC-Driven Build v2 sequencing pass in PR #450. ACD-1800g: four independent IT-PO passes over the new ACD-1600g, ACD-1800f and ACD-2000 trees flagged the same structural gap — several ACs have a mixed surface whose Python half is pytest-checkable but which also require repointing a prompt or JS surface no pytest assertion can verify (templates/agents/user-surface-smoker.md, templates/agents/live-surface-tester.md, templates/agents/change-scope-reviewer.md, templates/workflows-js/fast-lane-ship.js, templates/skills/building-epics/SKILL.md). Without an AC owning that half the residual risk is exact: the store can hold correct proofs the craft never reads, which is the phantom-done defect with the arrow reversed. The AC is verifiable rather than aspirational because render_effective_prompt.py (ACD-1700c) assembles an agent's full brief before a run, so 'the rendered brief resolves the store field and no longer resolves the removed ticket-body block' is an assertion that executes; the renderer also inlines each skills_invoked SKILL.md body via _resolve_agent_skills, so the building-epics repoint rides the same lever rather than needing its own mechanism. depends_on lists ACD-1700c for the renderer plus ACD-1800f and ACD-1600g because a craft cannot be repointed at a declaration slot that does not exist yet; ACD-1800c is deliberately excluded because per ADR-026 read-side-before-write-side this L1 is a prerequisite of ticket demotion and the edge would invert the migration order. Four verified hazards are recorded in the notes for the BA: the renderer prefers the deployed .claude/agents copy over templates/ so a test that skips build.py renders a stale brief; a rendered-brief assertion can still be presence-shaped unless it is differential across two fixture ACs and includes the negative half; the renderer cannot cover fast-lane-ship.js because that is a workflow not an agent template, so the existing unit_tests/_workflow_engine_harness.py should assert on the captured dispatch sequence rather than on meta.phases which is a progress-UI declaration not the executed order; and the five reader surfaces exactly consume the 5-L2 cap. This takes ACD-1800 to 7 of 7 L1 children — the tree is now full, recorded in both files. BO-530-3-i: superseded by ACD-2000a-2 (cap reached, no re-dispatch, escalate), ACD-2000a-2-i (the escalation payload clauses) and ACD-2000a-3 (configurable cap, message states the configured value) — mapped scenario by scenario because no single successor covers it. The reason is not that its test was weak: building-epics/SKILL.md states retry counters are in-memory and not persisted and justifies that because the on-disk agents map and comment log already hold the history, and ACD-1800c-1 removes both halves of that justification, so the AC had no durable place to count attempts once the migration lands. Separately verified that test_failure_rework_cap, the key its it_requirements call configurable, has exactly one occurrence repo-wide (that SKILL.md line) and is read by no code on any path. The presence-only test unit_tests/test_generate_ticket_from_ac.py::test_bo530_3i_building_epics_documents_test_failure_rework_cap is left in place deliberately, per store convention checked against 10 existing precedents where a covers tag points at a superseded AC and the test was retained (KM-VIS-013, six KM-KQS entries, ACD-400b, ACD-400b-5, FIN-100c-3): superseding a requirement means it has been restated, not that the behaviour stopped mattering. done_proof._collect_dangling_tags will report the tag as dangling because it requires status active, but nothing gates on dangling tags; deleting the test now would drop the only check that the documentation exists with no replacement, since ACD-2000a-3 is still draft and its it_requirements already bind the replacement to the same change that makes the cap mechanically real. All of that is written into BO-530-3-i's amended_by including an explicit instruction not to mark the AC done merely to silence the dangling report. Verified: all 39 files in the ACD-1800 folder valid against config/ac_store_schema.json via explicit glob; every superseded_by target resolves to a real AC; zero dependency cycles store-wide; the parent ACD-1800.yaml edit purely additive at 7 insertions and 0 deletions; full pre-commit suite green. Noted but not fixed: seven pre-existing schema violations in BO-500-computed-quality-gates using list-form it_requirements where the build-orchestration plus python-coder conditional requires object form. No production code changed; both ACs remain readiness draft."
pr: 452
commits:
  - 5609b2f49
breaking: false
---

## Entry
