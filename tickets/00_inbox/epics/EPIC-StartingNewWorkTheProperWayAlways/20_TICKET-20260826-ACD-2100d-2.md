---
ac_traceability:
  id: ACD-2100d-2
  path: docs/acceptance-criteria/ac-driven-dev/ACD-2100-entry-point-unblocked/ACD-2100d-2.yaml
ac_coverage: 3/3
advances_current_outcome: true
agents:
  ac-fulfillment-gate: signed_off
  ac-validator: signed_off
  architect-review: signed_off
  commit: failed
  documentation-expert: signed_off
  documentation-verifier: not_needed
  pr-reviewer: failed
  pull-request: needed
  python-coder: signed_off
  test-runner: signed_off
  test-writer: signed_off
change_target: pipeline
complexity: medium
components:
- ac_driven_dev
- build_pipeline
created: '2026-08-26'
depends_on: []
documentation_required: true
files_touched:
- scripts/build_helpers.py
- scripts/commit_guardian/check_output_drift.py
- scripts/commit_guardian/commit_guardian.json
- unit_tests/build_guards/test_acd_2100d_2.py
priority: medium
requires_adr: false
requires_diagram: false
risk_surface: contract_boundary
roadmap_phase: phase_1
source_ac: ACD-2100d-2
status: todo
title: A repair that is missing from the source an installed file is generated from
  is reported as not delivered
---

# A repair that is missing from the source an installed file is generated from is reported as not delivered

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `ACD-2100d-2` — A repair that is missing from the source an installed file is generated from is reported as not delivered — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `ACD-2100d-2`. Component: `ac-driven-dev`. Assigned agent: `python-coder`. Estimated complexity: `M`. Complexity: `medium`.

## Acceptance Criteria

```gherkin
Given a working copy in which an installed route file carries a repair that the
  source it is generated from does not carry,
When the check that decides whether a change is delivered runs over that working
  copy,
Then it reports the installed file as carrying behaviour its source does not and
  names the file,
And the change is not reported as delivered while that remains true,
And running the installer over that working copy leaves the installed file
  without the repair -- confirming the report rather than contradicting it.
```

- [x] AC-1: it reports the installed file as carrying behaviour its source does not and
- [x] AC-2: the change is not reported as delivered while that remains true
- [x] AC-3: running the installer over that working copy leaves the installed file

## Test Requirements

```yaml
tests:
- name: test_installed_file_carrying_a_repair_its_source_lacks_is_reported
  file: unit_tests/build_guards/test_acd_2100d_2.py
  covers:
  - ACD-2100d-2
  asserts: A working copy in which an installed route file carries a change its source
    does not; the check reports that file by name as carrying behaviour its source
    does not.
  framework: unittest
  type: integration
  angle: criterion
- name: test_change_is_not_reported_as_delivered_while_the_divergence_stands
  file: unit_tests/build_guards/test_acd_2100d_2.py
  covers:
  - ACD-2100d-2
  asserts: The verdict is consumed where 'delivered' is decided -- the divergent working
    copy is not reported as delivered -- so the check is wired, not merely present.
    A run that examines files and checks none of them, exiting clean, must fail this
    test.
  framework: unittest
  type: integration
  angle: reachability
- name: test_running_the_installer_confirms_the_report_by_removing_the_repair
  file: unit_tests/build_guards/test_acd_2100d_2.py
  covers:
  - ACD-2100d-2
  asserts: The installer is run over the same working copy and the installed file
    afterwards no longer carries the repair -- the report is confirmed by what the
    installer actually does, not by an opinion about paths.
  framework: unittest
  type: integration
  angle: real_artifact
- name: test_a_generated_file_that_only_differs_by_generation_is_not_reported
  file: unit_tests/build_guards/test_acd_2100d_2.py
  covers:
  - ACD-2100d-2
  asserts: A freshly installed file that differs from its source only in ways generation
    explains produces no report -- the check is not a raw byte comparison.
  framework: unittest
  type: integration
  angle: boundary
```

## Implementation Notes

```yaml
- The check must derive the source-to-installed relationship from the installer's
  own mapping, not from a hand-maintained list of file pairs. A second list of pairs
  goes stale the first time a file is added and the check then reports clean on the
  file it cannot see.
- The check must name the file. 'Something is out of sync' cannot be acted on and
  cannot be distinguished by a test from a check that found nothing.
- The check must not fire on a generated file that merely differs from its source
  in a way generation explains -- template expansion, header injection, path rewriting.
  Comparing raw bytes across a transform produces a check that is always red and is
  therefore always ignored.
- The check must be usable from the point where 'delivered' is decided, and its result
  must be consumed there. A check that computes a correct verdict nobody reads is
  inert -- the phantom-gate failure mode this repo keeps rediscovering.
- 'Must satisfy the repository error-handling policy: reading both copies is external
  I/O.'
- 'Must be idempotent and side-effect free: running the check must not modify either
  copy.'
- This is not a ban on touching an installed copy while debugging. The requirement
  is that a repair living only in build output is visible as undelivered before someone
  reports it as done.
- 'THE CHECK IS NOT MISSING -- IT IS PRESENT AND INERT, AND THAT IS A HARDER STARTING
  POINT THAN A BLANK PAGE. Verified by running scripts/commit_guardian/check_output_drift.py
  in this worktree on 2026-08-26: it exited 0 having examined 169 output files and
  checked exactly none of them. Its scan list is a hardcoded set of directory names,
  while the installer''s mapping keys every entry under the configured output root
  -- two namespaces that never intersect, so every file falls through the not-in-mapping
  branch. The first requirement above is therefore about the SCAN SIDE, not the mapping:
  the mapping is installer-derived and already correct. Derive the set of files to
  check, and the key each is looked up under, from the installer''s mapping itself,
  so a newly deployed directory is covered the day it appears.'
- 'THE NOT-IN-MAPPING BRANCH MUST NOT STAY FAIL-OPEN. Today an output file with no
  mapping entry prints an informational line and is skipped. That is indistinguishable,
  from the outside, from a file that was checked and found clean -- and it is the branch
  every one of those 169 files took. A file that is deployed but unmapped is either
  a defect in the mapping or an untracked output; either way the check must say so
  in a way that a caller can act on, because ''I could not see this file'' is the exact
  condition this record exists to make visible.'
- REPAIRING THE SCRIPT ALONE LEAVES THE GATE HALF-DEAD. The pre-commit registration
  in scripts/commit_guardian/commit_guardian.json restricts the hook to a hardcoded
  set of path prefixes. A deployed route file under a differently-configured output
  root does not match, so the hook never runs and the corrected verdict is never computed.
  Fix the trigger and the check together, and prefer deriving both from the same configured
  output root rather than restating it in a third place.
- 'OWNERSHIP BOUNDARY WITH ACD-2100d-2-i. This record owns the determination -- the
  installer-derived mapping and the per-file divergence verdict -- and the files that
  compute and report it. ACD-2100d-2-i owns the install-time announcement and the installer''s
  write paths. Neither record should modify the other''s files: the determination reaches
  the announcement through the delivers_to contract, not through a shared edit surface.'
```

## Agent Contracts

### documentation-expert

Existing docs to update / cross-link:

- docs/reference/plan-feature-layout-and-startup-checks.md (relationship: describes | status: planned | relevance: The reference page authored by ACD-2100d-4 must state which directory a repair has to land in for an install to carry it -- this record is the check that enforces it.)
- docs/known-issues/ac-driven-dev.md (relationship: describes | status: exists | relevance: KI-ACD-004's workaround note -- the deployed copy was patched by hand on 2026-08-18 and build.py overwrote it.)
- docs/architecture/adrs/ADR-001-self-hosting-boundary.md (relationship: describes | status: exists | relevance: Which directories are source and which are build output.)
- docs/build-pipeline.md (relationship: describes | status: exists | relevance: The generated-file relationships this check reasons over.)
- docs/architecture/components/build-orchestration.md (relationship: describes | status: exists | relevance: Where a delivery check sits relative to the rest of the build surface.)

This record owns none of the documents above -- every entry is a cross-link to a
document another record produces. The contract line that stood here demanded
`docs/reference/plan-feature-layout-and-startup-checks.md`, which ACD-2100d-4
creates; this ticket could never have satisfied it. Removed per BO-2200c-5-i
and -iii; the list above is retained as reading for the implementer.

documentation-expert's 08:00 sign-off stands: it wrote docs/build-drift-hook.md
and the commit-guardian known-issues entry, both committed. Only the coverage
requirement naming another record's page is withdrawn.

pr-reviewer and commit remain `failed` deliberately. Their findings -- a ruff
F541 and an assertion that could not fail -- were real and are fixed in
cdd8ea320; those phases need re-running to record a fresh verdict, not
hand-editing to a pass.

### Delivers To

- **Agent:** python-coder
- **Contract:** An installer-derived source-to-installed mapping plus a divergence determination per generated file -- the same determination the installer needs in order to announce that it is replacing a locally changed file. Delivered as a callable determination, not as a shared edit surface: ACD-2100d-2-i reads it and does not modify the files that compute it.

## Sign-offs

- [x] architect-review — 2026-08-26 16:06
- [x] test-writer — 2026-08-26 16:45
- [x] python-coder — 2026-08-26 17:05
- [x] test-runner — 2026-08-26 17:30
- [x] documentation-expert — 2026-08-31 08:00
- [ ] pr-reviewer — failed 2026-08-31 08:15
- [x] ac-validator — 2026-08-31 08:30
- [x] ac-fulfillment-gate — 2026-08-31 07:40
- [ ] commit — failed 2026-08-31 09:10
- [ ] pull-request

## Comments

### 2026-08-26 16:06 — architect-review (status: ok)

feedback-id: fb_2026-08-26_7b2e4646
completion_manifest:
  blast_radius_reviewed: true
  rubric_classification_recorded: true
  anomaly_flagged: true
Classified SMALL: files_touched is 4 files (scripts/build_helpers.py, scripts/commit_guardian/check_output_drift.py, scripts/commit_guardian/commit_guardian.json, unit_tests/build_guards/test_acd_2100d_2.py) — all within the single build-pipeline/commit_guardian tool-chain surface, no cross-package boundary crossing, and none of the always-large triggers (no Alembic migration, no hypertable change, no FastAPI/public-API surface, no ADR contract edit) fire. `requires_adr: false` and `requires_diagram: false` in the ticket frontmatter are confirmed correct. IMPORTANT ANOMALY for test-writer/python-coder: reading the current worktree HEAD (commit 83737a447) shows `check_output_drift.py`'s `_derive_scan_dirs()` already derives the scan set from the manifest's `output_mappings` keys (not a hardcoded directory list), the not-in-mapping branch already reports GAP/EXEMPT (never silent), and `commit_guardian.json` (both the repo copy and its `templates/scripts/commit_guardian/commit_guardian.json` mirror) already registers the hook with `always_run: true` instead of the stale hardcoded `files:` prefix list — the DECISION HISTORY block in the file attributes these exact fixes to "EPIC-BuildPipelinePhantomRemediation" review rounds dated 2026-08-25/26, i.e. this defect appears to have already been fixed by concurrent/prior work sharing this branch (BP-100k family), independently of this AC. Before implementing, python-coder should re-run the check against a deliberately-diverged fixture to confirm whether ACD-2100d-2's behavioural requirements are already met; if so, the remaining work narrows to authoring `unit_tests/build_guards/test_acd_2100d_2.py` per the four test_spec descriptors (criterion/reachability/real_artifact/boundary angles) to lock the behavior in, rather than re-implementing the scan/report logic. No design concerns beyond that: the ownership boundary with ACD-2100d-2-i (determination vs. announcement) is respected by the files_touched list, and no acceptance-criteria adjustments are needed.

### 2026-08-26 16:45 — test-writer (status: ok)

feedback-id: fb_2026-08-26_527b3f98
completion_manifest:
  tests_written_per_test_spec: true
  tests_green_before_coder_ran: true
  tdd_order_deviation_documented: true
Authored `unit_tests/build_guards/test_acd_2100d_2.py` with all four test_spec descriptors (criterion/reachability/real_artifact/boundary), each importing and calling the REAL, unedited fixture helpers from `test_bp_100k_2.py` (`_build_synthetic_full_package`, `_deploy_agents_and_write_manifest`, `_deploy_hook`, `_run_hook`) rather than hand-typed fixtures, per the Fixture Authenticity mandate. `test_running_the_installer_confirms_the_report_by_removing_the_repair` is a real-artifact round-trip: it hand-edits a real deployed file on disk, re-invokes the real `build_phases.build_agents()` installer, and reads the artifact back off disk to confirm the repair is gone — never mocking the write.

HONEST TDD-ORDER DEVIATION (per CLAUDE.md "TDD Order — test-writer Must Precede python-coder"): I ran the new suite before any coder work, per protocol, and all 4 tests passed on first run (`4 passed in 5.67s`, `python3 -m pytest unit_tests/build_guards/test_acd_2100d_2.py -v`) — **no red baseline exists**. This is not an under-specified-test false green: I independently reproduced architect-review's anomaly by hand before writing any assertion (a placeholder-bearing template deploys with genuinely different bytes yet reports clean; a hand-edited deployed file reports BLOCKED naming the exact key with exit 1; re-running `build_agents()` over the same working copy removes the hand-edited repair byte-for-byte). Each assertion targets the real `main()`/`check_output_drift()` entry point pre-commit would invoke (never an internal function called directly), so a reintroduction of the pre-2026-08-25 regressions named in `check_output_drift.py`'s own DECISION HISTORY (hardcoded scan dirs, a duplicated permissive `main()`, a hardcoded `files:` trigger in `commit_guardian.json`) would still turn these tests red. Per architect-review's explicit instruction ("the remaining work narrows to authoring the test file... to lock the behavior in, rather than re-implementing"), I did not attempt to force artificial redness by weakening the already-correct production code. `red_baseline_verified: false` is reported honestly in my structured output — python-coder should re-run this exact suite first; if it stays green with no production edit, that confirms the anomaly and python-coder's own sign-off comment should say so explicitly rather than claiming new work was done. No `## AC Coverage` table exists in this ticket body and there is no `### test-writer` subsection under `## Agent Contracts`, so §2c does not apply (falls through to plain §2 atomic sign-off per the v1 fallback / no-agent-section skip rule).

### 2026-08-26 17:05 — python-coder (status: ok)

feedback-id: fb_2026-08-26_71ae3479
completion_manifest:
  upstream_dependencies_verified: true
  production_code_matches_delivers_to_contract: true
  tests_green:
    result: true
    reason: "n/a — used for note only"
  no_op_confirmed_before_editing: true
CONFIRMED, NOT RE-IMPLEMENTED: independently re-verified architect-review's and test-writer's anomaly finding before touching anything. Read `scripts/commit_guardian/check_output_drift.py` in full: `_derive_scan_dirs()` already derives the scan set from `output_mappings` keys (never a hardcoded directory list, per its own DECISION HISTORY dated 2026-08-25/26), the not-in-mapping branch already emits `UNCOMPARABLE: GAP`/`EXEMPT` (never a silent skip), and `check_output_drift()` is the single scan+report+verdict implementation `main()` delegates to (no duplicated permissive `main()`). Read `scripts/build_helpers.py`: `write_build_manifest()` already records `output_mappings_error` and `output_mappings_skipped_sections` and `_compute_output_mappings()` derives the installer's own source-to-installed mapping — the installer-derived mapping this AC's `Delivers To` contract calls for already exists and is what the hook reads. Read `scripts/commit_guardian/commit_guardian.json`: the `check-output-drift` entry already carries `"always_run": true` (not a stale hardcoded `files:` prefix trigger), and `diff`'d identically against its `templates/scripts/commit_guardian/commit_guardian.json` mirror (only the `{{config.output_root}}` template-placeholder lines differ elsewhere in the file — expected, generation-only). Re-ran `unit_tests/build_guards/test_acd_2100d_2.py` fresh via `python3 -m pytest`: all 4 tests (criterion/reachability/real_artifact/boundary) pass in a clean process (4 passed in 4.95s), independently reproducing test-writer's own spot-check rather than trusting the prior report. Per the Contract-Shrinkage/no-op discipline and architect-review's explicit instruction, made NO production edits to `scripts/build_helpers.py`, `scripts/commit_guardian/check_output_drift.py`, or `scripts/commit_guardian/commit_guardian.json` — editing already-correct code to manufacture a diff would itself be the phantom-work failure mode this repo's CLAUDE.md warns against ("Commit messages must match the diff"). `red_baseline_verified: false` (test-writer's own honest report; no red baseline exists because production already satisfies the AC via concurrent EPIC-BuildPipelinePhantomRemediation work) — reported here explicitly rather than silently claimed as new work. No files were touched by this phase; `unit_tests/build_guards/test_acd_2100d_2.py` was authored by test-writer, not this phase. doc-enforcer and complexity-reduction were not invoked because no `.py` file was created or edited by this phase — nothing to lint or check for complexity growth.

### 2026-08-26 17:30 — test-runner (status: ok)
feedback-id: fb_2026-08-26_54114a5e
completion_manifest:
  test_suite_executed: true
  all_tests_passing: true
  failure_report_structured: true
Ran the full `unit_tests/build_guards` suite (137 tests, 4 subtests passed) including the four ACD-2100d-2 test_spec descriptors newly authored by test-writer (criterion/reachability/real_artifact/boundary); all green, zero regressions in the surrounding build-guard suite. This independently confirms python-coder's and test-writer's prior finding that check_output_drift.py's scan/report logic and commit_guardian.json's trigger were already correct from concurrent EPIC-BuildPipelinePhantomRemediation work — no production file needed a fix in this phase, consistent with git diff showing only the new test file added.

### 2026-08-31 07:20 — test-writer (status: ok)

feedback-id: fb_2026-08-31_a1c3e9d2
completion_manifest:
  duplicate_dispatch_confirmed_no_op: true
  tests_still_present_and_valid: true
Re-dispatched for this ticket after `test-writer` was already `signed_off` at 2026-08-26 16:45. Per signoff-skill §1 ("`signed_off` → terminal under normal conditions"), no re-edit of the frontmatter row or the `## Sign-offs` checkbox was made — both remain as the original 16:45 entry. Verified `unit_tests/build_guards/test_acd_2100d_2.py` is unchanged, still staged (`git status --porcelain` shows `A  unit_tests/build_guards/test_acd_2100d_2.py`), and still contains all four test_spec descriptors (criterion/reachability/real_artifact/boundary) previously authored. Re-ran the suite fresh: `python3 -m pytest unit_tests/build_guards/test_acd_2100d_2.py -v` → `4 passed in 6.84s`, matching the prior report exactly — no drift, no new red baseline, nothing to author. Wrote no new test files. Treating this invocation as a no-op confirmation rather than a duplicate authoring pass, consistent with how `architect-review` handled its own re-dispatch on this ticket at 07:16.

### 2026-08-31 07:16 — architect-review (status: ok)

feedback-id: fb_2026-08-31_0489f704
completion_manifest:
  blast_radius_reviewed: true
  rubric_classification_recorded: true
  duplicate_dispatch_confirmed_no_op: true
Re-dispatched for this ticket after `architect-review` was already `signed_off` at 2026-08-26 16:06. Per signoff-skill §1 ("`signed_off` → terminal under normal conditions"), no re-edit of the frontmatter row or the `## Sign-offs` checkbox was made — both remain as the original 16:06 entry. Re-verified the SMALL classification still holds before returning: `git status --porcelain` on this worktree shows none of `scripts/build_helpers.py`, `scripts/commit_guardian/check_output_drift.py`, or `scripts/commit_guardian/commit_guardian.json` as modified (only the ticket file itself and the new `unit_tests/build_guards/test_acd_2100d_2.py` are staged), confirming python-coder's own report that no production edit occurred and that the original rubric analysis (4 files, single build-pipeline/commit_guardian surface, no always-large trigger, `requires_adr: false`) is unaffected by anything that has happened on this branch since. No new architectural concerns; no acceptance-criteria adjustments. Treating this invocation as a no-op confirmation rather than a duplicate sign-off.

### 2026-08-31 07:35 — python-coder (status: ok)

feedback-id: (submit-failed)
completion_manifest:
  duplicate_dispatch_confirmed_no_op: true
  production_code_matches_delivers_to_contract: true
  tests_green:
    result: true
    reason: "n/a — used for note only"
Re-dispatched for this ticket after `python-coder` was already `signed_off` at 2026-08-26 17:05. Per signoff-skill §1 ("`signed_off` → terminal under normal conditions"), no re-edit of the frontmatter row or the `## Sign-offs` checkbox was made — both remain as the original 17:05 entry. Independently re-verified before returning: `git diff --stat -- scripts/build_helpers.py scripts/commit_guardian/check_output_drift.py scripts/commit_guardian/commit_guardian.json` against the worktree HEAD produced no output (no changes), and `git status --porcelain` confirms only the ticket file and `unit_tests/build_guards/test_acd_2100d_2.py` are staged — no production file has moved since the 2026-08-26 17:05 sign-off. Re-ran `python3 -m pytest unit_tests/build_guards/test_acd_2100d_2.py -v` fresh in this session: all 4 tests (criterion/reachability/real_artifact/boundary) pass (`4 passed in 6.97s`), matching test-runner's 17:30 full-suite result and test-writer's 07:20 re-confirmation. Made no production edits — the `check_output_drift.py` scan/report logic, the installer-derived mapping in `build_helpers.py`, and the `always_run: true` trigger in `commit_guardian.json` all remain correct from the prior EPIC-BuildPipelinePhantomRemediation work, per architect-review's original 16:06 finding and its own 07:16 re-confirmation. Treating this invocation as a no-op confirmation rather than a duplicate implementation pass.

### 2026-08-31 07:45 — test-runner (status: ok)

feedback-id: fb_2026-08-31_2898078b
completion_manifest:
  duplicate_dispatch_confirmed_no_op: true
  test_suite_executed: true
  all_tests_passing: true
  failure_report_structured: true
Re-dispatched for this ticket after `test-runner` was already `signed_off` at 2026-08-26 17:30. Per signoff-skill §1 ("`signed_off` → terminal under normal conditions"), no re-edit of the frontmatter row or the `## Sign-offs` checkbox was made — both remain as the original 17:30 entry. Confirmed via `git diff --stat HEAD -- scripts/build_helpers.py scripts/commit_guardian/check_output_drift.py scripts/commit_guardian/commit_guardian.json` that no production file has moved since that sign-off (only `unit_tests/build_guards/test_acd_2100d_2.py` is new, +399 lines). Re-ran the full `unit_tests/build_guards` suite fresh (`python3 -m pytest unit_tests/build_guards -v`): **137 passed, 4 subtests passed in 67.08s**, including all four ACD-2100d-2 test_spec descriptors (criterion/reachability/real_artifact/boundary), matching the original 17:30 run with zero regressions in the surrounding build-guard suite. Treating this invocation as a no-op confirmation, consistent with how architect-review, test-writer, and python-coder handled their own re-dispatches on this ticket.

### 2026-08-31 08:00 — documentation-expert (status: ok)

feedback-id: fb_2026-08-31_81b70b4d
completion_manifest:
  doc_written: true
  cross_links_added: true
  diataxis_genre_correct: true
Read `docs/known-issues/commit-guardian.md`'s `KI-CG-034` — the exact defect this AC
targets (scanner/installer namespace mismatch + stale `files:` trigger, both confirmed
fixed by architect-review/python-coder/test-runner in prior comments on this ticket) — and
found it still recorded as `Status: open`, and found `docs/build-drift-hook.md` §2B's "Edge
cases — safe exits" table still describing the pre-fix behaviour (unmapped output files
silently skipped with an INFO line and exit 0) as current. Both were stale against the
verified production code (`always_run: true` trigger, `UNCOMPARABLE: EXEMPT`/`GAP`/`MISSING`/
`UNREADABLE` reporting per BP-100k-3/-4/-6). Updated both: `KI-CG-034`'s status and a dated
"Fix landed" section correcting the record (attributing the fix to `BP-100k-3`/`BP-100k-4`,
not to `ACD-2100d-2`, per this ticket's own honest no-op findings — the AC's contribution is
the locking-in test file), and `build-drift-hook.md`'s edge-case table plus new prose
explaining the current `UNCOMPARABLE`/`RESULT` reporting contract and the `always_run`
trigger rationale. Cross-linked each doc to the other and to
`unit_tests/build_guards/test_acd_2100d_2.py`. Verified both edits pass
`check_doc_frontmatter.py` (`exit: 0`) and confirmed via `git diff --stat` (2 files changed,
79 insertions, 6 deletions).

**AC-1 not addressed — target document does not exist.** The `### documentation-expert`
Agent Contracts checkbox names `docs/reference/plan-feature-layout-and-startup-checks.md`,
marked `status: planned` and owned by the separate record `ACD-2100d-4`; the file is not on
disk in this worktree. Left the checkbox unchecked rather than falsely flip it — nothing in
this AC's ownership boundary authors that page. The substance the checkbox describes (this
check reports the installed file by name) is instead documented directly in the two files
updated above, which are the actual location of that behaviour today. Once `ACD-2100d-4`
authors the reference page, it should cross-link back to `KI-CG-034` and
`docs/build-drift-hook.md` §2B rather than duplicating the reporting-contract narrative.

Also left the three other "existing docs to update / cross-link" entries
(`docs/architecture/adrs/ADR-001-self-hosting-boundary.md`, `docs/build-pipeline.md`,
`docs/architecture/components/build-orchestration.md`) untouched: neither ADR-001 nor
build-pipeline.md contained any factual inaccuracy this fix needed to correct, and
`build-orchestration.md` describes epic/ticket dispatch sequencing (a different component,
`build_orchestration`) rather than the build.py compile/deploy pipeline this AC concerns —
its relevance note appears to be a doc-links generation mismatch of the kind
`KI-ACD-002` describes, not a genuine target. Flagging both as open follow-ups rather than
forcing low-value edits.

### 2026-08-31 08:15 — pr-reviewer (status: blocker)

feedback-id: fb_2026-08-31_d11b3b7c
completion_manifest:
  review_executed: true
  contract_validation_performed: true
  high_confidence_findings_verified_by_hand: true
  tests_green:
    result: true
    reason: "n/a — used for note only"

Scoped the diff to this ticket's `files_touched` (`git diff --cached --stat` against
`origin/main`-merge-base confirms `unit_tests/build_guards/test_acd_2100d_2.py` is the
ONLY change under this ticket's four listed files — no production edit, consistent
with every prior sign-off on this ticket). Ran the suite fresh (`python3 -m pytest
unit_tests/build_guards/test_acd_2100d_2.py -v` → 4 passed) and independently
re-verified the behavioural substance: all three ACs (report+name, not-delivered,
installer round-trip) are genuinely exercised against the real `main()`/
`check_output_drift()` entry point and a real `build_agents()` re-install, matching
architect-review's/test-writer's/python-coder's/test-runner's repeated independent
confirmations — no phantom-done here.

Two concrete defects in the new test file, both fixable by a quick test-writer pass
(no production code involved):

[H-1] unit_tests/build_guards/test_acd_2100d_2.py:392 — ruff F541 (f-string without
placeholders) on `f"BLOCKED"` in
`TestGenerationOnlyDifferenceIsNotReported.test_a_generated_file_that_only_differs_by_generation_is_not_reported`.
Confirmed live: `ruff check unit_tests/build_guards/test_acd_2100d_2.py` exits 1 with
this one violation. Per this repo's CLAUDE.md ("any ruff violation is a merge
blocker" / ruff is a required CI gate), this will fail the required Lint (ruff) check
on the PR. Fix: drop the stray `f` prefix (`"BLOCKED"`).

[H-2] unit_tests/build_guards/test_acd_2100d_2.py:391-395 — vacuous assertion in the
same test. `self.assertNotIn(f"BLOCKED", combined.split(output_key)[0][-200:] if
output_key in combined else "", ...)` is dead: I traced `check_output_drift()` (see
`scripts/commit_guardian/check_output_drift.py:605-803`) and confirmed a "verified"
(clean-match) output key is NEVER printed anywhere in the hook's stdout/stderr — only
`GAP`/`EXEMPT`/`MISSING`/`UNREADABLE`/`BLOCKED` keys are. In this test's clean-run
case `output_key` therefore never appears in `combined`, so the ternary always
evaluates to `""` and `assertNotIn("BLOCKED", "")` can never fail regardless of
implementation behaviour. This is exactly the "test passes on dead code" pattern this
repo's own CLAUDE.md warns about (Gate/Workflow ACs section) — it does not invalidate
the test's real assertions (`returncode == 0`, `GAP {output_key}` absent), which do
correctly cover the boundary AC, but the extra assertion should be removed or replaced
with something that can actually fail (e.g. asserting `"BLOCKED"` is absent from
`combined` as a whole).

Everything else reviewed clean: no silent-except / bare-except introduced, no
signature changes needing call-site audit, no card/registry or dispatch-instruction
surfaces touched by this diff, and the real-artifact round-trip test (descriptor 3)
satisfies the durable-side-effect coverage lens (writes the repair marker to a real
file, re-runs the real installer, reads the artifact back — not mock-only topology).
Medium-confidence note (not blocking): the ticket's own "Delivers To" contract
promises ACD-2100d-2-i "a callable determination" per generated file; the only
per-file-granular result today is the private `_scan_output_files()` (leading
underscore), while the public `check_output_drift()` returns only an aggregate int.
ACD-2100d-2-i is still `status: todo`, so this is a forward-looking readiness note
for whoever picks that ticket up, not a defect in this one.

Per signoff §4 ("pr-reviewer after finding a high-confidence blocker"), marking this
phase `failed` rather than `signed_off`. Suggested remediation: respawn `test-writer`
to fix H-1 (one-line f-string prefix removal) and H-2 (remove or repair the vacuous
assertion) in `unit_tests/build_guards/test_acd_2100d_2.py`, re-confirm all 4 tests
and `ruff check` both pass, then re-request `pr-reviewer`.

### 2026-08-31 09:10 — commit (status: blocker)

feedback-id: fb_2026-08-31_eea25ef0
completion_manifest:
  pre_commit_hooks_pass:
    result: false
    reason: "Commit was never attempted. Two upstream sign-offs remain failed and
      unresolved: pr-reviewer (2026-08-31 08:15) found a live ruff F541 violation
      and a vacuous assertion at unit_tests/build_guards/test_acd_2100d_2.py:391-395,
      and documentation-verifier (2026-08-31 07:43) found the Agent Contracts AC-1
      line is not pipe-delimited. Both are still present verbatim on disk: `ruff
      check unit_tests/build_guards/test_acd_2100d_2.py` still exits 1 on the same
      f-string line, and the ticket's `### documentation-expert` AC-1 line under
      `## Agent Contracts` still reads
      `- [ ] AC-1: [reference-doc] docs/reference/plan-feature-layout-and-startup-checks.md — ...`
      with zero pipe delimiters."
    remediation: "Respawn test-writer to fix H-1/H-2 in the test file and re-request
      pr-reviewer; respawn documentation-expert (or hand-edit) to reformat the AC-1
      line to the pipe-delimited SSOT format and re-request documentation-verifier.
      Re-request commit only after both are signed_off."
  commit_message_valid:
    result: false
    reason: "No commit was drafted or attempted; nothing to validate."
    remediation: "N/A until the commit step is re-run."
  ticket_staged:
    result: false
    reason: "This sign-off's own edits are staged for the next commit attempt, but
      no commit object was created this invocation."
    remediation: "Included automatically in the next successful commit."

Ran the mandatory pre-commit hook probe first (BO-1700d-3): initially failed with
`git_hook: false` because the probe was invoked from a cwd outside this worktree
(`Path.cwd()` resolved to the parent workspace, which is not a git repo); re-ran with
`env --chdir=<worktree-root>` and got a clean pass (`failing_checks: []`) — recording
this as a tooling anomaly, not a real hook-install problem, so a future commit
invocation is not misled by the first (wrong-cwd) result.

Also found and repaired a workspace-integrity issue before touching the ticket: a
concurrent process had reset this ticket file's *working-tree* copy back to its
pristine pre-signoff state (all agents `needed`, all Sign-offs unchecked, empty
Comments) while the git *index* still held the fully signed-off content from every
prior phase. Editing the working-tree copy directly would have silently clobbered
the staged sign-off history on the next `git add`. Ran `git checkout --
<ticket_path>` to restore the working tree from the index before making any edit,
confirmed via `git status --short` (clean `M `, no unstaged residual) and a fresh
`Read`, then proceeded.

Scoped `git diff --cached --stat` to this ticket's four `files_touched` entries:
only `unit_tests/build_guards/test_acd_2100d_2.py` (+399, new) is staged — no
production file has moved, consistent with every prior phase's honest no-op
finding. Refusing to commit while pr-reviewer and documentation-verifier are both
`failed`: committing test code with a live ruff violation would break the required
Lint gate on the PR, and the doc-contract format defect would remain unresolved.
Suggested remediation: respawn `test-writer` for H-1/H-2, respawn
`documentation-expert` (or hand-edit) for the AC-1 pipe-format fix, re-request
`pr-reviewer` and `documentation-verifier`, then re-request `commit`.

### 2026-08-31 08:30 — ac-validator (status: ok)

feedback-id: (submit-failed)
completion_manifest:
  acs_parsed: 3
  store_alignment_checked: true
  cross_file_contract_traced: true

Parsed all 3 ACs from `## Acceptance Criteria`. Store-alignment check passed:
`python3 scripts/commit_guardian/check_v2_ac_store_alignment.py --ticket
tickets/00_inbox/epics/EPIC-StartingNewWorkTheProperWayAlways/20_TICKET-20260826-ACD-2100d-2.md`
exited 0, no output. Because `files_touched` shows no staged production diff for this
ticket (`git diff --cached --stat` confirms only `unit_tests/build_guards/test_acd_2100d_2.py`
is new under this ticket's four listed files, matching every prior sign-off's honest
no-op finding), implementation evidence was gathered by reading the current file
contents directly rather than a diff hunk, and independently confirmed against
`git log`: the fix commit `ab9e91c41` ("gates that could not check stop reporting
passes (BP-100k-3/-4/-5, ...)") is already an ancestor of `origin/main`
(`git merge-base --is-ancestor ab9e91c41 origin/main` → exit 0) — the behaviour this
AC requires predates and is independent of this ticket, per the ownership-boundary
anomaly architect-review flagged on 2026-08-26 and every subsequent sign-off
reconfirmed.

AC-1 covered:
  implementation: scripts/commit_guardian/check_output_drift.py:463-471 — the
    not-in-mapping branch prints `UNCOMPARABLE: EXEMPT {out_key} ground=...` or
    `UNCOMPARABLE: GAP {out_key} action=run build.py to register it` (names the file
    by its output_mappings key; never a silent skip).
  test: `test_installed_file_carrying_a_repair_its_source_lacks_is_reported`
    (unit_tests/build_guards/test_acd_2100d_2.py:138) — asserts the exact output key
    appears in combined stdout/stderr and `result.returncode == 1` (line ~171). Ran
    fresh: `4 passed in 8.31s`.

AC-2 covered:
  implementation: scripts/commit_guardian/check_output_drift.py:806 `main()` —
    computes and returns/exits the verdict (`sys.exit(main())` at line 926); a
    divergent working copy exits 1 (BLOCKED), never 0.
  test: `test_change_is_not_reported_as_delivered_while_the_divergence_stands`
    (unit_tests/build_guards/test_acd_2100d_2.py:199) — asserts `result.returncode`
    is non-zero (rejects clean/delivered) and the BLOCKED block names the key
    (assertIn at line 237); explicitly designed so a check that "examines files and
    checks none of them, exiting clean" fails this test.
  test: `test_running_the_installer_confirms_the_report_by_removing_the_repair`
    (unit_tests/build_guards/test_acd_2100d_2.py:271) — hand-edits a real deployed
    file, re-invokes the real installer (`build_agents()`), and re-reads the file off
    disk to confirm the repair is gone (`assertEqual` at line 317), confirming the
    report rather than contradicting it.

Cross-file contract trace (Step 2d): the ticket's `Delivers To` names `python-coder`/
`ACD-2100d-2-i` as consumer of "a callable determination... delivered as a callable
determination, not a shared edit surface." `ACD-2100d-2-i` is `status: todo` and no
consuming file exists yet in this worktree, so there is no live cross-file contract to
trace against a real reader today — recording this as a forward-looking anomaly, not a
blocker, consistent with pr-reviewer's own 2026-08-31 08:15 medium-confidence note that
`check_output_drift()` currently exposes only an aggregate int while the per-file
result lives in the private `_scan_output_files()`. Whoever picks up `ACD-2100d-2-i`
must confirm at that time that the "callable determination" the contract promises is
actually exposed as a public per-file callable, not merely as a side-effect of the
CLI's stdout lines.

All 3 ACs covered. Flipped the `- [ ]` checkboxes for AC-1/AC-2/AC-3 to `- [x]` and
added `ac_coverage: 3/3` to frontmatter (no `## AC Coverage` table exists in this
ticket body to fill, consistent with test-writer's 2026-08-26 16:45 note that this
ticket predates that convention).

**Anomaly — not this phase's gate, flagging for the commit phase:** `pr-reviewer`
signed off `failed` at 2026-08-31 08:15 with a live H-1 finding (ruff F541 on line 392
of the new test file) that I independently re-confirmed still fails: `ruff check
unit_tests/build_guards/test_acd_2100d_2.py` exits 1 on the same line. AC coverage
(this phase's scope) is complete and correct, but the ticket is not commit-ready until
H-1 (and H-2, the vacuous `assertNotIn` on the same line range) are fixed by a
test-writer pass, per pr-reviewer's own suggested remediation.

### 2026-08-31 07:40 — ac-fulfillment-gate (status: ok)

feedback-id: (submit-failed)
completion_manifest:
  resolved_acs_non_empty: true
  work_status_verified: true
  implemented_by_verified: true
  covered_by_verified: true
  schema_valid_after_edit: true

Ran `.leafcutter/scripts/ac_store/ac_coverage_resolver.py --ticket` against this
ticket's two-key `ac_traceability` block: resolved 1 AC (`ACD-2100d-2`), which
`resolved_via: traceability_block`. Initial run reported `ok: false` — `work_status`
was `todo` on the AC YAML while every phase agent above (architect-review,
test-writer, python-coder, test-runner, ac-validator) independently confirmed the
behaviour is already satisfied by prior `EPIC-BuildPipelinePhantomRemediation` work
(commit `ab9e91c41`, confirmed an ancestor of `origin/main`) and locked in by the new,
behaviorally-real `unit_tests/build_guards/test_acd_2100d_2.py` (4/4 passing, verified
myself via a fresh `python3 -m pytest` run). `git diff --stat HEAD` confirmed
`unit_tests/build_guards/test_acd_2100d_2.py` (+399, new) is the ONLY file among this
ticket's four `files_touched` entries with any change — `scripts/build_helpers.py`,
`scripts/commit_guardian/check_output_drift.py`, and
`scripts/commit_guardian/commit_guardian.json` are byte-identical to HEAD, matching
every prior sign-off's honest no-op finding.

Auto-fixed `docs/acceptance-criteria/ac-driven-dev/ACD-2100-entry-point-unblocked/ACD-2100d-2.yaml`
append-only, per Step 3: (1) `work_status: todo` → `done`, licensed by
`files_touched ∩ diff` being non-empty (the test file); (2) appended
`unit_tests/build_guards/test_acd_2100d_2.py` to `implemented_by` (previously listed
only the ticket `.md` path, which is not in `files_touched`); (3) appended the same
path to `covered_by` (previously `[ACD-2100d-2-i]` only, an AC id, not a test path) —
licensed by four live `# covers: ACD-2100d-2` tags found via `grep -rn` in the test
file itself. No existing entries were removed. Ran
`scripts/commit_guardian/check_ac_schema.py` on the edited YAML: exit 0 (falls back to
manual field validation — `config/ac_store_schema.json` absent in this worktree, per
the script's own warning). Re-ran the resolver: `ok: true`, `failures: []`. This
phase's scope (AC store field accuracy) is satisfied; it does not adjudicate
pr-reviewer's separate ruff/vacuous-assertion findings (H-1/H-2) noted in
ac-validator's comment above — those remain open for the commit phase.
`submit_feedback.py` rejected `ac-fulfillment-gate` as an unregistered writer for
every category tried (`complete`), consistent with the concurrent `(submit-failed)`
seen on `ac-validator`'s own comment above; recorded as `(submit-failed)` per §2a
step 4 rather than aborting sign-off.

### 2026-08-31 07:43 — documentation-verifier (status: blocker)

feedback-id: fb_2026-08-31_90b25f50
completion_manifest:
  required_docs_list_parsed:
    result: false
    reason: "The ## Agent Contracts -> ### documentation-expert block's only AC line
      (AC-1, ticket line 182: '- [ ] AC-1: [reference-doc]
      docs/reference/plan-feature-layout-and-startup-checks.md — it reports the
      installed file as carrying behaviour its source does not and') contains no
      pipe ('|') delimiters at all, so the required target_path field cannot be
      parsed as the second pipe-delimited field per the SSOT format
      '- [ ] AC-N: <genre> | <target_path> | <content_constraint>'. Confirmed via
      grep -o '|' against the line: zero matches. This is a fail-closed parse
      failure per the documentation-verifier template's Step 2 (malformed-line
      branch), not a judgment call — an exception or ambiguous parse result must
      never produce status: ok."
    remediation: "Respawn documentation-expert (or hand-edit the ticket body) to
      rewrite the AC-1 line under ### documentation-expert into the pipe-delimited
      SSOT format, e.g. '- [ ] AC-1: reference-doc |
      docs/reference/plan-feature-layout-and-startup-checks.md | it reports the
      installed file as carrying behaviour its source does not'. Note
      documentation-expert's own 2026-08-31 08:00 comment already states that
      target file does not exist in this worktree and is owned by the separate
      record ACD-2100d-4 (status: planned) — once the line is reformatted, the
      genuine open question of whether this AC-1 checkbox can ever be satisfied by
      this ticket (versus being retargeted or dropped) still needs a human or
      architect-review decision; reformatting the pipe syntax alone does not
      resolve that ownership question."
  all_required_docs_present_in_diff:
    result: false
    reason: "Not evaluated — parsing failed at Step 2 before a required_docs list
      could be built, so Steps 3-6 (worktree root, git diff, coverage assertion,
      placeholder scan) were never reached."
    remediation: "Re-run documentation-verifier after the Agent Contracts line
      above is reformatted to the pipe-delimited SSOT format."
  no_placeholder_content_in_changed_docs:
    result: false
    reason: "Not evaluated — parsing failed at Step 2 before any required doc path
      was known, so no placeholder scan could run."
    remediation: "Re-run documentation-verifier after the Agent Contracts line
      above is reformatted to the pipe-delimited SSOT format."
Agent Contracts line is malformed (no pipe-delimited target_path):
  - [ ] AC-1: [reference-doc] docs/reference/plan-feature-layout-and-startup-checks.md — it reports the installed file as carrying behaviour its source does not and
Expected format: - [ ] AC-N: <genre> | <target_path> | <content_constraint>
Per the fail-closed posture (a parse exception or ambiguous result MUST NOT
produce status: ok), this phase cannot proceed to Steps 3-6 and marks itself
`failed`. Suggested remediation: respawn documentation-expert (or a human editor)
to rewrite the AC-1 line into the pipe-delimited genre | target_path |
content_constraint format the SSOT block requires, then re-request
documentation-verifier.
