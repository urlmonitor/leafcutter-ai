---
title: "Six defects found by driving the GE-120 epic, plus a pre-drive check that named a path that never existed"
date: "2026-08-25"
time: "21:15"
type: manual
components: 
  - build_orchestration
  - build_pipeline
  - feedback_collector
summary: "Files six known issues observed while /build-feature drove EPIC-TrustThatAGreenCheckActuallyChecked, opens a feedback-collector register, and corrects the CLAUDE.md pre-drive feedback check, which pointed at a path that does not exist in any layout."
description: "Documentation and one CLAUDE.md correction — no code changes. All six defects were observed against a live epic drive rather than read out of source, and each entry states whether it was reproduced or inferred. KI-BO-025 (high): the /build-feature planner scheduled 17 of 37 tickets, and the 20 it dropped are exactly the 20 carrying any depends_on — verified by set comparison, with zero dependency-carrying tickets planned. Its prompt asks for the maximal antichain of tickets whose depends_on is met, which at plan time means only the dependency-free ones, and the planner agent() call sits outside the batch loop so there is no second wave. An epic N levels deep needs N manual invocations and nothing says so. KI-BO-026 (medium): the completion guard does catch that shortfall and correctly withholds the complete verdict, but files the 20 under discovered_after_planning with the text 'added to the epic after the plan for this drive was fixed' — false, since all 20 predated the drive. The comparison cannot distinguish never-selected from added-mid-drive and asserts the latter, sending a reader after a change that never happened. KI-BO-027 (medium): target resolution returned the epic's ticket folder inside the main checkout as worktree_path; a later step created a real worktree and the drive used that, so no damage, but the value was wrong, the schema accepted it because it constrains only to a string, and had the second step reused the first's answer every agent would have been pointed at the user's main checkout — the KI-ACD-007 shape. KI-BP-016 (high): build.py's doc-index phase applies docs_root when computing the write path but passes target_root straight to generate_index, which hardcodes <root>/docs and never consults docs_root; in the self-hosting layout it therefore scans a five-entry stub and writes 'No docs found.' over the repo's 221-line index, deleting 172 rows and exiting 0. Reproduced directly by running the generator against each root: 0 versus 9 occurrences of the placeholder. This also corrects an earlier misattribution in this workspace, where a dirty docs/INDEX.md was blamed on a concurrent agent. KI-BP-017 (high): scripts/feedback/ is a gitignored build output that install_shims realizes at the project root only, and setup_ticket_worktree.py contains no reference to it, so the CWD-relative call the signoff skill and six agent templates prescribe crashes with Errno 2 inside any worktree and each affected phase records (submit-failed). Caught live from the running drive's own stderr and reproduced independently. It is the deployed-dependency-closure rule of BP-900g-8-ii broken for an executable rather than a config file. A new docs/known-issues/feedback-collector.md opens with KI-FC-001 (medium): submit_feedback.py resolves its default sink from __file__ while callers pass a CWD-relative --jsonl, and in a worktree .resolve() follows the symlink chain out and the ancestor walk stops at a .claude/ inside .leafcutter/, so one drive splits its feedback across two corpora and whichever the retrospective reads under-reports. An investigation initially concluded one id had never been persisted; it had, to the second sink, and that error is recorded in the entry. KI-FC-002 (medium, latent): the sidecar id-recovery fallback is keyed on whole seconds and every agent shares one stderr file, so under parallel dispatch an agent can read another agent's id — a wrong id rather than a missing one. Two entries in this drive shared a timestamp and only one sidecar survived. Finally CLAUDE.md's pre-drive check told readers to run 'ls <worktree-root>/.leafcutter/feedback_categories.yaml'; the file is deployed at .leafcutter/config/feedback_categories.yaml, so the documented check reported a missing file on every worktree including correctly-provisioned ones, making a real absence indistinguishable from the check's own error. Corrected, with the prior wrong path recorded inline so the correction is auditable. The GE-120 epic drive was still running when these were filed; none of its work is included here."
breaking: false
---

## Entry
