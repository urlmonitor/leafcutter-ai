id: BP-1400b-1
components:
  - build_pipeline
title: "A blocking style check runs on every web-app pull request and fails on any style-rule violation"
component: build-pipeline
level: L2
status: active
req_status: draft
work_status: todo
readiness: draft
priority: medium
roadmap_phase: phase_1
criteria: |
  Given the continuous-integration configuration for the repository,
  When a pull request is opened or updated with one or more changed files under
    the web-app directory (leafcutter-web/),
  Then a dedicated web-app style check runs automatically over the web-app code,
  And the check reports a failing (non-passing, blocking) result whenever a
    changed web-app file violates a style rule,
  And the check reports a passing result only when the web-app code satisfies
    every style rule,
  And the check is a hard, blocking check — not an advisory or informational one
    that a pull request can be merged despite.
depends_on:
  - BP-1400b
doc_links:
  - path: docs/acceptance-criteria/build_pipeline/PROJECT_CONTEXT.md
    relationship: describes
    status: exists
  - path: .github/workflows/ci.yml
    relationship: implements
    status: exists
assigned_agent: python-coder
estimated_complexity: M
delivers_to:
  ac_id: BP-1400a-4
  contract: "A named, blocking web-app style result reported on the pull request that the merge gate requires"
expects_from: null
origin_agent: business-analyst
created: 2026-07-21
amended_by: []
superseded_by: null
covered_by:
  - BP-1400b-1-i
implemented_by: []
notes: |
  A NEW blocking style check on the web-app pull-request flow, distinct from the
  build/type-check check (BP-1400a-1). Motivated by the real incident where a
  web-app style violation slipped through every check and shipped to production;
  the concrete regression class is anchored in BP-1400b-1-i.
change_target: pipeline
risk_surface: safety
