---
ac_traceability:
  id: ACD-2100d-2-i
  path: docs/acceptance-criteria/ac-driven-dev/ACD-2100-entry-point-unblocked/ACD-2100d-2-i.yaml
advances_current_outcome: true
agents:
  ac-fulfillment-gate: needed
  ac-validator: needed
  architect-review: needed
  commit: needed
  documentation-expert: needed
  documentation-verifier: needed
  pr-reviewer: needed
  pull-request: needed
  python-coder: needed
  test-runner: needed
  test-writer: needed
change_target: pipeline
complexity: low
components:
- ac_driven_dev
- build_pipeline
created: '2026-08-26'
depends_on:
- 20_TICKET-20260826-ACD-2100d-2.md
documentation_required: true
files_touched:
- scripts/build.py
- scripts/build_phases.py
- unit_tests/build_guards/test_acd_2100d_2_i.py
priority: medium
requires_adr: false
requires_diagram: false
risk_surface: contract_boundary
roadmap_phase: phase_1
source_ac: ACD-2100d-2-i
status: todo
title: An install that replaces a locally changed generated file says so rather than
  replacing it silently
---

# An install that replaces a locally changed generated file says so rather than replacing it silently

## Actor / Goal

As the leafcutter-ai system, I want to implement AC `ACD-2100d-2-i` — An install that replaces a locally changed generated file says so rather than replacing it silently — so that the acceptance criterion is satisfied.

## Context

This ticket was generated from AC store entry `ACD-2100d-2-i`. Component: `ac-driven-dev`. Assigned agent: `python-coder`. Estimated complexity: `S`. Complexity: `low`.

## Acceptance Criteria

```gherkin
Given an installed route file that has been changed since it was installed so
  that its contents differ from what the installer would produce,
When the installer runs,
Then it states in its output for that run that a local change is being replaced
  and names the file,
And the file's contents after the install are what the installer produces,
And a file that has not been changed since it was installed produces no such
  statement.
```

- [ ] AC-1: it states in its output for that run that a local change is being replaced
- [ ] AC-2: names the file
- [ ] AC-3: the file's contents after the install are what the installer produces
- [ ] AC-4: a file that has not been changed since it was installed produces no such

## Test Requirements

```yaml
tests:
- name: test_install_announces_a_locally_changed_generated_file_by_name
  file: unit_tests/build_guards/test_acd_2100d_2_i.py
  covers:
  - ACD-2100d-2-i
  asserts: The installer is run into a temporary target, one generated file is changed,
    and the installer is run again; its output for the second run states that a local
    change is being replaced and names exactly that file.
  framework: unittest
  type: integration
  angle: criterion
- name: test_unchanged_generated_files_produce_no_announcement
  file: unit_tests/build_guards/test_acd_2100d_2_i.py
  covers:
  - ACD-2100d-2-i
  asserts: Across the same two runs, the statement appears exactly once and names
    only the changed file -- no statement is produced for any file that was not changed.
  framework: unittest
  type: integration
  angle: boundary
- name: test_installed_contents_after_the_announced_replacement_are_the_installers_own
  file: unit_tests/build_guards/test_acd_2100d_2_i.py
  covers:
  - ACD-2100d-2-i
  asserts: After the second run the file's bytes equal what the installer produces
    for a clean install of that file -- the announcement does not come with a preservation
    or a merge.
  framework: unittest
  type: integration
  angle: real_artifact
- name: test_announcement_is_produced_by_running_the_real_installer
  file: unit_tests/build_guards/test_acd_2100d_2_i.py
  covers:
  - ACD-2100d-2-i
  asserts: The statement is observed in the real installer's captured output in a
    fresh process, not from a helper called directly.
  framework: unittest
  type: integration
  angle: reachability
```

## Implementation Notes

```yaml
- The install still wins. This does not make the installer refuse, preserve the local
  change, or prompt -- build output is generated and must be regenerated. The requirement
  is that the loss is announced, not prevented.
- The statement must fire only for files that actually diverged. Every generated file
  is replaced on every install; a message for each one is noise that gets filtered
  and then ignored, which is the same as no message.
- Divergence must be determined by the same computation ACD-2100d-2's delivery check
  uses. Two independent determinations will disagree, and a disagreement here means
  the check reports clean while the installer silently discards a repair -- the 2026-08-18
  incident exactly.
- The statement must name the file. A count of replaced files does not let the operator
  recover the change they are about to lose.
- The statement must appear in the installer's own output for that run, on a stream
  an operator running the installer normally sees -- not only in a log file that has
  to be sought out.
- 'Must satisfy the repository error-handling policy: reading the pre-install contents
  is external I/O and a failure to read must not silently suppress the statement.'
- Must not measurably slow a clean install -- the common case is that nothing diverged.
- 'THE INSTALLER HAS FOUR SEPARATE COMPARE-BEFORE-WRITE BRANCHES AND THE ANNOUNCEMENT
  MUST FIRE FROM EVERY ONE OF THEM. They are write_file in scripts/build.py (text,
  equality compare); _write in scripts/build_phases.py (text, equality compare); _files_content_identical
  in scripts/build_phases.py, used ahead of shutil.copy2 at roughly fourteen call sites
  (binary, SHA-256); and the workflow-script phase in scripts/build_phases.py, which
  performs its own inline SHA-256 comparison and does NOT route through _files_content_identical.
  That fourth branch is load-bearing for this epic: it is the path the route''s own
  deployed copy takes, so an implementation that instruments only the shared text helper
  is silent for exactly the file the epic is about while passing any test written against
  the text path.'
- CONSUME ACD-2100d-2's DETERMINATION, DO NOT AUTHOR ONE, AND DO NOT EDIT THE FILES
  THAT COMPUTE IT. The installer-derived mapping and the per-file divergence verdict
  are ACD-2100d-2's deliverable and arrive here through the expects_from contract.
  This record's surface is the write paths where the announcement is emitted; the determination's
  own surface belongs to the parent and is deliberately absent from this record's files_touched.
  If implementing this reveals that the determination needs to change, that is a change
  to ACD-2100d-2, not an extension of this record's scope.
- A COMPARE-BEFORE-WRITE BRANCH ALONE CANNOT SATISFY THE THIRD THEN CLAUSE, WHICH IS
  WHY THE SHARED DETERMINATION IS NOT OPTIONAL. Each branch knows only that what is
  on disk differs from what is about to be written -- which is equally true after an
  ordinary source change with no local edit at all. Announcing on that signal alone
  would fire on every legitimate template change and would be filtered within a week,
  which is the failure the second requirement above forbids. The parent's determination
  is what distinguishes the two cases.
```

## Agent Contracts

### documentation-expert

Existing docs to update / cross-link:

- docs/reference/plan-feature-layout-and-startup-checks.md (relationship: describes | status: planned | relevance: The reference page authored by ACD-2100d-4 must tell a reader that a local edit to a generated file is announced and then lost.)
- docs/known-issues/ac-driven-dev.md (relationship: describes | status: exists | relevance: KI-ACD-004's dead workaround -- the evaporation was silent, which is why it was mistaken for a fix.)
- docs/build-pipeline.md (relationship: describes | status: exists | relevance: The installer whose output carries the statement.)
- docs/architecture/adrs/ADR-001-self-hosting-boundary.md (relationship: describes | status: exists | relevance: Why build output is regenerated and why the loss is correct even though it must be announced.)

- [ ] AC-1: (unspecified genre) | docs/reference/plan-feature-layout-and-startup-checks.md | it states in its output for that run that a local change is being replaced

### Expects From

- **AC:** ACD-2100d-2
- **Contract:** An installer-derived source-to-installed mapping plus a per-file divergence determination -- the same computation, so the delivery check and the install-time announcement cannot disagree about which files diverged. Consumed as a callable determination; this record does not modify the files that compute it.

## Sign-offs

- [ ] architect-review
- [ ] test-writer
- [ ] python-coder
- [ ] test-runner
- [ ] documentation-expert
- [ ] pr-reviewer
- [ ] ac-validator
- [ ] ac-fulfillment-gate
- [ ] documentation-verifier
- [ ] commit
- [ ] pull-request

## Comments
